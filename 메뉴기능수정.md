# 메뉴 기능 점검·수정 + 테스트 데이터 투입 작업 계획

> 작성일: 2026-05-18 · 작성자: 강수 (Kang-Soo.Cho@kr.ey.com)
> 목적: 2차구현계획.md에 따라 새 메뉴 IA가 들어간 현재 상태에서 **각 메뉴를 클릭했을 때 실제로 데이터가 보이고 기능이 동작**하도록 점검하고, 데이터가 없는 메뉴는 **테스트(시드) 데이터를 투입**해 모든 메뉴가 빈 화면 없이 흐름을 마칠 수 있게 한다.

---

## v1.5 동기화 메모

현재 점검 기준은 24개 통합 메뉴가 아니라 `admin/tenant:list`를 포함한 **100개 active leaf**다. 각 leaf는 공통 workbench 5블록(상단 KPI/제목, 검색/필터, 목록 표, 행 액션, `+ 추가`)을 표시해야 하며, 사용자 추가/수정/삭제 행은 `leaf_records`에 저장되어 즉시 표에 반영된다. 따라서 빈 화면/placeholder/“기능 실행” 단일 버튼은 점검 실패로 본다.

## 1. 현황 요약 (점검 출발점)

### 1.1 코드 현황
- `frontend/index.html` + `frontend/app.js` + `frontend/app/{api,context,router,menu,screens}.js`는 **prototype IA 24개 메뉴**의 렌더 함수를 모두 가지고 있다(`frontend/app/screens.js`의 `routes` 맵과 `renderScreen` switch).
- `src/api.rs`에는 화면이 호출하는 라우트가 전부 등록되어 있다 — `/admin/menus`, `/validation/run`, `/validation/issues/:id/dismiss`, `/workflow/queue`, `/reports/reserve-trend`, `/business-years/:id/unlock`, `/notifications/:id` PATCH 포함.
- `migrations/20260518000100_phase2_menu_validation.sql`이 `menu_nodes`와 `validation_rules`(50+ 규칙)을 시드한다.
- `migrations/20260514000100_init.sql`·`…000300_seed_law_2021_2026.sql`·`…000400_form_versioning_admin.sql`은 **법령/세율/한도/서식 메타**까지 시드한다.

### 1.2 비어 있는 영역(메뉴 클릭 시 빈 화면이 나오는 원인)
1. **테넌트/사용자 없음** — `tenants`, `users`, `user_roles`에 시드가 없다. 따라서 `tenant=demo, login=admin`으로 로그인 자체가 실패한다.
2. **고객사/사업연도 없음** — `tenant_*` 스키마는 테넌트 생성 시 동적으로 만들어지므로 시드 마이그레이션으로 채우기 어렵고, 현재 자동 채움 경로가 없다.
3. **세무자료 없음** — `financial_statements`, `assets`, `transactions`, `vehicle_usage_logs`, `account_mappings`, `import_batches` 모두 빈 상태.
4. **세무조정 결과 없음** — `adjustments`, `reserves` 빈 상태.
5. **서식 결과 없음** — `forms_generated`/`form_attachments` 빈 상태(별지3호 미리보기·PDF 모두 placeholder).
6. **검증 이슈 없음** — `validation_issues` 비어 있음(룰셋은 있으나 한 번도 실행 안 됨).
7. **결재/워크플로 큐 없음** — `workflow_events` 비어 있고 `workflow/queue?assignee=me`도 빈 배열.
8. **전자신고 이력 없음** — `efilings` 비어 있고 history 화면이 비어 보임.
9. **알림/감사로그 없음** — `notifications`, `audit_logs` 신규 운영 환경에서 비어 있음.
10. **보고서 비어 있음** — `reports/{tax-burden, year-comparison, reserve-trend}` 모두 사업연도 데이터 의존.

요컨대 “DB는 비어 있고 마이그레이션이 채워주는 건 메타뿐”이므로, **운영 컨텍스트(테넌트→고객사→사업연도→세무자료→…→전자신고)** 를 만들어주는 시드가 필요하다.

---

## 2. 작업 원칙

1. **메뉴 → 호출 API → 빈 응답 → 시드 투입**의 4단계 체크리스트를 24개 메뉴 전부에 대해 수행한다.
2. 시드 데이터는 **마이그레이션이 아니라 별도 “데모 시더”** 로 분리한다. 운영 DB에는 자동 주입되지 않도록 명시적 호출 필요.
   - 옵션 A(권장): `cargo run --bin seed-demo` 또는 `cargo run -p cit-system --example seed_demo` 형태의 별도 바이너리.
   - 옵션 B: REST 엔드포인트 `POST /api/operations/seed-demo`(개발 환경에서만 활성).
   - 옵션 C: 통합 테스트(`tests/integration_flow.rs`)의 fixture를 추출해 `src/bin/seed_demo.rs`로 재사용.
3. 시드는 **idempotent** 해야 한다(이미 demo 테넌트가 있으면 ON CONFLICT 또는 사전 조회 후 skip).
4. 시드 1회로 24개 메뉴가 **빈 화면 없이** 표시되어야 한다.
5. 화면 자체의 기능(버튼·폼·검증 결과)도 함께 점검해 빈 응답이 아닌 **실패/타입 불일치** 케이스도 잡는다.

---

## 3. 메뉴별 점검표 + 필요한 시드 데이터

> 표 컬럼: `메뉴키 / 호출 API / 빈 응답 시 화면 증상 / 필요한 시드 데이터 / 점검 SQL or curl`. 24개 메뉴를 5개 그룹으로 나눠 기재한다.

### 3.1 ① 대시보드

| 메뉴 | API | 증상 | 시드 |
|---|---|---|---|
| `dashboard` | `GET …/dashboard`, `…/notifications`, `…/workflow/queue?assignee=me`, `…/audit-logs` | 4개 KPI 0, 결재함·알림·감사로그 빈 표 | 사업연도 1건 이상 + 알림 2건 + 감사로그 5건 |

**점검**: `curl -H "Authorization: Bearer $T" $BASE/api/tenants/demo/dashboard`가 `{business_year_count, filed_count, pending_review_count, due_soon_count}`을 0이 아닌 값으로 반환.

### 3.2 ② 신고 작업 (컨텍스트 필요)

| 메뉴 | API | 증상 | 시드 |
|---|---|---|---|
| `ws-start` | `…/customers`, `…/business-years` | 고객사·연도 표 비어 있어 진입 불가 | **고객사 3건**(㈜OOO, △△사, □□㈜) + **사업연도 5건**(2024 FILED, 2025 APPROVED/IN_REVIEW/DRAFT, 2026 DRAFT) + 각 연도 `snapshot`(law_version_id 매핑) |
| `ws-info` | `…/tax-data/validation`, `…/tax-data/financial-statements`, `…/tax-data/assets`, `…/tax-data/transactions`, `…/vehicle-usage-logs`, `…/tax-data/import-batches` | 카드/표 전부 빈값, 검증=불일치 | **재무제표 30라인**(차변=대변 일치), **자산 8건**, **거래 10건**, **차량운행기록 3건**, **계정매핑 7건**, **import batches 2건** |
| `ws-adj` | `…/adjustments`, `…/reserves`, `POST …/adjustments/*` | 17개 카드는 보이나 “조정 결과”·“유보 잔액” 빈 표 | 사업연도별로 B-1·B-3·B-4·B-12 결과 4건 + 유보 2건. 나머지는 “실행” 버튼으로 채울 수 있게 그대로 둠 |
| `ws-form` | `…/forms/attachments`, `…/forms/FORM3/preview`, `POST …/forms/:code`, PDF | “별지/부속서류” 빈 표, FORM3 “생성 전입니다.” | FORM3·FORM15·FORM22·FORM32·FORM50 각각 `forms_generated` 1행 + 부속서류 6건 |
| `ws-val` | `…/validation/rules`, `…/tax-data/validation`, `…/efilings/precheck`, `POST …/validation/run` | 룰 표는 보이나 실행 결과·이슈 비어 있음 | `POST /validation/run` 한 번 호출하여 `validation_issues` 8건 생성 |
| `ws-appr` | `…/workflow/queue`, `…/workflow`, `POST …/status` | 결재함·이력 비어 있음 | `workflow_events` 3행(REQUESTED·APPROVED·FILED 데모) + 결재대기 1행 |
| `ws-print` | `…/forms/attachments`, PDF bundle | 다운로드 가능하지만 표가 비면 “데이터 없음” | `forms_generated` 시드와 동일 → ws-form과 공유 |
| `ws-file` | `…/efilings/format-spec`, `…/efilings/precheck`, `…/efilings`, `POST …/efilings` | 사전검증은 메타로 채워지나 생성 이력 0 | `efilings` 1행(2024 FILED용) + `efile_format_spec`은 기존 시드 사용 |

### 3.3 ③ 사후 관리

| 메뉴 | API | 증상 | 시드 |
|---|---|---|---|
| `post-hist` | `…/business-years`, `…/efilings` | 사업연도 표는 채워지나 전자신고 이력 비어 있음 | `efilings` 시드(ws-file과 동일) |
| `post-amend` | `…/amendment-preview`, `POST …/unlock` | 차이 표 비어 있고 잠금해제 폼만 노출 | 2024 FILED 사업연도를 `locked_at` 설정 + `amendment_preview` 응답에 차이 1건이 표시되도록 “원 신고 vs 현재” 데이터 한 쌍 시드 |

### 3.4 ④ 분석/보고서

| 메뉴 | API | 증상 | 시드 |
|---|---|---|---|
| `rp-alerts` | `…/notifications`, `PATCH …/notifications/:id` | 알림 표 비어 있음 | `notifications` 4건(WARN 2 / INFO 2) |
| `rp-compare` | `…/reports/year-comparison` | 미니 차트·표 비어 있음 | 사업연도 2건 이상 + 각 사업연도 `adjustments` 합계가 0이 아니어야 함 |
| `rp-burden` | `…/reports/tax-burden` | 차트·표 비어 있음 | 사업연도별 `forms_generated` FORM3에 `taxable_income/total_tax_due` 채움 |
| `rp-reserve` | `…/reports/reserve-trend` | 차트·표 비어 있음 | `reserves` 3건 이상 |

### 3.5 ⑤ 관리

| 메뉴 | API | 증상 | 시드 |
|---|---|---|---|
| `ad-tenant` | `/api/tenants` (전역) | demo 외 테넌트 없으면 표 1행 | demo + sample-firm 2테넌트 |
| `ad-cust` | `…/customers` | 고객사 빈 표 | ws-start와 공유(3건) |
| `ad-user-list` | `/api/admin/tenants/demo/users` | 사용자 빈 표(=로그인 불가의 원인) | admin / writer01 / reviewer01 / tax01 4명 |
| `ad-role` | `/api/admin/roles`, `/api/admin/role-permissions` | 마이그레이션으로 채워짐 OK | 추가 시드 불필요 |
| `ad-menu-fn` | `/api/admin/menus` | menu_nodes 비어 있으면 빈 표 | menu_nodes 24행 시드(이미 마이그레이션에 있는지 확인 필요. 없으면 추가) |
| `ad-cacc` | `…/users` + `…/customers` | 사용자가 없으면 빈 표 | user_customer_access 4행 |
| `ad-law` | `/api/tax-laws`, `/api/law-versioning/summary`, `/api/tax-rates`, `/api/tax-limits` | 마이그레이션 시드 존재 OK | 추가 불필요 |
| `ad-form` | `/api/form-versioning/forms`, `…/versions`, `…/relationships` | 마이그레이션 시드 존재 OK | 추가 불필요 |
| `ad-audit` | `…/audit-logs` | 빈 표 | 시드 단계의 INSERT가 자동으로 `audit_logs`에 트리거되도록 보장(또는 시더 마지막에 5건 명시적 추가) |

---

## 4. “메뉴 작동” 점검 자동화

### 4.1 백엔드 스모크(추가) — `tests/menu_smoke.rs`
- 시드 시더 호출 → 로그인 → 24개 메뉴 각각의 1차 GET API 호출 → 200 OK & 빈 배열 아님(또는 빈이 정상인 메뉴는 화이트리스트).
- 화이트리스트 예시: `ws-info`의 `import-batches`는 비어도 OK(업로드해야 채워짐).

```text
seed → login → for each menu in {dashboard, ws-start, ws-info, ...}:
    assert response.status == 200
    assert response.body_meets(menu.expectations)
```

### 4.2 프론트엔드 수동 점검표 — `docs/menu_smoke_checklist.md`
- 사용자가 클릭할 순서: 대시보드 → 작업 시작 → 1~7단계 → 사후관리 2탭 → 보고서 4탭 → 관리 9탭.
- 각 단계에서 “표가 1행 이상 / 카드 KPI가 0이 아님 / 버튼 클릭 후 상태 변화” 3축 체크.

### 4.3 e2e 시나리오 — `frontend/tests/smoke.spec.js` (Playwright, 선택)
- 로그인 → 대시보드 KPI 확인 → 작업 시작 → 1단계 진입 → 검증 실행 → 결재 승인 → 출력 ZIP → 전자신고 생성 → 사후관리 진입.

---

## 5. 시드 데이터(데모 시더) 설계

### 5.1 산출물
| 경로 | 역할 |
|---|---|
| `src/bin/seed_demo.rs` | 단독 실행 시더(권장). `cargo run --bin seed-demo -- --tenant demo --reset` |
| `src/seed.rs` | 시더 라이브러리(트랜잭션 단위 함수). 통합 테스트와 코드 공유 |
| `scripts/seed-demo.sh` (옵션) | docker compose 환경에서 호출 래퍼 |
| `migrations/2026052X000100_demo_tenant_bootstrap.sql` | **개발 전용** 가드된 INSERT (테넌트=demo이 비어 있고 ENV `CIT_BOOTSTRAP_DEMO=true`일 때만). 운영 배포에서는 환경 변수 미설정으로 자동 무력화. |

### 5.2 시드 트랜잭션 순서 (idempotent)
1. `tenants` upsert: `code='demo'`, schema 생성(`tenant::create_tenant` 재사용).
2. `users` upsert: `admin / writer01 / reviewer01 / tax01`(`pwd_hash = argon2(env CIT_DEMO_ADMIN_PASSWORD or "ChangeMe123!")`). 모두 `use_2fa=false`로 시작.
3. `user_roles` 부여: SUPER_ADMIN→admin, TAX_REVIEWER→reviewer01, TAX_WRITER→writer01, TAX_EXPERT→tax01.
4. `tenant_demo.customers` upsert: 3개사(`CUST01` ㈜OOO, `CUST02` △△사, `CUST03` □□㈜) + `work_scopes` 8개 전부.
5. `user_customer_access`: admin→3사 OWNER, 나머지는 1~2사 EDITOR.
6. `tenant_demo.business_years`: 5건. 사업연도별 `law_version_id`를 시드 법령(CIT-2024~CIT-2026)에 매핑.
7. `tenant_demo.business_year_snapshots`: 각 사업연도별 1건(법령/세율/한도 락).
8. `tenant_demo.financial_statements`(2025-㈜OOO 사업연도에 한해 30라인) + `account_mappings` 7건.
9. `tenant_demo.assets` 8건 + `vehicle_usage_logs` 3건.
10. `tenant_demo.transactions` 10건(접대비/기부금/외주 등).
11. `tenant_demo.tax_data_import_batches` 2건(`COMPLETED`, `FAILED`).
12. `tenant_demo.adjustments` 4건(B-1, B-3, B-4, B-12) + `reserves` 2건.
13. `tenant_demo.forms_generated` 5건(FORM3, FORM15, FORM22, FORM32, FORM50) + `form_attachments` 6건.
14. `POST /validation/run`을 시더 마지막에 호출 → `validation_issues` 자동 생성.
15. `tenant_demo.workflow_events` 3건 + 2025 사업연도 1건을 `IN_REVIEW`로 둠.
16. `tenant_demo.efilings` 1건(2024 FILED용, 가짜 SHA-256/파일경로).
17. `tenant_demo.notifications` 4건 + `tenant_demo.audit_logs` 5건(시드의 마지막 단계에서 명시적으로).
18. `menu_nodes`가 비어 있으면 24행 INSERT(라벨/path/parent/sort).

### 5.3 안전 장치
- 시더는 `--reset` 플래그가 아니면 기존 행을 **덮어쓰지 않는다**.
- `--reset` 사용 시 `tenant_demo` 스키마 내부 데이터만 TRUNCATE(법령/서식 메타는 보존).
- 시더는 **반드시 환경 변수 `CIT_ALLOW_DEMO_SEED=true`** 가 있어야 동작 → 운영 사고 방지.

---

## 6. 단계별 작업 계획

### Step 1. 점검 도구 마련 (0.5일)
- [ ] `docs/menu_smoke_checklist.md` 작성: 24개 메뉴 × 3축 체크 항목.
- [ ] `tests/menu_smoke.rs` 골격 추가(seed → login → 24 GET).
- [ ] CI에 `cargo test --test menu_smoke` 추가(요건: 데모 시더 가용).

### Step 2. 현재 빈 메뉴 식별 (0.5일)
- [ ] 로컬에서 `docker compose up -d postgres api` → 빈 DB 상태에서 24개 메뉴 클릭.
- [ ] 결과를 위 표의 “증상” 컬럼과 대조하여 `docs/menu_smoke_findings.md` 작성(메뉴 키 × 실제 상태).
- [ ] 백엔드 500/타입 오류는 별도 이슈 등록(시드 부재가 아닌 코드 결함).

### Step 3. 시더 코드 작성 (2일)
- [ ] `src/seed.rs`: 트랜잭션 함수 17개(섹션 5.2의 각 단계).
- [ ] `src/bin/seed_demo.rs`: CLI 인자 파싱 + 환경 변수 가드 + `seed::run_all()` 호출.
- [ ] `tenants::create_tenant`·`tenant::create_customer`·`business_years::create` 등 기존 함수 최대한 재사용.
- [ ] argon2 해시 의존성 확인(`cargo.toml`의 `argon2` 또는 기존 인증 모듈 재사용).

### Step 4. 시더 실행 + 메뉴 재검증 (0.5일)
- [ ] `cargo run --bin seed-demo -- --reset` 실행.
- [ ] Step 2의 점검표를 다시 돌려 24개 메뉴 모두 “1행 이상 / KPI≠0 / 인터랙션 OK” 충족 확인.
- [ ] 부족한 행은 시더에 보강.

### Step 5. 코드 결함 보수 (1일)
- 시드를 했는데도 깨지는 메뉴는 코드 문제이므로 다음을 적용:
  - 응답 키 미스매치(예: 백엔드는 `total_tax_due`, 프론트는 `totalTaxDue`) → 직렬화 케이스 통일.
  - 빈 배열에서 `Math.max(1, ...arr)` 류 NaN → 가드 추가.
  - `requireWorkContext` 체크 누락 메뉴 보완.
  - 신규 라우트(`/admin/menus`, `/validation/run`, …)의 핸들러가 빈 응답을 반환할 경우 본문에 `[]` 명시(현재 null이면 프론트가 깨짐).

### Step 6. 자동화 + 회귀 방지 (0.5일)
- [ ] CI에서 시더가 통합 테스트 fixture로도 작동하는지 확인(`tests/integration_flow.rs`의 setup을 `seed::run_all_for_tenant`로 치환).
- [ ] `README.md`의 “Quick Start”에 “`docker compose run --rm api cargo run --bin seed-demo -- --reset`” 1줄 추가.
- [ ] `tests/menu_smoke.rs` green 확정 + `cargo clippy --all-targets -- -D warnings` 통과.

### Step 7. 시연 + 문서화 (0.5일)
- [ ] `docs/menu_smoke_findings.md`에 “Before/After” 비교 캡처(또는 텍스트 표).
- [ ] 본 문서(메뉴기능수정.md)의 진행 상황 갱신.
- [ ] 사용자(강수)에게 시연 1회 — 24개 메뉴 모두 빈 화면 없이 흘러가는지 확인.

---

## 7. 메뉴별 “인터랙션 OK” 판정 기준 (체크리스트 본문)

> Step 2/4에서 사용할 24항목 체크리스트. 각 항목은 “표시(O)” + “인터랙션(I)” 2축으로 본다.

| 메뉴 | O: 표시 기준 | I: 인터랙션 기준 |
|---|---|---|
| dashboard | KPI 4개 ≥1, 결재함 ≥1행 | “결재함 열기”→ws-appr 이동 |
| ws-start | 사업연도 ≥5행, 고객사 ≥3행 | 행의 [계속] 클릭 → ws-info 컨텍스트 설정 |
| ws-info | FS 카드 “일치”, 자산/거래/차량 ≥1행 | [실행]으로 validation 200, 파일 업로드(csv 작은 샘플) 1건 추가 |
| ws-adj | KPI(가산/차감) ≠0, 17 카드 표시 | 임의 카드 [실행] 후 “조정 결과” 행 +1 |
| ws-form | 부속서류 ≥5행, FORM3 미리보기 표 | [FORM3] 재생성 → 200, [PDF] 다운로드 OK |
| ws-val | rules ≥40, taxData·efile 비-null | [실행]→issues ≥1행, [무시] 클릭으로 행 제거 |
| ws-appr | queue ≥1, events ≥1 | [승인]→ status APPROVED로 변경, 컨텍스트 업데이트 |
| ws-print | attachments ≥5 | [일괄 ZIP] 다운로드 OK, 개별 PDF OK |
| ws-file | format-spec ≥1, precheck `valid` 평가됨 | [생성]→history +1, [다운로드] OK |
| post-hist | 사업연도 ≥5, 전자신고 ≥1 | (정보 화면) |
| post-amend | differences ≥1 | [해제] → context.lockMode=OPEN |
| rp-alerts | notifications ≥4 | [읽음]→상태 READ 토글 |
| rp-compare | rows ≥2, 차트 막대 ≥2 | (정보 화면) |
| rp-burden | rows ≥2 | (정보 화면) |
| rp-reserve | rows ≥3 | (정보 화면) |
| ad-tenant | tenants ≥1 | [등록] 폼 제출 → 201 |
| ad-cust | customers ≥3 | [등록] 폼 제출 → 201 |
| ad-user-list | users ≥4 | [등록] 폼 제출 → 201 |
| ad-role | roles ≥3, permissions ≥10 | [TAX_EXPERT 저장] → 200, 새 권한 반영 |
| ad-menu-fn | menus ≥24 | feature_flag 입력 후 [저장]→200, 재조회 반영 |
| ad-cacc | 사용자×고객사 ≥3행 | (정보 화면) |
| ad-law | laws ≥6, rates ≥5, limits ≥3 | [등록] → 201, 새 행 표시 |
| ad-form | forms ≥3, versions ≥3, relationships ≥1 | (정보 화면) |
| ad-audit | logs ≥5 | (정보 화면) |

---

## 8. 위험과 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| 시더가 운영 DB에서 실행되어 데이터 오염 | 사고 | 환경변수 `CIT_ALLOW_DEMO_SEED=true` 가드 + `--tenant` 강제 + 코드 내부에서 `tenant_code == "demo"` 강제 |
| 시더 idempotency 깨짐(중복 INSERT) | 재실행 실패 | 각 INSERT를 `ON CONFLICT DO NOTHING/UPDATE`로. 사업연도·세무자료처럼 `ON CONFLICT`가 어려운 경우 사전 SELECT로 skip |
| `validation/run` 호출이 시더 중 실패 | issues 미생성 | 시더 마지막 단계에서 catch + 로깅, 실패해도 다른 데이터는 유지 |
| 시드 비밀번호 노출 | 보안 사고 | 비밀번호는 ENV 또는 `--admin-password` 인자만 허용. 기본값은 “ChangeMe123!”로 두되 README에 변경 권고 명시 |
| 마이그레이션 변경 시 시더와 스키마 불일치 | 시더 깨짐 | `tests/menu_smoke.rs`가 CI에서 항상 시더를 실행하므로 즉시 발견 |

---

## 9. 일정 (총 5일)

| 일자 | 작업 |
|---|---|
| D1 오전 | Step 1 점검 도구 마련 |
| D1 오후 | Step 2 빈 메뉴 식별, findings 문서화 |
| D2~D3 | Step 3 시더 코드 작성 |
| D3 오후 | Step 4 시더 실행 + 메뉴 재검증 |
| D4 | Step 5 코드 결함 보수 |
| D5 오전 | Step 6 자동화 + 회귀 방지 |
| D5 오후 | Step 7 시연 + 문서 마무리 |

---

## 10. 완료 정의 (DoD)

1. `docker compose up`이후 한 줄(`cargo run --bin seed-demo -- --reset`)로 시드가 완료된다.
2. demo / admin / ChangeMe123! 로 로그인이 성공한다.
3. 24개 메뉴를 순서대로 클릭하여 빈 표/0 카운트/요청 실패가 **하나도 없다**.
4. ws-info → ws-adj → ws-form → ws-val → ws-appr → ws-print → ws-file → post-hist 로 흐름이 한 번 닫힌다.
5. `cargo test --test menu_smoke`, `tests/integration_flow.rs`, `cargo clippy --all-targets -- -D warnings` 가 모두 통과한다.
6. 본 문서와 `docs/menu_smoke_checklist.md`, `docs/menu_smoke_findings.md`가 보관된다.

---

## 11. 2026-05-19 완료 기준 업데이트

- `docs/menu_smoke_checklist.md`는 최종 24개 메뉴와 추가구현 Pack 1~6 화면/API 확인 기준을 반영한다.
- `docs/menu_smoke_findings.md`는 demo seed 전후 상태와 보안, 권한, 조정 이력, 보고서, 출력 이력 검증 범위를 반영한다.
- `ws-adj`는 17개 조정 모듈 실행에 더해 B1/B4/B15 공통 그리드, 이력, 증빙 첨부를 표시한다.
- `ad-role`, `ad-menu-fn`, `ad-cacc`, `ad-form`, `ad-audit`는 각각 함수 코드/역할 메뉴 권한, 메뉴 기능, 권한 위임, 서식 cycle-check, 감사 해시 검증을 표시한다.
- 직접 e-Tax 제출, ERP 연동, Redis 캐시 무효화는 외부 연동 전제 조건이 있어 메뉴 smoke/DoD에서는 P3 보류로 본다.
# 2026-05-19 v1.1 Menu Smoke Update

- The smoke target is now the v1.1 leaf tree: 5 root groups and 109 listed leaves, with the DoD minimum of 99 preserved.
- `docs/menu_smoke_checklist.md` now lists every leaf key and the data/interaction checks for the current S1 delegate screens.
- `tests/menu_full_tree.rs` verifies exact leaf coverage, hash paths, layout metadata, context guards, and one permission function per leaf.
- Admin menu data now expects the full v1.1 menu seed and `menu_functions`, not the older 24-entry tree.

---
