# 대시보드 세부 구현 계획서 v1.0

> **문서 버전**: v1.0
> **작성일**: 2026-05-22
> **작성자**: 강수 (Kang-Soo.Cho@kr.ey.com)
> **연관 문서**: `법인세_세무조정계산서_시스템_설계서.md`, `업무흐름.md`, `DB설계.md`
> **목적**: 대시보드(화면 B)의 5개 핵심 섹션(업무현황 / 신고마감 임박 / 알림·결재 대기함 / 최근활동 / 핵심지표)을 업무 흐름에 맞춰 재정의하고, 각 섹션의 화면 구성·데이터 소스·API·상태 모델·인터랙션을 구체화한다.

---

## 목차

1. [대시보드 전체 설계 원칙](#1-대시보드-전체-설계-원칙)
2. [레이아웃 구성 및 그리드 설계](#2-레이아웃-구성-및-그리드-설계)
3. [섹션 1 — 업무현황](#3-섹션-1--업무현황)
4. [섹션 2 — 신고마감 임박](#4-섹션-2--신고마감-임박)
5. [섹션 3 — 알림 / 결재 대기함](#5-섹션-3--알림--결재-대기함)
6. [섹션 4 — 최근활동](#6-섹션-4--최근활동)
7. [섹션 5 — 핵심지표 (KPI)](#7-섹션-5--핵심지표-kpi)
8. [API 명세 (대시보드 전용)](#8-api-명세-대시보드-전용)
9. [데이터 구성 상세](#9-데이터-구성-상세)
10. [역할별 표시 제어](#10-역할별-표시-제어)
11. [프론트엔드 구현 상세](#11-프론트엔드-구현-상세)
12. [구현 우선순위 및 일정](#12-구현-우선순위-및-일정)

---

## 1. 대시보드 전체 설계 원칙

### 1.1 업무 흐름과의 정합성

대시보드(화면 B)는 `업무흐름.md § 3.2`에서 정의한 **신고 작업 사이클의 진입점**이다. 사용자가 로그인 직후 도달하는 화면이므로, 다음 원칙을 따른다.

| 원칙 | 내용 |
|------|------|
| 다음 행동 우선 | 사용자가 지금 해야 할 일(결재 대기, 마감 임박, 반려 회신)을 가장 위에 배치 |
| 상태 기반 표시 | 사업연도 상태(`DRAFT → IN_REVIEW → APPROVED → FILED → AMENDED`)를 직접 매핑하여 수치 제공 |
| 역할별 차별화 | 같은 화면이라도 역할(TAX_EXPERT, TAX_REVIEWER, ASSISTANT 등)에 따라 보이는 항목과 카운트가 다름 |
| 원클릭 진입 | 모든 카운트 배지·목록 항목은 클릭 시 해당 업무 화면으로 바로 이동 |
| 실시간성 유지 | 결재 대기 수와 알림 수는 SSE(Server-Sent Events) 또는 30초 폴링으로 실시간 갱신 |
| 테넌트 격리 | 모든 대시보드 데이터는 JWT의 `tenant_id` 기준으로 격리된 쿼리를 실행 |

### 1.2 대시보드가 직접 연결하는 업무 흐름 단계

```
대시보드
 ├─[업무현황 카드 클릭]────────────► ② 신고 작업 > 해당 상태 작업 목록
 ├─[신고마감 행 클릭]─────────────► ② 신고 작업 > 해당 사업연도
 ├─[결재 대기 항목 클릭]──────────► ② 신고 작업 > 5. 결재함
 ├─[알림 항목 클릭]───────────────► 알림 센터 또는 해당 화면
 ├─[최근활동 항목 클릭]──────────► 해당 업무 화면
 └─[신고 작업 시작 ▶ 버튼]────────► ② 신고 작업 > 0. 작업 시작 (B-1)
```

---

## 2. 레이아웃 구성 및 그리드 설계

### 2.1 전체 대시보드 그리드

12컬럼 그리드 기준으로 다음과 같이 배치한다.

```
┌─────────────────────────────────────────────────────────────────────────┐
│  헤더: Logo │ 전체 검색  │ 알림 배지  │ 결재대기 배지  │ 사용자 메뉴   │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  [섹션 1] 업무현황 카드 5개 ─────────────────── (12/12 col, 풀폭)        │
│  ┌─────────┬─────────┬─────────┬─────────┬─────────┐                   │
│  │작성중   │검증대기  │결재대기  │승인완료  │신고완료  │                   │
│  └─────────┴─────────┴─────────┴─────────┴─────────┘                   │
│                                                                          │
│  [섹션 2] 신고마감 임박 ─── (5/12 col)   [섹션 3] 알림/결재 대기함 ─── (7/12) │
│  ┌──────────────────────────────┐ ┌──────────────────────────────────┐  │
│  │ D-7  / D-14 / D-30 목록     │ │ 탭: 알림(N) | 결재대기(M)         │  │
│  │ 마감일 기준 오름차순         │ │ 목록 (최신 10건)                  │  │
│  └──────────────────────────────┘ └──────────────────────────────────┘  │
│                                                                          │
│  [섹션 4] 최근활동 ─────── (5/12 col)   [섹션 5] 핵심지표 ─────── (7/12)  │
│  ┌──────────────────────────────┐ ┌──────────────────────────────────┐  │
│  │ 시간순 활동 피드 (최근 15건)  │ │ 세부담 추이 차트 + 업종별 도넛    │  │
│  └──────────────────────────────┘ └──────────────────────────────────┘  │
│                                                                          │
│  [신고 작업 시작 ▶] ─────────────────────────────────────────── (우하단)  │
└──────────────────────────────────────────────────────────────────────────┘
```

### 2.2 반응형 분기

| 브레이크포인트 | 레이아웃 변화 |
|----------------|---------------|
| ≥ 1440px | 2.1 레이아웃 그대로 |
| 1024px ~ 1439px | 섹션 2·3을 각 6/12, 섹션 4·5를 각 6/12 |
| < 1024px | 모든 섹션 단일 컬럼, 업무현황은 2×3 그리드로 축소 |

---

## 3. 섹션 1 — 업무현황

### 3.1 목적 및 업무 흐름 연계

`업무흐름.md § 1.3`의 사업연도 상태 모델(`DRAFT → IN_REVIEW → APPROVED → FILED → AMENDED`)을 카드 5개로 직접 표현한다. 로그인한 사용자가 현재 **본인이 담당하는** 사업연도들의 상태 분포를 한눈에 파악하고, 클릭 시 해당 목록으로 바로 진입한다.

### 3.2 카드 구성

| 카드 | 상태값 | 카드 제목 | 색상 | 클릭 대상 화면 |
|------|--------|----------|------|----------------|
| 카드 1 | `DRAFT` | 작성중 | 파랑(Blue-500) | ② 신고 작업 > 목록 (status=DRAFT) |
| 카드 2 | `IN_REVIEW` 중 검증 완료 대기 | 검증 대기 | 주황(Orange-400) | ② 신고 작업 > 4. 검증 |
| 카드 3 | `IN_REVIEW` 결재 진행 중 | 결재 대기 | 빨강(Red-500) | ② 신고 작업 > 5. 결재함 |
| 카드 4 | `APPROVED` | 승인 완료 | 초록(Green-500) | ② 신고 작업 > 목록 (status=APPROVED) |
| 카드 5 | `FILED` | 신고 완료 | 회색(Gray-500) | ③ 사후 관리 > 신고 이력 |

> **카드 2(검증 대기)와 카드 3(결재 대기)의 구분**: 두 카드 모두 DB 상태는 `IN_REVIEW`이나, `approval_lines.status = 'PENDING'` 여부로 결재 진행 중 여부를 구분한다. 검증은 완료(`validation_result = 'PASS'`)되었지만 아직 결재라인이 생성되지 않은 건이 카드 2에 해당한다.

### 3.3 카드 내부 구성

```
┌──────────────────────────────┐
│  아이콘  상태명               │
│  ──────────────────────────  │
│        [숫자: 큰 폰트]        │  ← 사업연도 건수 (담당 법인 기준)
│  고객사 N개                   │  ← 고유 고객사 수
│  ────────────────────────── │
│  [즉시 처리 필요] 또는 공백   │  ← D-7 이내 마감 건이 있으면 강조 표시
└──────────────────────────────┘
```

반려(REJECT) 건은 별도 카드 대신 카드 1(DRAFT) 위에 **빨간 배너**로 표시:
`"⚠ 반려 N건 — 재작성 필요"`

### 3.4 데이터 소스

**API**: `GET /api/dashboard/work-status`

**쿼리 로직**:

```sql
-- 사용자 담당 사업연도 상태 집계
SELECT
  by.status,
  COUNT(*)                        AS year_count,
  COUNT(DISTINCT by.customer_id)  AS customer_count,
  SUM(CASE WHEN by.filing_due_date <= CURRENT_DATE + 7
           THEN 1 ELSE 0 END)      AS urgent_count
FROM business_years by
JOIN v_user_effective_customer_access acc
  ON acc.customer_id = by.customer_id
  AND acc.user_id    = :userId
WHERE by.tenant_id = :tenantId
  AND by.status    != 'FILED'   -- 완료 제외, 별도 집계
GROUP BY by.status;
```

**응답 DTO**:

```json
{
  "workStatus": [
    {
      "status": "DRAFT",
      "label": "작성중",
      "yearCount": 12,
      "customerCount": 10,
      "urgentCount": 2,
      "color": "#3B82F6"
    },
    {
      "status": "IN_REVIEW_VALIDATION",
      "label": "검증 대기",
      "yearCount": 4,
      "customerCount": 4,
      "urgentCount": 1,
      "color": "#FB923C"
    },
    {
      "status": "IN_REVIEW_APPROVAL",
      "label": "결재 대기",
      "yearCount": 2,
      "customerCount": 2,
      "urgentCount": 0,
      "color": "#EF4444"
    },
    {
      "status": "APPROVED",
      "label": "승인 완료",
      "yearCount": 3,
      "customerCount": 3,
      "urgentCount": 0,
      "color": "#22C55E"
    },
    {
      "status": "FILED",
      "label": "신고 완료",
      "yearCount": 23,
      "customerCount": 20,
      "urgentCount": 0,
      "color": "#6B7280"
    }
  ],
  "rejectedCount": 1
}
```

**캐시**: Redis, TTL 60초 (`dashboard:work-status:{tenantId}:{userId}`)

---

## 4. 섹션 2 — 신고마감 임박

### 4.1 목적 및 업무 흐름 연계

`업무흐름.md Phase 18`에서 정의한 **D-30 인앱 알림** 정책을 시각적으로 구현한다. 마감일이 30일 이내이고 `FILED`가 아닌 사업연도를 긴급도 순으로 나열하여, 사용자가 우선순위를 즉시 판단할 수 있도록 한다.

### 4.2 화면 구성

```
┌─────────────────────────────────────────────────────────┐
│  🗓 신고마감 임박          [전체 보기 →]                  │
├─────┬──────────────┬──────────┬──────────┬──────────────┤
│긴급도│고객사명       │사업연도   │마감일     │상태           │
├─────┼──────────────┼──────────┼──────────┼──────────────┤
│D-3  │㈜OOO         │2025      │2026-05-25 │작성중(65%)   │
│D-7  │△△사         │2025      │2026-05-29 │검증 대기      │
│D-14 │□□㈜          │2025      │2026-06-05 │결재 대기      │
│D-22 │◇◇공업        │2025      │2026-06-13 │작성중(30%)   │
│D-29 │★★홀딩스      │2025      │2026-06-20 │DRAFT          │
└─────┴──────────────┴──────────┴──────────┴──────────────┘
  ⚠  표시: D-7 이내는 행 전체 빨간 배경, D-14 이내는 주황 배경
```

**긴급도 색상 규칙**:

| 잔여일 | 배경 색상 | 긴급도 배지 색 |
|--------|----------|---------------|
| 0 ~ 7일 | 연빨강 (Red-50) | 빨강 (Red-600) |
| 8 ~ 14일 | 연주황 (Orange-50) | 주황 (Orange-500) |
| 15 ~ 30일 | 연노랑 (Yellow-50) | 노랑 (Yellow-600) |

**진행률 표시**: DRAFT 상태 사업연도는 세무조정 17개 모듈 중 저장 완료 비율(%)을 함께 표시한다.

**"전체 보기" 링크**: D-30 초과 건을 포함한 전체 마감 캘린더 화면으로 이동.

### 4.3 데이터 소스

**API**: `GET /api/dashboard/filing-deadlines?withinDays=30`

**쿼리 로직**:

```sql
SELECT
  c.customer_name,
  by.fiscal_year,
  by.filing_due_date,
  by.status,
  by.id          AS business_year_id,
  -- 진행률: tax_adjustments 저장 모듈 수 / 17
  ROUND(
    COUNT(DISTINCT ta.module_code) FILTER (WHERE ta.is_saved = true)
    * 100.0 / 17, 0
  )              AS progress_pct,
  (by.filing_due_date - CURRENT_DATE) AS days_remaining
FROM business_years by
JOIN customers c
  ON c.id = by.customer_id
JOIN v_user_effective_customer_access acc
  ON acc.customer_id = by.customer_id
  AND acc.user_id    = :userId
LEFT JOIN tax_adjustments ta
  ON ta.business_year_id = by.id
WHERE by.tenant_id       = :tenantId
  AND by.status         NOT IN ('FILED', 'AMENDED')
  AND by.filing_due_date <= CURRENT_DATE + :withinDays
GROUP BY c.customer_name, by.fiscal_year, by.filing_due_date,
         by.status, by.id
ORDER BY by.filing_due_date ASC
LIMIT 10;
```

**응답 DTO**:

```json
{
  "deadlines": [
    {
      "businessYearId": "uuid",
      "customerName": "㈜OOO",
      "fiscalYear": 2025,
      "filingDueDate": "2026-05-25",
      "daysRemaining": 3,
      "status": "DRAFT",
      "progressPct": 65,
      "urgencyLevel": "CRITICAL"
    }
  ],
  "totalCount": 8
}
```

**urgencyLevel 계산**:
- `daysRemaining ≤ 7` → `CRITICAL`
- `daysRemaining ≤ 14` → `WARNING`
- `daysRemaining ≤ 30` → `NOTICE`

**캐시**: Redis, TTL 300초 (`dashboard:deadlines:{tenantId}:{userId}`)

---

## 5. 섹션 3 — 알림 / 결재 대기함

### 5.1 목적 및 업무 흐름 연계

두 가지 성격이 다른 정보를 **탭**으로 통합 표시한다.

- **알림 탭**: 시스템이 자동 생성하는 이벤트 알림 (`notifications` 테이블). D-30 마감 알림, 법령 개정 알림, 반려 알림, 잠금 해제 알림 등.
- **결재 대기 탭**: `business_years.status = 'IN_REVIEW'`이고 본인이 검토자(TAX_REVIEWER)로 지정된 결재 요청 목록.

### 5.2 화면 구성 — 알림 탭

```
┌──────────────────────────────────────────────────────────────┐
│  🔔 알림 (3)  │  ✅ 결재 대기 (2)           [알림 센터 →]    │
├──────────────────────────────────────────────────────────────┤
│ ● [D-30 마감] ㈜OOO 2025 사업연도 — 30일 후 마감           │
│              2026-05-22 09:15    [이동 →]   [확인]           │
├──────────────────────────────────────────────────────────────┤
│ ● [반려] △△사 세무조정 — 검토자 박OO님이 반려함             │
│              2026-05-21 17:30    [이동 →]   [확인]           │
├──────────────────────────────────────────────────────────────┤
│ ● [법령 개정] 2025 사업연도 접대비 한도 개정 적용 가능       │
│              2026-05-20 10:00    [이동 →]   [확인]           │
├──────────────────────────────────────────────────────────────┤
│                               [전체 읽음 처리]               │
└──────────────────────────────────────────────────────────────┘
```

**알림 유형 분류**:

| 알림 유형 코드 | 트리거 조건 | 아이콘 | 이동 대상 |
|----------------|------------|--------|----------|
| `DEADLINE_D30` | `filing_due_date - CURRENT_DATE = 30` | 🗓 | 해당 사업연도 |
| `DEADLINE_D7` | `filing_due_date - CURRENT_DATE = 7` | ⚠️ | 해당 사업연도 |
| `DEADLINE_DDAY` | `filing_due_date = CURRENT_DATE` | 🚨 | 해당 사업연도 |
| `APPROVAL_REJECTED` | `approval_lines.status → REJECTED` | ❌ | ② 신고 작업 > 5-3. 반려 이력 |
| `LAW_AMENDED` | `tax_law_versions.status → ACTIVE` | 📋 | 법령 변경 알림 센터 |
| `DELEGATION_RECEIVED` | `access_delegations` 신규 생성 | 👥 | 담당 법인 권한 화면 |
| `LOCK_RELEASED` | `business_years` UNLOCK 이벤트 | 🔓 | 해당 사업연도 |

### 5.3 화면 구성 — 결재 대기 탭

```
┌──────────────────────────────────────────────────────────────┐
│  🔔 알림 (3)  │  ✅ 결재 대기 (2)                            │
├──────────────────────────────────────────────────────────────┤
│ 고객사: △△사  사업연도: 2025                                  │
│ 요청자: 강수  요청일: 2026-05-22 10:30   D-14 마감           │
│ [서식 미리보기]  [검증 결과 보기]  [승인 →]  [반려]          │
├──────────────────────────────────────────────────────────────┤
│ 고객사: □□㈜  사업연도: 2025                                  │
│ 요청자: 이OO  요청일: 2026-05-21 16:00   D-7 마감 ⚠         │
│ [서식 미리보기]  [검증 결과 보기]  [승인 →]  [반려]          │
└──────────────────────────────────────────────────────────────┘
```

**결재 대기 카드 구성 요소**:
- 고객사명 / 사업연도
- 결재 요청자 / 요청 일시
- 잔여 마감일 (긴급 시 강조)
- 인라인 액션 버튼: `[서식 미리보기]`, `[검증 결과 보기]`, `[승인]`, `[반려]`
  - `[승인]` / `[반려]` 클릭 시 확인 모달 → `approval_lines.status` 변경

### 5.4 헤더 배지 실시간 갱신

- **알림 수 배지**: 미확인(`is_read = false`) 알림 수
- **결재 대기 수 배지**: `approval_lines.status = 'PENDING'`이고 본인 차례인 건 수
- 갱신 방식: **SSE** (`/api/notifications/stream`) — 이벤트 발생 시 서버 푸시, fallback으로 30초 폴링

### 5.5 데이터 소스

**알림 API**: `GET /api/dashboard/notifications?limit=10&unreadOnly=false`

```sql
SELECT
  n.id,
  n.type,
  n.title,
  n.body,
  n.reference_id,
  n.reference_type,
  n.is_read,
  n.created_at
FROM notifications n
WHERE n.tenant_id   = :tenantId
  AND n.recipient_id = :userId
ORDER BY n.is_read ASC, n.created_at DESC
LIMIT :limit;
```

**결재 대기 API**: `GET /api/dashboard/approvals-pending`

```sql
SELECT
  al.id            AS approval_line_id,
  al.business_year_id,
  c.customer_name,
  by.fiscal_year,
  by.filing_due_date,
  (by.filing_due_date - CURRENT_DATE) AS days_remaining,
  u.display_name   AS requester_name,
  al.requested_at
FROM approval_lines al
JOIN business_years by ON by.id = al.business_year_id
JOIN customers c        ON c.id = by.customer_id
JOIN users u            ON u.id = al.requester_id
WHERE al.tenant_id     = :tenantId
  AND al.approver_id   = :userId
  AND al.status        = 'PENDING'
ORDER BY by.filing_due_date ASC;
```

**알림 `notifications` 테이블 설계**:

```sql
CREATE TABLE notifications (
  id              UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id       UUID         NOT NULL,
  recipient_id    UUID         NOT NULL REFERENCES users(id),
  type            VARCHAR(50)  NOT NULL,  -- DEADLINE_D30, APPROVAL_REJECTED 등
  title           VARCHAR(200) NOT NULL,
  body            TEXT,
  reference_id    UUID,                   -- 연관 사업연도/법령 등의 PK
  reference_type  VARCHAR(50),            -- 'BUSINESS_YEAR', 'TAX_LAW_VERSION' 등
  is_read         BOOLEAN      NOT NULL DEFAULT false,
  created_at      TIMESTAMPTZ  NOT NULL DEFAULT now(),
  read_at         TIMESTAMPTZ,
  expires_at      TIMESTAMPTZ             -- 만료 후 자동 숨김
);

CREATE INDEX idx_notifications_recipient ON notifications(tenant_id, recipient_id, is_read, created_at DESC);
```

---

## 6. 섹션 4 — 최근활동

### 6.1 목적 및 업무 흐름 연계

`audit_logs` 테이블을 기반으로 **현재 사용자가 접근 권한을 가진 고객사에서 발생한** 주요 업무 이벤트를 시간순으로 표시한다. 팀 내 공동 작업 진행 상황을 파악하고, 최근 변경된 데이터로 빠르게 이동한다.

### 6.2 화면 구성

```
┌────────────────────────────────────────────────────────────────┐
│  🕐 최근활동                            [감사 로그 전체 →]      │
├────────────────────────────────────────────────────────────────┤
│ 14:32  강수   ㈜OOO / 2025    세무조정 > 접대비 저장           │
│ 13:15  박OO  △△사 / 2025    서식 생성 완료 (별지 제3호)       │
│ 11:50  이OO  □□㈜ / 2025    검증 실행 — ERROR 2건             │
│ 10:30  강수   △△사 / 2025    결재 요청 (IN_REVIEW 전환)        │
│ 09:15  김OO  ◇◇공업 / 2025  재무제표 임포트 완료             │
│ 어제   박OO  ★★홀딩스 / 2024 신고 제출 완료 (FILED)           │
│ 어제   이OO  ㈜OOO / 2024    잠금 해제 신청 (수정신고)         │
│                                                    [더 보기 ↓] │
└────────────────────────────────────────────────────────────────┘
```

**항목 클릭**: 해당 업무 화면(세무조정 모듈, 서식 미리보기, 검증 결과 등)으로 직접 이동.

### 6.3 활동 유형 분류

| 이벤트 코드 | 표시 메시지 예시 | 이동 대상 |
|-------------|-----------------|----------|
| `TAX_ADJ_SAVED` | `세무조정 > {모듈명} 저장` | 세무조정 해당 모듈 |
| `FORM_GENERATED` | `서식 생성 완료 ({서식명})` | 서식 미리보기 |
| `VALIDATION_RUN` | `검증 실행 — ERROR N건 / WARN M건` | 검증 결과 화면 |
| `REVIEW_REQUESTED` | `결재 요청 (IN_REVIEW 전환)` | 결재함 |
| `REVIEW_APPROVED` | `결재 승인 (APPROVED 전환)` | 해당 사업연도 |
| `REVIEW_REJECTED` | `결재 반려 — {사유}` | 반려 이력 |
| `EFILING_SUBMITTED` | `신고 제출 완료 (FILED)` | 신고 이력 |
| `FS_IMPORTED` | `재무제표 임포트 완료` | 재무제표 화면 |
| `LOCK_RELEASED` | `잠금 해제 신청 (수정신고)` | 해당 사업연도 |
| `AMENDMENT_SUBMITTED` | `수정신고 제출 완료 (AMENDED)` | 신고 이력 |

### 6.4 데이터 소스

**API**: `GET /api/dashboard/recent-activities?limit=15`

```sql
SELECT
  al.id,
  al.action_code,
  al.entity_type,
  al.entity_id,
  al.description,
  al.performed_by,
  u.display_name     AS performer_name,
  c.customer_name,
  by.fiscal_year,
  al.created_at
FROM audit_logs al
JOIN users u        ON u.id          = al.performed_by
LEFT JOIN business_years by ON by.id = al.entity_id
                              AND al.entity_type = 'BUSINESS_YEAR'
LEFT JOIN customers c       ON c.id  = by.customer_id
WHERE al.tenant_id  = :tenantId
  AND (
    -- 본인 활동
    al.performed_by = :userId
    OR
    -- 본인이 접근 권한 있는 고객사의 활동
    c.id IN (
      SELECT customer_id
      FROM v_user_effective_customer_access
      WHERE user_id = :userId AND tenant_id = :tenantId
    )
  )
ORDER BY al.created_at DESC
LIMIT :limit;
```

**`audit_logs` 연관 컬럼 확인 사항**:
- `action_code`: 이벤트 유형 코드
- `entity_type`: `'BUSINESS_YEAR'`, `'CUSTOMER'`, `'TAX_ADJUSTMENT'` 등
- `entity_id`: 대상 엔티티 PK (UUID)
- `description`: 자동 생성 메시지 (한국어)
- `hash_current`: 일별 무결성 체인 해시

**캐시**: Redis, TTL 60초 (`dashboard:activities:{tenantId}:{userId}`)

---

## 7. 섹션 5 — 핵심지표 (KPI)

### 7.1 목적 및 업무 흐름 연계

`업무흐름.md § 1.2`의 Phase 구분과 `설계서 § 5.1`의 KPI 항목(`당기 세부담 / 업종별 분포`)을 기반으로 회계법인 운영자와 세무전문가가 자주 확인하는 **업무 집약 지표**를 제공한다.

### 7.2 KPI 구성 (3개 영역)

#### 영역 A — 당기 세부담 추이 (꺾은선 차트)

```
┌──────────────────────────────────────────────────────────────┐
│  📊 당기 세부담 추이 (담당 법인 평균 실효세율 %)              │
│                                                              │
│   20%  ───────●                                             │
│   18%       /   \─────────────●                            │
│   16%   ───/                    \──────●                    │
│   14%  ─●─/                            \────●              │
│         2021   2022   2023   2024   2025                     │
│                                                              │
│  기간: [최근 5개년 ▼]    [담당 법인 전체 ▼]                   │
└──────────────────────────────────────────────────────────────┘
```

**표시 데이터**:
- X축: 사업연도 (최근 3·5년 선택 가능)
- Y축: 담당 법인 전체 평균 실효세율 (%) = `결정세액 / 과세표준 × 100`
- 개별 법인 필터: 특정 고객사 선택 시 해당 법인 단독 표시

#### 영역 B — 업종별 법인 분포 (도넛 차트)

```
┌──────────────────────────────────────────────────────────────┐
│  🍩 담당 법인 업종별 분포                                    │
│                                                              │
│          제조업 35%                                          │
│    ┌─────────────┐  도소매 22%                              │
│    │             │  IT·서비스 18%                            │
│    │   도넛 차트  │  건설 12%                                 │
│    │             │  금융 8%                                  │
│    └─────────────┘  기타 5%                                  │
└──────────────────────────────────────────────────────────────┘
```

#### 영역 C — 이월결손금 만료 예측 (바 차트)

```
┌──────────────────────────────────────────────────────────────┐
│  ⏳ 이월결손금 만료 예측 (N개 법인)       [상세 보기 →]        │
│                                                              │
│  2026년 : ████████ 3억원   (3개 법인)                       │
│  2027년 : ████ 1.5억원     (2개 법인)                       │
│  2028년 : ██████████ 5억원 (5개 법인)                       │
└──────────────────────────────────────────────────────────────┘
```

> `expiration_alerts` 및 `carryforward_loss` 테이블에서 집계. `업무흐름.md Phase 11`의 만료 임박 결손금 알림과 연계.

### 7.3 데이터 소스

**세부담 추이 API**: `GET /api/dashboard/kpi/tax-burden?years=5`

```sql
SELECT
  by.fiscal_year,
  ROUND(
    SUM(fd.determined_tax_amount) * 100.0
    / NULLIF(SUM(fd.taxable_income), 0), 2
  )  AS effective_tax_rate_pct,
  COUNT(DISTINCT by.customer_id) AS customer_count
FROM business_years by
JOIN form_data fd
  ON fd.business_year_id = by.id
  AND fd.form_code = 'FORM3'        -- 별지 제3호
JOIN v_user_effective_customer_access acc
  ON acc.customer_id = by.customer_id
  AND acc.user_id    = :userId
WHERE by.tenant_id  = :tenantId
  AND by.status IN ('FILED', 'APPROVED', 'AMENDED')
  AND by.fiscal_year >= EXTRACT(YEAR FROM CURRENT_DATE) - :years
GROUP BY by.fiscal_year
ORDER BY by.fiscal_year ASC;
```

**업종별 분포 API**: `GET /api/dashboard/kpi/industry-distribution`

```sql
SELECT
  c.industry_code,
  co.code_name        AS industry_name,
  COUNT(DISTINCT c.id) AS customer_count
FROM customers c
JOIN codes co
  ON co.code_group = 'INDUSTRY' AND co.code = c.industry_code
JOIN v_user_effective_customer_access acc
  ON acc.customer_id = c.id AND acc.user_id = :userId
WHERE c.tenant_id = :tenantId
GROUP BY c.industry_code, co.code_name
ORDER BY customer_count DESC;
```

**이월결손금 만료 API**: `GET /api/dashboard/kpi/loss-expiry?years=3`

```sql
SELECT
  cl.expiry_year,
  SUM(cl.remaining_amount) AS total_amount,
  COUNT(DISTINCT cl.customer_id) AS customer_count
FROM carryforward_loss cl
JOIN v_user_effective_customer_access acc
  ON acc.customer_id = cl.customer_id AND acc.user_id = :userId
WHERE cl.tenant_id       = :tenantId
  AND cl.remaining_amount > 0
  AND cl.expiry_year BETWEEN EXTRACT(YEAR FROM CURRENT_DATE)
                         AND EXTRACT(YEAR FROM CURRENT_DATE) + :years - 1
GROUP BY cl.expiry_year
ORDER BY cl.expiry_year ASC;
```

**캐시**: Redis, TTL 3600초 (`dashboard:kpi:{tenantId}:{userId}`)

---

## 8. API 명세 (대시보드 전용)

### 8.1 엔드포인트 목록

| Method | Path | 설명 | 캐시 TTL |
|--------|------|------|---------|
| GET | `/api/dashboard/summary` | 전체 대시보드 데이터 일괄 조회 (초기 로드용) | 60초 |
| GET | `/api/dashboard/work-status` | 업무현황 카드 데이터 | 60초 |
| GET | `/api/dashboard/filing-deadlines` | 신고마감 임박 목록 | 300초 |
| GET | `/api/dashboard/notifications` | 알림 목록 | 실시간 |
| GET | `/api/dashboard/approvals-pending` | 결재 대기 목록 | 30초 |
| GET | `/api/dashboard/recent-activities` | 최근활동 피드 | 60초 |
| GET | `/api/dashboard/kpi/tax-burden` | 세부담 추이 | 3600초 |
| GET | `/api/dashboard/kpi/industry-distribution` | 업종별 분포 | 3600초 |
| GET | `/api/dashboard/kpi/loss-expiry` | 이월결손금 만료 예측 | 3600초 |
| PATCH | `/api/notifications/{id}/read` | 알림 읽음 처리 | — |
| PATCH | `/api/notifications/read-all` | 전체 읽음 처리 | — |
| GET | `/api/notifications/stream` | SSE 스트림 (실시간 알림) | — |

### 8.2 공통 응답 구조

```json
{
  "success": true,
  "data": { ... },
  "meta": {
    "generatedAt": "2026-05-22T09:30:00Z",
    "tenantId": "uuid",
    "userId": "uuid"
  }
}
```

### 8.3 일괄 조회 API (`/api/dashboard/summary`)

초기 페이지 로드 시 모든 섹션 데이터를 단일 요청으로 받아 렌더링 성능을 최적화한다.

```json
{
  "workStatus": { ... },
  "filingDeadlines": { ... },
  "notifications": { ... },
  "approvalsPending": { ... },
  "recentActivities": { ... },
  "kpi": {
    "taxBurden": { ... },
    "industryDistribution": { ... },
    "lossExpiry": { ... }
  }
}
```

---

## 9. 데이터 구성 상세

### 9.1 관련 테이블 및 뷰 정리

| 테이블 / 뷰 | 섹션 | 용도 |
|-------------|------|------|
| `business_years` | 업무현황, 신고마감, 결재대기 | 사업연도 상태·마감일 |
| `customers` | 신고마감, 최근활동, KPI | 고객사명·업종코드 |
| `v_user_effective_customer_access` | 전 섹션 | 사용자 접근 가능 고객사 필터 |
| `notifications` | 알림 탭 | 알림 유형·읽음 여부 |
| `approval_lines` | 결재 대기 탭 | 결재 상태·요청자·승인자 |
| `audit_logs` | 최근활동 | 업무 이벤트 로그 |
| `tax_adjustments` | 신고마감 진행률 | 모듈별 저장 완료 여부 |
| `form_data` | KPI 세부담 | FORM3 결정세액·과세표준 |
| `carryforward_loss` | KPI 이월결손금 | 잔액·만료연도 |
| `codes` | KPI 업종 | 업종코드 명칭 |
| `users` | 최근활동, 결재대기 | 사용자 표시명 |

### 9.2 `business_years` 상태 모델 — 대시보드 매핑

| DB status | 대시보드 표시 | 섹션 1 카드 |
|-----------|-------------|-------------|
| `DRAFT` | 작성중 | 카드 1 |
| `IN_REVIEW` (approval_lines 없음 또는 미생성) | 검증 대기 | 카드 2 |
| `IN_REVIEW` (approval_lines.status = PENDING) | 결재 대기 | 카드 3 |
| `APPROVED` | 승인 완료 | 카드 4 |
| `FILED` | 신고 완료 | 카드 5 |
| `AMENDED` | 수정신고 | 카드 1에 별도 배지 |

### 9.3 `notifications` 자동 생성 트리거

| 트리거 | 실행 주체 | 내용 |
|--------|----------|------|
| 매일 오전 9시 스케줄러 | Spring Scheduler | D-30, D-7, D-Day 마감 알림 생성 |
| `business_years.status` → `IN_REVIEW` | 서비스 레이어 | 결재 요청 알림 (TAX_REVIEWER 대상) |
| `approval_lines.status` → `REJECTED` | 서비스 레이어 | 반려 알림 (작성자 대상) |
| `tax_law_versions.status` → `ACTIVE` | 서비스 레이어 | 법령 개정 알림 (담당 법인 작업자 대상) |
| `access_delegations` INSERT | 서비스 레이어 | 위임 수신 알림 (피위임자 대상) |

### 9.4 성능 고려사항

| 항목 | 방안 |
|------|------|
| `v_user_effective_customer_access` 조회 | Redis 권한 캐시 우선 사용 (TTL 5분), 캐시 미스 시 DB 조회 |
| 대시보드 summary API | 서버 사이드에서 5개 섹션 병렬 조회 (`CompletableFuture`) 후 병합 |
| KPI 차트 데이터 | 별도 TTL 3600초 캐시 (변경 빈도 낮음) |
| 최근활동 | 조회 전용 읽기 복제본(Read Replica) 사용 권장 |
| SSE 연결 | Nginx keep-alive 120초, `/api/notifications/stream` 전용 스레드 풀 분리 |

---

## 10. 역할별 표시 제어

### 10.1 섹션별 역할 가시성 매트릭스

| 섹션 | SUPER_ADMIN | TENANT_ADMIN | TAX_EXPERT | TAX_REVIEWER | ASSISTANT | VIEWER |
|------|:-----------:|:------------:|:----------:|:------------:|:---------:|:------:|
| 업무현황 | ● (전체) | ● (전체) | ● (본인 담당) | ● (본인 담당) | ● (본인 담당) | ● (읽기) |
| 신고마감 임박 | ● (전체) | ● (전체) | ● (본인 담당) | ● (본인 담당) | ● (본인 담당) | ● (읽기) |
| 알림 탭 | ● | ● | ● | ● | ● | ● |
| 결재 대기 탭 | – | – | – | ● (본인 결재) | – | – |
| 최근활동 | ● (전체) | ● (전체) | ● (접근 법인) | ● (접근 법인) | ● (접근 법인) | ● (읽기) |
| 핵심지표 | ● (전체) | ● (전체) | ● (담당 법인) | ● (담당 법인) | – | – |

### 10.2 역할별 카드 1(작성중) 필터 차이

| 역할 | `WHERE` 조건 추가 |
|------|-----------------|
| TAX_EXPERT | `AND acc.grade IN ('OWNER', 'CO_WORKER')` |
| ASSISTANT | `AND acc.grade IN ('ASSISTANT')` AND `AND ta.module_code IS NOT NULL` |
| TAX_REVIEWER | 카드 3(결재 대기) 위주, 카드 1은 자신이 요청받은 건만 |
| TENANT_ADMIN | 테넌트 전체 (본인 접근 제한 없음) |

---

## 11. 프론트엔드 구현 상세

### 11.1 컴포넌트 구조

```
src/
└── pages/
    └── Dashboard/
        ├── index.tsx                   ← 대시보드 페이지 루트
        ├── DashboardLayout.tsx         ← 그리드 레이아웃
        ├── sections/
        │   ├── WorkStatusSection.tsx   ← 섹션 1 업무현황
        │   ├── FilingDeadlineSection.tsx ← 섹션 2 신고마감
        │   ├── NotificationSection.tsx ← 섹션 3 알림/결재대기함
        │   ├── RecentActivitySection.tsx ← 섹션 4 최근활동
        │   └── KpiSection.tsx          ← 섹션 5 핵심지표
        ├── components/
        │   ├── WorkStatusCard.tsx      ← 카드 1개 단위
        │   ├── DeadlineRow.tsx         ← 마감 행 1개 단위
        │   ├── NotificationItem.tsx    ← 알림 항목 1개
        │   ├── ApprovalItem.tsx        ← 결재 대기 항목 1개
        │   ├── ActivityFeedItem.tsx    ← 활동 피드 항목 1개
        │   ├── TaxBurdenChart.tsx      ← Chart.js 꺾은선
        │   ├── IndustryDonutChart.tsx  ← Chart.js 도넛
        │   └── LossExpiryBarChart.tsx  ← Chart.js 바 차트
        └── hooks/
            ├── useDashboardSummary.ts  ← RTK Query 훅 (summary API)
            ├── useNotificationSSE.ts   ← SSE 연결 관리 훅
            └── useApprovalAction.ts    ← 승인/반려 mutation 훅
```

### 11.2 RTK Query 슬라이스

```typescript
// dashboardApi.ts
export const dashboardApi = createApi({
  reducerPath: 'dashboardApi',
  baseQuery: fetchBaseQuery({ baseUrl: '/api' }),
  tagTypes: ['DashboardSummary', 'Notifications', 'Approvals'],
  endpoints: (builder) => ({
    getDashboardSummary: builder.query<DashboardSummaryDto, void>({
      query: () => '/dashboard/summary',
      providesTags: ['DashboardSummary'],
    }),
    getNotifications: builder.query<NotificationListDto, { unreadOnly?: boolean }>({
      query: ({ unreadOnly = false }) =>
        `/dashboard/notifications?unreadOnly=${unreadOnly}`,
      providesTags: ['Notifications'],
    }),
    markNotificationRead: builder.mutation<void, string>({
      query: (id) => ({ url: `/notifications/${id}/read`, method: 'PATCH' }),
      invalidatesTags: ['Notifications', 'DashboardSummary'],
    }),
    approveBusinessYear: builder.mutation<void, string>({
      query: (approvalLineId) => ({
        url: `/approval-lines/${approvalLineId}/approve`,
        method: 'PATCH',
      }),
      invalidatesTags: ['DashboardSummary', 'Approvals'],
    }),
    rejectBusinessYear: builder.mutation<{ approvalLineId: string; reason: string }, void>({
      query: (body) => ({
        url: `/approval-lines/${body.approvalLineId}/reject`,
        method: 'PATCH',
        body: { reason: body.reason },
      }),
      invalidatesTags: ['DashboardSummary', 'Approvals'],
    }),
  }),
});
```

### 11.3 SSE 훅

```typescript
// useNotificationSSE.ts
export function useNotificationSSE() {
  const dispatch = useAppDispatch();

  useEffect(() => {
    const eventSource = new EventSource('/api/notifications/stream', {
      withCredentials: true,
    });

    eventSource.onmessage = (event) => {
      const notification = JSON.parse(event.data);
      dispatch(dashboardApi.util.invalidateTags(['Notifications', 'DashboardSummary']));
      // 토스트 알림 표시
      toast.info(notification.title);
    };

    eventSource.onerror = () => {
      eventSource.close();
      // 30초 후 재연결 (폴백)
      setTimeout(() => { /* reconnect */ }, 30_000);
    };

    return () => eventSource.close();
  }, [dispatch]);
}
```

### 11.4 라우팅 연계 (클릭 시 이동 경로)

| 클릭 위치 | 이동 경로 (React Router) |
|----------|------------------------|
| 업무현황 카드 1 (작성중) | `/workspace?status=DRAFT` |
| 업무현황 카드 2 (검증 대기) | `/workspace?status=IN_REVIEW&stage=validation` |
| 업무현황 카드 3 (결재 대기) | `/workspace?status=IN_REVIEW&stage=approval` |
| 업무현황 카드 4 (승인 완료) | `/workspace?status=APPROVED` |
| 업무현황 카드 5 (신고 완료) | `/post-filing/history` |
| 신고마감 임박 행 | `/workspace/start?businessYearId={id}` |
| 결재 대기 항목 | `/workspace/{businessYearId}/approval` |
| 알림 항목 | `notification.referenceUrl` (동적) |
| 최근활동 항목 | `activity.targetUrl` (동적, `audit_logs.entity_type` 기반 생성) |
| 신고 작업 시작 버튼 | `/workspace/start` (B-1 화면) |

---

## 12. 구현 우선순위 및 일정

### 12.1 단계별 구현 계획

| 단계 | 범위 | 예상 기간 | 비고 |
|------|------|----------|------|
| **Phase A** (기본) | 섹션 1 업무현황 + 섹션 2 신고마감 + `신고 작업 시작` 버튼 | 1주 | 가장 높은 업무 가치 |
| **Phase B** (알림) | 섹션 3 알림 탭 + `notifications` 테이블 생성 + 스케줄러 | 1주 | D-30 알림 정책 적용 |
| **Phase C** (결재) | 섹션 3 결재 대기 탭 + 인라인 승인·반려 + SSE | 1주 | `approval_lines` 연계 |
| **Phase D** (활동) | 섹션 4 최근활동 + `audit_logs` 연계 | 0.5주 | 읽기 전용 뷰 |
| **Phase E** (KPI) | 섹션 5 핵심지표 차트 3종 | 1주 | Chart.js 통합 |
| **Phase F** (최적화) | SSE 안정화, 캐시 튜닝, 반응형 레이아웃 | 0.5주 | 성능 검증 |

### 12.2 의존성

```
Phase A ──► Phase B ──► Phase C ──► Phase F
Phase A ──► Phase D ──► Phase F
Phase A ──► Phase E ──► Phase F
```

### 12.3 검증 항목

| 검증 항목 | 기준 |
|----------|------|
| 업무현황 카드 수치 정확성 | `business_years.status` 집계 결과와 일치 여부 |
| 마감 D-30 알림 자동 생성 | 스케줄러 실행 후 `notifications` 레코드 생성 확인 |
| 결재 대기 인라인 승인 | `approval_lines.status = 'APPROVED'` 및 `business_years.status = 'APPROVED'` 전환 확인 |
| 테넌트 격리 | 다른 테넌트 사용자의 데이터가 노출되지 않는지 확인 |
| 역할 가시성 | ASSISTANT 로그인 시 결재 대기 탭이 숨겨지는지 확인 |
| SSE 재연결 | 서버 재시작 후 30초 이내 클라이언트 재연결 확인 |
| 캐시 무효화 | 결재 승인 후 업무현황 카드 수치가 60초 이내 갱신되는지 확인 |

---

## 문서 변경 이력

| 버전 | 일자 | 작성자 | 변경내용 |
|------|------|--------|----------|
| 1.0 | 2026-05-22 | 강수 | 최초 작성 — 업무흐름.md + 설계서 기반 대시보드 5개 섹션 세부 구현 계획 |

---

**문의**: Kang-Soo.Cho@kr.ey.com
**연관 문서**:
- `법인세_세무조정계산서_시스템_설계서.md` (메인 설계서)
- `업무흐름.md` (업무 흐름 및 화면 구성)
- `DB설계.md` (DB 상세)
