# 추가 구현 계획 v1.6
## 화면 typology 도입 + 그리드 UX 정리 (`+ 추가` 인라인, `행 액션` 패널 제거)

> 작성일: 2026-05-18 · 작성자: 강수 (Kang-Soo.Cho@kr.ey.com)
> 선행 문서: `법인세_세무조정계산서_시스템_설계서.md`, `2차구현계획.md`, `메뉴기능수정.md`, `추가구현_v1.0~v1.5.md`
> 본 문서의 범위:
> 1) 100개 leaf가 **동일한 5블록 workbench**(KPI 카드 + 검색/필터 + 표 + 행 액션 + 추가 툴바)로 보이는 현 상태를 **기능에 맞는 화면 typology**로 분기.
> 2) **그리드형 화면**은 `+ 추가` 버튼을 패널 상단 오른쪽(`panel-head`)에 인라인 배치.
> 3) **`행 액션` 패널**(부가 정보만 표시)은 제거하고, 행 단위 액션은 표 마지막 컬럼의 인라인 버튼으로만 노출.
> 4) 각 화면의 **`수정` 버튼 클릭 시 실제 수정 모달/폼이 열리고 저장 API까지 정상 작동**하도록 공통 계약을 정의.

---

## 1. 증상 진단

### 1.1 모든 leaf가 똑같이 보인다
- `frontend/app/screens.js`의 `renderLeafTemplate(state)`(line 519~530)이 모든 leaf에 대해 다음 5블록을 동일 순서로 렌더한다.
  ```
  [Leaf workbench]  [검색/필터]  [목록 표]  [행 액션 패널]  [작업 + 추가]
  ```
- 입력해야 할 데이터 모양이 전혀 다른 leaf(예: `dashboard:overview`, `ws/file:precheck`, `admin/law:impact`)에서도 같은 표 + 같은 KPI 카드 + 같은 “행 액션” 패널이 나와 사용자가 “화면이 다 똑같다”고 느낀다.

### 1.2 `+ 추가` 버튼 위치가 어색
- 현재 `renderLeafToolbarBlock`(line 607~616)이 표 **아래**에 별도 패널을 두고 `+ 추가` 버튼을 표시.
- 사용자는 표 위에서 “이 표에 행을 추가하는 버튼은 어디 있나?”를 찾게 됨.
- 설계서 §5.3 와이어프레임에서 가장 흔한 패턴(`[추가] [삭제] [엑셀가져오기]`)도 표 상단·우측이다.

### 1.3 `행 액션` 패널이 중복·잉여
- `renderLeafRowActionBlock`(line 598~605)은 “첫 행을 기준으로 수정/삭제 액션이 준비되었습니다.” 같은 안내 문구만 표시.
- 표 마지막 컬럼에 이미 `[수정] [삭제]` 버튼이 인라인으로 있는 상황에서 별도 안내 패널은 잡음이며 화면을 어지럽힌다.

---

## 2. 복구 목표

1. **화면 typology 6종**으로 leaf 100개를 재분류해, leaf 데이터 모양에 맞는 시각적 구성이 자동 적용되도록 한다.
2. 그리드 typology에서는 `+ 추가`가 **표 상단-오른쪽(panel-head)** 에 인라인 배치된다.
3. `행 액션` 패널은 제거. 행 단위 액션은 표의 마지막 컬럼 `[수정] [삭제] …`로만 노출.
4. 각 typology별 디자인 토큰(여백·아이콘·라벨 색)을 정의해 “화면이 데이터에 맞게 이쁘게” 보이도록 한다.
5. 본 변경이 v1.5의 데이터 드리븐 워크벤치를 깨지 않게, **spec.typology** 한 줄로 분기 가능하게 한다.
6. 모든 수정 진입점은 `spec.update`와 `spec.form`을 기준으로 **선택 행 조회 → prefill → 검증 → PATCH/PUT 저장 → 목록 갱신** 흐름을 동일하게 따른다.

---

## 3. typology 6종 정의

| typology | 데이터 모양 | 본문 구성 | 대표 leaf |
|---|---|---|---|
| **grid** | 표 위주 CRUD | 상단 KPI 1줄 + 표(검색·필터·`+ 추가` 우상단) + 페이지네이션 | `admin/cust:list`, `admin/sec:users`, `ws/info:assets`, `ws/adj:B1`(그리드형 17조정) |
| **dashboard** | 5개 KPI + 보조 카드 다발 | 상단 5 KPI + 2~3개 카드(결재함/알림/최근활동) — 표 없음 | `dashboard:overview`, `dashboard:duesoon`, `dashboard:inbox`, `dashboard:recent` |
| **wizard** | 단계형(Step 1~4) | 단계 표시줄 + 단일 단계 본문 + “다음/이전” | `ws/file:precheck`, `ws/file:generate`, `ws/file:submit`, `ws/file:done` |
| **form** | 폼/모달 단건 입력 | 좌 폼 + 우 미리보기 | `ws/start:by-new`(폼만), `post/amend:unlock`(잠금해제 사유 폼), `admin/cacc:delegate`(위임 폼) |
| **chart** | 차트 중심 보고서 | 상단 KPI 1줄 + 라인/막대/도넛 차트 + 표(보조) | `report:tax-burden`, `report:year-compare`, `report:reserve-trend`, `report:loss-expiry`, `dashboard:kpi-tax` |
| **detail** | 단일 객체 상세 | 헤더(객체 메타) + 섹션 카드들(법령/서식/이벤트) | `ws/start:snapshot`, `post/hist:detail`, `admin/law:snapshots`, `admin/law:history` 일부 |

> 추가로 `grid+sidebar`(2-column: 좌 트리 + 우 표) 변종을 typology = `grid-tree`로 둔다(`admin/sec:menus`, `admin/code:manage`).

`spec.typology`(또는 v1.5 명세에서 `layout`/`shape`)가 위 6종 중 하나를 선택하면 `renderLeafScreen`이 해당 typology의 렌더러로 분기한다.

---

## 4. 변경 코드 골격

### 4.1 디스패치
```js
const TYPOLOGY_RENDERERS = {
  grid:       renderTypologyGrid,
  "grid-tree": renderTypologyGridTree,
  dashboard:  renderTypologyDashboard,
  wizard:     renderTypologyWizard,
  form:       renderTypologyForm,
  chart:      renderTypologyChart,
  detail:     renderTypologyDetail,
};

async function renderLeafScreen(env, key) {
  const spec = leafScreenSpecs[key];
  const data = await loadPrimary(env, spec);
  const state = { env, key, spec, meta: enrichMeta(env, key), rows: data };
  const renderer = TYPOLOGY_RENDERERS[spec.typology] || renderTypologyGrid;
  env.outlet.innerHTML = renderer(state);
  bindLeaf(state, spec.typology);
}
```

### 4.2 그리드 typology — `+ 추가` 인라인 + `행 액션` 패널 제거

```js
function renderTypologyGrid(state) {
  return `
    ${renderFlowChrome(state)}                       // workspace 한정
    ${renderKpiRow(state)}                           // 상단 1줄
    ${renderGridPanel(state)}                        // 표 + 인라인 + 추가
  `;
}

function renderGridPanel(state) {
  const { rows, columns, spec } = state;
  return `
    <section class="panel leaf-grid" data-leaf-key="${escapeHtml(state.key)}">
      <div class="panel-head">
        <div>
          <h2>${escapeHtml(spec.title || state.meta.title)}</h2>
          <p class="empty">${escapeHtml(spec.description || `${rows.length}건`)}</p>
        </div>
        <div class="panel-head-actions">
          ${renderGridFilters(spec)}                 // 검색·필터(컴팩트)
          <button class="primary-btn compact" data-leaf-create="${escapeHtml(state.key)}">+ 추가</button>
        </div>
      </div>
      <div class="table-wrap">
        <table>
          <thead>
            <tr>
              ${columns.map((c) => `<th>${escapeHtml(c.label)}</th>`).join("")}
              <th class="row-actions-th">관리</th>
            </tr>
          </thead>
          <tbody data-leaf-table-body>${renderTableRows(state)}</tbody>
        </table>
      </div>
      ${rows.length ? renderPagination(state) : renderEmptyHint(spec)}
    </section>`;
}
```

핵심 변경 4가지:
1. `+ 추가`를 표가 들어 있는 **같은 panel의 panel-head 우측**(`.panel-head-actions`)으로 이동.
2. 별도 “작업/툴바” 패널 제거.
3. 별도 `행 액션` 패널(`renderLeafRowActionBlock`) 제거.
4. 행 단위 액션은 표 마지막 컬럼 “관리”에만 노출(아이콘 버튼 `[✎ 수정] [🗑 삭제]`).

### 4.3 dashboard typology — 카드 다발
```js
function renderTypologyDashboard(state) {
  return `
    <section class="dashboard-grid">
      ${state.spec.kpis.map(kpiCard).join("")}
    </section>
    <section class="dashboard-secondary">
      ${state.spec.cards?.map(secondaryCard).join("") || ""}
    </section>`;
}
```
- 표 없음. KPI 카드 5장 + 결재함/알림/최근활동 카드 2~3장.
- `+ 추가`/`행 액션` 모두 없음.

### 4.4 wizard typology — 4-step
```js
function renderTypologyWizard(state) {
  const step = state.params?.step || state.spec.defaultStep || 1;
  return `
    <ol class="wizard-stepper">${[1,2,3,4].map((n)=>wizardStep(n, step)).join("")}</ol>
    <section class="panel">${state.spec.steps[step - 1].render(state)}</section>
    <div class="wizard-nav">
      <button class="secondary-btn" data-wizard-prev>◀ 이전</button>
      <button class="primary-btn" data-wizard-next>다음 ▶</button>
    </div>`;
}
```
- 본문은 단계별로 다른 폼/표/요약 카드.
- `+ 추가` 대신 단계별 액션 버튼(예: “파일 생성”, “제출”).

### 4.5 form typology — 좌 폼 + 우 미리보기
```js
function renderTypologyForm(state) {
  return `
    <section class="grid two">
      <article class="panel">${renderForm(state.spec.form, state.row || {})}</article>
      <article class="panel">${renderPreview(state)}</article>
    </section>`;
}
```
- “저장/취소” 버튼이 폼 우측 하단.
- `+ 추가` 없음(이미 폼 자체가 단건 등록/편집).
- `행 액션` 없음.

### 4.6 chart typology — 차트 중심
```js
function renderTypologyChart(state) {
  return `
    ${renderKpiRow(state)}
    <section class="panel chart-panel">
      <div class="panel-head">
        <h2>${escapeHtml(state.spec.title)}</h2>
        <select data-chart-range>${["3y","5y","10y"].map(r=>`<option>${r}</option>`).join("")}</select>
      </div>
      <div class="chart-area" data-chart-target></div>
      ${renderChartTable(state)}
    </section>`;
}
```
- 본문 = 차트 영역(Chart.js) + 보조 표.
- `+ 추가` 없음.
- 행 단위 액션 없음(읽기 전용 보고서).

### 4.7 detail typology — 객체 상세
```js
function renderTypologyDetail(state) {
  return `
    ${renderObjectHeader(state)}
    <section class="grid two">
      <article class="panel">${renderMetaCard(state)}</article>
      <article class="panel">${renderRelatedList(state)}</article>
    </section>`;
}
```
- 객체 헤더(타이틀·상태 pill·잠금 아이콘) + 메타 카드 + 관련 목록.
- `+ 추가`는 “관련 목록”에 있을 수도/없을 수도.

### 4.8 grid-tree typology — 2-column(좌 트리, 우 표)
```js
function renderTypologyGridTree(state) {
  return `
    <section class="grid layout-tree-and-grid">
      <aside class="panel tree-panel">${renderTree(state.spec.tree, state.tree)}</aside>
      <article class="panel">${renderGridPanel(state)}</article>
    </section>`;
}
```
- 좌측 트리(메뉴, 코드 그룹) + 우측 grid typology 동일 패널.

---

## 5. 100개 leaf의 typology 매핑

> 표가 길어 그룹별 요약. 미지정은 `grid`가 기본.

### 5.1 ② 신고 작업
| leaf | typology |
|---|---|
| `ws/start:customer-pick` | grid |
| `ws/start:by-pick` | grid |
| `ws/start:snapshot` | detail |
| `ws/info:fs` | detail (BS·IS·CF 탭) |
| `ws/info:mapping` | grid |
| `ws/info:assets` | grid |
| `ws/info:transactions` | grid |
| `ws/info:vehicle` | grid |
| `ws/info:consistency` | detail (검증 카드) |
| `ws/adj:B1`~`B17` | grid (인라인 편집) |
| `ws/form:form3` | detail |
| `ws/form:attachments` | grid |
| `ws/form:preview` | detail |
| `ws/form:linkage` | grid |
| `ws/val:run` | wizard (실행→결과) |
| `ws/val:issues` | grid |
| `ws/val:rules` | grid |
| `ws/appr:request` | form |
| `ws/appr:inbox` | grid |
| `ws/appr:rejected` | grid |
| `ws/print:preview` | detail |
| `ws/print:bulk` | grid (체크박스) |
| `ws/print:history` | grid |
| `ws/file:precheck/generate/submit/done` | wizard (4-step 공유) |

### 5.2 ③ 사후관리
| leaf | typology |
|---|---|
| `post/hist:list` | grid |
| `post/amend:unlock` | form |
| `post/amend:version` | form |
| `post/amend:diff` | detail (Before/After) |
| `post/amend:resubmit` | wizard |
| `post/correction` | form |

### 5.3 ④ 보고서
| leaf | typology |
|---|---|
| `report:year-compare` | chart |
| `report:tax-burden` | chart |
| `report:reserve-trend` | chart |
| `report:loss-expiry` | chart |
| `report:industry-stats` | chart |
| `report:custom` | form (정의) + grid (결과) |

### 5.4 ① 대시보드
| leaf | typology |
|---|---|
| `dashboard:overview` | dashboard |
| `dashboard:duesoon` | dashboard |
| `dashboard:inbox` | dashboard |
| `dashboard:recent` | dashboard |
| `dashboard:kpi-tax` | chart |

### 5.5 ⑤ 관리
| leaf | typology |
|---|---|
| `admin/tenant:list` | grid |
| `admin/cust:*` | grid |
| `admin/sec:users/roles/functions/mask/scope` | grid |
| `admin/sec:matrix` | grid (메뉴×기능 체크박스 행렬) |
| `admin/sec:menus` | grid-tree |
| `admin/cacc:*` | grid (assign/groups/rules/override) + form (delegate) |
| `admin/law:master/rates/limits/credits/depr-lives/sme/loss-rule/history` | grid |
| `admin/law:snapshots` | detail |
| `admin/law:impact` | wizard (대상 선택 → 시뮬레이션) |
| `admin/form:master/versions/validations/linkage-rule/efile-map` | grid |
| `admin/form:fields/field-ref` | grid-tree |
| `admin/form:migration` | wizard (드라이런/실행/롤백) |
| `admin/form:by-set` | detail |
| `admin/form:impact` | wizard |
| `admin/code:manage` | grid-tree |
| `admin/audit:events/login/perm/settings` | grid |

---

## 6. 데이터 “이쁘게” 보이기 — 컬럼 포맷 규약

`spec.columns[i].format` 키워드를 도입해 셀 렌더링을 일관화.

| format | 렌더 |
|---|---|
| `money` | 천단위 콤마 + 우측 정렬 + `원` 표기 |
| `bps` | `bps`를 `%`로 변환(예: 2500 → 25.00%) |
| `pct` | 0~100 정수 → `%` |
| `date` | `YYYY-MM-DD` |
| `datetime` | `YYYY-MM-DD HH:mm` (12시간 표기는 i18n) |
| `biz` | `123-45-67890` 사업자번호 |
| `corp` | `110111-1234567` 법인등록번호 |
| `tags` | 배열을 색칠된 chip으로 |
| `status` | 상태별 pill (DRAFT/IN_REVIEW/APPROVED/FILED/AMENDED) |
| `severity` | ERROR=빨강, WARN=주황, INFO=파랑 |
| `link` | `<a>` 클릭 시 다른 leaf로 hash 이동 |
| `boolean` | ✓ / ✗ |
| `progress` | 0~100 진행률 바 |
| `code` | monospace + 회색 박스 |
| `email/phone` | 마스킹 정책 적용 후 표시 |
| `actions` | 마지막 컬럼 전용 — `[✎ 수정] [🗑 삭제] [▶ 상세]` 아이콘 버튼 |

> 표 마지막 컬럼은 자동으로 `actions`로 지정되며, `+ 추가`는 panel-head로 이동.

---

## 7. CSS 토큰

`frontend/app.css`에 typology별 토큰 추가:

```css
.panel-head { display:flex; justify-content:space-between; align-items:flex-start; }
.panel-head-actions { display:flex; gap:8px; align-items:center; }
.panel-head-actions input[type="search"] { width:200px; }
.panel-head-actions select { min-width:120px; }

.row-actions-th { width: 130px; text-align:right; }
td.row-actions { text-align:right; }
td.row-actions button { padding: 2px 8px; }

.dashboard-grid { display:grid; grid-template-columns: repeat(5, 1fr); gap:12px; }
.dashboard-secondary { display:grid; grid-template-columns: repeat(3, 1fr); gap:12px; margin-top:16px; }

.wizard-stepper { display:flex; gap:0; list-style:none; padding:0; margin: 0 0 12px; }
.wizard-stepper li { flex:1; padding:8px 12px; border-bottom:2px solid var(--slate-200); }
.wizard-stepper li.active { border-color: var(--indigo-500); font-weight:600; }
.wizard-stepper li.done { border-color: var(--emerald-500); color: var(--slate-500); }

.chart-panel .chart-area { height: 280px; }
.layout-tree-and-grid { grid-template-columns: 280px 1fr; gap:12px; }

/* 제거: .leaf-row-actions-panel, .leaf-toolbar (독립 패널) */
```

---

## 8. 변경 파일 매트릭스

| 분류 | 파일 | 변경 |
|---|---|---|
| 프론트 코어 | `frontend/app/screens.js` | `renderLeafTemplate` → typology 디스패치로 교체. `renderLeafToolbarBlock`·`renderLeafRowActionBlock` 삭제. `renderGridPanel`이 검색/필터·추가 버튼을 panel-head로 흡수 |
| 프론트 액션 | `frontend/app/screens.js` 또는 `frontend/app/components/leaf/actions.js` | `bindLeafRowActions`, `openEditModal`, `submitUpdate`, `refreshLeafRows` 공통화. `[수정]` 버튼 클릭 시 선택 행 데이터로 폼 prefill |
| 프론트 스펙 | `frontend/app/screens.js` 내 `leafScreenSpecs` | leaf마다 `typology` + `columns[].format` + `kpis` 필드 추가 |
| 프론트 스타일 | `frontend/app.css` | typology 토큰·패널 헤더 액션 영역·행 액션 컬럼 스타일 |
| 컴포넌트 | `frontend/app/components/leaf/{grid,wizard,form,dashboard,chart,detail,gridtree}.js` (선택 분리) | 향후 분리할 경우 1차 골격 |
| 백엔드 | `src/modules.rs` | 메뉴 노드에 `typology` 힌트 필드 추가(기본값 grid) |
| 마이그레이션 | `2026052X000700_menu_typology.sql` | `menu_nodes.typology` 컬럼 추가, 100 leaf 기본값 시드 |
| 테스트 | `tests/frontend/typology.spec.js`, `tests/frontend/edit_actions.spec.js` | 각 typology가 올바른 DOM 시그너처(`data-typology`)를 출력하는지, 대표 leaf의 수정 버튼이 모달/저장/목록 갱신까지 통과하는지 |

---

## 9. 단계별 작업 (3일)

### D1 — typology 디스패치 + grid 정리 (가장 시급)
1. `renderLeafTemplate` → typology 디스패치 분기 도입.
2. **`renderLeafRowActionBlock` 삭제** + `renderLeafToolbarBlock` 삭제.
3. `renderGridPanel`이 검색·필터·`+ 추가`를 panel-head 우측에 인라인 배치.
4. 표 마지막 컬럼 “관리”(아이콘 버튼)로 통일.
5. `[수정]` 버튼 이벤트 위임을 공통 바인더로 연결하고, 선택 행이 없거나 `spec.update`가 없는 경우 명확한 비활성/안내 상태를 표시.
6. `leafScreenSpecs`의 모든 leaf에 `typology: "grid"` 기본값 부여(스펙 미정의 leaf 폴백).
7. 회귀: 100 leaf 클릭 → “행 액션 패널 없음 + `+ 추가`가 표 우상단 + 수정 버튼 클릭 가능”.

### D2 — dashboard / chart / detail typology
1. `renderTypologyDashboard` 구현 + `dashboard:*` 5 leaf 스펙 갱신.
2. `renderTypologyChart` 구현(Chart.js CDN 임시) + `report:*` 5 leaf, `dashboard:kpi-tax` 갱신.
3. `renderTypologyDetail` 구현 + `ws/start:snapshot`, `ws/form:form3`, `post/amend:diff`, `admin/law:snapshots` 갱신.
4. 컬럼 포맷 `money/bps/date/status/tags/severity/biz/corp/code/progress` 12종 헬퍼 작성.

### D3 — wizard / form / grid-tree + 회귀
1. `renderTypologyWizard` 구현 + `ws/file:precheck~done` 4 leaf, `ws/val:run`, `admin/law:impact`, `admin/form:migration/impact`.
2. `renderTypologyForm` 구현 + `ws/appr:request`, `post/amend:unlock/version`, `post/correction`, `admin/cacc:delegate`.
3. `renderTypologyGridTree` 구현 + `admin/sec:menus`, `admin/form:fields/field-ref`, `admin/code:manage`.
4. `tests/frontend/typology.spec.js` 작성.
5. 수동 시연: 6 typology 대표 leaf 6개 캡처 → 시각적 차이 확인.

---

## 10. 수정 버튼 정상 작동 계획

### 10.1 공통 수정 버튼 DOM 계약
표 마지막 컬럼의 수정 버튼은 모든 grid/grid-tree 화면에서 같은 data 속성을 가진다.

```html
<button
  type="button"
  class="icon-btn"
  data-row-edit
  data-leaf-key="admin/cust:list"
  data-row-id="CUST-001"
  aria-label="수정">
  ✎
</button>
```

- `data-row-id`는 `spec.rowKey` 값을 사용한다. `rowKey`가 없으면 `id`, `code`, `key` 순으로 fallback하되 dev 로그에 경고한다.
- 버튼은 별도 `행 액션` 패널에 두지 않고 표 마지막 컬럼 “관리” 안에만 둔다.
- `spec.update` 또는 수정 권한이 없으면 버튼을 렌더하지 않거나 `disabled` 처리하고, hover/title로 사유를 표시한다.

### 10.2 이벤트 바인딩 흐름
`bindLeaf(state, typology)` 내부에서 typology별 바인딩 후 공통 행 액션 바인딩을 마지막에 한 번 수행한다.

```js
function bindLeafRowActions(state) {
  state.env.outlet.addEventListener("click", async (event) => {
    const editButton = event.target.closest("[data-row-edit]");
    if (!editButton) return;

    const row = findRowById(state.rows, state.spec.rowKey, editButton.dataset.rowId);
    if (!row) return showToast("수정할 행을 찾을 수 없습니다.", "error");

    await openEditModal(state, row);
  });
}
```

핵심 원칙:
1. 행 번호(index)가 아니라 안정적인 `rowKey`로 선택 행을 찾는다.
2. 화면 재렌더 후 이벤트가 사라지지 않도록 outlet 단위 이벤트 위임을 사용한다.
3. 모달 열기 전 `spec.update`, `spec.form`, 권한을 한 번 더 검사한다.

### 10.3 수정 모달/폼 prefill
- `spec.form.fields`가 있으면 해당 필드 정의로 수정 폼을 만든다.
- `spec.form`이 없는 단순 grid는 `columns` 중 `editable !== false`인 컬럼으로 임시 폼을 구성한다.
- `readonly`, `computed`, `actions` 컬럼은 수정 폼에서 제외한다.
- `kind: "money" | "date" | "select" | "checkbox" | "textarea"` 등 기존 포맷과 입력 컴포넌트를 매핑한다.
- 모달 제목은 `{화면명} 수정`, 저장 버튼은 `저장`, 취소 버튼은 `취소`로 통일한다.

### 10.4 저장 API 계약
수정 저장은 `spec.update`를 단일 진입점으로 사용한다.

```js
async function submitUpdate(state, row, values) {
  const id = row[state.spec.rowKey];
  const endpoint = resolvePath(state.spec.update.path, { id, row, ctx: state.env.ctx });
  await api.request(endpoint, {
    method: state.spec.update.method || "PATCH",
    body: values,
  });
  await refreshLeafRows(state);
}
```

- 기본 메서드는 `PATCH`, leaf가 `PUT`을 요구하면 `spec.update.method = "PUT"`으로 지정한다.
- 저장 성공 후에는 모달을 닫고 목록을 재조회한다. 대용량 grid는 해당 행만 patch하는 최적화를 D3 이후 과제로 둔다.
- 저장 실패 시 모달을 닫지 않고 필드/상단 오류 메시지를 표시한다.
- 낙관적 업데이트는 1차 범위에서 제외하고, 서버 응답을 기준으로 화면을 갱신한다.

### 10.5 typology별 수정 동작

| typology | 수정 버튼 동작 |
|---|---|
| `grid` | 표 마지막 컬럼 `[수정]` → 모달 prefill → PATCH/PUT → 목록 재조회 |
| `grid-tree` | 우측 표의 `[수정]`은 grid와 동일. 좌측 트리는 노드 편집 버튼을 별도 `data-tree-edit`로 둔다 |
| `detail` | 객체 헤더 또는 섹션의 `수정` → 같은 폼 모달. 저장 후 detail 데이터 재조회 |
| `form` | 화면 자체가 수정 폼이면 별도 모달 없이 본문 저장. 목록형 결과가 있으면 grid 규칙 적용 |
| `wizard` | 단계별 수정 가능 항목만 `data-step-edit`로 연결. “다음/이전”과 충돌하지 않게 버튼 role 분리 |
| `chart` | 차트 설정 수정은 `data-chart-config-edit`로 분리. 데이터 행 수정이 있는 보조 표는 grid 규칙 적용 |
| `dashboard` | 일반적으로 수정 없음. 카드 설정이 있는 경우 카드별 `data-card-edit`로 별도 처리 |

### 10.6 테스트 계획
1. `tests/frontend/edit_actions.spec.js`를 추가해 대표 leaf 6개(`admin/cust:list`, `ws/adj:B1`, `admin/sec:menus`, `ws/start:snapshot`, `post/amend:unlock`, `report:custom`)를 검증한다.
2. 각 테스트는 `[수정]` 클릭 → 모달/폼 표시 → 기존 값 prefill → 값 변경 → 저장 → API 호출 method/path/body 확인 → 목록 또는 상세 재조회까지 본다.
3. 권한이 없는 사용자 fixture에서는 `[수정]` 버튼이 숨김/비활성 상태인지 확인한다.
4. `rowKey` 누락 fixture에서는 사용자 오류 대신 dev 경고와 안전한 비활성 처리가 되는지 확인한다.
5. 실패 응답(400/409/500)에서는 모달이 유지되고 오류 메시지가 보이는지 확인한다.

---

## 11. 회귀·점검 체크리스트

- [ ] 100개 leaf 어디에도 “행 액션” 제목 패널이 보이지 않는다.
- [ ] grid typology의 leaf에서는 표가 들어 있는 패널의 `panel-head` 우측에 검색·필터 + `+ 추가`가 함께 보인다.
- [ ] 표 마지막 컬럼은 “관리” 헤더 + `[✎][🗑]` 아이콘 행 액션이다.
- [ ] grid/grid-tree/detail의 `[수정]` 버튼을 클릭하면 선택 행/객체 값이 채워진 수정 모달 또는 폼이 열린다.
- [ ] 수정 저장 성공 후 모달이 닫히고 목록/상세 데이터가 서버 응답 기준으로 갱신된다.
- [ ] 수정 저장 실패 시 모달이 유지되고 오류 메시지가 표시된다.
- [ ] dashboard typology의 leaf는 표가 없고 KPI 카드 5장 + 보조 카드 2~3장이다.
- [ ] chart typology의 leaf는 본문 영역에 차트가 그려진다.
- [ ] wizard typology의 leaf는 단계 표시줄이 보이고 “다음/이전” 버튼이 우측 하단에 있다.
- [ ] form typology의 leaf는 좌 폼 + 우 미리보기 2-column이다.
- [ ] detail typology의 leaf는 객체 헤더 + 메타 카드 2개 이상이다.
- [ ] grid-tree typology의 leaf는 좌 트리 + 우 표 2-column이다.
- [ ] 컬럼에 `format: "money"`가 지정된 셀은 천단위 콤마와 우측 정렬을 가진다.

---

## 12. 위험 및 대응

| 위험 | 대응 |
|---|---|
| typology별 렌더러 추가로 `screens.js`가 비대화 | 1차로는 같은 파일에 함수만 추가, 안정화 후 `components/leaf/*.js`로 분리 |
| Chart.js CDN 의존 | 차트가 늦게 로드되더라도 표는 즉시 보이도록 차트 placeholder 우선 표시 |
| 마지막 컬럼 “관리”가 좁아 깨짐 | `.row-actions-th { width: 130px }` 고정 + 모바일에서 아이콘만 표시 |
| typology 미지정 leaf에서 폴백이 grid라 의도와 다를 수 있음 | leaf 스펙에 `typology` 누락된 leaf 목록을 dev 로그로 경고 |
| 기존 사용자 흐름(“행 액션 패널 클릭”)에 대한 도움말 깨짐 | `docs/menu_smoke_checklist.md`에 “행 액션은 표 안에서 보이도록 변경됨” 안내 추가 |
| leaf별 `rowKey`/`spec.update` 누락으로 수정 버튼이 실패 | 수정 버튼 렌더 전 계약 검사. 누락 leaf 목록을 테스트 실패와 dev 경고로 노출 |
| 재렌더 후 수정 버튼 이벤트가 끊김 | 개별 버튼 바인딩 대신 outlet 이벤트 위임으로 처리 |

---

## 13. 완료 정의 (DoD)

1. `renderLeafTemplate`이 `spec.typology`에 따라 6종 + grid-tree로 분기한다.
2. grid·grid-tree typology에서 `+ 추가` 버튼이 패널 상단 우측에 인라인 배치된다.
3. 어떤 leaf에서도 별도의 “행 액션” 패널이 보이지 않는다.
4. dashboard/chart/wizard/form/detail typology가 각각 다른 본문 시그너처(`data-typology`)를 출력한다.
5. 컬럼 포맷 12종(`money/bps/date/datetime/biz/corp/tags/status/severity/link/boolean/progress/code/email/phone/actions`)이 적용되어 표가 보기 좋다.
6. 각 typology의 수정 진입점이 정상 작동한다. 최소 grid/grid-tree/detail/form 대표 화면은 수정 클릭, prefill, 저장, 갱신, 오류 표시 테스트를 통과한다.
7. 100 leaf 회귀가 통과하고, 6 typology 대표 leaf 6장의 캡처가 `docs/typology_gallery.md`에 정리된다.
8. v1.5의 데이터 드리븐 워크벤치 모델·CRUD 동작·흐름 가시화(Stepper/LawBanner)가 그대로 유지된다.
