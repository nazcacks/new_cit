# 추가 구현 계획 v1.0
## (설계서 vs 현재 메뉴·기능 갭 분석 + 추가 구현 작업 계획)

> 작성일: 2026-05-18 · 작성자: 강수 (Kang-Soo.Cho@kr.ey.com)
> 기준 문서: `법인세_세무조정계산서_시스템_설계서.md` (v1.0)
> 비교 대상: `frontend/index.html`·`frontend/app/screens.js` (24개 메뉴) · `src/api.rs`(라우트) · `migrations/*` (DB) · `2차구현계획.md`·`메뉴기능수정.md`

---

## v1.5 동기화 메모

현재 구현 기준은 5개 루트 그룹, `admin/tenant:list` 포함 **100개 active leaf**, 그리고 v1.5 공통 leaf workbench다. 모든 leaf는 상단 KPI/제목, 검색/필터, 목록 표, 행 액션, `+ 추가` 버튼을 갖고, raw API 응답/JSON payload 표시 대신 업무 요약과 표를 렌더링한다.

## 1. 분석 방법

설계서를 다음 6개 영역으로 분해하고, 영역별 “설계서 정의 기능”을 항목으로 뽑은 뒤 각 항목을 **현재 구현 상태**와 매핑한다.

| 영역 | 설계서 챕터 |
|---|---|
| A. 법령·세율 버전 관리 | 3.2.0, 5-D, 5.3 F-1~F-5 |
| B. 인증/계정 | 3.2.1(인증), 5.2.1, 5.3 A |
| C. 시스템 관리/권한/메뉴 | 3.2.4, 5-B/5-C, 5.3 E-1~E-5 |
| D. 고객사·사업연도 | 3.2.2(고객), 5-A, 5.3 B-1 |
| E. 신고 작업 7단계 | 3.2.1/3.2.2/3.2.3/3.2.7/3.2.8, 5.2.2, 5.3 C / C-1~C-4 / D |
| F. 사후관리·분석/보고서·운영 | Phase 17~20, 5-③/5-④ |

상태 표기:
- ✅ 구현 완료(API+UI+데이터 흐름)
- ⚠️ 부분 구현(데이터/API는 있으나 UI·로직 미완)
- ❌ 미구현

---

## 2. 영역별 갭 분석

### 2.1 A. 법령·세율 버전 관리 (설계서 §3.2.0)

| 설계서 정의 기능 | 현 상태 | 근거 |
|---|---|---|
| 법령 버전 마스터(조회/등록/수정/승인) | ✅ | `POST/PUT /api/tax-laws*`, `ad-law` 화면 |
| 법인세율표(과세표준 구간별·시점별) | ✅ | `tax_rates`, `/api/tax-rates` |
| 한도·율표(접대비/기부금/대손/업무용차 등) | ✅ | `tax_limits`, `LIMIT` 카테고리 |
| 세액공제·감면 율표 | ⚠️ | `tax_limits`에 `CREDIT` 카테고리로 임시 저장. **전용 테이블 `tax_credit_versions` + 신성장/원천기술 가산 메타 부재** |
| 기준내용연수표 | ❌ | `DEPRECIATION_LIFE` 카테고리로 `tax_limits`에 임시. 자산 분류 ↔ 내용연수 ↔ 시점 매핑 부재 |
| 중소기업 판정기준(매출/자산/업종 조합) | ❌ | `SME_CRITERIA` 카테고리만 있고 업종·매출·자산 조합 평가 함수 없음 |
| 결손금 공제규정(연수/한도율 시점별) | ❌ | `LOSS_RULE` 카테고리만 있고 사업연도 결정 함수 없음 |
| 사업연도별 적용 스냅샷 | ✅ | `business_year_snapshots` + `…/snapshot` |
| **영향 시뮬레이션(고객사별 Before/After)** | ⚠️ | `POST /api/law-versioning/impact` 라우트는 있으나 UI(F-4)·실제 결과 산정 로직 미완 |
| **법령 변경 적용 워크플로**(등록→시뮬레이션→검토승인→활성화→공지→캐시무효화) | ❌ | 상태(DRAFT/REVIEWED/ACTIVE/RETIRED)만 있고 워크플로 단계·승인자·공지 발송 없음 |
| **개정 공지/이력(법조항·부칙·요약)** | ⚠️ | `law_amendment_history` 테이블 있으나 입력 UI·근거 첨부 없음 |
| **자동 회귀 테스트(버전별 표준 케이스)** | ❌ | 회귀 테스트 모음 미존재 |
| **계산 캐시 무효화 알림(미신고 BY)** | ❌ | 미신고분에 “재계산 권장” 알림 트리거 없음 |
| **근거 PDF 일괄 다운로드(F-5)** | ❌ | 스냅샷 PDF 미구현 |

### 2.2 B. 인증/계정 (설계서 §3.2.1, §5.2.1, §5.3 A)

| 기능 | 상태 | 근거 |
|---|---|---|
| 로그인(아이디·비밀번호) | ✅ | `/api/auth/login` |
| **2단계 인증 (TOTP 6자리)** | ⚠️ | `users.totp_secret/use_2fa` 컬럼 존재, 인증 흐름은 미강제. prototype 화면(`Login.otp`) UI 모방만 |
| **IP 화이트리스트 검증** | ❌ | `tenants.allowed_ips` 컬럼만 있고 미들웨어 enforce 없음 |
| **로그인 실패 5회 시 계정 잠금** | ⚠️ | `pwd_fail_count/locked` 컬럼 있으나 카운팅·잠금 로직 미확인 |
| **세션/리프레시 토큰** | ✅ | `auth_sessions` |
| **2FA Step-up(전자신고 제출 시)** | ❌ | 전자신고 `POST /efilings`에 재인증 게이트 없음 |
| **자동 로그아웃(아이들 타임아웃)** | ❌ | 미구현 |
| **로그인 이력(누가/언제/어디서)** | ⚠️ | `login_history` 테이블 있으나 `5-H 감사·로그`에서 노출 부족 |

### 2.3 C. 시스템 관리/권한/메뉴 (설계서 §3.2.4, §5.3 E-1~E-5)

| 기능 | 상태 | 근거 |
|---|---|---|
| 사용자 관리(목록·등록·잠금·2FA 리셋) | ✅ | `/api/admin/tenants/:tc/users…` |
| 역할 마스터 + 역할별 권한 매트릭스 | ⚠️ | `roles`, `role_permissions` 존재. **메뉴 × 12종 기능(READ/CREATE/UPDATE/DELETE/PRINT/EXPORT/IMPORT/APPROVE/SUBMIT/UNLOCK/LOG_VIEW/MASK_OFF)** 전체 매핑 UI 없음. 현재 `5-C`는 단순 매트릭스 표시. E-2 와이어프레임 미구현 |
| **메뉴 관리(트리/순서/활성 기능)** | ⚠️ | `menu_nodes` + `GET/PUT /api/admin/menus`. **메뉴별 활성 기능 체크박스(E-3)·순서 이동 UI** 없음 |
| **기능(Function) 코드 마스터** | ❌ | 12종 코드를 시드한 별도 마스터 테이블 없음(상수만 분산) |
| **필드 마스킹 정책** | ❌ | 주민등록번호·사업자번호·계좌번호 마스킹 정책·`MASK_OFF` 권한 없음 |
| **데이터 권한(Data Scope: ALL/ASSIGNED/OWNED/NONE)** | ❌ | API에서 row-level filter 미적용 |
| **사용자별 담당 법인 등급(OWNER/CO_WORKER/REVIEWER/ASSISTANT/VIEWER/BLOCKED)** | ⚠️ | `user_customer_access.access_level` 컬럼은 있으나 6단계 명시 enforcement 부족 |
| **고객사 그룹·자동 할당 룰** | ❌ | `customer_groups`, `customer_access_rules` 미존재 |
| **권한 위임/대행(휴가 위임 기간 한정)** | ❌ | `access_delegations` 미존재 |
| **권한 평가 우선순위(DENY > ALLOW, 사용자 > 역할)** | ⚠️ | API에서 균일하게 평가하지 않음. 도메인 함수 부재 |
| **세션 캐시 무효화(Redis pub/sub)** | ❌ | Redis 미사용. 권한 변경 즉시 반영 없음 |
| **권한 부여/회수 워크플로** | ❌ | UI/저장 모두 없음 |
| **감사로그 해시체인(hash_current)** | ⚠️ | 컬럼 존재, 검증 도구·일별 무결성 보고 없음 |

### 2.4 D. 고객사·사업연도 (설계서 §3.2.2(고객), §5.3 B-1)

| 기능 | 상태 | 근거 |
|---|---|---|
| 고객사 CRUD | ✅ | `…/customers` |
| 사업연도 CRUD | ✅ | `…/business-years` |
| **세무대리 계약(tax_agents)** | ⚠️ | `tax_agents` 스키마는 있으나 UI·API 미연결 |
| **사업연도 스냅샷 적용 법령·서식 표시(B-1 하단)** | ⚠️ | `snapshot` 응답은 JSON dump만, “법인세율 v2025-01 / 접대비한도 …” 라벨 매핑 없음 |
| **고객사별 대상 업무 범위(`work_scopes`) 8종 운영 UI** | ⚠️ | 컬럼 있으나 고객사 편집 폼에 체크박스 없음 |
| **소속 테넌트 컬럼/필터(`5-A`)** | ⚠️ | 단일 테넌트 컨텍스트라 컬럼은 부재 |

### 2.5 E. 신고 작업 7단계 (설계서 §3.2 + §5.2.2)

#### E-1. 세무정보 입력 (단계 1, 설계서 §3.2.1)

| 기능 | 상태 |
|---|---|
| 재무제표 CSV/XLSX 업로드 | ✅ |
| **회계 ERP 연동(더존/SAP/Oracle EBS API)** | ❌ |
| **표준 템플릿 다운로드(자료유형별)** | ⚠️ (`GET /api/tenants/:tc/tax-data/templates/:type` 있으나 내용 미검증) |
| 계정과목 매핑 | ✅ |
| **학습형 매핑(1회 후 자동 적용)** | ❌ |
| 자산대장 / 거래 명세 / 차량 운행기록 | ✅ |
| **12개월 미만 사업연도 월할 계산** | ❌ |
| 일관성 검증(차변=대변, 잔액) | ✅ |
| **자료 임포트 배치 + 오류 행 다운로드(엑셀)** | ⚠️ |

#### E-2. 세무조정 17개 모듈 (단계 2, 설계서 §3.2.2)

| 공통 기능 | 상태 |
|---|---|
| 17개 모듈 백엔드 엔진 | ✅ |
| **Excel-like 입력 그리드(복사·붙여넣기·다중행·단축키)** | ❌ (현재 “실행” 버튼만 있음) |
| **모듈별 Excel 임포트 + 표준 템플릿** | ❌ |
| **전기 이월 자동 복사**(자산대장·유보·결손금) | ⚠️ (자산·유보 일부만, 일괄 트리거 없음) |
| **자동계산 근거 툴팁(법조항·근거 문구)** | ❌ |
| **유보 자동 매핑(B-1~B-14 → B-15 갑/을)** | ✅ |
| **행 단위 변경 이력(누가/언제)** | ❌ |
| **행 단위 잠금/마감** | ❌ |
| **검증 오류 위치로 즉시 이동** | ⚠️ (이슈 패널에 “이동” 버튼 미연결) |
| **행 단위 메모/첨부** | ❌ |
| **법령 버전 자동 적용 배너(LawBanner)** | ⚠️ (`renderLawBanner`는 있으나 라벨 매핑 미완) |
| **법령 변경 “재계산 권장” 노란 배너** | ❌ |

| 모듈별 미흡 항목 | 상태 |
|---|---|
| B-3 접대비: **신용카드 미사용분 자동 분개**(건당 3만원/경조사비 20만원) | ⚠️ |
| B-4 감가상각: **자산 분류별 신고내용연수 ±25% 범위·즉시상각의제** | ⚠️ |
| B-5 퇴직급여: **DC/DB 구분, 누적 손금산입 부담금 추적** | ⚠️ |
| B-9 지급이자: **가지급금 적수×가중평균이자율 자동 계산** | ⚠️ (API는 단순 평균만) |
| B-10 업무용승용차: **5년 정액 강제, 800만/1,500만 한도, 처분손실 10년 이월** | ⚠️ |
| B-12 세액공제: **외국납부세액공제 / 재해손실세액공제 / 농어촌특별세** | ❌ |
| B-13 최저한세: **중소 7%/일반 17% 자동 분기** | ⚠️ |
| B-14 가산세: **수정신고 단계별 감면 90/75/50%** | ⚠️ |
| B-16 외국법인: **고정사업장 본·지점 손익 배분** | ⚠️ |
| B-17 연결납세: **연결법인 등록 + 모-자 거래 제거 + 산출세액 배분** | ⚠️ |

#### E-3. 서식 작성 (단계 3, 설계서 §3.2.3)

| 기능 | 상태 |
|---|---|
| 별지 제3호 미리보기 + PDF | ✅ |
| 부속서식 일람(상태·검증·금액·수정시각) | ✅ |
| **부속서식 100여 종 실제 생성기**(현재 FORM3/15/22/32/50만 PoC) | ❌ |
| **서식 시점별 버전 관리 UI**(G-1 마스터+버전) | ⚠️ (`ad-form` 단순 표) |
| **서식 항목 편집기(G-2)** — 라벨/계산식/검증룰 편집 | ❌ |
| **서식 간 합계/소계 연동 규칙 관리(G-5)** — `form_relationships`에 SUM/COPY/SUBTOTAL 규칙 편집 UI | ❌ |
| **서식 필드 참조/의존성 + 순환검사(G-6)** | ❌ |
| **버전 간 데이터 마이그레이션(G-3)** — 드라이런/실행/롤백 | ⚠️ (API는 있으나 UI 없음) |
| **사업연도별 적용 서식 세트(G-4)** | ❌ |
| **검증룰 버전 관리** | ⚠️ (`form_validations` 있고 시드는 있으나 UI 없음) |
| **수동 수정 표시(회색=자동연동, 흰색=수기)** | ❌ |

#### E-4. 검증 (단계 4, 설계서 §C-2)

| 기능 | 상태 |
|---|---|
| 룰 카탈로그 + 통합 검증 실행 | ✅ |
| 등급 표시(ERROR/WARN/INFO) | ✅ |
| **이슈 행 “이동” 버튼 → 해당 메뉴 hash 라우팅** | ⚠️ |
| **오류(X) 0건이어야 5. 결재 활성화** | ⚠️ (현재 disabled 로직 약함) |
| **142개 룰 전체 시드 + 모듈별 룰 카테고리화** | ⚠️ (현재 50+) |

#### E-5. 결재 (단계 5, 설계서 §C-3)

| 기능 | 상태 |
|---|---|
| 결재 대기함 + 승인/반려 | ✅ |
| **결재선(approval_lines) 다단계** | ⚠️ (테이블 존재, 다단계 검증 미완) |
| **반려 시 자동 되돌리기(2 또는 3 단계로 이동)** | ❌ |
| **검토 첨부파일** | ❌ |
| **검토 의견 + 변경 이력 표시** | ⚠️ |

#### E-6. 출력 (단계 6, 설계서 §5.3 D)

| 기능 | 상태 |
|---|---|
| 단일 PDF / ZIP 다운로드 | ✅ |
| **JasperReports 정식 PDF 템플릿** | ❌ (현재 더미 PDF) |
| **워터마크/봉인(DRAFT/APPROVED/FILED 상태별)** | ❌ |
| **출력 이력(다운로드 보관, 누가/언제 출력)** | ❌ |
| **인쇄 미리보기(브라우저)** | ⚠️ |

#### E-7. 전자신고 (단계 7, 설계서 §C-4 / §8 / Phase 16)

| 기능 | 상태 |
|---|---|
| 사전 검증 + 텍스트 파일(EUC-KR/Windows-949) 생성 | ✅ |
| 파일 다운로드 + 체크섬 | ✅ |
| **API 직접 제출(e-Tax 연동)** | ❌ |
| **2FA Step-up 재인증** | ❌ |
| **접수번호·접수일시·상태 ACCEPTED 처리** | ⚠️ (mock) |
| **FILED 시 스냅샷 동결(잠금 + lock_mode=LOCKED)** | ⚠️ (lock_mode 컬럼 있으나 자동 잠금 누락) |
| **레코드 매핑 관리(서식 ↔ efile_record_fields)** | ⚠️ |

### 2.6 F. 사후관리·분석/보고서·운영

| 기능 | 상태 |
|---|---|
| 신고 이력 + 전자신고 이력 | ✅ |
| **수정신고 잠금 해제(`unlock`) + 버전 모드 선택** | ✅ |
| **차이 보고서(amendment-preview) UI** | ⚠️ (JSON dump 수준) |
| **경정청구(별도 워크플로)** | ❌ |
| 알림 센터 + 읽음 처리 | ✅ |
| **마감 D-30 알림 자동 생성(스케줄러)** | ❌ |
| 사업연도 비교 / 세부담 분석 / 유보 잔액 추이 | ✅ |
| **이월결손금 만료 예측 보고서** | ❌ |
| **업종별/규모별 통계(회계법인용)** | ❌ |
| **사용자 정의 리포트(SELECT 컬럼/필터 저장)** | ❌ |
| **당기 세부담 추이 차트 / 업종별 도넛(대시보드 B 와이어프레임 하단)** | ❌ |
| 감사 로그 | ✅ |
| **감사로그 hash_current 일별 무결성 체인 검증 도구** | ❌ |
| /ready + 보안 헤더 + launch-readiness | ✅ |
| **온콜/핫픽스/정식 가입 절차 문서(`docs/phase20`)** | ⚠️ (스텁만) |

---

## 3. 우선순위 분류 (P0 ~ P3)

| 우선순위 | 정의 | 적용 기준 |
|---|---|---|
| P0 | 신고 흐름 정합성·법적 의무·보안 — 미구현 시 시스템 사용 불가 | 2FA, IP 화이트리스트, 잠금, FILED 동결, 행 단위 입력 그리드 등 |
| P1 | 설계서가 명시한 핵심 차별화 — 영업·UAT에서 “있어야 한다”로 평가되는 항목 | 영향 시뮬레이션, 서식 버전 관리 UI, 마이그레이션 드라이런, 행 단위 변경 이력, 법령 변경 배너 |
| P2 | 운영 효율을 끌어올리는 부가 기능 | 마감 알림 스케줄러, 이월결손금 만료 예측, 사용자 정의 리포트, JasperReports PDF, 워터마크 |
| P3 | 장기 로드맵 | e-Tax 직접 제출 API, 회계 ERP 연동, Redis pub/sub 캐시 무효화 |

---

## 4. 작업 단위 (Pack) 정의

> 1개 Pack = 1~2주 작업량의 응집된 기능 묶음. P0부터 순서대로 실행한다.

### Pack 1 (P0) — 신고 흐름·보안 정합성 ★ 출시 게이트
- **A. FILED 동결**: `business_years.locked_at`/`lock_mode` 자동 설정, 잠금 후 1~6단계 PUT 거부 미들웨어.
- **B. 2FA TOTP 강제 + IP 화이트리스트 미들웨어**: 로그인·전자신고 제출 두 경로 Step-up.
- **C. 로그인 실패 5회 잠금** + 관리자 잠금 해제 UI.
- **D. 행 단위 입력 그리드(B-1, B-4, B-15 우선)**: prototype의 “행 추가/삭제/Excel 가져오기” 그리드를 Vanilla JS로 구현(`grid.js` 컴포넌트).
- **E. 검증 “이동” 라우팅**: 룰 메타의 `target_path`를 hash 라우팅으로 연결.
- **F. 결재 반려 시 자동 되돌리기**: `workflow/events`에 `revert_to_step` 저장, 컨텍스트가 단계 2/3 으로 이동.
- 산출물: 통합 테스트 `tests/lock_and_2fa.rs`.

### Pack 2 (P1) — 법령·서식 버전 관리 UI
- **A. 영향 시뮬레이션(F-4) UI 완성**: 대상 BY×조정 모듈×차이 표.
- **B. 변경 적용 워크플로(F-1)**: DRAFT → REVIEWED → ACTIVE 단계와 승인자 지정 + 공지 알림 자동 발송.
- **C. 사업연도 스냅샷 라벨 매핑(F-5)**: `snapshot.snapshot_data`의 `law_version/rate/limit/form` 키를 사람이 읽는 라벨로 표시. “근거 PDF 일괄 다운로드”.
- **D. 서식 마스터·버전 UI(G-1)**: `form_versions` 목록·필터·상태 변경.
- **E. 서식 항목 편집기(G-2)**: JSON sections 편집 + 검증룰 +/- + JSON 보기.
- **F. 서식 연동 규칙 관리(G-5)**: `form_relationships`에 SUM/COPY/SUBTOTAL/필터·null·반올림·계산순서 편집 UI.
- **G. 서식 필드 참조/순환검사(G-6)**: 순환 의존성 검사 백엔드 함수 + UI.
- **H. 서식 마이그레이션 드라이런/실행/롤백 UI(G-3)** — 백엔드 API는 존재, UI 신규.
- **I. 사업연도별 적용 서식 세트(G-4)**.
- **J. 검증룰 버전 관리** — `form_validations`를 버전별로 묶어 표시.

### Pack 3 (P1) — 17 세무조정 모듈 UI 완성
- **A. 모듈별 그리드 + Excel 임포트/익스포트**: 모든 17개 모듈 통일 그리드. 모듈 메타에 컬럼 정의.
- **B. 한도·자동계산 카드** (전기 이월 / 당기 발생 / 한도 / 초과액).
- **C. 행 단위 변경 이력**: `adjustment_items_history`(신규) → 좌측 패널.
- **D. 행 단위 메모/첨부**: 첨부는 S3-호환 스토리지(`MinIO` Docker).
- **E. 자동계산 근거 툴팁**: `법조항 메타`를 `tax_law_versions.metadata.references[]`에 추가하고 UI 툴팁.
- **F. 법령 변경 알림 노란 배너**: 미신고 BY에 `law_amendment_history`의 `affected_modules` 매칭 → 배너 + “재계산” 버튼.
- **G. 모듈별 계산 보강**:
  - B-3 신용카드 미사용분 자동 분개.
  - B-4 즉시상각의제, 자본적/수익적 지출 판정.
  - B-9 가지급금 적수×가중평균이자율.
  - B-10 5년 정액 강제, 처분손실 10년 이월.
  - B-12 외국납부/재해손실/농어촌특별세.
  - B-14 단계별 감면 90/75/50%.
  - B-16/B-17 본격 구현.

### Pack 4 (P1) — 권한·메뉴 고급 모델
- **A. 12종 Function Code 마스터** + 메뉴-기능 N:M 시드(`menu_functions`).
- **B. 메뉴별 활성 기능 체크박스(E-3)**.
- **C. 역할×메뉴×기능 매트릭스 편집(E-2)** — 체크박스 그리드.
- **D. 데이터 권한(Data Scope) ALL/ASSIGNED/OWNED/NONE** + 모든 GET 라우트에 RLS 필터.
- **E. 필드 마스킹 정책** + `MASK_OFF` 권한 + 응답 직렬화 인터셉터.
- **F. 사용자별 담당 법인 권한 화면(E-4)** — 개별/그룹/조건/위임 4탭.
- **G. 권한 위임/대행** — `access_delegations` 테이블 + 위임 화면(E-5).
- **H. 권한 평가 우선순위(DENY > ALLOW)** 도메인 함수 + 단위 테스트.
- **I. 감사로그 hash_current 일별 검증 도구** + UI.

### Pack 5 (P1) — 워크플로·결재선·반려 흐름
- **A. 다단계 결재선(approval_lines)** — 결재 요청자/검토자/승인자 배정.
- **B. 검토 첨부파일·코멘트**.
- **C. 반려 시 단계 자동 되돌리기(Pack 1.F와 연동)**.
- **D. 결재 알림 자동 발송** — `notifications`에 자동 push.
- **E. 다음 단계 자동 안내** — “3 ▶ 4 ▶ 5” 진행 시 컨텍스트 진행률 자동 업데이트.

### Pack 6 (P2) — 사후관리·분석/보고서 보강
- **A. 차이 보고서(amendment-preview) 정식 UI** — Before/After 표 + 셀 단위 diff.
- **B. 경정청구 워크플로** — 별도 상태(`AMENDMENT_REQUESTED`).
- **C. 마감 D-30/D-7 알림 스케줄러** — `tokio-cron-scheduler` 또는 큐 잡으로 매일 09:00 실행.
- **D. 이월결손금 만료 예측 보고서** — `loss_carryforwards`에서 expires_year 기반.
- **E. 업종별/규모별 통계** — `customers.industry_code` × `business_years.year_label`.
- **F. 당기 세부담 추이 + 업종별 도넛 차트** — 대시보드 하단 Chart.js.
- **G. 사용자 정의 리포트(MVP)** — 컬럼 토글 + 필터 저장(`user_report_definitions`).
- **H. PDF 정식화** — JasperReports(Java)는 별도 사이드카로 두거나 `printpdf`/`weasyprint`로 대체 결정.
- **I. 워터마크/봉인** — 상태별 워터마크(`DRAFT`/`APPROVED`/`FILED`) PDF 합성.
- **J. 출력 이력** — `print_history`(신규) 기록.

### Pack 7 (P3) — 외부 연계
- **A. 회계 ERP 연동(더존/SAP/Oracle)** — 어댑터 모듈 + 자료 동기화.
- **B. e-Tax API 직접 제출** — NTS Gateway 자격 발급 후 연결.
- **C. Redis 도입** — 세션 캐시·권한 캐시 + pub/sub 무효화.
- **D. 회귀 테스트 셋(법령 버전별 표준 케이스)** — 12개 사업연도 표준 케이스 데이터.

---

## 5. 단계별 일정 (12주 안)

| 주차 | Pack | 비고 |
|---|---|---|
| W1~W2 | Pack 1 (P0) | 출시 게이트, 우선 종료 |
| W3~W5 | Pack 2 (P1) | 법령·서식 버전 UI |
| W6~W7 | Pack 3 (P1) | 17모듈 UI 그리드 |
| W8 | Pack 4 (P1) | 권한·메뉴 고급 |
| W9 | Pack 5 (P1) | 결재선·반려 |
| W10~W11 | Pack 6 (P2) | 사후/분석/PDF |
| W12 | Pack 7 (P3) | 외부 연계 시작·로드맵 |

각 Pack 종료 시 게이트:
1. `cargo test --all` 통과 + 신규 통합 테스트 ≥ 1건.
2. `cargo clippy --all-targets -- -D warnings` 통과.
3. `docs/menu_smoke_checklist.md` 회귀.
4. 사용자 시연 + 변경점 changelog 1줄.

---

## 6. 산출물 매트릭스

| Pack | 백엔드 (Rust) | DB 마이그레이션 | 프론트엔드 | 테스트 |
|---|---|---|---|---|
| 1 | `src/auth/totp.rs`, `src/auth/lockout.rs`, `src/middleware/ip_allowlist.rs`, `src/middleware/lock_guard.rs` | `2026052X000100_filed_lock.sql` | `app/components/grid.js`, screens.js 보강 | `tests/lock_and_2fa.rs` |
| 2 | `src/law/workflow.rs`, `src/forms/version_workflow.rs`, `src/forms/cycle_check.rs` | `2026052X000200_law_workflow.sql`, `…000300_form_version_workflow.sql` | `screens/admin_law/*`, `screens/admin_form/*` | `tests/law_workflow.rs`, `tests/form_versioning_ui.rs` |
| 3 | `src/adjustment/*.rs` 각 모듈 보강, `src/adjustment/history.rs` | `2026052X000400_adjustment_history_attachments.sql` | `screens/adjustment/*` 17모듈 그리드 | `tests/adjustment_modules_all.rs` |
| 4 | `src/permissions/*.rs`, `src/permissions/scope.rs`, `src/permissions/masking.rs`, `src/permissions/delegation.rs` | `2026052X000500_rbac_advanced.sql` | `screens/admin_role/*`, `screens/admin_menu/*`, `screens/admin_cacc/*` | `tests/permissions_evaluator.rs` |
| 5 | `src/workflow/approval_lines.rs`, `src/workflow/revert.rs` | `2026052X000600_approval_lines.sql` | `screens/ws_appr/*` | `tests/workflow_multi_step.rs` |
| 6 | `src/reports/loss_expiry.rs`, `src/reports/industry.rs`, `src/scheduler.rs`, `src/print/watermark.rs`, `src/print/history.rs` | `2026052X000700_reports_user_defined.sql`, `…000800_print_history.rs` | 대시보드 차트, 보고서 4개 신규, 출력 이력 화면 | `tests/scheduler.rs` |
| 7 | `src/integration/erp_*.rs`, `src/integration/etax.rs`, `src/cache/redis.rs` | — | — | 외부 연계 통합 테스트 |

---

## 7. 위험과 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| JasperReports(Java) 도입은 Rust 단일 스택 깨짐 | 운영 복잡도 증가 | 사이드카 컨테이너 또는 `printpdf`/`weasyprint`로 대체 결정을 Pack 6 초입에 의사결정 |
| 12종 Function Code × 24 메뉴 매트릭스의 마이그레이션 부담 | Pack 4 지연 | 1차로 기존 `role_permissions` 데이터를 자동 이관하는 변환 스크립트 작성 |
| e-Tax API 자격 발급은 외부 의존 | Pack 7 지연 | 자격 발급 일정과 분리하여 “수동 업로드” 경로 우선 유지 |
| 그리드 컴포넌트 자체 구현이 비대화 | Pack 3 지연 | 1차로 `Handsontable Community` 또는 `revogrid`(CDN)로 대체 후 자체 구현으로 점진 교체 |
| 권한 평가 변경이 기존 API 회귀 유발 | 운영 사고 | Pack 4 종료 전에 모든 GET 라우트에 대해 “권한 변화 0 회귀” e2e 테스트 작성 |

---

## 8. 완료 정의 (DoD)

1. 본 문서의 영역 A~F 표에서 ❌/⚠️로 표시된 항목이 Pack 1~6 종료 시 ✅ 또는 명시적 “P3로 이관”으로 정리된다.
2. 신고 흐름이 다음 시나리오로 닫힌 루프로 동작한다: 로그인(2FA) → 작업 시작 → 1~7단계(잠금/검증/결재) → 사후관리(잠금 해제) → 재신고.
3. 권한 모델이 다음을 모두 지킨다: DENY > ALLOW, Data Scope 자동 WHERE, 필드 마스킹, 위임 합집합.
4. 서식 버전 한 건을 등록 → 영향 시뮬레이션 → 마이그레이션 드라이런 → 활성화 → 사용자 공지까지 한 흐름이 가능하다.
5. CI: `cargo test --all`, `cargo clippy --all-targets -- -D warnings`, `tests/menu_smoke.rs`, 신규 Pack별 통합 테스트가 전부 green.
6. 문서: 본 문서 + `2차구현계획.md` + `메뉴기능수정.md` 3종이 일관된 메뉴 IA·기능 정의를 가리킨다.

---

## 9. 2026-05-19 구현 상태

- Pack 1~6은 현재 코드 기준으로 구현 완료 범위에 편입한다. 핵심 변경은 FILED 잠금/해제, TOTP 2FA, IP allowlist, 5회 실패 잠금, B1/B4/B15 입력 그리드, 법령/서식 버전 워크플로, 17개 세무조정 모듈 이력/첨부, 고급 RBAC, 다단계 결재, D-30/D-7 알림, 손실 만료/업종 통계/사용자 정의 보고서, PDF 워터마크/직인/출력 이력이다.
- 최종 메뉴 IA는 `src/modules.rs`와 `frontend/app/menu.js`, 화면 연결은 `frontend/app/screens.js`, 그리드 공통 컴포넌트는 `frontend/app/components/grid.js`를 기준으로 한다.
- 메뉴별 수동 확인 기준은 `docs/menu_smoke_checklist.md`, 전후 상태와 검증 타깃은 `docs/menu_smoke_findings.md`를 기준으로 한다.
- e-Tax 직접 제출, ERP 커넥터, Redis pub/sub 권한 캐시 무효화는 외부 자격/인프라가 필요한 P3 항목으로 명시 보류한다.

## 10. 참조

- `법인세_세무조정계산서_시스템_설계서.md` §3.2.0 ~ §5.3 (모든 와이어프레임)
- `DB설계.md` (별도 문서, 본 계획서의 DB 변경은 그 명명규칙 준수)
- `prototype/index.html` (UX 기준)
- `2차구현계획.md`(메뉴 IA 채택), `메뉴기능수정.md`(시드·점검)
# 2026-05-19 v1.1 Alignment Note

- v1.0 Pack 1-6 functionality remains the delegate implementation used by v1.1 S1 leaf routing.
- The final menu IA is now defined by `src/modules.rs` as 5 groups and 109 listed leaf routes; this supersedes the former 24 integrated menu surface while preserving those screens as delegates.
- Permission/menu smoke documentation now uses `docs/menu_smoke_checklist.md` and `tests/menu_full_tree.rs` for full leaf coverage.
- P3 external integrations remain unchanged: direct e-Tax submission, ERP connectors, and Redis pub/sub cache invalidation are still out of scope for this v1.1 completion.

---
