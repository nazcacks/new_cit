# 대시보드 화면 재구성 계획 — 업무현황 사용자 친화 개선

> **문서 버전**: v1.0
> **작성일**: 2026-06-04
> **작성자**: 강수 (Kang-Soo.Cho@kr.ey.com)
> **기준 문서**: `법인세_세무조정계산서_시스템_설계서.md` (§5.1 메뉴, B. 대시보드), `업무흐름.md` (3.2 로그인 진입, 2. 역할별 매트릭스, Phase 18 운영 흐름)
> **대상 코드**: `frontend/app/screens.js` (`renderDashboard` 외), `frontend/app.css`, `frontend/app/i18n.js`
> **목적**: 로그인 직후 진입점인 대시보드를 설계서·업무흐름의 단계/역할 모델에 맞춰 재배치하고, "내가 지금 무엇을 해야 하는지"가 한눈에 들어오도록 사용자 친화적으로 개선한다.

---

## 1. 배경 및 목표

### 1.1 대시보드의 위치 (업무흐름 기준)

업무흐름 3.2에 따르면 대시보드는 **로그인 직후 모든 사용자의 진입점**이며, 다음을 제공해야 한다.

- 진행중 작업 / 검토 / 제출 현황
- 최근 활동
- 신고 일정 (마감 임박)
- 세부담 추이

설계서 §5.1은 대시보드 하위 구성을 다음 5개로 명시한다.

```
① 대시보드
   ├── 내 작업 현황 (작성중 / 검토중 / 승인 / 신고 / 반려)
   ├── 신고 마감 임박 (D-30 / D-7 / D-Day)
   ├── 알림 / 결재 대기함
   ├── 최근 활동
   └── KPI (당기 세부담 / 업종별 분포)
```

### 1.2 개선 목표 (3가지 원칙)

1. **"다음 할 일" 우선** — 현황 숫자 나열이 아니라, 사용자가 클릭 한 번으로 다음 단계 화면(작업 시작/검증/결재/출력/신고)으로 진입하도록 한다.
2. **역할 맞춤** — 업무흐름 §2 역할 매트릭스에 따라 ASSISTANT/TAX_EXPERT/TAX_REVIEWER/ADMIN 별로 보이는 카드와 강조점을 다르게 한다.
3. **정보 위계 정리** — 마감·반려처럼 시간이 걸린 항목을 상단에, 참고용 지표(KPI)를 하단에 배치해 시선 흐름을 위→아래로 단순화한다.

---

## 2. 현황 진단 (현재 구현 vs 설계서/업무흐름)

현재 `renderDashboard`([screens.js:3629](frontend/app/screens.js:3629))는 이미 아래 구성을 렌더링한다.

| 영역 | 현재 함수 | 설계서/업무흐름 정합성 | 진단 |
|------|-----------|----------------------|------|
| Hero + 작업 시작 | `renderDashboard` 내 inline ([:3658](frontend/app/screens.js:3658)) | ○ 진입점·시작 버튼 | 제목/문구 보강 필요 |
| 반려 배너 | inline ([:3666](frontend/app/screens.js:3666)) | ○ 반려 상태 노출 | 위치/행동 유도 약함 |
| 내 작업 현황 카드 | `renderDashboardWorkStatusCards` ([:3119](frontend/app/screens.js:3119)) | △ 5개 상태 카드 존재, 아이콘 없음 | **아이콘/시각 구분 부족** |
| 신고 마감 임박 | `renderDashboardDeadlineTable` ([:3086](frontend/app/screens.js:3086)) | △ 표 형태, urgency 배지 | **D-30/D-7/D-Day 버킷 구분 부족** |
| 알림/결재 대기함 | `renderDashboardNotificationPanel` ([:3134](frontend/app/screens.js:3134)) | ○ 탭 분리 | 양호 |
| 최근 활동 | `renderDashboardRecentActivities` ([:3221](frontend/app/screens.js:3221)) | ○ 피드 + 이동 | 양호 |
| KPI (세부담/업종/결손) | `renderDashboardTaxBurdenKpi` ([:3315](frontend/app/screens.js:3315)) | ○ 추이+도넛+막대 | 양호, 하단 배치 확인 |
| 실시간 갱신 | `startDashboardRealtime` ([:3377](frontend/app/screens.js:3377)) | (추가 기능) | 유지 |

**핵심 결론**: 기능적 요소는 대부분 구현됨. 부족한 것은 **(a) 시각적 친화성(아이콘·색·위계), (b) 마감 D-day 버킷 구조, (c) 역할별 맞춤 강조, (d) "다음 할 일" 행동 유도**의 4가지다. 기능 재작성이 아니라 **재배치 + 표현 개선** 중심으로 진행한다.

---

## 3. 개선 후 화면 구성 (재배치안)

설계서 B. 대시보드 레이아웃을 기준으로 위→아래 4개 밴드로 정리한다.

```
┌─────────────────────────────────────────────────────────────┐
│ [밴드 0] 인사 + 오늘 날짜 + [신고 작업 시작 ▶]               │  ← Hero
├─────────────────────────────────────────────────────────────┤
│ [밴드 1] 내 작업 현황 (아이콘 카드 5종 + 반려 강조)          │
│   📝작성중  🔍검증대기  ⏳결재대기  ✅승인  🏆신고완료        │
│   (반려 N건이면 빨간 카드로 맨 앞에 끼워넣음 → 재작성 이동)  │
├──────────────────────────────┬──────────────────────────────┤
│ [밴드 2-좌] 신고 마감 임박     │ [밴드 2-우] 알림 / 결재 대기함 │
│   D-Day  ▸ ...                │   (탭: 알림 | 결재 대기)      │
│   D-7    ▸ ...                │                              │
│   D-30   ▸ ...                │                              │
├──────────────────────────────┴──────────────────────────────┤
│ [밴드 3-좌] 최근 활동          │ [밴드 3-우] KPI (역할 한정)   │
└─────────────────────────────────────────────────────────────┘
```

### 3.1 밴드 0 — Hero (진입/시작)

- 문구를 사용자 인사 중심으로: `"{사용자명}님, 안녕하세요"` + 오늘 날짜(요일) — 프로토타입(`prototype/index.html:447`)과 동일 톤.
- 우측 `[신고 작업 시작 ▶]` 버튼은 현행 유지(`ws/start:customer-pick`).
- 부제: "작성·검증·결재·승인·신고 진행 상황을 확인하고 다음 작업으로 이동하세요."

### 3.2 밴드 1 — 내 작업 현황 카드 (★ 핵심 개선)

설계서 작업 현황 5상태 + 반려를 **아이콘 + 상태색**으로 시각화한다.

| 상태 | 아이콘 | 색 톤 | 클릭 시 이동(현행 `dashboardStatusRoute`) |
|------|:---:|------|------|
| 작성중 (DRAFT) | 📝 | amber | `ws/start:customer-pick` |
| 검증 대기 (IN_REVIEW_VALIDATION) | 🔍 | sky | `ws/val:run` |
| 결재 대기 (IN_REVIEW_APPROVAL) | ⏳ | violet | `ws/appr:inbox` |
| 승인 완료 (APPROVED) | ✅ | indigo | `ws/print:preview` |
| 신고 완료 (FILED) | 🏆 | emerald | `post/hist:list` |
| **반려 (REJECTED)** | ⚠️ | rose | 재작성 화면(`ws/start:customer-pick`) |

- 각 카드: 큰 숫자(사업연도 수) + 보조(고객사 N개) + 긴급(즉시 처리 N건) — 현행 데이터 그대로 활용.
- **반려 처리**: 현재 별도 배너(`:3666`)를 카드 그리드 맨 앞 빨간 카드로 통합해 "재작성 필요"를 행동 유도형으로 노출.
- 접근성: `aria-label="업무현황"` 유지, 카드는 `button`이며 키보드 포커스 가능(현행 유지).

### 3.3 밴드 2-좌 — 신고 마감 임박 (D-day 버킷화)

업무흐름 Phase 18 "D-30 인앱 알림" + 설계서 "D-30 / D-7 / D-Day"에 맞춰 **표를 버킷 그룹으로** 재구성한다.

- 버킷: `D-Day(지남/당일)` → `D-7 이내` → `D-30 이내` 순.
- 각 행: D-day 배지(색=긴급도) + 고객사 + 사업연도 + 상태(진행률). 클릭 시 해당 사업연도 컨텍스트로 진입.
- 데이터는 현행 `/dashboard/filing-deadlines?withinDays=30` 그대로 사용, 프론트에서 `urgencyLevel`/`daysRemaining`로 그룹핑.
- `[전체 보기]` → `dashboard:duesoon` 유지.

### 3.4 밴드 2-우 — 알림 / 결재 대기함

- 현행 탭 구조 유지(`renderDashboardNotificationPanel`).
- 개선: 결재 대기 탭은 **TAX_REVIEWER에게만** 표시(현행 `canViewDashboardApprovals` 유지), 그 외 역할은 알림 단일 패널로 폭을 넓힌다.

### 3.5 밴드 3 — 최근 활동 / KPI

- 최근 활동: 현행 유지(`renderDashboardRecentActivities`).
- KPI: 현행 유지하되 **역할 게이팅 명확화**(`canViewDashboardKpi`). ASSISTANT 등 미열람 역할은 KPI 영역 대신 최근 활동이 전체 폭을 차지하도록 그리드 조정.

---

## 4. 역할별 맞춤 (업무흐름 §2 매트릭스 반영)

| 역할 | 강조 카드 | 결재 탭 | KPI | "다음 할 일" 기본 유도 |
|------|----------|:---:|:---:|------|
| ASSISTANT | 작성중·검증대기 | – | – | 정보입력/조정 계속 |
| TAX_EXPERT | 작성중·검증대기·승인 | – | ● | 검증 실행 / 신고 진행 |
| TAX_REVIEWER | 결재대기 | ● | ● | 결재함 처리 |
| TENANT_ADMIN / SUPER_ADMIN | 전체 | – | ● | 마감 모니터링 |

- 구현은 기존 `dashboardRoles`/`canViewDashboard*` 헬퍼([:3354~3364](frontend/app/screens.js:3354))를 확장해 카드 강조 클래스만 추가(데이터 분기 아님 — 표현만 분기).

---

## 5. 구현 작업 분해

> 원칙: 백엔드 API/데이터 계약은 변경하지 않는다. 프론트 표현 계층(`screens.js` 렌더 함수 + `app.css` + `i18n.js`)만 수정한다.

### 5.1 작업 항목

1. **i18n 키 추가** (`frontend/app/i18n.js`)
   - Hero 인사/부제, 상태별 라벨(이미 일부 존재), D-day 버킷 라벨(`D-Day/D-7/D-30`), 반려 카드 문구 — ko/en 동시.
2. **작업 현황 카드 개선** (`renderDashboardWorkStatusCards` [:3119](frontend/app/screens.js:3119))
   - 상태→아이콘 매핑 테이블 추가, 반려 카드를 그리드 선두에 합성.
   - inline 반려 배너([:3666](frontend/app/screens.js:3666)) 제거 후 카드로 흡수.
3. **마감 버킷화** (`renderDashboardDeadlineTable` [:3086](frontend/app/screens.js:3086))
   - 행 배열을 D-day 버킷으로 group → 버킷 헤더 + 행 렌더.
4. **Hero 문구/날짜** (`renderDashboard` inline [:3658](frontend/app/screens.js:3658))
   - 사용자명(`env.auth.user`)·오늘 날짜 표시.
5. **역할별 그리드/강조** (`renderDashboard` [:3668~3678](frontend/app/screens.js:3668))
   - KPI 미열람 시 `dashboard-lower-grid` 단일 컬럼 처리, 강조 클래스 부여.
6. **CSS** (`frontend/app.css` [:734~798](frontend/app.css:734))
   - 카드 아이콘 슬롯, 상태색 토큰, 반려 카드 변형, 마감 버킷 헤더, 반응형(기존 `@media` [:1673](frontend/app.css:1673) 보강).

### 5.2 변경하지 않는 것 (범위 고정)

- 대시보드 API 엔드포인트 및 응답 스키마.
- 라우팅 키(`dashboard:overview/duesoon/inbox/recent/kpi-tax`).
- 실시간 폴링/캐시 무효화 로직.
- 알림 읽음/결재 인라인 처리 핸들러(동작 유지, 마크업만 정리).

---

## 6. 진행 단계 및 우선순위

| 단계 | 작업 | 우선순위 | 비고 |
|------|------|:---:|------|
| 1 | i18n 키 + Hero 문구/날짜 | 높음 | 저위험, 즉시 체감 |
| 2 | 작업 현황 카드 아이콘/색/반려 통합 | 높음 | 핵심 친화성 |
| 3 | 마감 D-day 버킷화 | 중간 | 업무흐름 Phase 18 정합 |
| 4 | 역할별 강조/그리드 조정 | 중간 | 매트릭스 반영 |
| 5 | CSS 정리 + 반응형 | 중간 | 1~4 병행 |
| 6 | 검증(아래 §7) | 필수 | 마무리 |

---

## 7. 검증 방법

- **역할 스모크**: ASSISTANT / TAX_EXPERT / TAX_REVIEWER / TENANT_ADMIN 4계정으로 로그인 후 카드·결재 탭·KPI 노출 차이 확인.
- **상태 시나리오**: 작성중·검증대기·결재대기·승인·신고완료·반려가 각각 ≥1건인 데모 데이터로 카드 숫자/이동 경로 확인.
- **마감 버킷**: D-Day/D-7/D-30 각 1건 데이터로 그룹 헤더·배지색 확인.
- **반응형**: 데스크탑/태블릿 폭에서 그리드 붕괴 없음.
- **회귀**: 알림 읽음, 결재 인라인 승인/반려, 실시간 갱신, 전체 보기 이동 링크 정상.
- **i18n**: ko/en 전환 시 누락 키 없음.

---

## 8. 산출물

- 수정 파일: `frontend/app/screens.js`, `frontend/app.css`, `frontend/app/i18n.js`
- 본 계획서: `대시보드_수정_v1.0.md`

---

**문의**: Kang-Soo.Cho@kr.ey.com
**연관 문서**: `법인세_세무조정계산서_시스템_설계서.md`, `업무흐름.md`
