# 추가 구현 계획 v1.3
## leaf 메뉴 99개 × 단독 화면·기능 정상 연결

> 작성일: 2026-05-18 · 작성자: 강수 (Kang-Soo.Cho@kr.ey.com)
> 기준 문서: `법인세_세무조정계산서_시스템_설계서.md` §3.2 ~ §5.3
> 선행 문서: `2차구현계획.md`(prototype IA) · `메뉴기능수정.md`(시드) · `추가구현_v1.0.md`(기능 갭) · `추가구현_v1.1.md`(leaf 메뉴 트리 정의) · `추가구현_v1.2.md`(연결 결손 진단)
> 본 문서의 범위: **`추가구현_v1.2.md`에서 확정한 활성 leaf 99개에 대해 각 메뉴를 클릭했을 때 실제로 (a) 그 메뉴 전용 화면이 나오고, (b) 그 화면에서 정의된 기능이 동작**하도록 만드는 끝-단 구현 계획.

### v1.3 구현 기준 정정

`추가구현_v1.1.md` 초안의 세분 leaf 중 v1.2에서 통합 화면으로 접힌 항목은 v1.3 활성 99개에 포함하지 않는다. 제외/통합 항목은 `dashboard:kpi-industry`, `ws/start:by-new`, `ws/info:fs-bs`, `ws/info:fs-is`, `ws/info:fs-cf`, `post/hist:detail`, `admin/form:field-ref`, `admin/code:industry`, `admin/code:account`, `admin/code:misc`, `admin/audit:law`이다.

따라서 v1.3의 단독 화면 등록 대상은 현재 `/api/modules/tree`가 반환하는 활성 99 leaf이며, 접힌 항목의 기능은 각각 `dashboard:kpi-tax`, `ws/start:by-pick`, `ws/info:fs`, `post/hist:list`, `admin/form:fields`, `admin/code:manage`, `admin/audit:events` 화면에서 처리한다.

---

## 1. 현재 상태 확정

| 항목 | 상태 |
|---|---|
| 좌측 메뉴(99 leaf) | ✅ 구조·라우팅·URL 변경 모두 동작 (`menu.js`/`router.js`) |
| 메뉴 클릭 → URL 변경 | ✅ `#/workspace/ws/adj/B12` 등으로 정상 변경 |
| 메뉴 클릭 → 화면 본문 변경 | ❌ 모든 leaf가 **24개 통합 화면 중 하나로 위임**(`screenByDelegate[meta.delegate]`) — 본문 동일 |
| 메뉴 클릭 → 기능 동작 | ⚠️ 통합 화면에서 노출된 1~2개 액션만 동작, leaf별 단독 기능 부재 |
| 백엔드 leaf 메타 노출 | ⚠️ `menu_nodes` 트리는 내려오지만 `required_perm`/`requires_context`가 화면 진입에 반영 안 됨 |

결손은 단 한 곳: **leaf 키 → 단독 render 함수**가 없다. 본 문서는 이 한 점을 99개에 대해 해소하는 구체 작업서다.

---

## 2. 아키텍처 결정

### 2.1 디스패치 우선순위 변경
`frontend/app/screens.js`의 `renderScreen`을 다음 순서로 바꾼다.

```js
export async function renderScreen(env) {
  const meta = routes[env.key] || routes.dashboard;
  layoutChrome(env, meta);                          // stepper / lawBanner / subnav / watermark

  // 1) leaf 단독 화면 우선
  const leaf = screenByLeaf[env.key];
  if (leaf) return leaf({ ...env, meta });

  // 2) 미구현 leaf 폴백: 통합 화면 + leaf 자동 선택
  const combined = screenByDelegate[meta.delegate] || renderDashboard;
  return combined({ ...env, meta, leafKey: env.key });
}
```
- 한 leaf가 단독 모듈로 옮겨질 때마다 `screenByLeaf[leafKey]`에 등록만 추가하면 즉시 활성화.
- 미등록 leaf는 기존 통합 화면 + Track A 패치(leaf 자동 선택)로 동작 — 회귀 안전.

### 2.2 leaf 화면 모듈 시그니처

```js
// frontend/app/screens/<group>/<leaf>.js
import { request, escapeHtml, money } from "/app/api.js";
import { hasWorkContext } from "/app/context.js";

export const meta = {
  key: "ws/adj:B12",
  group: "workspace",
  layout: "workspace",
  parentKey: "ws/adj",
  title: "2-12 세액공제·감면",
  requires: ["work-context"],
  perm: { module: "adjustment", function: "READ" },
};

export async function render(env) {
  if (meta.requires.includes("work-context") && !hasWorkContext(env.context)) {
    return env.outlet.innerHTML = workContextEmptyState(env);
  }
  const root = `/api/tenants/${env.auth.user.tenant_code}/business-years/${env.context.byId}`;
  const data = await request(`${root}/adjustments/tax/B12`);
  env.outlet.innerHTML = `
    <section class="panel" data-leaf-key="${meta.key}">
      <header class="panel-head"><h2>${escapeHtml(meta.title)}</h2></header>
      <!-- 한도 카드 + 그리드 + 액션 -->
    </section>
  `;
  bind(env);
}

function bind(env) { /* 행 추가/삭제/저장 등 */ }
```

각 leaf는 위 시그니처를 따르고 `screenByLeaf`에 한 줄로 등록된다.

### 2.3 공통 컴포넌트

| 컴포넌트 | 책임 |
|---|---|
| `components/grid.js` | 행 추가/삭제/Excel 임포트/단축키(엔터·Ctrl+S) 입력 그리드 |
| `components/subnav.js` | 형제 leaf 목록 + active 강조 |
| `components/leaf-watermark.js` | 헤더 상단 leaf 라벨 워터마크 |
| `components/empty-state.js` | workContext 미설정 / 데이터 없음 / 권한 없음 안내 |
| `components/law-tooltip.js` | 법조항 근거 툴팁 (B-* 모듈) |
| `components/chart-mini.js` | 보고서 막대/추세 차트 |
| `components/form-modal.js` | 단건 등록/편집 모달 |

---

## 3. leaf별 화면·기능 명세 (99 leaf 매트릭스)

> 표 컬럼: `leaf 키 / 파일 경로 / 주 API(1차 호출) / 본문 / 액션 / 백엔드 신설 필요`. 신설 필요 시에만 표기한다.

### 3.1 ① 대시보드 (5 leaf)

| leaf | 파일 | 주 API | 본문 | 액션 | 신설 |
|---|---|---|---|---|---|
| `dashboard:overview` | `dashboard/overview.js` | `/dashboard` | 5 KPI 카드 | 카드 클릭 → 해당 leaf 이동 | — |
| `dashboard:duesoon` | `dashboard/duesoon.js` | `/business-years?dueWithinDays=30` | 마감 임박 표 | “계속” → `ws/start:by-pick` | `?dueWithinDays` 추가 |
| `dashboard:inbox` | `dashboard/inbox.js` | `/workflow/queue?assignee=me` | 결재 대기함 표 | “검토” → `ws/appr:inbox` | — |
| `dashboard:recent` | `dashboard/recent.js` | `/audit-logs?limit=20` | 최근 활동 타임라인 | 항목 클릭 → 감사 상세 | — |
| `dashboard:kpi-tax` | `dashboard/kpi_tax.js` | `/reports/tax-burden?range=5y` | 연도별 추세 라인 차트 | 연도 클릭 → `report:tax-burden` | `?range` 옵션 |

### 3.2 ② 신고 작업 — 0. 작업 시작 (3 leaf)

| leaf | 파일 | 주 API | 본문 | 액션 |
|---|---|---|---|---|
| `ws/start:customer-pick` | `workspace/start/customer_pick.js` | `/customers` | 고객사 검색·선택 표 | 선택 → `ws/start:by-pick`로 컨텍스트 인계 |
| `ws/start:by-pick` | `workspace/start/by_pick.js` | `/business-years` | 사업연도 표(상태·진행률) | “계속” → `setContext` 후 `ws/info:fs` |
| `ws/start:snapshot` | `workspace/start/snapshot.js` | `/business-years/:byId/snapshot` | 적용 법령·서식 카드 | “근거 PDF” 다운로드(신설) |

### 3.3 ② 신고 작업 — 1. 세무정보 입력 (6 leaf)

| leaf | 파일 | 주 API | 본문 | 액션 |
|---|---|---|---|---|
| `ws/info:fs` | `workspace/info/fs.js` | `/tax-data/financial-statements` | 자산/부채/자본 KPI + BS·IS·CF 탭 | Excel 업로드 / 검증 실행 |
| `ws/info:mapping` | `workspace/info/mapping.js` | `/customers/:cid/account-mappings` | 매핑 표 + 학습 표시 | 행 매핑/저장 |
| `ws/info:assets` | `workspace/info/assets.js` | `/tax-data/assets` | 자산대장 그리드 | Excel 임포트 / 행 편집 |
| `ws/info:transactions` | `workspace/info/transactions.js` | `/tax-data/transactions` | 거래 그리드 | Excel 임포트 |
| `ws/info:vehicle` | `workspace/info/vehicle.js` | `/vehicle-usage-logs` | 차량 운행기록 | 월별 입력 |
| `ws/info:consistency` | `workspace/info/consistency.js` | `POST /tax-data/validation` | 일관성 결과 카드 | “재검증” 실행 |

### 3.4 ② 신고 작업 — 2. 세무조정 17개 (17 leaf)

> 17개 모듈은 모두 동일 시그니처. 본문 = 한도 카드 + 입력 그리드 + 결과(가산/차감/유보), 액션 = 저장·법조항 보기·전기 이월 자동 복사.

| leaf | 파일 | 주 API | 모듈별 본문 특이사항 |
|---|---|---|---|
| `ws/adj:B1` | `workspace/adj/B1.js` | `POST /adjustments/income` | 익금산입/익금불산입/손금산입/손금불산입 4 섹션 |
| `ws/adj:B2` | `workspace/adj/B2.js` | `POST /adjustments/transactions/B2` | 특례/일반/비지정 + 10년 이월 |
| `ws/adj:B3` | `workspace/adj/B3.js` | `POST /adjustments/transactions/B3` | 수입금액 명세 + 신용카드 미사용분 |
| `ws/adj:B4` | `workspace/adj/B4.js` | `POST /adjustments/assets/B4` | 자산대장 연동, 즉시상각의제, 자본/수익적 |
| `ws/adj:B5` | `workspace/adj/B5.js` | `POST /adjustments/transactions/B5` | 가지급금 인정이자 |
| `ws/adj:B6` | `workspace/adj/B6.js` | `POST /adjustments/assets/B6` | 퇴직급여 추계액·DC/DB |
| `ws/adj:B7` | `workspace/adj/B7.js` | `POST /adjustments/assets/B7` | 대손충당금 한도(직전 3년 평균) |
| `ws/adj:B8` | `workspace/adj/B8.js` | `POST /adjustments/evaluation/B8` | 외화 화폐성/비화폐성 |
| `ws/adj:B9` | `workspace/adj/B9.js` | `POST /adjustments/transactions/B9` | 재고·유가증권 평가방법 |
| `ws/adj:B10` | `workspace/adj/B10.js` | `POST /adjustments/assets/B10` | 운행기록 연동 + 800/1500만 한도 |
| `ws/adj:B11` | `workspace/adj/B11.js` | `POST /adjustments/evaluation/B11` | 결손금 잔액·우선공제 순 |
| `ws/adj:B12` | `workspace/adj/B12.js` | `POST /adjustments/tax/B12` | R&D / 통합투자 / 외국납부 / 재해손실 / 농어촌특별세 |
| `ws/adj:B13` | `workspace/adj/B13.js` | `POST /adjustments/tax/B13` | 일반 17% / 중소 7% 자동 분기 |
| `ws/adj:B14` | `workspace/adj/B14.js` | `POST /adjustments/tax/B14` | 수정신고 단계별 감면 90/75/50% |
| `ws/adj:B15` | `workspace/adj/B15.js` | `POST /adjustments/evaluation/B15` | (갑) 자본 변동 + (을) 유보 자동 집계 |
| `ws/adj:B16` | `workspace/adj/B16.js` | `POST /adjustments/special/B16` | 외국법인 PE 배분 |
| `ws/adj:B17` | `workspace/adj/B17.js` | `POST /adjustments/special/B17` | 연결법인 등록 + 거래 제거 |

공통 컴포넌트: `grid.js`(행 입력) + `law-tooltip.js`(법조항) + `lawBanner` + `subnav.js`(형제 17 leaf).

### 3.5 ② 신고 작업 — 3. 서식 작성 (4 leaf)

| leaf | 파일 | 주 API | 본문 |
|---|---|---|---|
| `ws/form:form3` | `workspace/form/form3.js` | `/forms/FORM3/preview` | 별지 제3호 ①~⑭ 필드 표 |
| `ws/form:attachments` | `workspace/form/attachments.js` | `/forms/attachments` | 부속서류 100+ 일람 |
| `ws/form:preview` | `workspace/form/preview.js` | `/forms/:code/preview` (선택형) | 좌측 서식 선택 + 우측 미리보기 |
| `ws/form:linkage` | `workspace/form/linkage.js` | `/forms/linkage-check` (신설) | 자동연동 항목 차이 표 |

### 3.6 ② 신고 작업 — 4. 검증 (3 leaf)

| leaf | 파일 | 주 API | 본문 |
|---|---|---|---|
| `ws/val:run` | `workspace/val/run.js` | `POST /validation/run` | 실행 결과 KPI + 진행률 |
| `ws/val:issues` | `workspace/val/issues.js` | `/validation/issues` | 이슈 목록 + “이동” 버튼(hash) |
| `ws/val:rules` | `workspace/val/rules.js` | `/validation/rules` | 룰 카탈로그 표 + 활성 토글 |

### 3.7 ② 신고 작업 — 5. 결재 (3 leaf)

| leaf | 파일 | 주 API | 본문 |
|---|---|---|---|
| `ws/appr:request` | `workspace/appr/request.js` | `POST /workflow/request` (신설) | 결재 요청 폼 + 결재선 |
| `ws/appr:inbox` | `workspace/appr/inbox.js` | `/workflow/queue?assignee=me` | 결재함 + 승인/반려 |
| `ws/appr:rejected` | `workspace/appr/rejected.js` | `/workflow/events?status=REJECTED` | 반려 이력 + 재작성 진입 |

### 3.8 ② 신고 작업 — 6. 출력 (3 leaf)

| leaf | 파일 | 주 API | 본문 |
|---|---|---|---|
| `ws/print:preview` | `workspace/print/preview.js` | `/forms/:code/preview` | PDF 동등 미리보기 |
| `ws/print:bulk` | `workspace/print/bulk.js` | `POST /forms/pdf-bundle/download` | 체크박스 + ZIP |
| `ws/print:history` | `workspace/print/history.js` | `/print/history` (신설) | 출력 이력 (사용자/일시) |

### 3.9 ② 신고 작업 — 7. 전자신고 (4 leaf)

| leaf | 파일 | 주 API | 본문 |
|---|---|---|---|
| `ws/file:precheck` | `workspace/file/precheck.js` | `/efilings/precheck` | 사전검증 결과 |
| `ws/file:generate` | `workspace/file/generate.js` | `POST /efilings` | 파일 생성 + 체크섬 |
| `ws/file:submit` | `workspace/file/submit.js` | `POST /efilings/:id/submit` (신설) | 2FA Step-up + 제출 |
| `ws/file:done` | `workspace/file/done.js` | `/efilings/:id` | 접수번호·상태 ACCEPTED |

### 3.10 ③ 사후 관리 (6 leaf)

| leaf | 파일 | 주 API | 본문 |
|---|---|---|---|
| `post/hist:list` | `post/hist/list.js` | `/efilings` | 신고 이력 표 |
| `post/amend:unlock` | `post/amend/unlock.js` | `POST /business-years/:id/unlock` | 잠금 해제 신청 폼 |
| `post/amend:version` | `post/amend/version.js` | `/business-years/:id/amendment-version-mode` (신설) | 원/최신 버전 선택 |
| `post/amend:diff` | `post/amend/diff.js` | `/business-years/:id/amendment-preview` | Before/After 표 |
| `post/amend:resubmit` | `post/amend/resubmit.js` | `POST /business-years/:id/resubmit` (신설) | 재제출 진입 |
| `post/correction` | `post/correction.js` | `/correction-claims` (신설) | 경정청구 |

### 3.11 ④ 분석/보고서 (6 leaf)

| leaf | 파일 | 주 API | 본문 |
|---|---|---|---|
| `report:year-compare` | `report/year_compare.js` | `/reports/year-comparison` | 3·5년 비교 표 + 차트 |
| `report:tax-burden` | `report/tax_burden.js` | `/reports/tax-burden` | 실효세율·한계세율 |
| `report:reserve-trend` | `report/reserve_trend.js` | `/reports/reserve-trend` | 유보 항목별 추이 |
| `report:loss-expiry` | `report/loss_expiry.js` | `/reports/loss-expiry` (신설) | 결손금 만료 예측 |
| `report:industry-stats` | `report/industry_stats.js` | `/reports/industry-stats` (신설) | 업종·규모별 통계 |
| `report:custom` | `report/custom.js` | `/reports/custom` (신설) | 컬럼 토글 + 필터 저장 |

### 3.12 ⑤-A 고객사 관리 (3 leaf)

| leaf | 파일 | 주 API | 본문 |
|---|---|---|---|
| `admin/cust:list` | `admin/cust/list.js` | `/customers` | 고객사 표 + 등록/편집 모달 |
| `admin/cust:by-master` | `admin/cust/by_master.js` | `/business-years?bare=true` | 사업연도 마스터 |
| `admin/cust:agent` | `admin/cust/agent.js` | `/tax-agents` (신설) | 세무대리 계약 |

### 3.13 ⑤-B 사용자/권한 관리 (7 leaf)

| leaf | 파일 | 주 API | 본문 |
|---|---|---|---|
| `admin/sec:users` | `admin/sec/users.js` | `/admin/tenants/:tc/users` | 사용자 표 + 등록/잠금/2FA 리셋 |
| `admin/sec:roles` | `admin/sec/roles.js` | `/admin/roles` | 역할 마스터 + 복제 |
| `admin/sec:matrix` | `admin/sec/matrix.js` | `/admin/role-permissions` | 메뉴×12기능 체크박스 |
| `admin/sec:menus` | `admin/sec/menus.js` | `/admin/menus` | 메뉴 트리/활성 기능/순서 |
| `admin/sec:functions` | `admin/sec/functions.js` | `/admin/functions` (신설) | 기능 코드 마스터 |
| `admin/sec:mask` | `admin/sec/mask.js` | `/admin/field-masking` (신설) | 마스킹 정책 |
| `admin/sec:scope` | `admin/sec/scope.js` | `/admin/data-scope` (신설) | Data Scope 정의 |

### 3.14 ⑤-C 담당 법인 권한 (5 leaf)

| leaf | 파일 | 주 API | 본문 |
|---|---|---|---|
| `admin/cacc:assign` | `admin/cacc/assign.js` | `/admin/customer-access` | 사용자×고객사×등급 표 |
| `admin/cacc:groups` | `admin/cacc/groups.js` | `/admin/customer-groups` (신설) | 그룹 마스터 + 멤버 관리 |
| `admin/cacc:rules` | `admin/cacc/rules.js` | `/admin/customer-rules` (신설) | 업종/지역 자동 룰 |
| `admin/cacc:delegate` | `admin/cacc/delegate.js` | `/admin/access-delegations` (신설) | 위임/대행 |
| `admin/cacc:override` | `admin/cacc/override.js` | `/admin/customer-access/override` (신설) | 개별 예외 |

### 3.15 ⑤-D 법령·세율 버전 (10 leaf)

| leaf | 파일 | 주 API | 본문 |
|---|---|---|---|
| `admin/law:master` | `admin/law/master.js` | `/tax-laws` | F-1: 법령 마스터 |
| `admin/law:rates` | `admin/law/rates.js` | `/tax-rates?law_version_id=…` | F-2: 세율표 |
| `admin/law:limits` | `admin/law/limits.js` | `/tax-limits?category=LIMIT` | F-3: 한도·율 |
| `admin/law:credits` | `admin/law/credits.js` | `…?category=CREDIT` | 공제·감면 율 |
| `admin/law:depr-lives` | `admin/law/depr_lives.js` | `…?category=DEPRECIATION_LIFE` | 기준내용연수 |
| `admin/law:sme` | `admin/law/sme.js` | `…?category=SME_CRITERIA` | 중소기업 판정 |
| `admin/law:loss-rule` | `admin/law/loss_rule.js` | `…?category=LOSS_RULE` | 결손금 공제규정 |
| `admin/law:snapshots` | `admin/law/snapshots.js` | `/business-year-snapshots` | F-5: 사업연도 스냅샷 |
| `admin/law:impact` | `admin/law/impact.js` | `POST /law-versioning/impact` | F-4: 영향 시뮬레이션 |
| `admin/law:history` | `admin/law/history.js` | `/law-amendments` | 개정 공지/이력 |

### 3.16 ⑤-E 서식 버전 (9 leaf)

| leaf | 파일 | 주 API | 본문 |
|---|---|---|---|
| `admin/form:master` | `admin/form/master.js` | `/form-versioning/forms` | 별지 코드 마스터 |
| `admin/form:versions` | `admin/form/versions.js` | `/form-versioning/versions` | G-1: 버전 목록 |
| `admin/form:fields` | `admin/form/fields.js` | `/form-versioning/versions/:id/fields` (신설) | G-2: 항목 편집 |
| `admin/form:validations` | `admin/form/validations.js` | `/form-versioning/versions/:id/validations` (신설) | 검증룰 버전별 |
| `admin/form:linkage-rule` | `admin/form/linkage_rule.js` | `/form-versioning/relationships` | G-5: 연동 규칙 |
| `admin/form:migration` | `admin/form/migration.js` | `/form-versioning/migrations` | G-3: 드라이런/실행/롤백 |
| `admin/form:efile-map` | `admin/form/efile_map.js` | `/form-versioning/efile-map` (신설) | 홈택스 레코드 매핑 |
| `admin/form:by-set` | `admin/form/by_set.js` | `/form-versioning/by-set` (신설) | G-4: 사업연도 적용 세트 |
| `admin/form:impact` | `admin/form/impact.js` | `/form-versioning/impact` (신설) | 서식 영향 시뮬레이션 |

### 3.17 ⑤-F 코드 관리 (1 leaf)

| leaf | 파일 | 주 API | 본문 |
|---|---|---|---|
| `admin/code:manage` | `admin/code/manage.js` | `/codes?group=ALL` (신설) | 업종·표준계정·기타 코드 통합 관리 |

### 3.18 ⑤-G 감사/로그 (4 leaf)

| leaf | 파일 | 주 API | 본문 |
|---|---|---|---|
| `admin/audit:events` | `admin/audit/events.js` | `/tenants/:tc/audit-logs` | C/U/D 이벤트 표 + 해시 |
| `admin/audit:login` | `admin/audit/login.js` | `/login-history` (신설) | 로그인 이력 |
| `admin/audit:perm` | `admin/audit/perm.js` | `/permission-change-history` (신설) | 권한 변경 이력 |
| `admin/audit:settings` | `admin/audit/settings.js` | `/system-settings` (신설) | 시스템 설정 |

---

## 4. 백엔드 신설 라우트 정리

본 문서가 가리키는 “(신설)” 라우트를 단일 마이그레이션/PR로 묶어 선행 작업한다.

| 그룹 | 신설 라우트 |
|---|---|
| 보고서 | `GET /reports/industry-stats`, `GET /reports/loss-expiry`, `POST /reports/custom`, `GET /reports/custom/:id` |
| 사후관리 | `POST /business-years/:id/resubmit`, `GET /business-years/:id/amendment-version-mode`, `/correction-claims` (CRUD) |
| 결재 | `POST /workflow/request`, `GET /workflow/events?status=` |
| 출력 | `GET /print/history` |
| 전자신고 | `POST /efilings/:id/submit` (2FA Step-up 게이트) |
| 서식 | `GET/PUT /form-versioning/versions/:id/fields`, `…/validations`, `GET /form-versioning/field-references`, `GET /form-versioning/efile-map`, `GET /form-versioning/by-set`, `POST /form-versioning/impact` |
| 권한 | `GET /admin/functions`, `GET/PUT /admin/field-masking`, `GET/PUT /admin/data-scope`, `/admin/customer-groups`, `/admin/customer-rules`, `/admin/access-delegations`, `/admin/customer-access/override` |
| 감사 | `GET /login-history`, `GET /permission-change-history`, `GET /system-settings` |
| 고객사 | `/tax-agents` (CRUD) |
| 코드 | `GET /codes?group=…` |
| 대시보드 | `GET /business-years?dueWithinDays=N` 옵션 |
| 서식 작성 | `GET /forms/linkage-check` |

각 라우트는 `src/api.rs`에 등록 + 핸들러 모듈 + DTO + 권한 가드 + 통합 테스트 1건.

---

## 5. 마이그레이션·시드 영향

| 항목 | 변경 |
|---|---|
| `tax_agents`, `customer_groups`, `customer_group_members`, `customer_access_rules`, `access_delegations` | 신규 테이블 (설계서 §4.2) |
| `function_codes` | 12종 기능 코드 마스터 |
| `field_masking_policies`, `data_scope_policies` | 신규 |
| `print_history` | 신규 |
| `correction_claims` | 신규 |
| `report_custom_definitions` | 신규 |
| `system_settings` | 신규 |
| `form_field_definitions`, `form_field_references` | 신규(서식 항목/필드 참조) |
| 시드 | `메뉴기능수정.md` 시더에 위 신규 테이블 데모 3행씩 추가 |

---

## 6. 구현 단계 (Wave 1 ~ Wave 5)

전체 99 leaf를 한 번에 분리하지 않고 **사용자 가치 큰 leaf부터 5 Wave**로 나눈다. 각 Wave 종료 시 `screenByLeaf`에 등록된 leaf만 단독 화면, 나머지는 통합 화면 + leaf 자동 선택(v1.2 Track A) 폴백.

### Wave 1 (2주) — 작업 시작 + 세무정보 입력 + 핵심 세무조정 5개
- 대상 leaf: `ws/start:*` 3 + `ws/info:*` 6 + `ws/adj:B1/B3/B4/B12/B15` 5 = **14 leaf**
- 효과: 사용자가 한 사업연도를 시작해 핵심 5개 세무조정까지 단독 화면으로 처리 가능.
- 백엔드 신설: 없음(기존 API 활용).

### Wave 2 (1.5주) — 나머지 세무조정 12개 + 서식 4 + 검증 3
- 대상 leaf: `ws/adj:B2,B5..B11,B13,B14,B16,B17` 12 + `ws/form:*` 4 + `ws/val:*` 3 = **19 leaf**
- 백엔드 신설: `/forms/linkage-check`, `POST /validation/run` 결과 영속화 보강.

### Wave 3 (1.5주) — 결재·출력·전자신고·사후관리
- 대상: `ws/appr:*` 3 + `ws/print:*` 3 + `ws/file:*` 4 + `post/*` 6 = **16 leaf**
- 백엔드 신설: `POST /workflow/request`, `POST /efilings/:id/submit`(2FA Step-up), `POST /business-years/:id/resubmit`, `/correction-claims`, `/print/history`, `/business-years/:id/amendment-version-mode`.

### Wave 4 (2주) — 관리 5-D 법령 + 5-E 서식 + 5-G 감사
- 대상: `admin/law:*` 10 + `admin/form:*` 9 + `admin/audit:*` 4 = **23 leaf**
- 백엔드 신설: form-versioning leaf 6종, 감사 leaf 3종.

### Wave 5 (2주) — 관리 5-A/5-B/5-C/5-F + 대시보드 + 보고서
- 대상: `admin/cust:*` 3 + `admin/sec:*` 7 + `admin/cacc:*` 5 + `admin/code:*` 1 + `dashboard:*` 5 + `report:*` 6 = **27 leaf**
- 백엔드 신설: 권한 모델 신규 테이블(Wave 5 시작 시점에 마이그레이션 1회 완료).

**합계**: 14 + 19 + 16 + 23 + 27 = **99 leaf 완료**.

---

## 7. 빈 화면·권한 가드 (모든 leaf 공통)

각 leaf `render(env)`는 다음 4가지 가드를 통과해야 정상 본문을 그린다.

```js
function gate(env, meta) {
  if (meta.requires?.includes("work-context") && !hasWorkContext(env.context))
    return emptyState("ctx", env);
  if (!canAccess(env.auth, meta.perm))
    return emptyState("perm", meta);
  if (meta.featureFlag && !isFeatureEnabled(env, meta.featureFlag))
    return emptyState("flag", meta);
  return null;
}
```

`emptyState("ctx" | "perm" | "flag" | "loading" | "error")`는 `components/empty-state.js`로 통일된 안내를 표시한다(점선 박스 + 1차 액션 버튼).

---

## 8. 라우터·메뉴 연결 점검(체크리스트)

leaf 단독 화면을 등록할 때마다 다음 7가지를 점검한다.

1. `screenByLeaf[meta.key]`에 export 함수가 등록되어 있다.
2. `frontend/app/screens.js`가 해당 모듈을 import한다(또는 dynamic `import()`).
3. 백엔드 `menu_nodes`에 `key`가 존재하고 `path`가 `keyToHash(key)` 결과와 일치한다.
4. 좌측 메뉴에서 클릭 시 URL이 변경되고 `renderRoute` 1회 호출된다.
5. 본문 최상위 요소에 `data-leaf-key="<leafKey>"`가 출력된다(회귀 시그너처).
6. `requires` 가드 미충족 시 `empty-state`가 표시된다.
7. `screenByDelegate` 폴백을 호출하지 않는다(로그 또는 `console.assert`).

---

## 9. 테스트 전략

### 9.1 단위 — 화면 모듈 자체
각 leaf 모듈에 대해 `tests/frontend/<group>/<leaf>.spec.js` (또는 Jest jsdom):
- meta 직렬화
- gate 분기 3종 (ctx/perm/flag)
- 1차 API mock → 본문 셀렉터 ≥ 1 존재
- 액션 버튼 클릭 → 두 번째 API 호출

### 9.2 통합 — 백엔드 라우트
- `tests/leaf_routing.rs`: 99 leaf의 1차 API GET이 200 + 본문 시그너처(필수 키 존재) 통과
- `tests/leaf_perm.rs`: 12 기능 코드 × 6 역할 매트릭스에서 ALLOW/DENY가 일관

### 9.3 e2e (Playwright 또는 수동)
- Wave 1 종료 시: 18 leaf 클릭 → `data-leaf-key`가 본문 최상위에 출력되는지 검사
- Wave 5 종료 시: 99 leaf 전수 회귀

### 9.4 CI 게이트
- `cargo test --all` + 신설 leaf_* 통합 테스트
- `npm run leaf-smoke`(노드 스크립트, headless 브라우저 없이 router/menu만 mock)

---

## 10. 작업 산출물 매트릭스

| 분류 | 산출물 | 단위 |
|---|---|---|
| 프론트 화면 모듈 | `frontend/app/screens/<group>/<leaf>.js` × 99 | leaf |
| 프론트 컴포넌트 | `grid.js`, `subnav.js`, `leaf-watermark.js`, `empty-state.js`, `law-tooltip.js`, `chart-mini.js`, `form-modal.js` | 1회 |
| 프론트 디스패치 | `screenByLeaf` 등록 99행 | leaf |
| 백엔드 신설 라우트 | 표 §4의 약 25개 | wave별 묶음 |
| DB 마이그레이션 | 신규 테이블 13종 | wave별 묶음 |
| 시드 | 데모 시더에 leaf별 표시용 데이터 3행 | leaf |
| 테스트 | `tests/leaf_routing.rs`, `tests/leaf_perm.rs`, `tests/frontend/**.spec.js` | wave별 |
| 문서 | `docs/leaf_screen_catalog.md`(자동 생성 표) | 1회 |

---

## 11. 일정 (총 9주)

| 주차 | 활동 |
|---|---|
| W1 | 디스패치 우선순위 변경(`screenByLeaf` 우선) + 공통 컴포넌트 7종 + Wave 1 절반(`ws/start`+`ws/info`) |
| W2 | Wave 1 마무리(`ws/adj` 5종 단독) — Wave 1 종료, 사용자 시연 |
| W3 | Wave 2 (`ws/adj` 12 + `ws/form` 4) |
| W4 | Wave 2 마무리(`ws/val` 3) + Wave 3 시작 |
| W5 | Wave 3 (`ws/appr` + `ws/print` + `ws/file` + `post/*`) |
| W6 | Wave 4 백엔드 신설 라우트 + `admin/law` 10 |
| W7 | Wave 4 `admin/form` 10 + `admin/audit` 5 |
| W8 | Wave 5 권한 모델 마이그레이션 + `admin/sec`·`admin/cust`·`admin/cacc` |
| W9 | Wave 5 `admin/code`·`dashboard`·`report` + 회귀·e2e·문서화 |

---

## 12. 위험 및 대응

| 위험 | 대응 |
|---|---|
| 99 leaf 모듈로 분리 시 PR 수 폭증 | wave당 1 PR + `screenByLeaf` 등록은 별도 사소 PR로 분리 |
| 신설 백엔드 라우트의 핸들러 작업이 leaf 작업을 막음 | 신설 라우트는 wave 시작 1주 전 “인터페이스 stub”(빈 응답)로 먼저 머지하고 leaf 작업 병행 |
| 권한 가드 도입이 기존 화면 회귀 유발 | `canAccess`를 처음에는 “경고 로그만”으로 두고 wave 3에서 enforce로 전환 |
| 동일 그룹 leaf에서 본문 구조가 거의 같음(중복) | `templates/<group>.js`로 공통 본문 함수 분리 후 leaf는 데이터만 채움 |
| screenByDelegate 폴백이 의도치 않게 활성화 | leaf 등록 후 회귀 테스트가 `data-leaf-key` 부재를 fail 처리 |

---

## 13. 완료 정의 (DoD)

1. `screenByLeaf`에 99개 leaf가 모두 등록되어 있고, 미등록 leaf가 없다.
2. 좌측 메뉴 99개를 순차 클릭하면 본문 최상위 요소에 `data-leaf-key="<해당 leaf>"`가 출력된다.
3. 각 leaf의 1차 API 응답이 본문에 반영되고, 액션 버튼이 명세표대로 두 번째 API를 호출한다.
4. workContext/권한/feature_flag 미충족 시 `empty-state`가 표시된다.
5. 신설 백엔드 라우트 25종이 `src/api.rs`에 등록되어 200을 반환한다.
6. `tests/leaf_routing.rs`·`tests/leaf_perm.rs`·`tests/frontend/**` 가 전부 green.
7. `2차구현계획.md` · `메뉴기능수정.md` · `추가구현_v1.0~1.2.md` 가 가리키는 메뉴 IA·기능 정의와 본 문서가 일관된다.
