# 법령세율관리 모듈 구현 기준

이 파일은 `법령세율버전관리.md`의 상세 설계를 실제 구현 목표명인 법령세율관리 모듈 기준으로 연결하는 문서다.

## 구현된 메뉴 구조

```text
법령·세율 버전 관리
├── 법령 버전 마스터
├── 법인세율표
├── 한도·율표
├── 세액공제·감면 율표
├── 기준내용연수표
├── 중소기업 판정기준
├── 결손금 공제규정
├── 사업연도별 적용 스냅샷
├── 영향 시뮬레이션
└── 개정 공지/이력
```

## 구현 산출물

| 영역 | 파일 |
| --- | --- |
| 메뉴 트리 | `src/modules.rs` |
| API 라우팅 | `src/api.rs` |
| 법령·세율 도메인 로직 | `src/tax.rs` |
| 요청/응답 타입 | `src/domain.rs` |
| 웹 화면 | `frontend/index.html`, `frontend/app.js`, `frontend/app.css` |
| 검증 | `tests/integration_flow.rs` |

## 주요 동작

- 좌측 메뉴에서 법령·세율 버전 관리 하위 메뉴 10개를 선택할 수 있다.
- 각 하위 메뉴 선택 시 `frontend/app.js`의 `navigateLawRoute`가 전용 화면을 렌더링한다.
- 법령 버전, 세율표, 한도·율표, 공제·감면, 기준내용연수, 중소기업 판정기준, 결손금 공제규정은 실제 API와 DB를 통해 조회·등록된다.
- 사업연도별 적용 스냅샷은 기존 `by_law_snapshot` 생성/조회 API와 연결된다.
- 영향 시뮬레이션은 법령 버전별 세율/한도 행과 테넌트별 대상 사업연도 수를 실제 DB에서 집계한다.
- 개정 공지/이력은 `law_amendment_history`에 저장된다.

## Multi-Tenant 테넌트-고객사 관계 보완 (2026-05-15)

- 대상 사용자 조직은 `회계법인`, `세무법인`, `세무사사무소`이며, 시스템에서는 각각 독립 테넌트로 관리한다.
- 관계 모델은 `테넌트 1 : N 고객사`이다. 하나의 테넌트는 여러 고객사를 관리하고, 각 고객사는 반드시 하나의 테넌트에만 소속된다.
- UI 기준으로 `prototype/index.html`의 `5-0 테넌트 관리` 화면에서 테넌트 유형, 계약, 상태, 사용자 수, 고객사 수, 소속 고객사 샘플을 확인한다.
- `5-A 고객사 관리` 화면은 고객사 목록에 소속 테넌트 컬럼과 테넌트 필터를 제공하고, 신규 등록/편집 시 소속 테넌트를 선택한다.
- 사용자 권한은 테넌트 범위 안에서 적용되며, 특정 고객사 접근은 `5-E 담당 법인 권한`에서 사용자-고객사 단위로 추가 제한한다.
- DB/도메인 설계 시 고객사 테이블은 `tenant_id` 또는 `tenant_code` 외래키를 가져야 하며, 고객사 코드는 `tenant_id + customer_code` 범위에서 유일해야 한다.
## 사용자-테넌트-고객사 관리 보완 (2026-05-15)

- 사용자는 반드시 하나의 소속 테넌트(`tenant_id`)를 가진다. 테넌트는 회계법인, 세무법인, 세무사사무소 단위 조직이다.
- 사용자의 고객사 접근 범위는 `user_customer_access` 또는 동등한 매핑으로 관리하며, 한 사용자는 소속 테넌트 안의 여러 고객사에 접근할 수 있다.
- `prototype/index.html`의 `5-B 사용자 관리` 화면은 테넌트 필터, 고객사 필터, 소속 테넌트 컬럼, 고객사 권한 요약을 제공한다.
- 사용자 등록/편집 시 소속 테넌트를 먼저 선택하고, 선택한 테넌트에 속한 고객사만 접근 범위로 선택할 수 있다.
- 역할별 기능 권한은 `5-C 역할 / 권한 매트릭스`에서 관리하고, 특정 고객사 담당 등급 또는 차단 같은 예외는 `5-E 담당 법인 권한`에서 관리한다.
- 권한 판정 순서는 `테넌트 소속 확인 -> 고객사 접근 범위 확인 -> 역할/기능 권한 확인 -> 고객사별 예외 권한 확인`을 기본 원칙으로 한다.
## 고객사별 대상 업무 범위 보완 (2026-05-15)

- 사용자 권한은 `tenant_id + customer_id + work_scope` 조합으로 판정한다. 같은 테넌트와 고객사라도 사용자마다 허용 업무가 다를 수 있다.
- 대상 업무 코드는 `INFO`, `ADJUST`, `FORM`, `VALIDATE`, `APPROVE`, `PRINT`, `EFILE`, `POST`를 기본값으로 사용한다.
- 권한 저장은 `user_customer_work_scope` 테이블을 별도로 두거나, `user_customer_access`에 `work_scopes` JSON/배열 컬럼을 추가하는 방식으로 구현한다.
- `prototype/index.html`의 `5-B 사용자 관리` 화면은 대상 업무 필터와 고객사별 대상 업무 체크리스트를 제공한다.
- 사용자 등록/편집 시 소속 테넌트를 선택한 뒤, 해당 테넌트의 고객사를 선택하고, 선택된 고객사마다 허용 업무 범위를 별도로 지정한다.
- 업무 실행 전 권한 판정 순서는 `테넌트 소속 확인 -> 고객사 접근 확인 -> 고객사별 대상 업무 확인 -> 역할/기능 권한 확인 -> 예외 권한 확인`을 따른다.
- 예: 같은 `EY 회계법인 / ㈜OOO 제조` 고객사라도 A 사용자는 `ADJUST`, `FORM`, `VALIDATE`만 가능하고, B 사용자는 `APPROVE`, `PRINT`만 가능하게 분리할 수 있다.
## 테넌트별 고객사 대상 업무 구분 보완 (2026-05-15)

- 법령세율 기능 접근도 고객사 대상 업무와 사용자별 업무 권한을 모두 통과해야 한다.
- 고객사가 `VALIDATE`, `APPROVE`를 대상 업무로 갖지 않으면 해당 고객사 사용자에게 검증/결재 업무를 부여하지 않는다.
- 사용자 권한은 고객사별 대상 업무 범위의 부분집합으로 관리한다.

## Phase 7 세무정보 입력 연계 (2026-05-15)

- 사업연도별 세무정보 입력 데이터는 법령·세율 스냅샷이 생성된 사업연도에 연결된다.
- 재무제표, 자산대장, 거래 명세는 이후 세무조정 엔진에서 법령·세율 버전 기준으로 계산할 원천 데이터가 된다.
- 임포트 검증 오류가 남아 있으면 후속 검증/결재 단계에서 보정 대상으로 표시한다.

## Phase 8 세무조정 엔진 연계 (2026-05-15)

- B-1 계산 결과는 사업연도 법령 스냅샷 ID와 함께 저장된다.
- 적용 법령 배너는 스냅샷의 법령 버전 정보를 사용한다.
- 조정 항목의 법조항(`law_ref`)은 화면에서 근거 조항으로 표시된다.

## Phase 9 자산 기반 세무조정 연계 (2026-05-15)

- B-4 감가상각은 Phase 4 기준내용연수 한도 데이터를 조회해 세법상각한도를 계산한다.
- B-10 업무용승용차는 법정 한도와 운행기록 업무사용비율을 적용한다.
- 계산 결과는 스냅샷 ID와 함께 저장되어 법령 버전별 재계산 근거가 남는다.

## Phase 10 거래 기반 세무조정 연계 (2026-05-15)

- B-2 기부금은 법령 버전별 `DONATION_SPECIAL_LIMIT_BPS`, `DONATION_GENERAL_LIMIT_BPS`, `DONATION_CARRYFORWARD_YEARS`를 사용한다.
- B-3 접대비는 `ENTERTAINMENT_BASE_LIMIT`, `ENTERTAINMENT_REVENUE_RATE_BPS`, `ENTERTAINMENT_NO_CARD_DISALLOW_BPS`를 사용해 한도를 계산한다.
- B-9 지급이자는 `INTEREST_DEEMED_RATE_BPS`를 기본 가중평균 이자율로 사용하며 요청값이 있으면 요청값을 우선한다.
- 모든 거래 기반 조정 결과는 사업연도 스냅샷 ID와 함께 저장되어 법령 버전별 재계산 근거가 남는다.

## Phase 11 평가·이월·유보 연계 (2026-05-15)

- B-11 이월결손금은 법령 버전별 `LOSS_CARRYFORWARD_YEARS`와 `LOSS_DEDUCTION_LIMIT_BPS_SME|GENERAL`을 사용한다.
- B-7/B-8 평가 결과와 B-15 유보 집계는 사업연도 스냅샷 ID와 함께 저장된다.
- 법령 개정 영향 분석은 잠금 전 사업연도의 평가 포지션, 결손금 잔액, 유보 집계를 재계산 대상으로 식별할 수 있다.

## Phase 12 세액 연계 (2026-05-15)

- B-12는 `RND_CREDIT_BPS`, 투자·외국납부·재해·중소특별·창업 감면율을 법령 버전별로 사용한다.
- B-13은 `MINIMUM_TAX_RATE_BPS_SME|GENERAL`을 사용해 최저한세를 비교한다.
- B-14 계산 결과는 스냅샷 ID와 함께 저장되어 신고 시점의 가산세 계산 근거를 보존한다.

## 사용자-고객사 업무 권한 연계 보완 (2026-05-15)

- 법령세율 기능을 실행하려면 사용자가 소속 테넌트의 고객사에 접근할 수 있어야 하고, 해당 고객사가 관련 대상 업무를 포함해야 한다.
- 사용자별 업무 권한은 고객사 `work_scopes`의 부분집합으로 저장되므로, 고객사 대상 업무가 아닌 법령/검증/결재 작업은 권한으로 부여되지 않는다.
- 운영 UI의 사용자 편집 화면은 고객사별 대상 업무와 사용자 부여 업무를 함께 보여 법령 관련 작업 권한 누락을 사전에 확인하게 한다.

## Phase 13 특수 세무조정 연계 (2026-05-15)

- B-16 외국법인 세무조정은 사업연도 스냅샷과 함께 저장되어 당시 법령 기준의 원천징수/과세소득 산출 근거를 보존한다.
- B-17 연결납세는 연결 대상 법인과 내부거래 제거 근거를 저장해 법령 개정 영향 분석 시 재계산 대상으로 식별할 수 있다.

## Phase 14 서식 엔진 연계 (2026-05-15)

- FORM3 생성 결과는 사업연도 법령 스냅샷 ID와 서식 버전 ID를 함께 저장한다.
- 법령/세율 재계산 영향 분석은 FORM3의 자동 생성 필드와 수동 수정 필드를 구분해 재생성 가능 여부를 판단한다.
- 수동 수정 이력은 `form_data_history`에 보존되어 법령 개정 후 재생성 시 검토 대상이 된다.

## Phase 15 부속서식/PDF 연계 보완 (2026-05-15)

- 서식 PDF 출력은 사업연도별 법령 스냅샷과 서식 버전 해석 결과를 전제로 수행한다.
- 부속서식 일람 화면은 FORM3, FORM15, FORM22의 생성/검증 상태를 보여주며, PDF 다운로드 시 현재 스냅샷에 연결된 서식 데이터를 사용한다.
- 법령·세율 변경으로 재계산이 필요한 경우 기존 법령 스냅샷 잠금 원칙을 유지하고, 새 사업연도 또는 수정신고 흐름에서 새 서식 PDF를 생성한다.

## Phase 16 전자신고 연계 보완 (2026-05-15)

- 전자신고 파일은 사업연도 스냅샷의 `efile_master_ids`와 현재 적용 가능한 `efile_masters`를 기준으로 생성한다.
- 사전검증은 FORM3 총 납부세액 등 스냅샷 기반 계산 결과가 전자신고 메타와 일치하는지 확인한다.
- 법령/세율 개정 후 재계산이 필요한 경우 새 스냅샷과 새 전자신고 마스터 조합으로 파일을 다시 생성한다.
