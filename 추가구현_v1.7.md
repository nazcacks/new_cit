# 추가 구현 계획 v1.7
## 전 화면 다국어 정합성 복구 — 한국어/영어 설정 기반 UI 문구 전환

> 작성일: 2026-05-21 · 작성자: Codex
> 선행 문서: `추가구현_v1.0~v1.6.md`, `메뉴기능수정.md`, `메뉴_API_매트릭스.md`
> 본 문서의 범위:
> 1) 언어가 **한국어(ko)** 로 선택되어 있을 때 영어로 보이는 화면 문구를 제거한다.
> 2) 언어가 **English(en)** 로 선택되어 있을 때 한국어로 고정되어 있는 화면 문구도 영어로 전환되게 한다.
> 3) 메뉴/화면 제목/버튼/표 헤더/상태값/오류/빈 상태/모달/검증 메시지를 `state.locale` 기준으로 일관 렌더링한다.
> 4) v1.6의 typology 화면과 수정 버튼 계획에 들어가는 신규 문구도 같은 i18n 체계에 포함한다.

---

## 1. 증상 진단

### 1.1 i18n 사전 범위가 너무 좁다
- 현재 `frontend/app/i18n.js`에는 `health`, `auth`, `context`, `tenant`, `leaf`, `step` 일부 키만 있다.
- 반면 실제 화면에는 `screens.js`, `grid.js`, `index.html`, `menu.js`에 버튼/제목/상태/표 헤더가 직접 문자열로 들어가 있다.
- 결과: `state.locale === "ko"`여도 `Overview`, `Due soon`, `Tax workspace`, `Edit`, `Delete`, `Settings audit` 같은 문구가 그대로 보인다.

### 1.2 leaf route 제목이 영어 하드코딩
- `frontend/app/screens.js`의 `leafRoutes`는 대부분 영어 title/group으로 생성된다.
  - 예: `Dashboard`, `Overview`, `Tax workspace`, `Financial statements import`, `B14 Additional tax`, `Administration`
- `routeLabelsFromMenu()`가 메뉴 트리 라벨을 우선 사용하지만, 메뉴 트리 또는 fallback meta가 영어만 갖고 있으면 화면 상단 `routeGroup`, `routeTitle`이 영어로 남는다.

### 1.3 메뉴 트리 원본도 영문 중심
- `src/modules.rs`의 `prototype_menu_defs()`는 `Dashboard`, `Tax workspace`, `Post filing`, `Administration` 등 영어 라벨을 원본으로 가진다.
- 일부 seed는 `korean_label()`을 거쳐 한국어 label로 저장되지만, 런타임 API와 프론트 fallback이 동일한 `labels.ko/en` 계약을 갖고 있지 않아 화면별 편차가 생긴다.

### 1.4 정적 HTML과 공통 컴포넌트가 locale 변경을 따라가지 않는다
- `frontend/index.html`에는 로그인, 로그아웃, 언어 선택 aria-label, health 기본 문구 등 한국어 고정 문구가 있다.
- `frontend/app/menu.js`의 상태 pill은 `LOCKED`, `OPEN`처럼 영어 코드가 그대로 보인다.
- 언어 변경 시 `document.documentElement.lang`도 갱신하지 않아 접근성/브라우저 번역 기준이 실제 선택 언어와 다를 수 있다.

### 1.5 동적/업무 데이터 문구의 번역 기준이 없다
- 상태 코드, 권한명, 컬럼 포맷, 빈 상태 안내, 검증 오류, 수정 모달 라벨은 화면별로 제각각 만들어진다.
- 업무 코드 자체(`B1`, `FORM3`, `ACTIVE`)는 코드로 보여도 되지만, 사용자에게 보이는 설명 라벨은 locale별 표시명이 필요하다.

---

## 2. 복구 목표

1. 모든 사용자 노출 문자열은 `t(locale, key, params)` 또는 `labelForNode(node, locale)` 같은 locale-aware helper를 통해 렌더링한다.
2. 한국어 선택 시 전 화면에서 메뉴, 제목, 버튼, 표 헤더, 모달, 상태, 오류, 빈 상태가 한국어로 보인다.
3. 영어 선택 시 같은 화면이 영어로 보이고, 한국어 고정 문구가 남지 않는다.
4. 메뉴 노드와 leaf route는 `labels: { ko, en }`를 표준 계약으로 가진다.
5. v1.6 typology 화면(`grid`, `dashboard`, `wizard`, `form`, `chart`, `detail`, `grid-tree`)의 신규 UI 문구도 모두 번역 키로 관리한다.
6. 언어 변경은 새로고침 없이 현재 화면, 사이드바, 상단바, 모달/빈 상태까지 즉시 재렌더한다.
7. 테스트에서 ko/en 각각의 대표 화면에 영어/한국어 잔존 문구가 없는지 자동 확인한다.

---

## 3. i18n 아키텍처

### 3.1 단일 locale source 유지
현재 구조의 `state.locale = loadLocale()`과 `saveLocale(locale)`는 유지한다.

추가로 언어 변경 시 다음을 반드시 수행한다.

```js
function applyLocale(locale) {
  state.locale = saveLocale(locale);
  document.documentElement.lang = locale === "en" ? "en" : "ko";
  renderStaticShell(locale);
  renderRoute();
}
```

- `document.documentElement.lang` 갱신.
- 로그인 화면/상단바/사이드바/health/tenant switcher 재렌더.
- 현재 route 재렌더.
- 열려 있는 모달이 있으면 닫거나 locale-aware rerender를 수행한다. 1차 구현은 모달 닫기 + toast 안내로 단순화한다.

### 3.2 `t()` helper 확장
현재 `t(locale, key)`를 보강해 치환 파라미터와 fallback 경고를 지원한다.

```js
export function t(locale, key, params = {}) {
  const normalized = normalizeLocale(locale);
  const template = strings[normalized]?.[key] || strings.ko[key] || key;
  return interpolate(template, params);
}
```

- 예: `t(locale, "leaf.count", { count: rows.length })`
- ko/en 양쪽에 키가 없으면 dev console에 `missing i18n key` 경고.
- 테스트 환경에서는 누락 키를 실패로 처리할 수 있게 `collectMissingI18nKeys()` 보조 함수를 둔다.

### 3.3 번역 키 네임스페이스

| namespace | 예시 | 용도 |
|---|---|---|
| `app.*` | `app.title`, `app.subtitle` | 브랜드/앱 공통 |
| `auth.*` | `auth.login`, `auth.logout`, `auth.password` | 로그인/세션 |
| `common.*` | `common.add`, `common.edit`, `common.delete`, `common.save`, `common.cancel` | 버튼/공통 액션 |
| `nav.*` | `nav.dashboard`, `nav.workspace` | 상위 메뉴/경로 |
| `route.*` | `route.dashboard.overview`, `route.admin.audit.settings` | leaf 화면 제목 |
| `grid.*` | `grid.search`, `grid.filter`, `grid.actions`, `grid.empty` | 표/검색/페이지 |
| `modal.*` | `modal.editTitle`, `modal.deleteTitle` | 모달 제목/본문 |
| `status.*` | `status.locked`, `status.open`, `status.active` | 상태값 표시 |
| `field.*` | `field.customerName`, `field.bizRegNo` | 폼/표 컬럼 라벨 |
| `typology.*` | `typology.dashboard.updated`, `typology.wizard.next` | typology별 특수 문구 |
| `validation.*` | `validation.required`, `validation.invalidDate` | 검증 메시지 |

---

## 4. 변경 코드 골격

### 4.1 정적 shell 문구 렌더러
`index.html`에 박힌 정적 문구를 JS에서 locale별로 채운다.

```js
function renderStaticShell(locale) {
  $("appTitle").textContent = t(locale, "app.title");
  $("tenantSearchBtn").textContent = t(locale, "tenant.search");
  $("loginTenantLabel").textContent = t(locale, "auth.tenant");
  $("loginIdLabel").textContent = t(locale, "auth.loginId");
  $("loginPasswordLabel").textContent = t(locale, "auth.password");
  $("loginBtn").textContent = t(locale, "auth.login");
  $("logoutBtn").textContent = t(locale, "auth.logout");
  $("languageSelect").setAttribute("aria-label", t(locale, "language.select"));
}
```

필요하면 `index.html`에 `id` 또는 `data-i18n`을 추가한다.

```html
<strong data-i18n="app.title">법인세 세무조정계산서 시스템</strong>
<button id="logoutBtn" data-i18n="auth.logout">로그아웃</button>
```

### 4.2 leaf route title 계약
`leafRoute()`는 문자열 title 대신 locale label 객체를 받도록 확장한다.

```js
function leafRoute(key, groupKey, titleKey, layout, delegate) {
  return {
    key,
    groupKey,
    titleKey,
    layout,
    delegate,
    group: (locale) => t(locale, groupKey),
    title: (locale) => t(locale, titleKey),
  };
}
```

기존 hardcode는 다음처럼 교체한다.

```js
["dashboard:overview", "route.dashboard.overview"],
["ws/info:fs", "route.ws.info.fs"],
["admin/audit:settings", "route.admin.audit.settings"],
```

### 4.3 메뉴 노드 labels 표준화
백엔드 메뉴 API는 모든 노드에 `labels`를 포함한다.

```json
{
  "code": "dashboard",
  "label": "대시보드",
  "labels": {
    "ko": "대시보드",
    "en": "Dashboard"
  }
}
```

- `src/modules.rs`의 `MenuDef`에 `label_ko`, `label_en` 또는 `labels` 생성 helper를 둔다.
- DB seed(`menu_nodes`)도 `label`은 한국어 기본값으로 유지하되, API 응답에는 `labels.ko/en`을 제공한다.
- `labelForNode(node, locale)`는 이미 `labels.en/ko`를 우선하므로 이 계약을 강화한다.

### 4.4 공통 액션/표 문구 치환
v1.6에서 도입되는 행 액션과 그리드 헤더는 모두 `t()`를 쓴다.

```js
function renderLeafRowActions(state, item) {
  const locale = state.env.locale;
  return `
    <button data-row-edit aria-label="${escapeHtml(t(locale, "common.edit"))}">
      ${escapeHtml(t(locale, "common.edit"))}
    </button>
    <button data-row-delete aria-label="${escapeHtml(t(locale, "common.delete"))}">
      ${escapeHtml(t(locale, "common.delete"))}
    </button>`;
}
```

표 헤더도 컬럼별로 `labelKey`를 우선한다.

```js
{ key: "customer_name", labelKey: "field.customerName", format: "text" }
```

fallback 순서:
1. `column.labelKey` → `t(locale, labelKey)`
2. `column.labels?.[locale]`
3. 기존 `column.label`
4. `column.key`

### 4.5 상태/코드 라벨 치환
화면에 직접 노출되는 상태 코드는 `formatStatus(value, locale)`을 통과한다.

```js
const STATUS_LABEL_KEYS = {
  LOCKED: "status.locked",
  OPEN: "status.open",
  ACTIVE: "status.active",
  INACTIVE: "status.inactive",
  DRAFT: "status.draft",
  SUBMITTED: "status.submitted",
  APPROVED: "status.approved",
  REJECTED: "status.rejected",
};
```

- 내부 코드값은 그대로 유지한다.
- 사용자 표시만 locale별 라벨로 바꾼다.
- 배지 class는 기존 `statusClass(value)`를 유지한다.

---

## 5. 번역 사전 1차 확장 범위

### 5.1 공통/인증/상단바
필수 키:

```js
"app.title": "법인세 세무조정계산서 시스템",
"app.subtitle": "법인세 신고 작업",
"language.select": "언어 선택",
"auth.tenant": "테넌트",
"auth.loginId": "로그인 ID",
"auth.password": "비밀번호",
"auth.login": "로그인",
"auth.logout": "로그아웃",
"tenant.search": "테넌트 검색",
"health.pending": "확인 중",
"common.add": "추가",
"common.edit": "수정",
"common.delete": "삭제",
"common.save": "저장",
"common.cancel": "취소",
"common.search": "검색",
"common.filter": "필터",
"common.actions": "관리",
"common.detail": "상세",
"common.retry": "다시 시도",
```

영어 사전에는 각각 `Corporate Income Tax Adjustment System`, `Language`, `Login`, `Logout`, `Add`, `Edit` 등 대응 문구를 넣는다.

### 5.2 route 제목
`frontend/app/screens.js`의 모든 `leafRoutes` 항목을 route key로 이전한다.

예시:

| route key | ko | en |
|---|---|---|
| `route.dashboard.overview` | 개요 | Overview |
| `route.dashboard.duesoon` | 마감 임박 | Due soon |
| `route.ws.start.customerPick` | 고객사 선택 | Customer selection |
| `route.ws.info.fs` | 재무제표 가져오기 | Financial statements import |
| `route.ws.adj.B14` | B14 가산세 | B14 Additional tax |
| `route.admin.audit.settings` | 설정 변경 감사 | Settings audit |

### 5.3 typology/v1.6 신규 문구
v1.6 구현과 충돌하지 않도록 다음 키를 먼저 준비한다.

```js
"typology.grid.emptyTitle": "표시할 데이터가 없습니다.",
"typology.grid.emptyDescription": "조건을 바꾸거나 새 항목을 추가하세요.",
"typology.grid.addRow": "행 추가",
"typology.wizard.previous": "이전",
"typology.wizard.next": "다음",
"typology.form.preview": "미리보기",
"typology.chart.range": "기간",
"modal.editTitle": "{title} 수정",
"modal.deleteTitle": "{title} 삭제",
"modal.saveSuccess": "저장되었습니다.",
"modal.saveFailed": "저장하지 못했습니다.",
```

---

## 6. 파일별 작업 계획

| 파일 | 작업 |
|---|---|
| `frontend/app/i18n.js` | `strings.ko/en` 대폭 확장, `t(locale,key,params)`, missing key 수집, route/field/status helper 추가 |
| `frontend/index.html` | 정적 문구에 `id`/`data-i18n` 부여. 기본 문구는 한국어 유지하되 JS 렌더 대상화 |
| `frontend/app/app.js` | `renderStaticShell(locale)`, `applyLocale(locale)`, `document.documentElement.lang` 갱신, 언어 변경 시 현재 route 재렌더 |
| `frontend/app/menu.js` | `LOCKED/OPEN` 직접 표시 제거, 상태/aria/title 모두 `t()` 적용 |
| `frontend/app/screens.js` | `leafRoutes` title/group을 `route.*` key로 교체. 버튼/표/모달/empty-state/typology 문구를 `t()`로 치환 |
| `frontend/app/components/grid.js` | `Add sample`, `Import`, `Run`, 표 empty/action 문구를 locale 인자로 렌더 |
| `src/modules.rs` | 메뉴 API 응답에 `labels.ko/en` 표준 제공. prototype root/legacy node에도 labels 추가 |
| `tests/menu_full_tree.rs` | 모든 menu node가 `labels.ko/en`을 갖는지 검증 |
| `tests/frontend/i18n.spec.js` | ko/en 전환 시 대표 화면의 제목/버튼/상태 문구가 바뀌는지 검증 |
| `tests/frontend/typology.spec.js` | v1.6 typology 화면의 `+ 추가`, `관리`, `수정`, `삭제`가 locale별로 바뀌는지 검증 |

---

## 7. 단계별 작업 (3일)

### D1 — i18n 기반 정비
1. `i18n.js`의 `t()`를 parameter/fallback/missing-key 지원 형태로 확장.
2. `app.title`, `auth.*`, `common.*`, `status.*`, `grid.*`, `modal.*` 최소 사전 작성.
3. `index.html` 정적 문구에 `data-i18n` 또는 id를 부여.
4. `app.js`에 `renderStaticShell(locale)`과 `applyLocale(locale)` 추가.
5. 언어 변경 시 `document.documentElement.lang`, 로그인 화면, 상단바, 현재 route가 즉시 바뀌는지 확인.

### D2 — 메뉴/route/화면 문구 치환
1. `src/modules.rs` 메뉴 노드에 `labels.ko/en` 생성.
2. `leafRoutes`의 영어 title을 `route.*` key 기반으로 교체.
3. `screens.js`의 hardcoded button/empty/modal/table 문구를 `t()`로 치환.
4. `components/grid.js`가 `locale`을 받아 버튼/empty/action 문구를 렌더하게 수정.
5. `menu.js`의 `LOCKED/OPEN` 사용자 표시를 `status.locked/status.open`으로 치환.

### D3 — 검증/회귀
1. `tests/frontend/i18n.spec.js` 추가.
2. 대표 화면 12개에서 ko/en 스냅샷 또는 DOM 텍스트 검증.
3. `rg` 기반 하드코딩 점검 스크립트 추가 또는 테스트에 포함.
4. ko 선택 시 `Overview`, `Tax workspace`, `Edit`, `Delete`, `Actions`, `Settings audit` 같은 영어 잔존 문구가 없는지 확인.
5. en 선택 시 `대시보드`, `로그아웃`, `수정`, `삭제`, `관리` 같은 한국어 잔존 문구가 없는지 확인.

---

## 8. 테스트 계획

### 8.1 자동 테스트
대표 테스트 케이스:

| 화면 | ko 기대 | en 기대 |
|---|---|---|
| 로그인 | 로그인, 비밀번호, 테넌트 검색 | Login, Password, Search tenants |
| 대시보드 | 대시보드, 개요, 마감 임박 | Dashboard, Overview, Due soon |
| 신고 작업 | 신고 작업, 고객사 선택, 작업 컨텍스트 | Tax workspace, Customer selection, Work context |
| 그리드 화면 | 추가, 수정, 삭제, 관리, 검색 | Add, Edit, Delete, Actions, Search |
| 수정 모달 | `{화면명} 수정`, 저장, 취소 | Edit {title}, Save, Cancel |
| 상태 배지 | 잠금, 편집 가능 | Locked, Editable |
| 관리자 | 관리, 사용자, 설정 변경 감사 | Administration, Users, Settings audit |

### 8.2 하드코딩 탐지
다음 패턴은 계획상 허용 목록을 제외하고 점검한다.

```powershell
rg -n '"(Overview|Dashboard|Tax workspace|Administration|Edit|Delete|Save|Cancel|Actions|Settings audit)"' frontend src tests
rg -n '>(로그인|로그아웃|수정|삭제|저장|취소|관리)<' frontend
```

- 코드 상수/업무 코드/테스트 기대값은 허용 목록에 둔다.
- 사용자 화면에 직접 출력되는 literal은 `t()` 또는 `labels`로 교체한다.

### 8.3 수동 QA
1. 한국어 선택 → 새로고침 → 로그인 화면부터 관리자 화면까지 한국어 표시 확인.
2. 영어 선택 → 새로고침 → 동일 화면 영어 표시 확인.
3. 언어 선택을 바꾸고 새로고침 없이 현재 화면 제목/버튼/표 헤더가 바뀌는지 확인.
4. v1.6의 `+ 추가`, `수정`, `삭제`, 수정 모달 저장/오류 메시지도 locale을 따르는지 확인.

---

## 9. 회귀·점검 체크리스트

- [ ] `state.locale === "ko"`일 때 모든 화면의 사용자 표시 문구가 한국어다.
- [ ] `state.locale === "en"`일 때 모든 화면의 사용자 표시 문구가 영어다.
- [ ] 메뉴 API의 모든 노드가 `labels.ko`와 `labels.en`을 가진다.
- [ ] `leafRoutes`에 사용자 노출용 영어 literal title/group이 남지 않는다.
- [ ] `index.html`의 로그인/상단바 정적 문구가 언어 변경을 따라간다.
- [ ] `document.documentElement.lang`이 선택 언어와 일치한다.
- [ ] 표 헤더, 행 액션, 빈 상태, 페이지네이션, 검색/필터 문구가 locale을 따른다.
- [ ] 수정 모달의 제목/저장/취소/오류/성공 문구가 locale을 따른다.
- [ ] 상태 코드(`LOCKED`, `OPEN`, `ACTIVE` 등)가 사용자 화면에 그대로 노출되지 않는다.
- [ ] 누락된 i18n key가 dev console 또는 테스트에서 잡힌다.

---

## 10. 위험 및 대응

| 위험 | 대응 |
|---|---|
| 번역 키가 한 번에 너무 많아 누락 발생 | D1에서 공통/route/status 우선, D2에서 화면별 치환, D3에서 누락 키 테스트로 수렴 |
| 업무 코드와 표시 문구를 과도하게 번역 | `B1`, `FORM3`, `ACTIVE` 같은 원천 코드는 보존하고 설명 라벨만 번역 |
| 메뉴 DB seed와 프론트 fallback 라벨이 불일치 | 백엔드 API의 `labels.ko/en`를 우선 계약으로 두고 프론트 fallback은 보조로만 사용 |
| 언어 변경 후 일부 컴포넌트가 이전 locale로 남음 | `renderRoute()` 전체 재렌더를 기본으로 하고, 모달은 1차에서 닫거나 재생성 |
| 테스트에서 한국어/영어 혼용을 오탐 | 허용 목록(브랜드 CIT, 업무 코드 B1, API key, email 등)을 명시 |

---

## 11. 완료 정의 (DoD)

1. `frontend/app/i18n.js`가 공통/route/field/status/modal/grid/typology 문구의 ko/en 사전을 제공한다.
2. `frontend/index.html`, `app.js`, `menu.js`, `screens.js`, `components/grid.js`의 사용자 노출 문구가 locale-aware 방식으로 렌더된다.
3. `src/modules.rs`의 메뉴 응답이 모든 노드에 `labels.ko/en`을 포함한다.
4. 한국어 선택 시 대표 화면 12개에서 영어 UI 문구가 남지 않는다.
5. 영어 선택 시 대표 화면 12개에서 한국어 UI 문구가 남지 않는다.
6. 언어 변경 후 새로고침 없이 현재 화면 제목, 사이드바, 버튼, 표 헤더, 상태 배지가 갱신된다.
7. `tests/frontend/i18n.spec.js`와 기존 메뉴/typology 테스트가 통과한다.
8. v1.6에서 계획한 `+ 추가`, `수정`, `삭제`, 수정 모달 동작도 선택 언어에 맞는 문구로 표시된다.
