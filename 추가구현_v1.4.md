# 추가 구현 계획 v1.4
## 테넌트 선택 복구 + 업무 흐름 가시화 복구

> 작성일: 2026-05-18 · 작성자: 강수 (Kang-Soo.Cho@kr.ey.com)
> 선행 문서: `법인세_세무조정계산서_시스템_설계서.md` · `2차구현계획.md` · `메뉴기능수정.md` · `추가구현_v1.0~v1.3.md`
> 본 문서의 범위: v1.3 leaf 분리 작업 이후 발생한 두 가지 회귀(테넌트 선택 기능 소실, 업무 흐름 시각화 누락)를 복구해 “설계서 IA + 실무 흐름 + 멀티테넌트”가 동시에 만족되도록 한다.

---

## v1.5 동기화 메모

v1.4의 테넌트 전환·Stepper·LawBanner·StatePill·NextStepCard 복구 위에 v1.5 공통 leaf workbench를 적용한다. 따라서 `workspace` leaf는 흐름 4종을 유지하면서 본문에 5블록 CRUD UI를 표시하고, 사이드바 그룹 헤더는 현재 active leaf와 workContext 진행률 점을 함께 표시한다.

## 1. 회귀 진단

### 1.1 회귀 A — 테넌트 선택 기능 소실
- **현 상태**: v1.3 적용 후 좌측 메뉴 `⑤ 관리` 아래에 `5-0 테넌트 관리(ad-tenant)`가 보이지 않는다. 또한 SUPER_ADMIN/TENANT_ADMIN이 다른 테넌트로 전환할 UI도 없다.
- **원인 1 (메뉴 트리)**: v1.1이 “설계서 §5.1 메뉴 트리를 있는 그대로 채택”했는데, 설계서 §5.1의 5-A~5-G에 **`5-0 테넌트 관리`가 없다**. 반면 `prototype/index.html`(line 150~160) 및 `2차구현계획.md` 표의 `ad-tenant` 행에는 있었다. 결과적으로 v1.1 채택 트리에서 `ad-tenant`가 누락되고 그대로 v1.2·v1.3로 전파되었다.
- **원인 2 (상단 컨텍스트)**: 로그인 폼은 여전히 `tenant_code` 입력이 있지만, 로그인 후 **상단 헤더의 테넌트 표시(`signedTenant`)** 는 단순 라벨이고 “다른 테넌트로 전환” 동작이 없다. SUPER_ADMIN은 한 번 로그인하면 그 테넌트에 고정된다.
- **원인 3 (API 경로)**: 백엔드 라우트 `/api/tenants` (CRUD)는 존재하나, 프론트 모듈 `admin/tenant`가 v1.1 트리에서 빠지며 함께 사라졌다.

### 1.2 회귀 B — 전체 업무 흐름이 보이지 않음
- **현 상태**: leaf 99개로 분리한 결과 메뉴는 보이지만, 사용자가 다음을 한눈에 확인할 수 없다.
  1. “지금 신고 작업 어디까지 와 있나(0→1→2…→7)” — 단계 진행률
  2. “이 사업연도에 적용된 법령·서식 버전은 무엇인가” — Law Banner
  3. “지금 사업연도 상태는 DRAFT? IN_REVIEW? APPROVED? FILED?” — 상태 배지
  4. “다음으로 할 일은 무엇인가” — 다음 단계 추천
  5. “현재 고객사·사업연도 컨텍스트” — 사이드바 하단 배지
- **원인**: v1.3가 “leaf 단독 화면”에 초점을 맞추다 보니, leaf로 들어간 뒤 본문 영역만 그리고 끝낸다. `prototype/index.html`의 Stepper(line 365~404)·LawBanner(line 409~424)·Context badge(line 310~320)에 해당하는 셸이 일부 화면에서 비활성/누락 상태.
- **세부 코드 단서**:
  - `frontend/app.js:renderRoute()`는 `meta.layout==="workspace"`에서만 `renderStepper`를 호출한다. 그러나 v1.3로 leaf가 늘면서 일부 leaf의 `layout`이 잘못 매핑되거나 `data-leaf-key` 셔플로 stepper 슬롯이 비어 있는 경우가 있다.
  - `screens.js`의 leaf 폴백이 `renderLawBanner(env.lawBanner, env.context)`만 호출하고, “신고 작업 외 그룹”에서는 강제로 hide. 결과적으로 사용자가 신고 작업 화면에 들어와도 컨텍스트가 비어 있으면 LawBanner가 전혀 표시되지 않는다.
  - 사이드바의 `contextBadge`는 컨텍스트가 비어 있으면 “고객사 미선택”만 표시. 진입할 때마다 “지금 어디 있는지” 안내가 부재.

---

## 2. 복구 목표

1. **테넌트 선택을 3개 경로로 복구**한다.
   - (T1) 로그인 폼의 `tenant_code` 입력에 **자동완성/검색** 추가 + 최근 사용 테넌트 표시.
   - (T2) 상단 헤더(Topbar)에 **테넌트 스위처 드롭다운** 추가(SUPER_ADMIN/TENANT_ADMIN 권한자만).
   - (T3) `⑤ 관리`에 **`admin:tenant` leaf 복구**(5-0 테넌트 관리).
2. **업무 흐름 가시화를 4개 컴포넌트로 복구·강화**한다.
   - (F1) **Stepper** — 신고 작업 1~7단계의 진행률·완료 표시(설계서 §5.3 C 와이어프레임).
   - (F2) **LawBanner** — 적용 법령·세율·서식 버전 배지 + “근거 보기” 액션(§5.3 F-5).
   - (F3) **Status pill + Lock badge** — 사업연도 상태(DRAFT/IN_REVIEW/APPROVED/FILED/AMENDED) + 잠금 표시.
   - (F4) **다음 단계 추천 카드** — 화면 하단에 “다음으로 할 일” 1개 액션.
3. 두 회귀 복구가 v1.3의 leaf 단독 화면 모델을 깨지 않도록 한다(직교).

---

## 3. 복구 설계 — 테넌트 선택 (T1·T2·T3)

### 3.1 T1. 로그인 폼 강화 (`frontend/index.html` + `app.js`)
- `loginTenant` 입력 옆에 **`datalist`** 추가: 최근 사용 테넌트 5개를 `localStorage("cit.auth.recentTenants")`에 저장하고 placeholder/자동완성.
- 사용자가 `tenant_code`를 알지 못해도 “테넌트 검색” 버튼이 모달을 띄워 `GET /api/public/tenant-suggest?q=…`(신설) 결과로 자동완성.
- 로그인 성공 시 응답의 `tenant_code/tenant_name`을 최근 사용 목록 맨 앞으로 이동.

### 3.2 T2. 상단 테넌트 스위처 (`Topbar`)
- 위치: 헤더 우측의 `signedTenant` 라벨을 **드롭다운 버튼**으로 교체.
- 표시 데이터: `GET /api/auth/me`가 반환하는 `accessible_tenants[]`(신설 필드. 사용자가 접근 가능한 모든 테넌트의 `tenant_code/tenant_name/role`).
- 동작: 다른 테넌트 선택 시 `POST /api/auth/switch-tenant {tenant_code}`(신설) 호출 → 새 JWT 발급 → workContext 초기화 → 메뉴 트리 재로드 → `#/dashboard/overview`로 이동.
- 권한 가드: SUPER_ADMIN 또는 사용자에게 `accessible_tenants.length >= 2`일 때만 드롭다운이 보임. 일반 사용자는 종전 라벨 표시.
- UI: 드롭다운 항목 = `{tenant_name} · {tenant_code} · {role}`. 상단에 “이 테넌트로 작업중” 표시.

### 3.3 T3. `admin:tenant` leaf 복구
- **메뉴 트리**:
  - `src/modules.rs::prototype_menu_defs()`에 `admin/tenant` 그룹과 leaf 추가.
  - leaf 키 체계: `admin/tenant:list`, `admin/tenant:plan`, `admin/tenant:billing`(선택), `admin/tenant:status`. 사용자가 요구한 최소 범위는 `admin/tenant:list` 1개로도 충족된다.
  - 구현 기준은 `admin/tenant:list` 1개 leaf 복구이며, 현재 활성 leaf 수는 v1.3의 99개에서 100개로 갱신한다.
- **사이드바**:
  - `admin` 그룹 첫 항목(5-0)으로 노출. `5-A 고객사`보다 위.
  - 좌측 카테고리 라벨 `5-0. 테넌트` 추가.
- **백엔드 라우트**: `GET /api/tenants`(이미 존재) + `PATCH /api/tenants/:tenant_code/status`(신설: ACTIVE/SUSPENDED/CLOSED 토글) + `PATCH /api/tenants/:tenant_code/plan`(신설).
- **화면 구성** (prototype line 1117~1310의 `AdminTenant` 재이식):
  - 좌측: 테넌트 목록(검색·유형·상태 필터)
  - 우측: 선택 테넌트 편집(조직명/유형/사업자번호/대표/요금제/계약기간 + 사용중지·해지 버튼)
  - 상단: 유형별 카운트 카드(회계법인/세무법인/세무사사무소)
- **권한**: SUPER_ADMIN만 보이고 편집 가능. TENANT_ADMIN은 자신의 테넌트 행만 읽기 가능.

### 3.4 백엔드 신설 라우트 요약 (T1~T3)

| 라우트 | 용도 | 권한 |
|---|---|---|
| `GET /api/public/tenant-suggest?q=…` | 로그인 폼 자동완성(공개, rate-limit) | 공개 |
| `GET /api/auth/me` 응답에 `accessible_tenants[]` 추가 | 스위처 표시 | 로그인 사용자 |
| `POST /api/auth/switch-tenant` | 다른 테넌트로 세션 전환(새 JWT 발급) | SUPER_ADMIN 또는 다중 테넌트 접근자 |
| `PATCH /api/tenants/:tenant_code/status` | 활성/사용중지/해지 | SUPER_ADMIN |
| `PATCH /api/tenants/:tenant_code/plan` | 요금제 변경 | SUPER_ADMIN |

---

## 4. 복구 설계 — 업무 흐름 가시화 (F1·F2·F3·F4)

### 4.1 F1. Stepper (신고 7단계 진행)
- 표시 위치: `workspace` 그룹의 모든 leaf 진입 시 헤더 직하단(`#stepper` 슬롯).
- 데이터원:
  - 현재 leaf 키 → `parentKey`(`ws/start`/`ws/info`/…/`ws/file`) → 7단계 인덱스 매핑.
  - 사업연도 상태 + 단계별 완료 플래그(`business_years.progress` + `workflow_events`)로 done/active/pending 결정.
- 시각화:
  - 7칸 가로 스테퍼(번호·라벨·체크). 완료=초록, 활성=인디고, 미달=회색.
  - 클릭 시 그 단계의 첫 leaf로 이동(예: 2단계 클릭 → `ws/adj:B1`).
- 컴포넌트: `frontend/app/components/stepper.js`(이미 부분 존재) — leaf-key→step 매핑 표를 정비.

### 4.2 F2. LawBanner (적용 법령·서식)
- 표시 위치: `workspace` 그룹의 모든 leaf + `post/amend:*`에서도 표시.
- 데이터원: `workContext.snapshot` (`/business-years/:byId/snapshot`).
- 표시 항목: `법인세율 v…`, `접대비한도 v…`, `R&D공제 v…`, `별지제3호 v…` 4종 + “근거 보기” 클릭 시 `admin/law:snapshots`로 이동.
- 라벨 매핑: `snapshot.snapshot_data.{law_version,rate,limit,credits,form}` 키를 사람이 읽는 라벨로 치환하는 `lawLabel()` 헬퍼 추가(`frontend/app/i18n.js`에 매핑 테이블).
- 빈 컨텍스트: 신고작업 진입 시 컨텍스트가 비어 있으면 “고객사·사업연도를 선택하세요” + “작업 시작 ▶” 배너로 대체.

### 4.3 F3. 상태 Pill + 잠금 Badge
- 표시 위치: 사이드바 하단(`#contextBadge`) + Topbar 우측(`#stateBadge`).
- 데이터: `workContext.status` (`DRAFT|IN_REVIEW|APPROVED|FILED|AMENDED`) + `workContext.lockMode` (`OPEN|LOCKED`).
- 색상: DRAFT=회색, IN_REVIEW=인디고, APPROVED=주황, FILED=초록, AMENDED=보라.
- 잠금: 자물쇠 아이콘 + tooltip “신고 완료 — 사후관리에서 잠금 해제 신청 필요”.

### 4.4 F4. 다음 단계 추천 카드
- 표시 위치: 모든 `workspace/*` leaf 본문 하단.
- 로직: 다음 단계의 첫 leaf 키(예: `ws/info:fs` 끝 → `ws/adj:B1`)와 “현재 단계 완료 조건”(예: `validation.balanced === true`)을 비교해 카드 2개:
  - 좌: “지금 단계 완료 체크” — 조건 X면 빨강 / O면 초록.
  - 우: “다음 단계로 이동 ▶” — 활성 시 클릭 → 다음 leaf.
- 컴포넌트: `frontend/app/components/next-step-card.js` 신규.
- 데이터원: `GET /api/tenants/:tc/business-years/:byId/progress`(신설) — 응답 = 각 단계의 done/required/pending 플래그 + 다음 leaf 추천 키.

### 4.5 흐름 가시화의 비-workspace 적용
- `post`/`report`/`admin`에서도 “현재 그룹의 위치”를 보여주기 위해 **Breadcrumb**(헤더 좌측에 이미 있는 `routeGroup > routeTitle`)에 leaf 키 앞 단계의 그룹 라벨까지 표시(예: `⑤ 관리 > 5-D 법령 > 영향 시뮬레이션`).
- `admin` 그룹은 좌측 카테고리 트리 형태(5-0~5-G)로 펼쳐서 현재 위치를 항상 표시(추가구현_v1.1.md §4.2 admin 레이아웃 그대로).

---

## 5. 변경 파일 매트릭스

| 분류 | 파일 | 변경 |
|---|---|---|
| 백엔드 메뉴 | `src/modules.rs` | `admin/tenant` 그룹 + leaf 추가, sort_order 위치 조정(5-0) |
| 백엔드 라우트 | `src/api.rs` | T1·T2·T3 신설 5개 + 흐름용 `progress` 1개 |
| 백엔드 핸들러 | `src/tenant.rs` / `src/auth.rs` / `src/workflow.rs` | `switch_tenant`, `tenant_status/plan PATCH`, `progress` 구현 |
| DB 마이그레이션 | `2026052X000400_tenant_plan_status.sql` (있다면 무시) | `tenants.plan`, `tenants.suspended_at` 보강 |
| 프론트 셸 | `frontend/index.html` | `#stepper`, `#lawBanner`, `#stateBadge`, `#tenantSwitcher` 슬롯 보강 |
| 프론트 라우터 | `frontend/app/app.js` | `renderRoute`가 leaf의 group 기반으로 stepper/lawBanner/stateBadge를 항상 그리도록 정비 |
| 프론트 컴포넌트 | `frontend/app/components/{stepper, law-banner, state-pill, tenant-switcher, next-step-card}.js` | 신규/정비 |
| 프론트 메뉴 | `frontend/app/menu.js` | `admin/tenant` 트리 렌더, 권한 가드(`SUPER_ADMIN`)로 표시 제어 |
| 프론트 leaf 화면 | `frontend/app/screens/admin/tenant/list.js` 외 | prototype의 AdminTenant 컴포넌트 이식 |
| i18n | `frontend/app/i18n.js` | `lawLabel` 매핑 추가, “테넌트 전환” 라벨 |
| 테스트 | `tests/tenant_switch.rs`, `tests/admin_tenant_routing.rs`, `tests/menu_full_tree.rs` 갱신 | leaf 99 → 100 갱신 + 스위치 라우트 검증 |
| 시드 | `메뉴기능수정.md` 시더 | `admin/tenant` leaf용 추가 테넌트 2~3개(회계법인/세무법인/세무사사무소) |

---

## 6. 단계별 작업 (총 1주)

### Day 1 — 메뉴 트리·셸 슬롯 정비
1. `src/modules.rs`에 `admin/tenant` 그룹 + `admin/tenant:list`(우선 1개 leaf) 추가, `cargo build` 통과.
2. `migrations/2026052X000500_admin_tenant_menu.sql`: `menu_nodes`에 `admin/tenant`, `admin/tenant:list` 시드.
3. `frontend/index.html`에 `#tenantSwitcher`, `#stateBadge` 슬롯 추가, CSS 보강.
4. `tests/menu_full_tree.rs` 갱신 — admin/tenant 노드 검증.

### Day 2 — 테넌트 스위처(T2) + 로그인 자동완성(T1)
1. `GET /api/auth/me` 응답에 `accessible_tenants[]` 필드 추가.
2. `POST /api/auth/switch-tenant` 핸들러 작성: 권한 검사 + 새 JWT 발급 + 감사로그 기록.
3. `components/tenant-switcher.js` 신규 — 드롭다운 + 전환 액션.
4. 로그인 폼에 `datalist` + 최근 테넌트.
5. `GET /api/public/tenant-suggest` 핸들러(공개, rate-limit 30req/min/IP).

### Day 3 — admin/tenant 화면(T3)
1. `frontend/app/screens/admin/tenant/list.js` 작성: prototype `AdminTenant` 이식 + 좌목록·우편집·상단 카운트 카드.
2. `PATCH /api/tenants/:tenant_code/status`, `PATCH /api/tenants/:tenant_code/plan` 핸들러.
3. SUPER_ADMIN 권한 가드 + 단위 테스트.

### Day 4 — Stepper(F1) + LawBanner(F2) + 상태 Pill(F3)
1. `components/stepper.js` 정비 — leaf 키→step 매핑.
2. `components/law-banner.js` 신규 — `lawLabel()` 헬퍼로 사람-가독 라벨.
3. `components/state-pill.js` 신규 — 5상태 색상 + 잠금 아이콘.
4. `frontend/app/app.js:renderRoute`가 workspace/post/amend 그룹 진입 시 항상 stepper+banner+pill 갱신.

### Day 5 — 다음 단계 추천 카드(F4) + 회귀 테스트
1. `GET /api/tenants/:tc/business-years/:byId/progress` 핸들러 + DTO.
2. `components/next-step-card.js` 신규.
3. `tests/tenant_switch.rs`, `tests/admin_tenant_routing.rs`, `tests/progress.rs` 추가.
4. `docs/menu_smoke_checklist.md`에 “Stepper/Banner/Pill/추천 카드 모두 표시” 행 추가.

### Day 6 — 수동 시연 + 문서화
1. 사용자(강수)에게 시연: SUPER_ADMIN으로 로그인 → 테넌트 스위처 사용 → 5-0 테넌트 관리 진입 → 다른 테넌트로 전환 → 신고 작업 진입 → 1~7단계 Stepper 표시 확인.
2. `2차구현계획.md` 메뉴 매트릭스에 `admin/tenant` 행 추가.

---

## 7. 권한 모델 보강

| 권한 | T1 | T2 | T3 | F1~F4 |
|---|---|---|---|---|
| 공개(비로그인) | 자동완성 검색 가능(`/api/public/tenant-suggest`) | — | — | — |
| 일반 사용자 | 로그인 시 자동 표시 | 단일 테넌트면 라벨로만 | 차단 | 본인 사업연도에 대해 표시 |
| TENANT_ADMIN | — | 자신 + 위임된 테넌트 | 자기 테넌트 읽기 | 자기 테넌트 사업연도 |
| SUPER_ADMIN | — | 전체 테넌트 | CRUD + 상태/요금제 변경 | 모든 사업연도 |

`POST /api/auth/switch-tenant` 핸들러는 다음을 검사:
1. 요청 사용자가 대상 `tenant_code`에 등록되어 있거나, SUPER_ADMIN인지.
2. 대상 테넌트 `status='ACTIVE'`.
3. 통과 시 새 JWT(만료 1시간) 발급 + `auth_sessions` upsert + `audit_logs` 기록.

---

## 8. 회귀·테스트 계획

### 8.1 기능 회귀
- 로그인 → 테넌트 자동완성 → 정상 로그인.
- 멀티 테넌트 접근자 로그인 → 상단 스위처에 ≥2개 옵션 표시 → 다른 테넌트 전환 → 메뉴 트리·workContext 모두 갱신.
- SUPER_ADMIN → `⑤ 관리 > 5-0 테넌트` 진입 → 리스트·편집 동작.
- 일반 사용자 → 스위처 미표시, `admin/tenant:list` 진입 시 권한 안내(empty-state perm).

### 8.2 흐름 가시화 회귀
- `ws/start:by-pick` 진입 → 컨텍스트 미설정 → Stepper는 회색만 표시되고 “고객사·사업연도 선택” 안내 우선.
- 컨텍스트 설정 후 `ws/info:fs` 진입 → Stepper 1단계 active, LawBanner 4종 라벨 표시, 상태 pill = DRAFT.
- `ws/val:run` 실행 → 오류 0 → 다음 단계 카드 활성 → 클릭 → `ws/appr:request`.
- `ws/file:done` → 상태 pill = FILED, 잠금 아이콘 표시, Stepper 모두 done.

### 8.3 자동 테스트
- `tests/tenant_switch.rs`: 권한별 분기 4케이스.
- `tests/admin_tenant_routing.rs`: leaf 등록 + 권한 가드 + CRUD 200/403.
- `tests/progress.rs`: 단계별 done/required 판정 + 추천 leaf 키.
- `tests/menu_full_tree.rs`: leaf 키 100(99 + `admin/tenant:list` 1) 검증.

---

## 9. 위험 및 대응

| 위험 | 대응 |
|---|---|
| 설계서 §5.1에 `5-0 테넌트`가 없어 “설계서와 불일치” 의문 | 설계서 부록(2026-05-15 Phase 18·20 보완 노트)에 “회계법인/세무법인/세무사사무소 테넌트 관리”가 명시되어 있으므로 5-0으로 신설하고 본 문서 §3.3에 근거 명시 |
| 테넌트 스위처가 운영 사고로 이어질 위험 | 새 JWT 발급 시 만료 1시간 + `auth_sessions`에 `switched_from_tenant` 기록 + 감사로그 의무 |
| Stepper의 단계별 완료 판정이 사업연도마다 다름 | `progress` 응답을 진리원으로 두고 프론트는 표시만. 백엔드 단위 테스트로 단계별 판정 식 고정 |
| LawBanner 라벨 매핑이 누락된 키 발생 | `lawLabel()`이 누락 키 발견 시 원본 키를 그대로 표시하고 dev 로그에 경고 |
| leaf 100개 회귀로 PR 충돌 | T3 admin/tenant 도입 PR을 가장 먼저 머지, 나머지 흐름 가시화 PR은 컴포넌트 단위로 분리 |

---

## 10. 완료 정의 (DoD)

1. 로그인 폼에서 테넌트 자동완성이 동작하고, 최근 사용 테넌트 5개가 `localStorage`에 보존된다.
2. 상단 헤더의 테넌트 스위처를 통해 SUPER_ADMIN/다중 접근자가 다른 테넌트로 1클릭 전환된다(새 JWT + 메뉴 트리 재로드).
3. `⑤ 관리 > 5-0 테넌트` leaf가 존재하고 SUPER_ADMIN은 CRUD·상태·요금제를 변경할 수 있다.
4. `workspace`/`post/amend`/일부 `admin/law`·`admin/form` leaf 진입 시 Stepper / LawBanner / 상태 Pill / 다음 단계 추천 카드가 항상 표시된다.
5. 컨텍스트 미설정 또는 권한 부족 케이스는 `empty-state`로 일관 안내된다.
6. `tests/tenant_switch.rs` · `tests/admin_tenant_routing.rs` · `tests/progress.rs` · 갱신된 `tests/menu_full_tree.rs`가 전부 green.
7. 본 문서와 `추가구현_v1.3.md` 매트릭스가 `admin/tenant:list` 행을 포함하도록 동기화된다.
