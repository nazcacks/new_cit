# 법인세 세무조정계산서 시스템 — 단계별 구현 계획

> **문서 버전**: v1.0
> **작성일**: 2026-05-14
> **작성자**: 강수 (Kang-Soo.Cho@kr.ey.com)
> **연관 문서**: `법인세_세무조정계산서_시스템_설계서.md`, `DB설계.md`, `업무흐름.md`
> **목적**: 업무 흐름을 시스템으로 구현하기 위한 단계별 작업 분해 (WBS) — 각 단계의 목표, 범위, 작업 항목(BE/FE/DB), 산출물, 완료 기준, 의존성, 리스크를 정리하여 스프린트 계획·일정 관리·인력 배치의 기준으로 사용한다.

---

## 목차

1. [구현 전략 및 원칙](#1-구현-전략-및-원칙)
2. [전체 단계 요약](#2-전체-단계-요약)
3. [페이즈별 상세 구현 계획](#3-페이즈별-상세-구현-계획)
   - Phase 0. 프로젝트 셋업
   - Phase 1. 공통 기반 (인프라·인증·멀티테넌트)
   - Phase 2. 시스템 관리 (사용자·역할·메뉴·권한)
   - Phase 3. 사용자별 담당 법인 권한
   - Phase 4. 법령·세율 버전 관리
   - Phase 5. 서식 버전 관리
   - Phase 6. 고객사 / 사업연도
   - Phase 7. 세무정보 입력 (재무제표·자산·거래)
   - Phase 8. 세무조정 엔진 — 공통 / 소득금액조정 (B-1)
   - Phase 9. 세무조정 — 자산 기반 (B-4·5·6·10)
   - Phase 10. 세무조정 — 거래 기반 (B-2·3·9)
   - Phase 11. 세무조정 — 평가·이월·유보 (B-7·8·11·15)
   - Phase 12. 세무조정 — 세액 (B-12·13·14)
   - Phase 13. 세무조정 — 외국법인/연결 (B-16·17)
   - Phase 14. 서식 생성 엔진 / 별지 제3호
   - Phase 15. 100여 종 부속서식 / PDF 출력
   - Phase 16. 전자신고 (텍스트 포맷)
   - Phase 17. 워크플로 (검증·결재·잠금·수정신고)
   - Phase 18. 횡단 기능 (감사·알림·대시보드·보고서)
   - Phase 19. 통합 테스트 / 보안 진단 / UAT
   - Phase 20. 파일럿 → 정식 오픈
4. [횡단 작업 (Cross-cutting)](#4-횡단-작업-cross-cutting)
5. [테스트 전략](#5-테스트-전략)
6. [릴리스 / 배포 전략](#6-릴리스--배포-전략)
7. [팀 구성 / 인력 배치](#7-팀-구성--인력-배치)
8. [리스크 관리](#8-리스크-관리)

---

## 1. 구현 전략 및 원칙

| 원칙 | 적용 |
|------|------|
| 점진적 가치 전달 | 매 페이즈 종료 시 동작 가능한 시연(데모) 산출물 보유 |
| 의존성 우선 구현 | 인증·권한·법령버전 등 횡단 기반을 먼저 안정화 후 도메인 모듈 진입 |
| 데이터 외부화 | 세율/한도/공제율/서식구조는 코드가 아닌 DB 데이터 → 매년 개정 시 코드 배포 불필요 |
| 테스트 우선 | 도메인 로직(세무조정 계산)은 단위 테스트 커버리지 80% 이상 |
| Vertical Slice | 모듈별 BE/FE/DB/테스트를 한 스프린트에 묶어서 완성 |
| 페이즈 게이트 | 다음 페이즈 진입 전 코드 리뷰 + 보안 점검 + 회귀 테스트 통과 |
| 문서 동기화 | 각 페이즈 종료 시 설계서·API 문서·ERD 갱신 |

**스프린트 단위**: 2주 1 스프린트, 페이즈는 1~3 스프린트 분량

---

## 2. 전체 단계 요약

| Phase | 명칭 | 기간 | 의존성 | 주 산출물 |
|-------|------|------|--------|----------|
| 0 | 프로젝트 셋업 | 2주 | – | Repo, CI/CD, 인프라 |
| 1 | 공통 기반 | 4주 | 0 | 인증·JWT·멀티테넌트·공통 프레임 |
| 2 | 시스템 관리 | 4주 | 1 | 사용자/역할/메뉴/권한 매트릭스 |
| 3 | 담당 법인 권한 | 2주 | 2 | 사용자×법인 등급, 그룹, 위임 |
| 4 | 법령·세율 버전 | 3주 | 1 | 법령 버전, 세율표, 한도표, 스냅샷 |
| 5 | 서식 버전 관리 | 3주 | 4 | 서식 마스터, 메타, 매핑·마이그레이션 |
| 6 | 고객사 / 사업연도 | 2주 | 2,4,5 | 고객사 등록, 사업연도 + 스냅샷 자동생성 |
| 7 | 세무정보 입력 | 4주 | 6 | 재무제표 임포트, 매핑, 자산대장, 거래 |
| 8 | 세무조정 공통 + B-1 | 3주 | 4,5,7 | 조정 엔진 기반 + 소득금액조정 |
| 9 | 세무조정 (자산기반) | 4주 | 8 | B-4 감가상각, B-5 퇴직, B-6 대손, B-10 업무용차 |
| 10 | 세무조정 (거래기반) | 3주 | 8 | B-2 기부금, B-3 접대비, B-9 지급이자 |
| 11 | 세무조정 (평가·이월) | 3주 | 8 | B-7 외화, B-8 재고/유가증권, B-11 이월결손, B-15 유보 |
| 12 | 세무조정 (세액) | 3주 | 9,10,11 | B-12 세액공제, B-13 최저한세, B-14 가산세 |
| 13 | 세무조정 (외국/연결) | 3주 | 12 | B-16 외국법인, B-17 연결납세 |
| 14 | 서식 엔진 + 별지 제3호 | 3주 | 5,12 | 서식 자동생성, 별지 제3호 |
| 15 | 부속서식 + PDF | 4주 | 14 | 100여 종 서식, JasperReports |
| 16 | 전자신고 | 4주 | 15 | 텍스트 포맷 생성, 검증, 제출 |
| 17 | 워크플로 | 3주 | 14,16 | 검증·결재·잠금·수정신고 |
| 18 | 횡단 기능 | 3주 | 1~17 병행 | 감사로그, 알림, 대시보드, 보고서 |
| 19 | 통합/보안/UAT | 4주 | 18 | 부하·보안·UAT 통과 |
| 20 | 파일럿 → GA | 4주 | 19 | 정식 오픈 |

**총 기간**: 약 12개월 (병행 스프린트 일부 가정)

---

## 3. 페이즈별 상세 구현 계획

각 페이즈는 다음 항목으로 정리한다:
- **목표 / 범위 / 의존성**
- **작업 (BE / FE / DB / 기타)**
- **산출물**
- **완료 기준 (Definition of Done)**
- **리스크**

---

### Phase 0. 프로젝트 셋업 (2주)

**목표**: 개발 가능 환경 + 표준 프로세스 확립

**작업**:
- 인프라
  - AWS(서울 리전) 계정 분리: Dev / Staging / Prod
  - Kubernetes (EKS) 클러스터 셋업 (Dev → 점진적 Staging/Prod)
  - PostgreSQL 16 RDS, Redis ElastiCache 프로비저닝
  - S3 버킷(파일 저장), Secrets Manager, KMS
  - WAF + ALB + Route 53
- 리포지토리 및 빌드
  - Mono-repo 또는 BE/FE 분리 결정
  - Gradle 멀티모듈 구성 (BE: api / domain / common / efiling / forms)
  - Vite + React + TS 보일러플레이트
  - Code 컨벤션 (Spotless, ESLint, Prettier)
- CI/CD
  - GitLab CI / GitHub Actions
  - 단위 테스트 / 통합 테스트 / 정적분석(SonarQube, Snyk)
  - Docker 이미지 빌드 + ECR 푸시
  - Helm chart + ArgoCD 또는 동등 GitOps
- 관찰성
  - 로깅: Loki / ELK
  - 메트릭: Prometheus + Grafana
  - APM: OpenTelemetry / Datadog (선택)
- 문서/협업
  - Confluence/Notion, Jira, Slack 통합

**산출물**: Dev 환경 동작, "Hello World" 배포 자동화 완료

**완료 기준**: 코드 푸시 → 자동 빌드/테스트/배포가 동작하고 Grafana 대시보드 확인 가능

**리스크**: 인프라 설정 지연 → 대응: 가용한 임시 환경(VM) 병행

---

### Phase 1. 공통 기반 (4주)

**목표**: 인증·인가·멀티테넌트의 기반 위에 모든 후속 모듈을 올릴 수 있도록 함

**범위**:
- 멀티 테넌트 (Schema-per-Tenant)
- 인증 (ID/PW + 2FA TOTP)
- JWT + Refresh Token Rotation
- API Gateway / 공통 응답 / 예외 처리
- 로깅, 감사로그 인프라

**작업 (BE)**:
- Spring Boot 3 멀티모듈
- Hibernate `MultiTenantConnectionProvider` + `CurrentTenantIdentifierResolver`
- Spring Security + JWT Filter
- TOTP (Google Authenticator 호환, `dev.samstevens.totp`)
- 비밀번호 정책 (bcrypt cost=12), 잠금, 변경 주기
- Refresh Token Blacklist (Redis)
- 공통 응답 포맷 `{code, message, data, timestamp, traceId}`
- 글로벌 예외 핸들러
- 감사로그 AOP 인터셉터

**작업 (FE)**:
- 라우터 가드, AuthContext
- 로그인/2FA 화면 (A, A-2)
- 인증 실패 처리(잠금, 재시도)
- API 클라이언트 (Axios 인터셉터, 401 자동 갱신)

**작업 (DB)**:
- `public.tenants`, `public.users`, `public.login_history`
- `tenant_NNN` 스키마 자동 프로비저닝 스크립트
- Flyway/Liquibase 마이그레이션 시작

**산출물**: 로그인 가능한 빈 시스템 (대시보드 빈 페이지까지)

**완료 기준**:
- 2명의 테넌트가 같은 ID로 별개 로그인 가능 (격리 확인)
- 2FA 강제 적용
- 5회 실패 잠금, 30분 자동 해제
- E2E: 로그인 → 토큰 갱신 → 로그아웃

**리스크**: 멀티테넌트 라우팅 누락 시 데이터 누수 → 단위 테스트 강제

---

### Phase 2. 시스템 관리 (4주)

**목표**: 사용자/역할/메뉴/공통 기능 권한을 시스템 관리자가 운영할 수 있게 함

**작업 (BE)**:
- DTO/Service/Controller: User, Role, Menu, Function, MenuFunction, RoleMenuFunction, UserMenuFunctionOverride
- 권한 평가 서비스 `PermissionService.has(userId, menuCode, functionCode)`
- 권한 캐시(Redis pub/sub로 무효화)
- Spring Security `@PreAuthorize("@perm.has(...)")` SPEL 빈
- 필드 마스킹 정책 + 응답 직렬화 시 마스킹 (Jackson Serializer)

**작업 (FE)**:
- E-1 사용자 관리
- E-2 권한 매트릭스 (메뉴×기능 격자)
- E-3 메뉴 관리 (트리 + 활성 기능 체크박스)
- E-7 개별 권한 예외 설정
- `usePermission(menuCode, functionCode)` 훅 + `<Permitted>` 컴포넌트

**작업 (DB)**:
- `menus`, `functions`, `menu_functions`, `roles`, `user_role`
- `role_menu_function`, `user_menu_function_override`
- `user_data_scope`, `field_permissions`
- `permission_change_history`
- 시드 데이터: 12개 표준 Function, 7개 표준 Role, 기본 메뉴 트리

**산출물**: 관리자 화면에서 권한 매트릭스 편집 → 일반 사용자 화면의 버튼 활성화가 즉시 반영

**완료 기준**:
- DENY > ALLOW, 사용자 예외 > 역할 권한 동작
- 권한 변경 시 1초 이내 사용자 캐시 무효화
- 모든 변경이 감사로그에 기록

**리스크**: 권한 평가 N+1 쿼리 → 캐시·뷰로 해결

---

### Phase 3. 사용자별 담당 법인 권한 (2주)

**목표**: 사용자가 어떤 법인의 세무조정을 어떤 등급(OWNER/CO_WORKER/REVIEWER/ASSISTANT/VIEWER/BLOCKED)으로 다룰 수 있는지 관리

**작업 (BE)**:
- Service: `CustomerAccessService` (개별/그룹/룰/위임 통합 계산)
- View: `v_user_effective_customer_access` 생성/조회
- Hibernate Filter (도메인 엔티티에 `@Filter(name="customerAccess")`)
- 위임 만료 스케줄러 (매 5분 access_delegations 정리)
- 위임 시 양측 알림

**작업 (FE)**:
- E-4 담당 법인 권한 관리 (4가지 경로 통합 + 미리보기)
- E-5 권한 위임 화면
- E-6 고객사 그룹 관리
- 룰 미리보기 (룰 적용 시 영향 법인 N건)

**작업 (DB)**:
- `user_customer_access`, `customer_groups`, `customer_group_members`
- `user_customer_group_access`, `customer_access_rules`
- `access_delegations`, `customer_access_history`
- `v_user_effective_customer_access` 뷰

**산출물**: TENANT_ADMIN이 사용자에게 법인을 4가지 방식으로 배정 가능

**완료 기준**:
- BLOCKED는 어디서든 차단
- 위임 기간 만료 시 자동 해제
- 사용자 로그인 시 effective-permissions에 담당 법인 N건 반영

---

### Phase 4. 법령·세율 버전 관리 (3주)

**목표**: 매년 개정되는 세율·한도·공제율을 시점별로 관리하고, 사업연도에 자동 적용

**작업 (BE)**:
- 도메인: `TaxLawVersion`, `TaxRate`, `TaxLimit`, `TaxCreditVersion`, `DepreciationLife`, `SmeCriteria`, `LossRule`
- Service: `LawResolverService.resolveFor(byId, lawCode)`
- 라이프사이클 상태머신 (DRAFT → REVIEWED → ACTIVE → RETIRED)
- 영향 시뮬레이션 엔진 (Before/After 계산)
- 사용자 공지 발송 모듈

**작업 (FE)**:
- F-1 법령 버전 마스터
- F-2 법인세율표 (구간 입력)
- F-3 한도·율표
- F-4 영향 시뮬레이션
- (서식 버전 매핑 / XML 스키마 / 적용 스냅샷은 Phase 5 화면과 연계)

**작업 (DB)**:
- public 스키마에 11개 테이블 (`tax_law_versions` 외)
- 초기 데이터 적재 (2021~2026 5개년분)
- 함수: `get_applicable_rates(by_id)`

**산출물**: 2021~2026년 세율·한도·공제율이 시점별 적용 가능

**완료 기준**:
- 사업연도 종료일로 정확한 버전 결정
- 영향 시뮬레이션 결과 정확성 회귀 테스트 통과
- DRAFT → ACTIVE 라이프사이클 + 감사로그

**리스크**: 초기 데이터 적재 누락 → 5개년분 표준 케이스 테스트로 검증

---

### Phase 5. 서식 버전 관리 (3주)

**목표**: 매년 일부 개정되는 100여 종 법인세 서식을 시점별로 관리하고 데이터 마이그레이션

**작업 (BE)**:
- 도메인: `TaxForm`, `FormVersion`, `FormTemplate`, `FormValidation`, `FormFieldMigration`, `FormRelationship`
- 메타 JSONB 스키마 검증기
- 마이그레이션 엔진 (Dry-run / Execute / Rollback)
- 서식 버전 결정 함수 호출

**작업 (FE)**:
- G-1 서식 버전 목록
- G-2 서식 항목 편집기 (계산식 / 검증룰)
- G-3 항목 매핑 + 마이그레이션 화면
- G-4 사업연도 적용 서식 세트

**작업 (DB)**:
- `tax_forms`, `form_versions`, `form_templates`, `form_validations`
- `form_field_migration`, `form_data_migration_history`, `form_relationships`
- 함수: `get_form_version_for(by_id, form_code)`
- 초기 데이터: 5개년분 주요 서식 메타 + PDF 템플릿 자리

**산출물**: 관리자가 서식 메타·매핑을 등록·활성화하면 사업연도별로 자동 적용

**완료 기준**:
- 동일 사업연도 동일 서식 코드 → 정확히 1개 버전 선택
- 마이그레이션 Dry-run/Execute/Rollback 동작
- 분할(SPLIT) 매핑은 수동 검토 플래그 강제

---

### Phase 6. 고객사 / 사업연도 (2주)

**목표**: 고객 법인 등록과 사업연도 생성 + 적용 법령·서식 스냅샷 자동 생성

**작업 (BE)**:
- CustomerService, BusinessYearService
- 사업연도 생성 시 `by_law_snapshot` 자동 생성 (LawResolver + FormResolver 호출)
- 사업연도 상태머신 (DRAFT → IN_REVIEW → APPROVED → FILED → AMENDED)

**작업 (FE)**:
- 고객사 등록/조회 화면
- 사업연도 관리 화면
- F-5 / G-4 사업연도 스냅샷 조회 (Phase 4·5와 연계)

**작업 (DB)**:
- `customers`, `business_years`, `tax_agents`, `customer_users`
- `by_law_snapshot`

**산출물**: 고객사 + 사업연도 생성 후 어느 법령/서식이 적용되는지 명확히 표시

**완료 기준**:
- 사업연도 생성 시 자동 스냅샷
- FILED 상태에서 스냅샷 잠금
- 결산월 12월 외 케이스(3·6·9월 결산) 정상 처리

---

### Phase 7. 세무정보 입력 (4주)

**목표**: 재무제표·자산대장·거래 명세를 효율적으로 입력

**작업 (BE)**:
- Excel 임포트 (Apache POI), CSV 임포트
- 표준 템플릿 다운로드
- 계정과목 매핑 학습 (1회 매핑 후 재사용)
- ERP 연동 어댑터 (선택): 더존 i-Cube/SmartA, SAP
- 자산 자동분류(업무용차 판정)

**작업 (FE)**:
- 재무제표 입력 그리드 + Excel 업로드
- 계정과목 매핑 화면 (Drag&Drop)
- 자산대장 그리드 (대량 행, 정렬/필터)
- 거래 명세 (기부금/접대비 카테고리)
- 차변/대변 잔액 검증 패널

**작업 (DB)**:
- `financial_statements`, `fs_lines`
- `account_mappings`
- `assets`, `depreciation`
- `transactions`

**산출물**: Excel 업로드 → 검증 통과 → DB 적재 완료

**완료 기준**:
- 1만행 자산대장 < 5초 임포트
- 차변/대변 불일치 시 행 단위 오류 표시
- 매핑 학습 후 동일 고객사 재임포트 시 자동 매핑 ≥ 95%

**리스크**: Excel 포맷 다양성 → 표준 템플릿 강제 + 가이드 문서

---

### Phase 8. 세무조정 엔진 — 공통 + B-1 (3주)

**목표**: 17개 세무조정 모듈의 공통 기반 + B-1 소득금액조정 명세서

**작업 (BE)**:
- 공통 기반:
  - `TaxAdjustmentService` + 모듈별 SubService
  - 유보 자동 관리 엔진 (전기 → 당기 추인)
  - 법령 버전 파라미터 조회 (Phase 4 연동)
  - 계산 결과에 `snapshot_id` 항상 부착
  - 공통 검증/저장/잠금 인터페이스
- B-1 소득금액조정:
  - 익금산입/익금불산입/손금산입/손금불산입
  - 처분 자동 분류(유보/사외유출/기타)
  - 자동분개 (다른 모듈에서 흘러오는 결과 통합)

**작업 (FE)**:
- C. 세무조정 입력 (좌측 트리 + 상단 적용 법령 배너 + 중앙 그리드)
- B-1 4개 섹션 그리드 (Excel-like, 단축키)
- 법조항 툴팁

**작업 (DB)**:
- `tax_adjustments`, `adjustment_items`, `reserves`
- 공통 인덱스

**산출물**: 가장 단순한 세무조정(B-1)이 처음부터 끝까지 동작

**완료 기준**:
- 결산서당기순이익 → 차가감 소득금액 자동 계산
- 처분이 유보면 reserves 자동 생성
- 적용 법령 배너 표시

---

### Phase 9. 세무조정 — 자산 기반 (4주)

**범위**: B-4 감가상각 / B-5 퇴직급여 / B-6 대손충당금 / B-10 업무용승용차

**작업 (BE)**:
- B-4: 자산별 신고내용연수 vs 회계, 즉시상각의제, 자본적/수익적 지출 판정
- B-5: 추계액 한도, 사외적립금 확인, 확정기여형/급여형 구분
- B-6: 1%/실적률 한도, 대손금 손금산입 사유
- B-10: 운행기록 + 업무사용비율, 1,500만/8,000만 한도, 5년 정액법 강제

**작업 (FE)**:
- 4개 하위 모듈 입력 화면
- 자산대장 → 감가상각 결과 연동
- 차량별 운행기록 입력 (월별)

**작업 (DB)**:
- 자산 분류 / 차량별 운행기록 / 충당금 한도 산식 메타

**완료 기준**:
- Phase 4 한도 데이터로 자동 계산 일치
- 표준 테스트 케이스 (5개년 × 3개 법인 유형) 100% 통과

---

### Phase 10. 세무조정 — 거래 기반 (3주)

**범위**: B-2 기부금 / B-3 접대비 / B-9 지급이자

**작업 (BE)**:
- B-2: 특례/일반 한도, 10년 이월 관리
- B-3: 기본한도 + 수입금액 차등 한도율, 신용카드 미사용분
- B-9: 채권자불분명/수령자불분명/건설자금/업무무관 4유형, 가지급금 적수 × 가중평균 이자율

**작업 (FE)**:
- 한도 계산 결과 + 한도초과액 자동 표시
- 수입금액 명세 입력 (B-3)
- 가지급금 적수 시각화 (B-9)

**완료 기준**:
- 기부금 10년 이월 자동 관리
- 접대비 한도 계산이 법령 버전과 매칭

---

### Phase 11. 세무조정 — 평가·이월·유보 (3주)

**범위**: B-7 외화평가 / B-8 재고·유가증권 평가 / B-11 이월결손금 / B-15 자본금과 적립금

**작업 (BE)**:
- B-7: 화폐성/비화폐성 분류, 환산방법(종료일/거래일)
- B-8: 평가방법(선입선출/총평균/이동평균/개별/저가법)
- B-11: 결손금 연도별 잔액 + 공제한도(중소100%/일반80%), 만료 처리
- B-15: 자본금 변동 + 유보 자동 추인 (모든 B-1~B-14의 유보 집계)

**작업 (FE)**:
- 평가방법 신고 화면
- 이월결손금 연도별 잔액 그리드
- 자본금과 적립금 (을) 표 (유보 자동 집계)

**완료 기준**:
- B-15 (을)표가 다른 모든 모듈 유보를 정확히 집계
- 결손금 만료 알림

---

### Phase 12. 세무조정 — 세액 (3주)

**범위**: B-12 세액공제·감면 / B-13 최저한세 / B-14 가산세

**작업 (BE)**:
- B-12: R&D(25/8~15/0~2%), 통합투자, 외국납부, 재해, 중소특별, 창업감면, 농특세
- B-13: 최저한세 대상 공제·감면 한도 / 적용 계산
- B-14: 무신고/과소/납부지연/원천 가산세, 수정신고 시 감면(90/75/50)

**작업 (FE)**:
- 세액공제 선택 + 한도 자동 계산
- 최저한세 비교 화면
- 가산세 자동 산출 + 감면 신청

**완료 기준**:
- 산출세액 → 결정세액 흐름 정확성
- 최저한세 적용 케이스 5종 회귀 테스트

---

### Phase 13. 세무조정 — 외국법인/연결 (3주)

**범위**: B-16 외국법인 / B-17 연결납세

**작업 (BE)**:
- 외국법인: 국내원천소득 분류(이자/배당/사용료/인적용역/부동산/양도), 고정사업장 손익 배분
- 연결납세: 모-자법인 100% 지분, 연결법인 간 거래 제거, 산출세액 배분

**작업 (FE)**:
- 외국법인 모드 토글
- 연결법인 등록 + 연결조정 워크시트

**완료 기준**:
- 외국법인 테스트 케이스 통과
- 연결그룹 3개사 합산 테스트

---

### Phase 14. 서식 엔진 + 별지 제3호 (3주)

**목표**: 서식 메타 기반으로 입력 데이터 → 서식 자동 생성

**작업 (BE)**:
- FormDataService (사업연도별 form_data CRUD)
- form_relationships 기반 항목 자동 연동 (예: 별지 제3호 ② = SUM(별지 제15호 A))
- form_validations 평가 엔진 (수식 파서, ANTLR 또는 SpEL)
- 서식 변경 이력

**작업 (FE)**:
- D. 서식 미리보기 (화면상 PDF 동등 레이아웃)
- 자동연동(회색) / 직접입력(흰색) 시각 구분
- 항목 클릭 시 원천 입력 위치로 점프

**완료 기준**:
- 별지 제3호가 입력 데이터로부터 자동 완성
- 서식 간 합계 자동 일치
- 수동 수정 시 변경 이력 추적

---

### Phase 15. 부속서식 + PDF (4주)

**목표**: 100여 종 부속서식 자동 생성 + PDF 출력

**작업 (BE)**:
- JasperReports 통합 (PDF 렌더 서비스)
- 서식 버전별 .jrxml 템플릿 관리
- PDF 워터마크(작성중/승인완료)
- 일괄 PDF 생성 (ZIP)

**작업 (FE)**:
- 부속서식 일람 (진행상태 / 합계 일치 표시)
- 미리보기 + 다운로드 + 인쇄
- 일괄 다운로드 (선택 서식 묶음)

**작업 (DB)**:
- 5개년분 100여 종 .jrxml 적재 (Phase 5와 동시 진행)

**완료 기준**:
- 100여 종 서식 PDF 정상 렌더
- 사업연도 스냅샷의 form_version_set로 정확한 템플릿 자동 선택
- 일괄 PDF < 30초 (100개)

---

### Phase 16. 전자신고 (텍스트 포맷) (4주)

> **중요**: 홈택스 법인세 전자신고 파일은 **XML이 아닌 고정폭/위치기반 텍스트 포맷**이다. 각 서식의 각 항목 값을 정해진 자릿수·순서로 연결(concatenate)하여 단일 파일을 만든다. 참조 자료(`전자신고_2026년 1차 파일설명서` 폴더)의 파일 설명서/오류점검 문서를 기준으로 구현한다.

**목표**: 홈택스 전자신고 텍스트 파일 생성 + 사전 검증 + 제출

**작업 (BE)**:
- **파일 포맷 어댑터 모듈** (efiling 모듈)
  - 파일설명서 기반의 필드 메타 (서식코드, 항목번호, 시작위치, 길이, 데이터타입(N/AN/D), 패딩룰, 정렬)
  - 메타 자체를 DB 또는 JSON으로 외부화하여 매년 개정에 대응
  - Builder: 각 항목 값 → 자릿수 맞춰 연결 (Numeric은 zero-pad / right-align, String은 space-pad / left-align)
  - 레코드 종류 식별자 + 체크섬/합계 레코드
  - 인코딩: EUC-KR 또는 UTF-8 (홈택스 스펙 준수)
- **사전 검증 엔진**
  - 오류점검 문서의 규칙을 정형화 (필수/포맷/범위/교차검증/합계일치)
  - 사업자번호 체크섬, 일자 일관성, 합계 일치 등
- **파일 생성/저장**
  - SHA-256 해시, 사이즈, S3 암호화 저장
  - 신고 제출 시 사업연도 스냅샷 잠금
- **(선택) e-Tax API 직접 제출**
  - 가능한 경우 API 연동, 불가 시 수동 업로드 안내
- **수정신고/경정청구 파일 모드**

**작업 (FE)**:
- 전자신고 파일 생성 화면 (사전 검증 → 생성 → 제출)
- 검증 오류 목록 (위치로 점프)
- 신고 이력 / 접수번호 / 양시
- 제출 시 2FA Step-up

**작업 (DB)**:
- `efiling_history`, `efiling_files`, `efiling_validation`
- `efiling_format_spec` (서식별 항목 메타: 위치/길이/타입/패딩) — public 스키마
- `efiling_validation_rules` (오류점검 규칙)

**산출물**: 사업연도 1건 → 텍스트 파일 생성 → 홈택스 업로드 → 접수 확인

**완료 기준**:
- 표준 케이스 5개년 × 3개 법인 유형 텍스트 파일이 홈택스 모의 검증 통과
- 사전 검증으로 오류점검 문서의 100% 룰 적용
- 파일 무결성(해시) 보장

**리스크**:
- 파일 포맷이 매년 일부 변경 → 메타 외부화로 대응
- 인코딩/줄바꿈/패딩 차이로 인한 거부 → 골든 샘플과 바이트 단위 비교 회귀 테스트
- 오류점검 룰 누락 → 국세청 공식 오류점검 문서를 룰로 100% 변환

---

### Phase 17. 워크플로 (3주)

**목표**: 검증 → 결재(승인/반려) → 출력 → 신고 → 잠금 → 수정신고 워크플로

**작업 (BE)**:
- 사업연도 상태머신 (Spring Statemachine 또는 직접 구현)
- 결재 라인 설정 (작성자 → 검토자, 옵션)
- 결재 알림 (이메일/인앱)
- 신고 후 스냅샷 잠금
- 수정신고 모드 (잠금 해제 → 차이 비교)

**작업 (FE)**:
- 검증 결과 화면
- 결재함 (검토자 작업)
- 신고 이력
- 수정신고 진입 (사용 버전 선택)

**완료 기준**:
- 상태 전이 위반 시도 거부
- 결재 이력 추적
- 수정신고 시 원본과 차이 보고서

---

### Phase 18. 횡단 기능 (3주, Phase 2 이후 병행)

**목표**: 감사로그/알림/대시보드/보고서

**작업 (BE)**:
- 감사로그 AOP (모든 C/U/D + 민감 조회)
- 일별 해시 체인
- 알림 시스템 (인앱 + 이메일)
- 보고서 (사업연도 비교, 세부담 분석)
- 대시보드 데이터 API

**작업 (FE)**:
- B. 대시보드 (Phase 1에서 빈 구조 → 여기서 위젯 충실화)
- 알림 센터
- 보고서 화면

**완료 기준**:
- 모든 변경이 감사로그에 적재
- 사업연도 마감 D-30 알림
- 대시보드 < 2초 응답

### Phase 18 산출물 (2026-05-15 구현)

- DB: tenant schema에 `audit_logs.event_date`, `prev_hash`, `hash_current`를 추가하고 `notifications`를 신설했다.
- API: `/dashboard`, `/notifications`, `/audit-logs`, `/reports/tax-burden`, `/reports/year-comparison`를 추가했다.
- 로직: 고객사/사업연도 생성과 사업연도 상태 변경은 감사 로그에 남기고, 당일 감사 로그는 이전 해시를 포함한 일별 해시 체인으로 연결한다.
- 로직: 미신고 사업연도 중 마감일이 30일 이내인 건은 `사업연도 마감 D-30` 인앱 알림을 자동 생성한다.
- 화면: `9. 분석/운영` 모듈을 추가하고 운영 대시보드, 알림 센터, 세부담 분석, 연도 비교, 감사 로그 화면을 분리했다.
- 테스트: 통합 테스트에서 대시보드 집계, D-30 알림, 감사 로그 해시, 세부담 리포트, 연도 비교 리포트를 검증한다.

### Phase 18 완료 기준 확인

- 변경 이력은 `audit_logs`에 저장되고 `hash_current`가 32자 해시로 생성된다.
- D-30 대상 사업연도는 알림 센터와 대시보드 미확인 알림 수에 반영된다.
- 보고서 화면은 숨김 처리된 단일 화면이 아니라 메뉴별 독립 화면으로 렌더링된다.

---

### Phase 19. 통합 테스트 / 보안 진단 / UAT (4주)

**작업**:
- 통합 시나리오 테스트 (3.1~3.11 업무 흐름 전체)
- 부하 테스트 (동시 100명, 1만건 자산대장)
- 보안 진단 (OWASP ASVS, 외부 모의해킹)
- 개인정보 영향평가
- 회계법인 1~2곳 UAT
- 발견 결함 수정

**완료 기준**:
- 보안 진단 Critical/High 0건
- UAT 완료 보고서
- 운영 매뉴얼/사용자 매뉴얼 v1.0

---

### Phase 20. 파일럿 → 정식 오픈 (4주)

**작업**:
- 파일럿 테넌트 2~3곳 운영
- 24/7 온콜 체계
- 핫픽스 프로세스
- 사용자 교육 / 영상 가이드
- GA 발표

**완료 기준**:
- 파일럿 신고 100건 무사고
- SLA 99.5% 달성
- 정식 가입 프로세스 가동

---

## 4. 횡단 작업 (Cross-cutting)

| 항목 | 시작 | 종료 | 비고 |
|------|------|------|------|
| 인프라/CI·CD 운영 | Phase 0 | 지속 | DevOps 1명 전담 |
| 보안 점검 (정기) | Phase 1 | 지속 | 분기별 / Phase 19 집중 |
| 문서화 (설계서·API·매뉴얼) | Phase 0 | 지속 | 페이즈 종료시 동기화 |
| 코드 리뷰 / 정적분석 | Phase 0 | 지속 | 매 PR |
| 회귀 테스트 자동화 | Phase 1 | 지속 | 페이즈마다 골든 케이스 추가 |
| 법령/서식 데이터 적재 | Phase 4 | Phase 15 | 도메인 전문가 작업 |
| 사용자 교육 콘텐츠 | Phase 18 | Phase 20 | UI 안정화 후 |

---

## 5. 테스트 전략

### 5.1 테스트 피라미드

```
        E2E (Cypress / Playwright)             ─── 시나리오 50개
       Integration (Spring Boot Test)          ─── 모듈별 핵심 흐름
     Unit Test (JUnit5, Vitest)                ─── 계산 로직, 권한 평가
    Contract Test (Spring Cloud Contract)      ─── BE-FE 계약
```

### 5.2 영역별 커버리지 목표

| 영역 | 커버리지 |
|------|---------|
| 세무조정 계산 로직 (B-1 ~ B-17) | 단위 ≥ 90% |
| 권한 평가 / 데이터 권한 | 단위 ≥ 95% |
| 법령 버전 결정 / 서식 버전 결정 | 단위 100% (회귀) |
| 전자신고 텍스트 포맷 생성 | 골든 샘플 바이트 단위 100% |
| 일반 CRUD | 단위 ≥ 70% |
| E2E | 핵심 시나리오 25개 |

### 5.3 표준 테스트 케이스 세트

- 5개년 × 3개 법인 유형(일반/중소/외국) × 3개 업종 = 45 케이스
- 각 케이스마다 (재무제표 + 자산대장 + 거래) 골든 데이터
- 매 빌드마다 산출세액 일치 확인

---

## 6. 릴리스 / 배포 전략

### 6.1 환경

| 환경 | 용도 | 데이터 |
|------|------|--------|
| Dev | 개발자 작업 | 합성 / 일부 익명화 |
| Staging | UAT, 부하 테스트 | 익명화된 운영 데이터 사본 |
| Prod | 실 서비스 | 실데이터 |
| DR | 재해 복구 | Prod 복제 (다른 AZ) |

### 6.2 배포 방식

- Blue-Green 또는 Canary
- DB 마이그레이션: Flyway, Forward-only (롤백은 코드로)
- 기능 플래그 (LaunchDarkly 또는 자체): 페이즈별 점진 활성화

### 6.3 운영 정책

- 운영 시간: 24/7
- 정기 점검: 매주 일요일 02:00 ~ 04:00
- 신고 마감 임박 기간(3~5월) 변경 금지 동결 (Code Freeze)

---

## 7. 팀 구성 / 인력 배치

### 7.1 코어 팀 (총 16명)

| 역할 | 인원 | 주 페이즈 |
|------|------|----------|
| PM / PMO | 1 | 0~20 |
| 아키텍트 | 1 | 0~20 |
| 세무 도메인 전문가 | 2 | 4, 8~13, 15~16 집중 |
| BE 개발자 | 4 | 1~17 |
| FE 개발자 | 3 | 1~18 |
| DBA | 1 | 1~19 |
| QA | 2 | 7~20 |
| 보안 엔지니어 | 1 | 1, 19 집중 |
| DevOps | 1 | 0~20 |

### 7.2 페이즈별 인력 비중 (예시)

| 페이즈 | BE | FE | DBA | QA | 도메인 |
|--------|----|----|-----|----|--------|
| 1~3 (기반) | 4 | 2 | 1 | – | – |
| 4~5 (버전) | 3 | 2 | 1 | – | 2 |
| 6~7 (고객/입력) | 3 | 3 | 1 | 1 | 1 |
| 8~13 (조정) | 4 | 2 | 1 | 2 | 2 |
| 14~15 (서식) | 3 | 3 | 1 | 2 | 2 |
| 16 (전자신고) | 3 | 2 | 1 | 2 | 2 |
| 17~18 (워크플로/횡단) | 3 | 3 | 1 | 2 | 1 |
| 19~20 (테스트/오픈) | 2 | 2 | 1 | 2 | 1 |

---

## 8. 리스크 관리

| 리스크 | 영향 | 가능성 | 대응 |
|--------|------|--------|------|
| 멀티 테넌트 데이터 누수 | 상 | 중 | 스키마 분리 + 자동화 테스트 + 코드 리뷰 |
| 세무조정 계산 오류 | 상 | 중 | 5개년 표준 케이스 + 도메인 전문가 검증 |
| 전자신고 파일 포맷 오류 | 상 | 중 | 골든 샘플 + 홈택스 모의 검증 + 메타 외부화 |
| 매년 법령/서식 개정 누락 | 상 | 중 | 영향 시뮬레이션 + 공지 + 회귀 테스트 |
| 신고 마감 기간 시스템 장애 | 상 | 중 | Code Freeze + 24/7 온콜 + DR |
| 회계 ERP 임포트 호환성 | 중 | 중 | 표준 템플릿 우선, ERP 어댑터는 단계적 추가 |
| 일정 지연 | 중 | 중 | 페이즈 게이트, 병행 가능 작업 식별 |
| 도메인 전문가 의존성 | 중 | 중 | 지식 문서화, 결정사항 ADR 기록 |

---

## 문서 변경 이력

| 버전 | 일자 | 작성자 | 변경내용 |
|------|------|--------|----------|
| 1.0 | 2026-05-14 | 강수 | 단계별 구현 계획 v1.0 작성 (Phase 0~20, 횡단/테스트/배포/팀/리스크 포함) |

---

**문의**: Kang-Soo.Cho@kr.ey.com
**연관 문서**:
- `법인세_세무조정계산서_시스템_설계서.md` (메인 설계서)
- `DB설계.md` (DB 상세)
- `업무흐름.md` (업무 흐름 + 화면 매핑)

## Multi-Tenant 테넌트-고객사 관계 보완 (2026-05-15)

- 대상 사용자 조직은 `회계법인`, `세무법인`, `세무사사무소`이며, 시스템에서는 각각 독립 테넌트로 관리한다.
- 관계 모델은 `테넌트 1 : N 고객사`이다. 하나의 테넌트는 여러 고객사를 관리하고, 각 고객사는 반드시 하나의 테넌트에만 소속된다.
- UI 기준으로 `prototype/index.html`의 `5-0 테넌트 관리` 화면에서 테넌트 유형, 계약, 상태, 사용자 수, 고객사 수, 소속 고객사 샘플을 확인한다.
- `5-A 고객사 관리` 화면은 고객사 목록에 소속 테넌트 컬럼과 테넌트 필터를 제공하고, 신규 등록/편집 시 소속 테넌트를 선택한다.
- 사용자 권한은 테넌트 범위 안에서 적용되며, 특정 고객사 접근은 `5-E 담당 법인 권한`에서 사용자-고객사 단위로 추가 제한한다.
- DB/도메인 설계 시 고객사 테이블은 `tenant_id` 또는 `tenant_code` 외래키를 가져야 하며, 고객사 코드는 `tenant_id + customer_code` 범위에서 유일해야 한다.
## 사용자-테넌트-고객사 관리 보완 (2026-05-15)

- 사용자는 반드시 하나의 소속 테넌트(`tenant_id`)를 가진다. 테넌트는 회계법인, 세무법인, 세무사사무소 단위 조직이다.
- 사용자의 고객사 접근 범위는 `user_customer_access` 또는 동등한 매핑으로 관리하며, 한 사용자는 소속 테넌트 안의 여러 고객사에 접근할 수 있다.
- `prototype/index.html`의 `5-B 사용자 관리` 화면은 테넌트 필터, 고객사 필터, 소속 테넌트 컬럼, 고객사 권한 요약을 제공한다.
- 사용자 등록/편집 시 소속 테넌트를 먼저 선택하고, 선택한 테넌트에 속한 고객사만 접근 범위로 선택할 수 있다.
- 역할별 기능 권한은 `5-C 역할 / 권한 매트릭스`에서 관리하고, 특정 고객사 담당 등급 또는 차단 같은 예외는 `5-E 담당 법인 권한`에서 관리한다.
- 권한 판정 순서는 `테넌트 소속 확인 -> 고객사 접근 범위 확인 -> 역할/기능 권한 확인 -> 고객사별 예외 권한 확인`을 기본 원칙으로 한다.
## 고객사별 대상 업무 범위 보완 (2026-05-15)

- 사용자 권한은 `tenant_id + customer_id + work_scope` 조합으로 판정한다. 같은 테넌트와 고객사라도 사용자마다 허용 업무가 다를 수 있다.
- 대상 업무 코드는 `INFO`, `ADJUST`, `FORM`, `VALIDATE`, `APPROVE`, `PRINT`, `EFILE`, `POST`를 기본값으로 사용한다.
- 권한 저장은 `user_customer_work_scope` 테이블을 별도로 두거나, `user_customer_access`에 `work_scopes` JSON/배열 컬럼을 추가하는 방식으로 구현한다.
- `prototype/index.html`의 `5-B 사용자 관리` 화면은 대상 업무 필터와 고객사별 대상 업무 체크리스트를 제공한다.
- 사용자 등록/편집 시 소속 테넌트를 선택한 뒤, 해당 테넌트의 고객사를 선택하고, 선택된 고객사마다 허용 업무 범위를 별도로 지정한다.
- 업무 실행 전 권한 판정 순서는 `테넌트 소속 확인 -> 고객사 접근 확인 -> 고객사별 대상 업무 확인 -> 역할/기능 권한 확인 -> 예외 권한 확인`을 따른다.
- 예: 같은 `EY 회계법인 / ㈜OOO 제조` 고객사라도 A 사용자는 `ADJUST`, `FORM`, `VALIDATE`만 가능하고, B 사용자는 `APPROVE`, `PRINT`만 가능하게 분리할 수 있다.

### Phase 2 산출물 (2026-05-15 구현)

- DB: `roles`, `user_roles`, `role_permissions`, `admin_audit_events` 테이블 추가.
- API: `GET /api/admin/roles`, `GET /api/admin/role-permissions`, `PUT /api/admin/roles/{role_code}/permissions` 구현.
- API: `GET/POST /api/admin/tenants/{tenant_code}/users`, `PUT /api/admin/tenants/{tenant_code}/users/{login_id}`, 사용자 잠금/2FA 리셋 구현.
- 인증: 로그인/세션 조회 응답의 역할 목록을 `user_roles` 기반으로 조회하도록 변경.
- 화면: `frontend/index.html`에 `adminWorkspace` 추가, `frontend/app.js`에서 Admin 메뉴 클릭 시 실제 사용자/역할 화면으로 라우팅.
- 테스트: `tests/integration_flow.rs`에서 사용자 등록, 역할 변경, 잠금, 2FA 리셋, 권한 매트릭스 저장 검증.

### Phase 2 완료 기준 확인

- 관리자 API에서 사용자 생성/편집/잠금/2FA 리셋이 실제 DB에 반영된다.
- 역할별 기능 권한을 DB에 저장하고 다시 조회할 수 있다.
- 실제 실행 화면에서 Admin 메뉴가 사용자 관리/역할 권한 화면으로 연결된다.
- 검증 명령: `cargo test --all --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`.

### Phase 3 산출물 (2026-05-15 구현)

- DB: `user_customer_access`, `user_customer_work_scope` 테이블 추가.
- API: 사용자별 고객사 접근 등급(`OWNER`, `CO_WORKER`, `REVIEWER`, `ASSISTANT`, `VIEWER`, `BLOCKED`) 저장/조회 구현.
- API: 고객사별 대상 업무 범위(`INFO`, `ADJUST`, `FORM`, `VALIDATE`, `APPROVE`, `PRINT`, `EFILE`, `POST`) 저장/조회 구현.
- 화면: `prototype/index.html`의 `5-B 사용자 관리`와 `5-E 담당 고객사 / 업무 권한`에 고객사별 업무 범위 UI 추가.
- 화면: `frontend/app.js`의 실제 Admin 사용자 화면에서 고객사별 대상 업무 체크리스트를 API와 연동.
- 테스트: 통합 테스트에서 사용자-고객사-업무범위 생성 및 변경 결과를 검증.

### Phase 3 완료 기준 확인

- 한 사용자가 같은 테넌트 내 여러 고객사를 가질 수 있고, 고객사마다 서로 다른 업무 범위를 가진다.
- 고객사 접근 등급과 업무 범위가 사용자 조회 API에 함께 반환된다.
- 역할/기능 권한과 고객사/업무 접근 범위가 분리되어 저장된다.
- 검증 명령: `cargo test --all --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`.
## 테넌트별 고객사 대상 업무 구분 보완 (2026-05-15)

- 고객사별 대상 업무 범위는 테넌트 고객사 마스터의 `work_scopes`로 관리한다.
- 사용자별 고객사 업무 권한(`user_customer_work_scope`)은 고객사 `work_scopes`의 부분집합으로만 저장된다.
- API는 사용자 등록/수정 시 고객사가 대상 업무로 갖지 않는 `work_scope`를 거부한다.
- 화면은 `5-A 고객사 관리`에서 고객사 대상 업무를 보여주고, `5-B 사용자 관리`에서 사용자 부여 업무와 고객사 대상 업무를 함께 표시한다.
- 완료 기준: 한 테넌트 내 고객사별 대상 업무가 서로 달라도 사용자는 각 고객사의 대상 업무 안에서만 권한을 받을 수 있어야 한다.

### Phase 4 산출물 (2026-05-15 구현)

- DB: `tax_law_versions`, `tax_rates`, `tax_limits`, `law_amendment_history` 기반 법령/세율 버전 관리 구현.
- DB: 2021~2026 법령 버전과 법인세율표, 한도, 세액공제, 기준내용연수, 중소기업 판정기준, 결손금 규정 초기 데이터 적재.
- API: 법령 버전 생성/조회, 상태 전환, 세율표/한도표 생성/조회, 개정 이력 조회/등록, 영향 시뮬레이션 구현.
- API: 신규 법령 버전은 `DRAFT`로 생성되고 `DRAFT -> REVIEWED -> ACTIVE -> RETIRED` 흐름으로 전환되며 상태 변경 이력은 `law_amendment_history`에 남긴다.
- 화면: `frontend/app.js`의 법령 버전 관리 워크스페이스를 F-1~F-4 및 세부 정책 메뉴로 분리하고 실제 API와 연동.
- 테스트: 통합 테스트에서 2021~2026 시드 데이터, 상태 전환, 영향 시뮬레이션, 개정 이력, 세율/한도 API를 검증.

### Phase 4 완료 기준 확인

- 사업연도 종료일 기준 적용 법령 버전을 `ensure_law_snapshot`에서 결정하고 스냅샷에 법령/세율/한도/서식 버전 ID를 저장한다.
- 2021~2026 세율·한도·공제·감가상각 내용연수·중소기업 기준·결손금 규정 데이터가 API 조회 대상에 포함된다.
- 영향 시뮬레이션은 선택한 법령 버전의 세율/한도 행 수와 테넌트별 사업연도 영향을 반환한다.
- `DRAFT -> REVIEWED -> ACTIVE` 상태 전환과 개정 이력 생성이 통합 테스트로 검증된다.

### Phase 5 산출물 (2026-05-15 구현)

- DB: `tax_forms`, `form_templates`, `form_validations`, `form_field_migration` 테이블을 추가하고 기존 `form_versions`, `form_relationships`, `form_data_migration_history`와 연결.
- API: `GET/POST /api/form-versioning/forms`, `GET/POST /api/form-versioning/versions`, 서식 버전 상태 변경, 항목 매핑 등록/조회 구현.
- API: `GET /api/form-versioning/resolve`로 테넌트 사업연도별 적용 서식 버전을 결정.
- API: `POST /api/form-versioning/migrations/dry-run|execute|rollback`으로 서식 데이터 마이그레이션 검토, 실행, 롤백 구현.
- 화면: `frontend/index.html`에 `formVersioningWorkspace`를 추가하고 `frontend/app.js`에서 서식 버전, 항목 매핑, 마이그레이션, 적용 서식 조회 화면을 분리 구현.
- 테스트: 통합 테스트에서 서식 버전 생성/승인, 사업연도 적용 버전 결정, 항목 매핑, Dry-run/Execute/Rollback을 검증.

### Phase 5 완료 기준 확인

- 동일 사업연도/동일 서식 코드는 `resolve_form_version`에서 정확히 하나의 활성 또는 승인 버전을 선택한다.
- 서식 버전 변경 전 `dry-run`이 추가/삭제/공통 필드를 반환하고, `execute`가 `form_data`와 `form_data_migration_history`에 반영된다.
- `rollback`은 최신 마이그레이션 이력을 기준으로 이전 서식 버전으로 되돌리거나 신규 생성 데이터를 제거한다.
- 서식 메타·버전·매핑 화면은 실제 API와 연결되어 있으며 통합 테스트와 클리피 검증을 통과한다.

### Phase 6 산출물 (2026-05-15 구현)

- DB: 사업연도 기본 상태를 `DRAFT`로 전환하고 테넌트 스키마에 `tax_agents`, `customer_users` 테이블을 추가.
- API: `GET/POST /api/tenants/{tenant_code}/business-years`로 사업연도 생성/목록 조회 구현.
- API: 사업연도 생성 시 `ensure_law_snapshot`을 즉시 호출해 법령/세율/서식/전자신고 버전 스냅샷을 자동 생성.
- API: `POST /api/tenants/{tenant_code}/business-years/{by_id}/status`로 `DRAFT -> IN_REVIEW -> APPROVED -> FILED -> AMENDED` 상태 전환 구현.
- API: `FILED` 전환 시 `by_law_snapshot.locked = true`로 적용 스냅샷 잠금 처리.
- 화면: `frontend/index.html`에 `customerWorkspace`를 추가하고 고객사 등록/목록, 사업연도 생성/상태 변경/스냅샷 조회 화면을 실제 API와 연결.
- 테스트: 통합 테스트에서 사업연도 생성 후 자동 스냅샷 조회, 목록 조회, 상태 전환, FILED 스냅샷 잠금을 검증.

### Phase 6 완료 기준 확인

- 고객사와 사업연도를 생성하면 별도 수동 호출 없이 적용 법령/서식 스냅샷이 생성된다.
- FILED 상태 전환 시 사업연도 `locked_at`과 스냅샷 `locked`가 설정되어 이후 적용 기준을 고정한다.
- 고객사/사업연도 화면은 메뉴 클릭 시 독립 화면으로 열리고, 등록/상태 전환/스냅샷 조회가 실제 API로 동작한다.
- 사업연도 기간은 `start_date <= end_date` 제약을 유지하며 12월 결산과 비12월 결산을 동일한 스냅샷 결정 로직으로 처리한다.

### Phase 7 산출물 (2026-05-15 구현)

- DB: 테넌트 스키마에 `import_batches`, `import_errors`, `account_mappings`, `transactions`를 추가하고 `financial_statements`, `fs_lines`, `assets`에 임포트 배치/표준계정/자산분류 컬럼을 보강.
- API: `GET /api/tenants/{tenant_code}/tax-data/templates/{data_type}`로 재무제표·자산대장·거래 명세 표준 CSV 템플릿 다운로드 구현.
- API: `POST /api/tenants/{tenant_code}/business-years/{by_id}/tax-data/{data_type}/import`로 CSV/XLSX multipart 업로드, 행 검증, 배치 저장, 오류 저장, DB 적재 구현.
- API: 재무제표 라인, 자산대장, 거래 명세, 임포트 배치/오류, 고객사별 계정 매핑 조회/등록, 사업연도별 검증 요약 조회 구현.
- 로직: 재무제표 임포트 시 차변/대변 합계 검증, 표준계정 매핑 학습, 동일 고객사 재임포트 자동 매핑, 자산명/분류 기반 업무용차 자동 판정 구현.
- 화면: `frontend/index.html`에 `taxDataWorkspace`를 추가하고 재무제표 입력/임포트, 계정과목 매핑, 거래 명세, 자산/감가상각 화면을 메뉴별 독립 화면으로 구현.
- 테스트: 통합 테스트에서 템플릿 다운로드, 재무제표 임포트, 매핑 학습/재사용 95% 이상, 차변/대변 불일치 오류, 자산 업무용차 판정, 거래 명세 적재, 검증 요약을 검증.

### Phase 7 완료 기준 확인

- CSV/XLSX 업로드 API가 실제 multipart 파일을 받아 검증 통과 데이터만 테넌트 DB에 적재한다.
- 차변/대변 합계가 맞지 않으면 `VALIDATION_FAILED` 배치와 `import_errors` 행으로 오류가 표시된다.
- 고객사별 계정 매핑은 최초 표준계정 포함 업로드 시 학습되고, 동일 고객사 재임포트 시 자동 매핑률이 95% 이상으로 산출된다.
- 자산대장은 대량 삽입을 위해 bulk insert로 저장하며 업무용차 여부를 자동 판정한다.
- 세무정보 입력 화면은 `tax-data` 하위 메뉴별로 분리되어 실제 API와 연결되고 통합 테스트 및 JS 문법 검증을 통과한다.

### Phase 8 산출물 (2026-05-15 구현)

- DB: 테넌트 스키마에 `adjustment_items`를 추가하고 `reserves`에 `source_module`을 보강해 세무조정 항목과 유보를 모듈별로 추적.
- API: `POST /api/tenants/{tenant_code}/business-years/{by_id}/adjustments/income`로 B-1 소득금액조정 계산/저장 구현.
- API: `GET /api/tenants/{tenant_code}/business-years/{by_id}/adjustments/income`으로 B-1 조정 항목 조회, `GET /reserves`로 유보 조회 구현.
- 로직: 재무제표 `NET_INCOME` 라인을 자동으로 결산서상 당기순이익으로 읽고, 익금산입/익금불산입/손금산입/손금불산입 4개 섹션을 차가감 소득금액으로 계산.
- 로직: 처분이 `RESERVE`이거나 일시차이 항목이면 `reserves`를 자동 생성하고 다음 사업연도 이월연도를 설정.
- 화면: `frontend/index.html`에 `adjustmentWorkspace`를 추가하고 `adjustment` 하위 메뉴별 독립 화면과 B-1 그리드, 적용 법령 배너, 유보 결과를 실제 API와 연결.
- 테스트: 통합 테스트에서 재무제표 당기순이익 자동 인식, B-1 계산, 조정 항목 저장, 유보 자동 생성, 적용 법령 배너 반환을 검증.

### Phase 8 완료 기준 확인

- 결산서상 당기순이익은 재무제표 업로드 데이터의 `NET_INCOME` 표준계정에서 자동 산출된다.
- B-1 4개 섹션 합계로 `당기순이익 + 익금산입/손금불산입 - 익금불산입/손금산입 = 차가감 소득금액`을 계산한다.
- 처분이 유보인 조정 항목은 `reserves`에 자동 생성되며 조회 API와 화면에 표시된다.
- 계산 결과와 모든 조정 항목은 사업연도 스냅샷 ID와 연결되어 적용 법령 배너를 표시한다.
- 세무조정 화면은 `adjustment` 하위 메뉴별로 분리되어 B-1 화면이 실제 API와 연결되고 통합 테스트, JS 문법 검증, clippy를 통과한다.

### Phase 9 산출물 (2026-05-15 구현)

- DB: 테넌트 스키마에 `vehicle_usage_logs`를 추가해 차량별 월별 총 주행거리, 업무사용거리, 업무사용비율을 저장.
- API: `POST/GET /api/tenants/{tenant_code}/business-years/{by_id}/vehicle-usage-logs`로 업무용승용차 운행기록 등록/조회 구현.
- API: `POST/GET /api/tenants/{tenant_code}/business-years/{by_id}/adjustments/assets/{module_code}`로 B-4 감가상각, B-5 퇴직급여충당금, B-6 대손충당금, B-10 업무용승용차 계산/조회 구현.
- 로직: B-4는 자산대장과 Phase 4 `DEPRECIATION_LIFE` 한도 데이터를 사용해 회사상각비와 세법상각한도를 비교하고 `depreciation` 및 조정 항목을 저장.
- 로직: B-5는 추계액/사외적립금 기준, B-6은 1% 또는 실적대손 기준, B-10은 운행기록 업무사용비율과 1,500만/8,000만/5년 정액 한도를 적용.
- 화면: `adjustment` 하위 메뉴의 감가상각, 퇴직급여충당금, 대손충당금, 업무용승용차 화면을 실제 API 계산 화면으로 연결.
- 테스트: 통합 테스트에서 자산대장 연동, Phase 4 기준내용연수 사용, 퇴직/대손 한도 계산, 차량 운행기록 업무사용비율, B-10 한도 초과 조정을 검증.

### Phase 9 완료 기준 확인

- B-4 감가상각은 자산별 회사 내용연수와 Phase 4 기준내용연수 데이터를 비교해 한도초과액을 자동 계산한다.
- B-5/B-6은 입력값 기반 한도 산식으로 손금불산입 조정 항목과 유보를 생성한다.
- B-10은 차량별 운행기록의 업무사용비율을 우선 적용하고, 기록이 없으면 요청 업무사용비율을 사용한다.
- 모든 자산 기반 조정 결과는 `adjustment_items`, `tax_adjustments`, `reserves`, `depreciation`에 실제 저장되며 스냅샷 ID와 연결된다.
- 자산 기반 세무조정 화면은 메뉴별 독립 화면으로 열리고 통합 테스트, JS 문법 검증, clippy를 통과한다.

### Phase 10 산출물 (2026-05-15 구현)

- DB: 테넌트 스키마에 `donation_carryforwards`, `entertainment_revenue_breakdowns`, `loan_interest_facts`를 추가.
- DB: Phase 4 `tax_limits`에 기부금 특례/일반 한도율, 기부금 이월연수, 접대비 수입금액 한도율, 무증빙 접대비 손금불산입률, 지급이자 인정이자율 기본값을 법령 버전별로 추가.
- API: `POST/GET /api/tenants/{tenant_code}/business-years/{by_id}/adjustments/transactions/{module_code}`로 B-2 기부금, B-3 접대비, B-9 지급이자 계산/조회 구현.
- 로직: B-2는 거래 명세의 기부금 유형을 특례/일반으로 분류하고 법령 버전별 한도율을 적용해 한도초과액을 10년 이월로 자동 저장.
- 로직: B-3은 접대비 거래의 카드/비카드 증빙을 구분하고 수입금액 명세 기반 한도를 적용해 무증빙액과 한도초과액을 산출.
- 로직: B-9는 지급이자 거래를 채권자불분명, 수령자불분명, 건설자금, 업무무관 유형으로 분류하고 가지급금 적수/평균잔액에 가중평균 이자율을 적용.
- 화면: `adjustment` 하위 `기부금/접대비` 메뉴를 B-2/B-3/B-9 거래 기반 조정 화면으로 분리 연결하고 수입금액/가지급금 입력과 결과 표를 제공.
- 테스트: 통합 테스트에서 기부금 10년 이월, 접대비 한도와 법령 버전 한도율 매칭, 지급이자 4유형 및 가지급금 인정이자 계산을 검증.

### Phase 10 완료 기준 확인

- 기부금 한도초과액은 `donation_carryforwards`에 원천연도, 유형, 잔액, 만료연도(`사업연도 + 10`)로 저장된다.
- B-2 재계산 시 해당 사업연도의 기존 이월 행을 재작성하고, 이전 사업연도 이월 잔액은 만료 전 한도 범위에서 자동 사용한다.
- 접대비 한도는 사업연도 스냅샷의 `law_version_id`에 연결된 `tax_limits(ENTERTAINMENT_*)` 값을 사용한다.
- 지급이자는 거래 설명 기반 4유형과 입력된 가지급금 적수/평균잔액을 모두 조정 항목으로 저장한다.
- 거래 기반 세무조정 화면은 독립 메뉴 화면으로 열리고 통합 테스트 및 JS 문법 검증을 통과한다.

### Phase 11 산출물 (2026-05-15 구현)

- DB: 테넌트 스키마에 `valuation_positions`, `capital_changes`를 추가하고 `carryforward_loss`에 사용액, 만료액, 갱신시각을 보강.
- DB: Phase 4 `tax_limits`에 중소기업/일반법인 이월결손금 공제한도율(`LOSS_DEDUCTION_LIMIT_BPS_*`)을 추가.
- API: `POST/GET /api/tenants/{tenant_code}/business-years/{by_id}/adjustments/evaluation/{module_code}`로 B-7, B-8, B-11, B-15 계산/조회 구현.
- 로직: B-7/B-8은 평가 포지션별 장부금액과 세법평가액 차이를 조정 항목과 유보로 저장.
- 로직: B-11은 고객사별 이월결손금 잔액, 사용액, 만료액을 관리하고 중소기업 100%, 일반법인 80% 한도율을 적용.
- 로직: B-15는 해당 사업연도의 모든 모듈 유보를 집계하고 자본 변동 내역을 함께 자본금과 적립금 명세로 저장.
- 화면: 세무조정 메뉴에 외화평가, 재고·유가증권 평가, 이월결손금, 자본금과 적립금 독립 화면을 추가.
- 테스트: 통합 테스트에서 B-7/B-8 유보 생성, B-11 결손금 만료 알림, B-15 유보 집계 일치를 검증.

### Phase 11 완료 기준 확인

- B-15 집계 금액은 `GET /reserves`의 사업연도 유보 합계와 일치한다.
- B-11은 만료연도가 현재 사업연도 또는 다음 사업연도인 잔여 결손금을 `expiration_alerts`로 반환한다.
- B-7/B-8 평가 차이는 `valuation_positions`, `adjustment_items`, `reserves`에 함께 저장된다.
- 평가·이월·유보 화면은 메뉴별 독립 화면으로 열리고 통합 테스트, JS 문법 검증, clippy를 통과한다.

### Phase 12 산출물 (2026-05-15 구현)

- DB: 테넌트 스키마에 `tax_credit_claims`, `minimum_tax_results`, `penalty_tax_items`를 추가.
- DB: Phase 4 `tax_limits`에 투자·외국납부·재해·중소특별·창업 감면율과 최저한세율을 법령 버전별로 추가.
- API: `POST/GET /api/tenants/{tenant_code}/business-years/{by_id}/adjustments/tax/{module_code}`로 B-12, B-13, B-14 계산/조회 구현.
- 로직: B-12는 공제 유형별 대상금액, 공제율, 요청액, 허용액을 계산하고 산출세액 한도 내에서 결정세액을 차감.
- 로직: B-13은 과세표준과 일반 산출세액을 최저한세와 비교해 추가 납부세액을 산출.
- 로직: B-14는 무신고/과소/납부지연/원천 가산세 유형별 기준세액, 일수, 감면율을 적용.
- 화면: 세무조정 메뉴에 세액공제·감면, 최저한세, 가산세 독립 화면을 연결.
- 테스트: 통합 테스트에서 B-12 공제 후 결정세액, B-13 최저한세 추가세액, B-14 감면 적용 가산세를 검증하고 최저한세 5종 단위 회귀 테스트를 추가.

### Phase 12 완료 기준 확인

- B-12 응답은 `calculated_tax -> deductions -> determined_tax` 흐름을 반환한다.
- B-13은 일반 세액이 최저한세보다 낮은 경우 차액을 `ADD` 조정 항목으로 저장한다.
- B-14는 감면율 적용 후 가산세를 `penalty_tax_items`와 조정 항목에 저장한다.
- 최저한세 적용 케이스 5종 단위 테스트와 통합 테스트, JS 문법 검증, clippy를 통과한다.

## 사용자-고객사 업무 권한 구현 보완 (2026-05-15)

- 고객사 대상 업무는 테넌트 스키마의 `customers.work_scopes`로 관리한다.
- 사용자별 고객사 접근은 `user_customer_access`, 사용자별 고객사 업무 권한은 `user_customer_work_scope`로 분리 저장한다.
- 사용자 등록/수정 API는 요청된 `work_scope`가 고객사 대상 업무의 부분집합이 아니면 거부한다.
- 운영 UI(`frontend/app.js`)의 사용자 관리 화면은 신규 등록과 편집 모두에서 다중 고객사를 선택하고, 고객사별 접근 등급/주담당/업무 권한을 저장한다.
- 화면은 각 고객사의 대상 업무 목록을 보여주고, 대상 업무가 아닌 코드는 비활성화한다.

### Phase 13 산출물 (2026-05-15 구현)

- DB: 테넌트 스키마에 `foreign_income_items`, `consolidated_entities`, `consolidation_eliminations`를 추가.
- API: `POST/GET /api/tenants/{tenant_code}/business-years/{by_id}/adjustments/special/{module_code}`로 B-16, B-17 계산/조회 구현.
- 로직: B-16은 외국법인 국내원천 소득별 총수입, 귀속 비용, 고정사업장 배분율, 원천징수세액을 계산한다.
- 로직: B-17은 연결 대상 법인 과세소득 합계에서 내부거래 제거 항목을 반영해 연결 과세표준을 산출한다.
- 화면: 세무조정 메뉴에 외국법인, 연결납세 독립 화면을 연결.
- 테스트: 통합 테스트에서 B-16 과세소득/원천징수 합계와 B-17 연결 과세표준을 검증한다.

### Phase 13 완료 기준 확인

- B-16 응답은 `foreign_income_count`, `withholding_tax_total`, 과세대상 소득 조정 항목을 반환한다.
- B-17 응답은 연결 법인 수, 제거 항목 합계, 연결 과세표준을 반환한다.
- B-16/B-17 입력 근거는 각 전용 테이블에 저장되고 결과는 공통 조정 항목에 저장된다.
- JS 문법 검증, Rust 포맷/테스트, clippy, Docker API 빌드와 라이브 API 검증을 완료 대상으로 한다.

### Phase 14 산출물 (2026-05-15 구현)

- DB: 테넌트 스키마에 `form_data_history`를 추가해 FORM3 자동 생성/수동 수정 이력을 보관.
- API: `GET /forms/{form_code}/preview`로 서식 필드, 자동/수동 출처, 검증 결과, 변경 이력을 반환.
- API: `PUT /forms/{form_code}`로 수동 수정 필드를 저장하고 변경 전/후 JSON을 이력화.
- 로직: FORM3 생성 시 기존 원천 서식 데이터가 있으면 `form_relationships` 규칙을 적용해 대상 필드를 자동연동.
- 로직: `form_validations`의 `REQUIRED`, `MIN`, `EQUALS_FIELD` 규칙을 평가하는 서식 검증 엔진 추가.
- 화면: `6.1 과세표준 및 세액조정계산서(별지 제3호)` 메뉴에서 자동 생성, 미리보기, 수동 수정, 검증/이력 조회 UI 제공.
- 테스트: 통합 테스트에서 FORM15 -> FORM3 자동연동, FORM3 미리보기, 수동 수정 이력 저장을 검증.

### Phase 14 완료 기준 확인

- FORM3는 세무조정 결과와 서식 관계를 기반으로 자동 생성된다.
- 미리보기 응답은 자동연동 필드와 수동 수정 필드를 구분할 수 있는 `_meta`와 필드 출처를 제공한다.
- 수동 수정 시 `form_data_history`에 `MANUAL_UPDATE` 이력이 남는다.
- 서식 검증 결과와 변경 이력이 UI와 API에서 함께 조회된다.

### Phase 15 산출물 (2026-05-15 구현)

- API: `GET /forms/attachments`로 FORM3/FORM15/FORM22 부속서식 진행상태, 검증 오류 수, 대표 금액, 수정시각을 조회한다.
- API: `GET /forms/{form_code}/pdf`로 사업연도 스냅샷과 현재 서식 데이터를 반영한 PDF를 다운로드한다.
- API: `GET /forms/pdf-bundle/download`로 주요 부속서식 PDF를 ZIP으로 일괄 생성한다.
- 로직: PDF 출력 시 서식이 없으면 자동 생성하고, 서식 상태에 따라 `DRAFT`/`APPROVED` 워터마크를 부여한다.
- 로직: 현재 구현은 외부 JasperReports 런타임 대신 내장 PDF 렌더러와 ZIP 번들러로 동작하며, 추후 `form_versions.template_json`의 템플릿 메타를 확장해 100여 종 서식으로 확대한다.
- 화면: `6.2 100여 종 부속서식` 메뉴를 독립 화면으로 연결하고, 생성/PDF/ZIP 다운로드 조치를 제공한다.
- 프로토타입: `prototype/index.html`의 서식 작성 화면에 부속서식 상태표, 검증 수, PDF/ZIP 조치를 반영했다.
- 테스트: 통합 테스트에서 부속서식 목록, FORM3 PDF 바이트(`%PDF`), ZIP 바이트(`PK`)를 검증한다.

### Phase 15 완료 기준 확인

- 부속서식 목록은 사업연도별 생성 여부와 검증 오류 수를 반환한다.
- 개별 PDF 다운로드는 유효한 `application/pdf` 응답과 PDF 헤더를 반환한다.
- 일괄 다운로드는 `application/zip` 응답과 ZIP 헤더를 반환한다.
- `6.2 부속서식` 메뉴 클릭 시 숨김 처리된 단일 화면이 아니라 독립 부속서식 화면으로 열린다.

### Phase 16 산출물 (2026-05-15 구현)

- DB: public `efile_record_fields`에 `data_type`, `description`을 추가하고, `efile_validation_rules`를 신설했다.
- DB: `CIT-EFILE-2026`의 H/D/T 레코드 필드 위치, 길이, 타입, 패딩, 원천 경로를 메타로 적재했다.
- API: `GET /efilings/precheck`로 전자신고 사전검증 결과, 레코드 수, 체크섬 미리보기를 반환한다.
- API: `GET /efilings/format-spec`로 사업연도에 적용되는 전자신고 고정폭 필드 메타를 반환한다.
- 로직: 전자신고 빌더는 DB 메타의 레코드 길이/필드 위치를 기준으로 H/D/T 고정폭 레코드를 생성한다.
- 로직: 사업자번호 형식/체크섬, FORM3 납부세액, 레코드 길이 검증을 사전검증 규칙으로 평가한다.
- 로직: 파일 생성 시 ERROR 검증 이슈가 있으면 생성하지 않고, WARN 이슈는 `efiling_validation`에 저장한다.
- 화면: `8.1`, `8.2`, `8.3` 전자신고 메뉴를 독립 화면으로 연결해 사전검증, 포맷 메타, 신고 이력을 조회한다.
- 테스트: 통합 테스트에서 사전검증, 포맷 메타, 기존 전자신고 파일 다운로드를 함께 검증한다.

### Phase 16 완료 기준 확인

- 전자신고 포맷은 코드 상수만이 아니라 DB 메타로 조회 가능하다.
- 사전검증 API는 생성 전 오류/WARN을 반환하고 ERROR가 없을 때만 생성 가능 상태가 된다.
- 생성 파일은 고정폭 H/D/T 레코드와 CRLF 줄바꿈을 유지한다.
- UI에서 전자신고 메뉴 클릭 시 각 메뉴별 화면이 분리되어 열린다.

### Phase 17 산출물 (2026-05-15 구현)

- DB: 테넌트 스키마에 `workflow_events`, `approval_lines`를 추가했다.
- API: `GET /business-years/{by_id}/workflow`로 상태 전이 이벤트와 결재라인을 조회한다.
- API: `GET /business-years/{by_id}/amendment-preview`로 수정신고 진입 후 원 신고 상태 대비 차이 항목을 조회한다.
- 로직: DRAFT -> IN_REVIEW -> APPROVED -> FILED -> AMENDED 상태 전이를 검증하고 위반 시 거부한다.
- 로직: IN_REVIEW 진입 시 결재라인을 생성하고, APPROVED 전환 시 결재라인을 승인 처리한다.
- 로직: FILED 전환 시 사업연도 잠금 및 법령 스냅샷 잠금을 유지하고, AMENDED 전환 시 사업연도 작업 잠금을 해제한다.
- 화면: 사업연도 관리 화면에 워크플로 조회, 수정비교 조회, actor/comment 기반 상태 변경을 추가했다.
- 테스트: 통합 테스트에서 상태 전이 위반 거부, 결재 이력, FILED 잠금, AMENDED 잠금 해제, 수정신고 차이 미리보기를 검증한다.

### Phase 17 완료 기준 확인

- 허용되지 않은 상태 전이는 `BAD_REQUEST`로 거부된다.
- 결재 요청/승인 이력은 `workflow_events`, `approval_lines`에 남는다.
- 신고 완료 시 스냅샷은 잠기고, 수정신고 진입 시 사업연도 작업 잠금은 해제된다.
- 수정신고 미리보기 API는 원 신고 상태와 현재 상태의 차이 목록을 반환한다.
