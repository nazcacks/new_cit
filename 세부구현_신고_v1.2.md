# 신고작업 — 1. 세무정보 입력 세부 구현 계획서 v1.2

> **문서 버전**: v1.2
> **작성일**: 2026-06-02
> **작성자**: 강수 (Kang-Soo.Cho@kr.ey.com)
> **연관 문서**: `법인세_세무조정계산서_시스템_설계서.md`, `업무흐름.md`, `세부구현_신고_v1.0.md`, `세부구현_신고_v1.1.md`, `DB설계.md`
> **목적**:
> ② 신고 작업 > 1. 세무정보 입력의 전체 서브 단계를 업무 흐름에 맞춰 재구성하고, **복식부기(Double-Entry Bookkeeping)** 원리에 맞춘 데이터 구성을 더 세부적으로 구체화한다.
> v1.1 대비 **국세청 표준재무제표 항목(표준계정과목코드) 관리 기능**을 신규 추가하고, 고객사계정 → (세무 표준계정 / 표준재무제표 항목) **이중 매핑 체계**를 정립한다.

---

## v1.2 변경 요약 (v1.1 대비)

| 구분 | v1.1 | v1.2 추가/강화 |
|------|------|---------------|
| 표준 체계 | 세무조정용 표준계정(`standard_accounts`) 단일 | **표준재무제표 항목(`std_fs_items`)** 신규 + **이중 매핑 체계** 정립 |
| 신규 단계 | – | **1-2B. 표준재무제표 작성·검증** 서브 단계 신규 |
| 관리자 기능 | – | **5-F 코드 관리 > 표준재무제표 항목 마스터** (CRUD·버전·업종별·임포트) |
| 버전 관리 | by_law_snapshot(법령) | **표준재무제표 양식 버전**을 사업연도 스냅샷에 편입 |
| 전자신고 연계 | 서식 중심 | **표준재무제표 → 홈택스 XML** 매핑·생성 흐름 추가 |
| 세부화 | 화면 골격 | 필드 스펙, 계산식, 상태도, 에러코드, 트랜잭션 경계 보강 |

---

## 목차

1. [세무정보 입력 설계 원칙](#1-세무정보-입력-설계-원칙)
2. [서브 단계 전체 흐름](#2-서브-단계-전체-흐름)
3. [복식부기 데이터 모델 기초 — 이중 매핑 체계](#3-복식부기-데이터-모델-기초--이중-매핑-체계)
4. [표준재무제표 항목 관리 (신규)](#4-표준재무제표-항목-관리-신규)
5. [1-1. 재무제표 입력 및 임포트](#5-1-1-재무제표-입력-및-임포트)
6. [1-2. 계정과목 매핑 (이중 매핑)](#6-1-2-계정과목-매핑-이중-매핑)
7. [1-2B. 표준재무제표 작성·검증 (신규)](#7-1-2b-표준재무제표-작성검증-신규)
8. [1-3. 자산 및 감가상각](#8-1-3-자산-및-감가상각)
9. [1-4. 거래 명세](#9-1-4-거래-명세)
10. [1-5. 업무용승용차 운행기록](#10-1-5-업무용승용차-운행기록)
11. [1-9. 일관성 검증](#11-1-9-일관성-검증)
12. [데이터베이스 상세 설계](#12-데이터베이스-상세-설계)
13. [API 명세](#13-api-명세)
14. [전자신고 연계 — 표준재무제표 XML](#14-전자신고-연계--표준재무제표-xml)
15. [역할별 접근 제어](#15-역할별-접근-제어)
16. [프론트엔드 구현 상세](#16-프론트엔드-구현-상세)
17. [구현 우선순위 및 검증 항목](#17-구현-우선순위-및-검증-항목)
18. [현재 코드베이스 대조 Gap 표 및 실행 순서](#18-현재-코드베이스-대조-gap-표-및-실행-순서)

---

## 1. 세무정보 입력 설계 원칙

### 1.1 복식부기 원칙과 세무정보 입력의 관계

법인세 세무조정은 **기업회계기준(K-IFRS / K-GAAP)** 으로 작성된 재무제표를 출발점으로 한다. 재무제표는 복식부기 원리 위에 존재하므로, 시스템은 다음 불변 조건을 항상 유지해야 한다.

| 복식부기 불변 조건 | 시스템 검증 시점 |
|-----------------|----------------|
| 모든 계정의 차변 합계 = 대변 합계 | 임포트 즉시, 1-9 일관성 검증 |
| BS: 자산 합계 = 부채 합계 + 자본 합계 | 임포트 즉시 |
| IS: 당기순이익 = 수익 합계 - 비용 합계 | 임포트 즉시 |
| BS 기말자본 = BS 기초자본 + 당기순이익 + 자본 변동 | 1-9 일관성 검증 |
| 자산대장 취득원가 합계 ≤ BS 유형·무형자산 잔액 | 1-9 일관성 검증 |
| **표준재무제표 자산총계 = 결산 BS 자산총계** | 1-2B 표준재무제표 검증 (신규) |

### 1.2 핵심 설계 원칙

| 원칙 | 내용 |
|------|------|
| **이중 표준화** | 고객사 계정은 두 축으로 표준화된다. ① **세무 표준계정**(세무조정 집계용, `standard_accounts`) ② **표준재무제표 항목**(국세청 신고용, `std_fs_items`). 두 매핑은 독립적이며 충돌 검증으로 정합성 확보 |
| 표준계정 중심 | 세무조정은 세무 표준계정 기준, 전자신고 표준재무제표 제출은 표준재무제표 항목 기준 |
| 잔액(Balance) vs 발생(Movement) 분리 | BS는 기말 잔액, IS·CF는 당기 발생액. `fs_lines.balance_type`으로 구분 |
| 차변/대변 방향 명시 | 계정별 정상잔액 방향(`normal_balance`)을 표준계정 마스터에 정의하여 부호 오류 방지 |
| 전기 이월 자동화 | 자산대장·이월결손금·유보 잔액 + **계정 매핑(세무·표준재무제표)** 은 전기 데이터에서 자동 복사, 차이만 수정 |
| 버전 고정 | 표준재무제표 양식은 사업연도 `by_law_snapshot`에 버전이 고정되어, 신고 후에도 당시 양식으로 재현 가능 |
| 원천 추적성 | 세무조정·표준재무제표·전자신고에 사용된 모든 숫자는 원천 레코드 PK(`fs_lines.id`, `asset.id`, `transaction.id`)로 역추적 가능 |

---

## 2. 서브 단계 전체 흐름

v1.2에서 **1-2B 표준재무제표 작성·검증** 단계를 신설하여, 1-2 매핑 직후·1-3 자산 입력 전에 배치한다.

```
[사업연도 컨텍스트 확정]  ← by_law_snapshot 자동 생성 완료 확인
   - 법령 버전 + 서식 버전 + ★표준재무제표 양식 버전 + 업종/법인유형 결정
         ▼
[1-1. 재무제표 임포트]
   Excel(.xlsx)/CSV 업로드  또는  ERP API 연동(더존/SAP/Oracle EBS)
   BS / IS / CF / 자본변동표  →  차변·대변 일치 검증 →  fs_lines 적재
         ▼
[1-2. 계정과목 매핑]  (이중 매핑)
   ① 고객사 계정 ↔ 세무 표준계정 (세무조정 집계 기반)
   ② 고객사 계정 ↔ 표준재무제표 항목 (전자신고 제출용)
   학습 자동 적용 (각 축 독립 학습)
         ▼
[1-2B. 표준재무제표 작성·검증]   ★ 신규
   표준대차대조표 / 표준손익계산서 / (표준원가명세서) 자동 집계
   → 결산 재무제표 합계 = 표준재무제표 합계 검증
   → 미매핑/잔여(잡계정) 처리  →  전자신고 표준재무제표 확정
         ▼
[1-3. 자산 및 감가상각]
   자산대장 입력/임포트 → 세법 상각범위액 자동 계산 → B-4 원천
         ▼
[1-4. 거래 명세]
   기부금 / 접대비 / 채권 / 지급이자 → B-2,B-3,B-6,B-9 원천
         ▼
[1-5. 업무용승용차 운행기록]
   업무사용비율 계산 → B-10 원천
         ▼
[1-9. 일관성 검증]
   ① BS 자산=부채+자본  ② 자산대장≤BS  ③ 순이익=잉여금증가
   ④ 거래합계 vs IS 계정  ⑤ ★표준재무제표 총계 일치  ⑥ ★표준재무제표 미매핑 0건
   ERROR 0건 → 단계 2(세무조정) 진행 가능
```

---

## 3. 복식부기 데이터 모델 기초 — 이중 매핑 체계

### 3.1 두 개의 표준 축

```
                         ┌─────────────────────────────┐
                         │   고객사 계정 (account_code) │
                         │   예: 1101 보통예금          │
                         └──────────────┬──────────────┘
              ┌─────────────────────────┼─────────────────────────┐
              ▼ (축 1: 세무)                                       ▼ (축 2: 신고)
   ┌────────────────────────┐                       ┌──────────────────────────────┐
   │ 세무 표준계정           │                       │ 표준재무제표 항목            │
   │ standard_accounts       │                       │ std_fs_items                 │
   │ - tax_relevance 중심    │                       │ - 국세청 표준계정과목코드    │
   │ - 세무조정 집계 단위    │                       │ - 업종별/법인유형별 양식     │
   │ 예: CASH (현금성)       │                       │ 예: 1010 현금및현금성자산    │
   └───────────┬─────────────┘                       └──────────────┬───────────────┘
               ▼                                                     ▼
   세무조정 모듈(B-1~B-17) 집계                          전자신고 표준재무제표(표준BS/IS) 생성
```

- **축 1 (세무 표준계정)**: v1.1에서 정의한 `standard_accounts`. 세무조정에 직접 쓰이는 계정(접대비·기부금·감가상각비 등)을 의미 단위로 표준화.
- **축 2 (표준재무제표 항목)**: 신규. 국세청이 고시한 표준재무상태표·표준손익계산서의 항목 코드 체계. 전자신고 제출용 표준재무제표를 생성하기 위한 분류.
- 두 축은 **다대일(N:1)** 로 각각 매핑되며, 한 고객사 계정이 세무축에서는 `NULL`(세무무관)이어도 표준재무제표축에서는 반드시 한 항목으로 귀속되어야 한다(표준재무제표는 BS/IS 전체를 빠짐없이 분류).

### 3.2 표준계정 마스터 (standard_accounts) — v1.1 유지

```sql
CREATE TABLE standard_accounts (
  id               UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  code             VARCHAR(20) NOT NULL UNIQUE,   -- 예: CASH, AR, PPE_GROSS
  name_ko          VARCHAR(100) NOT NULL,
  fs_type          VARCHAR(10) NOT NULL,           -- BS | IS | CF | CE
  account_class    VARCHAR(20) NOT NULL,           -- ASSET | LIABILITY | EQUITY | REVENUE | EXPENSE | CONTRA
  normal_balance   VARCHAR(6)  NOT NULL,           -- DEBIT | CREDIT
  tax_relevance    VARCHAR(20),                    -- NET_INCOME | REVENUE | PPE | DONATION | ... | NULL
  sub_class        VARCHAR(30),                    -- CURRENT_ASSET | NON_CURRENT_ASSET 등
  is_active        BOOLEAN     NOT NULL DEFAULT true,
  sort_order       INT
);
```

핵심 표준계정 예시는 v1.1 §3.1과 동일(`NET_INCOME`, `REVENUE`, `PPE_GROSS`, `ENTERTAIN_EXP` 등).

### 3.3 fs_lines 의 이중 매핑 컬럼

`fs_lines`는 결산 재무제표(시산표 형태)를 저장하며, 두 매핑 결과를 모두 보유한다.

```
fs_lines.std_account_code   → 세무 표준계정 (축 1, v1.1)
fs_lines.std_fs_item_code   → 표준재무제표 항목 (축 2, 신규)
```

상세 DDL은 §12.2 참조.

---

## 4. 표준재무제표 항목 관리 (신규)

### 4.1 개념과 법적 근거

법인세 신고 시 법인은 결산 재무제표와 별개로 **표준재무제표(표준대차대조표·표준손익계산서·(제조업의 경우) 표준원가명세서·이익잉여금처분계산서)** 를 국세청이 정한 **표준계정과목코드** 체계로 작성해 전자신고 파일에 포함해 제출해야 한다.

| 표준재무제표 종류 | code | 대상 | 비고 |
|------------------|------|------|------|
| 표준대차대조표 | `STD_BS` | 전 법인 | 표준재무상태표 |
| 표준손익계산서 | `STD_IS` | 전 법인 | |
| 표준원가명세서 | `STD_COST` | 제조·건설·광업 등 | 업종 조건부 |
| 표준이익잉여금처분계산서 | `STD_RE` | 전 법인 | |

**핵심 특징**:
- 표준계정과목코드는 **업종(업태)·법인유형에 따라 양식 세트가 다르다** (일반법인용 / 금융보험업용 / 건설업용 등).
- 국세청 개정에 따라 **연도별로 코드/항목이 변경**될 수 있어 **버전 관리**가 필수.
- 결산 재무제표의 계정을 표준계정과목코드로 **빠짐없이 분류·합산**해야 하며, 총계는 결산 재무제표와 일치해야 한다.

### 4.2 표준계정 vs 표준재무제표 항목 차이

| 구분 | 세무 표준계정 (`standard_accounts`) | 표준재무제표 항목 (`std_fs_items`) |
|------|-------------------------------------|------------------------------------|
| 목적 | 내부 세무조정 집계 | 국세청 전자신고 제출 |
| 코드 체계 | 시스템 자체 정의(영문 의미코드) | 국세청 고시 숫자 코드 |
| 범위 | 세무 관련 계정 위주(NULL 허용) | BS/IS 전체 항목 빠짐없이 |
| 버전 | 거의 불변 | 연도별 개정 → 버전 관리 |
| 업종 구분 | 없음 | 업종/법인유형별 양식 세트 |
| 산출물 | B-1~B-17 입력값 | 표준BS/IS XML 레코드 |

### 4.3 마스터 데이터 구조

#### 4.3.1 양식 버전 (std_fs_item_versions)

```sql
CREATE TABLE std_fs_item_versions (
  id              UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  version_code    VARCHAR(40) NOT NULL UNIQUE,    -- 예: NTS-2025-GENERAL
  industry_type   VARCHAR(20) NOT NULL,           -- GENERAL | FINANCE | CONSTRUCTION | NPO 등
  corp_type       VARCHAR(20) NOT NULL DEFAULT 'DOMESTIC', -- DOMESTIC | FOREIGN | CONSOLIDATED
  effective_from  DATE        NOT NULL,            -- 적용 시작 사업연도 개시일
  effective_to    DATE,                            -- NULL = 현행
  nts_doc_ref     VARCHAR(200),                    -- 국세청 고시/별지서식 근거
  status          VARCHAR(15) NOT NULL DEFAULT 'DRAFT', -- DRAFT | REVIEWED | ACTIVE | RETIRED
  xml_schema_ver  VARCHAR(40),                     -- 연계 홈택스 XML 스키마 버전
  created_by      UUID        REFERENCES users(id),
  reviewed_by     UUID        REFERENCES users(id),
  created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
  activated_at    TIMESTAMPTZ,
  UNIQUE (industry_type, corp_type, effective_from)
);
```

#### 4.3.2 표준재무제표 항목 (std_fs_items)

```sql
CREATE TABLE std_fs_items (
  id              UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  version_id      UUID        NOT NULL REFERENCES std_fs_item_versions(id) ON DELETE CASCADE,
  stmt_type       VARCHAR(10) NOT NULL,            -- STD_BS | STD_IS | STD_COST | STD_RE
  item_code       VARCHAR(10) NOT NULL,            -- 국세청 표준계정과목코드 (예: 1010)
  item_name       VARCHAR(150) NOT NULL,           -- 예: 현금및현금성자산
  parent_code     VARCHAR(10),                     -- 상위 집계 항목 (계층 구조)
  level           INT         NOT NULL DEFAULT 1,  -- 1=대분류 ... 4=세목
  account_class   VARCHAR(20),                     -- ASSET | LIABILITY | EQUITY | REVENUE | EXPENSE
  normal_balance  VARCHAR(6),                      -- DEBIT | CREDIT
  is_subtotal     BOOLEAN     NOT NULL DEFAULT false, -- 합계행(직접 매핑 불가, 자식 합산)
  is_required     BOOLEAN     NOT NULL DEFAULT false, -- 필수 보고 항목
  agg_formula     TEXT,                            -- 합계행 산식 (자식 code 목록/식)
  xml_field_id    VARCHAR(40),                     -- 홈택스 XML 필드 ID 매핑
  sort_order      INT,
  is_active       BOOLEAN     NOT NULL DEFAULT true,
  UNIQUE (version_id, stmt_type, item_code)
);

CREATE INDEX idx_std_fs_items_ver ON std_fs_items(version_id, stmt_type, sort_order);
CREATE INDEX idx_std_fs_items_parent ON std_fs_items(version_id, stmt_type, parent_code);
```

**표준대차대조표(STD_BS) 항목 예시** (일반법인용 발췌):

| item_code | item_name | parent_code | level | class | subtotal |
|-----------|-----------|-------------|-------|-------|:---:|
| `1000` | 자산 | – | 1 | ASSET | ✔ |
| `1100` | 유동자산 | 1000 | 2 | ASSET | ✔ |
| `1110` | 당좌자산 | 1100 | 3 | ASSET | ✔ |
| `1010` | 현금및현금성자산 | 1110 | 4 | ASSET | |
| `1020` | 단기투자자산 | 1110 | 4 | ASSET | |
| `1030` | 매출채권 | 1110 | 4 | ASSET | |
| `1031` | 대손충당금(△) | 1110 | 4 | ASSET(CONTRA) | |
| `1200` | 재고자산 | 1100 | 3 | ASSET | ✔ |
| `1500` | 비유동자산 | 1000 | 2 | ASSET | ✔ |
| `1520` | 유형자산 | 1500 | 3 | ASSET | ✔ |
| `2000` | 부채 | – | 1 | LIABILITY | ✔ |
| `3000` | 자본 | – | 1 | EQUITY | ✔ |

**표준손익계산서(STD_IS) 항목 예시** (발췌):

| item_code | item_name | parent_code | level | class | subtotal |
|-----------|-----------|-------------|-------|-------|:---:|
| `4000` | 매출액 | – | 1 | REVENUE | ✔ |
| `4500` | 매출원가 | – | 1 | EXPENSE | ✔ |
| `5000` | 매출총이익 | – | 1 | – | ✔ |
| `5100` | 판매비와관리비 | – | 1 | EXPENSE | ✔ |
| `5130` | 접대비 | 5100 | 2 | EXPENSE | |
| `5140` | 기부금 | 5100 | 2 | EXPENSE | |
| `5170` | 감가상각비 | 5100 | 2 | EXPENSE | |
| `6000` | 영업이익 | – | 1 | – | ✔ |
| `8000` | 법인세비용차감전순이익 | – | 1 | – | ✔ |
| `8500` | 법인세비용 | – | 1 | EXPENSE | |
| `9000` | 당기순이익 | – | 1 | EQUITY | ✔ |

> 위 코드값은 설계 예시이며, 실제 국세청 고시 코드표는 §4.7 임포트 기능으로 일괄 등록·갱신한다.

#### 4.3.3 표준재무제표 매핑 (std_fs_mappings) — 학습형

```sql
CREATE TABLE std_fs_mappings (
  id               UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id        UUID        NOT NULL,
  customer_id      UUID        NOT NULL REFERENCES customers(id),
  version_id       UUID        NOT NULL REFERENCES std_fs_item_versions(id),
  account_code     VARCHAR(20) NOT NULL,           -- 고객사 계정코드
  account_name     VARCHAR(100),
  std_fs_item_code VARCHAR(10) NOT NULL,           -- 표준재무제표 항목 코드 (leaf only)
  is_auto_mapped   BOOLEAN     NOT NULL DEFAULT false,
  usage_count      INT         NOT NULL DEFAULT 1,
  last_used_at     TIMESTAMPTZ,
  created_by       UUID        REFERENCES users(id),
  created_at       TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at       TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (customer_id, version_id, account_code)
);
```

> 매핑 대상은 `is_subtotal = false`(leaf) 항목으로 제한한다. 합계행에는 직접 매핑할 수 없다.

### 4.4 업종별/법인유형별 양식 세트 선택 로직

```
사업연도 by_law_snapshot 생성 시:
  ① 고객사.업종코드(industry_code) + 법인유형(corp_type) 확인
  ② std_fs_item_versions 중 status='ACTIVE' AND effective_from ≤ 사업연도개시일
     AND (effective_to IS NULL OR effective_to ≥ 사업연도종료일)
     AND industry_type 매칭 (업종코드 → industry_type 매핑표 사용)
     AND corp_type 매칭
  ③ 후보가 여럿이면 effective_from 최신 1건 선택
  ④ by_law_snapshot.std_fs_version_id 에 고정 저장
  ⑤ 없으면 GENERAL/DOMESTIC 기본 양식으로 폴백 + 경고 로그
```

업종코드(KSIC) → `industry_type` 매핑은 5-F 코드 관리에서 관리한다(예: 26 제조업→GENERAL, 64~66 금융→FINANCE, 41~42 건설→CONSTRUCTION).

### 4.5 관리자 화면 — 표준재무제표 항목 마스터 관리 (5-F)

설계서 `5-F. 코드 관리 (업종/계정 표준코드)` 하위에 배치한다. 권한: SUPER_ADMIN(RCUD), TENANT_ADMIN(RCU, 활성화 제외).

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 5-F. 코드 관리 > 표준재무제표 항목 마스터                                  │
├──────────────────────────────────────────────────────────────────────────┤
│ 양식 버전: [NTS-2025-GENERAL ▼]  업종: 일반  법인유형: 내국  상태: ACTIVE │
│ [버전 추가] [버전 복제] [코드표 Excel 임포트] [XML필드 매핑] [활성화]      │
│ 표 종류 탭: [표준BS] [표준IS] [표준원가] [이익잉여금처분]                 │
├──────────────────────────────────────────────────────────────────────────┤
│ [표준BS]                                            [항목 추가] [Excel]    │
│ ┌────────┬──────────────────────┬──────┬─────┬──────┬──────┬──────────┐  │
│ │ 코드   │ 항목명               │ 상위 │레벨 │ 구분 │합계행│ XML필드  │  │
│ ├────────┼──────────────────────┼──────┼─────┼──────┼──────┼──────────┤  │
│ │ 1000   │ 자산                 │  –   │  1  │ 자산 │  ✔   │ BS_AST   │  │
│ │  1100  │  유동자산            │ 1000 │  2  │ 자산 │  ✔   │ BS_CUR   │  │
│ │   1110 │   당좌자산           │ 1100 │  3  │ 자산 │  ✔   │ BS_QCK   │  │
│ │    1010│    현금및현금성자산  │ 1110 │  4  │ 자산 │      │ BS_CASH  │  │
│ │    1030│    매출채권          │ 1110 │  4  │ 자산 │      │ BS_AR    │  │
│ │    1031│    대손충당금(△)     │ 1110 │  4  │ 자산 │      │ BS_ALW   │  │
│ │  1200  │  재고자산            │ 1100 │  3  │ 자산 │  ✔   │ BS_INV   │  │
│ │ ...    │                      │      │     │      │      │          │  │
│ └────────┴──────────────────────┴──────┴─────┴──────┴──────┴──────────┘  │
│                                                                          │
│ 무결성: ✔ 계층 일관(모든 parent 존재)  ✔ 합계행 산식 정의 100%          │
│         ⚠ XML필드 미매핑 3건                                             │
│ [무결성 검사 실행]  [버전 비교(전→신)]                                   │
└──────────────────────────────────────────────────────────────────────────┘
```

**관리 기능 상세**:

| 기능 | 설명 |
|------|------|
| 버전 추가/복제 | 신규 연도 양식 생성. 기존 버전 복제 후 차이만 수정(개정 대응) |
| 항목 CRUD | 코드/명/상위/레벨/구분/합계행/XML필드 편집 (트리 드래그로 sort_order·parent 변경) |
| Excel 임포트 | 국세청 코드표 일괄 등록(§4.7) |
| XML 필드 매핑 | 각 leaf·합계행을 홈택스 표준재무제표 XML 필드 ID에 연결 |
| 무결성 검사 | ① 모든 parent_code 존재 ② 합계행 산식이 자식 합과 일치하는 구조 ③ leaf 누락 없는지 ④ XML 필드 매핑 누락 |
| 버전 비교 | 전→신 버전 항목 변동(추가/삭제/코드변경/명칭변경) diff 표시 |
| 활성화 | DRAFT→REVIEWED→ACTIVE→RETIRED 상태 전이(검토자 분리) |

### 4.6 버전 관리 및 사업연도 스냅샷 연계

```
[국세청 표준재무제표 개정 고시]
   ▼
[5-F: 새 버전 생성 (기존 복제)] status=DRAFT
   ▼
[코드표 임포트 / 차이 편집 / XML 필드 매핑]
   ▼
[무결성 검사 통과 → REVIEWED (검토자)]
   ▼
[활성화 → ACTIVE]  (이전 버전 effective_to 자동 설정, RETIRED 예약)
   ▼
[신규 사업연도 생성 시 by_law_snapshot.std_fs_version_id 자동 고정]
   ▼
[기존 미신고 사업연도] → "표준재무제표 양식 개정" 배너 + 재적용 옵션
[신고 완료 사업연도]   → 스냅샷 고정, 당시 버전으로 영구 재현
```

`by_law_snapshot`에 `std_fs_version_id` 컬럼을 추가하여 법령 스냅샷과 동일한 잠금 정책을 적용한다.

### 4.7 임포트 (국세청 코드표 일괄 등록)

```
Excel 템플릿 (시트: STD_BS / STD_IS / STD_COST / STD_RE)
 열: 코드 | 항목명 | 상위코드 | 레벨 | 구분(자산/부채/자본/수익/비용) |
     합계행(Y/N) | 정상잔액(D/C) | 필수(Y/N) | XML필드ID | 정렬순서

처리:
 ① 헤더 검증
 ② 행 파싱 → std_fs_items 후보 생성
 ③ 무결성 사전검증
    - parent_code 가 동일 시트 내 존재
    - 합계행은 leaf 매핑 불가 플래그 일관
    - 코드 중복 없음
 ④ 버전 단위 upsert (version_id + stmt_type + item_code)
 ⑤ 결과 리포트 (신규 N / 갱신 M / 오류 K)
```

---

## 5. 1-1. 재무제표 입력 및 임포트

### 5.1 화면 구성

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 1-1. 재무제표 입력·임포트                    ㈜OOO / 2025               │
├──────────────────────────────────────────────────────────────────────────┤
│ [Excel 업로드]  [표준 템플릿 다운로드]  [ERP 연동(더존/SAP)]  [직접 입력]│
│ 적용 표준재무제표 양식: NTS-2025-GENERAL (일반/내국)  ← by_law_snapshot   │
│ 탭: [BS (재무상태표)] [IS (손익계산서)] [CF (현금흐름표)] [자본변동표]   │
├──────────────────────────────────────────────────────────────────────────┤
│ [BS — 재무상태표]                                                         │
│ ┌─────────────────────────────────────────────────────────────────────┐  │
│ │ 구분        │계정코드│ 계정명         │전기말잔액 │당기말잔액 │세무│표준│ │
│ ├─────────────┼───────┼────────────────┼──────────┼──────────┼───┼────┤ │
│ │ [차변/자산] │1101   │현금및현금성자산 │200,000,000│250,000,000│CASH│1010│ │
│ │             │1110   │매출채권         │300,000,000│350,000,000│AR │1030│ │
│ │             │1115   │대손충당금(△)   │-5,000,000 │-7,000,000 │BAD│1031│ │
│ │             │1120   │재고자산         │100,000,000│120,000,000│INV│1200│ │
│ │  자산 합계  │       │                │1,165,000,000│1,350,500,000│ │ │ │
│ │ [대변/부채] │3101   │매입채무         │200,000,000│220,000,000│ – │2010│ │
│ │ [대변/자본] │4201   │이익잉여금       │385,000,000│535,500,000│ – │3300│ │
│ ├─────────────┴───────┴────────────────┴──────────┴──────────┴───┴────┤ │
│ │ ✔ BS 균형: 1,350,500,000 = 1,350,500,000                            │  │
│ └─────────────────────────────────────────────────────────────────────┘  │
│ 임포트 이력: 2026-05-10 14:32 강수 Excel (4시트, 정상, 세무매핑87%/표준92%)│
└──────────────────────────────────────────────────────────────────────────┘
```

> 그리드에 **세무(축1)·표준(축2)** 매핑 결과 컬럼을 함께 노출하여 임포트 직후 매핑 현황을 한눈에 확인.

### 5.2 임포트 처리 흐름 (세부)

```
① 파일 업로드 (multipart/form-data, ≤ 20MB, .xlsx/.csv)
② 표준 템플릿 헤더 파싱 (열 순서 검증) — 실패 시 ERR_FS_HEADER
③ 행 단위 파싱
   - 계정코드: 문자열 ≤ 20자 (필수)
   - 잔액/발생액: 숫자, 음수 허용(차감계정)
   - 정상잔액방향: D|C (선택, 미입력 시 계정구분으로 추론)
④ 복식부기 검증
   A. BS 차변합계(자산)=대변합계(부채+자본) → 불일치 시 ERR_BS_UNBALANCED (FAIL)
   B. IS 당기순이익 = 수익-비용(법인세 차감 전) → 불일치 시 WARN
   C. IS 당기순이익 = BS 이익잉여금 당기말-전기말-배당 → 불일치 시 WARN
⑤ financial_statements(배치 메타) INSERT (status=PROCESSING)
⑥ fs_lines 행 단위 INSERT (account_code, amount, balance_type, normal_balance)
⑦ 세무 표준계정 자동 매핑 (account_mappings 학습 조회)
⑧ ★표준재무제표 항목 자동 매핑 (std_fs_mappings 학습 조회, by_law_snapshot 양식 기준)
⑨ NET_INCOME 표준계정 자동 감지 → B-1 당기순이익 필드 자동 연동
⑩ 배치 status=SUCCESS, 응답 (성공/오류/세무매핑률/표준매핑률)
```

전 과정은 단일 DB 트랜잭션으로 처리하며, ④-A 실패 시 전체 롤백(배치 status=FAILED 만 남김).

### 5.3 임포트 오류 처리

| 오류 코드 | 유형 | 심각도 | 처리 |
|----------|------|--------|------|
| `ERR_BS_UNBALANCED` | BS 차변≠대변 | ERROR | 임포트 전체 FAIL |
| `ERR_FS_HEADER` | 필수 열 누락/순서 오류 | ERROR | 전체 FAIL |
| `ERR_ROW_NUMBER` | 숫자 형식 오류(특정 행) | ERROR | 해당 행 SKIP, 나머지 적재 |
| `WARN_IS_NI` | IS 당기순이익 불일치 | WARN | 적재 후 경고 |
| `WARN_TAX_UNMAPPED` | 세무 표준계정 미매핑 | WARN | 1-2에서 처리 |
| `WARN_STDFS_UNMAPPED` | 표준재무제표 항목 미매핑 | WARN | 1-2/1-2B에서 처리 |

### 5.4 ERP 연동 (더존/SAP/Oracle EBS)

```
[ERP 연동] → 커넥터 선택 → 인증(저장된 자격증명/OAuth) → 계정과목·시산표 조회
 → 표준 임포트 파이프라인(§5.2 ③~⑩) 재사용
 - 더존: i-CUBE/Smart-A 계정원장 API
 - SAP: GL Account Balances (RFC/OData)
 - Oracle EBS: GL_BALANCES 추출
 매핑: ERP 계정코드 → fs_lines.account_code (커넥터별 필드 매핑 프로파일)
```

> 자격증명 입력·저장은 시스템 정책상 사용자 본인이 직접 등록하며, 본 시스템은 토큰만 보관(평문 저장 금지).

---

## 6. 1-2. 계정과목 매핑 (이중 매핑)

### 6.1 화면 구성 — 탭 분리

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 1-2. 계정과목 매핑                            ㈜OOO / 2025               │
├──────────────────────────────────────────────────────────────────────────┤
│ 탭: [세무 표준계정 매핑]  [표준재무제표 항목 매핑]                        │
├──────────────────────────────────────────────────────────────────────────┤
│ [표준재무제표 항목 매핑]  양식: NTS-2025-GENERAL                          │
│ 자동 매핑률: 92% (139/151)   [미매핑만] [전기매핑 이월] [전체 저장] [Excel]│
│ ┌────────┬──────────┬──────────┬──────────┬──────────────┬─────────────┐ │
│ │고객코드│ 계정명   │ 잔액     │표준코드  │ 표준항목명   │ 매핑방식    │ │
│ ├────────┼──────────┼──────────┼──────────┼──────────────┼─────────────┤ │
│ │1101    │보통예금  │250,000,000│1010     │현금및현금성자산│자동(학습)  │ │
│ │1110    │매출채권  │350,000,000│1030     │매출채권       │자동(학습)  │ │
│ │1115    │대손충당금│-7,000,000│1031     │대손충당금(△) │자동(학습)  │ │
│ │8210    │복리후생비│ ???      │ ???      │(미매핑)       │수동 필요   │ │
│ └────────┴──────────┴──────────┴──────────┴──────────────┴─────────────┘ │
│ ⚠ 미매핑 12건 — 표준재무제표는 전 계정 매핑 필수. 미매핑 시 잡계정 처리됨 │
│ [선택 행 일괄 매핑 ▼]  [유사명 추천 적용]                                 │
└──────────────────────────────────────────────────────────────────────────┘
```

### 6.2 두 축의 매핑 차이 (검증 규칙)

| 항목 | 세무 표준계정 매핑 | 표준재무제표 항목 매핑 |
|------|-------------------|------------------------|
| 미매핑 허용 | 허용(세무무관=NULL) | **불허(전 계정 필수)** — 미매핑은 잡계정 폴백 + WARN |
| 매핑 대상 | 세무 관련 계정 강제 확인 목록(§6.4) | leaf 항목만(합계행 불가) |
| class 충돌 검증 | – | 고객사 계정구분(자산/부채/…) ≠ 표준항목 class → ERROR |
| 학습 키 | customer_id+account_code | customer_id+version_id+account_code |

### 6.3 매핑 자동 학습 로직 (각 축 독립)

```
최초 매핑: 사용자 수동 매핑 → (account_mappings | std_fs_mappings) INSERT
재업로드(차기연도):
  SELECT 매핑 WHERE customer_id=:cid AND account_code=:code [AND version_id=:vid]
  match_rate ≥ 0.9 → 자동 적용(auto_mapped)
  match_rate < 0.9 → 사용자 확인 요청(suggested)
양식 버전 변경 시: 신 버전 항목코드로 자동 재매핑 시도(전 버전 코드 매핑 + 버전 비교 diff 활용)
```

### 6.4 세무 중요 표준계정 강제 확인 목록 (v1.1 유지)

`NET_INCOME / REVENUE / PPE_NET / ENTERTAIN_EXP / DONATION_EXP / INTEREST_EXP / PENSION_PROV / BAD_DEBT_PROV` 미매핑 시 1-9 ERROR. (상세 v1.1 §5.3과 동일)

---

## 7. 1-2B. 표준재무제표 작성·검증 (신규)

### 7.1 화면 구성

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 1-2B. 표준재무제표 작성·검증                  ㈜OOO / 2025               │
├──────────────────────────────────────────────────────────────────────────┤
│ 양식: NTS-2025-GENERAL  탭: [표준BS] [표준IS] [표준원가] [이익잉여금처분] │
│ [자동 집계 실행]  [미매핑 잡계정 보기]  [결산↔표준 차이]  [전자신고 미리]│
├──────────────────────────────────────────────────────────────────────────┤
│ [표준BS — 표준대차대조표]                                                 │
│ ┌────────┬──────────────────────┬──────────────┬──────────────┬────────┐ │
│ │ 코드   │ 표준항목             │ 당기말       │ 전기말       │ 매핑계정│ │
│ ├────────┼──────────────────────┼──────────────┼──────────────┼────────┤ │
│ │ 1000   │ 자산                 │1,350,500,000 │1,165,000,000 │  –     │ │
│ │  1110  │  당좌자산            │  593,000,000 │  495,000,000 │  –     │ │
│ │   1010 │   현금및현금성자산   │  250,000,000 │  200,000,000 │ 1101   │ │
│ │   1030 │   매출채권           │  350,000,000 │  300,000,000 │ 1110   │ │
│ │   1031 │   대손충당금(△)      │   -7,000,000 │   -5,000,000 │ 1115   │ │
│ │  1200  │  재고자산            │  120,000,000 │  100,000,000 │ 1120   │ │
│ │ 2000   │ 부채                 │  315,000,000 │  280,000,000 │  –     │ │
│ │ 3000   │ 자본                 │1,035,500,000 │  885,000,000 │  –     │ │
│ ├────────┴──────────────────────┴──────────────┴──────────────┴────────┤ │
│ │ ✔ 자산총계(1,350,500,000) = 부채+자본(1,350,500,000)                  │ │
│ │ ✔ 표준BS 자산총계 = 결산 BS 자산총계  (차이 0)                        │ │
│ │ ✔ 미매핑 잡계정: 0건                                                  │ │
│ └────────────────────────────────────────────────────────────────────┘ │
│ [표준재무제표 확정]  → 전자신고 표준재무제표 레코드 생성 가능            │
└──────────────────────────────────────────────────────────────────────────┘
```

### 7.2 자동 집계 로직

```
표준재무제표 leaf 값:
  std_fs_value[item_code] = Σ fs_lines.amount
                            WHERE fs_lines.std_fs_item_code = item_code
                              AND balance_type 매칭(BS=ENDING/BEGINNING, IS=MOVEMENT)

합계행(is_subtotal) 값:
  bottom-up 재귀 합산 (level 4 → 1)
  parent_value = Σ child_value   (agg_formula 가 있으면 식 우선)

미매핑 폴백:
  std_fs_item_code IS NULL 인 fs_lines → 구분별 "기타" 잡계정(예: BS 기타자산)
  → WARN_STDFS_UNMAPPED + 잡계정 목록 표시
```

### 7.3 표준재무제표 검증 규칙

| 규칙 코드 | 심각도 | 검증식 |
|----------|--------|--------|
| `CHK_STDBS_BALANCE` | ERROR | 표준BS 자산총계 = 부채총계+자본총계 |
| `CHK_STDBS_VS_FS` | ERROR | 표준BS 자산총계 = 결산 BS 자산총계 (차이 0) |
| `CHK_STDIS_VS_FS` | ERROR | 표준IS 당기순이익 = 결산 IS 당기순이익 |
| `CHK_STDFS_UNMAPPED` | ERROR | 표준재무제표 미매핑(잡계정) 0건 |
| `CHK_STDFS_CLASS` | ERROR | 모든 매핑의 계정구분 ↔ 표준항목 class 일치 |
| `CHK_STDCOST_REQ` | WARN | 제조업이면 표준원가명세서 작성 필요 |
| `CHK_STDFS_SUBTOTAL` | INFO | 합계행 = 자식 합산 일치(반올림 오차 ±1원 허용) |

### 7.4 확정(Freeze) 처리

```
[표준재무제표 확정] 클릭:
  ① CHK_STDBS_* / CHK_STDIS_VS_FS / CHK_STDFS_UNMAPPED ERROR 0건 확인
  ② std_fs_statements(확정 스냅샷) INSERT (값 동결, version_id 포함)
  ③ status=CONFIRMED → 전자신고 단계에서 표준재무제표 XML 생성 가능
  ④ 이후 fs_lines/매핑 변경 시 확정 해제 + 재집계 필요(배너 경고)
```

---

## 8. 1-3. 자산 및 감가상각

v1.1 §6 유지. v1.2에서 다음을 세부 보강한다.

### 8.1 감가상각 자동 계산 로직 (보강)

```
정액법 상각범위액 = (취득원가 - 잔존가액) / 신고내용연수 × (사용월수/12)
정률법 상각범위액 = 기초 미상각잔액 × 상각률
  상각률 = depreciation_lives 고시표(by_law_snapshot 기준) 조회 (이론식 fallback)
즉시상각의제:
  - 취득가액 100만원 미만 → 즉시 전액 손금
  - 내용연수 1년 미만 단기자산 → 즉시 손금
  - 소액수선비: 600만원 이하 OR 미상각잔액 5% 이하 → 수익적지출
한도초과(유보) = max(회사계상상각비 - 세법상각범위액, 0)
시인부족액      = max(세법상각범위액 - 회사계상상각비, 0)  (전기 부인액 추인 한도)
```

### 8.2 자산↔BS·표준BS 교차 검증 (보강)

```
자산대장 유형자산 취득원가 합계 = 결산 BS PPE_GROSS = 표준BS 유형자산(매핑 합)
자산대장 감가상각누계액 합계   = 결산 BS ACCUM_DEPR
무형자산 동일 검증
→ 1-9 / 1-2B 양쪽에서 교차 검증
```

기타 자산대장 화면·자산 상세 모달·전기 이월(carry-forward)은 v1.1 §6.1~6.3과 동일.

---

## 9. 1-4. 거래 명세

v1.1 §7 유지(기부금/접대비/채권/지급이자 4탭, 증빙·기부금 유형 분류, 수입금액 명세, 가지급금 인정이자). v1.2 보강:

### 9.1 거래↔IS↔표준IS 3중 정합

```
거래합계(기부금)  = 결산 IS 기부금계정 = 표준IS 기부금(5140) 매핑 합
거래합계(접대비)  ≈ 결산 IS 접대비계정 = 표준IS 접대비(5130) 매핑 합
거래합계(이자비용)= 결산 IS 이자비용계정 = 표준IS 이자비용 매핑 합
```

### 9.2 거래 → 세무조정 모듈 연동 맵 (보강)

| 탭 | 분류 키 | 연동 모듈 | 산출 |
|----|--------|----------|------|
| 기부금 | SPECIAL/GENERAL/NON_DEDUCTIBLE/POLITICAL | B-2 | 한도초과 10년 이월 |
| 접대비 | CORP_CARD/CASH_RECEIPT/TAX_INVOICE/NO_RECEIPT/CONDOLENCE | B-3 | 무증빙 손금불산입+한도초과 |
| 채권 | AR/미수금/부실채권 | B-6 | 대손충당금 한도초과 유보 |
| 지급이자 | GENERAL/CONSTRUCTION_FUND/NON_BUSINESS_ASSET/UNDERPAID_LOAN | B-9 | 인정이자·손금불산입 |

---

## 10. 1-5. 업무용승용차 운행기록

v1.1 §8 유지(차량별 월별 업무·총 거리, 업무사용비율 자동계산, 미작성 시 70% 적용, B-10 미리보기, `vehicle_mileage_logs` 테이블). 변경 없음.

---

## 11. 1-9. 일관성 검증

### 11.1 검증 규칙 (표준재무제표 규칙 통합)

v1.1 12개 규칙 + 표준재무제표 §7.3 규칙을 통합한 단일 검증 리포트.

```
━━━ [A] 복식부기 기본 ━━━
✔ CHK_BS_BALANCE / CHK_IS_BALANCE / CHK_EQUITY_ROLL
━━━ [B] 자산대장 ↔ BS ━━━
✔ CHK_PPE_COST / CHK_ACCUM_DEPR / CHK_INTANGIBLE
━━━ [C] 거래 ↔ IS ━━━
✔ CHK_DONATION_TXN / ⚠ CHK_ENTERTAIN_TXN / ✔ CHK_INTEREST_TXN
━━━ [D] 세무 표준계정 매핑 ━━━
✔ CHK_STD_ACCT_NET_INCOME / CHK_STD_ACCT_REVENUE / ⚠ CHK_PENSION_PROV_MAP
━━━ [E] 표준재무제표 (신규) ━━━
✔ CHK_STDBS_BALANCE / CHK_STDBS_VS_FS / CHK_STDIS_VS_FS
✔ CHK_STDFS_UNMAPPED(0건) / CHK_STDFS_CLASS / ⚠ CHK_STDCOST_REQ

[다음 단계 (2. 세무조정) ▶]  * ERROR 0건이므로 진행 가능
```

### 11.2 진행 게이트

- **단계 2(세무조정) 진입**: A~D ERROR 0건.
- **전자신고 표준재무제표 생성**: E 그룹 ERROR 0건 + 표준재무제표 확정(CONFIRMED).

---

## 12. 데이터베이스 상세 설계

### 12.1 신규/변경 테이블 요약

| 테이블 | 상태 | 비고 |
|--------|------|------|
| `std_fs_item_versions` | 신규 | 표준재무제표 양식 버전 |
| `std_fs_items` | 신규 | 표준재무제표 항목(국세청 코드) |
| `std_fs_mappings` | 신규 | 고객사계정→표준재무제표 항목 학습 매핑 |
| `std_fs_statements` | 신규 | 표준재무제표 확정 스냅샷 |
| `fs_lines` | 변경 | `std_fs_item_code` 컬럼 추가 |
| `by_law_snapshot` | 변경 | `std_fs_version_id` 컬럼 추가 |
| `financial_statements` | 변경 | `std_map_rate` 컬럼 추가 |

### 12.2 fs_lines (이중 매핑 컬럼 추가)

```sql
ALTER TABLE fs_lines
  ADD COLUMN std_fs_item_code  VARCHAR(10),     -- 표준재무제표 항목 (축 2)
  ADD COLUMN std_fs_auto_mapped BOOLEAN DEFAULT false,
  ADD COLUMN std_fs_confidence  NUMERIC(4,3);

CREATE INDEX idx_fs_lines_stdfs ON fs_lines(business_year_id, std_fs_item_code);
```

(기존 `std_account_code`(축1), `debit_amount`/`credit_amount` 생성열, `balance_type`, `normal_balance` 등은 v1.1 §10.2 유지.)

### 12.3 std_fs_statements (표준재무제표 확정 스냅샷)

```sql
CREATE TABLE std_fs_statements (
  id               UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id        UUID        NOT NULL,
  business_year_id UUID        NOT NULL REFERENCES business_years(id),
  version_id       UUID        NOT NULL REFERENCES std_fs_item_versions(id),
  stmt_type        VARCHAR(10) NOT NULL,          -- STD_BS | STD_IS | STD_COST | STD_RE
  status           VARCHAR(15) NOT NULL DEFAULT 'DRAFT', -- DRAFT | CONFIRMED | SUPERSEDED
  lines            JSONB       NOT NULL,
    -- [{"item_code":"1010","amount_cur":250000000,"amount_pri":200000000}, ...]
  total_check      JSONB,                          -- {"bs_balanced":true,"vs_fs_diff":0}
  confirmed_by     UUID        REFERENCES users(id),
  confirmed_at     TIMESTAMPTZ,
  created_at       TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (business_year_id, stmt_type, status) DEFERRABLE INITIALLY DEFERRED
);
```

### 12.4 by_law_snapshot (양식 버전 편입)

```sql
ALTER TABLE by_law_snapshot
  ADD COLUMN std_fs_version_id UUID REFERENCES std_fs_item_versions(id);
```

### 12.5 세무정보 입력 전체 ERD (v1.2)

```
std_fs_item_versions (양식 버전)
  │ 1:N
  └── std_fs_items (표준재무제표 항목)

financial_statements (임포트 배치)
  │ 1:N
  └── fs_lines ─┬─ std_account_code → standard_accounts   (축1 세무)
                └─ std_fs_item_code → std_fs_items         (축2 신고)

account_mappings  (축1 학습: customer+code → std_account)
std_fs_mappings   (축2 학습: customer+version+code → std_fs_item)

business_years
  ├── by_law_snapshot.std_fs_version_id → std_fs_item_versions
  ├── std_fs_statements (표준재무제표 확정 스냅샷)
  ├── assets ─ vehicle_mileage_logs
  └── transactions
```

---

## 13. API 명세

### 13.1 표준재무제표 마스터 (관리자, 5-F)

| Method | Path | 설명 |
|--------|------|------|
| GET | `/api/admin/std-fs/versions` | 양식 버전 목록(업종/법인유형/상태 필터) |
| POST | `/api/admin/std-fs/versions` | 버전 생성 |
| POST | `/api/admin/std-fs/versions/{id}/clone` | 버전 복제 |
| PUT | `/api/admin/std-fs/versions/{id}/status` | 상태 전이(REVIEWED/ACTIVE/RETIRED) |
| GET | `/api/admin/std-fs/versions/{id}/items?stmtType=STD_BS` | 항목 목록 |
| POST | `/api/admin/std-fs/versions/{id}/items` | 항목 추가 |
| PUT | `/api/admin/std-fs/items/{itemId}` | 항목 수정 |
| DELETE | `/api/admin/std-fs/items/{itemId}` | 항목 삭제 |
| POST | `/api/admin/std-fs/versions/{id}/import` | 코드표 Excel 일괄 등록 |
| GET | `/api/admin/std-fs/versions/{id}/integrity` | 무결성 검사 |
| GET | `/api/admin/std-fs/versions/diff?from={a}&to={b}` | 버전 비교 |

### 13.2 표준재무제표 매핑·작성 (신고작업, 1-2/1-2B)

| Method | Path | 설명 |
|--------|------|------|
| GET | `/api/workspace/{byId}/std-fs/mappings` | 표준재무제표 매핑 목록(미매핑 포함) |
| PUT | `/api/workspace/{byId}/std-fs/mappings/{code}` | 단건 매핑 저장 |
| POST | `/api/workspace/{byId}/std-fs/mappings/bulk` | 일괄 저장 |
| POST | `/api/workspace/{byId}/std-fs/mappings/carry-forward` | 전기 매핑 이월 |
| POST | `/api/workspace/{byId}/std-fs/aggregate` | 표준재무제표 자동 집계 |
| GET | `/api/workspace/{byId}/std-fs/statements?stmtType=STD_BS` | 표준재무제표 조회 |
| GET | `/api/workspace/{byId}/std-fs/validate` | 표준재무제표 검증(§7.3) |
| POST | `/api/workspace/{byId}/std-fs/confirm` | 표준재무제표 확정(Freeze) |

### 13.3 재무제표/매핑/자산/거래/운행/검증

v1.1 §11.1~11.6 유지. 임포트 응답에 `stdMapRate` 추가:

```json
{
  "batchId": "uuid", "status": "SUCCESS", "totalLines": 151, "errorCount": 0,
  "bsBalanced": true, "isNetIncome": 845500000,
  "autoMapRate": 87.4, "stdMapRate": 92.1, "errors": []
}
```

---

## 14. 전자신고 연계 — 표준재무제표 XML

```
[전자신고 파일 생성] (업무흐름 3.10)
   ▼
[사전 검증] 검증룰 + 홈택스 스펙 + ★표준재무제표 확정 여부(CONFIRMED) 확인
   ▼
[표준재무제표 XML 레코드 생성]
   - std_fs_statements(CONFIRMED) 읽기
   - std_fs_items.xml_field_id 매핑 적용 (by_law_snapshot.std_fs_version_id 기준)
   - 표준BS/표준IS/(표준원가)/이익잉여금처분 레코드 직렬화
   - SHA-256 해시 + S3 암호화 저장
   ▼
[전체 전자신고 패키지에 포함 → 제출]
```

| 검증 코드 | 시점 | 내용 |
|----------|------|------|
| `EFILE_STDFS_NOT_CONFIRMED` | 사전검증 | 표준재무제표 미확정 시 차단 |
| `EFILE_STDFS_XML_FIELD_MISSING` | XML 생성 | xml_field_id 미매핑 항목 존재 시 차단 |
| `EFILE_STDFS_TOTAL_MISMATCH` | XML 생성 | 표준재무제표 총계 ≠ 결산 총계 시 차단 |

---

## 15. 역할별 접근 제어

### 15.1 신고작업(세무정보 입력)

| 기능 | SUPER_ADMIN | TENANT_ADMIN | TAX_EXPERT | TAX_REVIEWER | ASSISTANT | VIEWER |
|------|:-:|:-:|:-:|:-:|:-:|:-:|
| 재무제표 임포트/수정 | ● | ● | ● | – | ● | – |
| 세무 표준계정 매핑 | ● | ● | ● | – | ● | – |
| **표준재무제표 항목 매핑** | ● | ● | ● | – | ● | – |
| **표준재무제표 집계/검증** | ● | ● | ● | ● | ● | – |
| **표준재무제표 확정** | ● | ● | ● | – | – | – |
| 자산/거래/운행 입력 | ● | ● | ● | – | ● | – |
| 일관성 검증 실행 | ● | ● | ● | ● | – | – |
| 조회/Excel 다운로드 | ● | ● | ● | ● | ● | ● |

### 15.2 표준재무제표 마스터(5-F 코드 관리)

| 기능 | SUPER_ADMIN | TENANT_ADMIN | 기타 |
|------|:-:|:-:|:-:|
| 양식 버전 조회 | ● | ● | – |
| 항목 CRUD / 임포트 | ● | ●(RCU) | – |
| 버전 활성화(ACTIVE) | ● | – | – |

> 설계서 §834 권한 매트릭스(코드 관리 = SUPER RCUD / TENANT RCU)와 일치.

---

## 16. 프론트엔드 구현 상세

### 16.1 컴포넌트 구조 (신규 추가분)

```
src/pages/Workspace/steps/Step1Info/
├── mapping/
│   ├── MappingTabNav.tsx              ← [세무] [표준재무제표] 탭
│   ├── TaxAccountMapping.tsx          ← 축1 (기존 MappingTable 재배치)
│   └── StdFsMapping.tsx               ← 축2 (신규)
├── stdfs/                             ← 1-2B 표준재무제표 (신규)
│   ├── index.tsx
│   ├── StdFsTabNav.tsx                ← 표준BS/IS/원가/이익잉여금처분
│   ├── StdFsSheet.tsx                 ← 계층 트리 그리드(합계행 자동)
│   ├── StdFsReconcile.tsx            ← 결산↔표준 차이/잡계정
│   └── StdFsConfirmBar.tsx           ← 확정 상태/버튼
└── validate/
    └── ValidationRuleTable.tsx        ← [E] 표준재무제표 그룹 추가

src/pages/Admin/CodeManagement/StdFs/   ← 5-F (신규)
├── VersionList.tsx
├── ItemTreeEditor.tsx                 ← 트리 드래그 편집
├── ItemImportModal.tsx
├── XmlFieldMapper.tsx
└── VersionDiff.tsx
```

### 16.2 표준재무제표 계층 트리 집계 컴포넌트

```typescript
// StdFsSheet.tsx — 합계행은 자식 합산으로 자동 계산, leaf만 매핑값 표시
interface StdFsRow {
  itemCode: string; itemName: string; parentCode?: string;
  level: number; isSubtotal: boolean;
  amountCur: number; amountPri: number;
  mappedAccounts?: string[];          // leaf 에 매핑된 고객사 계정코드
}
// 렌더: level 별 들여쓰기, isSubtotal=true 는 굵게/회색, 미매핑 leaf 는 경고 배지
```

### 16.3 RTK Query 슬라이스 (추가 엔드포인트)

```typescript
// infoApi 에 추가
aggregateStdFs: builder.mutation<StdFsAggResult, string>({
  query: (byId) => ({ url: `/workspace/${byId}/std-fs/aggregate`, method: 'POST' }),
  invalidatesTags: ['StdFs', 'Validate'],
}),
confirmStdFs: builder.mutation<void, string>({
  query: (byId) => ({ url: `/workspace/${byId}/std-fs/confirm`, method: 'POST' }),
  invalidatesTags: ['StdFs', 'Validate'],
}),
updateStdFsMapping: builder.mutation<void, { byId: string; code: string; itemCode: string }>({
  query: ({ byId, code, itemCode }) => ({
    url: `/workspace/${byId}/std-fs/mappings/${code}`, method: 'PUT',
    body: { stdFsItemCode: itemCode },
  }),
  invalidatesTags: ['StdFs', 'Validate'],
}),
```

`tagTypes` 에 `'StdFs'`, `'StdFsMaster'` 추가.

---

## 17. 구현 우선순위 및 검증 항목

### 17.1 단계별 구현 계획 (v1.2)

| Phase | 범위 | 기간 | 비고 |
|-------|------|------|------|
| **Phase A** | 1-1 재무제표 임포트(Excel) + BS/IS 균형 + fs_lines 적재 | 1.5주 | 복식부기 핵심 |
| **Phase B** | 세무 표준계정 마스터 + 1-2 세무 매핑 + 학습 | 1주 | 축1 |
| **Phase B2** | **표준재무제표 항목 마스터(5-F) + 버전/임포트/무결성** | 1.5주 | **신규 축2 기반** |
| **Phase B3** | **1-2 표준재무제표 매핑 + 1-2B 집계·검증·확정** | 1.5주 | **신규** |
| **Phase C** | 1-3 자산대장 + 이월 + 감가상각 자동계산 | 1.5주 | B-4 연동 |
| **Phase D** | 1-4 거래 명세(4탭) + IS·표준IS 3중 검증 | 1주 | B-2·3·6·9 |
| **Phase E** | 1-5 업무용승용차 운행기록 | 0.5주 | B-10 |
| **Phase F** | 1-9 일관성 검증(A~E 통합) | 0.5주 | |
| **Phase G** | **표준재무제표 → 홈택스 XML 연계** | 1주 | 전자신고 연동 |
| **Phase H** | ERP 연동(더존/SAP) + CF/CE 탭 | 1주 | 우선순위 낮음 |

### 17.2 검증 항목 (신규 포함)

| 검증 항목 | 기준 |
|----------|------|
| BS 균형 | 차변≠대변 시 임포트 FAIL |
| 이중 매핑 분리 | 세무축 NULL 허용 / 표준재무제표축 전 계정 매핑 강제 확인 |
| 양식 버전 선택 | 업종/법인유형/시행일 기준 양식 자동 선택 + 스냅샷 고정 |
| 표준재무제표 집계 | leaf 합산 → 합계행 bottom-up, 표준총계 = 결산총계(차이 0) |
| class 충돌 | 고객사 계정구분 ≠ 표준항목 class 시 ERROR |
| 확정/해제 | 확정 후 매핑 변경 시 확정 자동 해제 + 재집계 경고 |
| 버전 비교 | 전→신 양식 diff(추가/삭제/코드변경) 정확성 |
| XML 필드 | 미매핑 항목 존재 시 전자신고 차단 |
| 마스터 무결성 | parent 존재·합계행 산식·코드중복·필수항목 검사 |
| 테넌트 격리 | 타 테넌트 fs_lines/std_fs_mappings 접근 불가 |

---

## 18. 현재 코드베이스 대조 Gap 표 및 실행 순서

> **대조 기준일**: 2026-06-04
> **대조 대상**: 현재 Rust/Axum API, tenant schema/migrations, vanilla JS `frontend/app/screens.js`, Rust/Node 테스트
> **핵심 확인**: `std_fs`, `std-fs`, `std_fs_item`, `std_fs_statement`는 `src/`, `migrations/`, `frontend/`, `tests/` 구현 영역에서 아직 발견되지 않는다. 따라서 v1.2 신규 표준재무제표 축은 DB/API/프론트/테스트 전 영역 신규 구현 대상이다.

### 18.1 총괄 결론

- 기존 세무정보 입력 축은 `tax-data` 임포트/조회/검증, 계정 매핑, 차량 운행기록까지 동작하는 상태다. 근거는 `src/api.rs`의 `/tax-data/*`, `/account-mappings`, `/vehicle-usage-logs` 라우트와 `src/tax_data.rs`의 CSV/XLSX 임포트/검증 로직이다.
- 기존 축은 세무 표준계정 마스터(`standard_accounts`)를 별도 테이블로 두지 않고 `account_mappings.standard_account_code/name` 값을 직접 저장한다. v1.2의 마스터 기반 축1 정합성은 gap이다.
- v1.2 신규 축2인 표준재무제표(`std_fs`)는 마스터, 매핑, 집계, 확정, 전자신고 XML 게이트가 모두 gap이다.
- 구현 순서는 기존 `ws/info:*` 세무정보 입력 기능을 보존하고, `std_fs` 신규 축을 1-2 계정 매핑 직후와 1-3 자산 입력 전에 삽입하는 것으로 확정한다.

### 18.2 DB Gap 표

| 요구사항 | 현재 구현 근거 | 판정 | Gap | 다음 작업 |
|---|---|---:|---|---|
| 기존 세무정보 입력 테이블 | `tenant.rs`가 `import_batches`, `import_errors`, `account_mappings`, `financial_statements`, `fs_lines`, `assets`, `transactions`, `vehicle_usage_logs` 생성 | 부분 완료 | 임포트 중심 구조이며 행 단위 CRUD, 세무 표준계정 마스터 FK 없음 | 기존 테이블은 유지하고 필요한 편집 API만 후속 보강 |
| 세무 표준계정 마스터 `standard_accounts` | 구현 영역 검색 결과 없음 | Gap | 축1 매핑이 마스터 참조가 아닌 자유 코드/명칭 저장 | public 또는 tenant 범위 마스터 테이블/관리 API 설계 확정 |
| 표준재무제표 버전 `std_fs_item_versions` | 구현 영역 검색 결과 없음 | Gap | 업종/법인유형/상태/효력일 버전 관리 없음 | migration + tenant provisioning + seed/backfill |
| 표준재무제표 항목 `std_fs_items` | 구현 영역 검색 결과 없음 | Gap | 계층 항목, leaf 여부, XML field id, 총계식 없음 | 버전별 항목 테이블과 무결성 제약 추가 |
| 표준재무제표 학습 매핑 `std_fs_mappings` | 구현 영역 검색 결과 없음 | Gap | 고객사 계정 -> 표준재무제표 항목 축2 학습 없음 | customer+version+statement+source unique upsert |
| 확정 스냅샷 `std_fs_statements` | 구현 영역 검색 결과 없음 | Gap | 집계값 동결/CONFIRMED 상태 없음 | by_id+version+stmt_type+item_code 스냅샷 테이블 추가 |
| `fs_lines` 이중 매핑 컬럼 | 현재 `standard_account_code/name`만 존재 | Gap | `std_fs_item_code`, `std_fs_auto_mapped`, `std_fs_confidence` 없음 | 컬럼/인덱스 추가 후 임포트 로직 연결 |
| `by_law_snapshot.std_fs_version_id` | 현재 법령/서식/e-file id만 저장 | Gap | 사업연도별 표준재무제표 양식 버전 고정 불가 | 스냅샷 생성/복제/신고 잠금 로직에 버전 포함 |
| `financial_statements.std_map_rate` | 현재 컬럼 없음 | Gap | 표준재무제표 매핑률 저장/표시 불가 | 집계 또는 임포트 후 매핑률 갱신 컬럼 추가 |

### 18.3 API Gap 표

| 요구사항 | 현재 구현 근거 | 판정 | Gap | 다음 작업 |
|---|---|---:|---|---|
| 기존 재무제표/자산/거래 임포트 | `/api/tenants/:tenant_code/business-years/:by_id/tax-data/:data_type/import` 구현 | 완료 | 표준재무제표 자동 매핑은 없음 | std_fs 컬럼 추가 후 임포트 후처리 확장 |
| 기존 세무정보 조회/검증 | `/tax-data/financial-statements`, `/assets`, `/transactions`, `/tax-data/validation` 구현 | 부분 완료 | v1.2 E 그룹 검증, 표준BS/IS 합계 검증 없음 | validation summary와 validation_rules에 std_fs 규칙 통합 |
| 기존 세무 계정 매핑 | `/customers/:customer_id/account-mappings` GET/POST 구현 | 부분 완료 | 축1 마스터 FK/검증 없음, 축2 매핑 분리 없음 | 축1 보강과 축2 별도 API를 분리 구현 |
| 표준재무제표 관리자 API | `/api/admin/std-fs/versions...` 라우트 없음 | Gap | 버전 CRUD, clone, status, import, integrity, diff 전부 없음 | 현재 Axum 라우트 관례에 맞춰 admin std-fs 모듈 추가 |
| 표준재무제표 작업 API | `/api/workspace/{byId}/std-fs...` 라우트 없음 | Gap | mapping, carry-forward, aggregate, statements, validate, confirm 없음 | 현재 tenant-prefixed 관례로 `/api/tenants/:tenant_code/business-years/:by_id/std-fs/...` 구현 |
| 전자신고 std_fs 연계 | efiling precheck/file 생성은 있으나 std_fs 조건 없음 | Gap | `EFILE_STDFS_NOT_CONFIRMED`, XML 레코드 생성, 총계 mismatch 차단 없음 | efiling validation/generate/submit 게이트에 CONFIRMED 스냅샷 조건 추가 |

### 18.4 프론트 Gap 표

| 요구사항 | 현재 구현 근거 | 판정 | Gap | 다음 작업 |
|---|---|---:|---|---|
| 기존 세무정보 입력 화면 | `ws/info:fs`, `mapping`, `assets`, `transactions`, `vehicle`, `consistency` 렌더러 존재 | 완료 | 표준재무제표 축 UI 없음 | 기존 화면을 유지하며 신규 leaf/탭 추가 |
| 이중 매핑 탭 | 현재 계정 매핑 화면은 세무 표준계정 코드/명칭만 입력 | Gap | `[세무] [표준재무제표]` 탭 분리 없음 | `ws/info:mapping` 내부 탭 또는 별도 `ws/info:std-fs-mapping` 추가 |
| 1-2B 표준재무제표 작성/검증 화면 | 구현 영역 검색 결과 없음 | Gap | 계층 트리, 자동 집계, 미매핑, 차이, 확정 버튼 없음 | 신규 `ws/info:std-fs` leaf와 workbench 렌더러 추가 |
| 관리자 5-F 표준재무제표 마스터 | `admin/code:manage`는 범용 코드 관리만 존재 | Gap | std_fs 버전/항목/무결성/비교 전용 화면 없음 | `admin/code:std-fs` 또는 `admin/std-fs:*` leaf 추가 |
| 일관성 검증 E 그룹 | 현재 tax-data/validation 화면은 차대, 매핑, 배치 오류 중심 | Gap | 표준BS/IS, 미매핑, CONFIRMED 게이트 표시 없음 | validation workbench에 E 그룹과 std_fs jump action 추가 |
| 전자신고 화면 게이트 | efiling 화면은 FILED/2FA/권한 중심 | Gap | 표준재무제표 미확정 차단/미리보기 없음 | efiling precheck 결과에 std_fs 상태와 XML 생성 상태 노출 |

### 18.5 테스트 Gap 표

| 요구사항 | 현재 구현 근거 | 판정 | Gap | 다음 작업 |
|---|---|---:|---|---|
| 기존 세무정보 임포트 통합 테스트 | `tests/integration_flow.rs`가 FS/asset/transaction import와 validation 확인 | 완료 | std_fs 컬럼/집계는 검증 안 함 | 기존 테스트에 std_fs 자동 매핑 회귀 추가 |
| 기존 tax-data 프론트 구조 테스트 | `tests/frontend/tax_data_workbench.spec.js`, `workflow_stage_renderers.spec.js` 존재 | 완료 | 신규 std_fs leaf/탭 없음 | std_fs renderer, route spec, i18n, stage contract 테스트 추가 |
| DB migration 테스트 | 현재 std_fs migration 없음 | Gap | schema/unique/FK/tenant isolation 검증 없음 | migration 적용 + tenant provisioning assertion 추가 |
| 관리자 std-fs API 테스트 | 구현 없음 | Gap | version CRUD/status/import/integrity/diff 미검증 | Rust 통합 테스트로 정상/권한/무결성 경로 작성 |
| workspace std-fs API 테스트 | 구현 없음 | Gap | mapping/bulk/carry-forward/aggregate/confirm 미검증 | Rust 통합 테스트로 미매핑, 총계 mismatch, confirm 성공/실패 작성 |
| 전자신고 std_fs 게이트 테스트 | 구현 없음 | Gap | 미확정 차단, XML 총계 mismatch 차단 미검증 | efiling 통합 테스트에 `EFILE_STDFS_*` 케이스 추가 |
| 브라우저 smoke | 기존 phase smoke는 validation/post-amend/admin 일부만 확인 | Gap | std_fs 관리자/작업 화면 시각 검증 없음 | Browser smoke에 admin std-fs, mapping, 1-2B, efiling precheck 추가 |

### 18.6 확정 실행 순서

#### 사용자 업무 흐름 순서

공식 단계 완료 순서는 다음으로 확정한다. 임시 저장이나 파일 선업로드는 허용하더라도, 다음 단계 진행률과 전자신고 게이트는 이 순서를 따른다.

1. 작업 시작: 고객사/사업연도 선택, 법령 버전, 서식 버전, 표준재무제표 양식 버전(`std_fs_version_id`) 스냅샷 고정
2. 1-1 재무제표 입력/임포트: 기존 `ws/info:fs` 유지, 임포트 후 세무 표준계정과 표준재무제표 항목 자동 매핑 시도
3. 1-2 계정과목 매핑: 기존 세무 표준계정 매핑을 먼저 확정하고, 같은 단계에서 표준재무제표 항목 매핑을 별도 탭으로 확정
4. 1-2B 표준재무제표 작성/검증: 신규 `std_fs` 집계, 미매핑 0건, 표준BS/IS 총계 검증, CONFIRMED 스냅샷 생성
5. 1-3 자산 및 감가상각: 기존 자산 입력/임포트 유지, 표준BS 자산총계와 교차 검증 추가
6. 1-4 거래 명세: 기존 거래 입력/임포트 유지, 표준IS 수익/비용 총계와 교차 검증 추가
7. 1-5 업무용승용차 운행기록: 기존 차량 운행기록 입력 유지
8. 1-9 일관성 검증: 기존 A-D 검증에 E 그룹 표준재무제표 검증을 통합
9. 이후 세무조정, 서식 작성, 검증/결재, 출력, 전자신고: 전자신고 파일 생성 전 `std_fs_statements.CONFIRMED`와 E 그룹 ERROR 0건을 필수 게이트로 적용

#### 구현 실행 순서

| 순서 | 범위 | 이유 | 완료 증거 |
|---:|---|---|---|
| 1 | DB foundation: `standard_accounts` 보강 여부 확정, `std_fs_item_versions/items/mappings/statements`, `fs_lines` 컬럼, `by_law_snapshot.std_fs_version_id` | 모든 후속 API/UI가 의존하는 계약 | migration, tenant provisioning, schema 통합 테스트 |
| 2 | 관리자 std-fs 마스터 API/화면: 버전 CRUD, clone/status, item CRUD, import, integrity, diff | 사업연도 스냅샷에 고정할 ACTIVE 버전이 먼저 필요 | admin API 통합 테스트, frontend route/renderer 테스트 |
| 3 | 사업연도/스냅샷 연계: 신규 사업연도 생성과 기존 미신고 사업연도 backfill | 작업 시작 시점에 std_fs 버전이 잠겨야 재현 가능 | business-year/snapshot 통합 테스트 |
| 4 | 재무제표 임포트 확장: std_fs 자동 매핑, template/metadata/std_map_rate | 기존 1-1 기능에 신규 축을 최소 침습으로 연결 | 기존 import 테스트 확장, 미매핑 WARN 테스트 |
| 5 | 1-2 이중 매핑 API/화면: std_fs mapping 단건/bulk/carry-forward | 사용자가 미매핑을 해결해야 1-2B 확정 가능 | workspace API 테스트, frontend tab 테스트 |
| 6 | 1-2B 집계/검증/확정 API/화면 | v1.2 신규 핵심 업무 단계 | aggregate/validate/confirm 테스트, browser smoke |
| 7 | 자산/거래/일관성 검증 통합: 표준BS/IS 교차 검증과 E 그룹 | 기존 세무정보 입력의 완료 판정을 v1.2 기준으로 올림 | validation_rules 통합 테스트 |
| 8 | 전자신고 XML 연계와 제출 게이트 | 표준재무제표 확정 없이는 전자신고 제출 불가 | efiling precheck/generate/submit 통합 테스트 |
| 9 | 전체 회귀: Rust tests, frontend tests, browser smoke, docker 재시작 | DB/API/프론트/테스트 gap 폐쇄 검증 | `cargo test`, `npm run test:frontend`, smoke screenshot, `/health`/`/ready` |

---

## 문서 변경 이력

| 버전 | 일자 | 작성자 | 변경내용 |
|------|------|--------|----------|
| 1.1 | 2026-05-22 | 강수 | 최초 작성 — 1.세무정보입력 복식부기 기반 화면·데이터 구성 |
| 1.2 | 2026-06-02 | 강수 | 표준재무제표 항목 관리 기능 신규 추가(마스터·버전·업종별·이중 매핑·1-2B 작성/검증·전자신고 XML 연계) + 전 구간 세부화(에러코드·트랜잭션 경계·검증규칙·구현 단계) |

---

**문의**: Kang-Soo.Cho@kr.ey.com
**연관 문서**:
- `법인세_세무조정계산서_시스템_설계서.md` (메인 설계서, 5-F 코드 관리)
- `업무흐름.md` (3.5 세무정보 입력, 3.10 전자신고)
- `세부구현_신고_v1.1.md` (직전 버전 — 본 문서가 보강·대체)
- `DB설계.md`
