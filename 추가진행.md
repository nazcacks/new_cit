# 추가 진행 작업 목록

조사일: 2026-05-15  
대상: `D:\NEW_CIT` 법인세 세무조정계산서 시스템

## 1. 조사 요약

현재 프로젝트는 Rust/Axum 백엔드, PostgreSQL 멀티 테넌트 DB, 정적 HTML/CSS/JS 프론트엔드, Docker Compose 개발 환경으로 구성되어 있다. 핵심 데모 흐름은 동작하고 테스트도 통과하지만, 운영 가능한 세무 SaaS로 보기에는 인증/인가, 법령 버전 워크플로, 세무 계산 정확성, 서식/전자신고 매핑, 권한/감사/검증 체계가 아직 크게 부족하다.

검증 결과:

| 항목 | 결과 |
| --- | --- |
| Docker 서비스 | `postgres`, `api` 실행 중 |
| `/health` | 정상 응답 |
| `docker compose run --rm test` | 통과: unit 4개, integration 1개 |
| `docker compose run --rm clippy` | 통과 |
| `node --check frontend/app.js` | 통과 |
| 로컬 `cargo check --all-targets` | 로컬 PATH에 `cargo` 없음 |
| 저장소 상태 | `.git` 없음, `.gitignore` 없음, `target/` 포함 |

현재 개발 DB에는 통합 테스트와 UI 테스트가 만든 테넌트/법령/작업 데이터가 남아 있다. 테스트와 개발 데이터를 분리하거나 초기화 절차를 마련해야 한다.

## 2. 현재 전체 구성

### 2.1 백엔드

| 영역 | 주요 파일 | 현재 상태 |
| --- | --- | --- |
| 진입점/서버 | `src/main.rs`, `src/lib.rs` | Axum 서버, DB 마이그레이션, 워커 실행 |
| 설정/DB | `src/config.rs`, `src/db.rs` | `.env`, `DATABASE_URL`, PostgreSQL pool, sqlx migrator |
| 라우팅/API | `src/api.rs` | 인증, 테넌트, 법령, 계산, 서식, 전자신고, 작업 큐 API |
| 인증 | `src/auth.rs` | 세션 토큰 기반 로그인, 로그아웃, 로그인 이력 |
| 테넌트 | `src/tenant.rs` | public `tenants` + 테넌트별 schema 생성 |
| 법령/계산/서식 | `src/tax.rs` | 법령 버전, 세율, 한도, 스냅샷, 세무조정, FORM3/15/22 |
| 전자신고 | `src/efiling.rs` | Windows-949 고정길이 텍스트 파일 생성 |
| 큐 | `src/queue.rs` | DB 기반 job queue, retry, DLQ |
| 메뉴 | `src/modules.rs` | 설계서 기준 모듈 트리 JSON |
| 정적 파일 | `src/web.rs` | `frontend/` 파일을 `include_str!`로 내장 |

### 2.2 DB/마이그레이션

| 영역 | 주요 파일 | 현재 상태 |
| --- | --- | --- |
| public schema | `migrations/20260514000100_init.sql` | 테넌트, 사용자, 법령, 세율, 한도, 서식, 전자신고 마스터, jobs |
| 인증 세션/데모 계정 | `migrations/20260514000200_auth_sessions.sql` | `auth_sessions`, demo tenant/admin seed |
| tenant schema | `src/tenant.rs` | 고객사, 사업연도, 재무제표, 자산, 조정, 서식 데이터, 전자신고 이력 등 |

설계서 `DB설계.md` 대비 실제 DB는 MVP 수준이다. 권한/메뉴/기능, 사용자별 담당 법인 권한, 서식 검증/필드 참조/마이그레이션, 세액공제/내용연수/중소기업/결손금 규칙, 전자신고 상세 마스터 이력 테이블이 대부분 아직 없다.

### 2.3 프론트엔드

| 영역 | 주요 파일 | 현재 상태 |
| --- | --- | --- |
| HTML | `frontend/index.html` | 로그인, 대시보드, 법령 버전 관리, 데모 실행 화면 |
| JS | `frontend/app.js` | API 호출, 메뉴 렌더링, 데모 플로우, 법령 관리 화면 |
| CSS | `frontend/app.css` | 반응형 레이아웃, 패널/테이블/메뉴 스타일 |

`frontend/app.js`는 단일 파일이 58KB이고, `legacyRender...` 함수군과 새 `render...` 함수군이 중복으로 남아 있다. 기능은 동작하지만 유지보수성이 빠르게 나빠질 수 있는 상태다.

### 2.4 문서/자료

| 문서 | 역할 |
| --- | --- |
| `법인세_세무조정계산서_시스템_설계서.md` | 전체 시스템 설계 |
| `DB설계.md` | 목표 DB 모델 |
| `업무흐름.md` | 업무 흐름과 화면 구성 |
| `법령세율버전관리.md` | 법령/세율 버전 관리 상세 설계 |
| `법령세율관리.md` | 현재 구현 기준 요약 |
| `전자신고_2026년 1차 파일설명서/` | 홈택스 전자신고 원천 문서 |

## 3. 핵심 발견사항

### 3.1 가장 먼저 막아야 할 위험

1. 주요 API가 인증 없이 열려 있다.
   - 근거: `src/api.rs:37-91` 라우트 대부분이 전역 인증 미들웨어 없이 공개되어 있다.
   - 실제 확인: `GET /api/tenants`, `GET /api/jobs`, `GET /api/law-versioning/summary`가 토큰 없이 응답했다.

2. CORS가 전체 허용이다.
   - 근거: `src/api.rs:92` `CorsLayer::permissive()`.

3. 데모 비밀번호가 UI와 seed에 노출되어 있다.
   - 근거: `frontend/index.html:30`, `migrations/20260514000200_auth_sessions.sql:47`, `README.md`.

4. 2FA, 역할, IP 제한, 계정 잠금이 설계만 있고 실제 통제는 약하다.
   - 근거: `src/auth.rs:24-25`, `src/auth.rs:117-118` 역할이 하드코딩되어 있다.
   - `users.use_2fa`, `tenants.allowed_ips`, `pwd_fail_count`, `locked` 컬럼은 있으나 실질 정책 적용이 부족하다.

5. 테스트가 공유 개발 DB를 오염시킨다.
   - 현재 DB에 통합/UI 테스트 테넌트와 `unsupported_job` DLQ가 다수 남아 있다.

### 3.2 설계 대비 큰 구현 공백

1. 권한/메뉴/기능 권한 모델이 없다.
   - 설계: `roles`, `functions`, `menu_functions`, `role_menu_function`, 사용자별 담당 법인 권한.
   - 현재: 로그인 응답에 하드코딩 역할만 반환.

2. 법령 상태 워크플로가 느슨하다.
   - 설계: `DRAFT -> REVIEWED -> APPROVED -> ACTIVE -> RETIRED`.
   - 현재: 상태 값 검증은 있으나 전이 규칙, 승인자 분리, 기간 중복 검증, 활성화 side effect가 없다.

3. 계산 엔진이 법령 파라미터를 충분히 사용하지 않는다.
   - 근거: `src/tax.rs:603-608` 기부금 10%, 접대비 기본한도 등이 코드에 고정되어 있다.
   - `tax_limits`에 저장한 데이터가 실제 계산의 기준값으로 충분히 연결되지 않는다.

4. 계산 재실행 시 조정 행이 계속 누적된다.
   - 근거: `src/tax.rs:710-795` `persist_adjustments`가 항상 insert만 수행한다.
   - 완료 전까지는 재계산 결과와 이력/감사 목적 데이터를 분리해야 한다.

5. 서식 생성은 FORM3/FORM15/FORM22 JSON 중심이다.
   - 설계: 100여 종 서식, 필드 참조, 검증룰, 버전 마이그레이션, PDF 미리보기.
   - 현재: 기본 JSON payload 생성만 있음.

6. 전자신고는 DB 레이아웃을 충분히 쓰지 않는다.
   - 근거: `src/efiling.rs:198-244` 레코드 3개를 코드에서 직접 조립한다.
   - `efile_record_layouts`, `efile_record_fields`가 존재하지만 serializer의 중심 데이터로 쓰이지 않는다.

7. 테넌트 생성이 원자적이지 않다.
   - 근거: `src/tenant.rs:29-64` public tenant insert 후 schema provisioning을 별도 수행한다.
   - schema 생성 실패 시 tenant row만 남을 수 있다.

8. 오류 처리가 문자열 분류에 의존한다.
   - 근거: `src/api.rs:531-543` `map_anyhow`가 error message contains로 HTTP status를 결정한다.

9. 프론트엔드 유지보수성이 낮아지고 있다.
   - 근거: `frontend/app.js:564-1018` legacy 함수군, `frontend/app.js:1134-1659` 새 함수군 중복.

10. 운영/품질 기반이 부족하다.
   - `.git`, `.gitignore`, CI, rust-toolchain 고정, DB 백업/복구 리허설, 운영 로그/메트릭/감사 리포트가 없다.

## 4. 진행 원칙

아래 순서대로 진행한다.

1. 운영 위험을 먼저 줄인다: 인증/인가, CORS, 비밀번호, 테스트 DB 분리.
2. 데이터 모델을 안정화한다: 마이그레이션과 권한/법령/서식/전자신고 핵심 테이블.
3. 계산 정확성을 끌어올린다: 법령 파라미터 기반 계산, 재계산 이력, 검증.
4. 서식과 전자신고를 실제 홈택스 문서 기반으로 만든다.
5. 프론트엔드는 기능 안정화 후 구조를 나눈다.
6. 마지막으로 CI/CD, 운영 모니터링, 백업/복구, 문서 정합성을 완성한다.

## 5. 순서대로 진행할 작업목록

| 순서 | 우선순위 | 작업 | 주요 파일/영역 | 완료 기준 |
| ---: | --- | --- | --- | --- |
| 1 | P0 | 저장소 기준선 만들기 | `.git`, `.gitignore`, `README.md` | Git 저장소 초기화, `target/`, `.env`, Docker volume 산출물 제외 |
| 2 | P0 | 로컬/컨테이너 검증 명령 표준화 | `README.md`, `docker-compose.yml`, `rust-toolchain.toml` | `cargo`, Docker 기준 테스트 명령이 문서와 실제 환경에서 일치 |
| 3 | P0 | 테스트 DB 격리와 정리 전략 수립 | `tests/integration_flow.rs`, `docker-compose.yml` | 테스트마다 독립 DB/schema 사용 또는 종료 후 cleanup |
| 4 | P0 | 전역 인증 미들웨어/추출자 도입 | `src/api.rs`, `src/auth.rs` | `/health`, `/`, 정적 파일, `/api/auth/login` 외 API는 토큰 없으면 401 |
| 5 | P0 | CORS 정책 환경변수화 | `src/api.rs`, `.env.example` | dev/prod 허용 origin 분리, permissive 제거 |
| 6 | P0 | demo 계정/비밀번호 운영 노출 제거 | `frontend/index.html`, migrations, README | 화면 기본 비밀번호 제거, seed는 dev profile 전용화 |
| 7 | P0 | 공개 job/tenant/law API 보호 테스트 추가 | `tests/integration_flow.rs` | 무토큰 접근 401/403 테스트 통과 |
| 8 | P0 | 테넌트 생성 원자성 보장 | `src/tenant.rs` | tenant insert와 schema provisioning 실패 시 불완전 row 방지 |
| 9 | P0 | typed error 체계 정리 | `src/error.rs`, `src/api.rs` | 문자열 contains 기반 HTTP 매핑 제거 |
| 10 | P1 | RBAC/기능 권한 최소 모델 추가 | migrations, `src/auth.rs`, `src/api.rs` | roles/functions/user_role 기반 권한 평가 |
| 11 | P1 | 사용자별 담당 법인 권한 모델 추가 | migrations, tenant/customer API | 담당 고객사 범위 밖 조회/수정 차단 |
| 12 | P1 | 2FA/IP whitelist/계정잠금 실제 적용 | `src/auth.rs`, migrations | `use_2fa`, `allowed_ips`, 실패 횟수 lock 정책 적용 |
| 13 | P1 | 감사 로그 공통 기록 도입 | `src/api.rs`, tenant schema `audit_logs` | 중요 C/U/D, 로그인, 권한 변경, 법령 변경 기록 |
| 14 | P1 | 법령 상태 전이 규칙 구현 | `src/tax.rs`, `src/api.rs` | DRAFT/REVIEWED/APPROVED/ACTIVE/RETIRED 전이 제한 |
| 15 | P1 | 법령/세율/한도 기간 중복 검증 | `src/tax.rs`, migrations | ACTIVE 전환 전 중복 기간 차단 |
| 16 | P1 | 법령 활성화 side effect 구현 | `src/tax.rs` | 기존 활성 버전 종료일 정리, 영향 시뮬레이션 필수화 |
| 17 | P1 | 스냅샷 잠금/해제/재계산 API 추가 | `src/tax.rs`, `src/api.rs` | FILED/locked 상태에서는 기존 스냅샷 고정 |
| 18 | P1 | 계산 결과 재실행 정책 정리 | `src/tax.rs` | 현재 결과 upsert와 이력 보관 분리, 중복 row 누적 방지 |
| 19 | P1 | 세무 계산 파라미터 DB 기반화 | `src/tax.rs`, `tax_limits`, 신규 테이블 | 기부금, 접대비, 결손금, 세액공제 기준이 법령 버전 데이터에서 로드 |
| 20 | P1 | 17개 세무조정 모듈을 단계적으로 확장 | `src/tax.rs`, tenant schema | 설계서 B-1~B-17 중 우선순위별 계산/입력/검증 구현 |
| 21 | P1 | 재무제표/자산/거래 입력 API 구현 | `src/tenant.rs`, 신규 API | 계산 엔진이 수동 숫자 입력이 아닌 원천 데이터와 연결 |
| 22 | P1 | 서식 메타/검증/필드 참조 모델 확장 | migrations, `src/tax.rs` | `form_validations`, `form_field_references`, migration history 구현 |
| 23 | P1 | FORM3/15/22 외 서식 확장 전략 확정 | `src/tax.rs`, 설계서 | 핵심 별지 우선순위와 생성 규칙 문서화 |
| 24 | P1 | PDF/미리보기 출력 경로 설계 | 신규 renderer | 서식 JSON에서 화면/PDF 출력까지 연결 |
| 25 | P1 | 전자신고 serializer를 DB 레이아웃 기반으로 전환 | `src/efiling.rs`, efile tables | 레코드/필드 정의를 DB에서 읽어 byte 길이 검증 |
| 26 | P1 | 전자신고 사전 검증 구현 | `src/efiling.rs`, `efiling_validation` | 사업자번호, 필수값, byte 길이, 합계 검증 후 오류 저장 |
| 27 | P1 | 홈택스 2026 문서 기반 레이아웃 적재 | `전자신고_2026년 1차 파일설명서/`, migrations | 파일설명서의 주요 레코드/필드가 seed 또는 import로 DB 반영 |
| 28 | P2 | job queue 신뢰성 강화 | `src/queue.rs` | stale running recovery, idempotency key, job type registry |
| 29 | P2 | 프론트엔드 중복 코드 제거 | `frontend/app.js` | legacy 함수군 제거, 기능 회귀 없음 |
| 30 | P2 | 프론트엔드 모듈화 | `frontend/` | API client, auth, law screens, demo flow 파일 분리 |
| 31 | P2 | 권한 기반 UI 표시/버튼 제어 | `frontend/app.js`, auth API | 메뉴와 버튼이 effective permissions에 따라 표시 |
| 32 | P2 | 업무 흐름 화면 확장 | `업무흐름.md`, `frontend/` | 고객사, 담당자 배정, 데이터 입력, 검증, 승인 화면 흐름 구현 |
| 33 | P2 | 입력값 검증/오류 UX 정리 | frontend, API errors | 필드 단위 오류와 서버 오류가 사용자에게 정확히 표시 |
| 34 | P2 | 운영 관측성 추가 | tracing, metrics, audit | request id, structured logs, health/readiness, job metrics |
| 35 | P3 | CI/CD 구성 | `.github/workflows` 또는 동등 도구 | fmt/check/test/clippy/docker build 자동 실행 |
| 36 | P3 | 마이그레이션/백업 운영 절차 | migrations, 문서 | rollback, backup, restore rehearsal 절차 문서화 |
| 37 | P3 | 프로덕션 Docker hardening | `Dockerfile`, `docker-compose.yml` | non-root user, healthcheck, secrets, resource limits |
| 38 | P3 | 설계서와 README 정합성 정리 | 문서 전체 | 구현된 것/미구현/로드맵이 문서에서 일관 |
| 39 | P3 | 부하/보안 테스트 계획 수립 | tests, docs | 멀티 테넌트 누수, 권한 우회, 동시 job 처리 테스트 |
| 40 | P3 | 배포 전 운영 체크리스트 작성 | 신규 문서 | 환경변수, 백업, 인증, 로그, 계정, 데이터 초기화 점검표 |

## 6. 다음 작업의 권장 세부 순서

### 6.1 1차 스프린트: 운영 위험 차단

1. `.gitignore` 추가: `target/`, `.env`, 임시 파일, Docker/IDE 산출물 제외.
2. 전역 인증 미들웨어 적용.
3. 무토큰 접근 차단 테스트 작성.
4. `CorsLayer::permissive()` 제거 및 `ALLOWED_ORIGINS` 환경변수 추가.
5. 화면의 기본 비밀번호 제거.
6. 통합 테스트가 독립 DB/schema를 사용하도록 변경.

완료 기준:

- `docker compose run --rm test` 통과.
- `docker compose run --rm clippy` 통과.
- 토큰 없이 `/api/tenants`, `/api/jobs`, `/api/tax-laws` 접근 시 401 또는 403.

### 6.2 2차 스프린트: 권한/감사 기반

1. `roles`, `functions`, `user_role` 최소 테이블 추가.
2. 로그인 응답의 하드코딩 역할 제거.
3. API별 최소 권한 매핑 추가.
4. `audit_logs` 기록 헬퍼 추가.
5. 로그인 이력에 IP/User-Agent 저장.
6. 실패 로그인 lock 정책 적용.

완료 기준:

- 역할 없는 사용자는 보호 API 접근 불가.
- 권한 변경/법령 변경/전자신고 파일 생성이 감사 로그에 남음.

### 6.3 3차 스프린트: 법령 버전 안정화

1. 상태 전이 규칙 구현.
2. DRAFT 외 상세 항목 수정 차단.
3. 세율/한도 기간 중복 검증.
4. ACTIVE 전환 시 영향 시뮬레이션 결과 확인.
5. 스냅샷 잠금/해제/재계산 API 추가.

완료 기준:

- `법령세율버전관리.md`의 18.1~18.2 수용 기준이 테스트로 검증됨.

### 6.4 4차 스프린트: 계산 정확성

1. `tax_limits`와 신규 정책 테이블을 계산 엔진 입력으로 사용.
2. 재계산 시 현재 결과와 이력을 분리.
3. 기부금, 접대비, 감가상각, 결손금, 세액공제 경계값 테스트 추가.
4. 사업연도 상태가 `FILED` 또는 스냅샷 잠금이면 재계산 제한.

완료 기준:

- 동일 입력 반복 계산 시 결과 row가 무한 누적되지 않음.
- 법령 버전별 회귀 테스트가 통과.

### 6.5 5차 스프린트: 서식/전자신고

1. 서식 필드/검증/참조 테이블 추가.
2. FORM3/15/22를 template 기반 생성으로 전환.
3. 전자신고 레코드/필드 정의를 DB에서 읽도록 변경.
4. 홈택스 2026 파일설명서의 핵심 레코드 seed/import.
5. `efiling_validation` 오류 저장과 다운로드 차단 정책 구현.

완료 기준:

- 전자신고 파일 생성 전에 필수 검증이 실행됨.
- 생성 파일 레코드 길이와 encoding byte 길이가 DB 레이아웃과 일치.

### 6.6 6차 스프린트: 화면/운영 품질

1. `frontend/app.js` legacy 함수 제거.
2. JS를 기능별 파일로 분리하거나 최소한 섹션별 모듈로 정리.
3. 권한 기반 메뉴/버튼 표시.
4. 검증 결과, 승인, 출력, 전자신고 제출 화면 보강.
5. CI/CD, 백업/복구, 운영 체크리스트 문서화.

완료 기준:

- `node --check frontend/app.js` 또는 분리된 JS 전체 문법 검사 통과.
- 핵심 화면 플로우가 통합 테스트 또는 브라우저 테스트로 검증됨.

## 7. 작업 전 확인 명령

```powershell
docker compose ps
Invoke-RestMethod http://localhost:8080/health
docker compose run --rm test
docker compose run --rm clippy
node --check frontend\app.js
```

로컬 Rust 검증을 쓰려면 별도 설치 또는 PATH 설정 후 다음을 사용한다.

```powershell
$env:DATABASE_URL = "postgres://cit:cit@localhost:5432/cit"
cargo test --all --all-targets
cargo clippy --all-targets --all-features -- -D warnings
```

## 8. 최종 판단

이 프로젝트는 데모 가능한 MVP 뼈대는 이미 갖췄다. 다음 단계의 핵심은 기능을 더 붙이는 것보다 먼저 보안 경계, 권한 모델, 테스트 격리, 법령 버전 무결성, 계산 재현성을 잡는 것이다. 이 기반을 먼저 세우면 이후 서식 100여 종과 홈택스 전자신고 상세 매핑을 확장해도 구조가 버틸 수 있다.

## Multi-Tenant 테넌트-고객사 관계 보완 (2026-05-15)

- 대상 사용자 조직은 `회계법인`, `세무법인`, `세무사사무소`이며, 시스템에서는 각각 독립 테넌트로 관리한다.
- 관계 모델은 `테넌트 1 : N 고객사`이다. 하나의 테넌트는 여러 고객사를 관리하고, 각 고객사는 반드시 하나의 테넌트에만 소속된다.
- UI 기준으로 `prototype/index.html`의 `5-0 테넌트 관리` 화면에서 테넌트 유형, 계약, 상태, 사용자 수, 고객사 수, 소속 고객사 샘플을 확인한다.
- `5-A 고객사 관리` 화면은 고객사 목록에 소속 테넌트 컬럼과 테넌트 필터를 제공하고, 신규 등록/편집 시 소속 테넌트를 선택한다.
- 사용자 권한은 테넌트 범위 안에서 적용되며, 특정 고객사 접근은 `5-E 담당 법인 권한`에서 사용자-고객사 단위로 추가 제한한다.
- DB/도메인 설계 시 고객사 테이블은 `tenant_id` 또는 `tenant_code` 외래키를 가져야 하며, 고객사 코드는 `tenant_id + customer_code` 범위에서 유일해야 한다.
## 사용자-테넌트-고객사 관리 보완 (2026-05-15)

- 사용자는 반드시 하나의 소속 테넌트(`tenant_id`)를 가진다. 테넌트는 회계법인, 세무법인, 세무사사무소 단위 조직이다.
- 사용자의 고객사 접근 범위는 `user_customer_access` 또는 동등한 매핑으로 관리하며, 한 사용자는 소속 테넌트 안의 여러 고객사에 접근할 수 있다.
- `prototype/index.html`의 `5-B 사용자 관리` 화면은 테넌트 필터, 고객사 필터, 소속 테넌트 컬럼, 고객사 권한 요약을 제공한다.
- 사용자 등록/편집 시 소속 테넌트를 먼저 선택하고, 선택한 테넌트에 속한 고객사만 접근 범위로 선택할 수 있다.
- 역할별 기능 권한은 `5-C 역할 / 권한 매트릭스`에서 관리하고, 특정 고객사 담당 등급 또는 차단 같은 예외는 `5-E 담당 법인 권한`에서 관리한다.
- 권한 판정 순서는 `테넌트 소속 확인 -> 고객사 접근 범위 확인 -> 역할/기능 권한 확인 -> 고객사별 예외 권한 확인`을 기본 원칙으로 한다.
## 고객사별 대상 업무 범위 보완 (2026-05-15)

- 사용자 권한은 `tenant_id + customer_id + work_scope` 조합으로 판정한다. 같은 테넌트와 고객사라도 사용자마다 허용 업무가 다를 수 있다.
- 대상 업무 코드는 `INFO`, `ADJUST`, `FORM`, `VALIDATE`, `APPROVE`, `PRINT`, `EFILE`, `POST`를 기본값으로 사용한다.
- 권한 저장은 `user_customer_work_scope` 테이블을 별도로 두거나, `user_customer_access`에 `work_scopes` JSON/배열 컬럼을 추가하는 방식으로 구현한다.
- `prototype/index.html`의 `5-B 사용자 관리` 화면은 대상 업무 필터와 고객사별 대상 업무 체크리스트를 제공한다.
- 사용자 등록/편집 시 소속 테넌트를 선택한 뒤, 해당 테넌트의 고객사를 선택하고, 선택된 고객사마다 허용 업무 범위를 별도로 지정한다.
- 업무 실행 전 권한 판정 순서는 `테넌트 소속 확인 -> 고객사 접근 확인 -> 고객사별 대상 업무 확인 -> 역할/기능 권한 확인 -> 예외 권한 확인`을 따른다.
- 예: 같은 `EY 회계법인 / ㈜OOO 제조` 고객사라도 A 사용자는 `ADJUST`, `FORM`, `VALIDATE`만 가능하고, B 사용자는 `APPROVE`, `PRINT`만 가능하게 분리할 수 있다.
## 테넌트별 고객사 대상 업무 구분 보완 (2026-05-15)

- 고객사별 대상 업무 범위와 사용자별 부여 업무 범위를 분리했다.
- 사용자에게 부여 가능한 업무는 해당 고객사의 대상 업무 범위로 제한한다.
- UI와 API는 고객사 대상 업무와 사용자 부여 업무를 함께 보여주어, 테넌트 내 고객사별 업무 차이를 확인할 수 있게 한다.

## Phase 7 세무정보 입력 진행 (2026-05-15)

- 재무제표, 자산대장, 거래 명세 CSV/XLSX 업로드 API와 화면을 추가했다.
- 임포트 배치/오류 이력, 차변/대변 검증, 계정 매핑 학습/재사용, 업무용차 자동분류를 구현했다.
- `tax-data` 메뉴는 하위 메뉴별 독립 화면으로 열리며 실제 API와 연결된다.

## Phase 8 세무조정 엔진 진행 (2026-05-15)

- B-1 소득금액조정 API, 조정 항목 저장, 유보 자동 생성을 추가했다.
- 재무제표 `NET_INCOME`을 결산서상 당기순이익으로 자동 인식한다.
- `adjustment` 메뉴에 적용 법령 배너와 B-1 입력 그리드를 분리 화면으로 추가했다.

## Phase 9 자산 기반 세무조정 진행 (2026-05-15)

- B-4/B-5/B-6/B-10 자산 기반 조정 API와 화면을 추가했다.
- 차량 월별 운행기록 저장과 업무사용비율 계산을 구현했다.
- 감가상각은 자산대장과 Phase 4 기준내용연수 데이터를 연동한다.
