# 신고작업 — 1. 세무정보 입력 세부 구현 계획서 v1.1

> **문서 버전**: v1.1
> **작성일**: 2026-05-22
> **작성자**: 강수 (Kang-Soo.Cho@kr.ey.com)
> **연관 문서**: `법인세_세무조정계산서_시스템_설계서.md`, `업무흐름.md`, `세부구현_신고_v1.0.md`, `DB설계.md`
> **목적**: ② 신고 작업 > 1. 세무정보 입력의 전체 서브 단계를 업무 흐름에 맞춰 재구성하고, **복식부기(Double-Entry Bookkeeping)** 원리에 맞춘 데이터 구성을 구체화한다.

---

## 목차

1. [세무정보 입력 설계 원칙](#1-세무정보-입력-설계-원칙)
2. [서브 단계 전체 흐름](#2-서브-단계-전체-흐름)
3. [복식부기 데이터 모델 기초](#3-복식부기-데이터-모델-기초)
4. [1-1. 재무제표 입력 및 임포트](#4-1-1-재무제표-입력-및-임포트)
5. [1-2. 계정과목 매핑](#5-1-2-계정과목-매핑)
6. [1-3. 자산 및 감가상각](#6-1-3-자산-및-감가상각)
7. [1-4. 거래 명세](#7-1-4-거래-명세)
8. [1-5. 업무용승용차 운행기록](#8-1-5-업무용승용차-운행기록)
9. [1-9. 일관성 검증](#9-1-9-일관성-검증)
10. [데이터베이스 상세 설계](#10-데이터베이스-상세-설계)
11. [API 명세](#11-api-명세)
12. [역할별 접근 제어](#12-역할별-접근-제어)
13. [프론트엔드 구현 상세](#13-프론트엔드-구현-상세)
14. [구현 우선순위 및 검증 항목](#14-구현-우선순위-및-검증-항목)

---

## 1. 세무정보 입력 설계 원칙

### 1.1 복식부기 원칙과 세무정보 입력의 관계

법인세 세무조정은 **기업회계기준(K-IFRS / K-GAAP)** 으로 작성된 재무제표를 출발점으로 한다. 재무제표는 복식부기 원리 위에 존재하므로, 시스템은 다음 불변 조건을 항상 유지해야 한다.

| 복식부기 불변 조건 | 시스템 검증 시점 |
|-----------------|----------------|
| 모든 계정의 차변 합계 = 대변 합계 | 임포트 즉시, 1-9 일관성 검증 |
| BS: 자산 합계 = 부채 합계 + 자본 합계 | 임포트 즉시 |
| IS: 당기순이익 = 수익 합계 - 비용 합계 | 임포트 즉시 |
| BS 기말자본 = BS 기초자본 + 당기순이익 + 자본 변동 | 1-9 일관성 검증 |
| 자산대장 취득원가 합계 ≤ BS 유형·무형자산 잔액 | 1-9 일관성 검증 |

### 1.2 핵심 설계 원칙

| 원칙 | 내용 |
|------|------|
| 표준계정 중심 | 고객사마다 다른 계정과목을 **표준계정(Standard Account Code)** 으로 매핑한 뒤, 세무조정은 표준계정 기준으로 처리 |
| 잔액(Balance) vs 발생(Movement) 분리 | BS는 기말 잔액, IS·CF는 당기 발생액. 두 성격을 `fs_lines.balance_type`으로 구분 |
| 차변/대변 방향 명시 | 계정별 정상잔액 방향(`normal_balance`: DEBIT / CREDIT)을 표준계정 마스터에 정의하여 부호 오류 방지 |
| 전기 이월 자동화 | 자산대장·이월결손금·유보 잔액은 전기 사업연도 데이터에서 자동 복사, 사용자는 차이 부분만 수정 |
| 원천 추적성 | 세무조정 계산에 사용된 모든 숫자는 `fs_lines.id`, `asset.id`, `transaction.id` 등 원천 레코드 PK로 역추적 가능 |

---

## 2. 서브 단계 전체 흐름

```
[사업연도 컨텍스트 확정]  ← by_law_snapshot 자동 생성 완료 확인
         ▼
[1-1. 재무제표 임포트]
   Excel(.xlsx)/CSV 업로드  또는  ERP API 연동(더존/SAP/Oracle EBS)
   BS / IS / CF / 자본변동표  →  차변·대변 일치 검증 →  fs_lines 적재
         ▼
[1-2. 계정과목 매핑]
   고객사 계정 ↔ 표준계정 매핑 (학습 자동 적용)
   매핑 결과 → 세무조정 모듈의 계정별 집계 기반
         ▼
[1-3. 자산 및 감가상각]
   자산대장 입력/임포트  →  취득원가/내용연수/상각방법 기록
   세법 상각범위액 자동 계산  →  B-4 감가상각 조정 원천
         ▼
[1-4. 거래 명세]
   기부금 / 접대비(증빙유형 포함) / 채권 / 지급이자 거래 입력
   B-2, B-3, B-6, B-9 조정 원천
         ▼
[1-5. 업무용승용차 운행기록]
   차량별 월별 업무·총 거리 입력  →  업무사용비율 계산
   B-10 조정 원천
         ▼
[1-9. 일관성 검증]
   ① BS 자산 = 부채 + 자본
   ② 자산대장 잔액 ≤ BS 유형·무형자산 잔액
   ③ IS 당기순이익 = BS 이익잉여금 증가
   ④ 접대비 거래 합계 vs BS 접대비 계정 잔액
   ⑤ 기부금 거래 합계 vs IS 기부금 계정
   오류(ERROR) 0건 → 단계 2(세무조정) 진행 가능
```

---

## 3. 복식부기 데이터 모델 기초

### 3.1 표준계정 마스터 (standard_accounts)

모든 고객사 계정은 이 표준계정 마스터로 수렴된다.

```sql
CREATE TABLE standard_accounts (
  id               UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  code             VARCHAR(20) NOT NULL UNIQUE,   -- 예: CASH, AR, PPE_GROSS
  name_ko          VARCHAR(100) NOT NULL,
  fs_type          VARCHAR(10) NOT NULL,           -- BS | IS | CF | CE (자본변동)
  account_class    VARCHAR(20) NOT NULL,
    -- ASSET | LIABILITY | EQUITY | REVENUE | EXPENSE | CONTRA
  normal_balance   VARCHAR(6) NOT NULL,            -- DEBIT | CREDIT
    -- 자산·비용: DEBIT 정상, 부채·자본·수익: CREDIT 정상
  tax_relevance    VARCHAR(20),
    -- NET_INCOME | REVENUE | PPE | INTANGIBLE | INVESTMENT
    -- DONATION | ENTERTAINMENT | INTEREST_EXP | AR | PENSION | INVENTORY
    -- NULL = 세무조정 직접 연관 없음
  sub_class        VARCHAR(30),
    -- CURRENT_ASSET | NON_CURRENT_ASSET | CURRENT_LIABILITY 등
  is_active        BOOLEAN NOT NULL DEFAULT true,
  sort_order       INT
);
```

**핵심 표준계정 예시**:

| code | name_ko | fs_type | account_class | normal_balance | tax_relevance |
|------|---------|---------|---------------|---------------|---------------|
| `NET_INCOME` | 당기순이익 | IS | EQUITY | CREDIT | NET_INCOME |
| `REVENUE` | 매출액(수입금액) | IS | REVENUE | CREDIT | REVENUE |
| `PPE_GROSS` | 유형자산(총취득원가) | BS | ASSET | DEBIT | PPE |
| `ACCUM_DEPR` | 감가상각누계액 | BS | CONTRA | CREDIT | PPE |
| `PPE_NET` | 유형자산(장부가) | BS | ASSET | DEBIT | PPE |
| `INTANGIBLE` | 무형자산 | BS | ASSET | DEBIT | INTANGIBLE |
| `AR` | 매출채권 | BS | ASSET | DEBIT | AR |
| `DONATION_EXP` | 기부금 | IS | EXPENSE | DEBIT | DONATION |
| `ENTERTAIN_EXP` | 접대비 | IS | EXPENSE | DEBIT | ENTERTAINMENT |
| `INTEREST_EXP` | 이자비용 | IS | EXPENSE | DEBIT | INTEREST_EXP |
| `PENSION_PROV` | 퇴직급여충당금 | BS | LIABILITY | CREDIT | PENSION |
| `BAD_DEBT_PROV` | 대손충당금 | BS | CONTRA | CREDIT | AR |
| `INVENTORY` | 재고자산 | BS | ASSET | DEBIT | INVENTORY |
| `RETAINED_EARN` | 이익잉여금 | BS | EQUITY | CREDIT | NULL |
| `CAPITAL_STOCK` | 자본금 | BS | EQUITY | CREDIT | NULL |

### 3.2 fs_lines (재무제표 행) 구조 원칙

재무제표의 모든 행은 **계정코드 + 금액 + 차변/대변 방향** 세 요소로 구성된다.

```
차변(DEBIT)에 기입하는 경우:
  - 자산 증가 / 자산 취득
  - 비용 발생
  - 부채 감소 / 자본 감소

대변(CREDIT)에 기입하는 경우:
  - 부채 증가 / 자본 증가
  - 수익 발생
  - 자산 감소
```

`fs_lines`는 **결산 재무제표(시산표 형태)**를 저장하므로, 각 계정의 기말 잔액을 정상잔액 방향으로 입력한다.

---

## 4. 1-1. 재무제표 입력 및 임포트

### 4.1 화면 구성

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 1-1. 재무제표 입력·임포트                    ㈜OOO / 2025               │
├──────────────────────────────────────────────────────────────────────────┤
│ [Excel 업로드]  [표준 템플릿 다운로드]  [ERP 연동(더존/SAP)]  [직접 입력]│
│                                                                          │
│ 탭: [BS (재무상태표)] [IS (손익계산서)] [CF (현금흐름표)] [자본변동표]   │
├──────────────────────────────────────────────────────────────────────────┤
│ [BS — 재무상태표]                                                         │
│                                                                          │
│ ┌─────────────────────────────────────────────────────────────────────┐  │
│ │ 구분         │ 계정코드    │ 계정명          │ 전기말잔액 │ 당기말잔액 │  │
│ ├──────────────┼────────────┼─────────────────┼────────────┼────────────┤  │
│ │ [차변/자산]   │            │                  │            │            │  │
│ │   유동자산   │ 1101       │ 현금및현금성자산  │ 200,000,000│ 250,000,000│  │
│ │              │ 1102       │ 단기금융상품      │  50,000,000│  80,000,000│  │
│ │              │ 1110       │ 매출채권          │ 300,000,000│ 350,000,000│  │
│ │              │ 1115       │ 대손충당금(△)    │ -5,000,000 │ -7,000,000 │  │
│ │              │ 1120       │ 재고자산          │ 100,000,000│ 120,000,000│  │
│ │   비유동자산  │ 2101       │ 토지             │ 200,000,000│ 200,000,000│  │
│ │              │ 2102       │ 건물             │ 300,000,000│ 300,000,000│  │
│ │              │ 2103       │ 감가상각누계액(△)│ -50,000,000│ -62,500,000│  │
│ │              │ 2110       │ 기계장치          │ 100,000,000│ 115,000,000│  │
│ │              │ 2111       │ 감가상각누계액(△)│ -30,000,000│ -45,000,000│  │
│ │   자산 합계  │            │                  │1,165,000,000│1,350,500,000│ │
│ ├──────────────┼────────────┼─────────────────┼────────────┼────────────┤  │
│ │ [대변/부채]   │            │                  │            │            │  │
│ │   유동부채   │ 3101       │ 매입채무          │ 200,000,000│ 220,000,000│  │
│ │   비유동부채 │ 3201       │ 퇴직급여충당금    │  80,000,000│  95,000,000│  │
│ │   부채 합계  │            │                  │  280,000,000│ 315,000,000│ │
│ ├──────────────┼────────────┼─────────────────┼────────────┼────────────┤  │
│ │ [대변/자본]   │            │                  │            │            │  │
│ │              │ 4101       │ 자본금            │ 500,000,000│ 500,000,000│  │
│ │              │ 4201       │ 이익잉여금        │ 385,000,000│ 535,500,000│  │
│ │   자본 합계  │            │                  │  885,000,000│1,035,500,000│ │
│ ├──────────────┼────────────┼─────────────────┼────────────┼────────────┤  │
│ │ 부채+자본 합계│           │                  │1,165,000,000│1,350,500,000│ │
│ ├──────────────┴────────────┴─────────────────┴────────────┴────────────┤  │
│ │ ✔ BS 균형 (자산 = 부채+자본): 1,350,500,000 = 1,350,500,000          │  │
│ └─────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│ 임포트 이력:  2026-05-10 14:32  강수  Excel 업로드  (4개 시트, 정상)     │
│ [이전 임포트 삭제 및 재업로드]  [행 단위 오류 보기]                       │
└──────────────────────────────────────────────────────────────────────────┘
```

### 4.2 IS (손익계산서) 탭

```
┌─────────────────────────────────────────────────────────────────────────┐
│ IS — 손익계산서 (당기: 2025.01.01 ~ 2025.12.31)                        │
├──────────┬────────────┬─────────────────────────┬────────────────────┤
│ 구분      │ 계정코드    │ 계정명                  │ 당기발생액         │
├──────────┼────────────┼─────────────────────────┼────────────────────┤
│ [수익]   │ 5101       │ 제품매출                 │   3,000,000,000   │
│          │ 5102       │ 상품매출                 │     500,000,000   │
│  수익 합계│            │                         │   3,500,000,000   │
├──────────┼────────────┼─────────────────────────┼────────────────────┤
│ [비용]   │ 6101       │ 매출원가                 │   2,100,000,000   │
│          │ 6201       │ 급여                     │     400,000,000   │
│          │ 6202       │ 퇴직급여                 │      15,000,000   │
│          │ 6203       │ 복리후생비               │      20,000,000   │
│          │ 6210       │ 감가상각비               │      27,500,000   │
│          │ 6220       │ 접대비                   │      48,000,000   │
│          │ 6230       │ 기부금                   │       8,000,000   │
│          │ 6240       │ 이자비용                 │      10,000,000   │
│          │ 6290       │ 기타영업외비용           │      21,000,000   │
│  비용 합계│            │                         │   2,649,500,000   │
├──────────┼────────────┼─────────────────────────┼────────────────────┤
│ 법인세비용 │ 7101      │ 법인세비용               │       5,000,000   │
├──────────┴────────────┴─────────────────────────┴────────────────────┤
│ 당기순이익 = 3,500,000,000 - 2,649,500,000 - 5,000,000 = 845,500,000  │
│ ✔ IS 균형: 수익 = 비용 + 법인세비용 + 당기순이익                        │
│ ✔ 당기순이익(845,500,000) = BS 이익잉여금 증가(535,500K - 385,000K)    │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.3 CF (현금흐름표) / 자본변동표 탭

```
[CF 탭] — 영업·투자·재무 활동별 현금흐름 입력
  - 간접법 작성 시: 당기순이익 + 비현금 조정 항목
  - 직접법 작성 시: 수취·지급 현금 직접 입력
  - 세무조정에서 직접 사용 빈도는 낮으나, 이자·배당 수취/지급 분류가
    B-9 지급이자 조정의 보조 자료로 활용됨

[자본변동표 탭] — 자본 항목별 기초·증감·기말 입력
  - 유상증자, 자기주식, 배당금 지급, 당기순이익 귀속 추적
  - B-15 자본금과 적립금 조정의 원천 데이터
```

### 4.4 Excel 표준 템플릿 구조

임포트 파일은 다음 시트 구조를 따른다.

| 시트명 | 열 구조 |
|--------|---------|
| `BS` | `계정코드` / `계정명` / `전기말잔액` / `당기말잔액` / `정상잔액방향(D/C)` |
| `IS` | `계정코드` / `계정명` / `당기발생액` / `정상잔액방향(D/C)` |
| `CF` | `구분(영업/투자/재무)` / `항목명` / `금액` |
| `CE` | `자본항목코드` / `항목명` / `기초잔액` / `증가` / `감소` / `기말잔액` |

**임포트 처리 흐름**:

```
① 파일 업로드 (multipart/form-data)
   ▼
② 표준 템플릿 헤더 파싱 (열 순서 검증)
   ▼
③ 행 단위 파싱
   - 계정코드: 문자열, 최대 20자
   - 잔액/발생액: 숫자, 음수 허용 (대손충당금·감가상각누계액 등 차감 계정)
   - 정상잔액방향: D(차변) 또는 C(대변)
   ▼
④ 복식부기 검증
   A. BS 차변합계(자산) = 대변합계(부채+자본) → 불일치 시 FAIL
   B. IS 당기순이익 = 수익 - 비용(법인세 차감 전) → 불일치 시 WARN
   C. IS 당기순이익 = BS 이익잉여금 당기말 - 전기말 - 배당 → 불일치 시 WARN
   ▼
⑤ financial_statements (배치 메타) INSERT
   ▼
⑥ fs_lines 행 단위 INSERT (account_code, amount, balance_type, period_type 포함)
   ▼
⑦ 표준계정 자동 매핑 시도 (account_mappings 학습 결과 조회)
   ▼
⑧ NET_INCOME 표준계정 자동 감지 → B-1 당기순이익 필드 자동 연동
   ▼
⑨ 임포트 결과 응답 (성공 건수 / 오류 건수 / 자동 매핑률)
```

### 4.5 임포트 오류 처리

| 오류 유형 | 심각도 | 처리 |
|----------|--------|------|
| BS 차변≠대변 | ERROR | 임포트 전체 FAIL, 계속 불가 |
| 필수 열 누락 | ERROR | 임포트 전체 FAIL |
| 숫자 형식 오류 (특정 행) | ERROR | 해당 행만 SKIP, 나머지 적재. 오류 목록 반환 |
| IS 당기순이익 불일치 | WARN | 적재 후 경고 표시 |
| 표준계정 미매핑 | WARN | 적재 후 1-2 매핑 단계에서 처리 |

---

## 5. 1-2. 계정과목 매핑

### 5.1 화면 구성

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 1-2. 계정과목 매핑                           ㈜OOO / 2025               │
├──────────────────────────────────────────────────────────────────────────┤
│ 자동 매핑률: 87% (131/151 계정)   [미매핑만 보기]  [전체 저장]  [Excel]  │
│                                                                          │
│ ┌─────────────────────────────────────────────────────────────────────┐  │
│ │ 고객사 계정                     │ 표준 계정                          │  │
│ │ 코드   │ 계정명     │ 잔액      │ 표준코드   │ 표준명    │ 매핑방식  │  │
│ ├────────┼───────────┼───────────┼────────────┼──────────┼──────────┤  │
│ │ 1101   │ 보통예금   │250,000,000│ CASH       │ 현금및현금│ 자동(학습)│  │
│ │ 1110   │ 매출채권   │350,000,000│ AR         │ 매출채권  │ 자동(학습)│  │
│ │ 1115   │ 대손충당금 │ -7,000,000│ BAD_DEBT_PROV│대손충당금│ 자동(학습)│ │
│ │ 1120   │ 제품재고   │120,000,000│ INVENTORY  │ 재고자산  │ 자동(학습)│  │
│ │ 2102   │ 건물       │300,000,000│ BLDG_GROSS │ 건물(취득)│ 자동(학습)│  │
│ │ 6210   │ 감가상각비 │ 27,500,000│ DEPR_EXP   │ 감가상각비│ 자동(학습)│  │
│ │ 6220   │ 접대비     │ 48,000,000│ ENTERTAIN_EXP│접대비  │ 자동(학습)│  │
│ │ 6230   │ 기부금     │  8,000,000│ DONATION_EXP│기부금   │ 자동(학습)│  │
│ │ 8210   │ 복리후생비 │     ???   │ ???        │ (미매핑)  │ 수동 필요 │  │
│ │ 9110   │ 잡손실     │     ???   │ OTHER_EXP  │ 기타비용  │ 수동 지정 │  │
│ └─────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│ [8210 복리후생비 매핑 선택 ▼]                                            │
│   ○ SALARY_EXP (급여)   ○ WELFARE_EXP (복리후생비)  ○ OTHER_EXP        │
│   ○ 새 표준계정 생성    ○ 세무조정 무관 (NULL)                           │
│                                                                          │
│ ⚠ 세무관련 미매핑: ENTERTAIN_EXP(접대비), DONATION_EXP(기부금) 등       │
│   세무조정에 직접 사용되는 계정은 반드시 매핑해 주세요.                   │
└──────────────────────────────────────────────────────────────────────────┘
```

### 5.2 매핑 자동 학습 로직

```
최초 매핑 (신규 고객사 또는 첫 임포트):
  사용자 수동 매핑 → account_mappings INSERT
  → tenant_id + customer_id + account_code → standard_code 저장

재업로드 (같은 고객사 차기 연도):
  SELECT * FROM account_mappings
  WHERE customer_id = :cid AND account_code = :code
  → match_rate 계산 (이전 3개년 평균 신뢰도 가중)
  → match_rate >= 0.9 → 자동 적용 (auto_mapped = true)
  → match_rate < 0.9 → 사용자 확인 요청 (suggested = true)
```

### 5.3 세무 중요 표준계정 강제 확인 목록

다음 표준계정이 매핑되지 않으면 1-9 일관성 검증에서 ERROR 처리:

| 표준계정 코드 | 세무조정 연계 | 미매핑 시 영향 |
|-------------|-------------|--------------|
| `NET_INCOME` | B-1 당기순이익 자동 연동 | B-1 기초 값 누락 |
| `REVENUE` | B-3 접대비 수입금액 한도 | 접대비 한도 계산 불가 |
| `PPE_NET` | B-4 감가상각 자산 비교 | 자산대장 BS 검증 불가 |
| `ENTERTAIN_EXP` | B-3 접대비 계정 확인 | 접대비 일관성 검증 불가 |
| `DONATION_EXP` | B-2 기부금 계정 확인 | 기부금 일관성 검증 불가 |
| `INTEREST_EXP` | B-9 지급이자 원천 | 지급이자 조정 원천 확인 불가 |
| `PENSION_PROV` | B-5 퇴직급여충당금 잔액 | 충당금 한도 계산 불가 |
| `BAD_DEBT_PROV` | B-6 대손충당금 잔액 | 대손 한도 계산 불가 |

---

## 6. 1-3. 자산 및 감가상각

### 6.1 화면 구성 — 자산대장

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 1-3. 자산 및 감가상각                        ㈜OOO / 2025               │
├──────────────────────────────────────────────────────────────────────────┤
│ [자산 추가]  [Excel 임포트]  [표준 템플릿]  [전기 이월 자동 복사]         │
│ 분류: [전체▼]   상각방법: [전체▼]   상태: [전체▼]                        │
├──────────────────────────────────────────────────────────────────────────┤
│ 자산 목록                                                                │
│ ┌──┬───────┬────────┬────────────┬────────────┬────┬────┬──────┬──────┐ │
│ │No│ 자산명 │ 분류   │ 취득일     │ 취득원가   │내용│상각│ 회계  │ 세법 │ │
│ │  │       │       │            │            │연수│법  │ 상각비 │ 한도 │ │
│ ├──┼───────┼────────┼────────────┼────────────┼────┼────┼──────┼──────┤ │
│ │ 1│ 본사건물│ 건물  │2010-05-01  │500,000,000 │ 40 │정액│12,500│12,500│ │
│ │ 2│기계A  │ 기계장치│2022-01-01  │100,000,000 │ 8  │정률│15,000│14,500│ │
│ │ 3│차량B  │ 차량운반│2023-03-15  │ 40,000,000 │ 5  │정액│ 8,000│ 8,000│ │
│ │ 4│SW라이센│ 무형자산│2024-06-01  │ 10,000,000 │ 5  │정액│ 2,000│ 2,000│ │
│ └──┴───────┴────────┴────────────┴────────────┴────┴────┴──────┴──────┘ │
│                                                                          │
│ BS 검증:                                                                 │
│   자산대장 유형자산 취득원가 합계: 640,000,000                           │
│   BS 유형자산 취득원가 합계:      640,000,000  ✔                        │
│   자산대장 무형자산 합계:          10,000,000                            │
│   BS 무형자산 합계:                10,000,000  ✔                        │
│                                                                          │
│ [B-4 감가상각 조정으로 연동 ▶]  한도초과 예상: 500,000원 (기계A)         │
└──────────────────────────────────────────────────────────────────────────┘
```

### 6.2 자산 상세 입력 모달

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 자산 등록 / 수정                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│ 기본 정보                                                                │
│   자산명:       [기계장치 A________________________]                      │
│   자산 분류:    [기계장치▼]   -- BLDG / MACHINERY / VEHICLE / SOFTWARE 등│
│   취득일:       [2022-01-01]  사용개시일: [2022-02-01]                   │
│   취득원가:     [100,000,000]   잔존가액: [0] (정률법 시 무의미)          │
│   자산관리번호: [AST-2022-001]                                            │
│                                                                          │
│ 회계 상각 정보                                                            │
│   회계상 내용연수: [8]년   상각방법: [정률법▼]                            │
│   전기말 장부가액: [85,000,000]   전기말 상각누계액: [15,000,000]         │
│   당기 회사 계상 상각비: [15,000,000]  ← IS 자동 연산 확인용              │
│                                                                          │
│ 세법 상각 정보 (by_law_snapshot 적용)                                     │
│   기준내용연수: 8년   신고내용연수: [8]년 (범위: 6~10년)                  │
│   세법상 상각범위액: [14,500,000]  (자동 계산)                            │
│   세법 산출 근거: 정률법 상각률 0.250 × (100,000,000 - 15,000,000 누계)   │
│     = 85,000,000 × 0.250 = 21,250,000 → 한도: min(15,000,000, 21,250,000)│
│     → 한도 14,500,000 적용 (내용연수 8년 기준 보정)                       │
│   ► 한도초과: 500,000원 → B-4 유보 발생 예정                             │
│                                                                          │
│ 업무용승용차 여부: ☐ (해당 시 체크 → 1-5 운행기록 연동)                  │
│                                                                          │
│ 자본적지출 내역: [추가]                                                   │
│   2024-03-10  수선비: 7,000,000 → 자본적지출 판정 (600만원 초과)         │
│                                                                          │
│ [저장]  [삭제]                                                            │
└──────────────────────────────────────────────────────────────────────────┘
```

### 6.3 감가상각 자동 계산 로직

```
정액법 상각범위액:
  = (취득원가 - 잔존가액) / 신고내용연수
  × (사용월수 / 12)    ← 취득연도 월할 계산

정률법 상각범위액:
  = 기초 미상각잔액 × 상각률
  상각률 = 1 - (잔존가액 / 취득원가) ^ (1 / 신고내용연수)
  → 국세청 고시 기준내용연수별 상각률표(depreciation_lives) 적용

즉시상각의제 자동 판정:
  - 취득가액 100만원 미만 → 즉시 전액 손금산입
  - 단기사용자산(내용연수 1년 미만) → 즉시 손금산입
  - 소액수선비 → 600만원 이하 or 미상각잔액 5% 이하 → 수익적지출
```

---

## 7. 1-4. 거래 명세

### 7.1 화면 구성 — 탭 구조

거래 명세는 4가지 세무 목적별로 탭을 분리한다.

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 1-4. 거래 명세                               ㈜OOO / 2025               │
├──────────────────────────────────────────────────────────────────────────┤
│ 탭: [기부금] [접대비] [채권(대손)] [지급이자/가지급금]                    │
└──────────────────────────────────────────────────────────────────────────┘
```

### 7.2 기부금 탭

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 기부금 거래 명세                                                          │
│ [행 추가]  [Excel 임포트]  IS 기부금계정 합계: 8,000,000원               │
├───┬───────────────┬──────────┬──────────┬──────────────┬───────────────┤
│No │ 수령 단체명   │ 수령 사업│ 기부금   │ 금액         │ 사유/근거     │
│   │               │ 자번호   │ 유형     │              │               │
├───┼───────────────┼──────────┼──────────┼──────────────┼───────────────┤
│ 1 │ ○○사회복지재단│1234-5678 │ 특례(50%)│  5,000,000  │ 사회복지법인  │
│ 2 │ △△대학교     │2345-6789 │ 일반(10%)│  3,000,000  │ 교육기관      │
├───┴───────────────┴──────────┴──────────┴──────────────┴───────────────┤
│ 합계: 8,000,000   ✔ IS 기부금계정(8,000,000)과 일치                      │
│ [B-2 기부금 조정으로 연동 ▶]                                             │
└──────────────────────────────────────────────────────────────────────────┘
```

**기부금 유형 분류**:
- `SPECIAL` — 특례 기부금 (50% 한도, 사회복지·교육·의료법인 등, 법인세법 §24②)
- `GENERAL` — 일반 기부금 (10% 한도, 법정 외 단체, 법인세법 §24③)
- `NON_DEDUCTIBLE` — 비지정 기부금 (전액 손금불산입)
- `POLITICAL` — 정치자금 (별도 처리)

### 7.3 접대비 탭

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 접대비 거래 명세                                                          │
│ [행 추가]  [Excel 임포트]  IS 접대비계정 합계: 48,000,000원              │
├───┬────────────┬────────────┬────────────┬──────────┬───────────────────┤
│No │ 지출일자   │ 거래처명   │ 증빙 유형  │ 금액     │ 비고              │
├───┼────────────┼────────────┼────────────┼──────────┼───────────────────┤
│ 1 │ 2025-01-15 │ ○○상사    │ 법인카드   │2,000,000 │ 영업 미팅        │
│ 2 │ 2025-03-22 │ △△주식회사│ 현금영수증 │  500,000 │ 거래처 접대       │
│ 3 │ 2025-05-10 │ □□상회    │ 무증빙     │  300,000 │ 경조사            │
│ 4 │ 2025-06-30 │ ★★물산    │ 법인카드   │3,000,000 │ 신제품 발표회    │
│ ... │           │            │            │          │                   │
├───┴────────────┴────────────┴────────────┴──────────┴───────────────────┤
│ 소계: 법인카드·현금영수증: 47,000,000   무증빙: 1,000,000               │
│ ✔ IS 접대비계정(48,000,000)과 일치                                        │
│ ⚠ 무증빙 1,000,000원: 건당 3만원 초과분 전액 손금불산입 대상             │
│ [B-3 접대비 조정으로 연동 ▶]                                             │
└──────────────────────────────────────────────────────────────────────────┘
```

**증빙 유형 분류**:
- `CORP_CARD` — 법인신용카드
- `CASH_RECEIPT` — 현금영수증
- `TAX_INVOICE` — 세금계산서
- `NO_RECEIPT` — 무증빙 (전액 손금불산입 대상)
- `CONDOLENCE` — 경조사비 (20만원 이하 예외 인정)

**수입금액 명세 (접대비 한도 계산용)**:

```
┌──────────────────────────────────────────────────────────────────────────┐
│ [수입금액 명세]                                                           │
│   일반 수입금액: 3,000,000,000   특정 수입금액: 500,000,000              │
│   ← IS 매출(REVENUE 표준계정) 자동 연동 (수정 가능)                      │
│ 접대비 한도 미리보기:                                                     │
│   기본한도: 36,000,000 (중소기업)                                         │
│   수입금액 한도: 3,000,000,000×0.30% + 500,000,000×0.20%×10% = 9,100,000│
│   총 한도: 45,100,000                                                     │
└──────────────────────────────────────────────────────────────────────────┘
```

### 7.4 채권(대손) 탭

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 채권 명세 (대손충당금 설정 기준)                                          │
│ [행 추가]  [Excel 임포트]  BS 매출채권 잔액: 350,000,000                 │
├───┬──────────────┬────────────┬──────────┬──────────┬───────────────────┤
│No │ 채권 종류    │ 거래처명   │ 만기일   │ 기말잔액 │ 비고              │
├───┼──────────────┼────────────┼──────────┼──────────┼───────────────────┤
│ 1 │ 매출채권     │ 전체       │ -        │350,000,000│ BS 자동 연동     │
│ 2 │ 미수금       │ ○○전자    │2025-06-30│ 10,000,000│ 장기미수금        │
│ 3 │ 부실채권     │ △△물산    │2024-12-31│  5,000,000│ 6개월 경과 부도   │
├───┴──────────────┴────────────┴──────────┴──────────┴───────────────────┤
│ 설정 대상 채권 합계: 360,000,000                                          │
│ BS 대손충당금 기말잔액: 7,000,000                                         │
│ 법정 설정 한도: max(1%, 직전3개년 평균대손실적률) × 채권 합계             │
│   = max(1%, 0.8%) × 360,000,000 = 3,600,000                              │
│ 한도초과 충당금: 7,000,000 - 3,600,000 = 3,400,000 → B-6 유보 예정      │
│ [B-6 대손충당금 조정으로 연동 ▶]                                         │
└──────────────────────────────────────────────────────────────────────────┘
```

### 7.5 지급이자/가지급금 탭

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 지급이자 거래 명세                                                        │
│ [행 추가]  [Excel 임포트]  IS 이자비용 합계: 10,000,000원               │
├──────────────────────────────────────────────────────────────────────────┤
│ [지급이자 유형별 분류]                                                    │
│ ┌──┬───────────────────────────────┬──────────────┬─────────────────┐   │
│ │No│ 차입금 종류                   │ 평균잔액     │ 이자율(%)       │   │
│ ├──┼───────────────────────────────┼──────────────┼─────────────────┤   │
│ │ 1│ 금융기관 차입금 (일반)        │  100,000,000 │ 4.5%            │   │
│ │ 2│ 사채                          │   50,000,000 │ 5.0%            │   │
│ │ 3│ 건설자금 이자 (자본화 대상)   │   50,000,000 │ 4.0%            │   │
│ └──┴───────────────────────────────┴──────────────┴─────────────────┘   │
│                                                                          │
│ [가지급금 명세]  (B-9 인정이자 계산 기초)                                │
│ ┌──┬────────────────┬────────────┬──────────────┬──────────────────┐    │
│ │No│ 가지급금 대상  │ 발생일     │ 적수(잔액×일수)│ 비고           │    │
│ ├──┼────────────────┼────────────┼──────────────┼──────────────────┤    │
│ │ 1│ 대표이사 홍길동│ 2025-01-01 │ 2,000,000,000│ 연평균 5,479,452│    │
│ │ 2│ 이사 김△△    │ 2025-03-15 │   500,000,000│                  │    │
│ └──┴────────────────┴────────────┴──────────────┴──────────────────┘    │
│                                                                          │
│ 가중평균 차입이자율: 4.6%  (또는 당좌대출이자율 4.6% 중 선택)            │
│ 인정이자 = 가지급금 적수 합계 × 이자율 / 365                             │
│          = 2,500,000,000 × 4.6% / 365 = 315,068원                       │
│ [B-9 지급이자 조정으로 연동 ▶]                                           │
└──────────────────────────────────────────────────────────────────────────┘
```

**지급이자 4가지 유형**:
- `GENERAL` — 일반 금융비용 (기본 적용)
- `CONSTRUCTION_FUND` — 건설자금이자 (취득원가 산입, 유보)
- `NON_BUSINESS_ASSET` — 업무무관자산 관련 (비율 안분 손금불산입)
- `UNDERPAID_LOAN` — 가지급금 관련 (인정이자 계산)

---

## 8. 1-5. 업무용승용차 운행기록

### 8.1 화면 구성

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 1-5. 업무용승용차 운행기록                   ㈜OOO / 2025               │
├──────────────────────────────────────────────────────────────────────────┤
│ 대상 차량: [전체▼]   [차량 추가]   [Excel 임포트]                         │
│                                                                          │
│ ┌────────────────────────────────────────────────────────────────────┐   │
│ │ 차량: 차량B (40,000,000원, 정액법 5년) — 차번: 12가 3456           │   │
│ ├─────┬──────────┬──────────┬──────────┬──────────┬─────────────────┤   │
│ │ 월  │ 업무거리  │ 총 거리   │ 업무사용  │ 비고      │                 │   │
│ │     │ (km)     │ (km)     │ 비율(%)   │           │                 │   │
│ ├─────┼──────────┼──────────┼──────────┼──────────┼─────────────────┤   │
│ │ 1월 │  1,800   │   2,000  │  90.00%  │           │                 │   │
│ │ 2월 │  1,500   │   2,000  │  75.00%  │           │                 │   │
│ │ 3월 │  1,600   │   2,200  │  72.73%  │           │                 │   │
│ │  ...│          │          │          │           │                 │   │
│ │12월 │  1,700   │   2,100  │  80.95%  │           │                 │   │
│ ├─────┴──────────┴──────────┴──────────┴──────────┤                 │   │
│ │ 연간 합계: 업무 19,600km / 총 24,000km = 81.67%  │                 │   │
│ └────────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│ 업무사용비율: 81.67% → 운행기록 작성                                     │
│   (미작성 시: 70% 고정 적용, 연 한도 15,000,000 × 70% = 10,500,000)     │
│                                                                          │
│ B-10 계산 미리보기:                                                       │
│   차량관련비용(감가상각+임차료+유류비 등): 12,000,000                    │
│   업무사용분: 12,000,000 × 81.67% = 9,800,400                           │
│   감가상각비: 8,000,000 → 연 한도 8,000,000 적용 ✔                      │
│   손금불산입(비업무분): 12,000,000 - 9,800,400 = 2,199,600              │
│ [B-10 업무용승용차 조정으로 연동 ▶]                                     │
└──────────────────────────────────────────────────────────────────────────┘
```

### 8.2 운행기록 데이터 구조

```sql
CREATE TABLE vehicle_mileage_logs (
  id               UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id        UUID        NOT NULL,
  business_year_id UUID        NOT NULL REFERENCES business_years(id),
  asset_id         UUID        NOT NULL REFERENCES assets(id),  -- 1-3 자산과 연결
  log_year         INT         NOT NULL,
  log_month        INT         NOT NULL CHECK (log_month BETWEEN 1 AND 12),
  business_km      NUMERIC(10,2) NOT NULL DEFAULT 0,  -- 업무 거리
  total_km         NUMERIC(10,2) NOT NULL DEFAULT 0,  -- 총 거리
  business_ratio   NUMERIC(5,4) GENERATED ALWAYS AS (  -- 자동 계산
    CASE WHEN total_km = 0 THEN 0
         ELSE ROUND(business_km / total_km, 4) END
  ) STORED,
  note             TEXT,
  created_at       TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at       TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (business_year_id, asset_id, log_year, log_month)
);
```

---

## 9. 1-9. 일관성 검증

### 9.1 화면 구성

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 1-9. 입력 데이터 일관성 검증                 ㈜OOO / 2025               │
├──────────────────────────────────────────────────────────────────────────┤
│ [일관성 검증 실행]   최종 실행: 2026-05-22 16:00                         │
│ 총 12개 규칙  |  ERROR 0  |  WARN 2  |  INFO 1                          │
│                                                                          │
│ ━━━ [A] 복식부기 기본 검증 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ ✔ [ERROR] BS 균형: 자산(1,350,500,000) = 부채(315,000,000) + 자본(1,035,500,000)│
│ ✔ [ERROR] IS 균형: 수익(3,500,000,000) - 비용(2,649,500,000) - 법인세(5,000,000)│
│           = 당기순이익(845,500,000)                                      │
│ ✔ [ERROR] 자본 연결: BS기초이익잉여금(385,000,000) + 당기순이익(845,500,000)│
│           - 배당(695,000,000) = BS기말이익잉여금(535,500,000)            │
│                                                                          │
│ ━━━ [B] 자산대장 ↔ BS 검증 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ ✔ [ERROR] 유형자산 취득원가: 자산대장(640,000,000) = BS(640,000,000)    │
│ ✔ [ERROR] 감가상각누계액: 자산대장(107,500,000) = BS(107,500,000)       │
│ ✔ [ERROR] 무형자산: 자산대장(10,000,000) = BS(10,000,000)              │
│                                                                          │
│ ━━━ [C] 거래 명세 ↔ IS 검증 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ ✔ [ERROR] 기부금 거래 합계(8,000,000) = IS 기부금계정(8,000,000)        │
│ ⚠ [WARN]  접대비 거래 합계(47,500,000) ≠ IS 접대비계정(48,000,000)      │
│           차이: 500,000원  → 거래 명세 재확인 또는 계정 범위 조정         │
│ ✔ [ERROR] 이자비용 거래 합계(10,000,000) = IS 이자비용계정(10,000,000)  │
│                                                                          │
│ ━━━ [D] 세무 관련 표준계정 매핑 검증 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ ✔ [ERROR] NET_INCOME 표준계정 매핑 완료 (6201 복리후생비 → WELFARE_EXP) │
│ ✔ [ERROR] REVENUE(수입금액) 표준계정 매핑 완료                           │
│ ⚠ [WARN]  PENSION_PROV(퇴직급여충당금) 미매핑 — B-5 조정 시 수동 입력  │
│ ℹ [INFO]  CF(현금흐름표) 미입력 — B-9 이자 분류에 보조 활용 권장         │
│                                                                          │
│ [다음 단계 (2. 세무조정) ▶]   * ERROR 0건이므로 진행 가능               │
└──────────────────────────────────────────────────────────────────────────┘
```

### 9.2 검증 규칙 정의

| 규칙 코드 | 분류 | 심각도 | 검증 식 |
|----------|------|--------|---------|
| `CHK_BS_BALANCE` | 복식부기 | ERROR | `SUM(fs_lines WHERE fs_type='BS' AND account_class IN ('ASSET')) = SUM(...IN ('LIABILITY','EQUITY'))` |
| `CHK_IS_BALANCE` | 복식부기 | ERROR | `SUM(IS.REVENUE) - SUM(IS.EXPENSE) - 법인세비용 = NET_INCOME` |
| `CHK_EQUITY_ROLL` | 복식부기 | ERROR | `기초이익잉여금 + 당기순이익 - 배당 = 기말이익잉여금` |
| `CHK_PPE_COST` | 자산↔BS | ERROR | `SUM(assets.acquisition_cost WHERE asset_class='PPE') = BS.PPE_GROSS` |
| `CHK_ACCUM_DEPR` | 자산↔BS | ERROR | `SUM(assets.accumulated_depr) = BS.ACCUM_DEPR` |
| `CHK_INTANGIBLE` | 자산↔BS | ERROR | `SUM(assets.book_value WHERE asset_class='INTANGIBLE') = BS.INTANGIBLE` |
| `CHK_DONATION_TXN` | 거래↔IS | ERROR | `SUM(transactions WHERE type='DONATION') = IS.DONATION_EXP` |
| `CHK_ENTERTAIN_TXN` | 거래↔IS | WARN | `SUM(transactions WHERE type='ENTERTAINMENT') ≈ IS.ENTERTAIN_EXP` |
| `CHK_INTEREST_TXN` | 거래↔IS | ERROR | `SUM(transactions WHERE type='INTEREST') = IS.INTEREST_EXP` |
| `CHK_STD_ACCT_NET_INCOME` | 매핑 | ERROR | `NET_INCOME 표준계정 매핑 존재` |
| `CHK_STD_ACCT_REVENUE` | 매핑 | ERROR | `REVENUE 표준계정 매핑 존재` |
| `CHK_PENSION_PROV_MAP` | 매핑 | WARN | `PENSION_PROV 표준계정 매핑 존재` |

---

## 10. 데이터베이스 상세 설계

### 10.1 financial_statements (임포트 배치 메타)

```sql
CREATE TABLE financial_statements (
  id                UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id         UUID        NOT NULL,
  business_year_id  UUID        NOT NULL REFERENCES business_years(id),
  import_batch_no   INT         NOT NULL DEFAULT 1,  -- 재임포트 시 증가
  source_type       VARCHAR(20) NOT NULL,
    -- EXCEL | CSV | ERP_DOUZONE | ERP_SAP | ERP_ORACLE | MANUAL
  file_name         VARCHAR(255),
  file_s3_path      TEXT,
  status            VARCHAR(20) NOT NULL DEFAULT 'PROCESSING',
    -- PROCESSING | SUCCESS | FAILED | SUPERSEDED (재임포트 시 이전 배치)
  bs_debit_total    NUMERIC(18,0),  -- BS 차변(자산) 합계
  bs_credit_total   NUMERIC(18,0),  -- BS 대변(부채+자본) 합계
  bs_balanced       BOOLEAN,        -- 차변 = 대변 여부
  is_net_income     NUMERIC(18,0),  -- IS 당기순이익
  total_lines       INT,
  error_count       INT DEFAULT 0,
  auto_map_rate     NUMERIC(5,2),   -- 자동 매핑률 (%)
  imported_by       UUID        NOT NULL REFERENCES users(id),
  imported_at       TIMESTAMPTZ NOT NULL DEFAULT now(),
  error_detail      JSONB,
    -- [{"row": 5, "col": "당기말잔액", "msg": "숫자 형식 오류"}]
  UNIQUE (business_year_id, import_batch_no)
);
```

### 10.2 fs_lines (재무제표 행 — 복식부기 핵심)

```sql
CREATE TABLE fs_lines (
  id                UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id         UUID        NOT NULL,
  fs_id             UUID        NOT NULL REFERENCES financial_statements(id)
                                ON DELETE CASCADE,
  business_year_id  UUID        NOT NULL REFERENCES business_years(id),

  -- 고객사 계정 원본
  account_code      VARCHAR(20) NOT NULL,   -- 고객사 계정코드 (1101, 6220 등)
  account_name      VARCHAR(100) NOT NULL,  -- 고객사 계정명

  -- 복식부기 핵심 컬럼
  fs_type           VARCHAR(5)  NOT NULL,   -- BS | IS | CF | CE
  balance_type      VARCHAR(10) NOT NULL,
    -- ENDING    : BS 기말잔액 (당기말)
    -- BEGINNING : BS 기초잔액 (전기말)
    -- MOVEMENT  : IS/CF 당기발생액
  normal_balance    VARCHAR(6)  NOT NULL,   -- DEBIT | CREDIT (계정의 정상잔액 방향)
  amount            NUMERIC(18,0) NOT NULL,
    -- 정상잔액 기준 양수 입력.
    -- 차감 계정(대손충당금·감가상각누계액)은 음수로 입력
  debit_amount      NUMERIC(18,0) GENERATED ALWAYS AS (
    CASE WHEN normal_balance = 'DEBIT'  THEN GREATEST(amount, 0)
         WHEN normal_balance = 'CREDIT' THEN GREATEST(-amount, 0)
         ELSE 0 END
  ) STORED,
  credit_amount     NUMERIC(18,0) GENERATED ALWAYS AS (
    CASE WHEN normal_balance = 'CREDIT' THEN GREATEST(amount, 0)
         WHEN normal_balance = 'DEBIT'  THEN GREATEST(-amount, 0)
         ELSE 0 END
  ) STORED,

  -- 표준계정 매핑 (1-2 단계 후 채워짐)
  std_account_code  VARCHAR(20) REFERENCES standard_accounts(code),
  is_auto_mapped    BOOLEAN     DEFAULT false,
  map_confidence    NUMERIC(4,3),  -- 0.000 ~ 1.000

  -- 정렬·그룹
  sort_order        INT,
  sub_category      VARCHAR(50),  -- 유동자산 | 비유동자산 | 유동부채 등

  created_at        TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- 핵심 인덱스
CREATE INDEX idx_fs_lines_by_year ON fs_lines(business_year_id, fs_type, balance_type);
CREATE INDEX idx_fs_lines_std_acct ON fs_lines(business_year_id, std_account_code);
```

**집계 뷰 — 복식부기 균형 확인**:

```sql
CREATE VIEW v_fs_balance_check AS
SELECT
  fsl.business_year_id,
  fs.import_batch_no,
  -- BS 차변(자산) 합계
  SUM(fsl.debit_amount)  FILTER (WHERE fsl.fs_type='BS' AND fsl.balance_type='ENDING')
    AS bs_asset_total,
  -- BS 대변(부채+자본) 합계
  SUM(fsl.credit_amount) FILTER (WHERE fsl.fs_type='BS' AND fsl.balance_type='ENDING')
    AS bs_liab_equity_total,
  -- IS 순이익
  SUM(fsl.credit_amount) FILTER (WHERE fsl.fs_type='IS')
  - SUM(fsl.debit_amount)  FILTER (WHERE fsl.fs_type='IS')
    AS is_net_income,
  -- 균형 여부
  (SUM(fsl.debit_amount)  FILTER (WHERE fsl.fs_type='BS' AND fsl.balance_type='ENDING')
   = SUM(fsl.credit_amount) FILTER (WHERE fsl.fs_type='BS' AND fsl.balance_type='ENDING'))
    AS bs_balanced
FROM fs_lines fsl
JOIN financial_statements fs ON fs.id = fsl.fs_id
GROUP BY fsl.business_year_id, fs.import_batch_no;
```

### 10.3 account_mappings (계정 매핑 학습)

```sql
CREATE TABLE account_mappings (
  id               UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id        UUID        NOT NULL,
  customer_id      UUID        NOT NULL REFERENCES customers(id),
  account_code     VARCHAR(20) NOT NULL,    -- 고객사 계정코드
  account_name     VARCHAR(100),
  std_account_code VARCHAR(20) REFERENCES standard_accounts(code),
  is_auto_mapped   BOOLEAN     NOT NULL DEFAULT false,
  usage_count      INT         NOT NULL DEFAULT 1,   -- 학습 횟수
  last_used_at     TIMESTAMPTZ,
  created_by       UUID        REFERENCES users(id),
  created_at       TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at       TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (customer_id, account_code)
);
```

### 10.4 assets (자산대장 — 복식부기 취득원가 기준)

```sql
CREATE TABLE assets (
  id                 UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id          UUID        NOT NULL,
  business_year_id   UUID        NOT NULL REFERENCES business_years(id),
  asset_no           VARCHAR(50),              -- 자산관리번호
  asset_name         VARCHAR(200) NOT NULL,
  asset_class        VARCHAR(30) NOT NULL,
    -- BUILDING | MACHINERY | VEHICLE | FIXTURE | LAND
    -- SOFTWARE | PATENT | GOODWILL | LEASE | INVEST_PROPERTY
  acquisition_date   DATE        NOT NULL,
  service_start_date DATE,                     -- 사용개시일 (월할 계산 기준)
  acquisition_cost   NUMERIC(18,0) NOT NULL,  -- 취득원가 (차변 금액)
  residual_value     NUMERIC(18,0) NOT NULL DEFAULT 0,
  capital_expenditures JSONB,
    -- [{"date":"2024-03-10","amount":7000000,"type":"CAPITAL","note":"수선"}]

  -- 회계 상각 정보
  acct_useful_life   INT,                -- 회계 내용연수 (년)
  acct_depr_method   VARCHAR(10),        -- STRAIGHT_LINE | DECLINING_BALANCE
  accumulated_depr_prior NUMERIC(18,0) DEFAULT 0,  -- 전기말 누계 상각
  acct_depr_current  NUMERIC(18,0),     -- 당기 회사 계상 상각비

  -- 세법 상각 정보 (by_law_snapshot 기준 자동 계산)
  tax_useful_life    INT,                -- 신고 내용연수
  tax_depr_rate      NUMERIC(6,4),       -- 정률법 상각률 (depreciation_lives에서 조회)
  tax_depr_limit     NUMERIC(18,0),      -- 세법 상각범위액 (자동 계산)
  depr_excess        NUMERIC(18,0) GENERATED ALWAYS AS (  -- 한도초과 (유보 발생)
    GREATEST(COALESCE(acct_depr_current,0) - COALESCE(tax_depr_limit,0), 0)
  ) STORED,
  depr_shortage      NUMERIC(18,0) GENERATED ALWAYS AS (  -- 시인부족액
    GREATEST(COALESCE(tax_depr_limit,0) - COALESCE(acct_depr_current,0), 0)
  ) STORED,

  -- 업무용승용차 여부
  is_business_vehicle BOOLEAN NOT NULL DEFAULT false,
  vehicle_reg_no      VARCHAR(20),        -- 차량번호

  snapshot_id        UUID        REFERENCES by_law_snapshot(id),
  is_active          BOOLEAN     NOT NULL DEFAULT true,
  prev_year_asset_id UUID        REFERENCES assets(id),  -- 전기 자산 연결
  created_at         TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at         TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

### 10.5 transactions (거래 명세 — 세무조정 원천)

```sql
CREATE TABLE transactions (
  id                 UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id          UUID        NOT NULL,
  business_year_id   UUID        NOT NULL REFERENCES business_years(id),

  txn_type           VARCHAR(30) NOT NULL,
    -- DONATION | ENTERTAINMENT | BAD_DEBT | INTEREST_EXPENSE | LOAN_RECEIVABLE
  txn_date           DATE        NOT NULL,
  counterparty_name  VARCHAR(200),           -- 거래처명
  counterparty_biz_no VARCHAR(20),          -- 사업자번호

  -- 복식부기 관점: 차변 계정과 대변 계정 모두 기록
  debit_account_code  VARCHAR(20),           -- 차변 계정 (예: 접대비 6220)
  credit_account_code VARCHAR(20),           -- 대변 계정 (예: 보통예금 1101)
  amount             NUMERIC(18,0) NOT NULL,

  -- 세무 목적별 추가 속성 (JSONB)
  tax_attributes     JSONB,
  --  기부금:       {"donation_type":"SPECIAL","recipient_type":"사회복지법인"}
  --  접대비:       {"evidence_type":"CORP_CARD","is_condolence":false}
  --  채권:         {"receivable_type":"AR","overdue_months":7}
  --  지급이자:     {"interest_type":"GENERAL","loan_balance":100000000}
  --  가지급금:     {"borrower_name":"홍길동","daily_balance":5479452,"days":365}

  note               TEXT,
  source_fs_line_id  UUID REFERENCES fs_lines(id),  -- 원천 재무제표 행 연결
  created_by         UUID REFERENCES users(id),
  created_at         TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_transactions_by_year_type ON transactions(business_year_id, txn_type);
```

### 10.6 세무정보 입력 전체 ERD

```
financial_statements (임포트 배치)
  │ 1:N
  └── fs_lines (재무제표 행)
        │  N:1
        └── standard_accounts (표준계정 마스터)
              ↑ 참조
account_mappings (학습 매핑: customer_id + account_code → std_account_code)

business_years
  │ 1:N
  ├── assets (자산대장)
  │     │ 1:N
  │     └── vehicle_mileage_logs (업무용승용차 운행기록)
  │
  └── transactions (거래 명세)
        └── (source_fs_line_id → fs_lines)
```

---

## 11. API 명세

### 11.1 재무제표 관련

| Method | Path | 설명 |
|--------|------|------|
| POST | `/api/workspace/{byId}/fs/import` | Excel/CSV 임포트 |
| POST | `/api/workspace/{byId}/fs/erp-sync` | ERP 연동 (더존/SAP) |
| GET | `/api/workspace/{byId}/fs` | 재무제표 조회 (탭별 필터) |
| GET | `/api/workspace/{byId}/fs/balance-check` | BS/IS 균형 검증 결과 |
| PUT | `/api/workspace/{byId}/fs/lines/{lineId}` | 행 단위 수정 |
| DELETE | `/api/workspace/{byId}/fs/batch/{batchId}` | 임포트 배치 삭제 (재임포트 전) |
| GET | `/api/workspace/{byId}/fs/template` | 표준 Excel 템플릿 다운로드 |

**임포트 응답 예시**:
```json
{
  "batchId": "uuid",
  "status": "SUCCESS",
  "totalLines": 151,
  "errorCount": 0,
  "bsBalanced": true,
  "bsAssetTotal": 1350500000,
  "bsLiabEquityTotal": 1350500000,
  "isNetIncome": 845500000,
  "autoMapRate": 87.4,
  "errors": []
}
```

### 11.2 계정과목 매핑 관련

| Method | Path | 설명 |
|--------|------|------|
| GET | `/api/workspace/{byId}/account-mappings` | 매핑 목록 (미매핑 포함) |
| PUT | `/api/workspace/{byId}/account-mappings/{code}` | 단건 매핑 저장 |
| POST | `/api/workspace/{byId}/account-mappings/bulk` | 일괄 저장 |
| GET | `/api/workspace/{byId}/account-mappings/suggestions` | AI 매핑 추천 목록 |
| GET | `/api/standard-accounts` | 표준계정 마스터 목록 |

### 11.3 자산대장 관련

| Method | Path | 설명 |
|--------|------|------|
| GET | `/api/workspace/{byId}/assets` | 자산대장 목록 |
| POST | `/api/workspace/{byId}/assets` | 자산 단건 추가 |
| PUT | `/api/workspace/{byId}/assets/{assetId}` | 자산 수정 |
| DELETE | `/api/workspace/{byId}/assets/{assetId}` | 자산 삭제 |
| POST | `/api/workspace/{byId}/assets/import` | Excel 임포트 |
| POST | `/api/workspace/{byId}/assets/carry-forward` | 전기 자산 이월 복사 |
| GET | `/api/workspace/{byId}/assets/bs-reconcile` | 자산대장↔BS 검증 |
| GET | `/api/workspace/{byId}/assets/depr-preview` | 감가상각 자동 계산 미리보기 |

### 11.4 거래 명세 관련

| Method | Path | 설명 |
|--------|------|------|
| GET | `/api/workspace/{byId}/transactions?type={txnType}` | 유형별 거래 목록 |
| POST | `/api/workspace/{byId}/transactions` | 거래 추가 |
| PUT | `/api/workspace/{byId}/transactions/{txnId}` | 거래 수정 |
| DELETE | `/api/workspace/{byId}/transactions/{txnId}` | 거래 삭제 |
| POST | `/api/workspace/{byId}/transactions/import?type={txnType}` | Excel 임포트 |
| GET | `/api/workspace/{byId}/transactions/is-reconcile` | 거래↔IS 검증 |

### 11.5 운행기록 관련

| Method | Path | 설명 |
|--------|------|------|
| GET | `/api/workspace/{byId}/vehicle-mileage` | 전체 차량 운행기록 |
| GET | `/api/workspace/{byId}/vehicle-mileage/{assetId}` | 차량별 운행기록 |
| PUT | `/api/workspace/{byId}/vehicle-mileage/{assetId}` | 월별 운행기록 저장 |
| POST | `/api/workspace/{byId}/vehicle-mileage/import` | Excel 일괄 임포트 |

### 11.6 일관성 검증

| Method | Path | 설명 |
|--------|------|------|
| POST | `/api/workspace/{byId}/info/validate` | 일관성 검증 실행 |
| GET | `/api/workspace/{byId}/info/validate/latest` | 최신 검증 결과 |

---

## 12. 역할별 접근 제어

| 기능 | SUPER_ADMIN | TENANT_ADMIN | TAX_EXPERT | TAX_REVIEWER | ASSISTANT | VIEWER |
|------|:-----------:|:------------:|:----------:|:------------:|:---------:|:------:|
| 재무제표 임포트 | ● | ● | ● | – | ● | – |
| 재무제표 수정 | ● | ● | ● | – | ● | – |
| 재무제표 조회 | ● | ● | ● | ● | ● | ● |
| 계정 매핑 | ● | ● | ● | – | ● | – |
| 자산 추가/수정 | ● | ● | ● | – | ● | – |
| 자산 삭제 | ● | ● | ● | – | – | – |
| 거래 명세 입력 | ● | ● | ● | – | ● | – |
| 운행기록 입력 | ● | ● | ● | – | ● | – |
| 일관성 검증 실행 | ● | ● | ● | ● | – | – |
| Excel 다운로드 | ● | ● | ● | ● | ● | ● |

---

## 13. 프론트엔드 구현 상세

### 13.1 컴포넌트 구조

```
src/pages/Workspace/steps/Step1Info/
├── index.tsx                        ← 서브 탭 라우터 (1-1 ~ 1-9)
├── SubTabNav.tsx                    ← 좌측 서브 단계 트리
│
├── fs/                              ← 1-1. 재무제표
│   ├── index.tsx
│   ├── FsSheetTabs.tsx              ← BS/IS/CF/CE 탭
│   ├── BsSheet.tsx                  ← 재무상태표 그리드
│   ├── IsSheet.tsx                  ← 손익계산서 그리드
│   ├── CfSheet.tsx                  ← 현금흐름표 그리드
│   ├── CeSheet.tsx                  ← 자본변동표 그리드
│   ├── FsImportModal.tsx            ← Excel/ERP 임포트 모달
│   ├── FsBalanceCheck.tsx           ← BS/IS 균형 상태 표시
│   └── FsImportHistory.tsx          ← 임포트 이력
│
├── mapping/                         ← 1-2. 계정과목 매핑
│   ├── index.tsx
│   ├── MappingTable.tsx             ← 고객사계정 ↔ 표준계정 그리드
│   ├── StdAccountSelector.tsx       ← 표준계정 드롭다운
│   └── MappingStats.tsx             ← 매핑률·경고 현황
│
├── assets/                          ← 1-3. 자산 및 감가상각
│   ├── index.tsx
│   ├── AssetTable.tsx               ← 자산대장 그리드
│   ├── AssetFormModal.tsx           ← 자산 상세 등록/수정 모달
│   ├── AssetImport.tsx              ← Excel 임포트
│   ├── DeprPreview.tsx              ← 감가상각 자동 계산 미리보기
│   └── BsReconcile.tsx              ← 자산대장↔BS 검증 결과
│
├── transactions/                    ← 1-4. 거래 명세
│   ├── index.tsx
│   ├── TxnTabNav.tsx                ← 기부금/접대비/채권/지급이자 탭
│   ├── DonationTab.tsx
│   ├── EntertainmentTab.tsx
│   ├── EntertainmentRevenueSummary.tsx  ← 수입금액 명세 (접대비 한도용)
│   ├── BadDebtTab.tsx
│   └── InterestTab.tsx
│
├── vehicle/                         ← 1-5. 업무용승용차 운행기록
│   ├── index.tsx
│   ├── VehicleSelector.tsx          ← 차량 목록 선택
│   ├── MileageGrid.tsx              ← 월별 운행기록 12×3 그리드
│   └── BusinessRatioSummary.tsx     ← 업무사용비율 + B-10 미리보기
│
└── validate/                        ← 1-9. 일관성 검증
    ├── index.tsx
    ├── ValidationRuleTable.tsx      ← 검증 결과 12개 규칙 표시
    └── ValidationSummary.tsx        ← ERROR/WARN/INFO 카운트
```

### 13.2 복식부기 균형 실시간 표시 컴포넌트

```typescript
// BsBalanceIndicator.tsx
// BS 그리드 하단에 항상 표시
interface BsBalanceState {
  assetTotal: number;       // 차변 합계 (자산)
  liabEquityTotal: number;  // 대변 합계 (부채+자본)
  isBalanced: boolean;
  diff: number;
}

export const BsBalanceIndicator: React.FC<BsBalanceState> = ({
  assetTotal, liabEquityTotal, isBalanced, diff
}) => (
  <div className={`balance-bar ${isBalanced ? 'balanced' : 'unbalanced'}`}>
    <span>자산: {fmt(assetTotal)}</span>
    <span>=</span>
    <span>부채+자본: {fmt(liabEquityTotal)}</span>
    {!isBalanced && (
      <span className="error">⚠ 차이: {fmt(Math.abs(diff))}</span>
    )}
    {isBalanced && <span className="ok">✔ 균형</span>}
  </div>
);
```

### 13.3 RTK Query 슬라이스 (세무정보 입력)

```typescript
export const infoApi = createApi({
  reducerPath: 'infoApi',
  tagTypes: ['FS', 'Mapping', 'Asset', 'Transaction', 'Vehicle', 'Validate'],
  endpoints: (builder) => ({
    importFs: builder.mutation<FsImportResult, { byId: string; file: File }>({
      query: ({ byId, file }) => {
        const form = new FormData();
        form.append('file', file);
        return { url: `/workspace/${byId}/fs/import`, method: 'POST', body: form };
      },
      invalidatesTags: ['FS', 'Mapping', 'Validate'],
    }),
    getFs: builder.query<FsLine[], { byId: string; fsType: string; balanceType: string }>({
      query: ({ byId, fsType, balanceType }) =>
        `/workspace/${byId}/fs?fsType=${fsType}&balanceType=${balanceType}`,
      providesTags: ['FS'],
    }),
    updateMapping: builder.mutation<void, { byId: string; accountCode: string; stdCode: string }>({
      query: ({ byId, accountCode, stdCode }) => ({
        url: `/workspace/${byId}/account-mappings/${accountCode}`,
        method: 'PUT',
        body: { stdAccountCode: stdCode },
      }),
      invalidatesTags: ['Mapping', 'Validate'],
    }),
    carryForwardAssets: builder.mutation<CarryForwardResult, string>({
      query: (byId) => ({
        url: `/workspace/${byId}/assets/carry-forward`,
        method: 'POST',
      }),
      invalidatesTags: ['Asset', 'Validate'],
    }),
    runInfoValidate: builder.mutation<InfoValidateResult, string>({
      query: (byId) => ({
        url: `/workspace/${byId}/info/validate`,
        method: 'POST',
      }),
      invalidatesTags: ['Validate'],
    }),
  }),
});
```

---

## 14. 구현 우선순위 및 검증 항목

### 14.1 단계별 구현 계획

| Phase | 범위 | 기간 | 비고 |
|-------|------|------|------|
| **Phase A** | 1-1 재무제표 임포트 (Excel) + BS/IS 균형 검증 + fs_lines 적재 | 1.5주 | 복식부기 핵심 |
| **Phase B** | 표준계정 마스터 + 1-2 계정과목 매핑 + 학습 로직 | 1주 | |
| **Phase C** | 1-3 자산대장 + 전기 이월 + 감가상각 자동 계산 | 1.5주 | B-4 연동 포함 |
| **Phase D** | 1-4 거래 명세 (4종 탭) + IS 교차 검증 | 1주 | B-2·B-3·B-6·B-9 연동 |
| **Phase E** | 1-5 업무용승용차 운행기록 | 0.5주 | B-10 연동 |
| **Phase F** | 1-9 일관성 검증 (12개 규칙) 통합 | 0.5주 | |
| **Phase G** | ERP 연동 (더존/SAP) + CF/CE 탭 | 1주 | 우선순위 낮음 |

### 14.2 검증 항목

| 검증 항목 | 기준 |
|----------|------|
| BS 균형 | 차변(자산) ≠ 대변(부채+자본) 시 임포트 FAIL 처리 확인 |
| IS 순이익 연동 | NET_INCOME 표준계정 매핑 후 B-1 당기순이익 자동 반영 확인 |
| 자산대장↔BS 검증 | 자산대장 취득원가 합계 ≠ BS PPE_GROSS 시 WARN 표시 확인 |
| 감가상각 자동 계산 | 정액/정률법별 세법 상각범위액 계산 정확성 확인 |
| 접대비 수입금액 연동 | REVENUE 표준계정 매핑 후 B-3 수입금액 자동 반영 확인 |
| 가지급금 인정이자 | 적수 × 이자율 / 365 계산 정확성 확인 |
| 전기 이월 | 자산대장 carry-forward 후 prev_year_asset_id 연결 확인 |
| 재임포트 처리 | 기존 배치 SUPERSEDED 처리 후 신규 배치 적용 확인 |
| 테넌트 격리 | 다른 테넌트 fs_lines 접근 불가 확인 |

---

## 문서 변경 이력

| 버전 | 일자 | 작성자 | 변경내용 |
|------|------|--------|----------|
| 1.1 | 2026-05-22 | 강수 | 최초 작성 — 1.세무정보입력 복식부기 기반 화면·데이터 구성 세부 구현 계획 |

---

**문의**: Kang-Soo.Cho@kr.ey.com
**연관 문서**:
- `법인세_세무조정계산서_시스템_설계서.md`
- `업무흐름.md`
- `세부구현_신고_v1.0.md` (신고작업 전체 구현 계획)
- `세부구현_v1.0.md` (대시보드 구현 계획)
- `DB설계.md`
