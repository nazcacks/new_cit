# 세부구현_신고_v1.0.md 초상세 goal 명령 v4

이 문서는 `세부구현_신고_v1.0.md`를 실제 진행 중 끊어서 맡길 수 있도록 더 작은 `goal:` 명령으로 분해한 목록이다.
v3보다 한 단계 더 세분화해서, 각 기능을 `현황 확인 -> DB/모델 -> 백엔드 API -> 프론트 화면 -> 상태/권한/잠금 -> 테스트/브라우저 검증` 순서로 나눈다.

## 사용 원칙

- 한 번에 하나의 `goal:` 블록만 실행한다.
- 각 goal은 관련 코드 수정, 테스트 추가/수정, 포맷, 필요한 검증 명령 실행, 프론트 변경 시 브라우저 확인까지 포함한다.
- 한 goal에서 수정 파일이 과도하게 늘어나면 완료 보고에 분리 후보를 남기고 다음 goal로 넘긴다.
- 백엔드 강제 규칙이 프론트 표시보다 먼저 구현되어야 한다.
- tenant/customer/business_year 격리, 역할 권한, FILED 잠금은 쓰기 API마다 반복 검증한다.
- 완료 보고에는 변경 파일, 검증 명령, 브라우저 확인 여부, 남은 위험을 짧게 기록한다.

## 공통 완료 기준

- 원문 문서의 해당 장/절 요구사항과 실제 구현 차이를 확인했다.
- 기존 코드베이스의 DB/API/프론트 패턴을 우선 사용했다.
- 새 기능의 정상, 빈 상태, 권한 없음, 다른 테넌트 접근, FILED 잠금 케이스 중 해당되는 항목을 테스트했다.
- 프론트 화면 변경 시 메뉴 진입, 주요 버튼, 로딩/빈/오류 상태, 모바일 또는 좁은 폭 표시를 확인했다.
- 다음 goal이 사용할 route key, DTO, fixture, 테스트 데이터가 코드나 문서에 남아 있다.

---

## T00. 기준선과 추적표

### T00-01. 원문 요구사항 원자화

```text
goal: [T00-01] 세부구현_신고_v1.0.md 1~16장을 원자 요구사항 단위로 쪼개고, 장/절/요구사항/영향영역/우선순위/후속 goal ID 컬럼을 가진 추적표를 만들어줘.
선행: 없음
작업: 원문 목차와 본문을 대조해 누락 없이 요구사항을 뽑고, 중복되거나 모호한 요구사항은 별도 메모로 표시
완료: 모든 장/절이 추적표에 연결되고 이후 goal에서 참조 가능한 ID가 있음
검증: 원문 목차와 추적표 항목을 대조해 빠진 절이 없는지 확인
```

### T00-02. 현재 DB 스키마 대조

```text
goal: [T00-02] 신고작업 요구사항과 현재 migration/schema/model을 대조해 DB gap 표를 만들어줘.
선행: T00-01
작업: business_years, by_law_snapshot, financial_statements, tax_adjustments, reserves, form_data, validation, approval, efiling 관련 테이블과 컬럼 확인
완료: 존재/부분존재/없음/명세불일치/마이그레이션 필요 여부가 표로 정리됨
검증: migrations, Rust 모델, SQL 사용처를 함께 확인
```

### T00-03. 현재 API 라우트 대조

```text
goal: [T00-03] 세부구현_신고_v1.0.md 13.1 API 목록과 현재 서버 라우트를 매핑해줘.
선행: T00-01
작업: workspace, business-years, financial-statements, adjustments, forms, validate, approval, print, efile 라우트의 구현 상태 확인
완료: endpoint별 구현/부분구현/없음/경로불일치/권한검증/테스트 여부가 표로 정리됨
검증: 라우터 등록 파일과 관련 테스트 파일을 대조
```

### T00-04. 현재 프론트 메뉴와 화면 대조

```text
goal: [T00-04] 신고작업 단계 0~7과 하위 메뉴의 현재 route key, 메뉴 노드, renderer, DOM 식별자를 매핑해줘.
선행: T00-01
작업: 공용 placeholder 화면, 개별 구현 화면, 테스트만 있는 화면, 메뉴만 있는 화면을 구분
완료: 프론트에서 먼저 분리해야 할 화면과 재사용 가능한 화면이 명확해짐
검증: 메뉴 정의, 화면 dispatcher, frontend spec을 대조
```

### T00-05. 테스트 기준선 기록

```text
goal: [T00-05] 신고작업 구현 전 현재 백엔드/프론트 테스트 기준선을 기록해줘.
선행: T00-02, T00-03, T00-04
작업: 관련 Rust 테스트, frontend 테스트, 문법 검사, 포맷 검사 실행 가능 여부 확인
완료: 통과/실패/미실행 사유와 기존 실패 여부가 기록됨
검증: 실행한 명령과 핵심 결과를 완료 보고에 포함
```

### T00-06. 공통 fixture 설계

```text
goal: [T00-06] 신고작업 전체 goal에서 재사용할 tenant/customer/business_year fixture 구조를 설계해줘.
선행: T00-02, T00-05
작업: DRAFT, IN_REVIEW, APPROVED, FILED, AMENDED 상태와 정상/권한없음/다른 tenant 케이스 fixture 정의
완료: 이후 API 테스트에서 같은 fixture를 재사용할 수 있음
검증: 최소 하나의 smoke test에서 fixture 생성과 조회 성공
```

### T00-07. 역할 fixture 설계

```text
goal: [T00-07] 역할별 접근 제어 테스트에 사용할 사용자 fixture를 만들어줘.
선행: T00-06
작업: TAX_EXPERT, TAX_REVIEWER, ASSISTANT, VIEWER, TENANT_ADMIN, 다른 tenant 사용자 준비
완료: 같은 byId에 대해 허용/차단 사용자를 쉽게 만들 수 있음
검증: 대표 조회 API에서 허용/차단 smoke test 추가
```

### T00-08. FILED 잠금 fixture 설계

```text
goal: [T00-08] FILED 이후 수정 차단을 반복 검증할 business_year fixture를 만들어줘.
선행: T00-06
작업: FILED business_year, 잠긴 snapshot, 연결된 form_data, tax_adjustments, efiling_history를 생성할 수 있게 구성
완료: 모든 쓰기 API 테스트에서 FILED 잠금 fixture 재사용 가능
검증: 대표 저장 API 하나에서 LOCKED_FILED 또는 지정 오류 반환 확인
```

### T00-09. 브라우저 검증 체크리스트

```text
goal: [T00-09] 프론트 goal마다 반복 사용할 브라우저 확인 체크리스트를 만들어줘.
선행: T00-04
작업: 서버 실행, 로그인/진입, 메뉴 이동, 버튼 동작, 빈 상태, 오류 상태, 스크린샷 기준 정리
완료: 프론트 변경 완료 보고에 같은 체크리스트를 붙일 수 있음
검증: 기존 화면 하나를 실제 브라우저로 확인
```

### T00-10. 우선순위와 실행 순서 재정렬

```text
goal: [T00-10] T00 결과를 기준으로 T01~T14 실행 순서와 병목 위험을 재정렬해줘.
선행: T00-01~T00-09
작업: DB 선행, API 선행, 프론트 병렬 가능, 테스트 데이터 필요성, 외부 연동 위험을 기준으로 순서 조정
완료: 다음 goal 하나를 바로 시작할 수 있는 실행 순서가 확정됨
검증: 각 track의 첫 goal 선행조건이 충족됐는지 확인
```

---

## T01. 공통 도메인, 상태, 진행률

### T01-01. business_year 상태 타입

```text
goal: [T01-01] business_years.status를 DRAFT/IN_REVIEW/APPROVED/FILED/AMENDED 공통 타입으로 정리해줘.
선행: T00-02
작업: 문자열 분산 사용처를 찾고, 파싱/직렬화/표시용 label 변환을 공통화
완료: 새 API와 기존 API가 같은 상태 타입을 재사용 가능
검증: 정상 상태, 알 수 없는 상태, JSON 직렬화 테스트 추가
```

### T01-02. 상태 전이 허용표

```text
goal: [T01-02] 신고작업 상태 전이 허용표를 코드와 테스트로 고정해줘.
선행: T01-01
작업: DRAFT->IN_REVIEW, IN_REVIEW->APPROVED, APPROVED->FILED, FILED->AMENDED, 반려 시 DRAFT 복귀 규칙 정의
완료: 허용/차단 전이가 명확히 테스트됨
검증: 상태별 성공/실패 전이 단위 테스트 추가
```

### T01-03. 상태 전이 적용 헬퍼

```text
goal: [T01-03] business_year 상태 변경 시 전이 검증, 감사 로그, 잠금 처리를 연결할 공통 헬퍼를 만들어줘.
선행: T01-02
작업: 직접 status update 사용처를 줄일 수 있는 함수 또는 서비스 도입
완료: 결재, 전자신고, 수정신고 API가 같은 상태 변경 경로를 사용할 수 있음
검증: 전이 성공, 전이 차단, audit hook 호출 테스트 추가
```

### T01-04. 단계 코드 타입

```text
goal: [T01-04] INFO/ADJUST/FORM/VALIDATE/APPROVE/PRINT/EFILE 단계 코드를 공통 타입으로 정의해줘.
선행: T01-01
작업: 표시 순서, 라우팅 키, API JSON 값, 프론트 stepper 값의 기준을 통일
완료: context API와 프론트 스테퍼가 같은 단계 코드 체계를 사용 가능
검증: 직렬화, 순서, 알 수 없는 코드 처리 테스트 추가
```

### T01-05. stepStatus 타입

```text
goal: [T01-05] PENDING/IN_PROGRESS/COMPLETED/WARN/LOCKED 단계 상태 타입을 정의해줘.
선행: T01-04
작업: 상태별 표시 label, 편집 가능 여부, 진입 가능 여부 매핑을 공통화
완료: 단계별 완료/경고/잠금 상태를 일관된 응답으로 표현 가능
검증: JSON 응답 스냅샷과 표시 매핑 테스트 추가
```

### T01-06. stepStatus 계산 골격

```text
goal: [T01-06] 세부구현_신고_v1.0.md 2.4 기준으로 stepStatus 계산 골격을 구현해줘.
선행: T01-04, T01-05
작업: 데이터가 없는 단계는 PENDING, 현재 단계는 IN_PROGRESS, FILED는 LOCKED로 계산하는 기본 흐름 구현
완료: 상세 완료 조건 연결 전에도 context API가 단계 상태를 반환 가능
검증: 빈 사업연도, 일부 완료, FILED 상태 테스트 추가
```

### T01-07. INFO 완료 조건

```text
goal: [T01-07] INFO 완료 조건을 stepStatus 계산 함수에 연결해줘.
선행: T01-06
작업: financial_statements 존재와 fs_validation_result PASS 여부 반영
완료: 재무정보가 없거나 검증 실패면 INFO가 완료로 표시되지 않음
검증: 재무제표 없음, 검증 실패, 검증 통과 케이스 테스트
```

### T01-08. ADJUST 완료 조건

```text
goal: [T01-08] ADJUST 완료 조건을 stepStatus 계산 함수에 연결해줘.
선행: T01-06
작업: 필수 모듈 B-1, B-15 저장 완료와 모듈 오류 상태 반영
완료: 필수 모듈 저장 완료 전에는 ADJUST가 완료되지 않음
검증: 필수 모듈 미완료, 완료, 오류 경고 케이스 테스트
```

### T01-09. FORM 완료 조건

```text
goal: [T01-09] FORM 완료 조건을 stepStatus 계산 함수에 연결해줘.
선행: T01-06
작업: FORM3 생성 완료, form_validation_errors 0건 여부 반영
완료: 주요 서식이 없거나 오류가 있으면 FORM 완료 차단
검증: FORM3 없음, 오류 있음, 오류 없음 테스트
```

### T01-10. VALIDATE 완료 조건

```text
goal: [T01-10] VALIDATE 완료 조건을 stepStatus 계산 함수에 연결해줘.
선행: T01-06
작업: 최신 validation_run의 ERROR 0건 여부 반영
완료: 최신 검증이 없거나 ERROR가 있으면 결재 단계 진입 불가 상태로 계산
검증: 검증 전, ERROR 있음, ERROR 0건 테스트
```

### T01-11. APPROVE 완료 조건

```text
goal: [T01-11] APPROVE 완료 조건을 stepStatus 계산 함수에 연결해줘.
선행: T01-06
작업: approval_lines 최종 승인과 business_years.status APPROVED 여부 반영
완료: 승인 완료 후 출력 단계 진입 가능 상태가 계산됨
검증: 요청 전, 승인 대기, 반려, 최종 승인 테스트
```

### T01-12. PRINT/EFILE 완료 조건

```text
goal: [T01-12] PRINT와 EFILE 완료 조건을 stepStatus 계산 함수에 연결해줘.
선행: T01-06
작업: 출력 이력 존재, efiling_history ACCEPTED 상태 반영
완료: PDF 생성과 접수 완료가 각각 진행률과 stepStatus에 반영됨
검증: 출력 이력 없음/있음, GENERATED/SUBMITTED/ACCEPTED 테스트
```

### T01-13. 전체 진행률 계산

```text
goal: [T01-13] 세부구현_신고_v1.0.md 11.3 기준으로 전체 진행률 계산 함수를 구현해줘.
선행: T01-07~T01-12
작업: INFO 20, ADJUST 30, FORM 20, VALIDATE 10, APPROVE 10, EFILE 10 가중치 적용
완료: 작업 목록과 context API가 같은 진행률 계산을 재사용
검증: 단계 조합별 진행률 테스트 추가
```

### T01-14. 법령 스냅샷 잠금 규칙

```text
goal: [T01-14] FILED 전환 시 by_law_snapshot와 신고 데이터 잠금 규칙을 정리해줘.
선행: T01-03
작업: FILED 상태에서 snapshot, form_data, tax_adjustments 수정 차단 기준 구현
완료: FILED 이후 계산 기준과 수정 차단 기준이 코드로 표현됨
검증: FILED 전환 후 snapshot 잠금 테스트 추가
```

---

## T02. 공통 백엔드 API 기반

### T02-01. workspace context 조회 헬퍼

```text
goal: [T02-01] byId 기준 workspace context 조회 공통 헬퍼를 구현해줘.
선행: T00-06
작업: tenant, customer, business_year, snapshot, status를 중복 쿼리 없이 조회
완료: 신고작업 API가 같은 context 헬퍼를 사용할 수 있음
검증: 정상, 없음, 다른 tenant 접근 테스트 추가
```

### T02-02. tenant/customer 격리 헬퍼

```text
goal: [T02-02] 신고작업 API의 tenant/customer/business_year 격리 검증을 공통화해줘.
선행: T02-01
작업: 다른 tenant byId, 권한 없는 customer, 삭제/비활성 고객사 케이스 정책 정의
완료: 격리 실패 응답이 API마다 일관됨
검증: 다른 tenant, 다른 customer, 권한 없는 customer 테스트
```

### T02-03. 역할 권한 헬퍼

```text
goal: [T02-03] 세부구현_신고_v1.0.md 14장 기준 read/write/approve/submit 권한 헬퍼를 구현해줘.
선행: T00-07, T02-01
작업: TAX_EXPERT, TAX_REVIEWER, ASSISTANT, VIEWER의 단계별 권한 매핑 구현
완료: API가 역할별 권한을 공통 방식으로 검사
검증: 역할별 허용/차단 통합 테스트 추가
```

### T02-04. FILED 쓰기 잠금 헬퍼

```text
goal: [T02-04] FILED 이후 모든 쓰기 API에서 재사용할 잠금 헬퍼를 구현해줘.
선행: T00-08, T01-14
작업: FILED와 잠긴 snapshot 상태를 확인하고 지정 오류 코드로 차단
완료: 수정 API가 같은 오류 코드와 메시지로 차단 가능
검증: 대표 저장 API 2개 이상에서 FILED 차단 테스트
```

### T02-05. 공통 에러 응답

```text
goal: [T02-05] 세부구현_신고_v1.0.md 13.2 기준 공통 에러 응답을 정리해줘.
선행: T02-02, T02-03, T02-04
작업: VALIDATION_REQUIRED, LOCKED_FILED, FORBIDDEN, NOT_FOUND, CONFLICT 응답 형식 통일
완료: 대표 오류 응답 JSON이 문서와 맞음
검증: 오류 응답 스냅샷 테스트 추가
```

### T02-06. 낙관적 동시성 기준

```text
goal: [T02-06] 신고작업 저장 API의 updated_at 기반 충돌 처리 기준을 구현해줘.
선행: T02-05
작업: 오래된 updated_at 또는 revision 저장 시 CONFLICT 반환 패턴 확정
완료: 이후 저장 API가 같은 충돌 처리 방식을 재사용 가능
검증: 대표 저장 API 1개 이상에서 충돌 테스트 추가
```

### T02-07. 감사 로그 인터페이스

```text
goal: [T02-07] 신고작업 주요 변경 이벤트 감사 로그 인터페이스를 정리해줘.
선행: T02-05
작업: 생성, 저장, 검증, 결재, 출력, 전자신고, 수정신고 이벤트 payload 기준 정의
완료: 대표 이벤트가 감사 로그로 남는 경로가 있음
검증: 대표 이벤트 audit hook 호출 테스트 추가
```

### T02-08. context API

```text
goal: [T02-08] GET /api/workspace/{byId}/context API를 구현 또는 보강해줘.
선행: T01-06~T01-13, T02-01
작업: 고객사, 사업연도, 상태, 진행률, 법령 스냅샷, stepStatus, 잠금 상태 반환
완료: 프론트 스테퍼와 컨텍스트 배너가 이 응답만으로 렌더링 가능
검증: 정상, 권한 없음, 다른 tenant, FILED 케이스 통합 테스트
```

### T02-09. 작업 목록 요약 쿼리

```text
goal: [T02-09] GET /api/workspace/list에서 사용할 작업 목록 요약 쿼리를 구현해줘.
선행: T01-13, T02-02
작업: 고객사, 사업연도, 상태, 진행률, 마감일, 담당자, 최근 작업 시각 반환
완료: 단계 0 작업 시작 화면에 필요한 목록 데이터가 준비됨
검증: tenant 격리, 정렬, 필터, 빈 목록 테스트
```

### T02-10. 작업 목록 API

```text
goal: [T02-10] GET /api/workspace/list API를 구현 또는 보강해줘.
선행: T02-09
작업: status, owner, recent, search 필터와 pagination 기준 적용
완료: 권한 범위 내 고객사/사업연도만 반환됨
검증: 필터별 결과, pagination, 다른 tenant 접근 테스트
```

### T02-11. 공통 DTO 정리

```text
goal: [T02-11] 신고작업 공통 DTO 이름과 JSON 필드 형식을 정리해줘.
선행: T02-08, T02-10
작업: context/list/summary/error 응답 필드명과 null 처리 기준 정리
완료: 프론트 API client가 일관된 응답을 사용할 수 있음
검증: JSON 직렬화 스냅샷 테스트 추가
```

---

## T03. 공통 프론트 기반

### T03-01. route key 상수

```text
goal: [T03-01] 신고작업 단계 0~7과 하위 메뉴의 프론트 route key 상수를 정리해줘.
선행: T00-04
작업: 메뉴 정의, 화면 dispatcher, 테스트가 같은 route key를 쓰도록 정리
완료: route key 중복/누락 위험이 줄어듦
검증: route key 중복/누락 테스트 추가
```

### T03-02. 프론트 context 상태

```text
goal: [T03-02] 선택된 tenant/customer/business_year context 상태 관리를 구현해줘.
선행: T03-01
작업: 메뉴 이동, 새로고침, 직접 URL 진입, 없는 byId 접근에서 상태 복구 또는 차단
완료: 모든 신고작업 화면이 같은 context를 사용
검증: 프론트 테스트와 직접 URL 진입 브라우저 확인
```

### T03-03. context API client

```text
goal: [T03-03] 프론트에서 context API를 호출하고 로딩/오류/권한없음 상태를 처리해줘.
선행: T02-08, T03-02
작업: fetch helper 또는 기존 API client 패턴으로 context 조회 연결
완료: 신고작업 화면 진입 시 context 기반 렌더링 가능
검증: 성공, 403, 404, 네트워크 오류 테스트
```

### T03-04. 공통 작업 헤더

```text
goal: [T03-04] 신고작업 공통 작업 헤더를 구현해줘.
선행: T03-03
작업: 고객사명, 사업연도, 상태, 마감일, 진행률, 마지막 저장 시각 표시
완료: 단계 1~7 화면에서 같은 헤더를 재사용 가능
검증: 프론트 테스트와 브라우저 확인
```

### T03-05. 법령 스냅샷 배너

```text
goal: [T03-05] 법령 스냅샷 배너를 구현해줘.
선행: T03-03
작업: 법인세율, 접대비한도, R&D공제, 서식 버전, 법령 변경 알림 수 표시
완료: snapshot 없음/있음/잠금 상태가 구분 표시됨
검증: 프론트 테스트와 브라우저 확인
```

### T03-06. 공통 스테퍼 DOM

```text
goal: [T03-06] 단계 1~7 공통 스테퍼 DOM을 구현해줘.
선행: T01-04, T01-05, T03-03
작업: 완료, 작업중, 경고, 대기, 잠금 상태와 접근 가능한 단계 클릭 이동 구현
완료: context stepStatus만으로 스테퍼 표시가 결정됨
검증: 상태별 렌더링 테스트와 브라우저 확인
```

### T03-07. 스테퍼 이동 가드

```text
goal: [T03-07] 스테퍼 클릭 이동 시 상태/권한 기반 진입 가드를 구현해줘.
선행: T03-06
작업: 대기/진입불가/권한없음 단계 클릭 차단과 안내 메시지 표시
완료: 프론트 이동이 백엔드 규칙과 어긋나지 않음
검증: 권한별/상태별 이동 테스트
```

### T03-08. 공통 읽기 전용 모드

```text
goal: [T03-08] 신고작업 화면의 읽기 전용 모드 공통 처리 방식을 구현해줘.
선행: T03-03, T02-04
작업: IN_REVIEW, APPROVED, FILED 상태별 버튼 비활성화, 입력 잠금, 안내 표시
완료: 각 화면에서 같은 readOnly 플래그를 재사용 가능
검증: 읽기 전용 화면 테스트와 브라우저 확인
```

### T03-09. 공통 저장 상태 표시

```text
goal: [T03-09] 신고작업 저장 버튼, 저장 중, 저장 완료, 저장 실패, 충돌 메시지 공통 표시를 구현해줘.
선행: T02-06, T03-08
작업: 저장 상태 label, 오류 alert, 재시도 동작, 최신 데이터 새로고침 연결
완료: 이후 입력 화면들이 같은 저장 UX를 사용할 수 있음
검증: 성공/실패/충돌 프론트 테스트
```

### T03-10. 공통 표/그리드 편집 패턴

```text
goal: [T03-10] 신고작업의 표/그리드 편집 공통 패턴을 정리해줘.
선행: T03-08, T03-09
작업: 행 추가/삭제, 숫자 입력, 금액 포맷, 붙여넣기, 검증 오류 표시 기준 정리
완료: 세무정보, 조정, 서식 화면에서 재사용 가능한 패턴이 생김
검증: 대표 그리드 테스트 추가
```

### T03-11. 공통 빈 상태와 오류 상태

```text
goal: [T03-11] 신고작업 공통 빈 상태와 오류 상태 UI를 구현해줘.
선행: T03-03
작업: 데이터 없음, 권한 없음, 아직 생성 전, 서버 오류, 네트워크 오류 상태 구분
완료: 이후 화면이 같은 상태 컴포넌트 또는 렌더링 패턴을 사용
검증: 상태별 프론트 테스트
```

### T03-12. 공통 브라우저 smoke

```text
goal: [T03-12] 공통 헤더, 배너, 스테퍼, readOnly 상태가 실제 브라우저에서 깨지지 않는지 smoke 검증해줘.
선행: T03-04~T03-11
작업: 정상 DRAFT, 권한 없음, FILED fixture로 메뉴 진입과 핵심 DOM 확인
완료: 공통 UI 변경의 브라우저 기준선이 확보됨
검증: 브라우저 확인 결과와 스크린샷 또는 관찰 메모 기록
```

---

## T04. 단계 0 작업 시작

### T04-01. 단계 0 데이터 모델 gap

```text
goal: [T04-01] 단계 0 작업 시작 화면에 필요한 DB/API 데이터 gap을 정리해줘.
선행: T02-10, T03-01
작업: 고객사, 사업연도, 상태, 진행률, 마감일, 최근 작업, 담당자, 법령 스냅샷 필드 확인
완료: 추가 migration 또는 쿼리 보강 필요성이 명확해짐
검증: T00 추적표와 실제 schema를 대조
```

### T04-02. 작업 목록 필터 API 보강

```text
goal: [T04-02] 단계 0 작업 목록 API의 검색/필터/정렬을 보강해줘.
선행: T02-10, T04-01
작업: 고객사 검색, 내 담당, 최근 작업, 상태, 마감일 정렬 파라미터 처리
완료: 프론트 필터 UI가 API만으로 동작 가능
검증: 필터 조합, pagination, tenant 격리 테스트
```

### T04-03. 작업 목록 화면 shell

```text
goal: [T04-03] 단계 0 작업 시작 화면의 기본 레이아웃과 목록 shell을 구현해줘.
선행: T03-11, T04-02
작업: 검색 영역, 필터, 목록 테이블/카드, 선택 영역, 주요 버튼 배치
완료: API 데이터 없이도 로딩/빈/오류/샘플 상태 렌더링 가능
검증: 프론트 테스트와 브라우저 확인
```

### T04-04. 작업 목록 API 연결

```text
goal: [T04-04] 단계 0 작업 목록 화면을 실제 API와 연결해줘.
선행: T04-03
작업: 검색/필터 입력값과 API query 연결, 결과 렌더링, pagination 또는 더보기 처리
완료: 권한 범위 내 작업 목록만 표시됨
검증: 프론트 테스트와 브라우저에서 필터 동작 확인
```

### T04-05. 사업연도 생성 요청 모델

```text
goal: [T04-05] POST /api/business-years 요청/응답 모델을 세부구현_신고_v1.0.md 3.2 기준으로 정리해줘.
선행: T04-01
작업: 고객사, 사업연도, 시작일, 종료일, 신고기한, 신고유형, 중복 방지 조건 정의
완료: API handler와 프론트 모달이 같은 모델을 사용 가능
검증: request validation 테스트 추가
```

### T04-06. 법령 스냅샷 선택 로직

```text
goal: [T04-06] 사업연도 종료일 기준 by_law_snapshot 생성 로직을 구현 또는 보강해줘.
선행: T04-05
작업: effective_from 기준 max version 선택, 서식 버전 세트 선택, 누락 법령 오류 처리
완료: 새 사업연도 생성 시 snapshot이 자동 생성됨
검증: 연도별 version 선택, 누락 version 오류 테스트
```

### T04-07. 사업연도 생성 API

```text
goal: [T04-07] POST /api/business-years API를 단계 0 신규 생성 흐름에 맞게 완성해줘.
선행: T04-05, T04-06, T02-03
작업: 권한 검사, 중복 연도 방지, DRAFT 생성, snapshot 생성, audit log 연결
완료: 생성 후 프론트가 새 byId로 단계 1에 진입 가능
검증: 성공, 중복, 권한 없음, snapshot 누락 테스트
```

### T04-08. 신규 사업연도 모달

```text
goal: [T04-08] 단계 0 신규 사업연도 만들기 모달을 구현해줘.
선행: T04-07
작업: 입력 validation, 법령 스냅샷 미리보기, 생성 중/실패/성공 상태, 생성 후 이동 처리
완료: 사용자 입력으로 새 사업연도를 만들 수 있음
검증: 프론트 테스트와 브라우저 생성 흐름 확인
```

### T04-09. 고객사 등록 진입

```text
goal: [T04-09] 단계 0의 고객사 등록 버튼과 기존 고객사 관리 흐름을 연결해줘.
선행: T04-03
작업: 권한별 버튼 표시, 기존 등록 화면 이동, 등록 후 작업 시작 복귀 가능성 확인
완료: 단계 0에서 고객사 없음 상태의 다음 행동이 명확함
검증: 권한별 표시 테스트와 브라우저 확인
```

### T04-10. 단계 0 권한과 FILED 표시

```text
goal: [T04-10] 단계 0 목록에서 역할별 표시와 FILED 사업연도 표시를 구현해줘.
선행: T04-04, T03-08
작업: VIEWER 조회 전용, ASSISTANT 제한, FILED 완료 표시, AMENDED 재작업 표시
완료: 목록에서 진입 가능/조회 가능/불가 상태가 구분됨
검증: 역할별 프론트 테스트와 브라우저 확인
```

### T04-11. 단계 0 회귀 검증

```text
goal: [T04-11] 단계 0 작업 시작 전체 흐름을 회귀 검증해줘.
선행: T04-01~T04-10
작업: 목록 조회, 검색, 필터, 선택, 신규 생성, 단계 1 이동, 권한 없음, 빈 목록 확인
완료: 단계 0을 실제 업무 시작점으로 사용할 수 있음
검증: 관련 Rust/frontend 테스트와 브라우저 smoke 실행
```

---

## T05. 단계 1 세무정보 입력

### T05-01. 세무정보 DB gap 정리

```text
goal: [T05-01] 단계 1 세무정보 입력에 필요한 DB gap을 정리해줘.
선행: T00-02
작업: financial_statements, fs_lines, account_mappings, assets, depreciation, transactions, car logs 테이블 확인
완료: 필요한 migration, 모델 보강, 기존 테이블 재사용 여부가 정리됨
검증: 원문 4.6 관련 테이블과 실제 schema 대조
```

### T05-02. 재무제표 조회 DTO/API

```text
goal: [T05-02] GET /api/workspace/{byId}/financial-statements API를 구현 또는 보강해줘.
선행: T05-01, T02-03
작업: BS/IS/CF/자본변동 탭별 요약과 행 데이터를 반환
완료: 단계 1 재무제표 화면이 조회 데이터를 렌더링 가능
검증: 정상, 빈 데이터, 권한 없음, 다른 tenant 테스트
```

### T05-03. 재무제표 수동 저장 API

```text
goal: [T05-03] 재무제표 행 데이터를 수동 저장하는 API를 구현해줘.
선행: T05-02, T02-04, T02-06
작업: 행 validation, 금액 합계, updated_at 충돌, FILED 잠금 처리
완료: 화면에서 직접 수정한 재무제표를 저장 가능
검증: 성공, validation 실패, 충돌, FILED 차단 테스트
```

### T05-04. 재무제표 import 파서 골격

```text
goal: [T05-04] Excel/CSV 재무제표 import 파서 골격을 구현해줘.
선행: T05-01
작업: 템플릿 헤더 판별, 시트/구분/계정/금액 파싱, 행 단위 오류 구조 정의
완료: 실제 저장 전 dry-run 결과를 반환 가능
검증: 정상 파일, 헤더 오류, 금액 오류 parser 테스트
```

### T05-05. 재무제표 import API

```text
goal: [T05-05] POST /api/workspace/{byId}/financial-statements/import API를 구현해줘.
선행: T05-04, T02-04
작업: multipart 업로드, dry-run/commit 처리, import_batch 상태, 행 오류 반환
완료: 통과 시 financial_statements와 fs_lines가 적재됨
검증: 성공, 실패, FILED 차단, 권한 없음 테스트
```

### T05-06. 재무제표 화면 shell

```text
goal: [T05-06] 단계 1-1 재무제표 화면 shell을 구현해줘.
선행: T03-10, T05-02
작업: 탭, 요약 영역, 그리드, import 이력, 저장 버튼, 오류 표시 배치
완료: 로딩/빈/오류/readOnly 상태가 렌더링됨
검증: 프론트 테스트와 브라우저 확인
```

### T05-07. 재무제표 화면 API 연결

```text
goal: [T05-07] 재무제표 화면을 조회/저장/import API와 연결해줘.
선행: T05-03, T05-05, T05-06
작업: 탭별 데이터 렌더링, 수동 수정 저장, 업로드 결과 표시, NET_INCOME 연동 표시
완료: 1-1 재무제표 업무가 화면에서 동작
검증: 프론트 테스트와 브라우저 업로드 또는 mock 확인
```

### T05-08. 계정과목 매핑 조회 API

```text
goal: [T05-08] GET /api/workspace/{byId}/account-mappings API를 구현해줘.
선행: T05-01, T02-03
작업: 고객사 계정, 표준 계정, 매핑 방식, 자동 매핑률, 미매핑 수 반환
완료: 1-2 계정과목 매핑 화면 데이터 준비
검증: 정상, 미매핑, 권한 없음, 다른 tenant 테스트
```

### T05-09. 계정과목 매핑 저장 API

```text
goal: [T05-09] PUT /api/workspace/{byId}/account-mappings API를 구현해줘.
선행: T05-08, T02-04, T02-06
작업: 수동 매핑 저장, 학습 플래그, 표준 계정 validation, 충돌 처리
완료: 미매핑 계정을 사용자가 매핑하고 저장 가능
검증: 성공, 잘못된 표준계정, 충돌, FILED 차단 테스트
```

### T05-10. 계정과목 매핑 화면

```text
goal: [T05-10] 단계 1-2 계정과목 매핑 화면을 구현해줘.
선행: T05-08, T05-09, T03-10
작업: 자동 매핑률, 미매핑 필터, 표준계정 선택, 전체 저장, 오류 표시
완료: API 데이터로 매핑 조회/수정/저장이 가능
검증: 프론트 테스트와 브라우저 확인
```

### T05-11. 자산 조회 API

```text
goal: [T05-11] GET /api/workspace/{byId}/assets API를 구현해줘.
선행: T05-01, T02-03
작업: 자산명, 유형, 취득일, 취득가액, 내용연수, 회사상각, 세법한도, 초과액 반환
완료: 1-3 자산/감가상각 화면 데이터 준비
검증: 정상, 빈 데이터, 권한 없음 테스트
```

### T05-12. 자산 저장 API

```text
goal: [T05-12] PUT /api/workspace/{byId}/assets API를 구현해줘.
선행: T05-11, T02-04, T02-06
작업: 자산 행 validation, 추가/수정/삭제, depreciation 재계산 트리거 연결
완료: 자산대장 변경을 저장 가능
검증: 성공, validation 실패, 충돌, FILED 차단 테스트
```

### T05-13. 감가상각 계산 헬퍼

```text
goal: [T05-13] 자산별 세법상 감가상각 한도 계산 헬퍼를 구현해줘.
선행: T05-11
작업: 정액법/정률법, 취득일, 내용연수, 사업월수, 즉시상각의제 기본 규칙 반영
완료: B-4 감가상각 조정에서 재사용 가능한 계산 결과가 생성됨
검증: 계산 케이스 단위 테스트 추가
```

### T05-14. 자산/감가상각 화면

```text
goal: [T05-14] 단계 1-3 자산 및 감가상각 화면을 구현해줘.
선행: T05-12, T05-13, T03-10
작업: 자산 추가, Excel import 진입, 분류 필터, 한도초과 경고, B-4 이동 버튼 구현
완료: 자산대장을 조회/수정하고 초과액을 확인 가능
검증: 프론트 테스트와 브라우저 확인
```

### T05-15. 거래 명세 조회 API

```text
goal: [T05-15] GET /api/workspace/{byId}/transactions API를 구현해줘.
선행: T05-01, T02-03
작업: 기부금, 접대비, 채권, 지급이자 거래 유형별 조회와 필터 지원
완료: 1-4 거래 명세 화면과 조정 모듈 원천 데이터 준비
검증: 유형별 조회, 빈 데이터, 권한 없음 테스트
```

### T05-16. 거래 명세 저장 API

```text
goal: [T05-16] PUT /api/workspace/{byId}/transactions API를 구현해줘.
선행: T05-15, T02-04, T02-06
작업: 거래 유형별 필수값, 금액 validation, 증빙 여부, 조정 모듈 재계산 플래그 처리
완료: 거래 명세를 화면에서 저장 가능
검증: 성공, validation 실패, FILED 차단 테스트
```

### T05-17. 거래 명세 화면

```text
goal: [T05-17] 단계 1-4 거래 명세 화면을 구현해줘.
선행: T05-15, T05-16, T03-10
작업: 거래 유형 탭, 필터, 행 추가/삭제, 증빙 상태, 저장, 관련 조정 이동 버튼 구현
완료: 기부금/접대비/채권/지급이자 원천 데이터를 관리 가능
검증: 프론트 테스트와 브라우저 확인
```

### T05-18. 업무용승용차 원천자료 API

```text
goal: [T05-18] 업무용승용차 월별 운행기록 API를 구현해줘.
선행: T05-01, T02-03
작업: 차량별 월별 총주행/업무주행/비율/관련비용 조회와 저장
완료: B-10 업무용승용차 조정에서 사용할 원천자료 준비
검증: 조회, 저장, 비율 계산, FILED 차단 테스트
```

### T05-19. 업무용승용차 화면

```text
goal: [T05-19] 단계 1-5 업무용승용차 운행기록 화면을 구현해줘.
선행: T05-18, T03-10
작업: 차량 선택, 월별 운행기록 그리드, 업무사용비율 계산, B-10 이동 버튼 구현
완료: 업무용승용차 원천자료를 입력하고 계산 결과를 확인 가능
검증: 프론트 테스트와 브라우저 확인
```

### T05-20. 단계 1 일관성 검증 엔진

```text
goal: [T05-20] 단계 1 입력 데이터 일관성 검증 엔진을 구현해줘.
선행: T05-02, T05-11, T05-15
작업: BS 자산 합계, IS 매출액, 접대비 거래 합계, 기부금 거래 매칭 규칙 구현
완료: INFO_VALID 화면과 stepStatus에서 사용할 검증 결과 생성 가능
검증: PASS, WARN, ERROR 규칙별 단위 테스트
```

### T05-21. 단계 1 검증 API

```text
goal: [T05-21] POST /api/workspace/{byId}/info/validate API를 구현해줘.
선행: T05-20, T02-03
작업: 일관성 검증 실행, 결과 저장, 최신 결과 조회 기준 정리
완료: INFO 완료 조건에 사용할 fs_validation_result가 갱신됨
검증: 성공, WARN만 있음, ERROR 있음, 권한 없음 테스트
```

### T05-22. 단계 1 검증 화면

```text
goal: [T05-22] 단계 1-9 입력 데이터 일관성 검증 화면을 구현해줘.
선행: T05-21
작업: 검증 실행 버튼, 결과 목록, WARN/ERROR 표시, 관련 화면 이동 버튼 구현
완료: 사용자가 세무조정 전 입력 데이터 상태를 확인 가능
검증: 프론트 테스트와 브라우저 확인
```

### T05-23. INFO stepStatus 연결

```text
goal: [T05-23] 단계 1 저장/검증 결과가 context stepStatus.INFO와 진행률에 반영되게 해줘.
선행: T01-07, T05-21
작업: 재무제표 저장과 검증 실행 후 context 무효화 또는 재조회 연결
완료: INFO 완료 후 단계 2 진입 가능 상태가 표시됨
검증: context API 통합 테스트와 프론트 이동 테스트
```

### T05-24. 단계 1 readOnly 회귀

```text
goal: [T05-24] 단계 1 모든 세무정보 입력 화면의 readOnly/FILED 회귀를 검증해줘.
선행: T05-06~T05-23, T03-08
작업: IN_REVIEW/APPROVED/FILED 상태에서 입력/저장/import 차단과 조회 표시 확인
완료: 단계 1 쓰기 동작이 상태 규칙을 우회하지 않음
검증: 백엔드 잠금 테스트, 프론트 버튼 비활성 테스트, 브라우저 확인
```

---

## T06. 단계 2 세무조정 공통

### T06-01. 세무조정 DB gap 정리

```text
goal: [T06-01] 단계 2 세무조정 17개 모듈에 필요한 DB gap을 정리해줘.
선행: T00-02
작업: tax_adjustments, adjustment_items, reserves, carryforward_loss, 모듈별 원천자료 테이블 확인
완료: 공통 테이블로 처리할 항목과 전용 테이블이 필요한 항목이 구분됨
검증: 원문 5.2, 5.3, 12.2와 실제 schema 대조
```

### T06-02. 모듈 코드 enum/seed

```text
goal: [T06-02] 세부구현_신고_v1.0.md 12.2 기준 tax_adjustments 모듈 코드 enum과 seed를 정리해줘.
선행: T06-01
작업: B01~B17 코드, 표시명, 연계 서식, 필수 여부, 순서 정의
완료: 목록 API와 프론트 모듈 트리가 같은 모듈 정의를 사용
검증: 중복 코드, 누락 코드, 표시 순서 테스트
```

### T06-03. 세무조정 목록 API

```text
goal: [T06-03] GET /api/workspace/{byId}/adjustments API를 구현해줘.
선행: T06-02, T02-03
작업: 17개 모듈 목록, 저장 상태, 오류 상태, 자동연동 여부, readOnly 여부 반환
완료: 단계 2 모듈 트리와 진행 상태 렌더링 가능
검증: 정상, 빈 상태, 권한 없음, FILED 조회 테스트
```

### T06-04. 모듈별 조회 API 공통

```text
goal: [T06-04] GET /api/workspace/{byId}/adjustments/{moduleCode} 공통 조회 구조를 구현해줘.
선행: T06-03
작업: 모듈 header, adjustment_items, 원천자료 요약, 계산 결과, updated_at 반환
완료: 각 모듈 화면이 같은 응답 구조로 시작 가능
검증: 존재/없음/잘못된 moduleCode/권한 없음 테스트
```

### T06-05. 모듈별 저장 API 공통

```text
goal: [T06-05] POST /api/workspace/{byId}/adjustments/{moduleCode}/save 공통 저장 구조를 구현해줘.
선행: T06-04, T02-04, T02-06
작업: header upsert, items 저장, 계산 결과 저장, updated_at 충돌, FILED 차단 처리
완료: 수동 조정 항목 저장과 자동 계산 결과 저장이 가능
검증: 성공, validation 실패, 충돌, FILED 차단 테스트
```

### T06-06. 유보 연계 엔진

```text
goal: [T06-06] 조정 항목 저장 시 reserves를 자동 갱신하는 엔진을 구현해줘.
선행: T06-05
작업: 유보/△유보/기타 처분 구분, 전기말/당기증가/당기추인/기말잔액 계산
완료: 유보 항목이 B-15에서 자동 집계 가능
검증: 신규 유보, 추인, 삭제/수정, 중복 저장 테스트
```

### T06-07. B-1 자동 합산 엔진

```text
goal: [T06-07] 모든 조정 모듈 결과를 B-1 소득금액조정에 자동 합산하는 엔진을 구현해줘.
선행: T06-05, T06-06
작업: 익금산입/손금불산입, 손금산입/익금불산입, 자동/수동 출처 구분
완료: B-1과 FORM3에 연결할 차가감 소득금액 계산 가능
검증: 자동 항목 추가/수정/삭제와 수동 항목 보호 테스트
```

### T06-08. 모듈 트리 화면

```text
goal: [T06-08] 단계 2 세무조정 모듈 트리 화면을 구현해줘.
선행: T06-03, T03-06
작업: 그룹별 17개 모듈, 저장/오류/잠금 상태, 이전/다음 이동, 현재 모듈 표시
완료: 사용자가 모듈별 화면으로 이동 가능
검증: 프론트 테스트와 브라우저 확인
```

### T06-09. 공통 조정 항목 편집 UI

```text
goal: [T06-09] 세무조정 공통 조정 항목 편집 UI를 구현해줘.
선행: T06-04, T06-05, T03-10
작업: 항목명, 금액, 처분, 출처, 자동 여부, 행 추가/삭제, 저장, 검증 버튼 구현
완료: 모듈별 특화 화면이 공통 편집 영역을 재사용 가능
검증: 수동/자동 행, readOnly, 오류 표시 테스트
```

### T06-10. ADJUST stepStatus 연결

```text
goal: [T06-10] 세무조정 저장 결과가 context stepStatus.ADJUST와 진행률에 반영되게 해줘.
선행: T01-08, T06-03, T06-05
작업: B-1/B-15 필수 모듈 저장 여부, 모듈 오류 상태, context 무효화 연결
완료: 필수 모듈 완료 후 단계 3 진입 가능 상태가 표시됨
검증: context API 통합 테스트와 프론트 이동 테스트
```

### T06-11. 세무조정 readOnly 회귀

```text
goal: [T06-11] 단계 2 공통 세무조정 화면과 저장 API의 readOnly/FILED 회귀를 검증해줘.
선행: T06-03~T06-10, T03-08
작업: IN_REVIEW/APPROVED/FILED 상태에서 저장/행추가/import 차단과 조회 표시 확인
완료: 단계 2 공통 쓰기 동작이 상태 규칙을 우회하지 않음
검증: 백엔드 잠금 테스트, 프론트 버튼 비활성 테스트, 브라우저 확인
```

---

## T07. 단계 2 세무조정 모듈별 구현

각 모듈은 `엔진 -> API 보강 -> 화면 -> 연동 회귀` 순서로 진행한다. 같은 패턴이 반복되므로, 완료 보고에는 계산 근거와 B-1/B-15/FORM3 연동 여부를 반드시 남긴다.

### T07-01. B01 소득금액조정 엔진

```text
goal: [T07-01] B01 소득금액조정명세서 엔진을 구현해줘.
선행: T06-07
작업: 결산서 당기순이익, A/B 섹션, 자동/수동 항목, 차가감 소득금액 계산 구현
완료: 다른 모듈 자동 항목과 수동 입력을 함께 계산 가능
검증: 자동 합산, 수동 보호, FORM3 연결값 테스트
```

### T07-02. B01 화면

```text
goal: [T07-02] B01 소득금액조정명세서 화면을 구현해줘.
선행: T07-01, T06-09
작업: A/B 섹션 탭, 소계, 자동연동/수동입력 구분, 저장, FORM3 연동 안내 표시
완료: B01 데이터를 조회/수정/저장 가능
검증: 프론트 테스트와 브라우저 확인
```

### T07-03. B02 기부금 엔진

```text
goal: [T07-03] B02 기부금 조정 엔진을 구현해줘.
선행: T05-15, T06-05
작업: 특례/일반 기부금 한도, 이월 기부금, 한도초과액, B-1 손금산입 연결 계산
완료: 기부금 거래 원천자료로 조정 결과 생성 가능
검증: 전액 손금, 한도초과, 이월 기부금 테스트
```

### T07-04. B02 화면

```text
goal: [T07-04] B02 기부금 조정 화면을 구현해줘.
선행: T07-03, T06-09
작업: 거래 연동 목록, 유형별 한도 계산, 이월/초과 표시, 저장 구현
완료: 사용자가 기부금 조정 결과를 확인하고 저장 가능
검증: 프론트 테스트와 브라우저 확인
```

### T07-05. B03 접대비 엔진

```text
goal: [T07-05] B03 기업업무추진비 조정 엔진을 구현해줘.
선행: T05-15, T06-05
작업: 기본한도, 수입금액 한도, 무증빙 전액 불산입, 한도초과 손금불산입 계산
완료: 접대비 거래 원천자료로 B-1 익금산입 항목 생성 가능
검증: 증빙 있음, 무증빙, 한도초과, 중소기업 여부 테스트
```

### T07-06. B03 화면

```text
goal: [T07-06] B03 기업업무추진비 조정 화면을 구현해줘.
선행: T07-05, T06-09
작업: 한도 계산 요약, 증빙 상태, 초과액, B-1 연동 결과 표시와 저장 구현
완료: 사용자가 접대비 조정 결과를 확인하고 저장 가능
검증: 프론트 테스트와 브라우저 확인
```

### T07-07. B04 감가상각 엔진

```text
goal: [T07-07] B04 감가상각비 조정 엔진을 구현해줘.
선행: T05-13, T06-05
작업: 회사계상액, 세법한도액, 한도초과액, 유보 자동생성, B-1 연동 계산
완료: 자산대장과 depreciation 결과로 조정 항목 생성 가능
검증: 초과 없음, 초과 있음, 즉시상각의제 테스트
```

### T07-08. B04 화면

```text
goal: [T07-08] B04 감가상각비 조정 화면을 구현해줘.
선행: T07-07, T06-09
작업: 자산대장 연동 목록, 한도초과 합계, 유보 표시, 자산대장 이동 버튼 구현
완료: 사용자가 감가상각 조정 결과를 확인하고 저장 가능
검증: 프론트 테스트와 브라우저 확인
```

### T07-09. B05 퇴직급여충당금 엔진/화면

```text
goal: [T07-09] B05 퇴직급여충당금 조정 엔진과 화면을 구현해줘.
선행: T06-05, T06-09
작업: 한도 계산, 한도초과 유보, 전기유보 추인, B-1/B-15 연동 구현
완료: B05 조정 조회/저장/연동이 가능
검증: 계산 테스트, API 테스트, 프론트 테스트와 브라우저 확인
```

### T07-10. B06 대손충당금 엔진/화면

```text
goal: [T07-10] B06 대손충당금 조정 엔진과 화면을 구현해줘.
선행: T06-05, T06-09
작업: 채권 잔액, 설정률, 세법한도, 한도초과 유보, B-1/B-15 연동 구현
완료: B06 조정 조회/저장/연동이 가능
검증: 계산 테스트, API 테스트, 프론트 테스트와 브라우저 확인
```

### T07-11. B07 외화평가 엔진/화면

```text
goal: [T07-11] B07 외화평가 조정 엔진과 화면을 구현해줘.
선행: T06-05, T06-09
작업: 외화자산/부채, 기준환율, 평가손익, 세무조정 항목 생성 구현
완료: B07 조정 조회/저장/연동이 가능
검증: 계산 테스트, API 테스트, 프론트 테스트와 브라우저 확인
```

### T07-12. B08 재고·유가증권 평가 엔진/화면

```text
goal: [T07-12] B08 재고·유가증권 평가 조정 엔진과 화면을 구현해줘.
선행: T06-05, T06-09
작업: 평가방법, 평가손실, 세법 인정 여부, 조정 항목 생성 구현
완료: B08 조정 조회/저장/연동이 가능
검증: 계산 테스트, API 테스트, 프론트 테스트와 브라우저 확인
```

### T07-13. B09 지급이자 엔진/화면

```text
goal: [T07-13] B09 지급이자 손금불산입 엔진과 화면을 구현해줘.
선행: T05-15, T06-05, T06-09
작업: 지급이자 원천자료, 가지급금/업무무관 기준, 손금불산입 계산 구현
완료: B09 조정 조회/저장/연동이 가능
검증: 계산 테스트, API 테스트, 프론트 테스트와 브라우저 확인
```

### T07-14. B10 업무용승용차 엔진/화면

```text
goal: [T07-14] B10 업무용승용차 관련비용 조정 엔진과 화면을 구현해줘.
선행: T05-18, T05-19, T06-05, T06-09
작업: 업무사용비율, 관련비용, 한도초과, 운행기록 없음 WARN, B-1 연동 구현
완료: B10 조정 조회/저장/연동이 가능
검증: 계산 테스트, API 테스트, 프론트 테스트와 브라우저 확인
```

### T07-15. B11 이월결손금 엔진

```text
goal: [T07-15] B11 이월결손금 공제 엔진을 구현해줘.
선행: T06-05
작업: 발생연도, 잔액, 만료연도, 공제한도, 당기공제, 잔여액, 당기만료 알림 계산
완료: B-1 손금산입과 FORM3 이월결손금 항목 연동 가능
검증: 만료 임박, 한도 부족, 전액 공제, 잔액 이월 테스트
```

### T07-16. B11 화면

```text
goal: [T07-16] B11 이월결손금 공제 화면을 구현해줘.
선행: T07-15, T06-09
작업: 발생연도별 목록, 당기공제 입력/자동추천, 만료 알림, 저장 구현
완료: 사용자가 이월결손금 공제액을 확인하고 저장 가능
검증: 프론트 테스트와 브라우저 확인
```

### T07-17. B12 세액공제·감면 엔진/화면

```text
goal: [T07-17] B12 세액공제·감면 엔진과 화면을 구현해줘.
선행: T06-05, T06-09
작업: 산출세액 연동, 공제 유형별 대상금액, 허용 공제액, FORM3 공제·감면 연결 구현
완료: B12 조정 조회/저장/연동이 가능
검증: 계산 테스트, API 테스트, 프론트 테스트와 브라우저 확인
```

### T07-18. B13 최저한세 엔진/화면

```text
goal: [T07-18] B13 최저한세 엔진과 화면을 구현해줘.
선행: T07-17, T06-09
작업: 공제 후 일반세액, 최저한세, 추가 납부세액 계산과 FORM3 연결 구현
완료: B13 조정 조회/저장/연동이 가능
검증: 계산 테스트, API 테스트, 프론트 테스트와 브라우저 확인
```

### T07-19. B14 가산세 엔진/화면

```text
goal: [T07-19] B14 가산세 엔진과 화면을 구현해줘.
선행: T06-05, T06-09
작업: 무신고/과소신고/납부지연/지급명세서 유형, 기준세액, 감면율 계산 구현
완료: B14 조정 조회/저장/FORM3 연동이 가능
검증: 계산 테스트, API 테스트, 프론트 테스트와 브라우저 확인
```

### T07-20. B15 자본금과 적립금 엔진

```text
goal: [T07-20] B15 자본금과 적립금 엔진을 구현해줘.
선행: T06-06
작업: 갑표 자본/잉여금, 을표 reserves 자동 집계, 전기말/당기증감/기말잔액 계산
완료: 모든 유보 항목이 B15 을표에 자동 집계됨
검증: reserves 신규/추인/잔액 불일치 테스트
```

### T07-21. B15 화면

```text
goal: [T07-21] B15 자본금과 적립금 화면을 구현해줘.
선행: T07-20, T06-09
작업: 갑/을 탭, 유보 자동 집계 목록, 수동 수정 제한, 저장/검증 표시 구현
완료: 사용자가 자본금과 적립금 조정 결과를 확인 가능
검증: 프론트 테스트와 브라우저 확인
```

### T07-22. B16 외국법인 엔진/화면

```text
goal: [T07-22] B16 외국법인 세무조정 엔진과 화면을 구현해줘.
선행: T06-05, T06-09
작업: 해당 없음 기본 상태, 외국법인 선택 시 필요한 입력/계산/검증 처리
완료: 일반 내국법인에서는 해당 없음으로 처리되고 필요 시 입력 가능
검증: 해당 없음, 해당 있음, 권한/잠금 테스트와 브라우저 확인
```

### T07-23. B17 연결납세 엔진/화면

```text
goal: [T07-23] B17 연결납세 엔진과 화면을 구현해줘.
선행: T06-05, T06-09
작업: 해당 없음 기본 상태, 연결납세 선택 시 그룹/법인별 입력 구조와 검증 처리
완료: 일반 단독 법인은 해당 없음으로 처리되고 필요 시 입력 가능
검증: 해당 없음, 해당 있음, 권한/잠금 테스트와 브라우저 확인
```

### T07-24. 세무조정 전체 연동 회귀

```text
goal: [T07-24] B01~B17 전체 저장, B-1 합산, reserves, B15, FORM3 연동 회귀를 검증해줘.
선행: T07-01~T07-23, T06-10
작업: 대표 fixture로 필수 모듈과 선택 모듈 저장 후 연동값 비교
완료: 단계 2 전체 흐름이 단계 3 서식 작성으로 넘어갈 수 있음
검증: 통합 테스트, 프론트 모듈 이동 테스트, 브라우저 smoke
```

---

## T08. 단계 3 서식 작성

### T08-01. form 모델 gap 정리

```text
goal: [T08-01] 단계 3 서식 작성에 필요한 form_data/form_relationships/form_validations gap을 정리해줘.
선행: T00-02
작업: FORM3, 부속서식, form_data_history, 수동 수정 이력, 연동 규칙 테이블 확인
완료: migration/model/API 보강 필요성이 정리됨
검증: 원문 6.3, 12.1과 실제 schema 대조
```

### T08-02. 서식 master/version 조회 API

```text
goal: [T08-02] snapshot 기준 서식 master/version 조회 API 또는 헬퍼를 구현해줘.
선행: T08-01, T02-01
작업: 사업연도 snapshot의 서식 버전 세트와 서식 목록을 조회
완료: 부속서식 목록과 자동 생성 API가 같은 version 기준을 사용
검증: snapshot별 서식 목록, 누락 version 테스트
```

### T08-03. FORM3 자동 산출 엔진

```text
goal: [T08-03] 별지 제3호 FORM3 자동 산출 엔진을 구현해줘.
선행: T07-24, T08-02
작업: 당기순이익, 익금산입, 손금산입, 차가감 소득금액, 이월결손금, 세율, 산출세액, 공제, 기납부세액 계산
완료: 세무정보와 세무조정 결과로 FORM3 JSON을 생성 가능
검증: 원문 예시와 대표 계산 케이스 테스트
```

### T08-04. FORM3 조회/저장 API

```text
goal: [T08-04] GET/PUT /api/workspace/{byId}/forms/{formCode} API를 FORM3 기준으로 구현해줘.
선행: T08-03, T02-04, T02-06
작업: 자동 필드 보호, 직접 입력 필드 저장, form_data_history 기록, 충돌 처리
완료: FORM3 조회/수동 수정/이력 기록이 가능
검증: 조회, 저장, 자동필드 수정 차단, FILED 차단 테스트
```

### T08-05. FORM3 화면

```text
goal: [T08-05] 단계 3-1 별지 제3호 화면을 구현해줘.
선행: T08-04, T03-10
작업: 자동연동 필드와 직접입력 필드 구분, 저장, 서식 검증, 미리보기 버튼 표시
완료: 사용자가 FORM3 주요 항목을 확인하고 직접입력 항목을 저장 가능
검증: 프론트 테스트와 브라우저 확인
```

### T08-06. 부속서식 목록 API

```text
goal: [T08-06] GET /api/workspace/{byId}/forms/list API를 구현해줘.
선행: T08-02
작업: 서식코드, 서식명, 생성상태, 검증오류 수, PDF 가능 여부, 수정 가능 여부 반환
완료: 부속서식 일람 화면 데이터 준비
검증: 생성/미생성/오류 상태, 권한 없음 테스트
```

### T08-07. 부속서식 일람 화면

```text
goal: [T08-07] 단계 3-2 부속서식 일람 화면을 구현해줘.
선행: T08-06
작업: 전체 생성, 미생성 필터, 상태/오류 표시, PDF/수정 버튼, ZIP 다운로드 진입 구현
완료: 사용자가 부속서식 생성 현황을 확인 가능
검증: 프론트 테스트와 브라우저 확인
```

### T08-08. 공통 서식 필드 렌더러

```text
goal: [T08-08] JSONB form_data를 화면 필드로 렌더링하는 공통 서식 필드 렌더러를 구현해줘.
선행: T08-04
작업: 금액, 문자, 날짜, 선택값, 자동/수동, 오류 표시, readOnly 처리
완료: 주요 부속서식 화면 구현에 재사용 가능
검증: 필드 타입별 프론트 테스트
```

### T08-09. 서식 자동 생성 API

```text
goal: [T08-09] POST /api/workspace/{byId}/forms/generate API를 구현해줘.
선행: T08-03, T08-06, T02-04
작업: 전체/선택 서식 생성, 기존 수동 수정 보호, 생성 결과 요약 반환
완료: FORM3과 주요 부속서식 form_data가 자동 생성됨
검증: 전체 생성, 선택 생성, 수동 수정 보호, FILED 차단 테스트
```

### T08-10. 주요 부속서식 자동 채움

```text
goal: [T08-10] BJ_15, BJ_22, BJ_23, BJ_20, BJ_08, BJ_50갑/을 주요 부속서식 자동 채움을 구현해줘.
선행: T07-24, T08-09
작업: 각 조정 모듈 결과를 대응 부속서식 JSON으로 변환
완료: 주요 부속서식이 조정 데이터와 일치하게 생성됨
검증: 서식별 매핑 테스트와 form_data 스냅샷 테스트
```

### T08-11. 서식 수동 수정 보호

```text
goal: [T08-11] 서식 재계산 시 수동 수정 필드를 보호하는 정책을 구현해줘.
선행: T08-04, T08-09
작업: 자동 필드 재계산, 수동 필드 유지, 충돌 또는 덮어쓰기 확인 처리
완료: 사용자의 직접 입력이 자동 생성으로 사라지지 않음
검증: 재생성 전후 수동값 유지 테스트
```

### T08-12. form_relationships 조회 API

```text
goal: [T08-12] form_relationships 규칙 조회 API 또는 헬퍼를 구현해줘.
선행: T08-01
작업: 원천서식, 대상서식, 필드, 집계방식, severity 기준 조회
완료: 서식 간 연동 검증 엔진이 규칙을 재사용 가능
검증: 규칙 조회와 누락 규칙 테스트
```

### T08-13. 서식 간 연동 검증

```text
goal: [T08-13] 세부구현_신고_v1.0.md 6.2 기준 서식 간 연동 검증을 구현해줘.
선행: T08-12, T08-10
작업: BJ_03.② = BJ_15.A, BJ_03.③ = BJ_15.B, BJ_03.⑤ = BJ_22, BJ_03.⑦ = BJ_08 규칙 구현
완료: 불일치 시 validation result로 등록 가능
검증: 일치/불일치 케이스 테스트
```

### T08-14. FORM stepStatus 연결

```text
goal: [T08-14] 서식 생성/검증 결과가 context stepStatus.FORM과 진행률에 반영되게 해줘.
선행: T01-09, T08-09, T08-13
작업: FORM3 생성 완료, form validation error 0건, context 무효화 연결
완료: FORM 완료 후 단계 4 검증 진입 가능 상태가 표시됨
검증: context API 통합 테스트와 프론트 이동 테스트
```

### T08-15. 단계 3 readOnly 회귀

```text
goal: [T08-15] 단계 3 서식 작성 화면과 API의 readOnly/FILED 회귀를 검증해줘.
선행: T08-05~T08-14, T03-08
작업: IN_REVIEW/APPROVED/FILED 상태에서 저장/재생성 차단과 조회 표시 확인
완료: 단계 3 쓰기 동작이 상태 규칙을 우회하지 않음
검증: 백엔드 잠금 테스트, 프론트 버튼 비활성 테스트, 브라우저 확인
```

---

## T09. 단계 4 검증

### T09-01. validation 모델 보강

```text
goal: [T09-01] validation_runs와 validation_results 모델을 세부구현_신고_v1.0.md 7장 기준으로 구현 또는 보강해줘.
선행: T00-02
작업: run scope, severity, area, message, detail, targetFormCode, targetField, jumpUrl 저장 구조 확인
완료: 검증 실행 이력과 결과를 저장 가능
검증: migration/model 테스트
```

### T09-02. 검증 규칙 registry

```text
goal: [T09-02] FORM/RESERVE/EFILE_SPEC/LAW_CHANGE 검증 규칙 registry를 구현해줘.
선행: T09-01
작업: 규칙 코드, 영역, severity, 활성 여부, 실행 함수 연결 구조 정의
완료: 검증 실행 API가 scope별 규칙을 선택 실행 가능
검증: registry 등록/필터 테스트
```

### T09-03. 핵심 FORM 검증 규칙

```text
goal: [T09-03] 핵심 FORM 검증 규칙을 구현해줘.
선행: T08-13, T09-02
작업: 필수 항목 누락, 서식 간 합계 불일치, FORM3 납부세액 계산 오류 검증
완료: ERROR가 있어야 하는 서식 오류를 잡을 수 있음
검증: 규칙별 오류/정상 테스트
```

### T09-04. 핵심 RESERVE 검증 규칙

```text
goal: [T09-04] 핵심 RESERVE 검증 규칙을 구현해줘.
선행: T07-20, T09-02
작업: 전기말 유보잔액 불일치, 기말잔액 계산 오류, B15 집계 불일치 검증
완료: 유보 관련 ERROR/WARN을 검출 가능
검증: 규칙별 오류/정상 테스트
```

### T09-05. 법령 변경 WARN 규칙

```text
goal: [T09-05] 법령 변경 또는 재계산 권장 WARN 규칙을 구현해줘.
선행: T03-05, T09-02
작업: snapshot 이후 새 법령 version 존재 여부와 영향 영역 표시
완료: 재계산 권장 WARN을 검증 결과에 표시 가능
검증: 새 version 있음/없음 테스트
```

### T09-06. 검증 실행 API

```text
goal: [T09-06] POST /api/workspace/{byId}/validate API를 구현해줘.
선행: T09-02~T09-05, T02-03
작업: scope별 규칙 실행, 결과 저장, summary 반환, context 무효화
완료: 일괄 검증 결과가 저장되고 ERROR 0건 여부를 판단 가능
검증: ERROR 있음/없음, 권한 없음, FILED 조회 테스트
```

### T09-07. 최신 검증 결과 API

```text
goal: [T09-07] GET /api/workspace/{byId}/validate/latest API를 구현해줘.
선행: T09-06
작업: 최신 run, summary, 결과 목록, 실행 전 빈 상태 반환
완료: 검증 화면과 결재 요청 화면이 같은 최신 결과를 사용
검증: 실행 전/후 최신 결과 테스트
```

### T09-08. 검증 실행 화면

```text
goal: [T09-08] 단계 4 검증 실행 화면을 구현해줘.
선행: T09-06, T09-07
작업: 일괄 검증 실행 버튼, 최종 실행 시각, summary, 결재 가능 상태 표시
완료: 사용자가 검증을 실행하고 전체 결과를 확인 가능
검증: 프론트 테스트와 브라우저 확인
```

### T09-09. 검증 이슈 목록 화면

```text
goal: [T09-09] 단계 4 검증 이슈 목록 화면을 구현해줘.
선행: T09-07
작업: ERROR/WARN/INFO 필터, 영역, 메시지, 위치, 이동 버튼, 재계산 버튼 표시
완료: 사용자가 검증 이슈를 필터링하고 원인 화면으로 이동 가능
검증: 프론트 테스트와 브라우저 확인
```

### T09-10. 검증 규칙 목록 화면

```text
goal: [T09-10] 단계 4 검증 규칙 목록 화면을 구현해줘.
선행: T09-02
작업: 규칙 코드, 영역, severity, 설명, 활성 여부, 마지막 실행 결과 표시
완료: 사용자가 어떤 규칙이 실행되는지 확인 가능
검증: 프론트 테스트와 브라우저 확인
```

### T09-11. VALIDATE stepStatus 연결

```text
goal: [T09-11] 검증 ERROR 0건일 때 stepStatus.VALIDATE와 결재 요청 가능 상태를 연결해줘.
선행: T01-10, T09-06
작업: ERROR가 있으면 단계 5 진입과 결재 요청 API를 백엔드에서 차단
완료: 검증 통과 전 결재가 불가능함
검증: ERROR 있음/없음 결재 요청 차단 테스트
```

---

## T10. 단계 5 결재 / 승인

### T10-01. approval_lines 모델

```text
goal: [T10-01] approval_lines 모델과 상태값을 구현 또는 보강해줘.
선행: T00-02
작업: 요청자, 검토자, 순번, 상태, 요청일, 처리일, 반려 사유, memo 저장 구조 확인
완료: 결재 라인과 처리 이력을 저장 가능
검증: migration/model 테스트
```

### T10-02. 결재 요청 가능 조건

```text
goal: [T10-02] 결재 요청 전 검증 통과, 권한, 중복 요청 조건을 백엔드에서 강제해줘.
선행: T09-11, T10-01, T02-03
작업: ERROR 있음, 권한 없음, 이미 IN_REVIEW, 검토자 부적합 케이스 차단
완료: 결재 요청 API가 사전 조건 없이 호출돼도 안전함
검증: 조건별 차단 테스트
```

### T10-03. 결재 요청 API

```text
goal: [T10-03] POST /api/workspace/{byId}/approval/request API를 완성해줘.
선행: T10-02, T02-07
작업: 검토자 선택, 결재 라인 생성, IN_REVIEW 전환, 편집 잠금, 알림/감사 로그 연결
완료: 작성자가 결재 요청을 생성 가능
검증: 요청 성공, 중복 요청, ERROR 차단, 권한 없음 테스트
```

### T10-04. 작성자 결재 요청 화면

```text
goal: [T10-04] 단계 5 작성자 결재 요청 화면을 구현해줘.
선행: T09-07, T10-03
작업: 검증 요약, 검토자 선택, 추가 검토자, 요청 memo, 첨부 진입, 요청 버튼 구현
완료: 사용자가 화면에서 결재 요청을 보낼 수 있음
검증: 프론트 테스트와 브라우저 확인
```

### T10-05. 검토자 결재함 API

```text
goal: [T10-05] 검토자 결재함 조회 API를 구현해줘.
선행: T10-01, T02-03
작업: 내 결재 대기 목록, 사업연도 요약, 검증 요약, 요청자, 요청일, 상태 필터 반환
완료: 검토자가 자기 결재 대상만 조회 가능
검증: 검토자별 목록 격리, 다른 tenant 차단 테스트
```

### T10-06. 승인 API

```text
goal: [T10-06] POST /api/workspace/{byId}/approval/{lineId}/approve API를 구현해줘.
선행: T10-05, T02-03, T01-03
작업: 검토자 권한, 순번 검증, line 상태 갱신, 최종 승인 시 APPROVED 전환
완료: 승인 처리와 상태 전이가 가능
검증: 권한 없음, 순번 오류, 최종 승인 테스트
```

### T10-07. 반려 API

```text
goal: [T10-07] POST /api/workspace/{byId}/approval/{lineId}/reject API를 구현해줘.
선행: T10-05, T02-03, T01-03
작업: 반려 사유 필수, line 상태 갱신, business_year DRAFT 복귀, 작성자 알림
완료: 반려 후 작성자가 다시 수정 가능
검증: 사유 없음 차단, 상태 복귀, 알림/감사 로그 테스트
```

### T10-08. 검토자 결재함 화면

```text
goal: [T10-08] 검토자 결재함과 승인/반려 화면을 구현해줘.
선행: T10-05~T10-07
작업: 목록, 상세 요약, 세무조정/서식/변경이력/첨부 이동, 승인, 반려, 의견 입력 구현
완료: 검토자가 화면에서 승인/반려 처리 가능
검증: 프론트 테스트와 브라우저 확인
```

### T10-09. 반려 재작업 화면

```text
goal: [T10-09] 반려된 신고작업의 재작업 진입 화면을 구현해줘.
선행: T10-07
작업: 반려 사유, 수정 필요 영역 이동, 재검증/재요청 안내, DRAFT 편집 가능 표시
완료: 작성자가 반려 사유를 보고 재작업 가능
검증: 프론트 테스트와 브라우저 확인
```

### T10-10. 결재 알림과 감사 로그

```text
goal: [T10-10] 결재 요청/승인/반려 이벤트의 알림과 감사 로그를 연결해줘.
선행: T10-03, T10-06, T10-07
작업: 이벤트별 수신자, 메시지, payload, 링크 기준 구현
완료: 결재 상태 변경이 알림과 감사 로그로 추적됨
검증: 이벤트별 알림/로그 테스트
```

### T10-11. APPROVE stepStatus 연결

```text
goal: [T10-11] 승인 완료 상태가 context stepStatus.APPROVE와 진행률에 반영되게 해줘.
선행: T01-11, T10-06
작업: 최종 승인 후 출력 단계 진입 가능 상태와 진행률 갱신
완료: APPROVED 상태에서 단계 6 출력으로 이동 가능
검증: context 갱신 통합 테스트와 프론트 이동 테스트
```

---

## T11. 단계 6 출력 PDF

### T11-01. 출력 대상 API

```text
goal: [T11-01] PDF 출력 대상 서식 목록 API를 구현해줘.
선행: T08-06, T10-11
작업: APPROVED/FILED 상태에서 출력 가능한 서식, 주요 서식, 오류 여부, 선택 상태 반환
완료: 출력 화면에서 대상 목록을 렌더링 가능
검증: 상태별 권한, 대상 선택, 오류 서식 테스트
```

### T11-02. PDF 생성 요청/응답 모델

```text
goal: [T11-02] PDF 생성 요청/응답 모델을 정의해줘.
선행: T11-01
작업: 선택 서식, 워터마크, 미리보기/다운로드, ZIP 여부, 생성 이력 응답 형식 정의
완료: API handler와 프론트가 같은 모델을 사용
검증: 요청 validation 테스트
```

### T11-03. 워터마크 매핑

```text
goal: [T11-03] 사업연도 상태별 PDF 워터마크 매핑을 구현해줘.
선행: T11-02, T01-01
작업: DRAFT 작성중, IN_REVIEW 검토중, APPROVED 승인완료, FILED 신고완료, AMENDED 수정신고 매핑
완료: 출력 생성 시 상태별 워터마크가 일관됨
검증: 상태별 매핑 테스트
```

### T11-04. PDF 생성 API

```text
goal: [T11-04] POST /api/workspace/{byId}/print API를 구현해줘.
선행: T11-02, T11-03, T02-03
작업: 서식 데이터 조회, 렌더링 어댑터 호출, 파일 저장, 다운로드 링크, 출력 이력 기록
완료: PDF 또는 ZIP 생성 요청이 처리됨
검증: 성공, 권한 없음, 생성 실패, 빈 대상 테스트
```

### T11-05. 출력 미리보기 화면

```text
goal: [T11-05] 단계 6 출력 미리보기 화면을 구현해줘.
선행: T11-01, T11-04
작업: 출력 대상 선택, 워터마크 선택, 미리보기 생성, 다운로드 버튼 구현
완료: 사용자가 출력 전 대상과 워터마크를 확인 가능
검증: 프론트 테스트와 브라우저 확인
```

### T11-06. 일괄 출력 화면

```text
goal: [T11-06] 전체 서식/주요 서식/사용자 정의 일괄 출력 화면을 구현해줘.
선행: T11-05
작업: 출력 대상 모드, 체크박스, ZIP 다운로드, 생성 중 상태, 실패 표시 구현
완료: 다중 서식을 한 번에 생성/다운로드 가능
검증: 프론트 테스트와 브라우저 확인
```

### T11-07. 출력 이력 API

```text
goal: [T11-07] GET /api/workspace/{byId}/print/history API를 구현해줘.
선행: T11-04
작업: 생성일시, 생성자, 서식 수, 파일 링크, 워터마크, 상태 반환
완료: 출력 이력 화면 데이터 준비
검증: 이력 있음/없음, 권한 없음 테스트
```

### T11-08. 출력 이력 화면

```text
goal: [T11-08] 단계 6 출력 이력 화면을 구현해줘.
선행: T11-07
작업: 이력 테이블, 다운로드, 만료 링크 표시, 생성자/서식 수/생성일시 표시
완료: 사용자가 과거 출력물을 확인 가능
검증: 프론트 테스트와 브라우저 확인
```

### T11-09. PRINT stepStatus 연결

```text
goal: [T11-09] 출력 이력이 context stepStatus.PRINT와 진행률에 반영되게 해줘.
선행: T01-12, T11-04
작업: 출력 성공 후 context 무효화 또는 재조회 연결
완료: 출력 완료 상태가 단계 진행률에 반영됨
검증: context API 통합 테스트와 프론트 이동 테스트
```

---

## T12. 단계 7 전자신고

### T12-01. efiling_history 모델

```text
goal: [T12-01] efiling_history 모델을 세부구현_신고_v1.0.md 10.3 기준으로 구현 또는 보강해줘.
선행: T00-02
작업: file_path, hash, size, schema_version, nts_receipt_no, submission_method, status, amendment 연결 확인
완료: 신고파일 생성부터 접수까지 이력을 저장 가능
검증: migration/model 테스트
```

### T12-02. 전자신고 규격 master

```text
goal: [T12-02] snapshot 기준 전자신고 규격 master 조회 구조를 구현해줘.
선행: T12-01, T08-02
작업: schema_version, record layout, encoding, fixed-width field definition 조회
완료: 사전검증과 파일 생성이 같은 규격을 사용
검증: version 선택, 누락 규격 오류 테스트
```

### T12-03. 전자신고 사전 검증 엔진

```text
goal: [T12-03] 전자신고 사전 검증 엔진을 구현해줘.
선행: T09-02, T12-02
작업: 사업자번호 체크섬, FORM3 납부세액, 필수항목, 고정폭 길이, XML/schema 규칙 검증
완료: 제출 전 오류를 API에서 검출 가능
검증: 규칙별 성공/실패 테스트
```

### T12-04. 전자신고 사전 검증 API

```text
goal: [T12-04] POST /api/workspace/{byId}/efile/validate API를 구현해줘.
선행: T12-03, T02-03
작업: APPROVED 상태 확인, 사전 검증 실행, 결과 저장, summary 반환
완료: 파일 생성 전 사전검증을 실행 가능
검증: 성공, 오류 있음, APPROVED 전 차단, 권한 없음 테스트
```

### T12-05. 고정길이 파일 생성기

```text
goal: [T12-05] 홈택스 법인세 고정길이 신고파일 생성기를 구현해줘.
선행: T12-02, T12-04
작업: EUC-KR 인코딩, 레코드 순서, 필드 padding, 금액/날짜 포맷, SHA-256 계산 구현
완료: form_data 기반 신고파일 bytes와 hash를 생성 가능
검증: 고정폭 길이, 인코딩, hash, 샘플 레코드 테스트
```

### T12-06. 전자신고 파일 생성 API

```text
goal: [T12-06] POST /api/workspace/{byId}/efile/generate API를 구현해줘.
선행: T12-05
작업: 사전검증 통과 확인, 파일 생성, 저장, efiling_history GENERATED 기록, 다운로드 정보 반환
완료: 제출 전 신고파일을 생성하고 다운로드 가능
검증: 성공, 사전검증 미통과, 파일 저장 실패, 권한 없음 테스트
```

### T12-07. 전자신고 4단계 UI shell

```text
goal: [T12-07] 단계 7 전자신고 4단계 Step UI shell을 구현해줘.
선행: T03-06, T12-04, T12-06
작업: 사전검증, 파일생성, 제출, 접수확인 단계 표시와 상태 전환 UI 구성
완료: 전자신고 화면의 전체 흐름이 보임
검증: 프론트 테스트와 브라우저 확인
```

### T12-08. 사전검증/파일생성 화면

```text
goal: [T12-08] 전자신고 사전검증과 파일생성 화면을 API와 연결해줘.
선행: T12-04, T12-06, T12-07
작업: 검증 실행, 결과 표시, 파일 생성, 파일명/크기/hash/다운로드 표시
완료: 사용자가 전자신고 파일 생성 전 과정을 화면에서 수행 가능
검증: 프론트 테스트와 브라우저 확인
```

### T12-09. 2FA Step-up 검증 API

```text
goal: [T12-09] 전자신고 제출 직전 2FA Step-up 검증 API를 구현해줘.
선행: T02-03
작업: TOTP one-time 검증, 재사용 방지, 만료 처리, 실패 횟수 제한, audit log 연결
완료: 제출 API가 유효한 step-up 토큰 없이는 실행되지 않음
검증: 성공, 실패, 만료, 재사용 차단 테스트
```

### T12-10. 전자신고 제출 API

```text
goal: [T12-10] POST /api/workspace/{byId}/efile/submit API를 구현해줘.
선행: T12-06, T12-09, T01-03, T01-14
작업: submit 권한, APPROVED 상태, 파일 생성 여부, 2FA 검증, 제출 방식, 접수 결과 처리
완료: 성공 시 efiling_history ACCEPTED와 business_year FILED 전환이 동작
검증: 성공, 2FA 없음, 권한 없음, 상태 오류, 제출 실패 테스트
```

### T12-11. 제출 화면

```text
goal: [T12-11] 전자신고 제출 화면을 구현해줘.
선행: T12-09, T12-10
작업: 수동 업로드/API 제출 선택, 2FA 입력 모달, 제출 중/성공/실패 표시
완료: 사용자가 제출 전 인증을 거쳐 신고를 완료할 수 있음
검증: 프론트 테스트와 브라우저 확인
```

### T12-12. 접수 확인 API

```text
goal: [T12-12] GET /api/workspace/{byId}/efile/history 또는 접수 확인 API를 구현해줘.
선행: T12-10
작업: 신고파일 이력, 제출 상태, 접수번호, 접수일, 수정신고 여부 반환
완료: 접수 결과 화면 데이터 준비
검증: 이력 있음/없음, 권한 없음, FILED 상태 테스트
```

### T12-13. 접수 확인 화면

```text
goal: [T12-13] 전자신고 접수 확인 화면을 구현해줘.
선행: T12-12
작업: 접수번호, 제출 방식, 제출자, 제출/접수 시각, 파일 hash, 재다운로드 표시
완료: 사용자가 신고 완료 증빙 정보를 확인 가능
검증: 프론트 테스트와 브라우저 확인
```

### T12-14. EFILE stepStatus 연결

```text
goal: [T12-14] 전자신고 ACCEPTED 상태가 context stepStatus.EFILE과 진행률에 반영되게 해줘.
선행: T01-12, T12-10
작업: 접수 완료 후 context 갱신, FILED 잠금 표시, 단계 7 완료 표시 연결
완료: 신고 완료 후 전체 진행률 100%와 잠금 상태가 표시됨
검증: context API 통합 테스트와 프론트 이동 테스트
```

---

## T13. FILED 잠금과 수정신고

### T13-01. FILED 백엔드 전체 회귀표

```text
goal: [T13-01] FILED 이후 수정 차단이 필요한 모든 쓰기 API 목록을 표로 정리해줘.
선행: T02-04, T05~T12 주요 API
작업: 단계 1~7 쓰기 API, import, generate, approve, efile submit, amendment 예외를 구분
완료: 잠금 적용/예외/미적용 API가 명확해짐
검증: 라우터와 테스트 목록 대조
```

### T13-02. FILED 백엔드 회귀 테스트

```text
goal: [T13-02] FILED 이후 쓰기 API 전체 회귀 테스트를 추가해줘.
선행: T13-01, T00-08
작업: 대표 API가 아니라 각 단계의 주요 쓰기 API를 FILED fixture로 호출
완료: FILED 잠금을 우회하는 API가 테스트로 잡힘
검증: 관련 Rust 테스트 실행
```

### T13-03. FILED 프론트 읽기 전용 회귀

```text
goal: [T13-03] FILED 상태에서 단계 1~7 프론트 화면이 읽기 전용으로 보이는지 회귀 테스트해줘.
선행: T03-08, T13-02
작업: 저장/import/generate/submit 버튼 비활성화, 조회 가능, 잠금 배너 표시 확인
완료: FILED 상태 프론트가 백엔드 잠금과 일치
검증: frontend 테스트와 브라우저 smoke
```

### T13-04. 수정신고 생성 요청 모델

```text
goal: [T13-04] 수정신고 생성 요청/응답 모델을 정의해줘.
선행: T12-14
작업: 원본 filed byId, 수정신고 사유, 생성 방식, parent_efiling_id, AMENDED 상태 기준 정의
완료: 수정신고 API와 화면이 같은 모델을 사용 가능
검증: 요청 validation 테스트
```

### T13-05. 수정신고 생성 API

```text
goal: [T13-05] FILED 신고에서 AMENDED 수정신고를 생성하는 API를 구현해줘.
선행: T13-04, T01-03
작업: 원본 FILED 확인, 권한 확인, 새 business_year 또는 amendment record 생성, 상태 전이
완료: 원본 신고를 보존하면서 수정신고 작업을 시작 가능
검증: 성공, FILED 아님, 권한 없음, 중복 수정신고 테스트
```

### T13-06. 수정신고 데이터 복사

```text
goal: [T13-06] 수정신고 생성 시 원본 신고 데이터를 복사하는 로직을 구현해줘.
선행: T13-05
작업: snapshot, 세무정보, 조정, 서식, 검증 결과 기준 데이터 복사와 새 revision 처리
완료: 수정신고가 원본 데이터에서 시작하되 독립적으로 수정 가능
검증: 원본 불변, 복사 데이터 수정 가능, 연결 ID 테스트
```

### T13-07. 수정신고 진입 화면

```text
goal: [T13-07] FILED 사업연도에서 수정신고 시작 화면을 구현해줘.
선행: T13-05, T13-06
작업: 수정신고 버튼, 사유 입력, 생성 확인, 생성 후 AMENDED 작업 진입
완료: 사용자가 화면에서 수정신고를 시작 가능
검증: 프론트 테스트와 브라우저 확인
```

### T13-08. 수정신고 변경분 비교 API

```text
goal: [T13-08] 원본 FILED와 AMENDED 작업의 변경분 비교 API를 구현해줘.
선행: T13-06
작업: 세무정보, 조정, 서식, 납부세액, 전자신고 파일 변경분 요약 반환
완료: 수정신고 제출 전 변경 내역을 검토 가능
검증: 변경 있음/없음, 권한 없음 테스트
```

### T13-09. 수정신고 변경분 화면

```text
goal: [T13-09] 수정신고 변경분 비교 화면을 구현해줘.
선행: T13-08
작업: 원본/수정 값, 변경 사유, 영향 서식, 추가 납부/환급 차이 표시
완료: 사용자가 수정신고 변경 내용을 제출 전 확인 가능
검증: 프론트 테스트와 브라우저 확인
```

---

## T14. 업로드, 첨부, 변경 이력, 운영 품질

### T14-01. Excel 템플릿 목록 API

```text
goal: [T14-01] 신고작업 Excel 템플릿 목록 API를 구현해줘.
선행: T05-04
작업: 재무제표, 자산, 거래, 업무용승용차 템플릿 metadata 반환
완료: 화면에서 표준 템플릿 다운로드 목록 표시 가능
검증: 템플릿 목록, 권한 없음 테스트
```

### T14-02. Excel 템플릿 다운로드 API

```text
goal: [T14-02] 신고작업 Excel 템플릿 다운로드 API를 구현해줘.
선행: T14-01
작업: 템플릿 파일 생성 또는 정적 파일 제공, 파일명/콘텐츠 타입/권한 처리
완료: 사용자가 각 입력 화면에서 표준 템플릿을 받을 수 있음
검증: 다운로드 응답, 없는 템플릿, 권한 없음 테스트
```

### T14-03. Excel 업로드 검증 공통화

```text
goal: [T14-03] 신고작업 Excel/CSV 업로드 검증과 행 오류 응답을 공통화해줘.
선행: T05-05, T14-02
작업: 파일 크기, 확장자, 헤더, 인코딩, 행 오류 형식, dry-run 공통 처리
완료: 재무제표/자산/거래 import가 같은 오류 구조를 사용
검증: 공통 parser 테스트와 대표 API 테스트
```

### T14-04. 첨부 증빙 메타데이터 API

```text
goal: [T14-04] 신고작업 첨부 증빙 메타데이터 API를 구현해줘.
선행: T02-03
작업: 업무 단계, 모듈, 파일명, 크기, 업로더, 연결 ID, 삭제 여부 저장
완료: 결재와 거래 명세에서 첨부파일을 연결 가능
검증: 업로드 메타 저장, 조회, 삭제, 권한 없음 테스트
```

### T14-05. 증빙 첨부 UI

```text
goal: [T14-05] 신고작업 공통 증빙 첨부 UI를 구현해줘.
선행: T14-04
작업: 파일 추가, 목록, 다운로드, 삭제, 업로드 중/실패 상태, readOnly 처리
완료: 결재/거래/조정 화면에서 첨부 UI 재사용 가능
검증: 프론트 테스트와 브라우저 확인
```

### T14-06. 변경 이력 공통 API

```text
goal: [T14-06] 신고작업 변경 이력 공통 API를 구현해줘.
선행: T02-07
작업: 대상 타입, 대상 ID, 변경 필드, 이전값/변경값, 변경자, 변경일시, source 반환
완료: 서식, 조정, 결재 화면에서 변경 이력을 조회 가능
검증: 이력 있음/없음, 권한 없음, 다른 tenant 테스트
```

### T14-07. 변경 이력 화면

```text
goal: [T14-07] 신고작업 변경 이력 화면을 구현해줘.
선행: T14-06
작업: 필터, 대상별 그룹, 변경 전/후 표시, 상세 보기, 빈 상태 구현
완료: 사용자가 신고작업의 주요 변경 이력을 확인 가능
검증: 프론트 테스트와 브라우저 확인
```

### T14-08. 대용량 fixture

```text
goal: [T14-08] 대용량 신고작업 성능 검증용 fixture를 만들어줘.
선행: T05~T08 주요 데이터 구조
작업: 많은 fs_lines, assets, transactions, adjustment_items, form_data를 가진 fixture 생성
완료: API 성능과 프론트 렌더링을 반복 측정 가능
검증: fixture 생성 시간과 기본 조회 성공 확인
```

### T14-09. 주요 API 성능 측정

```text
goal: [T14-09] 주요 신고작업 API의 성능 기준선을 측정하고 병목을 정리해줘.
선행: T14-08
작업: context, list, financial statements, adjustments, forms, validate API 응답 시간 측정
완료: 느린 쿼리와 인덱스 필요 지점이 문서화됨
검증: 측정 명령과 결과 기록
```

### T14-10. 인덱스와 페이지네이션

```text
goal: [T14-10] 신고작업 주요 조회 API에 필요한 인덱스와 페이지네이션을 보강해줘.
선행: T14-09
작업: tenant_id, business_year_id, module_code, form_code, created_at 기준 인덱스 검토
완료: 대용량 fixture에서도 주요 조회가 기준 시간 안에 동작
검증: 성능 재측정과 migration 테스트
```

### T14-11. 캐싱과 무효화

```text
goal: [T14-11] context, stepStatus, progress, form summary의 캐싱과 무효화 기준을 정리해줘.
선행: T14-09
작업: 저장/검증/결재/출력/전자신고 후 갱신되어야 하는 데이터와 캐시 범위 정의
완료: 오래된 진행률이나 상태가 화면에 남지 않음
검증: 저장 후 context 갱신 테스트
```

### T14-12. 역할/테넌트/잠금 전체 회귀

```text
goal: [T14-12] 신고작업 전체 API의 역할, 테넌트 격리, FILED 잠금 회귀 테스트를 정리해줘.
선행: T02~T13 주요 API
작업: 누락 API 목록을 찾고 공통 fixture로 최소 회귀 테스트 추가
완료: 접근 제어 회귀가 한 번에 실행 가능
검증: 관련 Rust 테스트 실행
```

### T14-13. 대표 성공 E2E

```text
goal: [T14-13] DRAFT에서 FILED까지 대표 성공 경로 E2E를 구현해줘.
선행: T04~T12
작업: 작업 시작, 세무정보 입력, 조정 저장, 서식 생성, 검증, 결재, 출력, 전자신고 완료 흐름 검증
완료: 문서의 0~7단계 성공 흐름이 자동 테스트로 보장됨
검증: E2E 테스트와 브라우저 확인
```

### T14-14. 오류/반려 E2E

```text
goal: [T14-14] 검증 오류와 결재 반려 경로 E2E를 구현해줘.
선행: T09, T10
작업: 검증 ERROR로 결재 차단, WARN만 있을 때 결재 가능, 반려 후 DRAFT 복귀와 재요청 확인
완료: 실패/재작업 흐름이 자동 테스트로 보장됨
검증: E2E 테스트와 브라우저 확인
```

### T14-15. 전자신고 실패/재시도 E2E

```text
goal: [T14-15] 전자신고 실패와 재시도 경로 E2E를 구현해줘.
선행: T12
작업: 사전검증 실패, 파일 생성 실패, 2FA 실패, 제출 실패, 재시도 후 성공 흐름 확인
완료: 제출 실패 시 FILED로 잘못 전환되지 않음
검증: E2E 테스트와 브라우저 확인
```

### T14-16. 원문 대비 최종 차이표

```text
goal: [T14-16] 세부구현_신고_v1.0.md 원문 대비 최종 구현 차이표를 작성해줘.
선행: T04~T14 주요 구현 완료
작업: 구현 완료, 일부 완료, 의도적 제외, 후속 필요 항목을 장/절별로 정리
완료: 인수자가 남은 범위와 구현된 범위를 빠르게 판단 가능
검증: T00 추적표와 최종 코드/테스트를 대조
```

### T14-17. 인수 점검과 최종 테스트

```text
goal: [T14-17] 신고작업 구현 전체 인수 점검과 최종 테스트를 실행해줘.
선행: T14-13~T14-16
작업: 포맷, 백엔드 테스트, 프론트 테스트, E2E, 브라우저 주요 화면 확인, 남은 위험 정리
완료: 신고작업 0~7단계 구현 완료 여부를 인수 가능한 형태로 보고
검증: 실행한 전체 명령과 브라우저 확인 결과 기록
```

---

## 추천 실행 순서

1. `T00-01`~`T00-10`으로 기준선을 먼저 고정한다.
2. `T01`, `T02`, `T03`을 완료해 상태/권한/API/UI 공통 기반을 만든다.
3. `T04` 단계 0을 끝내 실제 작업 컨텍스트를 만들 수 있게 한다.
4. `T05` 단계 1 입력 데이터를 먼저 완성한다.
5. `T06` 공통 세무조정 기반을 만든 뒤 `T07` 모듈별 구현을 진행한다.
6. `T08` 서식 작성, `T09` 검증, `T10` 결재를 순서대로 연결한다.
7. `T11` 출력, `T12` 전자신고를 완료한 뒤 `T13` 잠금/수정신고를 닫는다.
8. `T14` 운영 품질, 성능, E2E, 최종 차이표로 마무리한다.

## 첫 실행 추천 goal

```text
goal: [T00-01] 세부구현_신고_v1.0.md 1~16장을 원자 요구사항 단위로 쪼개고, 장/절/요구사항/영향영역/우선순위/후속 goal ID 컬럼을 가진 추적표를 만들어줘.
선행: 없음
작업: 원문 목차와 본문을 대조해 누락 없이 요구사항을 뽑고, 중복되거나 모호한 요구사항은 별도 메모로 표시
완료: 모든 장/절이 추적표에 연결되고 이후 goal에서 참조 가능한 ID가 있음
검증: 원문 목차와 추적표 항목을 대조해 빠진 절이 없는지 확인
```
