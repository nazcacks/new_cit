# `세부구현_신고_v1.0.md` 상세 분할 goal 명령

아래 goal은 위에서부터 순서대로 진행한다. 각 goal은 해당 범위의 구현, 관련 테스트 추가/수정, `cargo fmt`, 필요한 `cargo test`/`npm run test:frontend`, 프론트 변경 시 브라우저 확인까지 완료한 뒤 종료한다.

## 진행 원칙

- 한 goal 안에서는 문서에 없는 대규모 리팩터링을 하지 않는다.
- 기존 DB/API/프론트 패턴을 우선 사용한다.
- tenant/customer/business_year 접근 제어는 모든 신고작업 API에 반복 검증한다.
- 상태 전이와 잠금 규칙은 백엔드에서 먼저 강제하고, 프론트는 사용성 표시로 보조한다.
- 각 단계 완료 후 다음 단계가 의존할 테스트 데이터를 남긴다.

---

## 00. 현황 점검과 작업 기준선

```text
goal: 세부구현_신고_v1.0.md 전체를 기준으로 현재 코드베이스의 신고작업 구현 현황을 점검해줘. 기존 DB 테이블, API 라우트, 프론트 라우트, 테스트 파일을 매핑하고 Phase A~H에서 이미 구현된 항목과 누락된 항목을 구분한 뒤, 필요한 최소 선행 정리와 검증 결과를 보고해줘.
```

## 01. 신고작업 공통 도메인/상태 모델 정리

```text
goal: 세부구현_신고_v1.0.md 11장 사업연도 상태 모델을 기준으로 신고작업 공통 도메인 모델을 정리해줘. business_year 상태값, 단계 코드, 진행률 계산, 단계 활성화 규칙, FILED/AMENDED 전환 조건을 백엔드 타입과 테스트로 고정하고 기존 API에서 재사용할 수 있게 해줘.
```

## 02. 신고작업 권한/테넌트 격리 기반

```text
goal: 세부구현_신고_v1.0.md 14장 역할별 접근 제어를 기준으로 신고작업 공통 권한 검증을 구현해줘. tenant/customer/business_year 접근, 고객사별 업무 범위, 역할별 단계 접근, ASSISTANT 제한, 다른 테넌트 접근 403/404 처리를 공통 헬퍼와 통합 테스트로 검증해줘.
```

## 03. 신고작업 API 공통 응답/에러/잠금 처리

```text
goal: 세부구현_신고_v1.0.md 13장 API 명세를 기준으로 신고작업 API 공통 규칙을 정리해줘. 공통 에러 응답, 잠금 상태에서 수정 차단, 낙관적 동시성 또는 updated_at 충돌 처리, 감사 로그 연결 지점을 구현하고 대표 API 테스트를 추가해줘.
```

---

## A0-01. 공통 스테퍼 UI

```text
goal: 세부구현_신고_v1.0.md 2.1~2.2를 기준으로 신고작업 공통 스테퍼 UI를 구현해줘. 단계 1~7 표시, 현재 단계, 완료/진행/잠금/비활성 상태, 클릭 이동 가능 여부, 모바일 반응형 표시를 기존 프론트 패턴에 맞게 만들고 프론트 테스트로 검증해줘.
```

## A0-02. 컨텍스트 배너와 작업 헤더

```text
goal: 세부구현_신고_v1.0.md 2.3을 기준으로 신고작업 컨텍스트 배너를 구현해줘. 고객사명, 사업연도, 신고유형, 상태, 담당자, 마감일, 진행률, 마지막 저장 시각을 표시하고 데이터 로딩/권한 없음/사업연도 없음 상태를 테스트해줘.
```

## A0-03. 단계 활성화/잠금 프론트 연결

```text
goal: 세부구현_신고_v1.0.md 2.4와 11장을 기준으로 프론트 단계 활성화/잠금 표시를 연결해줘. 백엔드 상태 규칙에 따라 메뉴 진입, 버튼 활성화, 편집 가능 여부, FILED 이후 읽기 전용 표시가 일관되게 동작하는지 테스트해줘.
```

## A1-01. 단계 0 고객사/사업연도 목록 API

```text
goal: 세부구현_신고_v1.0.md 3.1과 3.3을 기준으로 단계 0 작업 시작에 필요한 고객사/사업연도 목록 API를 구현해줘. 권한 범위 내 고객사만 노출하고, 사업연도 상태·신고유형·진행률 요약을 반환하며 tenant 격리 테스트를 추가해줘.
```

## A1-02. 단계 0 작업 시작 화면

```text
goal: 세부구현_신고_v1.0.md 3.1을 기준으로 단계 0 작업 시작 화면을 구현해줘. 고객사 검색/필터, 사업연도 카드 또는 테이블, 상태별 진입 버튼, 빈 상태, 로딩 상태, 권한별 표시 제어를 프론트에 연결하고 테스트해줘.
```

## A1-03. 사업연도 신규 생성 흐름

```text
goal: 세부구현_신고_v1.0.md 3.2를 기준으로 사업연도 신규 생성 흐름을 구현해줘. 신규 생성 API, 중복 연도 방지, 초기 상태 DRAFT/STARTED 설정, 법령 버전 스냅샷 기준 생성, 생성 후 단계 1 진입까지 통합 테스트로 검증해줘.
```

## A1-04. 작업 컨텍스트 저장과 라우팅 복구

```text
goal: 신고작업 단계 0에서 선택한 tenant/customer/business_year 컨텍스트를 프론트 라우팅에 안정적으로 연결해줘. 새로고침, 직접 URL 진입, 권한 변경, 없는 사업연도 접근 시 올바르게 복구 또는 차단되도록 구현하고 테스트해줘.
```

## A2-01. 세무정보 입력 데이터 모델/API

```text
goal: 세부구현_신고_v1.0.md 4.6을 기준으로 단계 1 세무정보 입력 데이터 모델과 API를 구현해줘. 재무제표, 계정과목 매핑, 자산/감가상각, 검증 결과를 저장/조회할 수 있게 하고 사업연도 잠금 및 tenant 격리를 테스트해줘.
```

## A2-02. 재무제표 입력 화면

```text
goal: 세부구현_신고_v1.0.md 4.2를 기준으로 단계 1-1 재무제표 입력 화면을 구현해줘. 손익계산서/재무상태표 주요 계정 입력, 금액 합계, 저장, 수정, 입력 오류 표시, B-1 결산서당기순이익 연동 값을 테스트해줘.
```

## A2-03. 계정과목 매핑 화면

```text
goal: 세부구현_신고_v1.0.md 4.3을 기준으로 단계 1-2 계정과목 매핑 화면을 구현해줘. 표준계정 매핑, 미매핑 계정 경고, 자동 추천 또는 기존 매핑 재사용, 저장 후 검증 상태 반영을 프론트/API 테스트로 확인해줘.
```

## A2-04. 자산 및 감가상각 입력 화면

```text
goal: 세부구현_신고_v1.0.md 4.4를 기준으로 단계 1-3 자산 및 감가상각 입력 화면을 구현해줘. 자산 목록, 취득가액, 내용연수, 회사상각액, 세법한도, 한도초과액 계산과 B-4 자동 반영 후보 생성을 테스트해줘.
```

## A2-05. 단계 1 일관성 검증

```text
goal: 세부구현_신고_v1.0.md 4.5를 기준으로 단계 1-9 일관성 검증 화면과 API를 구현해줘. 재무제표 균형, 필수 매핑 누락, 감가상각 한도초과, 다음 단계 진입 가능 여부를 검증하고 오류/경고/정보 표시를 테스트해줘.
```

---

## B1. 세무조정 공통 모듈 프레임

```text
goal: 세부구현_신고_v1.0.md 5.1과 5.3을 기준으로 단계 2 세무조정 공통 모듈 프레임을 구현해줘. 모듈 트리, 모듈 상태, adjustment 저장/조회 API, adjustment_items 구조, 자동/수동 항목 구분, reserves 연계 훅을 만들고 테스트해줘.
```

## B2. B-1 소득금액조정명세서

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-1 소득금액조정명세서를 구현해줘. 결산서당기순이익 자동 반영, 익금산입/손금불산입, 손금산입/익금불산입, 자동 연동 항목 합산, 수동 항목 편집, 저장 후 재계산 테스트를 추가해줘.
```

## B3. B-4 감가상각비 조정

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-4 감가상각비 조정을 구현해줘. 단계 1 자산 데이터에서 세법한도 초과액을 불러오고, 감가상각 조정 저장 시 B-1 익금산입과 reserves에 자동 반영되도록 통합 테스트해줘.
```

## B4. B-2 기부금 조정

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-2 기부금 조정을 구현해줘. 기부금 유형, 한도 계산, 한도초과/손금불산입 항목, 이월 기부금 처리를 저장하고 B-1 및 관련 서식 연동 값을 테스트해줘.
```

## B5. B-3 접대비 조정

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-3 접대비 조정을 구현해줘. 접대비 입력, 증빙 구분, 한도 계산, 무증빙 경고, 손금불산입 자동 생성, B-1 익금산입 자동 반영을 테스트해줘.
```

## B6. 핵심 세무조정 유보 연계 검증

```text
goal: Phase B 핵심 모듈 B-1/B-4/B-2/B-3의 유보 자동 연계를 집중 검증해줘. 각 모듈 저장 후 reserves 갱신, 기존 자동 항목 중복 방지, 재저장 시 정정, 삭제 시 회수, B-1 합산 일치 여부를 통합 테스트로 보강해줘.
```

---

## C1. B-11 이월결손금

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-11 이월결손금을 구현해줘. 연도별 발생액/공제액/잔액, 공제 한도, 당기 만료 경고, B-1 손금산입 및 FORM3 연동 값을 테스트해줘.
```

## C2. B-15 자본금과 적립금

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-15 자본금과 적립금을 구현해줘. reserves 전체 집계, 유보 발생/감소/잔액, 자본금·적립금 표 표시, B-15 합계와 서식 연동 값을 테스트해줘.
```

## C3. B-12 세액공제·감면

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-12 세액공제·감면을 구현해줘. 공제감면 항목 입력, 법령 버전 기준 한도, R&D 등 주요 항목, FORM3 공제·감면 연동, 법령 변경 경고를 테스트해줘.
```

## C4. B-13 최저한세

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-13 최저한세를 구현해줘. 감면 후 세액과 최저한세 비교, 추가납부액 계산, FORM3 최저한세 반영, 관련 검증 규칙을 테스트해줘.
```

## C5. B-14 가산세

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-14 가산세를 구현해줘. 가산세 유형, 산출 기준, 기간/율 계산, 수동 조정, FORM3 가산세 반영, 오류 입력 방지를 테스트해줘.
```

## C6. 나머지 세무조정 모듈 일괄 확장

```text
goal: 세부구현_신고_v1.0.md Phase C 범위 중 아직 구현되지 않은 나머지 세무조정 모듈을 공통 모듈 프레임에 맞춰 추가해줘. 각 모듈은 최소 저장/조회/상태/잠금/tenant 격리 테스트를 갖추고, 자동 연동이 필요한 모듈은 B-1 또는 reserves 연계 테스트를 추가해줘.
```

## C7. 세무조정 모듈 완료 상태와 단계 진행률

```text
goal: 단계 2 세무조정 전체 모듈의 완료 상태와 사업연도 진행률 계산을 연결해줘. 필수 모듈 B-1/B-15 완료 조건, 선택 모듈 상태, 검증 경고가 있는 경우의 다음 단계 진입 가능 여부를 API/프론트 테스트로 확인해줘.
```

---

## D1. 서식 작성 데이터 모델/API

```text
goal: 세부구현_신고_v1.0.md 6.3을 기준으로 단계 3 서식 작성 데이터 모델과 API를 구현해줘. form_data 저장/조회, form_code/version, 자동 산출 필드와 수동 입력 필드 구분, 이력 관리, 잠금 상태 수정을 테스트해줘.
```

## D2. 별지 제3호 FORM3 화면

```text
goal: 세부구현_신고_v1.0.md 6.1의 별지 제3호 화면을 구현해줘. B-1/B-2/B-11/B-12/B-13/B-14 값 자동 반영, 과세표준/산출세액/납부세액 표시, 수동 입력 가능 필드 저장, 프론트 테스트를 추가해줘.
```

## D3. 부속서식 일람과 서식 라우팅

```text
goal: 세부구현_신고_v1.0.md 6.1의 부속서식 일람을 구현해줘. 서식 목록, 작성 필요/완료/오류 상태, 서식별 진입, 검색/필터, 권한별 편집 가능 여부, 빈 상태를 프론트 테스트로 검증해줘.
```

## D4. 주요 부속서식 자동 채움

```text
goal: 단계 3 주요 부속서식 자동 채움을 구현해줘. 세무조정 결과에서 필요한 부속서식 필드를 채우고, 자동 값과 사용자 수정 값을 구분하며, 재계산 시 사용자 수정 보호 규칙을 테스트해줘.
```

## D5. 서식 간 연동 검증

```text
goal: 세부구현_신고_v1.0.md 6.2를 기준으로 서식 간 연동 검증을 구현해줘. FORM3 필드와 B-15 등 부속 데이터 합계 일치, 누락/불일치 오류, 검증 결과에서 해당 서식으로 이동하는 액션을 테스트해줘.
```

---

## E1. 검증 엔진 API

```text
goal: 세부구현_신고_v1.0.md 7.2~7.3을 기준으로 단계 4 검증 엔진 API를 구현해줘. 오류/경고/정보 분류, 모듈별 rule_code, 대상 화면 이동 정보, 재검증, 검증 결과 저장/조회, tenant 격리를 테스트해줘.
```

## E2. 검증 화면

```text
goal: 세부구현_신고_v1.0.md 7.1을 기준으로 단계 4 검증 화면을 구현해줘. 검증 실행 버튼, 결과 요약, 심각도 필터, 모듈 필터, 상세 메시지, 이동 액션, 검증 통과/실패 상태를 프론트 테스트와 브라우저 확인으로 검증해줘.
```

## E3. 결재 요청 API와 작성자 화면

```text
goal: 세부구현_신고_v1.0.md 8.1을 기준으로 단계 5 작성자 결재 요청 흐름을 구현해줘. 검증 통과 전 요청 차단, 결재자 선택, 요청 메모, 상태 SUBMITTED 전환, 알림 생성, 작성자 화면 상태 표시를 테스트해줘.
```

## E4. 결재함과 승인/반려

```text
goal: 세부구현_신고_v1.0.md 8.2~8.3을 기준으로 검토자 결재함과 승인/반려를 구현해줘. 결재 대기 목록, 상세 보기, 승인/반려 메모, APPROVED/REJECTED 전환, 반려 후 재작업 가능 상태, 역할별 접근을 테스트해줘.
```

## E5. 결재 알림과 감사 로그

```text
goal: 단계 5 결재 이벤트를 알림과 감사 로그에 연결해줘. 결재 요청/승인/반려 시 대상 사용자 알림, 읽음 처리, dashboard/workflow와의 상태 동기화, 감사 로그 기록을 통합 테스트로 검증해줘.
```

---

## F1. PDF 출력 데이터 준비

```text
goal: 세부구현_신고_v1.0.md 9.1~9.2를 기준으로 단계 6 PDF 출력 데이터 준비 API를 구현해줘. 출력 대상 서식 선택, FORM3/부속서식 데이터 조합, 출력 가능 상태 검증, tenant 격리와 FILED 전후 동작을 테스트해줘.
```

## F2. PDF 생성과 워터마크

```text
goal: 세부구현_신고_v1.0.md 9.2~9.3을 기준으로 PDF 생성과 워터마크 상태 매핑을 구현해줘. DRAFT/APPROVED/FILED 상태별 워터마크, 생성 이력, 다운로드 응답, 실패 처리, 출력 화면 표시를 테스트해줘.
```

## F3. 전자신고 파일 데이터 모델/API

```text
goal: 세부구현_신고_v1.0.md 10.3과 전자신고 파일설명서를 기준으로 단계 7 전자신고 파일 데이터 모델과 API를 구현해줘. 신고파일 생성 요청, 파일 메타데이터, 오류검증 결과, 제출 이력, 접수번호 저장 구조를 테스트해줘.
```

## F4. 전자신고 4단계 UI

```text
goal: 세부구현_신고_v1.0.md 10.1을 기준으로 단계 7 전자신고 4단계 UI를 구현해줘. 파일 생성, 오류검증, 제출 준비, 제출 완료 단계를 표시하고 각 단계의 버튼/상태/오류/재시도 흐름을 프론트 테스트와 브라우저 확인으로 검증해줘.
```

## F5. 신고파일 생성 규칙

```text
goal: 전자신고_2026년 1차 파일설명서의 법인세 파일 규격을 기준으로 신고파일 생성 규칙을 구현해줘. 레코드 구분, 필드 길이, 숫자/문자 패딩, 필수값 누락 오류, 생성 결과 스냅샷 테스트를 추가해줘.
```

## F6. 전자신고 오류검증 연동

```text
goal: 전자신고 오류검증 문서를 기준으로 신고파일 오류검증을 구현해줘. 파일 생성 전/후 검증, 오류 코드, 메시지, 대상 서식/필드 이동, 오류가 있으면 제출 차단하는 규칙을 테스트해줘.
```

## F7. 2FA Step-up 제출

```text
goal: 세부구현_신고_v1.0.md 10.1과 16.3을 기준으로 전자신고 제출 시 2FA Step-up을 구현해줘. TOTP 토큰 없으면 403, 유효 토큰이면 제출 허용, ASSISTANT 제출 차단, 세션/감사 로그 기록을 통합 테스트로 검증해줘.
```

## F8. 제출 후 FILED 전환과 접수 정보

```text
goal: 세부구현_신고_v1.0.md 10.2를 기준으로 전자신고 제출 후 처리를 구현해줘. 접수번호/접수일시/제출파일 해시 저장, 사업연도 FILED 전환, 이후 편집 잠금, 대시보드/작업목록 상태 갱신을 테스트해줘.
```

---

## G1. FILED 이후 백엔드 불변성

```text
goal: 세부구현_신고_v1.0.md Phase G와 16.3을 기준으로 FILED 이후 백엔드 불변성을 강제해줘. form_data, tax_adjustments, reserves, efiling_file 관련 수정/삭제 API가 차단되는지 통합 테스트로 검증해줘.
```

## G2. FILED 이후 프론트 읽기 전용 모드

```text
goal: FILED 상태에서 신고작업 프론트 전체가 읽기 전용으로 동작하게 해줘. 입력 필드, 저장 버튼, 재계산 버튼, 결재/전자신고 버튼의 비활성화와 안내 표시가 단계별로 일관적인지 프론트 테스트와 브라우저 확인으로 검증해줘.
```

## G3. AMENDED 수정신고 사업연도 생성

```text
goal: 세부구현_신고_v1.0.md Phase G의 AMENDED 모드를 구현해줘. 원신고 FILED 사업연도에서 수정신고 작업을 생성하고, original_business_year 참조, 수정 사유, 초기 데이터 복사, 별도 상태 전이를 테스트해줘.
```

## G4. 수정신고 변경분 관리

```text
goal: AMENDED 수정신고에서 원신고 대비 변경분을 관리해줘. 변경 필드 비교, 조정 항목 변경 이력, 수정신고 출력/전자신고에 필요한 차이 데이터, 원신고 불변성 유지를 테스트해줘.
```

---

## H1. 대용량 입력/조회 성능 점검

```text
goal: 세부구현_신고_v1.0.md Phase H를 기준으로 대용량 신고작업 데이터를 점검해줘. 대량 계정/자산/조정항목/서식 데이터에서 주요 API 응답과 프론트 렌더링 병목을 찾고, 필요한 인덱스/페이지네이션/쿼리 최적화를 적용한 뒤 성능 테스트 또는 재현 스크립트로 검증해줘.
```

## H2. Excel 업로드/다운로드 안정화

```text
goal: Phase H의 대용량 Excel 처리를 구현 또는 안정화해줘. 재무제표/계정매핑/자산/조정항목 Excel 업로드와 다운로드에서 템플릿 검증, 오류 행 표시, 대용량 처리, tenant 격리, 잠금 상태 차단을 테스트해줘.
```

## H3. 신고작업 API 캐싱/무효화

```text
goal: 신고작업 주요 조회 API의 캐싱과 무효화 전략을 정리해줘. 사업연도 요약, 단계 상태, 서식 데이터, 검증 결과, 결재 상태가 저장/검증/승인/제출 후 60초 안에 최신 상태로 반영되도록 구현하고 테스트해줘.
```

## H4. 신고작업 E2E 시나리오

```text
goal: 신고작업 전체 E2E 시나리오를 구현해줘. 사업연도 생성부터 세무정보 입력, 세무조정, 서식 작성, 검증, 결재, PDF 출력, 전자신고 제출, FILED 잠금까지 대표 성공 흐름을 자동 테스트와 브라우저 확인으로 검증해줘.
```

## H5. 역할/테넌트/잠금 회귀 테스트 확장

```text
goal: 신고작업 전체에 대한 역할, 테넌트, 잠금 회귀 테스트를 확장해줘. SUPER_ADMIN/TENANT_ADMIN/TAX_EXPERT/TAX_REVIEWER/ASSISTANT별 허용·차단 행위, 다른 테넌트 접근, FILED 이후 수정 차단을 모든 주요 API와 화면에서 검증해줘.
```

## H6. 최종 문서 차이와 운영 점검

```text
goal: 세부구현_신고_v1.0.md 구현 결과와 실제 코드 차이를 최종 점검해줘. 누락된 API/화면/테스트/운영 문서 차이를 보완하고, README 또는 관련 운영 문서에 신고작업 실행·검증 방법이 필요한 경우 최소 범위로 업데이트해줘.
```

---

## 99. 최종 인수 점검

```text
goal: 세부구현_신고_v1.0.md 전체 구현 결과를 인수 점검해줘. 문서 16.3 검증 항목 전체를 기준으로 스냅샷 계산 정확성, 유보 자동 연계, 서식 간 연동, 상태별 메뉴 활성화, 2FA Step-up, 잠금 불변성, 테넌트 격리, 역할 분리를 재검증하고 누락된 테스트나 문서 차이를 보완해줘.
```

---

# 초세분화 실행판

아래 명령은 위의 큰 goal을 더 작게 쪼갠 실행 단위다. 실제 구현을 이어갈 때는 이 섹션의 `S00-01`부터 순서대로 하나씩 붙여넣어 진행한다. 각 goal은 코드 구현, 테스트 추가/수정, 필요한 포맷팅과 자동 테스트, 프론트 변경 시 브라우저 확인까지 포함한다.

## S00. 기준선과 갭 분석

### S00-01. 문서-코드 구현 현황표

```text
goal: 세부구현_신고_v1.0.md의 0~7단계, 11~16장을 기준으로 현재 코드의 DB 테이블, Rust API, 프론트 라우트, 테스트 파일을 매핑한 구현 현황표를 만들어줘. 이미 구현됨/부분 구현/미구현/문서와 다름을 구분하고, 이후 goal에서 참조할 우선순위와 검증 명령을 함께 정리해줘.
```

### S00-02. DB 스키마 갭 점검

```text
goal: 세부구현_신고_v1.0.md 12장 데이터 구성 상세를 기준으로 현재 migrations와 DB 모델의 누락 필드를 점검해줘. business_years, financial_statements, tax_adjustments, adjustment_items, reserves, form_data, validation_runs, approval_lines, efiling_history 관련 차이를 정리하고 필요한 마이그레이션 후보만 제안해줘.
```

### S00-03. API 라우트 갭 점검

```text
goal: 세부구현_신고_v1.0.md 13장 API 명세를 기준으로 현재 백엔드 라우트를 점검해줘. 단계별 조회/저장/검증/상태전이 API의 존재 여부, 요청/응답 형태, 권한 검증 여부, 잠금 검증 여부를 표로 만들고 바로 구현할 작은 API 단위로 재정렬해줘.
```

### S00-04. 프론트 메뉴와 화면 갭 점검

```text
goal: 세부구현_신고_v1.0.md 2~10장과 15장 프론트엔드 구현 상세를 기준으로 현재 프론트 메뉴/라우트/화면 구현 상태를 점검해줘. 각 신고 단계와 서브 화면이 독립 화면으로 열리는지, 공통 더미 화면에 머무는지, 필요한 테스트 selector가 있는지 확인하고 수정 대상 목록을 만들어줘.
```

### S00-05. 테스트 기준선 고정

```text
goal: 신고작업 구현을 시작하기 전 현재 테스트 기준선을 고정해줘. cargo fmt, 관련 cargo test, npm run test:frontend 실행 가능 여부를 확인하고 실패가 있으면 신고작업과 직접 관련된 최소 원인만 분리해줘. 이후 goal에서 반복 사용할 테스트 명령 목록을 문서화해줘.
```

### S00-06. 신고작업 테스트 fixture 준비

```text
goal: 신고작업 통합 테스트에 반복 사용할 tenant/customer/business_year fixture를 정리해줘. DRAFT, IN_PROGRESS, VALIDATED, APPROVED, FILED 상태와 SUPER_ADMIN, TENANT_ADMIN, TAX_EXPERT, TAX_REVIEWER, ASSISTANT 역할을 테스트에서 쉽게 만들 수 있게 하고 tenant 격리 기본 검증을 추가해줘.
```

## S01. 공통 상태, 권한, 레이아웃

### S01-01. business_year 상태 enum과 전이 규칙

```text
goal: 세부구현_신고_v1.0.md 11.1을 기준으로 business_year 상태 enum과 허용 전이 규칙을 백엔드에 고정해줘. DRAFT/STARTED/VALIDATED/SUBMITTED/APPROVED/FILED/AMENDED 전이를 검증하고 허용되지 않는 전이는 테스트에서 실패하도록 만들어줘.
```

### S01-02. 단계 완료 조건 계산

```text
goal: 세부구현_신고_v1.0.md 2.4와 11.2를 기준으로 INFO, ADJUST, FORM, VALIDATE, APPROVE, PRINT, EFILE 단계 완료 조건 계산 함수를 구현해줘. 각 단계별 최소 데이터 조건과 실패 케이스를 백엔드 단위 테스트로 고정하고 기존 API에서 재사용할 수 있게 해줘.
```

### S01-03. 진행률 계산 API

```text
goal: 세부구현_신고_v1.0.md 11.3을 기준으로 신고작업 진행률 계산을 구현해줘. 단계별 가중치 또는 완료 개수 기준을 명확히 하고, 사업연도 요약 API에서 progressPercent와 stepStatus를 반환하도록 테스트해줘.
```

### S01-04. FILED 잠금 공통 헬퍼

```text
goal: FILED 상태와 by_law_snapshot.is_locked 상태를 기준으로 신고작업 수정 API를 차단하는 공통 잠금 헬퍼를 구현해줘. form_data, tax_adjustments, reserves, efiling 관련 대표 수정 API에서 409 또는 표준 에러가 반환되는지 테스트해줘.
```

### S01-05. 신고작업 권한 공통 헬퍼

```text
goal: 세부구현_신고_v1.0.md 14장을 기준으로 신고작업 권한 검증 공통 헬퍼를 구현해줘. tenant/customer/business_year 범위, 고객사별 업무 범위, 역할별 단계 접근, ASSISTANT 제한, 다른 테넌트 접근 차단을 대표 API 통합 테스트로 검증해줘.
```

### S01-06. 공통 에러 응답과 감사 로그 연결

```text
goal: 세부구현_신고_v1.0.md 13.2를 기준으로 신고작업 API의 공통 에러 응답 형식을 정리해줘. 권한 없음, 잠금 상태, 검증 실패, 동시 수정 충돌, 찾을 수 없음 응답을 표준화하고 대표 수정/상태전이 API에 감사 로그 기록 지점을 추가해줘.
```

### S01-07. 프론트 작업 컨텍스트 상태

```text
goal: 프론트에서 tenant/customer/business_year 작업 컨텍스트를 안정적으로 관리하도록 구현해줘. 단계 0에서 선택한 사업연도, 직접 URL 진입, 새로고침, 없는 사업연도, 권한 없는 사업연도 접근을 처리하고 프론트 테스트로 검증해줘.
```

### S01-08. 공통 스테퍼 UI

```text
goal: 세부구현_신고_v1.0.md 2.1~2.2를 기준으로 신고작업 공통 스테퍼 UI를 구현해줘. 완료/진행/경고/대기/잠금 상태, 클릭 이동 가능 여부, 모바일 표시를 구현하고 각 상태가 DOM에서 구분되는지 프론트 테스트로 검증해줘.
```

### S01-09. 컨텍스트 배너와 작업 헤더

```text
goal: 세부구현_신고_v1.0.md 2.3을 기준으로 컨텍스트 배너와 작업 헤더를 구현해줘. 고객사명, 사업연도, 신고유형, 상태, 마감일, 법령 스냅샷, 법령 변경 알림, 마지막 저장 시각을 표시하고 로딩/빈 상태/권한 없음 상태를 테스트해줘.
```

### S01-10. 프론트 단계 진입 가드

```text
goal: 백엔드 stepStatus와 business_year 상태를 프론트 단계 진입 가드에 연결해줘. 비활성 단계 직접 URL 접근, FILED 이후 편집 화면 접근, 검증 전 결재 진입, 승인 전 출력/신고 진입이 올바르게 차단 또는 읽기 전용 처리되는지 테스트해줘.
```

## S02. 단계 0 작업 시작

### S02-01. 고객사/사업연도 목록 API

```text
goal: 세부구현_신고_v1.0.md 3.1과 3.3을 기준으로 단계 0 고객사/사업연도 목록 API를 구현해줘. 권한 범위 내 고객사만 노출하고 사업연도, 신고유형, 상태, 진행률, 마감일, 최근 작업 시각을 반환하며 tenant 격리를 테스트해줘.
```

### S02-02. 작업 시작 목록 화면

```text
goal: 단계 0 작업 시작 화면을 구현해줘. 고객사 검색, 사업연도 필터, 상태 배지, 진행률, 마감일, 계속 작업 버튼, 빈 상태, 로딩 상태를 연결하고 목록 API 응답에 따라 화면이 갱신되는지 프론트 테스트로 검증해줘.
```

### S02-03. 사업연도 신규 생성 API

```text
goal: 세부구현_신고_v1.0.md 3.2를 기준으로 사업연도 신규 생성 API를 구현해줘. 고객사와 사업연도 중복 방지, 신고유형 선택, 초기 상태 설정, 법령 스냅샷 생성, 생성 후 context 응답을 통합 테스트로 검증해줘.
```

### S02-04. 법령 스냅샷 미리보기와 확정

```text
goal: 사업연도 생성 전 적용될 법령/세율/서식 버전을 미리 보여주는 API와 화면 처리를 구현해줘. 세부구현_신고_v1.0.md의 스냅샷 기반 계산 원칙에 따라 생성 시점 버전이 고정되고 이후 조회에서 동일하게 반환되는지 테스트해줘.
```

### S02-05. 작업 컨텍스트 복구와 브라우저 확인

```text
goal: 단계 0에서 사업연도 선택 후 단계 1로 진입하는 흐름을 완성해줘. 새로고침, 직접 URL, 권한 변경, 삭제된 사업연도 접근을 검증하고 브라우저에서 단계 0 목록에서 사업연도 선택 후 공통 헤더와 스테퍼가 정상 표시되는지 확인해줘.
```

## S03. 단계 1 세무정보 입력

### S03-01. 세무정보 입력 데이터 모델

```text
goal: 세부구현_신고_v1.0.md 4.6을 기준으로 단계 1 데이터 모델과 마이그레이션을 정리해줘. 재무제표, 계정과목 매핑, 자산/감가상각, 일관성 검증 결과를 business_year에 연결하고 tenant 격리와 FILED 잠금 테스트를 추가해줘.
```

### S03-02. 재무제표 조회/저장 API

```text
goal: 세부구현_신고_v1.0.md 4.2를 기준으로 재무제표 조회/저장 API를 구현해줘. 손익계산서와 재무상태표 주요 계정 입력, 합계 계산, 결산서당기순이익 산출, 수정 시 updated_at 충돌 또는 표준 저장 규칙을 테스트해줘.
```

### S03-03. 재무제표 입력 화면

```text
goal: 단계 1-1 재무제표 입력 화면을 구현해줘. 계정별 금액 입력, 합계 표시, 저장/취소, 입력 오류, 저장 성공 표시, B-1 연동 예정 값 표시를 프론트 테스트와 브라우저 확인으로 검증해줘.
```

### S03-04. 계정과목 매핑 API

```text
goal: 세부구현_신고_v1.0.md 4.3을 기준으로 계정과목 매핑 API를 구현해줘. 회사 계정과 표준 계정 연결, 미매핑 목록, 기존 매핑 재사용, 저장 후 검증 상태 변경을 통합 테스트로 검증해줘.
```

### S03-05. 계정과목 매핑 화면

```text
goal: 단계 1-2 계정과목 매핑 화면을 구현해줘. 미매핑 계정 필터, 표준계정 선택, 추천 매핑 표시, 일괄 저장, 누락 경고, 검증 상태 반영을 프론트 테스트와 브라우저 확인으로 검증해줘.
```

### S03-06. 자산/감가상각 API

```text
goal: 세부구현_신고_v1.0.md 4.4를 기준으로 자산 및 감가상각 API를 구현해줘. 자산 목록, 취득가액, 내용연수, 회사상각액, 세법한도, 한도초과액 계산을 저장/조회하고 B-4 후보 데이터 생성 여부를 테스트해줘.
```

### S03-07. 자산/감가상각 화면

```text
goal: 단계 1-3 자산 및 감가상각 화면을 구현해줘. 자산 행 추가/수정/삭제, 세법한도 계산 표시, 한도초과 경고, 저장, B-4 반영 후보 표시를 프론트 테스트와 브라우저 확인으로 검증해줘.
```

### S03-08. 단계 1 일관성 검증 API

```text
goal: 세부구현_신고_v1.0.md 4.5를 기준으로 단계 1 일관성 검증 API를 구현해줘. 재무제표 균형, 필수 계정 매핑 누락, 감가상각 한도초과, 필수 입력 누락을 오류/경고/정보로 분류하고 다음 단계 진입 가능 여부를 테스트해줘.
```

### S03-09. 단계 1 검증 화면과 진행률 연결

```text
goal: 단계 1-9 일관성 검증 화면을 구현하고 단계 완료 상태와 진행률에 연결해줘. 검증 실행, 결과 요약, 상세 메시지, 대상 화면 이동, 오류가 있는 경우 단계 2 진입 차단을 프론트/API 테스트와 브라우저 확인으로 검증해줘.
```

## S04. 단계 2 세무조정

### S04-01. 세무조정 공통 데이터/API

```text
goal: 세부구현_신고_v1.0.md 5.1과 5.3을 기준으로 tax_adjustments와 adjustment_items 공통 조회/저장 API를 구현해줘. module_code, 자동/수동 항목, 계산 기준, 상태, 잠금, tenant 격리를 통합 테스트로 검증해줘.
```

### S04-02. 세무조정 모듈 트리와 공통 화면

```text
goal: 단계 2 세무조정 공통 화면을 구현해줘. 17개 모듈 트리, 모듈 상태, 현재 모듈 작업 영역, 저장/재계산 버튼, 오류/경고 표시, 잠금 상태 표시를 프론트 테스트와 브라우저 확인으로 검증해줘.
```

### S04-03. B-1 소득금액조정 계산 API

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-1 소득금액조정명세서 계산 API를 구현해줘. 결산서당기순이익, 익금산입/손금불산입, 손금산입/익금불산입, 자동 연동 항목 합계, 각 사업연도 소득금액 계산을 테스트해줘.
```

### S04-04. B-1 소득금액조정 화면

```text
goal: B-1 소득금액조정명세서 화면을 구현해줘. 자동 반영 항목, 수동 조정 항목 추가/수정/삭제, 합계와 산출 결과, 저장 후 재계산을 프론트 테스트와 브라우저 확인으로 검증해줘.
```

### S04-05. 유보 자동 연계 공통 처리

```text
goal: 세무조정 모듈 저장 시 reserves 테이블을 갱신하는 공통 유보 연계 처리를 구현해줘. 자동 생성 항목 중복 방지, 재저장 시 정정, 삭제 시 회수, FILED 잠금 차단, 감사 로그를 통합 테스트로 검증해줘.
```

### S04-06. B-4 감가상각비 조정 API

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-4 감가상각비 조정 API를 구현해줘. 단계 1 자산 데이터에서 한도초과액을 불러오고 손금불산입/유보 항목을 생성하며 B-1과 reserves에 반영되는지 테스트해줘.
```

### S04-07. B-4 감가상각비 조정 화면

```text
goal: B-4 감가상각비 조정 화면을 구현해줘. 자산별 회사상각액, 세법한도, 한도초과액, 조정 반영 여부, 저장/재계산, B-1 반영 미리보기를 프론트 테스트와 브라우저 확인으로 검증해줘.
```

### S04-08. B-2 기부금 조정

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-2 기부금 조정을 구현해줘. 기부금 유형, 한도 계산, 한도초과액, 이월 기부금, B-1 손금불산입 연동, 관련 서식 연동 후보를 API와 화면 테스트로 검증해줘.
```

### S04-09. B-3 접대비 조정

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-3 접대비 조정을 구현해줘. 접대비 입력, 증빙 구분, 한도 계산, 무증빙 경고, 손금불산입 자동 생성, B-1 연동을 API와 화면 테스트로 검증해줘.
```

### S04-10. B-11 이월결손금

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-11 이월결손금을 구현해줘. 연도별 발생액, 공제액, 잔액, 공제 한도, 당기 만료 경고, B-1 손금산입과 FORM3 연동 값을 테스트해줘.
```

### S04-11. B-15 자본금과 적립금

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-15 자본금과 적립금을 구현해줘. reserves 전체 집계, 유보 발생/감소/잔액, 자본금·적립금 표, B-15 합계와 FORM3/부속서식 연동 값을 테스트해줘.
```

### S04-12. B-12 세액공제·감면

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-12 세액공제·감면을 구현해줘. 공제감면 항목 입력, 법령 스냅샷 기준 한도, R&D 등 주요 항목, FORM3 공제·감면 연동, 법령 변경 경고를 API와 화면 테스트로 검증해줘.
```

### S04-13. B-13 최저한세

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-13 최저한세를 구현해줘. 감면 후 세액과 최저한세 비교, 추가납부액 계산, FORM3 최저한세 반영, 검증 규칙 연계를 테스트해줘.
```

### S04-14. B-14 가산세

```text
goal: 세부구현_신고_v1.0.md 5.2의 B-14 가산세를 구현해줘. 가산세 유형, 산출 기준, 기간/율 계산, 수동 조정, FORM3 가산세 반영, 오류 입력 방지를 API와 화면 테스트로 검증해줘.
```

### S04-15. 나머지 세무조정 모듈 기본 골격

```text
goal: 세부구현_신고_v1.0.md의 17개 세무조정 모듈 중 아직 개별 구현되지 않은 모듈을 공통 모듈 프레임에 맞춰 추가해줘. 각 모듈은 독립 라우트/화면, 최소 저장/조회/상태/잠금/tenant 격리 테스트를 갖추게 해줘.
```

### S04-16. 세무조정 완료 상태와 진행률

```text
goal: 단계 2 세무조정 모듈 완료 상태를 사업연도 stepStatus와 진행률에 연결해줘. 필수 모듈 B-1/B-15 완료 조건, 선택 모듈 상태, 경고가 있는 경우 다음 단계 진입 가능 여부를 API/프론트 테스트로 검증해줘.
```

### S04-17. 핵심 세무조정 통합 검증

```text
goal: B-1, B-4, B-2, B-3, B-11, B-15, B-12, B-13, B-14의 핵심 연동을 통합 검증해줘. 각 모듈 저장 후 B-1 합산, reserves, FORM3 후보 값이 일관되게 갱신되는지 테스트하고 브라우저에서 주요 모듈 화면 진입을 확인해줘.
```

## S05. 단계 3 서식 작성

### S05-01. 서식 데이터 모델/API

```text
goal: 세부구현_신고_v1.0.md 6.3을 기준으로 form_data 데이터 모델과 조회/저장 API를 구현해줘. form_code, version, 자동 산출 필드, 수동 입력 필드, 이력, 잠금, tenant 격리를 테스트해줘.
```

### S05-02. 별지 제3호 자동 산출 백엔드

```text
goal: 세부구현_신고_v1.0.md 6.1의 별지 제3호 산출 규칙을 백엔드에 구현해줘. B-1/B-2/B-11/B-12/B-13/B-14 값 자동 반영, 과세표준, 산출세액, 납부세액 계산을 테스트해줘.
```

### S05-03. 별지 제3호 화면

```text
goal: 별지 제3호 화면을 구현해줘. 자동 산출 값, 수동 입력 가능 필드, 계산 근거 표시, 저장/재계산, 오류 표시를 프론트 테스트와 브라우저 확인으로 검증해줘.
```

### S05-04. 부속서식 일람 화면

```text
goal: 세부구현_신고_v1.0.md 6.1의 부속서식 일람을 구현해줘. 서식 목록, 작성 필요/완료/오류 상태, 검색/필터, 서식별 진입, 권한별 편집 가능 여부, 빈 상태를 프론트 테스트로 검증해줘.
```

### S05-05. 공통 서식 편집 화면

```text
goal: 부속서식별 공통 편집 화면을 구현해줘. form_code 라우팅, 필드 렌더링, 자동 값 읽기 전용, 수동 값 편집, 저장, 검증 메시지, FILED 읽기 전용 상태를 프론트/API 테스트로 검증해줘.
```

### S05-06. 자동 값과 수동 수정 보호

```text
goal: 세무조정 결과로 채워지는 자동 값과 사용자가 수정한 값을 구분하는 규칙을 구현해줘. 재계산 시 자동 값은 갱신되고 수동 수정 값은 보존되며 충돌 또는 재확인 필요 상태가 표시되는지 테스트해줘.
```

### S05-07. 서식 간 연동 검증

```text
goal: 세부구현_신고_v1.0.md 6.2를 기준으로 서식 간 연동 검증을 구현해줘. FORM3 필드와 부속서식/B-15 합계 불일치, 누락, 음수 불가 항목을 검증하고 결과에서 해당 서식으로 이동하는 액션을 테스트해줘.
```

## S06. 단계 4 검증과 단계 5 결재

### S06-01. 검증 실행 데이터 모델/API

```text
goal: 세부구현_신고_v1.0.md 7.2~7.3을 기준으로 validation_runs와 validation_results 조회/실행 API를 구현해줘. rule_code, severity, target_route, target_field, 메시지, 재검증, 최신 결과 조회, tenant 격리를 테스트해줘.
```

### S06-02. 핵심 검증 규칙 세트

```text
goal: 단계 1~3의 핵심 검증 규칙을 구현해줘. 필수 세무정보 누락, 미완료 세무조정 모듈, FORM3 합계 불일치, ERROR 존재 시 결재 요청 차단을 통합 테스트로 검증해줘.
```

### S06-03. 검증 화면

```text
goal: 세부구현_신고_v1.0.md 7.1을 기준으로 검증 화면을 구현해줘. 검증 실행 버튼, 결과 요약, 심각도 필터, 모듈 필터, 상세 메시지, 대상 화면 이동 액션, 통과/실패 상태를 프론트 테스트와 브라우저 확인으로 검증해줘.
```

### S06-04. 검증 통과 상태 전이

```text
goal: 검증 결과 ERROR 0건일 때 business_year 상태와 stepStatus를 VALIDATED로 전환하는 흐름을 구현해줘. 경고만 있는 경우, 오류가 있는 경우, 이전 단계 수정 후 재검증이 필요한 경우를 API/프론트 테스트로 검증해줘.
```

### S06-05. 결재 데이터 모델/API

```text
goal: 세부구현_신고_v1.0.md 8.3~8.4를 기준으로 approval_lines와 결재 상태 전이 API를 구현해줘. 결재 요청, 검토자 지정, 승인, 반려, 메모, 상태 SUBMITTED/APPROVED/REJECTED 전이를 통합 테스트로 검증해줘.
```

### S06-06. 작성자 결재 요청 화면

```text
goal: 세부구현_신고_v1.0.md 8.1을 기준으로 작성자 결재 요청 화면을 구현해줘. 검증 통과 전 요청 차단, 결재자 선택, 요청 메모, 제출 후 상태 표시, 반려 후 재요청 흐름을 프론트 테스트로 검증해줘.
```

### S06-07. 검토자 결재함과 승인/반려 화면

```text
goal: 세부구현_신고_v1.0.md 8.2를 기준으로 검토자 결재함을 구현해줘. 결재 대기 목록, 상세 보기, 승인/반려 메모, 권한별 버튼 표시, 승인 후 APPROVED 전환, 반려 후 재작업 가능 상태를 테스트해줘.
```

### S06-08. 결재 알림과 감사 로그

```text
goal: 결재 요청/승인/반려 이벤트를 알림과 감사 로그에 연결해줘. 대상 사용자 알림 생성, 읽음 처리, 대시보드/작업목록 상태 동기화, 감사 로그 기록을 통합 테스트로 검증해줘.
```

## S07. 단계 6 출력과 단계 7 전자신고

### S07-01. PDF 출력 준비 API

```text
goal: 세부구현_신고_v1.0.md 9.1~9.2를 기준으로 PDF 출력 데이터 준비 API를 구현해줘. 출력 대상 서식 선택, FORM3/부속서식 데이터 조합, 출력 가능 상태 검증, tenant 격리와 FILED 전후 동작을 테스트해줘.
```

### S07-02. PDF 생성과 워터마크

```text
goal: 세부구현_신고_v1.0.md 9.2~9.3을 기준으로 PDF 생성과 워터마크 매핑을 구현해줘. DRAFT/APPROVED/FILED 상태별 워터마크, 생성 이력, 다운로드 응답, 실패 처리를 테스트해줘.
```

### S07-03. 출력 화면

```text
goal: 단계 6 출력 화면을 구현해줘. 출력 대상 선택, 미리보기 또는 생성 상태, 다운로드, 워터마크 표시, 오류 상태, 승인 전/후 버튼 제어를 프론트 테스트와 브라우저 확인으로 검증해줘.
```

### S07-04. 전자신고 파일 데이터 모델/API

```text
goal: 세부구현_신고_v1.0.md 10.3을 기준으로 efiling 파일 데이터 모델과 API를 구현해줘. 파일 생성 요청, 파일 메타데이터, 오류검증 결과, 제출 이력, 접수번호 저장 구조를 테스트해줘.
```

### S07-05. 전자신고 고정길이 파일 생성

```text
goal: 전자신고_2026년 1차 파일설명서의 법인세 파일 규격을 기준으로 신고파일 생성 규칙을 구현해줘. 레코드 구분, 필드 길이, 숫자/문자 패딩, 필수값 누락 오류, 생성 결과 스냅샷 테스트를 추가해줘.
```

### S07-06. 전자신고 오류검증

```text
goal: 전자신고 파일 생성 전후 오류검증을 구현해줘. 필수 레코드 누락, 필드 길이 오류, 숫자 형식 오류, 서식 값 불일치, 오류가 있으면 제출 차단하는 규칙을 API 테스트로 검증해줘.
```

### S07-07. 전자신고 4단계 UI

```text
goal: 세부구현_신고_v1.0.md 10.1을 기준으로 전자신고 4단계 UI를 구현해줘. 파일 생성, 오류검증, 제출 준비, 제출 완료 상태와 각 단계의 버튼/오류/재시도 흐름을 프론트 테스트와 브라우저 확인으로 검증해줘.
```

### S07-08. 2FA Step-up 제출

```text
goal: 전자신고 제출 직전 2FA Step-up을 구현해줘. TOTP 토큰 없으면 제출 차단, 유효 토큰이면 제출 허용, ASSISTANT 제출 차단, 세션/감사 로그 기록을 통합 테스트로 검증해줘.
```

### S07-09. 제출 후 FILED 전환

```text
goal: 세부구현_신고_v1.0.md 10.2를 기준으로 제출 후 처리를 구현해줘. 접수번호/접수일시/제출파일 해시 저장, business_year FILED 전환, 이후 편집 잠금, 대시보드/작업목록 상태 갱신을 테스트해줘.
```

## S08. 잠금, 수정신고, 안정화, 인수

### S08-01. FILED 백엔드 불변성 회귀

```text
goal: FILED 상태에서 신고작업 백엔드 불변성을 전체 회귀 테스트로 고정해줘. 세무정보, 세무조정, 유보, 서식, 검증, 결재, 출력, 전자신고 관련 수정/삭제 API가 모두 차단되는지 통합 테스트로 검증해줘.
```

### S08-02. FILED 프론트 읽기 전용 회귀

```text
goal: FILED 상태에서 신고작업 프론트 전체가 읽기 전용으로 동작하도록 회귀 테스트를 보강해줘. 입력 필드, 저장/재계산/결재/전자신고 버튼 비활성화와 안내 표시를 단계별로 검증하고 브라우저에서 대표 화면을 확인해줘.
```

### S08-03. AMENDED 수정신고 생성

```text
goal: 세부구현_신고_v1.0.md Phase G의 AMENDED 모드를 구현해줘. 원신고 FILED 사업연도에서 수정신고를 생성하고 original_business_year 참조, 수정 사유, 초기 데이터 복사, 별도 상태 전이를 테스트해줘.
```

### S08-04. 수정신고 변경분 관리

```text
goal: AMENDED 수정신고에서 원신고 대비 변경분 관리를 구현해줘. 변경 필드 비교, 조정 항목 변경 이력, 수정신고 출력/전자신고에 필요한 차이 데이터, 원신고 불변성 유지를 테스트해줘.
```

### S08-05. Excel 업로드/다운로드

```text
goal: 신고작업 Excel 업로드/다운로드를 구현 또는 안정화해줘. 재무제표, 계정매핑, 자산, 조정항목 템플릿 검증, 오류 행 표시, 대용량 처리, tenant 격리, FILED 잠금 차단을 테스트해줘.
```

### S08-06. 대용량 성능과 인덱스

```text
goal: 대용량 신고작업 데이터에서 주요 API와 프론트 렌더링 성능을 점검해줘. 대량 계정/자산/조정항목/서식 데이터를 만들고 병목 쿼리, 필요한 인덱스, 페이지네이션 또는 lazy rendering을 적용한 뒤 재현 스크립트나 성능 테스트로 검증해줘.
```

### S08-07. 캐싱과 무효화

```text
goal: 신고작업 주요 조회 API의 캐싱과 무효화 전략을 정리해줘. 사업연도 요약, 단계 상태, 서식 데이터, 검증 결과, 결재 상태가 저장/검증/승인/제출 후 최신 상태로 반영되도록 구현하고 테스트해줘.
```

### S08-08. 대표 성공 E2E 시나리오

```text
goal: 신고작업 대표 성공 E2E 시나리오를 구현해줘. 사업연도 생성, 세무정보 입력, 세무조정, 서식 작성, 검증, 결재, PDF 출력, 전자신고 제출, FILED 잠금까지 자동 테스트와 브라우저 확인으로 검증해줘.
```

### S08-09. 역할/테넌트/잠금 전체 회귀

```text
goal: 신고작업 전체에 대한 역할, 테넌트, 잠금 회귀 테스트를 확장해줘. SUPER_ADMIN/TENANT_ADMIN/TAX_EXPERT/TAX_REVIEWER/ASSISTANT별 허용·차단 행위, 다른 테넌트 접근, FILED 이후 수정 차단을 모든 주요 API와 화면에서 검증해줘.
```

### S08-10. 최종 문서 차이와 인수 점검

```text
goal: 세부구현_신고_v1.0.md 전체 구현 결과를 최종 인수 점검해줘. 문서 16.3 검증 항목을 기준으로 스냅샷 계산, 유보 자동 연계, 서식 간 연동, 상태별 메뉴 활성화, 2FA Step-up, 잠금 불변성, 테넌트 격리, 역할 분리를 재검증하고 누락된 테스트나 운영 문서를 보완해줘.
```

---

# 마이크로 실행판

아래 명령은 `S00~S08`보다 더 작은 단위다. 한 번에 하나의 `goal:`만 실행하는 것을 전제로 하며, 각 goal은 가능한 한 단일 산출물(DB, API, 화면, 테스트, 브라우저 검증 중 하나)에 집중한다.

## M00. 기준선 고정

### M00-01. 원문 요구사항 인덱스

```text
goal: 세부구현_신고_v1.0.md의 2~16장을 구현 항목 단위로 인덱싱해줘. 각 항목에 요구사항 ID, 문서 위치, 관련 DB/API/프론트/테스트 후보, 선행 의존성을 붙여 이후 작업 체크리스트로 쓸 수 있게 정리해줘.
```

### M00-02. 현재 DB 매핑표

```text
goal: 현재 migrations와 src/domain.rs를 기준으로 신고작업 관련 DB/도메인 매핑표를 작성해줘. business_years, law snapshot, tax_adjustments, reserves, form_data, validation, approval, efiling 관련 테이블의 구현 여부와 누락 필드를 표시해줘.
```

### M00-03. 현재 API 매핑표

```text
goal: 현재 Rust 라우트와 핸들러를 기준으로 신고작업 API 매핑표를 작성해줘. 세부구현_신고_v1.0.md 13.1의 엔드포인트와 실제 구현을 비교하고, 각 API의 권한 검증, 잠금 검증, 테스트 존재 여부를 표시해줘.
```

### M00-04. 현재 프론트 라우트 매핑표

```text
goal: 현재 프론트 메뉴, 라우트, 화면 함수를 기준으로 신고작업 화면 매핑표를 작성해줘. 단계 0~7과 하위 화면별로 독립 화면 여부, 공통 더미 화면 사용 여부, 필요한 data selector, 브라우저 검증 대상 URL을 정리해줘.
```

### M00-05. 테스트 실행 기준선

```text
goal: 신고작업 구현 전 테스트 기준선을 고정해줘. cargo fmt, cargo test 중 신고작업 관련 테스트, npm run test:frontend 실행 결과를 확인하고 실패가 있으면 기존 실패와 신규 작업 대상 실패를 분리해줘.
```

### M00-06. 테스트 fixture 설계

```text
goal: 신고작업 테스트 fixture 설계를 작성해줘. demo tenant, 고객사 2개, 사업연도 DRAFT/STARTED/VALIDATED/APPROVED/FILED, 역할 SUPER_ADMIN/TENANT_ADMIN/TAX_EXPERT/TAX_REVIEWER/ASSISTANT를 반복 생성하는 헬퍼 구조를 제안해줘.
```

### M00-07. 구현 순서 재정렬

```text
goal: M00-01~M00-06 결과를 바탕으로 실제 구현 순서를 재정렬해줘. 선행 DB, 공통 권한/잠금, API, 화면, 통합 검증 순서로 묶고 병렬로 진행하면 안 되는 의존성만 명확히 표시해줘.
```

## M01. 공통 백엔드 기반

### M01-01. 사업연도 상태 타입

```text
goal: 세부구현_신고_v1.0.md 11.1을 기준으로 business_year 상태 타입을 백엔드에 명확히 정의해줘. DRAFT, STARTED, VALIDATED, SUBMITTED, APPROVED, FILED, AMENDED 값을 파싱/직렬화하고 잘못된 값이 들어오면 표준 에러가 나도록 단위 테스트를 추가해줘.
```

### M01-02. 상태 전이 검증 함수

```text
goal: business_year 상태 전이 검증 함수를 구현해줘. 허용 전이와 금지 전이를 표로 고정하고 DRAFT→STARTED, VALIDATED→SUBMITTED, APPROVED→FILED, FILED→AMENDED 등 대표 케이스를 단위 테스트로 검증해줘.
```

### M01-03. 단계 코드 타입

```text
goal: 신고작업 단계 코드 INFO, ADJUST, FORM, VALIDATE, APPROVE, PRINT, EFILE을 백엔드 타입으로 정리해줘. API 응답에서 동일한 문자열을 사용하도록 직렬화 테스트를 추가하고 기존 문자열 중복을 줄여줘.
```

### M01-04. stepStatus 계산 골격

```text
goal: 세부구현_신고_v1.0.md 2.4 기준으로 stepStatus 계산 함수의 골격을 구현해줘. 각 단계가 PENDING, IN_PROGRESS, COMPLETED, WARNING, LOCKED 중 하나를 반환하도록 하고 아직 데이터가 없는 단계는 명시적인 TODO 상태가 아니라 보수적인 PENDING으로 처리해줘.
```

### M01-05. 진행률 계산 함수

```text
goal: 세부구현_신고_v1.0.md 11.3 기준으로 사업연도 진행률 계산 함수를 구현해줘. 단계 완료 개수 또는 문서의 가중치 기준 중 현재 코드에 맞는 방식을 선택하고 0%, 일부 완료, 100% 완료 케이스를 테스트해줘.
```

### M01-06. 사업연도 컨텍스트 조회 헬퍼

```text
goal: tenant_code, customer_id, business_year_id를 받아 신고작업 컨텍스트를 조회하는 백엔드 헬퍼를 구현해줘. 고객사명, 사업연도, 상태, 신고기한, 스냅샷 ID, 잠금 여부를 반환하고 없는 사업연도와 다른 테넌트 접근을 테스트해줘.
```

### M01-07. 권한 검증 헬퍼

```text
goal: 세부구현_신고_v1.0.md 14장을 기준으로 신고작업 권한 검증 헬퍼를 구현해줘. 역할별 조회/작성/검토/제출 가능 여부와 고객사별 접근 범위를 검증하고 ASSISTANT의 제출 차단 테스트를 추가해줘.
```

### M01-08. FILED 잠금 헬퍼

```text
goal: FILED 상태 또는 by_law_snapshot.is_locked 상태에서 수정 API를 차단하는 공통 헬퍼를 구현해줘. 대표 수정 API 하나에 먼저 연결하고 409 또는 기존 표준 에러 형식으로 실패하는지 테스트해줘.
```

### M01-09. 공통 API 에러 형식

```text
goal: 세부구현_신고_v1.0.md 13.2를 기준으로 신고작업 API 공통 에러 형식을 정리해줘. code, message, details, trace_id 또는 기존 패턴에 맞는 필드를 사용하고 권한 없음, 잠금, 검증 실패, not found의 대표 테스트를 추가해줘.
```

### M01-10. 감사 로그 연결 지점

```text
goal: 신고작업 상태 변경과 주요 저장 API에서 감사 로그를 남길 수 있는 공통 호출 지점을 구현해줘. 사업연도 상태 변경, 세무조정 저장, 결재 승인, 전자신고 제출 중 하나를 대표로 연결하고 테스트에서 로그 생성 여부를 확인해줘.
```

## M02. 공통 프론트 기반

### M02-01. 신고작업 라우트 상수

```text
goal: 프론트의 신고작업 단계 0~7 라우트를 상수로 정리해줘. 각 단계 route key, label, required step status, read-only 조건을 한곳에서 관리하고 기존 메뉴가 이 상수를 사용하도록 최소 수정해줘.
```

### M02-02. 작업 컨텍스트 상태

```text
goal: 프론트에서 선택된 tenant/customer/business_year 작업 컨텍스트를 관리하는 상태 구조를 구현해줘. 직접 URL 진입, 새로고침, 컨텍스트 없음, 권한 없음 상태를 구분하고 프론트 테스트를 추가해줘.
```

### M02-03. 컨텍스트 API 클라이언트

```text
goal: GET /api/workspace/{businessYearId}/context 또는 현재 라우트에 맞는 컨텍스트 조회 클라이언트를 구현해줘. 로딩, 성공, 403, 404, 네트워크 오류 상태를 화면에서 재사용할 수 있게 테스트해줘.
```

### M02-04. 공통 스테퍼 DOM 구조

```text
goal: 세부구현_신고_v1.0.md 2.1~2.2 기준으로 공통 스테퍼 컴포넌트를 구현해줘. 단계 라벨, 현재 단계, 완료/진행/대기/경고/잠금 상태가 data-step-status로 드러나도록 만들고 렌더링 테스트를 추가해줘.
```

### M02-05. 스테퍼 이동 가드

```text
goal: 공통 스테퍼 클릭 이동 가드를 구현해줘. 비활성 단계 클릭, 검증 전 결재 단계 진입, 승인 전 출력/전자신고 진입, FILED 이후 편집 단계 진입이 올바르게 차단 또는 읽기 전용 처리되는지 테스트해줘.
```

### M02-06. 컨텍스트 배너

```text
goal: 세부구현_신고_v1.0.md 2.3 기준으로 컨텍스트 배너를 구현해줘. 고객사명, 사업연도, 상태, 잠금 여부, 신고기한, D-day, 법령 스냅샷 요약, 법령 변경 알림 수를 표시하고 빈 상태 테스트를 추가해줘.
```

### M02-07. 공통 읽기 전용 표시

```text
goal: 신고작업 공통 읽기 전용 표시를 구현해줘. FILED, 권한 부족, 승인 후 수정 제한 상태에서 입력과 저장 버튼이 비활성화되고 이유가 표시되는지 프론트 테스트로 검증해줘.
```

### M02-08. 공통 저장 상태 표시

```text
goal: 신고작업 화면에서 재사용할 저장 상태 표시를 구현해줘. 저장 중, 저장 완료, 저장 실패, 마지막 저장 시각, 재시도 액션을 표준화하고 대표 화면 하나에 연결해 테스트해줘.
```

### M02-09. 공통 브라우저 검증 스크립트

```text
goal: 신고작업 프론트 변경 후 반복 사용할 브라우저 검증 스크립트 또는 절차를 정리해줘. 로그인, 단계 라우트 진입, 스테퍼/배너 존재, 에러 화면 없음, 대표 버튼 클릭을 확인하는 기준을 문서화해줘.
```

## M03. 단계 0 작업 시작

### M03-01. 작업 목록 조회 쿼리

```text
goal: 세부구현_신고_v1.0.md 3.3의 SQL 의도를 기준으로 권한 범위 내 사업연도 작업 목록 조회 쿼리를 구현해줘. 고객사명, 사업연도, 상태, 마감일, D-day, 진행률, 최근 수정일을 반환하고 tenant 격리를 테스트해줘.
```

### M03-02. 작업 목록 API

```text
goal: 단계 0 작업 목록 API를 구현해줘. 검색어, 담당 고객사 필터, 상태 필터, 최근 작업 정렬을 지원하고 빈 결과, 권한 없음, 정상 목록 응답 테스트를 추가해줘.
```

### M03-03. 작업 목록 화면

```text
goal: 단계 0 작업 시작 목록 화면을 구현해줘. 검색 입력, 필터, 사업연도 테이블 또는 카드, 상태 배지, 진행률, 마감 D-day, 계속 작업 버튼을 표시하고 API 응답에 따른 렌더링 테스트를 추가해줘.
```

### M03-04. 사업연도 생성 요청 모델

```text
goal: 사업연도 신규 생성 요청 모델을 구현해줘. customer_id, fiscal_year, start_date, end_date, filing_due_date, filing_type을 검증하고 중복 연도와 잘못된 기간을 API 테스트로 차단해줘.
```

### M03-05. 법령 스냅샷 생성

```text
goal: 사업연도 생성 시 종료일 기준 법령/세율/서식 버전 스냅샷을 생성해줘. 유효일자 기준 max version 선택, 스냅샷 ID 저장, 생성 후 재조회 일치 여부를 통합 테스트로 검증해줘.
```

### M03-06. 사업연도 생성 API

```text
goal: POST /api/business-years 또는 현재 패턴의 사업연도 생성 API를 완성해줘. 생성 성공, 중복 방지, 권한 없는 고객사 차단, 생성 직후 context 응답 반환을 테스트해줘.
```

### M03-07. 신규 사업연도 모달

```text
goal: 단계 0 화면에 신규 사업연도 생성 모달을 구현해줘. 고객사, 사업연도, 기간, 신고기한, 신고유형 입력과 오류 표시, 생성 성공 후 목록 갱신 및 새 사업연도 선택을 프론트 테스트로 검증해줘.
```

### M03-08. 작업 선택 후 단계 1 이동

```text
goal: 단계 0에서 사업연도 선택 후 단계 1 세무정보 입력으로 이동하는 흐름을 구현해줘. 작업 컨텍스트 저장, URL 갱신, 새로고침 복구, 없는 사업연도 접근 차단을 프론트 테스트로 검증해줘.
```

### M03-09. 단계 0 브라우저 확인

```text
goal: 브라우저에서 단계 0 작업 시작 화면을 확인해줘. 로그인 후 목록 표시, 검색/필터 동작, 사업연도 선택, 신규 생성 모달 열림, 단계 1 이동 후 스테퍼와 컨텍스트 배너 표시를 검증하고 문제를 수정해줘.
```

## M04. 단계 1 세무정보 입력

### M04-01. 재무제표 테이블/모델 점검

```text
goal: 세부구현_신고_v1.0.md 4.2와 4.6 기준으로 재무제표 저장 구조를 점검해줘. 손익계산서와 재무상태표 계정, 금액, 사업연도 연결, 이력/updated_at, FILED 잠금에 필요한 필드가 없으면 최소 마이그레이션을 추가해줘.
```

### M04-02. 재무제표 조회 API

```text
goal: 단계 1-1 재무제표 조회 API를 구현해줘. business_year_id 기준 손익계산서/재무상태표 데이터를 반환하고 데이터 없음, 다른 테넌트, 권한 없음 케이스를 테스트해줘.
```

### M04-03. 재무제표 저장 API

```text
goal: 단계 1-1 재무제표 저장 API를 구현해줘. 계정별 금액 저장, 합계 계산, 결산서당기순이익 산출, updated_at 충돌 또는 기존 저장 규칙, FILED 잠금 차단을 테스트해줘.
```

### M04-04. 재무제표 입력 화면

```text
goal: 단계 1-1 재무제표 입력 화면을 구현해줘. 계정 행 입력, 합계 표시, 저장 버튼, 입력 오류, 저장 성공 상태, 결산서당기순이익 미리보기를 프론트 테스트로 검증해줘.
```

### M04-05. 계정과목 매핑 모델/API

```text
goal: 세부구현_신고_v1.0.md 4.3 기준으로 계정과목 매핑 조회/저장 API를 구현해줘. 원장 계정, 표준계정, 세무조정 연계 코드, 미매핑 상태를 저장하고 tenant 격리와 FILED 잠금을 테스트해줘.
```

### M04-06. 계정과목 매핑 화면

```text
goal: 단계 1-2 계정과목 매핑 화면을 구현해줘. 미매핑 필터, 표준계정 선택, 자동 추천 표시, 저장, 누락 경고를 프론트 테스트와 브라우저 확인으로 검증해줘.
```

### M04-07. 자산/감가상각 모델/API

```text
goal: 세부구현_신고_v1.0.md 4.4 기준으로 자산 및 감가상각 조회/저장 API를 구현해줘. 취득가액, 내용연수, 회사상각액, 세법한도, 한도초과액을 계산하고 B-4 연동 후보를 테스트해줘.
```

### M04-08. 자산/감가상각 화면

```text
goal: 단계 1-3 자산 및 감가상각 화면을 구현해줘. 자산 목록, 행 추가/수정, 세법한도 계산 결과, 한도초과 경고, 저장/재계산 버튼을 프론트 테스트로 검증해줘.
```

### M04-09. 세무정보 검증 API

```text
goal: 세부구현_신고_v1.0.md 4.5 기준으로 단계 1 일관성 검증 API를 구현해줘. 재무상태표 균형, 손익계산서 필수값, 미매핑 계정, 감가상각 한도초과를 오류/경고/정보로 분류해 테스트해줘.
```

### M04-10. 세무정보 검증 화면

```text
goal: 단계 1-9 일관성 검증 화면을 구현해줘. 검증 실행, 결과 요약, 오류/경고 목록, 대상 화면 이동, 오류가 있으면 단계 2 진입 차단 표시를 프론트 테스트로 검증해줘.
```

### M04-11. 단계 1 완료 상태 연결

```text
goal: 단계 1 세무정보 입력 완료 조건을 stepStatus와 진행률에 연결해줘. 재무제표 저장과 검증 PASS일 때 INFO가 COMPLETED가 되고 오류가 있으면 WARNING 또는 IN_PROGRESS가 되도록 API/프론트 테스트를 추가해줘.
```

### M04-12. 단계 1 브라우저 확인

```text
goal: 브라우저에서 단계 1 전체 흐름을 확인해줘. 재무제표 입력, 계정 매핑, 자산 입력, 일관성 검증 실행, 스테퍼 상태 변경, 저장 상태 표시가 정상 동작하는지 확인하고 발견한 문제를 수정해줘.
```

## M05. 단계 2 세무조정

### M05-01. 세무조정 공통 저장 모델

```text
goal: 세부구현_신고_v1.0.md 5.3 기준으로 tax_adjustments와 adjustment_items 공통 저장 모델을 정리해줘. module_code, item_type, amount, source, reserve_type, status, updated_at, business_year 연결을 점검하고 부족한 필드를 보완해줘.
```

### M05-02. 세무조정 공통 조회 API

```text
goal: 단계 2 세무조정 공통 조회 API를 구현해줘. 사업연도별 모듈 목록, 각 모듈 상태, 조정 항목, 자동/수동 구분, 저장 가능 여부를 반환하고 권한/잠금/tenant 격리를 테스트해줘.
```

### M05-03. 세무조정 공통 저장 API

```text
goal: 단계 2 세무조정 공통 저장 API를 구현해줘. module_code별 수동 조정 항목 저장, 자동 항목 보호, 합계 재계산, FILED 잠금 차단, 잘못된 모듈 코드 차단을 테스트해줘.
```

### M05-04. 세무조정 모듈 트리 화면

```text
goal: 단계 2 세무조정 모듈 트리 화면을 구현해줘. 17개 모듈, 완료/작업중/오류 상태, 검색 또는 그룹, 현재 모듈 선택, 빈 상태를 프론트 테스트로 검증해줘.
```

### M05-05. 공통 조정 항목 편집 UI

```text
goal: 세무조정 공통 조정 항목 편집 UI를 구현해줘. 수동 항목 추가/수정/삭제, 자동 항목 읽기 전용, 금액 합계, 저장/재계산, 오류 표시를 대표 모듈에서 테스트해줘.
```

### M05-06. 유보 연계 엔진

```text
goal: 세무조정 저장 시 reserves를 갱신하는 공통 유보 연계 엔진을 구현해줘. 자동 생성 항목 중복 방지, 금액 변경 시 정정, 항목 삭제 시 회수, reserve_type별 집계를 통합 테스트로 검증해줘.
```

### M05-07. B-1 계산 API

```text
goal: B-1 소득금액조정명세서 계산 API를 구현해줘. 결산서당기순이익, 익금산입/손금불산입, 손금산입/익금불산입, 자동 연동 항목 합계, 각 사업연도 소득금액 계산을 테스트해줘.
```

### M05-08. B-1 화면

```text
goal: B-1 소득금액조정명세서 화면을 구현해줘. 자동 반영 항목, 수동 조정 항목, 분류별 합계, 계산 결과, 저장 후 재계산을 프론트 테스트와 브라우저 확인으로 검증해줘.
```

### M05-09. B-4 감가상각 API

```text
goal: B-4 감가상각비 조정 API를 구현해줘. 단계 1 자산 데이터에서 한도초과액을 불러오고 손금불산입/유보 항목을 생성하며 B-1과 reserves 반영을 통합 테스트로 검증해줘.
```

### M05-10. B-4 화면

```text
goal: B-4 감가상각비 조정 화면을 구현해줘. 자산별 회사상각액, 세법한도, 한도초과액, 조정 반영 여부, B-1 반영 미리보기를 프론트 테스트로 검증해줘.
```

### M05-11. B-2 기부금 조정

```text
goal: B-2 기부금 조정을 API와 화면으로 구현해줘. 기부금 유형, 한도 계산, 한도초과액, 이월 기부금, B-1 손금불산입 연동, 관련 서식 연동 후보를 테스트해줘.
```

### M05-12. B-3 접대비 조정

```text
goal: B-3 접대비 조정을 API와 화면으로 구현해줘. 접대비 입력, 증빙 구분, 한도 계산, 무증빙 경고, 손금불산입 자동 생성, B-1 연동을 테스트해줘.
```

### M05-13. B-11 이월결손금

```text
goal: B-11 이월결손금 조정을 API와 화면으로 구현해줘. 연도별 발생액, 공제액, 잔액, 공제 한도, 당기 만료 경고, B-1 손금산입과 FORM3 연동 후보를 테스트해줘.
```

### M05-14. B-15 자본금과 적립금

```text
goal: B-15 자본금과 적립금 화면과 집계 API를 구현해줘. reserves 전체 집계, 유보 발생/감소/잔액, 자본금·적립금 표, B-15 합계와 FORM3 연동 후보를 테스트해줘.
```

### M05-15. B-12 세액공제·감면

```text
goal: B-12 세액공제·감면을 API와 화면으로 구현해줘. 공제감면 항목, 법령 스냅샷 기준 한도, R&D 등 주요 항목, FORM3 공제·감면 연동, 법령 변경 경고를 테스트해줘.
```

### M05-16. B-13 최저한세

```text
goal: B-13 최저한세를 API와 화면으로 구현해줘. 감면 후 세액과 최저한세 비교, 추가납부액 계산, FORM3 최저한세 반영 후보, 검증 규칙 연계를 테스트해줘.
```

### M05-17. B-14 가산세

```text
goal: B-14 가산세를 API와 화면으로 구현해줘. 가산세 유형, 산출 기준, 기간/율 계산, 수동 조정, FORM3 가산세 반영 후보, 오류 입력 방지를 테스트해줘.
```

### M05-18. 나머지 모듈 라우트/화면 골격

```text
goal: 세부구현_신고_v1.0.md의 17개 세무조정 모듈 중 아직 구현되지 않은 모듈의 독립 라우트와 화면 골격을 추가해줘. 각 모듈이 공통 더미 화면이 아니라 개별 화면으로 열리고 최소 조회/저장/잠금 테스트를 갖게 해줘.
```

### M05-19. 세무조정 완료 상태

```text
goal: 단계 2 세무조정 완료 상태 계산을 구현해줘. 필수 모듈 B-1/B-15 완료 조건, 선택 모듈 경고 상태, 오류가 있을 때 단계 3 진입 차단을 API/프론트 테스트로 검증해줘.
```

### M05-20. 세무조정 브라우저 확인

```text
goal: 브라우저에서 단계 2 세무조정 주요 화면을 확인해줘. 모듈 트리, B-1, B-4, B-15, 나머지 모듈 대표 화면 진입, 저장 버튼 상태, 스테퍼 상태가 정상인지 검증하고 문제를 수정해줘.
```

## M06. 단계 3 서식 작성

### M06-01. form_data 모델/API 점검

```text
goal: 세부구현_신고_v1.0.md 6.3 기준으로 form_data 모델과 API를 점검해줘. form_code, version, field values, 자동/수동 값 구분, 이력, business_year 연결, FILED 잠금에 필요한 구조를 보완해줘.
```

### M06-02. 서식 목록 API

```text
goal: 단계 3 서식 목록 API를 구현해줘. 사업연도에 필요한 별지 제3호와 부속서식 목록, 작성 필요 여부, 완료 상태, 오류 수, 마지막 수정 시각을 반환하고 tenant 격리를 테스트해줘.
```

### M06-03. FORM3 자동 산출 API

```text
goal: 별지 제3호 FORM3 자동 산출 API를 구현해줘. B-1, B-2, B-11, B-12, B-13, B-14 값을 반영해 과세표준, 산출세액, 납부세액 후보를 계산하고 스냅샷 테스트를 추가해줘.
```

### M06-04. FORM3 화면

```text
goal: 별지 제3호 화면을 구현해줘. 자동 산출 값, 수동 입력 필드, 계산 근거, 저장/재계산, 오류 표시, 읽기 전용 상태를 프론트 테스트와 브라우저 확인으로 검증해줘.
```

### M06-05. 부속서식 일람 화면

```text
goal: 부속서식 일람 화면을 구현해줘. 서식 검색, 작성 필요/완료/오류 필터, 서식별 진입, 상태 배지, 빈 상태, 권한별 편집 버튼 표시를 프론트 테스트로 검증해줘.
```

### M06-06. 공통 서식 필드 렌더러

```text
goal: 부속서식 공통 필드 렌더러를 구현해줘. 텍스트, 숫자, 날짜, 선택, 계산 필드 타입을 표시하고 자동 산출 필드는 읽기 전용으로 처리하며 필드별 오류 메시지를 테스트해줘.
```

### M06-07. 서식 저장 API

```text
goal: 서식 field values 저장 API를 구현해줘. 자동 값 변경 차단, 수동 값 저장, updated_at 충돌 또는 기존 저장 규칙, FILED 잠금 차단, 저장 이력 생성을 테스트해줘.
```

### M06-08. 자동 값 재계산 보호

```text
goal: 세무조정 결과로 채워지는 자동 값과 사용자가 수정한 수동 값을 구분하는 재계산 보호 규칙을 구현해줘. 자동 값은 갱신되고 수동 값은 보존되며 충돌 시 재확인 상태가 표시되는지 테스트해줘.
```

### M06-09. 서식 간 연동 규칙 API

```text
goal: 세부구현_신고_v1.0.md 6.2 기준으로 서식 간 연동 규칙 API를 구현해줘. FORM3 필드와 부속서식 합계, B-15 합계, 음수 불가 항목, 누락 항목을 검증하고 target_route를 반환하게 해줘.
```

### M06-10. 서식 간 연동 화면 표시

```text
goal: 서식 간 연동 검증 결과를 화면에 표시해줘. 오류/경고 목록, 대상 서식 이동, 대상 필드 강조, 재검증 버튼을 구현하고 프론트 테스트로 검증해줘.
```

### M06-11. 단계 3 완료 상태

```text
goal: 단계 3 서식 작성 완료 상태를 stepStatus에 연결해줘. FORM3 생성 완료와 서식 검증 ERROR 0건일 때 FORM 단계가 COMPLETED가 되고 오류가 있으면 WARNING 또는 IN_PROGRESS가 되도록 테스트해줘.
```

### M06-12. 서식 작성 브라우저 확인

```text
goal: 브라우저에서 단계 3 서식 작성 흐름을 확인해줘. FORM3 진입, 부속서식 일람, 공통 서식 편집, 서식 간 연동 검증, 저장/재계산, 스테퍼 상태를 확인하고 문제를 수정해줘.
```

## M07. 단계 4 검증과 단계 5 결재

### M07-01. validation_runs 모델/API

```text
goal: 세부구현_신고_v1.0.md 7.2~7.3 기준으로 validation_runs와 validation_results 조회/실행 API를 구현해줘. rule_code, severity, target_route, target_field, message, 최신 실행 여부, tenant 격리를 테스트해줘.
```

### M07-02. 핵심 검증 규칙 1차

```text
goal: 단계 1~3의 핵심 검증 규칙 1차 세트를 구현해줘. 세무정보 누락, 필수 세무조정 미완료, FORM3 합계 불일치, 서식 오류 존재 시 ERROR로 분류되는지 테스트해줘.
```

### M07-03. 검증 실행 화면

```text
goal: 단계 4 검증 화면을 구현해줘. 검증 실행 버튼, 결과 요약, ERROR/WARNING/INFO 필터, 대상 화면 이동, 최신 실행 시각, 통과/실패 상태를 프론트 테스트로 검증해줘.
```

### M07-04. 검증 통과 상태 전이

```text
goal: 검증 결과 ERROR 0건일 때 사업연도 상태와 stepStatus를 VALIDATED로 전환하는 흐름을 구현해줘. 경고만 있는 경우, 오류가 있는 경우, 이전 단계 수정 후 재검증 필요 상태를 API 테스트로 검증해줘.
```

### M07-05. 결재 모델/API

```text
goal: 세부구현_신고_v1.0.md 8.3~8.4 기준으로 approval_lines와 결재 상태 전이 API를 구현해줘. 결재 요청, 검토자 지정, 승인, 반려, 메모, SUBMITTED/APPROVED/REJECTED 전이를 테스트해줘.
```

### M07-06. 작성자 결재 요청 화면

```text
goal: 작성자 결재 요청 화면을 구현해줘. 검증 통과 전 요청 차단, 결재자 선택, 요청 메모, 제출 후 상태 표시, 반려 후 재요청 흐름을 프론트 테스트로 검증해줘.
```

### M07-07. 검토자 결재함 화면

```text
goal: 검토자 결재함과 승인/반려 화면을 구현해줘. 결재 대기 목록, 상세 보기, 승인/반려 메모, 권한별 버튼 표시, 승인 후 APPROVED 전환, 반려 후 재작업 상태를 테스트해줘.
```

### M07-08. 결재 알림

```text
goal: 결재 요청, 승인, 반려 이벤트를 알림에 연결해줘. 대상 사용자 알림 생성, 읽음 처리, 대시보드/작업목록 상태 동기화를 통합 테스트로 검증해줘.
```

### M07-09. 결재 감사 로그

```text
goal: 결재 요청, 승인, 반려 이벤트의 감사 로그 기록을 구현해줘. actor, target business_year, action, before/after status, memo 포함 여부를 테스트해줘.
```

### M07-10. 검증/결재 브라우저 확인

```text
goal: 브라우저에서 단계 4 검증과 단계 5 결재 흐름을 확인해줘. 검증 실행, 오류 필터, target 이동, 결재 요청, 검토자 화면 진입, 승인/반려 버튼 상태를 검증하고 문제를 수정해줘.
```

## M08. 단계 6 출력과 단계 7 전자신고

### M08-01. PDF 출력 대상 API

```text
goal: 세부구현_신고_v1.0.md 9.1 기준으로 PDF 출력 대상 조회 API를 구현해줘. FORM3와 부속서식 목록, 출력 가능 상태, 승인 전/후 제약, FILED 전후 동작, tenant 격리를 테스트해줘.
```

### M08-02. PDF 생성 API

```text
goal: PDF 생성 API를 구현해줘. 선택 서식 데이터 조합, 생성 이력 저장, 다운로드 응답, 실패 처리, 대용량 서식 처리 기본 한계를 테스트해줘.
```

### M08-03. 워터마크 매핑

```text
goal: 세부구현_신고_v1.0.md 9.3 기준으로 PDF 워터마크 매핑을 구현해줘. DRAFT, APPROVED, FILED 상태별 워터마크가 적용되고 상태 변경 후 새 출력에 반영되는지 테스트해줘.
```

### M08-04. 출력 화면

```text
goal: 단계 6 출력 화면을 구현해줘. 출력 대상 선택, 미리보기 또는 생성 진행 상태, 다운로드, 워터마크 표시, 오류 상태, 승인 전/후 버튼 제어를 프론트 테스트로 검증해줘.
```

### M08-05. efiling 모델/API

```text
goal: 세부구현_신고_v1.0.md 10.3 기준으로 efiling 데이터 모델과 API를 구현해줘. 파일 생성 요청, 파일 메타데이터, 오류검증 결과, 제출 이력, 접수번호 저장 구조를 테스트해줘.
```

### M08-06. 전자신고 규격 매핑표

```text
goal: 전자신고_2026년 1차 파일설명서의 법인세 파일 규격을 코드에서 사용할 매핑표로 정리해줘. 레코드 구분, 필드명, 길이, 타입, 필수 여부, 원천 form field를 표로 만들고 스냅샷 테스트 기반을 준비해줘.
```

### M08-07. 고정길이 파일 생성기

```text
goal: 전자신고 고정길이 파일 생성기를 구현해줘. 숫자/문자 패딩, 날짜 형식, 필수값 누락, 길이 초과, 레코드 순서를 처리하고 대표 레코드 스냅샷 테스트를 추가해줘.
```

### M08-08. 전자신고 오류검증 API

```text
goal: 전자신고 파일 생성 전후 오류검증 API를 구현해줘. 필수 레코드 누락, 필드 길이 오류, 숫자 형식 오류, 서식 값 불일치, ERROR 존재 시 제출 차단을 테스트해줘.
```

### M08-09. 전자신고 4단계 UI

```text
goal: 세부구현_신고_v1.0.md 10.1 기준으로 전자신고 4단계 UI를 구현해줘. 파일 생성, 오류검증, 제출 준비, 제출 완료 상태와 각 단계의 버튼/오류/재시도 흐름을 프론트 테스트로 검증해줘.
```

### M08-10. 2FA Step-up API 연결

```text
goal: 전자신고 제출 직전 2FA Step-up 검증을 구현해줘. TOTP 토큰 없음, 잘못된 토큰, 만료 토큰, 유효 토큰, ASSISTANT 제출 차단을 통합 테스트로 검증해줘.
```

### M08-11. 제출 처리와 FILED 전환

```text
goal: 전자신고 제출 성공 후 처리를 구현해줘. 접수번호, 접수일시, 제출파일 해시, efiling_history 저장, business_year FILED 전환, by_law_snapshot 잠금을 테스트해줘.
```

### M08-12. 출력/전자신고 브라우저 확인

```text
goal: 브라우저에서 단계 6 출력과 단계 7 전자신고 흐름을 확인해줘. PDF 대상 선택, 파일 생성, 오류검증, 2FA 모달, 제출 완료 상태, FILED 이후 읽기 전용 표시를 검증하고 문제를 수정해줘.
```

## M09. 수정신고, 안정화, 인수

### M09-01. FILED 백엔드 잠금 회귀

```text
goal: FILED 상태에서 신고작업 수정/삭제 API가 모두 차단되는지 회귀 테스트를 추가해줘. 세무정보, 세무조정, reserves, form_data, validation, approval, output, efiling 관련 대표 API를 포함해줘.
```

### M09-02. FILED 프론트 읽기 전용 회귀

```text
goal: FILED 상태에서 신고작업 프론트 전체가 읽기 전용으로 표시되는지 회귀 테스트를 추가해줘. 입력 필드, 저장/재계산/결재/전자신고 버튼, 안내 배너를 단계별 대표 화면에서 검증해줘.
```

### M09-03. 수정신고 생성 API

```text
goal: AMENDED 수정신고 생성 API를 구현해줘. 원신고 FILED 사업연도만 원본으로 허용하고 original_business_year 참조, 수정 사유, 초기 데이터 복사, 별도 상태 전이를 테스트해줘.
```

### M09-04. 수정신고 화면

```text
goal: 수정신고 생성과 작업 진입 화면을 구현해줘. 원신고 선택, 수정 사유 입력, 생성 후 AMENDED 사업연도 이동, 원신고 읽기 전용 유지, 권한별 버튼 표시를 프론트 테스트로 검증해줘.
```

### M09-05. 수정신고 변경분 비교

```text
goal: AMENDED 수정신고에서 원신고 대비 변경분 비교 기능을 구현해줘. 변경 필드, 조정 항목 변경, 세액 차이, 출력/전자신고에 필요한 차이 데이터를 API 테스트로 검증해줘.
```

### M09-06. Excel 템플릿 다운로드

```text
goal: 신고작업 Excel 템플릿 다운로드를 구현해줘. 재무제표, 계정매핑, 자산, 조정항목 템플릿을 현재 필드 정의와 맞추고 다운로드 응답과 기본 형식을 테스트해줘.
```

### M09-07. Excel 업로드 검증

```text
goal: 신고작업 Excel 업로드 검증을 구현해줘. 필수 컬럼 누락, 타입 오류, 금액 형식 오류, 알 수 없는 계정, 대용량 행 제한, FILED 잠금 차단을 테스트해줘.
```

### M09-08. 대용량 성능 시나리오

```text
goal: 대용량 신고작업 성능 시나리오를 작성해줘. 계정, 자산, 조정항목, 서식 필드가 많은 사업연도 fixture를 만들고 주요 API 응답 시간과 프론트 렌더링 병목을 측정해 개선 후보를 정리해줘.
```

### M09-09. 인덱스와 페이지네이션

```text
goal: 대용량 성능 시나리오에서 확인된 병목에 필요한 인덱스, 페이지네이션, lazy rendering을 적용해줘. 변경 전후를 비교할 수 있는 테스트 또는 재현 명령을 남겨줘.
```

### M09-10. 캐싱과 무효화

```text
goal: 신고작업 주요 조회 API의 캐싱과 무효화 전략을 구현해줘. 사업연도 요약, stepStatus, 서식 데이터, 검증 결과, 결재 상태가 저장/검증/승인/제출 후 최신 상태로 반영되는지 테스트해줘.
```

### M09-11. 대표 성공 E2E

```text
goal: 대표 성공 E2E 시나리오를 구현해줘. 사업연도 생성, 세무정보 입력, 세무조정, 서식 작성, 검증, 결재, PDF 출력, 전자신고 제출, FILED 잠금까지 자동 테스트와 브라우저 확인으로 검증해줘.
```

### M09-12. 역할/테넌트 회귀

```text
goal: 신고작업 전체 역할/테넌트 회귀 테스트를 확장해줘. SUPER_ADMIN, TENANT_ADMIN, TAX_EXPERT, TAX_REVIEWER, ASSISTANT별 허용/차단 행위와 다른 테넌트 접근 차단을 주요 API와 화면에서 검증해줘.
```

### M09-13. 문서 차이 보정

```text
goal: 세부구현_신고_v1.0.md와 실제 구현 차이를 최종 비교해줘. 의도적으로 다르게 구현한 부분, 미구현 항목, 운영상 주의사항, 테스트 명령을 README 또는 관련 문서에 최소 범위로 보완해줘.
```

### M09-14. 최종 인수 점검

```text
goal: 신고작업 전체 최종 인수 점검을 수행해줘. 스냅샷 계산, 유보 자동 연계, 서식 간 연동, 상태별 메뉴 활성화, 2FA Step-up, 잠금 불변성, 테넌트 격리, 역할 분리를 재검증하고 남은 리스크를 정리해줘.
```

---

# 나노 실행판 v2

이 섹션은 위의 마이크로 실행판보다 더 잘게 나눈 실행용 goal이다. 한 goal은 가능하면 하나의 데이터 모델, 하나의 API, 하나의 화면, 하나의 계산 규칙, 하나의 회귀 테스트 묶음만 끝내도록 작성했다.

## 사용 규칙

- `N00`부터 순서대로 진행한다. 단, 같은 단계 안의 화면 goal은 선행 API goal이 끝난 뒤 병렬로 진행할 수 있다.
- 각 goal은 구현, 테스트, 필요한 문서 갱신, 실행 결과 보고까지 포함한다.
- 백엔드 변경 goal은 `cargo fmt`와 관련 `cargo test`를 실행한다.
- 프론트 변경 goal은 관련 프론트 테스트와 브라우저 화면 확인까지 완료한다.
- FILED 잠금, tenant 격리, customer/business_year 권한은 API가 생길 때마다 반복 검증한다.
- 완료 보고에는 변경 파일, 실행한 테스트, 남은 리스크를 짧게 남긴다.

---

## N00. 기준선과 추적표

### N00-01. 원문 요구사항 추적표

```text
goal: [N00-01] 세부구현_신고_v1.0.md 1~16장을 요구사항 추적표로 변환해줘. 각 항목에 문서 절 번호, 기능명, 대상 DB/API/프론트/테스트, 구현 상태, 후속 goal 번호를 적고 이후 작업에서 갱신할 수 있게 해줘. 검증: 누락된 장/절이 없는지 목차와 대조해 보고해줘.
```

### N00-02. 현재 DB 스키마 기준선

```text
goal: [N00-02] 현재 migrations와 src 도메인 모델을 기준으로 신고작업 DB 기준선을 정리해줘. business_years, financial_statements, fs_lines, account_mappings, assets, depreciation, transactions, tax_adjustments, adjustment_items, reserves, form_data, validation_runs, approval_lines, efiling_history의 존재 여부와 부족한 컬럼을 표로 남겨줘. 검증: 실제 migration 파일명과 누락 DDL을 함께 보고해줘.
```

### N00-03. 현재 API 기준선

```text
goal: [N00-03] 세부구현_신고_v1.0.md 13.1 엔드포인트 목록과 현재 src/api.rs 라우트를 비교해줘. 구현됨/부분구현/미구현을 구분하고, 각 미구현 API를 이후 N goal에 연결해줘. 검증: 라우트 문자열 검색 결과와 테스트 파일 존재 여부를 함께 보고해줘.
```

### N00-04. 현재 프론트 라우트 기준선

```text
goal: [N00-04] 신고작업 프론트 라우트와 화면 구현 현황을 점검해줘. 작업 시작, 세무정보, 세무조정, 서식, 검증, 결재, 출력, 전자신고 화면별 실제 렌더 함수와 테스트 존재 여부를 매핑하고 개별 화면으로 분리되지 않은 항목을 표시해줘. 검증: leaf screen 계약 테스트에 반영할 갭 목록을 보고해줘.
```

### N00-05. 테스트 실행 기준선

```text
goal: [N00-05] 신고작업 구현 전 테스트 기준선을 고정해줘. 현재 통과/실패하는 cargo test, 프론트 테스트, 브라우저 확인 가능 URL을 정리하고 실패가 있으면 신고작업과 관련 있는지 분류해줘. 검증: 이후 goal에서 비교할 수 있도록 실행 명령과 핵심 결과를 문서에 남겨줘.
```

### N00-06. 신고작업 fixture 설계

```text
goal: [N00-06] 신고작업 테스트 fixture를 설계해줘. 정상 고객사, 권한 없는 고객사, DRAFT/IN_REVIEW/APPROVED/FILED/AMENDED 사업연도, 대표 재무제표, 자산, 거래, 조정, 서식 데이터를 포함하고 API/프론트/E2E 테스트에서 재사용 가능하게 해줘. 검증: fixture 생성 함수 또는 seed 경로와 사용 예시를 남겨줘.
```

---

## N01. 공통 상태 모델과 DB 기반

### N01-01. business_year 상태 enum

```text
goal: [N01-01] business_year 상태값 DRAFT, IN_REVIEW, APPROVED, FILED, AMENDED를 백엔드 타입 또는 검증 상수로 고정해줘. 잘못된 상태 저장을 차단하고 기존 데이터와 호환되는지 확인해줘. 검증: 상태 파싱/직렬화/잘못된 상태 거부 단위 테스트를 추가해줘.
```

### N01-02. 상태 전이 규칙

```text
goal: [N01-02] 세부구현_신고_v1.0.md 11.1 상태 전이 다이어그램을 기준으로 상태 전이 검증 함수를 구현해줘. DRAFT→IN_REVIEW, IN_REVIEW→APPROVED, APPROVED→FILED, FILED→AMENDED, 반려 시 DRAFT 복귀만 허용하고 잘못된 전이는 거부해줘. 검증: 허용/거부 전이를 모두 테스트해줘.
```

### N01-03. 단계 코드와 단계 메타데이터

```text
goal: [N01-03] 신고작업 단계 0~7과 서브 단계 코드를 공통 메타데이터로 정의해줘. 단계명, 화면 ID, 필요 역할, 선행 완료 조건, 편집 가능 상태를 한 곳에서 조회하게 하고 API와 프론트가 재사용할 수 있게 해줘. 검증: 단계 코드 누락과 중복을 테스트해줘.
```

### N01-04. 단계 활성화 매트릭스

```text
goal: [N01-04] 세부구현_신고_v1.0.md 11.2 상태별 단계 활성화 매트릭스를 백엔드 규칙으로 구현해줘. 상태별 편집 가능/조회 전용/진입 불가를 계산하고 프론트 컨텍스트 API가 사용할 수 있게 해줘. 검증: 모든 상태와 모든 단계 조합 테스트를 추가해줘.
```

### N01-05. 진행률 계산 함수

```text
goal: [N01-05] 세부구현_신고_v1.0.md 11.3 기준으로 진행률 계산 함수를 구현해줘. 단계별 완료 여부, 필수 모듈 완료, 검증 ERROR 0건, 결재/출력/신고 상태를 반영해 0~100%를 반환하게 해줘. 검증: 빈 작업, 부분 완료, APPROVED, FILED 케이스를 테스트해줘.
```

### N01-06. FILED 잠금 공통 헬퍼

```text
goal: [N01-06] FILED 상태 사업연도 수정 차단을 위한 공통 헬퍼를 구현해줘. 세무정보, 세무조정, 서식, 검증, 결재, 출력, 전자신고 저장성 API에서 재사용할 수 있게 하고 AMENDED는 별도 작업으로 허용해줘. 검증: FILED 수정 요청이 일관된 에러 코드로 실패하는 테스트를 추가해줘.
```

### N01-07. 사업연도 컨텍스트 조회 헬퍼

```text
goal: [N01-07] tenant/customer/business_year를 함께 조회하는 공통 컨텍스트 헬퍼를 구현해줘. 없는 사업연도, 다른 tenant 사업연도, 접근 불가 고객사, 삭제/비활성 고객사를 구분해 처리해줘. 검증: 정상/404/403 케이스 통합 테스트를 추가해줘.
```

### N01-08. 감사 로그 연결 지점

```text
goal: [N01-08] 신고작업 상태 변경과 주요 저장 작업에 사용할 감사 로그 연결 지점을 정의해줘. 생성, 저장, 검증 실행, 결재 요청, 승인/반려, PDF 생성, 전자신고 생성/제출을 이벤트 타입으로 나누고 최소 메타데이터를 기록하게 해줘. 검증: 대표 이벤트 기록 테스트를 추가해줘.
```

---

## N02. 권한, 공통 API, 컨텍스트

### N02-01. 고객사 접근 권한 검증

```text
goal: [N02-01] 세부구현_신고_v1.0.md 14.2 순서에 맞춰 고객사 접근 권한 검증을 구현해줘. v_user_effective_customer_access 또는 현재 코드의 동등한 권한 원천을 사용해 tenant/customer 접근을 먼저 차단해줘. 검증: 접근 가능 고객사와 불가능 고객사 API 테스트를 추가해줘.
```

### N02-02. 업무 범위 권한 검증

```text
goal: [N02-02] user_customer_work_scope 또는 현재 권한 구조를 기준으로 ADJUST, FORM, VALIDATE, APPROVE, EFILE 업무 범위 검증을 구현해줘. 같은 고객사라도 업무 범위가 없으면 관련 API를 차단해줘. 검증: 범위별 허용/거부 테스트를 추가해줘.
```

### N02-03. 역할별 단계 접근 검증

```text
goal: [N02-03] 세부구현_신고_v1.0.md 14.1 역할 매트릭스를 API 권한 검증에 반영해줘. TAX_EXPERT, TAX_REVIEWER, ASSISTANT, VIEWER별 편집/조회/차단을 구분하고 ASSISTANT의 결재·전자신고 차단을 강제해줘. 검증: 역할별 대표 API 테스트를 추가해줘.
```

### N02-04. 공통 API 에러 형식

```text
goal: [N02-04] 신고작업 API 공통 에러 응답 형식을 구현해줘. VALIDATION_REQUIRED, FORBIDDEN_SCOPE, LOCKED_FILED, INVALID_STATE_TRANSITION, NOT_FOUND_CONTEXT 같은 코드를 일관되게 반환하게 해줘. 검증: 대표 실패 API가 동일한 JSON 구조를 반환하는지 테스트해줘.
```

### N02-05. workspace context API

```text
goal: [N02-05] GET /api/workspace/{byId}/context를 구현해줘. 고객사명, 사업연도, 신고유형, 상태, 담당자, 마감일, 진행률, 마지막 저장 시각, stepStatus, readOnly 여부를 반환하게 해줘. 검증: 상태별 stepStatus와 권한별 readOnly 응답 테스트를 추가해줘.
```

### N02-06. workspace list API

```text
goal: [N02-06] GET /api/workspace/list를 구현해줘. 권한 범위 내 고객사와 사업연도만 반환하고 고객사명, 연도, 신고유형, 상태, 진행률, 마감일, 담당자 요약을 포함해줘. 검증: tenant 격리, 고객사 권한, 상태 필터 테스트를 추가해줘.
```

### N02-07. 공통 updated_at 충돌 처리

```text
goal: [N02-07] 신고작업 저장 API에 사용할 updated_at 기반 충돌 처리 규칙을 구현해줘. 클라이언트가 보낸 기준 시각보다 서버 데이터가 최신이면 409를 반환하고 최신 데이터를 함께 내려줘. 검증: 정상 저장과 충돌 저장 테스트를 추가해줘.
```

---

## N03. 공통 프론트 셸

### N03-01. 신고작업 라우트 상수

```text
goal: [N03-01] 신고작업 프론트 라우트 상수를 정리해줘. 작업 시작, 세무정보 서브 단계, 세무조정 모듈, 서식, 검증, 결재, 출력, 전자신고가 개별 라우트로 이동할 수 있게 하고 기존 메뉴 패턴과 맞춰줘. 검증: 라우트-렌더러 계약 테스트를 추가해줘.
```

### N03-02. 작업 컨텍스트 프론트 상태

```text
goal: [N03-02] 프론트에서 businessYearId 기반 작업 컨텍스트를 로드하고 보관하는 공통 상태를 구현해줘. 새로고침, 직접 URL 진입, 없는 사업연도, 권한 없음, 로딩 실패를 구분해서 표시해줘. 검증: 컨텍스트 로딩/실패 프론트 테스트를 추가해줘.
```

### N03-03. 공통 스테퍼 DOM

```text
goal: [N03-03] 세부구현_신고_v1.0.md 2.1~2.2 기준으로 신고작업 공통 스테퍼 DOM을 구현해줘. 0~7단계 표시, 현재/완료/잠금/비활성 상태, 클릭 이동 버튼, 작은 화면 표시를 포함해줘. 검증: 상태별 data 속성과 버튼 활성화 테스트, 브라우저 확인을 완료해줘.
```

### N03-04. 컨텍스트 배너

```text
goal: [N03-04] 세부구현_신고_v1.0.md 2.3 기준으로 컨텍스트 배너를 구현해줘. 고객사명, 사업연도, 신고유형, 상태, 담당자, 마감일, 진행률, 마지막 저장 시각을 표시하고 로딩/권한 없음/사업연도 없음 상태를 분리해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N03-05. 단계 진입 가드

```text
goal: [N03-05] 백엔드 context의 stepStatus를 기준으로 프론트 단계 진입 가드를 구현해줘. 잠긴 단계 직접 URL 접근 시 안내 화면 또는 허용 가능한 이전 단계로 이동시키고 조회 전용 단계는 읽기 전용으로 열어줘. 검증: 직접 URL, 스테퍼 클릭, 잠금 상태 테스트를 추가해줘.
```

### N03-06. 공통 읽기 전용 처리

```text
goal: [N03-06] FILED, IN_REVIEW, 권한 부족 상태에서 프론트 입력과 저장 버튼을 읽기 전용으로 만드는 공통 유틸을 구현해줘. 화면별로 중복 구현하지 않도록 기존 패턴에 맞게 연결해줘. 검증: 대표 입력 화면에서 저장 버튼 차단과 안내 배너를 테스트해줘.
```

### N03-07. 공통 저장 상태 표시

```text
goal: [N03-07] 신고작업 화면 공통 저장 상태 표시를 구현해줘. 저장 중, 저장 완료 시각, 저장 실패, 충돌 발생, 재시도 버튼을 같은 패턴으로 표시하게 해줘. 검증: 성공/실패/409 충돌 프론트 테스트를 추가해줘.
```

### N03-08. 공통 브라우저 검증 스크립트

```text
goal: [N03-08] 신고작업 화면별 브라우저 확인을 반복 실행할 수 있는 검증 스크립트 또는 절차를 만들어줘. 각 라우트가 비어 있지 않고, 올바른 data-stage/title을 렌더링하며, 주요 버튼이 클릭 가능한지 확인해줘. 검증: 최소 작업 시작, 세무정보, 세무조정, 서식, 검증 화면을 실제 브라우저에서 확인해줘.
```

---

## N04. 단계 0 작업 시작

### N04-01. 작업 목록 쿼리

```text
goal: [N04-01] 작업 시작 화면에 필요한 고객사/사업연도 목록 쿼리를 구현해줘. 고객사명, 사업연도, 신고유형, 상태, 진행률, 마감일, 담당자를 한 번에 가져오고 권한 범위를 반영해줘. 검증: fixture 기반 쿼리 테스트를 추가해줘.
```

### N04-02. 작업 목록 API

```text
goal: [N04-02] GET /api/workspace/list를 작업 시작 화면 요구사항에 맞게 완성해줘. 검색어, 상태 필터, 고객사 필터, 정렬, 빈 결과를 지원해줘. 검증: 필터/정렬/권한/빈 결과 API 테스트를 추가해줘.
```

### N04-03. 작업 시작 목록 화면

```text
goal: [N04-03] 단계 0 작업 시작 목록 화면을 구현해줘. 검색, 필터, 상태 배지, 진행률, 진입 버튼, 빈 상태, 로딩 상태를 포함하고 선택한 사업연도로 단계 1에 진입하게 해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N04-04. 사업연도 생성 요청 모델

```text
goal: [N04-04] POST /api/business-years 요청/응답 모델을 정리해줘. customer_id, year_label, start_date, end_date, filing_type, 담당자, 신고 마감일을 검증하고 중복 사업연도를 차단해줘. 검증: 유효성/중복/권한 테스트를 추가해줘.
```

### N04-05. 법령 스냅샷 생성

```text
goal: [N04-05] 사업연도 생성 시 by_law_snapshot을 함께 생성해줘. 적용 법령 버전, 세율, 한도, 서식 버전, 전자신고 스키마 버전을 현재 기준으로 고정하고 이후 신고 완료 시 잠글 수 있게 해줘. 검증: 생성된 스냅샷 값과 참조 무결성 테스트를 추가해줘.
```

### N04-06. 신규 사업연도 생성 API

```text
goal: [N04-06] POST /api/business-years를 완성해줘. 요청 검증, 권한 검증, DRAFT 상태 생성, 법령 스냅샷 생성, 감사 로그 기록, 생성 후 context 반환을 포함해줘. 검증: 성공/중복/권한없음/잘못된 날짜 테스트를 추가해줘.
```

### N04-07. 신규 사업연도 모달

```text
goal: [N04-07] 작업 시작 화면에 신규 사업연도 생성 모달을 구현해줘. 고객사 선택, 기간, 신고유형, 담당자, 법령 스냅샷 미리보기, 생성 버튼, 오류 표시를 포함해줘. 검증: 프론트 테스트와 브라우저 생성 흐름 확인을 완료해줘.
```

### N04-08. 작업 컨텍스트 복구

```text
goal: [N04-08] 작업 선택 후 URL/상태에 businessYearId를 반영하고 새로고침해도 같은 작업으로 복구되게 해줘. 권한이 사라진 경우 접근 차단 화면으로 전환해줘. 검증: 직접 URL, 새로고침, 없는 ID, 권한 없음 테스트를 추가해줘.
```

---

## N05. 단계 1 세무정보 입력

### N05-01. financial_statements 모델 점검

```text
goal: [N05-01] financial_statements와 fs_lines 모델을 세부구현_신고_v1.0.md 4.2/4.6에 맞게 점검하고 부족한 컬럼을 migration으로 보완해줘. 임포트 배치, 시트 구분, 계정코드, 계정명, 전기말, 당기말, 금액, 행 오류를 저장할 수 있게 해줘. 검증: migration과 모델 테스트를 추가해줘.
```

### N05-02. 재무제표 조회 API

```text
goal: [N05-02] GET /api/workspace/{byId}/financial-statements를 구현해줘. BS/IS/CF/자본변동 탭별 행, 요약 합계, 임포트 이력, 자동 매핑 결과를 반환해줘. 검증: tenant 격리, 없는 데이터, 정상 요약 테스트를 추가해줘.
```

### N05-03. 재무제표 저장 API

```text
goal: [N05-03] 재무제표 행 직접 저장 API를 구현해줘. 사용자가 입력한 BS/IS/CF 행을 검증하고 합계와 updated_at 충돌 처리를 적용해줘. 검증: 정상 저장, 타입 오류, FILED 잠금, 충돌 테스트를 추가해줘.
```

### N05-04. 재무제표 Excel 임포트 API

```text
goal: [N05-04] POST /api/workspace/{byId}/financial-statements/import를 구현해줘. 표준 템플릿 헤더 파싱, 필수 컬럼 검증, 행 단위 오류 반환, 차변/대변 합계 검증, 실패 배치 기록을 포함해줘. 검증: 정상 파일, 헤더 오류, 금액 오류, 합계 불일치 테스트를 추가해줘.
```

### N05-05. NET_INCOME 자동 연동

```text
goal: [N05-05] 재무제표 저장/임포트 후 IS.NET_INCOME을 B-1 소득금액조정의 결산서상 당기순이익 원천값으로 연결해줘. 값 변경 시 관련 조정/서식 캐시를 무효화해줘. 검증: 재무제표 변경 후 B-1 조회값이 갱신되는 테스트를 추가해줘.
```

### N05-06. 재무제표 입력 화면

```text
goal: [N05-06] 단계 1-1 재무제표 입력 화면을 구현해줘. BS/IS/CF/자본변동 탭, Excel 업로드, 템플릿 다운로드, 요약 합계, 행 오류 보기, 임포트 이력을 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N05-07. account_mappings 모델/API

```text
goal: [N05-07] account_mappings 모델과 GET/PUT /api/workspace/{byId}/account-mappings를 구현해줘. 고객사 계정과 표준 계정 매핑, 자동/수동/학습 방식, 미매핑 상태를 저장하고 조회하게 해줘. 검증: 자동 매핑, 수동 변경, 미매핑 필터, 권한 테스트를 추가해줘.
```

### N05-08. 계정과목 매핑 화면

```text
goal: [N05-08] 단계 1-2 계정과목 매핑 화면을 구현해줘. 자동 매핑률, 미매핑만 보기, 표준계정 선택, 전체 저장, 저장 후 검증 상태 반영을 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N05-09. assets/depreciation 모델/API

```text
goal: [N05-09] assets와 depreciation 모델 및 GET/PUT /api/workspace/{byId}/assets를 구현해줘. 자산명, 유형, 취득일, 취득가액, 내용연수, 상각방법, 회사상각액, 세법한도, 한도초과액을 저장/조회하게 해줘. 검증: 정상 저장, 금액 계산, FILED 잠금 테스트를 추가해줘.
```

### N05-10. 감가상각 한도 계산

```text
goal: [N05-10] 자산별 감가상각 세법한도와 한도초과액 계산을 구현해줘. 정액법/정률법 기본 케이스, 내용연수, 취득일 안분, 즉시상각의제 기본 판정을 포함해줘. 검증: 계산 단위 테스트와 B-4 후보 생성 테스트를 추가해줘.
```

### N05-11. 자산 및 감가상각 화면

```text
goal: [N05-11] 단계 1-3 자산 및 감가상각 화면을 구현해줘. 자산 추가, Excel 임포트 진입점, 분류 필터, 회사상각액/세법한도/한도초과 표시, B-4 이동 버튼을 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N05-12. transactions 모델/API

```text
goal: [N05-12] transactions 모델과 GET/PUT /api/workspace/{byId}/transactions를 구현해줘. 기부금, 접대비, 채권, 지급이자, 업무용승용차 관련 거래를 유형별로 저장/조회하게 해줘. 검증: 유형별 저장, 권한, FILED 잠금 테스트를 추가해줘.
```

### N05-13. 거래 명세 화면

```text
goal: [N05-13] 단계 1-4 거래 명세 화면을 구현해줘. 기부금/접대비/채권/지급이자 탭, 행 추가, Excel 업로드 진입점, 계정 합계와 거래 합계 차이 표시를 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N05-14. 업무용승용차 입력 화면

```text
goal: [N05-14] 단계 1-5 업무용승용차 운행기록 화면을 구현해줘. 차량별 월별 운행거리, 업무사용비율, 관련 비용, 한도초과 후보를 입력/계산하고 B-10 연동 후보를 만들게 해줘. 검증: 계산 테스트, 프론트 테스트, 브라우저 확인을 완료해줘.
```

### N05-15. 세무정보 일관성 검증 API

```text
goal: [N05-15] 단계 1-9 일관성 검증 API를 구현해줘. BS 자산 합계와 자산대장, IS 매출액과 거래 명세, 접대비/기부금 거래 합계, 필수 매핑 누락을 ERROR/WARN/INFO로 반환해줘. 검증: 정상/경고/오류 케이스 테스트를 추가해줘.
```

### N05-16. 세무정보 일관성 검증 화면

```text
goal: [N05-16] 단계 1-9 일관성 검증 화면을 구현해줘. 검증 실행 버튼, 결과 목록, 위치 이동 버튼, 경고만 있을 때 다음 단계 진입 가능 표시, 오류가 있을 때 차단 표시를 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

---

## N06. 단계 2 세무조정 공통

### N06-01. tax_adjustments 모델

```text
goal: [N06-01] tax_adjustments 모델을 세부구현_신고_v1.0.md 5.3/12.2에 맞게 정리해줘. 17개 module_code, business_year_id, 상태, 저장 여부, 마지막 계산 시각, snapshot_id를 저장하게 해줘. 검증: 모듈 코드 중복/누락 테스트를 추가해줘.
```

### N06-02. adjustment_items 모델

```text
goal: [N06-02] adjustment_items 모델을 구현해줘. 조정 항목명, 섹션, 금액, 처분, 출처, 자동/수동 여부, source_module, source_row_id, 정렬 순서를 저장하게 해줘. 검증: 자동 항목과 수동 항목 저장/조회 테스트를 추가해줘.
```

### N06-03. reserves 모델과 계산

```text
goal: [N06-03] reserves 모델과 closing_balance 계산을 구현해줘. prior_balance, current_increase, current_decrease, closing_balance, module_code, snapshot_id를 저장하고 B-1/B-15에서 재사용하게 해줘. 검증: 기초/증가/추인/기말 잔액 테스트를 추가해줘.
```

### N06-04. 세무조정 모듈 목록 API

```text
goal: [N06-04] GET /api/workspace/{byId}/adjustments를 구현해줘. 17개 모듈의 이름, 분류, 연계 서식, 저장 상태, 오류 수, 완료 여부, 다음/이전 모듈 정보를 반환해줘. 검증: 빈 사업연도 기본 목록과 저장 후 상태 갱신 테스트를 추가해줘.
```

### N06-05. 모듈별 조회 API 공통

```text
goal: [N06-05] GET /api/workspace/{byId}/adjustments/{moduleCode} 공통 조회 흐름을 구현해줘. 모듈 헤더, 입력 행, 자동 연동 행, 계산 요약, 법령 스냅샷, 읽기 전용 여부를 반환하게 해줘. 검증: 정상/없는 모듈/권한/FILED 잠금 테스트를 추가해줘.
```

### N06-06. 모듈별 저장 API 공통

```text
goal: [N06-06] POST /api/workspace/{byId}/adjustments/{moduleCode}/save 공통 저장 흐름을 구현해줘. 수동 행 저장, 자동 행 보호, 유보 갱신 훅, B-1 재계산 훅, form_data 무효화, 감사 로그를 포함해줘. 검증: 저장/자동행 수정 거부/FILED 잠금/무효화 테스트를 추가해줘.
```

### N06-07. 세무조정 모듈 트리 화면

```text
goal: [N06-07] 단계 2 세무조정 모듈 트리 화면을 구현해줘. 소득금액, 자산기반, 거래기반, 평가/이월, 세액계산, 특수 분류와 17개 모듈 상태를 표시하고 라우트 이동을 지원해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N06-08. 공통 조정 그리드

```text
goal: [N06-08] 세무조정 공통 Excel-like 조정 그리드를 구현해줘. 행 추가/삭제, 복사/붙여넣기 기본, 금액 포맷, 처분 선택, 자동 행 읽기 전용, 저장/검증 버튼을 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N06-09. 법령 버전 배너

```text
goal: [N06-09] 세무조정 화면 상단에 적용 법령 버전 배너를 구현해줘. by_law_snapshot의 세율/한도/공제 버전과 잠금 상태를 표시하고 법령 변경 시 재계산 권장 경고를 보여줘. 검증: 스냅샷 있음/없음/잠금 상태 프론트 테스트를 추가해줘.
```

---

## N07. 핵심 세무조정 모듈

### N07-01. B-1 원천값 집계 API

```text
goal: [N07-01] B01_INCOME_ADJ 소득금액조정 원천값 집계 API를 구현해줘. IS.NET_INCOME, 다른 모듈 자동 익금산입/손금불산입, 전기유보 추인, 수동 조정 항목을 합산해 차가감 소득금액을 계산해줘. 검증: 자동/수동/전기유보 케이스 테스트를 추가해줘.
```

### N07-02. B-1 저장과 FORM3 연동

```text
goal: [N07-02] B-1 저장 시 FORM3 ①~④ 원천값을 갱신하는 연동을 구현해줘. 자동 산출값과 수동 수정 보호 규칙을 지키고 form_data 갱신 이력을 남겨줘. 검증: B-1 저장 후 FORM3 조회값 변경 테스트를 추가해줘.
```

### N07-03. B-1 화면

```text
goal: [N07-03] B-1 소득금액조정명세서 화면을 구현해줘. A 익금산입/손금불산입, B 손금산입/익금불산입, 자동/수동 출처, 소계, 차가감 소득금액, 저장 버튼을 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N07-04. B-4 감가상각 계산 API

```text
goal: [N07-04] B04_DEPRECIATION 감가상각비 조정 계산 API를 구현해줘. assets/depreciation에서 회사계상액, 세법한도, 한도초과액을 가져와 유보 자동 생성 후보와 B-1 익금산입 자동 항목을 만들게 해줘. 검증: 한도초과 있음/없음 테스트를 추가해줘.
```

### N07-05. B-4 저장과 유보 연계

```text
goal: [N07-05] B-4 저장 시 한도초과액을 reserves와 B-1 자동 항목으로 연계해줘. 기존 자동 항목 중복 생성 없이 재저장 시 갱신되게 해줘. 검증: 최초 저장, 금액 변경 저장, 중복 방지 테스트를 추가해줘.
```

### N07-06. B-4 화면

```text
goal: [N07-06] B-4 감가상각비 조정 화면을 구현해줘. 자산대장 연동 표, 회사계상액, 세법한도액, 한도초과액, 처분, 한도초과 합계, 자산대장 이동 버튼을 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N07-07. B-2 기부금 유형/한도 모델

```text
goal: [N07-07] B02_DONATION 기부금 유형과 한도 계산 모델을 구현해줘. 특례, 일반, 이월 기부금, 한도초과, 손금산입 가능액을 표현하고 법령 스냅샷 한도를 사용하게 해줘. 검증: 특례/일반/한도초과 계산 테스트를 추가해줘.
```

### N07-08. B-2 저장과 B-1 연동

```text
goal: [N07-08] B-2 저장 시 기부금 한도초과액을 B-1과 FORM3 ⑤에 연동해줘. 거래 명세 원천과 수동 조정 차이를 표시하고 재저장 시 자동 항목을 갱신해줘. 검증: 한도초과 0원/발생 케이스 테스트를 추가해줘.
```

### N07-09. B-2 화면

```text
goal: [N07-09] B-2 기부금 조정 화면을 구현해줘. 기부금 거래 목록, 유형 선택, 소득금액, 특례/일반 한도, 이월 기부금, 한도초과액, 저장 버튼을 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N07-10. B-3 접대비 한도 계산

```text
goal: [N07-10] B03_ENTERTAINMENT 접대비 한도 계산을 구현해줘. 중소기업 여부, 기본한도, 수입금액 한도, 신용카드/현금영수증 증빙, 무증빙 전액 불산입, 한도초과액을 계산해줘. 검증: 중소기업/일반/무증빙/한도초과 테스트를 추가해줘.
```

### N07-11. B-3 저장과 B-1 연동

```text
goal: [N07-11] B-3 저장 시 무증빙 손금불산입과 한도초과액을 B-1 익금산입 자동 항목으로 연동해줘. 거래 명세 변경 후 재계산 시 기존 자동 항목을 갱신해줘. 검증: 거래 변경 재계산과 중복 방지 테스트를 추가해줘.
```

### N07-12. B-3 화면

```text
goal: [N07-12] B-3 기업업무추진비 조정 화면을 구현해줘. 기본한도, 수입금액 한도, 증빙별 지출액, 무증빙 불산입, 총 한도초과, B-1 반영 안내를 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N07-13. B-11 이월결손금 모델/API

```text
goal: [N07-13] B11_LOSS_CARRYOVER 이월결손금 모델과 조회/저장 API를 구현해줘. 발생연도, 잔액, 만료연도, 당기공제액, 잔여액, 만료 알림, 공제한도를 관리하게 해줘. 검증: 만료 임박, 한도 초과 공제 차단, 잔액 갱신 테스트를 추가해줘.
```

### N07-14. B-11 FORM3 연동과 화면

```text
goal: [N07-14] B-11 저장 시 FORM3 ⑦ 이월결손금과 B-1 손금산입 자동 항목을 갱신하고 화면을 구현해줘. 공제한도, 총 공제액, 잔여액, 만료 경고를 표시해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N07-15. B-15 자본금과 적립금 집계

```text
goal: [N07-15] B15_CAPITAL_RESERVE 자본금과 적립금 집계 API를 구현해줘. 자본금/이익잉여금 입력값과 reserves 전체를 집계해 BJ_50갑/을 원천 데이터를 만들게 해줘. 검증: 여러 모듈 유보 합산과 전기말/당기증감/기말 잔액 테스트를 추가해줘.
```

### N07-16. B-15 화면

```text
goal: [N07-16] B-15 자본금과 적립금 화면을 구현해줘. 갑표 자본금/이익잉여금, 을표 유보소득 자동 집계, 모듈별 출처, 불일치 경고를 표시해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N07-17. B-12 세액공제 모델/API

```text
goal: [N07-17] B12_TAX_CREDIT 세액공제·감면 모델과 계산 API를 구현해줘. R&D, 통합투자, 중소기업특별감면 등 공제 유형별 대상금액, 한도, 실공제액, 허용 공제액을 계산해줘. 검증: 유형별 한도와 FORM3 ⑬ 연동 테스트를 추가해줘.
```

### N07-18. B-12 화면

```text
goal: [N07-18] B-12 세액공제·감면 화면을 구현해줘. 공제 유형 선택, 대상금액 입력, 한도/허용 공제액 계산, 산출세액 연동, FORM3 반영 안내를 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N07-19. B-13 최저한세 계산/API

```text
goal: [N07-19] B13_MIN_TAX 최저한세 계산 API를 구현해줘. 공제 후 일반세액과 최저한세를 비교하고 최저한세 초과분을 추가 납부세액으로 산출해 FORM3 ⑭에 연동해줘. 검증: 추가 납부 없음/발생 케이스 테스트를 추가해줘.
```

### N07-20. B-13 화면

```text
goal: [N07-20] B-13 최저한세 화면을 구현해줘. 과세표준, 최저세율, 일반세액, 공제 후 세액, 추가 납부세액 계산표와 저장 버튼을 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N07-21. B-14 가산세 계산/API

```text
goal: [N07-21] B14_PENALTY 가산세 계산 API를 구현해줘. 무신고, 과소신고, 납부지연, 지급명세서 등 유형별 기준세액, 세율, 수정신고 감면율을 적용하고 FORM3 ⑮에 연동해줘. 검증: 유형별 계산 테스트를 추가해줘.
```

### N07-22. B-14 화면

```text
goal: [N07-22] B-14 가산세 화면을 구현해줘. 가산세 유형 추가, 기준세액, 세율, 감면율, 산출 가산세, FORM3 반영 안내를 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N07-23. B-5 퇴직급여 기본 골격

```text
goal: [N07-23] B05_RETIREMENT 퇴직급여충당금 모듈의 기본 API와 화면 골격을 구현해줘. 입력/저장/완료 상태, B-1 자동 항목 연결 지점을 만들고 상세 계산은 후속 확장 가능하게 분리해줘. 검증: 라우트, 저장, 읽기 전용 테스트를 추가해줘.
```

### N07-24. B-6 대손충당금 기본 골격

```text
goal: [N07-24] B06_BAD_DEBT 대손충당금 모듈의 기본 API와 화면 골격을 구현해줘. 채권 거래 원천 연결, 한도 계산 자리, B-1 자동 항목 연결 지점을 포함해줘. 검증: 라우트, 저장, 읽기 전용 테스트를 추가해줘.
```

### N07-25. B-7 외화평가 기본 골격

```text
goal: [N07-25] B07_FOREX 외화평가 모듈의 기본 API와 화면 골격을 구현해줘. 외화자산/부채, 환율, 평가손익 입력 구조와 B-1 자동 항목 연결 지점을 포함해줘. 검증: 라우트, 저장, 읽기 전용 테스트를 추가해줘.
```

### N07-26. B-8 재고·유가증권 평가 기본 골격

```text
goal: [N07-26] B08_INVENTORY 재고·유가증권 평가 모듈의 기본 API와 화면 골격을 구현해줘. 평가방법, 장부가, 평가액, 평가손익 입력 구조와 B-1 자동 항목 연결 지점을 포함해줘. 검증: 라우트, 저장, 읽기 전용 테스트를 추가해줘.
```

### N07-27. B-9 지급이자 기본 골격

```text
goal: [N07-27] B09_INTEREST 지급이자 손금불산입 모듈의 기본 API와 화면 골격을 구현해줘. 지급이자 거래 원천, 손금불산입 계산 자리, B-1 자동 항목 연결 지점을 포함해줘. 검증: 라우트, 저장, 읽기 전용 테스트를 추가해줘.
```

### N07-28. B-10 업무용승용차 기본 골격

```text
goal: [N07-28] B10_BUSINESS_CAR 업무용승용차 모듈의 기본 API와 화면 골격을 구현해줘. 운행기록 원천, 업무사용비율, 관련비용 한도, B-1 자동 항목 연결 지점을 포함해줘. 검증: 라우트, 저장, 읽기 전용 테스트를 추가해줘.
```

### N07-29. B-16 외국법인 기본 골격

```text
goal: [N07-29] B16_FOREIGN_CORP 외국법인 세무조정 모듈의 기본 API와 화면 골격을 구현해줘. 해당 여부 플래그, 필요한 입력 placeholder, 완료 상태, 권한/잠금 규칙을 포함해줘. 검증: 해당 없음/해당 있음 라우트 테스트를 추가해줘.
```

### N07-30. B-17 연결납세 기본 골격

```text
goal: [N07-30] B17_CONSOLIDATED 연결납세 모듈의 기본 API와 화면 골격을 구현해줘. 해당 여부 플래그, 연결법인 목록 placeholder, 완료 상태, 권한/잠금 규칙을 포함해줘. 검증: 해당 없음/해당 있음 라우트 테스트를 추가해줘.
```

### N07-31. 세무조정 완료 상태 계산

```text
goal: [N07-31] 17개 세무조정 모듈의 완료 상태 계산을 구현해줘. 필수 모듈, 해당 없음 모듈, 저장됨, 검증 오류 수를 반영해 단계 2 완료 여부와 진행률을 갱신해줘. 검증: 모든 핵심 모듈 완료/미완료/해당없음 테스트를 추가해줘.
```

---

## N08. 단계 3 서식 작성

### N08-01. form_data 모델/API 점검

```text
goal: [N08-01] form_data와 form_data_history 모델을 세부구현_신고_v1.0.md 6.3에 맞게 점검하고 보완해줘. form_code, business_year_id, snapshot_id, data JSON, 자동/수동 필드, 잠금 여부, 변경 이력을 저장하게 해줘. 검증: 저장/이력/잠금 테스트를 추가해줘.
```

### N08-02. 서식 목록 API

```text
goal: [N08-02] GET /api/workspace/{byId}/forms/list를 구현해줘. 서식코드, 서식명, 생성 상태, 검증 오류 수, PDF 가능 여부, 수정 가능 여부를 반환해줘. 검증: 생성완료/미생성/오류 있음 케이스 테스트를 추가해줘.
```

### N08-03. FORM3 자동 산출 API

```text
goal: [N08-03] 별지 제3호 FORM3 자동 산출 API를 구현해줘. B-1, B-2, B-11, B-12, B-13, B-14, 기납부세액 원천을 모아 ①~⑰ 값을 계산하고 자동 필드로 저장해줘. 검증: 주요 항목 계산과 차감납부세액 테스트를 추가해줘.
```

### N08-04. FORM3 조회/저장 API

```text
goal: [N08-04] GET/PUT /api/workspace/{byId}/forms/{formCode} 중 FORM3 조회/수동 수정 흐름을 구현해줘. 자동 필드는 직접 수정 차단 또는 override 이력으로 관리하고 직접 입력 필드는 저장 가능하게 해줘. 검증: 자동 필드 보호, 직접 필드 저장, 이력 테스트를 추가해줘.
```

### N08-05. FORM3 화면

```text
goal: [N08-05] 별지 제3호 과세표준 및 세액조정계산서 화면을 구현해줘. 자동연동 필드와 직접입력 필드 구분, 계산식 표시, 저장, 서식 검증, 변경 이력을 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N08-06. 부속서식 일람 화면

```text
goal: [N08-06] 부속서식 일람 화면을 구현해줘. 전체 생성, 미생성만 보기, ZIP 다운로드 진입점, 서식코드/서식명/상태/검증오류/조치 버튼을 표시해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N08-07. 공통 서식 편집 화면

```text
goal: [N08-07] FORM3 외 부속서식 공통 편집 화면 골격을 구현해줘. form_code별 JSON 필드 렌더링, 자동/수동 필드 표시, 저장, 검증 결과 표시, PDF 미리보기 진입점을 포함해줘. 검증: 대표 부속서식 2개 라우트 테스트를 추가해줘.
```

### N08-08. 서식 자동 생성 API

```text
goal: [N08-08] POST /api/workspace/{byId}/forms/generate를 구현해줘. 세무조정 데이터에서 주요 부속서식을 생성하고 기존 수동 수정 필드는 보호해줘. 검증: 전체 생성, 단일 서식 생성, 수동 필드 보호 테스트를 추가해줘.
```

### N08-09. 서식 간 연동 규칙 모델/API

```text
goal: [N08-09] form_relationships 기반 서식 간 연동 규칙 모델과 조회 API를 구현해줘. BJ_03과 BJ_15/BJ_22/BJ_08 등 주요 규칙을 저장하고 검증 엔진에서 사용할 수 있게 해줘. 검증: 규칙 로딩과 대상 필드 매핑 테스트를 추가해줘.
```

### N08-10. 서식 간 연동 검증

```text
goal: [N08-10] 세부구현_신고_v1.0.md 6.2 서식 간 연동 검증을 구현해줘. BJ_03.②=SUM(BJ_15.A), BJ_03.③=SUM(BJ_15.B), BJ_03.⑤=BJ_22, BJ_03.⑦=BJ_08 규칙을 ERROR/WARN으로 반환해줘. 검증: 일치/불일치 테스트를 추가해줘.
```

### N08-11. 단계 3 완료 상태

```text
goal: [N08-11] 서식 작성 단계 완료 상태를 계산해줘. 필수 서식 생성, FORM3 저장, 서식 간 ERROR 0건, FILED 잠금 여부를 반영해 context 진행률을 갱신해줘. 검증: 완료/미완료/오류 있음 테스트를 추가해줘.
```

---

## N09. 단계 4 검증과 단계 5 결재

### N09-01. validation_runs 모델/API

```text
goal: [N09-01] validation_runs와 validation_results 모델 및 POST /api/workspace/{byId}/validate를 구현해줘. scope, 실행자, 실행시각, ERROR/WARN/INFO 요약, 상세 결과, jumpUrl을 저장하고 반환해줘. 검증: 실행 이력 저장과 최신 결과 조회 테스트를 추가해줘.
```

### N09-02. 핵심 검증 규칙 1차

```text
goal: [N09-02] 핵심 검증 규칙 1차 세트를 구현해줘. 서식 간 합계 불일치, 유보 잔액 오류, 필수 항목 누락, 기부금/접대비/감가상각 주요 오류를 ERROR/WARN/INFO로 분류해줘. 검증: 규칙별 재현 fixture와 테스트를 추가해줘.
```

### N09-03. 최신 검증 결과 API

```text
goal: [N09-03] GET /api/workspace/{byId}/validate/latest를 구현해줘. 최신 validation_run과 요약, 결과 목록, 결재 요청 가능 여부를 반환해줘. 검증: 검증 전/검증 후/오류 있음/오류 없음 테스트를 추가해줘.
```

### N09-04. 검증 화면

```text
goal: [N09-04] 단계 4 검증 화면을 구현해줘. 일괄 검증 실행, 최종 실행 시각, ERROR/WARN/INFO 요약, 필터, 위치 이동 버튼, 결재 가능/불가 표시를 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N09-05. 검증 통과 상태 전이

```text
goal: [N09-05] ERROR 0건일 때만 결재 요청 가능하도록 상태/권한 검증을 연결해줘. ERROR가 있으면 VALIDATION_REQUIRED 에러로 차단하고 WARN/INFO만 있으면 진행 가능하게 해줘. 검증: ERROR 있음/없음 결재 요청 차단 테스트를 추가해줘.
```

### N09-06. approval_lines 모델/API

```text
goal: [N09-06] approval_lines 모델과 결재 요청 API를 구현해줘. 검토자, 순번, 상태, 요청 메모, 첨부 메타데이터, 요청자, 요청 시각을 저장하고 business_years.status를 IN_REVIEW로 전환해줘. 검증: 정상 요청, 검증 오류 차단, 권한 차단 테스트를 추가해줘.
```

### N09-07. 작성자 결재 요청 화면

```text
goal: [N09-07] 단계 5 작성자 결재 요청 화면을 구현해줘. 검증 결과 요약, 검토자 선택, 추가 검토자, 메모, 첨부파일 메타 표시, 결재 요청 버튼, 요청 후 잠금 안내를 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N09-08. 승인 API

```text
goal: [N09-08] POST /api/workspace/{byId}/approval/{lineId}/approve를 구현해줘. 검토자 권한 확인, 결재선 상태 갱신, 모든 필수 승인 완료 시 business_years.status를 APPROVED로 전환해줘. 검증: 권한 없음, 순번 오류, 최종 승인 상태 전이 테스트를 추가해줘.
```

### N09-09. 반려 API

```text
goal: [N09-09] POST /api/workspace/{byId}/approval/{lineId}/reject를 구현해줘. 반려 사유를 저장하고 business_years.status를 DRAFT로 돌리며 편집 잠금을 해제해줘. 검증: 반려 사유 필수, 권한 없음, 상태 전이 테스트를 추가해줘.
```

### N09-10. 검토자 결재함 화면

```text
goal: [N09-10] 검토자 결재함과 승인/반려 화면을 구현해줘. 내 결재 대기 목록, 사업연도 요약, 검증 요약, 승인/반려 버튼, 반려 사유 입력을 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N09-11. 결재 알림

```text
goal: [N09-11] 결재 요청, 승인, 반려 시 알림 생성 또는 기존 알림 시스템 연결을 구현해줘. 작성자와 검토자에게 필요한 이벤트만 생성하고 중복 알림을 방지해줘. 검증: 이벤트별 알림 생성 테스트를 추가해줘.
```

### N09-12. 결재 감사 로그

```text
goal: [N09-12] 결재 요청/승인/반려 감사 로그를 구현해줘. 사업연도, 결재선, 이전 상태, 이후 상태, 수행자, 메모 또는 반려 사유를 기록해줘. 검증: 각 결재 이벤트의 감사 로그 테스트를 추가해줘.
```

---

## N10. 단계 6 출력과 단계 7 전자신고

### N10-01. PDF 출력 대상 API

```text
goal: [N10-01] POST /api/workspace/{byId}/print를 위한 출력 대상 조회/요청 모델을 구현해줘. 전체 서식, 주요 서식, 사용자 정의 대상, 워터마크 옵션을 검증하고 APPROVED/FILED 권한 규칙을 반영해줘. 검증: 대상 선택과 권한 테스트를 추가해줘.
```

### N10-02. PDF 생성 API

```text
goal: [N10-02] PDF 생성 API를 구현해줘. by_law_snapshot의 서식 버전, form_data, 워터마크, 페이지 정보, 저장 경로 또는 다운로드 링크를 생성하고 출력 이력을 남겨줘. 실제 렌더러가 없으면 명확한 어댑터와 테스트 더블을 둬줘. 검증: 생성 요청, 이력 저장, 실패 처리 테스트를 추가해줘.
```

### N10-03. 워터마크 매핑

```text
goal: [N10-03] 사업연도 상태별 PDF 워터마크 매핑을 구현해줘. DRAFT=작성중, IN_REVIEW=검토중, APPROVED=승인완료, FILED=신고완료+접수번호, AMENDED=수정신고를 적용해줘. 검증: 상태별 워터마크 테스트를 추가해줘.
```

### N10-04. 출력 화면

```text
goal: [N10-04] 단계 6 출력 화면을 구현해줘. 출력 대상 선택, 워터마크 옵션, PDF 생성, ZIP 다운로드 진입점, 출력 이력, 상태별 가능/불가 안내를 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N10-05. efiling_history 모델

```text
goal: [N10-05] efiling_history 모델을 세부구현_신고_v1.0.md 10.3에 맞게 구현해줘. file_path, file_hash, file_size_bytes, schema_version, nts_receipt_no, submission_method, status, submitted_at, accepted_at, amendment 연결을 저장하게 해줘. 검증: 모델/마이그레이션 테스트를 추가해줘.
```

### N10-06. 전자신고 사전 검증 API

```text
goal: [N10-06] POST /api/workspace/{byId}/efile/validate를 구현해줘. 사업자번호 체크섬, FORM3 총 납부세액, 필수 항목, 고정폭 레코드 길이, 스키마 기준 오류를 반환해줘. 검증: 통과/실패 케이스 테스트를 추가해줘.
```

### N10-07. 전자신고 규격 매핑표

```text
goal: [N10-07] 홈택스 법인세 전자신고 고정길이 파일 생성을 위한 규격 매핑표를 구현해줘. form_data 필드와 레코드 타입, 위치, 길이, 인코딩, padding 규칙을 분리된 설정으로 관리하게 해줘. 검증: 대표 필드 매핑과 길이 테스트를 추가해줘.
```

### N10-08. 고정길이 파일 생성기

```text
goal: [N10-08] 전자신고 고정길이 파일 생성기를 구현해줘. EUC-KR 또는 필요한 인코딩, 위치/길이 padding, 레코드 순서, 파일명, SHA-256, 파일 크기를 생성하게 해줘. 검증: 샘플 데이터의 레코드 길이와 해시 생성 테스트를 추가해줘.
```

### N10-09. 전자신고 파일 생성 API

```text
goal: [N10-09] POST /api/workspace/{byId}/efile/generate를 구현해줘. 사전 검증 통과 후 신고파일을 생성하고 efiling_history.status=GENERATED로 저장하며 다운로드 정보를 반환해줘. 검증: 성공, 검증 오류 차단, FILED 중복 생성 차단 테스트를 추가해줘.
```

### N10-10. 전자신고 4단계 UI

```text
goal: [N10-10] 단계 7 전자신고 4단계 UI를 구현해줘. 사전검증, 파일생성, 제출, 접수확인 상태를 단계별로 표시하고 생성 파일명, 크기, 해시, 제출 방식, 접수 결과 영역을 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N10-11. 2FA Step-up 제출 API

```text
goal: [N10-11] POST /api/workspace/{byId}/efile/submit을 2FA Step-up 토큰 필수로 구현해줘. 토큰 검증, 제출 방식 API/MANUAL, 제출 권한, APPROVED 상태 요구, 재제출 차단을 포함해줘. 검증: 토큰 없음, 토큰 오류, 권한 없음, 정상 제출 테스트를 추가해줘.
```

### N10-12. 제출 후 FILED 전환

```text
goal: [N10-12] 전자신고 제출 성공 후 접수번호를 저장하고 business_years.status를 FILED로 전환해줘. by_law_snapshot, form_data, tax_adjustments를 잠그고 감사 로그와 알림을 남겨줘. 검증: FILED 전환, 접수번호 저장, 잠금 적용 테스트를 추가해줘.
```

---

## N11. FILED 잠금과 수정신고

### N11-01. FILED 백엔드 불변성 회귀

```text
goal: [N11-01] FILED 상태에서 모든 신고작업 수정 API가 차단되는지 회귀 테스트를 추가해줘. 재무제표, 매핑, 자산, 거래, 조정, reserves, form_data, validation, approval, output, efiling 대표 API를 포함해줘. 검증: 모든 저장성 API가 LOCKED_FILED로 실패해야 해줘.
```

### N11-02. FILED 프론트 읽기 전용 회귀

```text
goal: [N11-02] FILED 상태에서 신고작업 프론트 전체가 읽기 전용으로 표시되는지 회귀 테스트를 추가해줘. 입력 필드, 저장/재계산/결재/전자신고 버튼, 안내 배너, 출력 가능 여부를 단계별 대표 화면에서 검증해줘.
```

### N11-03. 수정신고 생성 API

```text
goal: [N11-03] AMENDED 수정신고 생성 API를 구현해줘. 원신고 FILED 사업연도만 원본으로 허용하고 original_business_year 참조, 수정 사유, 초기 데이터 복사, 별도 스냅샷 생성 또는 원본 스냅샷 참조 정책을 명확히 해줘. 검증: 정상 생성과 잘못된 원본 차단 테스트를 추가해줘.
```

### N11-04. 수정신고 작업 진입 화면

```text
goal: [N11-04] 수정신고 생성과 작업 진입 화면을 구현해줘. 원신고 선택, 수정 사유 입력, 생성 후 AMENDED 사업연도 이동, 원신고 읽기 전용 유지, 권한별 버튼 표시를 포함해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N11-05. 수정신고 데이터 복사

```text
goal: [N11-05] 수정신고 생성 시 원신고의 세무정보, 세무조정, 서식, 주요 첨부 메타데이터를 새 사업연도 작업으로 복사해줘. efiling_history는 parent 연결만 남기고 새 제출 이력은 별도로 관리해줘. 검증: 복사 대상/제외 대상 테스트를 추가해줘.
```

### N11-06. 수정신고 변경분 비교 API

```text
goal: [N11-06] AMENDED 수정신고에서 원신고 대비 변경분 비교 API를 구현해줘. 변경 필드, 조정 항목 변경, 세액 차이, 서식 차이, 전자신고 변경분 요약을 반환해줘. 검증: 금액 변경, 행 추가, 행 삭제 테스트를 추가해줘.
```

### N11-07. 수정신고 변경분 화면

```text
goal: [N11-07] 수정신고 변경분 비교 화면을 구현해줘. 원신고/수정신고 값 비교, 변경 사유, 세액 차이, 서식 차이, 전자신고 반영 여부를 표시해줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

---

## N12. 안정화, 성능, 인수

### N12-01. Excel 템플릿 다운로드

```text
goal: [N12-01] 신고작업 Excel 템플릿 다운로드를 구현해줘. 재무제표, 계정매핑, 자산, 거래, 조정항목 템플릿을 현재 필드 정의와 맞추고 다운로드 응답을 테스트해줘. 검증: 파일명, 시트명, 헤더, 필수 컬럼 테스트를 추가해줘.
```

### N12-02. Excel 업로드 검증 공통화

```text
goal: [N12-02] 신고작업 Excel 업로드 검증을 공통화해줘. 필수 컬럼 누락, 타입 오류, 금액 형식 오류, 알 수 없는 계정, 대용량 행 제한, FILED 잠금 차단을 공통 결과 형식으로 반환해줘. 검증: 각 오류 유형 테스트를 추가해줘.
```

### N12-03. 대용량 fixture

```text
goal: [N12-03] 대용량 신고작업 성능 fixture를 만들어줘. 많은 계정, 자산, 거래, 조정항목, 서식 필드를 가진 사업연도를 생성하고 주요 API/화면 측정에 재사용하게 해줘. 검증: fixture 생성 시간과 데이터 건수를 보고해줘.
```

### N12-04. 주요 API 성능 측정

```text
goal: [N12-04] 대용량 fixture로 주요 신고작업 API 성능을 측정해줘. workspace context, financial-statements, adjustments, forms/list, validate/latest 응답 시간을 기록하고 병목 쿼리를 정리해줘. 검증: 재현 가능한 측정 명령과 결과 표를 남겨줘.
```

### N12-05. 인덱스와 페이지네이션

```text
goal: [N12-05] 성능 측정에서 확인된 병목에 인덱스, 페이지네이션, 필요한 조회 범위 제한을 적용해줘. 변경 전후 응답 시간을 비교하고 기존 테스트가 통과하는지 확인해줘. 검증: migration, 쿼리 테스트, 성능 비교 결과를 남겨줘.
```

### N12-06. 프론트 lazy rendering

```text
goal: [N12-06] 대용량 테이블 화면의 프론트 렌더링을 안정화해줘. 재무제표, 거래, 조정 그리드, 부속서식 목록에서 pagination 또는 lazy rendering을 적용하고 레이아웃 밀림을 줄여줘. 검증: 프론트 테스트와 브라우저 확인을 완료해줘.
```

### N12-07. 캐싱과 무효화

```text
goal: [N12-07] 신고작업 주요 조회 API와 프론트 상태의 캐싱/무효화 전략을 정리하고 구현해줘. 저장, 검증, 승인, 출력, 제출 후 context, adjustment, form, validation, approval, efile 데이터가 최신으로 갱신되게 해줘. 검증: 이벤트별 stale data 방지 테스트를 추가해줘.
```

### N12-08. 대표 성공 E2E

```text
goal: [N12-08] 대표 성공 E2E 시나리오를 구현해줘. 사업연도 생성, 세무정보 입력, 세무조정 핵심 모듈, FORM3 생성, 검증 통과, 결재 승인, PDF 출력, 전자신고 제출, FILED 잠금까지 자동 테스트와 브라우저 확인으로 검증해줘.
```

### N12-09. 오류/반려 E2E

```text
goal: [N12-09] 오류와 반려 E2E 시나리오를 구현해줘. 서식 간 검증 ERROR로 결재 차단, 오류 수정 후 결재 요청, 검토자 반려, DRAFT 복귀, 재수정 후 재요청 흐름을 검증해줘. 검증: 자동 테스트와 브라우저 확인을 완료해줘.
```

### N12-10. 역할/테넌트 전체 회귀

```text
goal: [N12-10] 신고작업 전체 역할/테넌트 회귀 테스트를 확장해줘. SUPER_ADMIN, TENANT_ADMIN, TAX_EXPERT, TAX_REVIEWER, ASSISTANT, VIEWER별 허용/차단 행위와 다른 테넌트 접근 차단을 주요 API와 화면에서 검증해줘.
```

### N12-11. 문서 차이 보정

```text
goal: [N12-11] 세부구현_신고_v1.0.md와 실제 구현 차이를 최종 비교해줘. 의도적으로 다르게 구현한 부분, 미구현 항목, 운영상 주의사항, 테스트 명령을 README 또는 관련 문서에 최소 범위로 보완해줘.
```

### N12-12. 최종 인수 점검

```text
goal: [N12-12] 신고작업 전체 최종 인수 점검을 수행해줘. 스냅샷 계산, 유보 자동 연계, 서식 간 연동, 상태별 메뉴 활성화, 2FA Step-up, FILED 불변성, 수정신고, 테넌트 격리, 역할 분리를 재검증하고 남은 리스크를 정리해줘.
```
