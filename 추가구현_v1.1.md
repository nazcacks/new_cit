# 추가 구현 계획 v1.1
## 설계서 기준 메뉴 재구성 + 메뉴별 기능의 개별 화면 분리

> 작성일: 2026-05-18 · 작성자: 강수 (Kang-Soo.Cho@kr.ey.com)
> 기준 문서: `법인세_세무조정계산서_시스템_설계서.md` §5.1 메뉴 구조, §5.2 화면 흐름, §5.3 와이어프레임 A~G
> 선행 문서: `2차구현계획.md`(메뉴 IA 채택) · `메뉴기능수정.md`(시드·점검) · `추가구현_v1.0.md`(기능 갭)
> 변경 요지: **현재 24개 메뉴(한 화면 = 여러 기능)** 를 **설계서가 정의한 leaf 메뉴 단위 개별 화면(약 80개)** 으로 분리하고, 메뉴/라우팅/렌더 구조를 그에 맞게 재정비한다.

---

## 1. 문제 정의

### 1.1 현재 구조의 한계

| 구분 | 현재 | 설계서 |
|---|---|---|
| ② 신고 작업 1단계(세무정보 입력) | 1개 화면(`ws-info`)에 좌측 6-탭으로 6개 기능 묶음 | 1-1 ~ 1-5, 1-9 각 **별도 메뉴 / 별도 URL / 별도 화면** |
| ② 신고 작업 2단계(세무조정) | 1개 화면(`ws-adj`)에 17개 카드 + “실행” 버튼 | 2-1 ~ 2-17 각 **별도 메뉴 / 별도 URL / 본문은 와이어프레임 C 레이아웃** |
| ② 신고 작업 3단계(서식) | 1개 화면(`ws-form`)에 별지3호 + 부속서식 일람 통합 | 3-1 별지3호 / 3-2 부속서식 일람 / 3-3 미리보기 / 3-4 연동 점검 분리 |
| ② 신고 작업 4단계(검증) | 1개 화면(`ws-val`) | 4-1 일괄검증 실행 / 4-2 이슈 목록 / 4-3 룰 적용 결과 |
| ② 신고 작업 5단계(결재) | 1개 화면(`ws-appr`) | 5-1 결재 요청 / 5-2 결재함 / 5-3 반려 이력 |
| ② 신고 작업 6단계(출력) | 1개 화면(`ws-print`) | 6-1 미리보기 / 6-2 단일/일괄 / 6-3 출력 이력 |
| ② 신고 작업 7단계(전자신고) | 1개 화면(`ws-file`) 4-step Wizard | 7-1 사전검증 / 7-2 파일 생성 / 7-3 제출 / 7-4 완료 — 4개 화면 |
| ③ 사후 관리 | `post-hist`, `post-amend` 2개 | 신고 이력 / 수정신고(4 화면) / 경정청구 |
| ⑤ 관리 5-D 법령 | 1개 화면(`ad-law`) | F-1 ~ F-5 + 10개 항목(법령/세율/한도/공제/내용연수/SME/결손금/스냅샷/시뮬레이션/이력) — 10개 화면 |
| ⑤ 관리 5-G 서식 | 1개 화면(`ad-form`) | G-1 ~ G-6 + 10개 항목 — 10개 화면 |
| ⑤ 관리 5-B 사용자/권한 | 3개 화면(`ad-user-list`, `ad-role`, `ad-menu-fn`) | E-1 사용자 / E-2 역할권한 / E-3 메뉴관리 / 기능코드 / 필드마스킹 / 데이터권한 — 6개 화면 |
| ⑤ 관리 5-C 담당 법인 권한 | 1개 화면(`ad-cacc`) | E-4 4탭(개별·그룹·자동룰·위임) + E-5 위임 — 5개 화면 |

요지: **메뉴가 적고 한 화면이 비대화**되어 권한 매트릭스(메뉴 × 12 기능 × 데이터 권한)와도 어긋난다.

### 1.2 목표

1. 설계서 §5.1 메뉴 트리를 **있는 그대로** 좌측 메뉴로 펼친다(접고/펼치기 포함).
2. **leaf 메뉴 = 단일 화면 = 단일 책임**. 한 화면은 한 기능만 다룬다.
3. **공통 레이아웃** 3종(스테퍼 + LawBanner + 컨텍스트 / 일반 화면 / Admin 화면)을 분리한다.
4. 라우팅은 hash-URL 한 단계 더 깊어진다: `#/workspace/ws-adj/B12/credits` 같은 형태도 허용.
5. 권한 매트릭스의 “메뉴 × 기능”과 일대일로 매핑 가능해진다.

---

## 2. 설계서 기준 전체 메뉴 트리 (leaf 단위)

> 아래 키는 신규 메뉴 키 체계. 콜론(`:`) 뒤가 leaf 식별자, 그 이전이 그룹 경로.

### 2.1 ① 대시보드 — `dashboard`

| 키 | 라벨 | 설계서 |
|---|---|---|
| `dashboard:overview` | 내 작업 현황 | §5.3 B (KPI) |
| `dashboard:duesoon` | 신고 마감 임박 | §5.3 B |
| `dashboard:inbox` | 알림/결재 대기함 | §5.3 B |
| `dashboard:recent` | 최근 활동 | §5.3 B |
| `dashboard:kpi-tax` | 당기 세부담 추이 | §5.3 B (Chart.js) |
| `dashboard:kpi-industry` | 업종별 분포 | §5.3 B (도넛) |

> 대시보드는 단일 페이지에 카드로 합쳐 표시하되 각 카드의 “자세히” 버튼이 leaf 화면으로 점프할 수 있게 한다. leaf는 독립 화면으로도 라우팅 가능.

### 2.2 ② 신고 작업 — `workspace`

#### 0. 작업 시작 — `ws/start`
| 키 | 라벨 |
|---|---|
| `ws/start:customer-pick` | 고객사 선택 |
| `ws/start:by-pick` | 사업연도 선택 |
| `ws/start:by-new` | 신규 사업연도 생성 |
| `ws/start:snapshot` | 적용 법령·서식 스냅샷 확인 |

#### 1. 세무정보 입력 — `ws/info`
| 키 | 라벨 |
|---|---|
| `ws/info:fs` | 1-1. 재무제표 입력/임포트 |
| `ws/info:fs-bs` | 1-1-1. BS |
| `ws/info:fs-is` | 1-1-2. IS |
| `ws/info:fs-cf` | 1-1-3. CF |
| `ws/info:mapping` | 1-2. 계정과목 매핑 |
| `ws/info:assets` | 1-3. 자산 및 감가상각 (자산대장) |
| `ws/info:transactions` | 1-4. 거래 명세 |
| `ws/info:vehicle` | 1-5. 업무용승용차 운행기록 |
| `ws/info:consistency` | 1-9. 입력 데이터 일관성 검증 |

#### 2. 세무조정 17개 — `ws/adj`
| 키 | 라벨 | 부속서식 |
|---|---|---|
| `ws/adj:B1` | 2-1. 소득금액조정명세서 | 별지 제15호 |
| `ws/adj:B2` | 2-2. 기부금 조정 | 별지 제22호 |
| `ws/adj:B3` | 2-3. 접대비 조정 | 별지 제23호 |
| `ws/adj:B4` | 2-4. 감가상각비 조정 | 별지 제20호 |
| `ws/adj:B5` | 2-5. 퇴직급여/퇴직연금 | 별지 제32/33호 |
| `ws/adj:B6` | 2-6. 대손충당금/대손금 | 별지 제34호 |
| `ws/adj:B7` | 2-7. 외화자산·부채 평가 | 별지 제40호 |
| `ws/adj:B8` | 2-8. 재고·유가증권 평가 | 별지 제41호 |
| `ws/adj:B9` | 2-9. 지급이자 손금불산입 | 별지 제29호 |
| `ws/adj:B10` | 2-10. 업무용승용차 | 별지 제29호의2 |
| `ws/adj:B11` | 2-11. 이월결손금 | 별지 제8호 |
| `ws/adj:B12` | 2-12. 세액공제·감면 | 별지 제8호 부표 |
| `ws/adj:B13` | 2-13. 최저한세 | 별지 제4호 |
| `ws/adj:B14` | 2-14. 가산세 | 별지 제5호 |
| `ws/adj:B15` | 2-15. 자본금과 적립금 | 별지 제50호 갑/을 |
| `ws/adj:B16` | 2-16. 외국법인 | — |
| `ws/adj:B17` | 2-17. 연결납세 | — |

#### 3. 서식 작성 — `ws/form`
| 키 | 라벨 |
|---|---|
| `ws/form:form3` | 3-1. 별지 제3호 |
| `ws/form:attachments` | 3-2. 부속서식 일람 |
| `ws/form:preview` | 3-3. 서식 미리보기 |
| `ws/form:linkage` | 3-4. 서식 간 연동 점검 |

#### 4. 검증 — `ws/val`
| 키 | 라벨 |
|---|---|
| `ws/val:run` | 4-1. 일괄 검증 실행 |
| `ws/val:issues` | 4-2. 오류·경고 목록 |
| `ws/val:rules` | 4-3. 검증룰 적용 결과 |

#### 5. 결재 — `ws/appr`
| 키 | 라벨 |
|---|---|
| `ws/appr:request` | 5-1. 결재 요청 |
| `ws/appr:inbox` | 5-2. 결재함 |
| `ws/appr:rejected` | 5-3. 반려 이력 / 재작성 |

#### 6. 출력 — `ws/print`
| 키 | 라벨 |
|---|---|
| `ws/print:preview` | 6-1. PDF 미리보기 |
| `ws/print:bulk` | 6-2. 단일/일괄 출력 |
| `ws/print:history` | 6-3. 출력 이력 |

#### 7. 전자신고 — `ws/file`
| 키 | 라벨 |
|---|---|
| `ws/file:precheck` | 7-1. 전자신고 사전 검증 |
| `ws/file:generate` | 7-2. 전자신고 파일 생성 |
| `ws/file:submit` | 7-3. 제출 / 접수 확인 |
| `ws/file:done` | 7-4. 신고 완료 |

### 2.3 ③ 사후 관리 — `post`

| 키 | 라벨 |
|---|---|
| `post/hist:list` | 신고 이력 목록 |
| `post/hist:detail` | 신고 이력 상세 (접수번호/양시) |
| `post/amend:unlock` | 수정신고 — 잠금 해제 신청 |
| `post/amend:version` | 수정신고 — 사용 버전 선택 |
| `post/amend:diff` | 수정신고 — 차이 보고서 |
| `post/amend:resubmit` | 수정신고 — 재제출 |
| `post/correction` | 경정청구 |

### 2.4 ④ 분석/보고서 — `report`

| 키 | 라벨 |
|---|---|
| `report:year-compare` | 사업연도별 비교 (3개년/5개년) |
| `report:tax-burden` | 세부담 분석 (실효세율/한계세율) |
| `report:reserve-trend` | 유보 잔액 추이 |
| `report:loss-expiry` | 이월결손금 만료 예측 |
| `report:industry-stats` | 업종별/규모별 통계 |
| `report:custom` | 사용자 정의 리포트 |

### 2.5 ⑤ 관리 — `admin`

#### 5-A 고객사 관리 — `admin/cust`
| 키 | 라벨 |
|---|---|
| `admin/cust:list` | 고객사 등록/조회 |
| `admin/cust:by-master` | 사업연도 마스터 |
| `admin/cust:agent` | 세무대리 계약 |

#### 5-B 사용자/권한 관리 — `admin/sec`
| 키 | 라벨 |
|---|---|
| `admin/sec:users` | 사용자 등록/조회 (E-1) |
| `admin/sec:roles` | 역할 마스터 |
| `admin/sec:matrix` | 권한 매트릭스 (메뉴 × 기능) (E-2) |
| `admin/sec:menus` | 메뉴 관리 (트리/활성 기능) (E-3) |
| `admin/sec:functions` | 기능(Function) 코드 관리 |
| `admin/sec:mask` | 필드 마스킹 정책 |
| `admin/sec:scope` | 데이터 권한 (Data Scope) |

#### 5-C 담당 법인 권한 — `admin/cacc`
| 키 | 라벨 |
|---|---|
| `admin/cacc:assign` | 사용자별 담당 법인 배정 (E-4 ①) |
| `admin/cacc:groups` | 고객사 그룹 관리 (E-4 ②) |
| `admin/cacc:rules` | 자동 할당 룰 (업종/지역/유형) (E-4 ③) |
| `admin/cacc:delegate` | 권한 위임/대행 (E-5) |
| `admin/cacc:override` | 개별 권한 예외 설정 |

#### 5-D 법령·세율 버전 관리 — `admin/law`
| 키 | 라벨 |
|---|---|
| `admin/law:master` | 법령 버전 마스터 (F-1) |
| `admin/law:rates` | 법인세율표 (F-2) |
| `admin/law:limits` | 한도·율표 (F-3) |
| `admin/law:credits` | 세액공제·감면 율표 |
| `admin/law:depr-lives` | 기준내용연수표 |
| `admin/law:sme` | 중소기업 판정기준 |
| `admin/law:loss-rule` | 결손금 공제규정 |
| `admin/law:snapshots` | 사업연도별 적용 스냅샷 (F-5) |
| `admin/law:impact` | 영향 시뮬레이션 (F-4) |
| `admin/law:history` | 개정 공지/이력 |

#### 5-E 서식 버전 관리 — `admin/form`
| 키 | 라벨 |
|---|---|
| `admin/form:master` | 서식 마스터 (별지 코드) |
| `admin/form:versions` | 서식 버전 (메타 + PDF 템플릿) (G-1) |
| `admin/form:fields` | 서식 항목 편집기 (G-2) |
| `admin/form:validations` | 검증 룰 (버전별) |
| `admin/form:linkage-rule` | 서식 간 연동 규칙 (G-5) |
| `admin/form:field-ref` | 항목 매핑/필드 참조 (G-6) |
| `admin/form:migration` | 데이터 마이그레이션 (드라이런/실행/롤백) (G-3) |
| `admin/form:efile-map` | 홈택스 전자신고 레코드 매핑 |
| `admin/form:by-set` | 사업연도별 적용 서식 세트 (G-4) |
| `admin/form:impact` | 서식 영향 시뮬레이션 |

#### 5-F 코드 관리 — `admin/code`
| 키 | 라벨 |
|---|---|
| `admin/code:industry` | 업종 코드 |
| `admin/code:account` | 표준 계정과목 |
| `admin/code:misc` | 기타 코드 그룹 |

#### 5-G 감사/로그 — `admin/audit`
| 키 | 라벨 |
|---|---|
| `admin/audit:events` | 감사 로그 (C/U/D 이벤트) |
| `admin/audit:login` | 로그인 이력 |
| `admin/audit:perm` | 권한 변경 이력 |
| `admin/audit:law` | 법령/서식 개정 이력 |
| `admin/audit:settings` | 시스템 설정 |

**총 leaf 화면 수 합계**: 6 + (4+9+17+4+3+3+3+4) + 7 + 6 + (3+7+5+10+10+3+5) = **약 99개**(설계서 §5.1 + 와이어프레임 분기 포함).

---

## 3. 라우팅·렌더 구조 변경

### 3.1 라우팅 형식
- 현재: `#/dashboard`, `#/ws-info`, `#/ws-adj`
- 신규: `#/<group>/<menuKey>[?params]`
  - `#/dashboard/overview`
  - `#/workspace/ws/info/fs-bs?byId=12`
  - `#/workspace/ws/adj/B12?byId=12`
  - `#/admin/law/master`
  - `#/admin/form/migration?formCode=BJ_15&from=2025-01&to=2025-02`

### 3.2 라우터 책임 분리
```
frontend/app/
├── router.js          // hash → {group, menuKey, params} 파싱
├── menu.js            // 메뉴 트리 렌더 + 접기/펼치기 + 활성화
├── context.js         // workContext (변경 없음)
├── api.js             // 변경 없음
├── layouts/
│   ├── workspace.js   // Stepper + LawBanner + (좌측 서브내비) + 본문
│   ├── plain.js       // 일반(대시보드/사후/보고서)
│   └── admin.js       // Admin (좌측 카테고리 + 본문)
└── screens/
    ├── dashboard/
    │   ├── overview.js
    │   ├── duesoon.js
    │   ├── inbox.js
    │   ├── recent.js
    │   ├── kpi-tax.js
    │   └── kpi-industry.js
    ├── workspace/
    │   ├── start/{customer-pick,by-pick,by-new,snapshot}.js
    │   ├── info/{fs,fs-bs,fs-is,fs-cf,mapping,assets,transactions,vehicle,consistency}.js
    │   ├── adj/{B1..B17}.js
    │   ├── form/{form3,attachments,preview,linkage}.js
    │   ├── val/{run,issues,rules}.js
    │   ├── appr/{request,inbox,rejected}.js
    │   ├── print/{preview,bulk,history}.js
    │   └── file/{precheck,generate,submit,done}.js
    ├── post/{hist-list,hist-detail,amend-unlock,amend-version,amend-diff,amend-resubmit,correction}.js
    ├── report/{year-compare,tax-burden,reserve-trend,loss-expiry,industry-stats,custom}.js
    └── admin/
        ├── cust/{list,by-master,agent}.js
        ├── sec/{users,roles,matrix,menus,functions,mask,scope}.js
        ├── cacc/{assign,groups,rules,delegate,override}.js
        ├── law/{master,rates,limits,credits,depr-lives,sme,loss-rule,snapshots,impact,history}.js
        ├── form/{master,versions,fields,validations,linkage-rule,field-ref,migration,efile-map,by-set,impact}.js
        ├── code/{industry,account,misc}.js
        └── audit/{events,login,perm,law,settings}.js
```

각 화면 파일은 다음 시그니처를 따른다:
```js
export const meta = {
  key: "ws/adj:B12",
  group: "workspace",
  layout: "workspace",     // workspace | plain | admin
  title: "2-12. 세액공제·감면",
  parentKey: "ws/adj",
  requires: ["work-context"],
  perm: { module: "adjustment", function: "READ" },
};
export async function render(env) { /* ... */ }
```

라우터는 `meta.layout`에 맞는 레이아웃을 그리고, `render(env)`로 본문만 채운다.

### 3.3 백엔드 메뉴 트리
`src/modules.rs::prototype_menu_tree()`를 **leaf 단위**로 확장한다. 각 leaf는 다음 필드를 가진다:
```jsonc
{
  "key": "ws/adj:B12",
  "label": "2-12. 세액공제·감면",
  "path": "#/workspace/ws/adj/B12",
  "parent_key": "ws/adj",
  "layout": "workspace",
  "requires_context": ["customer_id", "business_year_id"],
  "required_perm": { "module": "adjustment", "function": "READ" },
  "feature_flag": null,
  "enabled": true,
  "sort_order": 12
}
```

`migrations/2026052X000200_menu_full_tree.sql`에서 `menu_nodes`를 99행 시드한다.

---

## 4. 레이아웃 정의

### 4.1 Workspace 레이아웃 (스테퍼 + LawBanner + 좌측 서브내비)

```
┌────────────────────────────────────────────────────────────────┐
│ ② 신고작업 > 2.세무조정 > 2-12 세액공제·감면   고객사:㈜OOO    │
├────────────────────────────────────────────────────────────────┤
│ ①정보입력 ─ ②조정 ▶─ ③서식 ─ ④검증 ─ ⑤결재 ─ ⑥출력 ─ ⑦신고   │
├────────────────────────────────────────────────────────────────┤
│ ⓘ 적용 법령: 2025 사업연도 (CIT-2025-01)                       │
├──────────────────┬─────────────────────────────────────────────┤
│ [좌측 서브내비]   │ [본문 — leaf 화면]                          │
│ ▶ 2-1 ~ 2-11    │                                              │
│ ▼ 2-12 세액공제 │                                              │
│ ▶ 2-13 ~ 2-17   │                                              │
└──────────────────┴─────────────────────────────────────────────┘
```
- 스테퍼·LawBanner는 라우터가 그린다.
- 좌측 서브내비는 현재 그룹(`ws/adj`)의 형제 leaf 목록을 표시.
- 본문은 leaf의 `render(env)`가 단독 책임.

### 4.2 Admin 레이아웃 (좌측 카테고리 + 본문)

```
┌────────────────────────────────────────────────────────────────┐
│ ⑤ 관리 > 5-D 법령·세율 버전 > 영향 시뮬레이션                  │
├────────┬───────────────────────────────────────────────────────┤
│ 5-A ▶ │                                                        │
│ 5-B ▶ │ [본문 — leaf 화면]                                     │
│ 5-C ▶ │                                                        │
│ 5-D ▼ │                                                        │
│  · 마스터                                                       │
│  · 세율                                                         │
│  · 한도                                                         │
│  · …                                                            │
│  · 영향 시뮬레이션 ◀ active                                    │
│  · 이력                                                         │
│ 5-E ▶ │                                                        │
└────────┴───────────────────────────────────────────────────────┘
```

### 4.3 Plain 레이아웃 (단순 패널)
- 대시보드/사후관리/분석보고서의 leaf 화면용.
- 상단 breadcrumb + 본문.

---

## 5. 단계별 작업 (Pack 정의)

### Pack A. 라우팅·레이아웃·메뉴 트리 기반 (1.5주)
1. `menu_nodes`를 99 leaf로 확장하는 마이그레이션 작성.
2. `src/modules.rs::prototype_menu_tree()` 응답 스키마에 `parent_key, layout, requires_context, required_perm` 추가.
3. 프론트 `router.js`/`menu.js`/`layouts/{workspace,plain,admin}.js` 신규.
4. `screens/_placeholder.js` — leaf가 아직 비어 있으면 “준비 중” 본문을 그리는 폴백.
5. `tests/menu_full_tree.rs` — 99 leaf가 모두 200을 반환하고 layout 매칭이 맞는지 검사.

**완료 판정**: 좌측 메뉴를 클릭하면 99개 URL이 모두 라우팅되고, 본문은 “준비 중” 또는 기존 통합 화면을 표시한다.

### Pack B. 신고 작업(②) 분할 (3주)
- B1 ~ B17 + 0~7 단계의 leaf 화면을 기존 통합 화면에서 분리해 단일 책임 화면으로 옮긴다.
- 모듈별 그리드 컴포넌트(`grid.js`)는 `추가구현_v1.0.md` Pack 3에서 신규 작성한 것을 재사용.
- 좌측 서브내비는 `ws/adj` 그룹의 17개 형제 leaf를 보여주고, prototype의 그룹 헤더(통합/자산기반/거래기반/평가/유보/세액/특수)를 카테고리로 사용.

세부 작업:
- 0. 작업 시작 4개 leaf
- 1. 세무정보 입력 9개 leaf (BS/IS/CF 포함)
- 2. 세무조정 17개 leaf
- 3. 서식 작성 4개 leaf
- 4. 검증 3개 leaf
- 5. 결재 3개 leaf
- 6. 출력 3개 leaf
- 7. 전자신고 4개 leaf

각 leaf는:
- 단일 API GET/POST 호출
- 본문은 표 + 폼 + 액션 버튼만
- 다른 leaf로의 이동은 “다음 단계 ▶”·“이전 단계 ◀” 또는 형제 서브내비

### Pack C. 관리(⑤) 분할 (2.5주)
- 5-A 3 leaf, 5-B 7 leaf, 5-C 5 leaf, 5-D 10 leaf, 5-E 10 leaf, 5-F 3 leaf, 5-G 5 leaf.
- 5-D / 5-E는 설계서 §5.3 F·G의 와이어프레임 (F-1~F-5, G-1~G-6)을 leaf별로 1:1 매핑.
- 5-B 매트릭스(`admin/sec:matrix`)는 메뉴 × 12 기능 체크박스 그리드. `추가구현_v1.0.md` Pack 4의 백엔드 모델과 짝지어 구현.

### Pack D. 대시보드·사후관리·보고서 분할 (1주)
- 대시보드는 단일 페이지에 카드 합쳐 표시하되 각 카드에 “자세히” 버튼이 leaf로 점프.
- 사후관리는 신고 이력(목록+상세), 수정신고 4 leaf, 경정청구 1 leaf로 분리.
- 보고서는 6 leaf(year-compare, tax-burden, reserve-trend, loss-expiry, industry-stats, custom) — 각각 단일 차트 + 표.

### Pack E. 권한·접근 제어 일관 적용 (1주)
- 모든 leaf가 `meta.requires`와 `meta.perm`을 갖는다.
- 라우터가 진입 전 `requires`(컨텍스트), `perm`(권한)을 검사.
- 미충족 시 자동으로 “필요 항목 안내” 화면(단일 컴포넌트)을 그린다.
- `permissions::can(user, menu_key, function_code, customer_id?)` 도메인 함수가 모든 API와 화면의 진리원.

### Pack F. 회귀·자동화 (0.5주)
- `tests/menu_full_tree.rs` 99 leaf 회귀.
- `docs/menu_smoke_checklist.md` 갱신(99 leaf 클릭 체크).
- Playwright 스모크: 핵심 30 leaf를 자동 클릭.

---

## 6. 메뉴 ↔ API ↔ 백엔드 매핑 (요약)

> 모든 leaf는 단일 GET 또는 단일 POST가 본 화면의 1차 호출. 부가 호출은 부속 패널/버튼에서만 일어난다.

| 그룹 | leaf | 1차 호출 |
|---|---|---|
| dashboard | overview/duesoon/inbox/recent/kpi-tax/kpi-industry | `/dashboard`, `/business-years?dueSoon`, `/workflow/queue?assignee=me`, `/audit-logs?limit=10`, `/reports/tax-burden?range=5y`, `/reports/industry-stats` |
| ws/start | customer-pick | `/customers` |
| ws/start | by-pick | `/business-years` |
| ws/start | by-new | `POST /business-years` |
| ws/start | snapshot | `/business-years/:by_id/snapshot` |
| ws/info | fs / fs-bs / fs-is / fs-cf | `/tax-data/financial-statements?statement_type=BS|IS|CF` |
| ws/info | mapping | `/customers/:cid/account-mappings` |
| ws/info | assets | `/tax-data/assets` |
| ws/info | transactions | `/tax-data/transactions` |
| ws/info | vehicle | `/vehicle-usage-logs` |
| ws/info | consistency | `/tax-data/validation` |
| ws/adj:Bn | n ∈ 1..17 | 모듈별 `/adjustments/(income|assets|transactions|evaluation|tax|special)/Bn` |
| ws/form | form3 | `/forms/FORM3/preview` |
| ws/form | attachments | `/forms/attachments` |
| ws/form | preview | `/forms/:code/preview` |
| ws/form | linkage | `/forms/linkage-check`(신설) |
| ws/val | run | `POST /validation/run` |
| ws/val | issues | `/validation/issues` |
| ws/val | rules | `/validation/rules` |
| ws/appr | request | `POST /workflow/request` |
| ws/appr | inbox | `/workflow/queue?assignee=me` |
| ws/appr | rejected | `/workflow/events?status=REJECTED` |
| ws/print | preview | `/forms/:code/preview` |
| ws/print | bulk | `/forms/pdf-bundle/download` |
| ws/print | history | `/print/history`(신설) |
| ws/file | precheck | `/efilings/precheck` |
| ws/file | generate | `POST /efilings` |
| ws/file | submit | `/efilings/:id/submit`(신설) |
| ws/file | done | `/efilings/:id` |
| post/hist | list | `/efilings` |
| post/hist | detail | `/efilings/:id` |
| post/amend | unlock | `POST /business-years/:id/unlock` |
| post/amend | version | `/business-years/:id/amendment-version-mode` |
| post/amend | diff | `/business-years/:id/amendment-preview` |
| post/amend | resubmit | `POST /business-years/:id/resubmit`(신설) |
| post/correction | — | `/correction-claims`(신설) |
| report | year-compare | `/reports/year-comparison` |
| report | tax-burden | `/reports/tax-burden` |
| report | reserve-trend | `/reports/reserve-trend` |
| report | loss-expiry | `/reports/loss-expiry`(신설) |
| report | industry-stats | `/reports/industry-stats`(신설) |
| report | custom | `/reports/custom`(신설) |
| admin/cust | list / by-master / agent | `/customers`, `/business-years?bare=true`, `/tax-agents`(신설) |
| admin/sec | users / roles / matrix / menus / functions / mask / scope | `/admin/tenants/:tc/users`, `/admin/roles`, `/admin/role-permissions`(매트릭스), `/admin/menus`, `/admin/functions`(신설), `/admin/field-masking`(신설), `/admin/data-scope`(신설) |
| admin/cacc | assign / groups / rules / delegate / override | `/admin/customer-access`, `/admin/customer-groups`(신설), `/admin/customer-rules`(신설), `/admin/access-delegations`(신설), `/admin/customer-access/override`(신설) |
| admin/law | master / rates / limits / credits / depr-lives / sme / loss-rule / snapshots / impact / history | `/tax-laws`, `/tax-rates`, `/tax-limits?category=LIMIT`, `…?category=CREDIT`, `…?category=DEPRECIATION_LIFE`, `…?category=SME_CRITERIA`, `…?category=LOSS_RULE`, `/business-year-snapshots`, `/law-versioning/impact`, `/law-amendments` |
| admin/form | master / versions / fields / validations / linkage-rule / field-ref / migration / efile-map / by-set / impact | `/form-versioning/forms`, `…/versions`, `…/versions/:id/fields`(신설), `…/versions/:id/validations`(신설), `…/relationships`, `…/field-references`(신설), `…/migrations`, `…/efile-map`(신설), `…/by-set`(신설), `…/impact`(신설) |
| admin/code | industry / account / misc | `/codes?group=INDUSTRY|ACCOUNT|MISC`(신설) |
| admin/audit | events / login / perm / law / settings | `/tenants/:tc/audit-logs`, `/login-history`(신설), `/permission-change-history`(신설), `/law-amendment-history`, `/system-settings`(신설) |

신설 API는 모두 본 계획서의 산출물에 포함되며 `추가구현_v1.0.md` Pack과 매칭되도록 구분한다.

---

## 7. 영향 분석 (현행 코드)

| 파일/영역 | 변경 |
|---|---|
| `frontend/index.html` | `app-main` 내부에 `route-outlet` 외에 `subnav-outlet`, `stepper-outlet`, `law-banner-outlet` 슬롯 추가 |
| `frontend/app.js` | 라우트 호출을 `screens.js`(단일) → `router.js + screens/<group>/<key>.js` 모듈 로더로 교체 |
| `frontend/app/screens.js` | **삭제(또는 thin shim)**. 화면 모듈로 분리 |
| `frontend/app/menu.js` | 메뉴 트리가 `parent_key`·`layout`을 사용하도록 변경 |
| `frontend/app/context.js` | leaf 메뉴 진입 시 `requires_context` 검사 함수 추가 |
| `src/modules.rs` | leaf 99개를 갖는 트리 반환 |
| `migrations/2026052X000200_menu_full_tree.sql` | `menu_nodes` 99행 시드 |
| `src/api.rs` | 신설 라우트(7장 표에서 “신설”) 추가 |
| `tests/menu_full_tree.rs` | leaf 단위 200/권한/레이아웃 검사 |

`screens.js`(현 969줄)는 leaf별로 5~80줄 모듈로 쪼개진다. 평균 leaf 30줄로 99 × 30 ≈ **약 3000 LoC**, 현재보다 늘지만 단일 책임이 보장된다.

---

## 8. 마이그레이션 전략 (단계적 컷오버)

설계서 메뉴는 즉시 1대1 전환이 어렵다. 다음 3단계로 점진 전환한다.

| 단계 | 정책 |
|---|---|
| **S1. 메뉴 트리만 펼치고 화면은 기존 24개 통합 화면 재사용** | leaf 클릭 시 일단 기존 통합 화면을 띄우되 진입 시 해당 sub-tab을 자동 선택(`?subtab=B12`) |
| **S2. leaf별 화면 본격 분리(우선 신고 작업 1·2·4·7단계)** | 사용자가 가장 자주 닿는 leaf부터 단일 화면으로 교체 |
| **S3. 5-D, 5-E 등 관리 화면 leaf 분리 + 신설 API 매칭** | 백엔드 신설 라우트가 준비된 leaf부터 분리 |

S1→S2→S3 진행 시 “레이아웃은 같고 본문만 교체”가 가능해 사용자 학습 비용이 적다.

---

## 9. 일정 (총 9주)

| 주차 | Pack |
|---|---|
| W1~W1.5 | Pack A (라우팅·레이아웃·트리) |
| W2~W4 | Pack B (신고 작업 leaf 분할 — S1·S2 동시 진행) |
| W4.5~W6.5 | Pack C (관리 leaf 분할 — S1·S3) |
| W7 | Pack D (대시보드/사후/보고서 분할) |
| W8 | Pack E (권한 일관 적용) |
| W8.5~W9 | Pack F (회귀·자동화·문서) |

---

## 10. 위험과 대응

| 위험 | 대응 |
|---|---|
| 99 leaf × 신설 API = 작업량 폭증 | S1(통합 화면 재사용) 단계로 메뉴 트리·라우팅만 먼저 정착시키고, 화면은 점진 교체 |
| leaf가 너무 잘게 쪼개져 사용자 클릭 수 증가 | 좌측 서브내비를 강화(키보드 J/K, 가장 자주 쓰는 leaf 핀 고정) |
| 메뉴 키 체계 변경으로 권한 마이그레이션 필요 | `menu_nodes`에 `legacy_key` 컬럼 추가 + 변환 스크립트 1회 실행 |
| screens.js 파일 분할 PR이 비대화 | leaf 그룹(7~10개)을 PR 단위로 쪼개고 layout 도입 PR을 선행 |
| 백엔드 신설 라우트가 늦어지는 leaf | 해당 leaf는 “준비 중” 화면을 그대로 두고 다른 leaf 먼저 분리 |

---

## 11. 완료 정의 (DoD)

1. 좌측 메뉴가 설계서 §5.1의 전체 트리를 그대로 보여준다(99 leaf, 그룹 5개).
2. 모든 leaf URL이 `#/<group>/<key>` 형식으로 라우팅되고, 새로고침/공유 가능하다.
3. 각 leaf는 단일 책임 화면이다(통합 화면 잔재 없음, 또는 명시적으로 S1 상태임을 라벨 표시).
4. 진입 시 컨텍스트/권한 검사가 라우터에서 일관 적용된다.
5. `tests/menu_full_tree.rs`와 `docs/menu_smoke_checklist.md`(99행)이 모두 green.
6. 본 문서 + `2차구현계획.md` + `메뉴기능수정.md` + `추가구현_v1.0.md`가 일관된 메뉴 IA·기능 정의를 가리킨다.
# 2026-05-19 Implementation Update

- `src/modules.rs` now exposes the v1.1 recursive 5-group menu tree with every leaf enumerated in this document. The rows add up to 109 leaves, so the implementation preserves all 109 while also satisfying the DoD's `>= 99` intent.
- `menu_nodes` persistence now carries `layout`, `parent_key`, `requires_context`, `required_perm_module`, and `required_perm_function`; runtime seeding also populates `menu_functions` for the menu-function matrix.
- `frontend/app/router.js` supports shareable deep links such as `#/workspace/ws/adj/B12?byId=12`, `#/admin/law/master`, and `#/report/loss-expiry`.
- `frontend/app/menu.js` renders the full recursive sidebar. Workspace routes show the stepper and LawBanner; plain/report/post and admin routes hide them.
- `frontend/app/screens.js` implements S1: each leaf has an independent route and visible S1 status banner while delegating to the existing integrated screen until later leaf-by-leaf extraction.
- `tests/menu_full_tree.rs` asserts exact 109-key coverage, `>= 99` DoD coverage, layout metadata, context guards, one permission mapping per leaf, and representative frontend deep-link support.

---
