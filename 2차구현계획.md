# 법인세 세무조정 시스템 — 2차 구현 계획

> 작성일: 2026-05-18 · 작성자: 강수 (Kang-Soo.Cho@kr.ey.com)
> 대상: `prototype/index.html` 기반 UX·메뉴 체계로 `frontend/`를 재구성하고, 각 메뉴 기능을 실제 백엔드 API와 연결하여 동작시키는 작업.

---

## 1. 배경과 목적

### 1.1 배경
- 1차 구현(`단계별구현_claude.md`, `추가진행.md`)에서 백엔드(Rust/Axum) 모듈과 DB 스키마(`migrations/2026051500*`)는 대부분 구축됨.
- 현재 운영되는 화면(`frontend/index.html` + `frontend/app.js`)은 **백엔드 모듈 트리(0~9)** 를 그대로 좌측 메뉴로 나열한 “모듈 콘솔” 형태로, 한 페이지에 모든 패널이 펼쳐져 있음.
- 반면 디자인 시안인 `prototype/index.html`은 **세무 실무 흐름(① 대시보드 / ② 신고 작업 7단계 / ③ 사후 관리 / ④ 분석·보고서 / ⑤ 관리)** 중심의 IA를 제시함.

### 1.2 목적
1. `prototype/index.html`의 메뉴 IA(정보 구조)를 정식 메뉴로 채택한다.
2. 메뉴에서 호출되는 화면이 **목업이 아니라 실제 백엔드 API로 동작**하도록 한다.
3. 화면 단위가 아닌 **“업무 컨텍스트(고객사 + 사업연도)”** 를 축으로 라우팅·상태·권한이 흐르도록 정비한다.
4. 결과적으로 사용자가 로그인 → 고객사·연도 선택 → 신고 7단계 → 신고 완료까지 한 흐름으로 끝낼 수 있게 한다.

---

## 2. 현재 상태 분석

### 2.1 `prototype/index.html` 메뉴 트리 (목표 IA)

| 키 | 라벨 | 비고 |
|---|---|---|
| `dashboard` | ① 대시보드 | 상태별 카운트, 마감 임박, 결재함, 최근 활동 |
| `workspace` | ② 신고 작업 | 고객사·연도 컨텍스트 필요. 하위 0~7단계 스테퍼 |
| `ws-start` | 0. 작업 시작 | 고객사/사업연도 선택, 적용 스냅샷 미리보기 |
| `ws-info` | 1. 세무정보 입력 | 재무제표 / 계정매핑 / 자산 / 거래 / 차량 / 일관성 검증 |
| `ws-adj` | 2. 세무조정 | 17개 모듈(B-1 ~ B-17) 좌측 트리 |
| `ws-form` | 3. 서식 작성 | 별지 제3호 + 부속서식 ~100종 |
| `ws-val` | 4. 검증 | 142개 룰, 오류·경고·정보 등급 |
| `ws-appr` | 5. 결재 | 검토함, 승인/반려, 워크플로 이벤트 |
| `ws-print` | 6. 출력 | 서식 선택 → PDF/ZIP |
| `ws-file` | 7. 전자신고 | 사전검증 → 파일생성 → 제출 → 접수결과 |
| `post-hist` | ③-1 신고 이력 | 접수번호, 제출일, 상태 |
| `post-amend` | ③-2 수정신고/경정청구 | 잠금 해제 + 사용 버전 선택 |
| `rp-alerts` | ④-1 알림 센터 | WARN/INFO 등급 |
| `rp-compare` | ④-2 사업연도 비교 | 연도별 조정합계·유보 |
| `rp-burden` | ④-3 세부담 분석 | 과세표준·납부세액·실효세율 |
| `rp-reserve` | ④-4 유보 잔액 추이 | 항목별 잔액·구분 |
| `ad-tenant` | ⑤-0 테넌트 관리 | 멀티테넌트 등록·요금제·상태 |
| `ad-cust` | ⑤-A 고객사 관리 | 고객사 프로필·계약·담당자 |
| `ad-user-list` | ⑤-B 사용자 관리 | 사용자·역할·고객사 접근권 |
| `ad-role` | ⑤-C 역할/권한 매트릭스 | 모듈×기능 ALLOW/DENY |
| `ad-menu-fn` | ⑤-D 메뉴/기능 관리 | 메뉴 트리·활성 기능 |
| `ad-cacc` | ⑤-E 담당 법인 권한 | 사용자×고객사×작업범위 |
| `ad-law` | ⑤-F 법령·세율 버전 | 법령·세율·한도·공제·내용연수 등 10탭 |
| `ad-form` | ⑤-G 서식 버전 | 서식 메타·매핑·마이그레이션·미리보기 |
| `ad-audit` | ⑤-H 감사·로그 | 모듈/사용자/일자 필터 |

### 2.2 현재 `frontend/`가 그리는 메뉴 (출발점)
- 좌측 메뉴는 백엔드 `src/modules.rs::module_tree()`가 반환하는 모듈 트리(0. 법령버전 ~ 9. 분석운영)로 동적 생성됨(`renderModuleMenu` in `frontend/app.js`).
- 메뉴 클릭 → 그 모듈의 패널로 스크롤하고 해당 API 호출(`navigateLawRoute`, `navigateAdminRoute`, …).
- 작업 컨텍스트(현재 고객사·사업연도) 개념 없음 → 모든 패널이 “전역 콘솔” 형태.

### 2.3 백엔드 API 매핑 가능성 (실측)
`src/api.rs::router()`의 등록된 라우트를 prototype 메뉴에 매핑하면 다음과 같이 **대부분 이미 존재**한다.

| prototype 메뉴 | 매핑 가능한 백엔드 라우트 |
|---|---|
| ① 대시보드 | `GET /api/tenants/:tenant_code/dashboard`, `GET /api/operations/launch-readiness`, `GET /api/jobs` |
| ② 작업 시작 (`ws-start`) | `GET /api/tenants/:tenant_code/customers`, `GET /api/tenants/:tenant_code/business-years`, `GET …/business-years/:by_id/snapshot` |
| 1. 세무정보 입력 | `GET/POST …/tax-data/financial-statements`, `…/account-mappings`, `…/tax-data/assets`, `…/transactions`, `…/vehicle-usage-logs`, `POST …/tax-data/validation`, `POST …/tax-data/:data_type/import` |
| 2. 세무조정 | `POST …/business-years/:by_id/adjustments/income`, `…/adjustments/assets/:module_code`, `…/adjustments/transactions/:module_code`, `…/adjustments/evaluation/:module_code`, `…/adjustments/tax/:module_code`, `…/adjustments/special/:module_code`, `GET …/reserves` |
| 3. 서식 작성 | `GET …/forms/:form_code`, `GET …/forms/:form_code/preview`, `GET …/forms/:form_code/pdf`, `GET …/forms/attachments`, `POST …/forms/pdf-bundle/download` |
| 4. 검증 | `POST …/tax-data/validation`, `POST …/efilings/precheck` (검증 룰셋 일부는 신설 필요) |
| 5. 결재 | `PATCH/POST …/business-years/:by_id/workflow`, `…/business-years/:by_id/status` |
| 6. 출력 | `GET …/forms/:form_code/pdf`, `POST …/forms/pdf-bundle/download` |
| 7. 전자신고 | `GET …/efilings/format-spec`, `POST …/efilings/precheck`, `POST …/efilings`, `GET …/efilings/:efiling_id/file` |
| ③ 사후 관리 | `GET …/efilings`, `GET …/business-years/:by_id/amendment-preview` |
| ④ 분석/보고서 | `GET …/notifications`, `GET …/reports/tax-burden`, `GET …/reports/year-comparison`, `GET …/audit-logs` |
| ⑤ 관리 5-0 ~ 5-H | `…/tenants`, `…/admin/tenants/:tenant_code/users…`, `…/admin/roles`, `…/admin/role-permissions`, `…/admin/roles/:role_code/permissions`, `…/tax-laws`, `…/tax-rates`, `…/tax-limits`, `…/law-amendments`, `…/law-versioning/summary`, `…/law-versioning/impact`, `…/form-versioning/*`, `…/tenants/:tenant_code/audit-logs` |

### 2.4 결과 요약
- 메뉴 IA만 prototype 기준으로 재배치하면, 백엔드는 대체로 그대로 활용 가능.
- 신설/보강이 필요한 것은 **(a) 워크플로 상태 전이 API**, **(b) 검증 룰 카탈로그/룰 실행 결과 영속화**, **(c) 메뉴/기능 관리(5-D)의 RBAC 메타데이터**, **(d) 일부 사후관리(잠금해제·경정청구) 엔티티**.

---

## 3. 메뉴 변경 방향

### 3.1 IA 결정
`prototype/index.html`의 `MENU` 상수(라인 128~161)를 **신규 frontend의 메뉴 단일 소스**로 채택한다. 백엔드 `src/modules.rs::module_tree()`는 다음과 같이 재정비한다.

- 기존 top-level 0~9 모듈 트리를 **legacy 트리**로 유지하되 `/api/modules/legacy-tree`로 별도 노출(개발자/감사용).
- 신규 `/api/modules/tree`는 `prototype`의 5대 메뉴 + 하위 키 구조를 반환한다.  
  반환 JSON 스키마(요지):
  ```jsonc
  {
    "code": "cit-system",
    "children": [
      { "code": "dashboard", "label": "대시보드", "path": "/app/dashboard", "icon": "📊" },
      { "code": "workspace", "label": "신고 작업", "path": "/app/workspace",
        "children": [ /* ws-start ~ ws-file 8개 */ ],
        "requires_context": ["customer_id", "business_year_id"] },
      { "code": "post",      "label": "사후 관리",   "children": [ /* post-hist, post-amend */ ] },
      { "code": "reports",   "label": "분석/보고서", "children": [ /* rp-* 4개 */ ] },
      { "code": "admin",     "label": "관리",        "children": [ /* ad-tenant ~ ad-audit 9개 */ ] }
    ]
  }
  ```
- 메뉴 노드에는 `requires_context`, `required_permissions[]`, `feature_flag` 필드를 추가하여 5-D 메뉴/기능 관리에서 활성화·비활성화 가능하도록 함.

### 3.2 URL/라우팅
- 단일 페이지(SPA)이지만 hash 라우팅을 추가한다. `#/dashboard`, `#/workspace/ws-info?byId=12`, `#/admin/ad-role` 형태.
- `popstate`/`hashchange` 이벤트로 `navigateMenu(key, params)`를 호출한다.
- “고객사·사업연도”가 결정되면 hash 파라미터(`?byId=…&customerId=…`)에 보존하여 새로고침/공유 가능.

### 3.3 컨텍스트 모델
프론트엔드 `state`에 `workContext`를 추가한다.
```js
state.workContext = {
  customerId: null,
  customerName: null,
  byId: null,
  fy: null,
  period: null,
  status: null,        // DRAFT | IN_REVIEW | APPROVED | FILED | AMENDED
  progress: 0,
  snapshot: null,      // /business-years/:by_id/snapshot
  lockMode: "OPEN",    // OPEN | LOCKED
};
```
- 컨텍스트 미설정 시 `② 신고 작업` 하위 메뉴(0번 “작업 시작” 제외)는 비활성화.
- 컨텍스트 설정 시 좌측 사이드바 하단에 `current.customerName / fy / status` 표시(프로토타입 Sidebar 라인 310~320).
- 컨텍스트는 `localStorage("cit.work.context")`에 직렬화하여 세션 간 보존.

---

## 4. 산출물 정의 (Deliverables)

| 분류 | 파일/엔드포인트 | 변경 유형 |
|---|---|---|
| 메뉴 트리 | `src/modules.rs::module_tree()` | 재작성 (prototype IA) |
| 권한 모델 | `migrations/2026051X000X_menu_rbac.sql` | 신규 (메뉴 키 ↔ 권한 매핑) |
| 워크플로 | `src/forms.rs` / `src/domain.rs` 일부 + 신규 `src/workflow.rs` | 신규 + 재배치 |
| 검증 룰 | `src/tax.rs` + 신규 `src/validation_rules.rs` | 신규 룰 카탈로그 |
| 프론트 셸 | `frontend/index.html` | 재작성 (Sidebar/Topbar/Router/Workspace 라우트 영역) |
| 프론트 로직 | `frontend/app.js` → `frontend/app/` 폴더 분할(권장) | 재구성 |
| 프론트 스타일 | `frontend/app.css` | 추가 (스테퍼, LawBanner, 카드) |
| 문서 | `2차구현계획.md`(본 문서), `메뉴_API_매트릭스.md`(부속) | 작성 |
| 테스트 | `tests/menu_routing.rs`, `tests/workflow_transition.rs` | 신규 |

---

## 5. 단계별 구현 계획 (Phase 0 ~ Phase 12)

> 각 Phase는 “목적 → 작업 → 산출물 → 완료 판정” 4축으로 정의한다. Phase 0~2는 기반, 3~10은 메뉴별, 11~12는 보강·통합이다.

### Phase 0. 베이스라인 잠금
- **목적**: 현재 동작하는 frontend/백엔드 상태를 회귀 기준으로 동결.
- **작업**
  1. `git tag phase1-baseline` 부여.
  2. `cargo test --all` / `cargo clippy --all-targets -- -D warnings` 통과 스냅샷 보관.
  3. `frontend/app.js`의 현재 `state`, `lawScreens`, `navigate*Route` 시그니처 문서화.
- **완료 판정**: 태그 생성 + CI green.

### Phase 1. 메뉴 트리 교체 (백엔드)
- **목적**: 백엔드가 prototype IA를 1차 진리원으로 노출.
- **작업**
  1. `src/modules.rs`에 `prototype_menu_tree()` 함수 추가. 기존 `module_tree()`는 `legacy_module_tree()`로 이름 변경.
  2. `src/api.rs`
     - `/api/modules/tree` 핸들러를 `prototype_menu_tree()`로 변경.
     - `/api/modules/legacy-tree` 추가(개발용).
  3. 메뉴 노드 스키마에 `requires_context`, `required_permissions[]`, `feature_flag` 필드 직렬화.
  4. `migrations/2026051X000X_menu_rbac.sql` 신규: `menu_node(key, parent_key, label, path, requires_context, feature_flag, required_perm_module, required_perm_function, sort_order)` 시드.
- **완료 판정**: `curl /api/modules/tree`가 새 트리 반환, 단위테스트 `modules::tests::prototype_tree_shape` 통과.

### Phase 2. 프론트엔드 셸 재작성
- **목적**: prototype의 `Sidebar / TopBar / Stepper / LawBanner / Router`를 frontend에 이식.
- **작업**
  1. `frontend/index.html` 골격을 다음으로 교체:
     - 좌: `<aside id="cwk-sidebar">`, 우상단: `<header id="cwk-topbar">`, 메인: `<main id="cwk-route-outlet">`.
  2. `frontend/app.js` 분할:
     - `app/router.js` (hash 라우터, `navigate(key, params)`)
     - `app/menu.js` (트리 렌더, 활성화 표시, 권한·컨텍스트 비활성)
     - `app/context.js` (workContext 저장/복원)
     - `app/api.js` (request, uploadRequest — 기존 유지)
     - 화면별 `app/screens/*.js` 로 분리
  3. 좌측 사이드바는 prototype의 5대 메뉴 카드 디자인을 그대로 사용. 다만 컬러 토큰은 `frontend/app.css`에 정의.
  4. `LawBanner`, `Stepper` 컴포넌트(라이트 JS 클래스) 신규.
- **완료 판정**: 로그인 후 좌측에 prototype과 동일한 메뉴가 나타나고, 각 메뉴 클릭 시 hash가 바뀌며 “준비 중” 플레이스홀더가 표시됨.

### Phase 3. ① 대시보드 (`dashboard`)
- **목적**: 실데이터 기반 카운트·마감·결재함·최근활동.
- **API**: `GET /api/tenants/:tenant_code/dashboard`, `GET /api/tenants/:tenant_code/notifications`, `GET /api/tenants/:tenant_code/business-years?state=…`
- **작업**
  1. 상단 5종 카드(작성중/검증대기/결재대기/승인완료/신고완료)는 dashboard 응답의 `status_counts`로 채움. 미존재 필드는 백엔드 보강.
  2. “신고 마감 임박”은 `business-years`에서 `due_date - today <= 30d` 필터.
  3. “내 결재함”은 `workflow_events`에서 `assignee = me && state = IN_REVIEW`.
  4. “최근 활동”은 `audit-logs`의 최신 10건(필터: `module ∈ {workflow, tax-data, adjustment}`).
- **완료 판정**: 모의 데이터 없이 빈 테넌트에서도 깨지지 않으며, 카운트가 DB 변경에 즉시 반영됨.

### Phase 4. ② 신고 작업 — 0. 작업 시작 (`ws-start`)
- **목적**: 고객사 × 사업연도를 선택하고 workContext 확정.
- **API**: `GET /api/tenants/:tenant_code/customers`, `GET /api/tenants/:tenant_code/business-years`, `GET /api/tenants/:tenant_code/business-years/:by_id/snapshot`
- **작업**
  1. 고객사·연도 목록 테이블 + 진행률(progress) + 상태 배지.
  2. 행 선택 → `snapshot` 호출 → 적용 법령(법인세율, 접대비한도, R&D 공제율, 별지제3호 버전) 카드 표시.
  3. `[계속 ▶]` 클릭 시 `setWorkContext({...})` 후 `#/workspace/ws-info?byId=…&customerId=…`로 이동.
  4. 새 사업연도 등록 모달(`POST …/business-years`) 추가.
- **완료 판정**: 컨텍스트 설정 후 좌측 사이드바 하단에 고객사·연도·상태 표시, 1~7단계 메뉴 활성화.

### Phase 5. ② 신고 작업 — 1. 세무정보 입력 (`ws-info`)
- **목적**: 재무제표·계정매핑·자산·거래·차량·일관성 검증.
- **API**:
  - `GET/POST /api/tenants/:tc/business-years/:by_id/tax-data/financial-statements`
  - `GET/POST /api/tenants/:tc/customers/:cust_id/account-mappings`
  - `GET/POST /api/tenants/:tc/business-years/:by_id/tax-data/assets`
  - `GET/POST /api/tenants/:tc/business-years/:by_id/tax-data/transactions`
  - `GET/POST /api/tenants/:tc/business-years/:by_id/vehicle-usage-logs`
  - `POST /api/tenants/:tc/business-years/:by_id/tax-data/:data_type/import` (Excel)
  - `POST /api/tenants/:tc/business-years/:by_id/tax-data/validation`
- **작업**
  1. prototype의 6-탭 좌측 서브내비 그대로 채택(`1-1` ~ `1-9`).
  2. Excel 가져오기 → multipart 업로드 → `import-batches` 상태 폴링 → 에러 시 `import-batches/:batch_id/errors` 결과 표시.
  3. 1-9 일관성 검증: `tax-data/validation` 호출 결과를 etag 등급(✓/!/X)로 표시.
- **완료 판정**: 적어도 재무제표·자산은 입력→저장→재조회 라운드트립이 화면에서 가능. validation 통과 시 1단계 진행률 ≥ 70%.

### Phase 6. ② 신고 작업 — 2. 세무조정 (`ws-adj`)
- **목적**: 17개 세무조정 모듈(B-1 ~ B-17) 운영.
- **API**(라우트는 모두 존재):
  - 통합: `POST …/adjustments/income`
  - 자산기반: `POST …/adjustments/assets/:module_code`
  - 거래기반: `POST …/adjustments/transactions/:module_code`
  - 평가: `POST …/adjustments/evaluation/:module_code`
  - 세액: `POST …/adjustments/tax/:module_code`
  - 특수: `POST …/adjustments/special/:module_code`
  - 유보: `GET …/business-years/:by_id/reserves`
- **작업**
  1. prototype의 좌측 그룹 트리(통합/자산기반/거래기반/평가/유보/세액/특수) 그대로.
  2. 각 모듈별 “행 그리드 + 한도 카드 + 자동 분개” 컴포넌트화. 행 단위 CRUD는 모듈별 `POST` 페이로드로 일괄 저장.
  3. “자동 분개” 버튼은 백엔드의 기본 룰 결과(`/adjustments/income` 응답의 `auto_entries`)를 미리보기.
  4. 모듈 간 자동연동(예: 2-3 접대비 한도초과 → B-1 통합)은 응답에 포함된 `derived_from`을 UI에서 회색 표시.
- **완료 판정**: 17개 모듈 모두 “목록 조회 + 저장 + 합계 자동 산출”까지 동작. `business-years/:by_id/adjustments`(전체) 응답이 일관됨.

### Phase 7. ② 신고 작업 — 3. 서식 작성 (`ws-form`)
- **목적**: 별지 제3호와 부속서식 100여 종 운영.
- **API**: `GET …/forms/:form_code`, `…/forms/:form_code/preview`, `…/forms/:form_code/pdf`, `…/forms/attachments`, `POST …/forms/pdf-bundle/download`, `GET /api/form-versioning/resolve`
- **작업**
  1. 상단: 별지 제3호 미리보기(텍스트 폼) — `forms/FORM3/preview` 결과 렌더.
  2. 하단: 부속서식 일람 — `forms/attachments` 응답으로 카드/표.
  3. 자동연동 라인(②, ③, ⑤ 등)은 백엔드 `links[]`에 따라 회색 배경 + 점프 링크.
  4. “전체 ZIP” = `forms/pdf-bundle/download`, “선택 PDF” = 선택 form_code 배열로 호출.
- **완료 판정**: 별지3호 + 최소 5개 부속서식(15호, 20호, 22호, 32호, 50호)의 미리보기·PDF 다운로드 정상 동작.

### Phase 8. ② 신고 작업 — 4. 검증 (`ws-val`)
- **목적**: 142개(목표) 룰 카탈로그 결과 표시 및 오류 카운트 0이어야 다음 단계.
- **API/신설**
  - 기존: `POST …/tax-data/validation`(데이터 일관성), `POST …/efilings/precheck`(전자신고 사전검증)
  - **신설**: `POST /api/tenants/:tc/business-years/:by_id/validation/run` — 전사 룰셋 실행, `POST …/validation/issues/:issue_id/dismiss` — 무시 처리, `GET …/validation/rules` — 카탈로그
- **작업**
  1. 룰 카탈로그 테이블 신설: `migrations/…_validation_rules.sql`(`rule_code, severity, area, message_template, applies_to`). 17개 모듈·서식·전자신고 룰을 시드.
  2. `src/validation_rules.rs` 신규: 각 룰에 대해 `Rule::evaluate(ctx) -> Vec<Issue>`.
  3. 화면: 오류/경고/정보 카운트, 이슈 표, 행별 “이동” 버튼(해당 메뉴/서브키로 hash 라우팅).
  4. 오류 0이면 “다음 (5. 결재)” 활성, 아니면 비활성(prototype 로직 그대로).
- **완료 판정**: 시드 룰 50개 이상으로 일관성·접대비·법령 알림이 잡히고, 모든 오류 해소 후 다음 단계로 이동 가능.

### Phase 9. ② 신고 작업 — 5. 결재 (`ws-appr`)
- **목적**: 워크플로 상태 전이(IN_REVIEW → APPROVED/REJECTED), 검토 의견 기록.
- **API/신설**
  - 기존: `GET/PATCH …/business-years/:by_id/workflow`, `…/business-years/:by_id/status`
  - **신설**(필요시): `POST …/workflow/events`(검토 코멘트 + 첨부), `GET …/workflow/queue?assignee=me`
- **작업**
  1. 내 결재 대기함: `workflow/queue?assignee=me`.
  2. 검토 카드: 변경 항목 수, 첨부 수, 검증 결과 요약(Phase 8 결과 캐시).
  3. 승인/반려 버튼 → `PATCH workflow {to: APPROVED|DRAFT, comment}` 호출, 성공 시 상태배지 변경.
  4. “워크플로 이력 / 수정신고 차이” 패널: `workflow_events` + `amendment-preview`.
- **완료 판정**: 결재자가 승인 시 사업연도 상태 `APPROVED`, 작성자에게 알림 생성, `business_years.progress = 85%`.

### Phase 10. ② 신고 작업 — 6. 출력 / 7. 전자신고 (`ws-print`, `ws-file`)
- **목적**: 출력 묶음과 홈택스 레코드 파일 일련 단계.
- **API**: `…/forms/pdf-bundle/download`, `…/efilings/format-spec`, `…/efilings/precheck`, `POST …/efilings`, `GET …/efilings/:efiling_id/file`
- **작업**
  1. 6. 출력: 서식 체크박스 → `pdf-bundle/download`로 일괄 PDF 또는 ZIP.
  2. 7. 전자신고 4-step:
     - Step1: `efilings/format-spec` + `efilings/precheck` 결과 표.
     - Step2: `POST /efilings` 호출, 결과(jobId, 파일명, sha256, 크기) 표시.
     - Step3: 제출 — “API 직접 제출” 시 백엔드 보유 자격 사용, “수동 업로드” 시 `efilings/:id/file` 다운로드.
     - Step4: 접수 결과 — 접수번호, 일시, 상태. 성공 시 상태 `FILED`, `progress=100`, lockMode=`LOCKED`.
- **완료 판정**: 데모 사업연도에 대해 ZIP 다운로드 + EUC-KR 위치기반 파일 생성 + DLQ 시뮬레이션까지 OK.

### Phase 11. ③ 사후 관리 (`post-hist`, `post-amend`)
- **목적**: 신고 이력 + 잠금 해제·수정신고 진입.
- **API**: `GET …/efilings`, `GET …/business-years/:by_id/amendment-preview`, **신설** `POST …/business-years/:by_id/unlock`(이유, 사용 버전 모드).
- **작업**
  1. 이력 표: filed 사업연도들 + 접수번호 + 상태.
  2. 잠금 해제 신청 모달: “원 신고시점 버전 / 최신 버전” 라디오 + 사유. `unlock` 호출 시 `lockMode=OPEN`, 상태 `AMENDED`.
  3. 차이 미리보기는 `amendment-preview` 응답 그대로.
- **완료 판정**: filed 사업연도에 대해 잠금 해제 → 재편집 → 재신고 가능한 경로가 닫힌 루프로 작동.

### Phase 12. ④ 분석/보고서 (`rp-alerts`, `rp-compare`, `rp-burden`, `rp-reserve`)
- **API**: 기존 `notifications`, `reports/tax-burden`, `reports/year-comparison`. **신설**: `GET …/reports/reserve-trend`.
- **작업**
  1. 알림 센터: WARN/INFO 토글, “읽음” 처리(`PATCH notifications/:id`).
  2. 사업연도 비교: 응답을 라인 차트(Chart.js, CDN)로 렌더. 텍스트만으로도 가능.
  3. 세부담 분석: 과세표준·납부세액·실효세율 테이블 + 추세선.
  4. 유보 잔액: `reserve-trend`로 신설.
- **완료 판정**: 최근 2개 사업연도가 있는 테넌트에서 4개 보고서 모두 비어 보이지 않음.

### Phase 13. ⑤ 관리 (5-0 ~ 5-H)
- **5-0 테넌트 관리**: `…/tenants` CRUD, 상태 토글, 요금제·계약 기간.
- **5-A 고객사 관리**: `…/customers` CRUD + 담당자(`customer_access`).
- **5-B 사용자 관리**: `…/admin/tenants/:tc/users` + `…/users/:login_id/status` + `…/reset-2fa`.
- **5-C 역할/권한 매트릭스**: `…/admin/roles`, `…/admin/roles/:role_code/permissions` PUT.
- **5-D 메뉴/기능 관리**: Phase 1에서 만든 `menu_node` 테이블에 대해 `feature_flag`·`required_permissions` 편집 화면.
- **5-E 담당 법인 권한**: 사용자×고객사×work_scopes 매트릭스. 기존 admin API의 `customer_access[]` 사용.
- **5-F 법령·세율 버전**: 현 `lawScreens` 10탭을 5-F 아래로 이동(메뉴만 옮기고 화면 재사용).
- **5-G 서식 버전**: 기존 `formScreens` 화면 5-G 아래로 이동.
- **5-H 감사·로그**: `…/tenants/:tc/audit-logs` 필터(모듈/사용자/일자) UI.
- **완료 판정**: 5대 영역 모두 “목록 + 단건 편집 + 즉시 반영” 작동.

### Phase 14. 통합·회귀·문서화
- **작업**
  1. e2e 시나리오 스크립트 작성: 로그인 → 작업 시작 → 1~7단계 → 사후관리.
  2. `cargo test --all`, 새 통합 테스트 `tests/menu_routing.rs`, `tests/workflow_transition.rs` 추가.
  3. `frontend/`에 간단한 Playwright(또는 Cypress) 스모크 1세트.
  4. `README.md`의 “Web UI” 섹션 갱신, `2차구현계획.md`(본 문서) 산출물 링크 추가.
- **완료 판정**: CI green + 매뉴얼 시연 1회 완료.

---

## 6. 메뉴 ↔ 화면 ↔ API 매트릭스 (요약)

| 메뉴 키 | 화면 컴포넌트 | 주요 API | 컨텍스트 필요 | Phase |
|---|---|---|---|---|
| dashboard | DashboardScreen | `/dashboard`, `/notifications`, `/audit-logs` | × | 3 |
| ws-start | WorkStartScreen | `/customers`, `/business-years`, `/business-years/:id/snapshot` | × | 4 |
| ws-info | WorkInfoScreen (6-탭) | `/tax-data/*`, `/account-mappings`, `/vehicle-usage-logs`, `/tax-data/validation` | ○ | 5 |
| ws-adj | WorkAdjScreen | `/adjustments/{income,assets,transactions,evaluation,tax,special}/*`, `/reserves` | ○ | 6 |
| ws-form | WorkFormScreen | `/forms/*`, `/form-versioning/resolve` | ○ | 7 |
| ws-val | WorkValScreen | `/validation/*`, `/tax-data/validation`, `/efilings/precheck` | ○ | 8 |
| ws-appr | WorkApprScreen | `/business-years/:id/workflow`, `/business-years/:id/status` | ○ | 9 |
| ws-print | WorkPrintScreen | `/forms/:code/pdf`, `/forms/pdf-bundle/download` | ○ | 10 |
| ws-file | WorkFileScreen | `/efilings/format-spec`, `/efilings/precheck`, `/efilings`, `/efilings/:id/file` | ○ | 10 |
| post-hist | PostHistScreen | `/efilings`, `/business-years` | × | 11 |
| post-amend | PostAmendScreen | `/business-years/:id/unlock`, `/business-years/:id/amendment-preview` | ○ | 11 |
| rp-alerts | ReportsAlerts | `/notifications` | × | 12 |
| rp-compare | ReportsCompare | `/reports/year-comparison` | × | 12 |
| rp-burden | ReportsBurden | `/reports/tax-burden` | × | 12 |
| rp-reserve | ReportsReserve | `/reports/reserve-trend` (신설) | × | 12 |
| ad-tenant | AdminTenant | `/tenants` | × | 13 |
| ad-cust | AdminCustomer | `/customers` | × | 13 |
| ad-user-list | AdminUsers | `/admin/tenants/:tc/users` | × | 13 |
| ad-role | AdminRoles | `/admin/roles`, `/admin/role-permissions` | × | 13 |
| ad-menu-fn | AdminMenu | `/admin/menus` (신설) | × | 13 |
| ad-cacc | AdminCustomerAccess | `/admin/tenants/:tc/users/:lid` | × | 13 |
| ad-law | AdminLaw (기존 lawScreens) | `/tax-laws`, `/tax-rates`, `/tax-limits`, `/law-amendments`, `/law-versioning/*` | × | 13 |
| ad-form | AdminForm (기존 formScreens) | `/form-versioning/*` | × | 13 |
| ad-audit | AdminAudit | `/tenants/:tc/audit-logs` | × | 13 |

부속 문서 `메뉴_API_매트릭스.md`를 별도 작성해 “페이로드 예시 + 응답 키 + 화면 필드 매핑”까지 상세화한다(Phase 1과 함께).

---

## 7. 백엔드 보강 필요 항목

| 항목 | 현 상태 | 보강 |
|---|---|---|
| 워크플로 큐 조회 | 라우트는 있으나 응답 스펙 불명확 | `GET /api/tenants/:tc/workflow/queue?assignee=...` 추가, `WorkflowEvent` DTO 확정 |
| 검증 룰 카탈로그/실행 | 일부만 존재 | `validation_rules` 테이블 + `/validation/*` 라우트 신설 |
| 메뉴/기능 관리 | 정적 트리 | `menu_node` 테이블 + `/admin/menus` CRUD |
| 잠금 해제·수정신고 | preview만 | `POST /business-years/:id/unlock`, 상태 전이 규칙 |
| 보고서 reserve-trend | 없음 | `GET /reports/reserve-trend` 신설 |
| 알림 읽음 처리 | unread만 노출 | `PATCH /notifications/:id` |

---

## 8. 일정 (안)

| 주차 | 범위 |
|---|---|
| W1 | Phase 0 ~ 2 (베이스라인, 백엔드 메뉴 트리, 프론트 셸) |
| W2 | Phase 3 ~ 5 (대시보드, 작업 시작, 세무정보 입력) |
| W3 | Phase 6 (세무조정 17모듈) |
| W4 | Phase 7 ~ 8 (서식, 검증 + 룰 카탈로그) |
| W5 | Phase 9 ~ 10 (결재, 출력/전자신고) |
| W6 | Phase 11 ~ 12 (사후관리, 보고서) |
| W7 | Phase 13 (관리 5-0 ~ 5-H) |
| W8 | Phase 14 (회귀, 문서화, 시연) |

---

## 9. 위험과 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| frontend/app.js 단일 파일 비대화 | 유지보수성 저하 | Phase 2에서 `frontend/app/`로 모듈 분리. import-map 또는 빌드 도입은 별도 결정 |
| 백엔드 라우트와 화면 필드 불일치 | 화면 깨짐 | Phase 1에 부속 매트릭스 작성 + 각 Phase 시작 시 응답 스키마 합의 |
| 검증 룰 142개 시드 작업량 | Phase 8 지연 | 우선순위 룰 50개 → 100개 → 142개 단계적 확장 |
| 워크플로/잠금 모델 회귀 위험 | 신고 완료 후 데이터 무결성 | 통합 테스트 우선 작성(`workflow_transition.rs`) |
| 프로토타입 React → frontend의 Vanilla JS 변환 비용 | Phase 2 지연 | 화면 단위로 점진 이식. 신규 화면은 Vanilla로, 복잡 화면은 동일 DOM 구조 유지 |

---

## 10. 완료 정의 (DoD)

1. 좌측 메뉴가 prototype과 동일한 5대/하위 키로 표시된다.
2. 모든 메뉴 클릭이 hash URL 변경 + 실제 백엔드 API 호출로 이어진다.
3. workContext가 결정되면 1~7단계 메뉴가 활성화되고, 단계 간 이동 시 진행률·상태가 업데이트된다.
4. 7단계의 마지막 “접수 결과 ACCEPTED”까지 데모 데이터로 한 번 통과한다.
5. ⑤ 관리 메뉴에서 사용자/역할/법령/서식/감사 화면이 모두 라이브 데이터로 표시된다.
6. `cargo test --all`, `cargo clippy -- -D warnings`, 신규 통합 테스트 2개가 통과한다.
7. 본 문서와 부속 매트릭스가 `D:\NEW_CIT/`에 보관된다.

---

## 11. 2026-05-19 정합성 업데이트

- Phase 0~14 계획은 현재 `src/modules.rs`의 5대 메뉴 IA와 `frontend/app/{router,menu,context,screens}.js` 기준으로 구현되어 있다.
- 추가구현 v1.0 Pack 1~6 결과를 반영해 신고 작업 1~7단계, 사후관리, 보고서, 관리 메뉴는 모두 실제 API를 호출한다.
- 신규 관리 표면은 함수 코드, 메뉴-기능, 역할-메뉴-기능, 고객 접근 위임, 감사 해시 검증, 서식 관계 cycle-check를 포함한다.
- 보고서 표면은 세부담/사업연도/유보 추이에 더해 손실 만료, 업종 통계, 사용자 정의 보고서를 포함한다.
- 외부 ERP/e-Tax/Redis 연동은 본 2차 구현 완료 기준에서 제외하고 P3 로드맵으로 유지한다.
# 2026-05-19 v1.1 Menu Tree Update

- Phase 1/2 menu work is now expanded from the former 24 integrated menu entries to the v1.1 recursive leaf tree.
- `/api/modules/tree` returns 5 root groups and 109 listed leaf routes. `/api/modules/legacy-tree` still keeps the phase-1 audit tree.
- Frontend routing now supports deep links in the `#/<group>/<key>` style and maps them to workspace/plain/admin layouts.
- S1 migration is active: leaf routes are independent and visible, while current integrated screens remain delegates until each leaf is fully extracted.
- Verification is covered by `tests/menu_full_tree.rs`, `tests/menu_routing.rs`, and `docs/menu_smoke_checklist.md`.

---
