# 추가 구현 계획 v1.5
## leaf 화면 본문 CRUD UI 복원 + 업무 흐름 가시화 일관 적용

> 작성일: 2026-05-18 · 작성자: 강수 (Kang-Soo.Cho@kr.ey.com)
> 선행 문서: `법인세_세무조정계산서_시스템_설계서.md`, `2차구현계획.md`, `메뉴기능수정.md`, `추가구현_v1.0~v1.4.md`
> 본 문서의 범위: 현재 leaf 100개가 “기능 실행” 버튼 1개만 본문에 보이는 placeholder 상태로 머물러 있는 것을 **각 leaf의 세부 기능 화면(목록·폼·필터) + 추가/수정/삭제 동작**으로 복원한다. 동시에 업무 흐름 가시화(Stepper·LawBanner·Sidebar 앵커·다음 단계 추천)를 leaf 진입 시 “항상” 보이게 일관 적용한다.

---

## 1. 증상 진단

### 1.1 본문 “기능 실행 버튼만 보임”
- 코드 위치: `frontend/app/screens.js:469 renderLeafScreen(env, key)`
- 현재 본문은 다음 한 덩어리뿐:
  ```html
  <section class="panel leaf-screen" data-leaf-key="…">
    <div class="panel-head">
      <span class="badge info">Leaf screen</span>
      <h2>{leaf.title}</h2>
      <p>{leaf.key} · {perm.module}:{perm.function}</p>
      <button class="primary-btn" data-leaf-action>기능 실행</button>
    </div>
    <div class="leaf-action-result" aria-live="polite"></div>
  </section>
  ```
- 표시되는 것: 메뉴 제목 + 메뉴 키 + 권한 라벨 + **단 1개의 “기능 실행” 버튼**.
- 누락된 것: **목록 표 / 등록 폼 / 수정 모달 / 삭제 확인 / 검색·필터 / 페이지네이션 / 카드 KPI**.
- 결과: 사용자는 메뉴 100개 어디를 눌러도 “해당 메뉴의 일은 무엇이고, 어떤 데이터가 있는지” 알 수 없고 “기능 실행”만 한 번 누를 수 있다.

### 1.2 업무 흐름 가시화 부재
- `app.js:renderRoute()`는 `meta.layout==="workspace"`일 때만 stepper를 그린다. leaf 본문이 placeholder라서 본문 내부 “다음 단계 카드”, “법령 배너”, “단계 진행률” 시각도 함께 사라져 있다.
- 사이드바도 그룹 헤더(① 대시보드/② 신고작업/③ 사후관리/④ 분석·보고서/⑤ 관리)가 현재 어디에 있는지 명시적 active 표시가 미흡.
- v1.4에서 Stepper/LawBanner/StatePill 컴포넌트를 정의했지만, 실제 leaf 본문이 placeholder인 상태라 사용자가 “흐름이 보이지 않는다”고 느낀다.

### 1.3 백엔드는 대부분 준비됨
- `src/api.rs`에 GET/POST/PUT/PATCH/DELETE 라우트가 leaf별로 거의 모두 존재한다.
- 누락 영역(추가구현_v1.3 §4에서 신설 표기됨): `/tax-agents`, `/admin/functions`, `/admin/customer-groups`, `/admin/access-delegations`, 일부 보고서·서식 라우트.
- 즉 본 회귀는 **백엔드가 아니라 프론트 본문 템플릿 부재**의 문제.

---

## 2. 복구 목표

1. **leaf 본문 = (제목 + KPI 카드 + 목록/표 + 행 액션 + “+ 추가” + 필터/검색 + 빈 상태)** 의 기본 골격을 100개 leaf 전부에 즉시 제공.
2. **추가/수정/삭제(CRUD)** 가 실제 백엔드 API에 연결되어 화면이 변경된다.
3. **흐름 가시화 4종 (Stepper, LawBanner, StatePill, NextStepCard)** 가 leaf 진입 시 항상 적절히 표시된다.
4. v1.3의 디스패치 모델(`screenByLeaf` 우선)을 유지하면서, **단일 데이터 드리븐 렌더러**로 leaf 100개를 한 번에 살린다.
5. 별도 작업 없이 시연 가능한 leaf 수: **5종 통합 화면 폴백 → 100% 본문 표시**.

---

## 3. 아키텍처 — 데이터 드리븐 leaf 본문 (`leaf-template`)

### 3.1 leaf 스펙 보강
각 leaf의 명세 객체(`leafScreenSpecs[key]`)에 다음 필드를 추가한다.

```js
leafScreenSpecs["admin/cust:list"] = {
  primary: { method: "GET",  path: "/api/tenants/:tc/customers" },
  create:  { method: "POST", path: "/api/tenants/:tc/customers" },
  update:  { method: "PATCH", path: "/api/tenants/:tc/customers/:id" },
  remove:  { method: "DELETE", path: "/api/tenants/:tc/customers/:id" },
  rowKey: "customer_id",
  columns: [
    { key: "customer_code", label: "코드", width: 100 },
    { key: "customer_name", label: "고객사", grow: 2 },
    { key: "biz_reg_no",    label: "사업자번호", format: "biz" },
    { key: "industry_code", label: "업종", width: 80 },
    { key: "work_scopes",   label: "업무범위", format: "tags" },
  ],
  filters: [
    { key: "q",        label: "검색", kind: "text" },
    { key: "industry", label: "업종", kind: "code-select", group: "INDUSTRY" },
  ],
  kpis: [
    { label: "전체",       compute: (rows) => rows.length },
    { label: "활성",       compute: (rows) => rows.filter(r => r.status === "ACTIVE").length },
    { label: "중소기업",   compute: (rows) => rows.filter(r => r.is_sme).length },
  ],
  form: {
    fields: [
      { key: "customer_code", label: "고객사 코드",  kind: "text",  required: true,  pattern: "^[A-Z0-9]{3,10}$" },
      { key: "customer_name", label: "고객사명",     kind: "text",  required: true },
      { key: "biz_reg_no",    label: "사업자번호",   kind: "biz",   required: true },
      { key: "industry_code", label: "업종 코드",    kind: "code",  group: "INDUSTRY" },
      { key: "is_sme",        label: "중소기업 여부", kind: "checkbox" },
      { key: "work_scopes",   label: "업무 범위",    kind: "multi-checkbox", options: ["INFO","ADJUST","FORM","VALIDATE","APPROVE","PRINT","EFILE","POST"] },
    ],
  },
  permActions: { create: "CREATE", update: "UPDATE", remove: "DELETE" },
  emptyState: {
    title: "등록된 고객사가 없습니다",
    description: "신규 고객사를 등록해 신고 작업을 시작할 수 있습니다.",
    actionLabel: "+ 새 고객사 등록",
  },
};
```

100개 leaf 모두 이 스펙 한 객체로 표현된다. **각 leaf 모듈을 따로 작성하지 않아도, 스펙만 채우면 본문이 완성**된다.

### 3.2 통합 렌더러 `renderLeafScreen` 교체
현 placeholder 렌더러를 다음 5블록으로 확장한다.

```js
async function renderLeafScreen(env, key) {
  const spec = leafScreenSpecs[key];
  const meta = enrichMeta(env, key);
  if (failGate(env, key, spec, meta)) return;            // ctx/perm/flag empty-state

  const data = await loadPrimary(env, spec);             // GET 목록

  env.outlet.innerHTML = `
    ${renderFlowChrome(env, meta)}                       // Stepper + LawBanner + StatePill
    ${renderKpis(spec, data)}                            // 상단 카드 KPI
    ${renderFilters(spec)}                               // 검색/필터
    ${renderTable(spec, data, env)}                      // 목록 표 + 행 액션
    ${renderToolbar(spec, env)}                          // "+ 추가" + Excel 임포트
    ${renderNextStepCard(env, meta)}                     // workspace 한정
  `;

  bindFilters(env, spec);
  bindToolbar(env, spec, key);
  bindRowActions(env, spec, key, data);
}
```

5블록은 컴포넌트 경계를 유지하되, 현재 구현은 배포 파일 수를 늘리지 않기 위해 `frontend/app/screens.js`의 공통 helper로 제공한다.

### 3.6 구현 동기화

현재 구현은 파일 수를 늘리지 않고 `frontend/app/screens.js`의 공통 workbench 헬퍼(`renderLeafTemplate`, `renderLeafTableBlock`, `bindLeafTemplate`)로 5블록을 제공한다. leaf별 기본 데이터는 기존 1차 API에서 읽고, 사용자 추가/수정/삭제 보조 행은 `leaf_records` API에 저장한다.

### 3.3 추가/수정/삭제 동작

| 액션 | UI | API |
|---|---|---|
| 추가 | 툴바 “+ 추가” → `form-modal.js` → 폼 제출 | `spec.create.method spec.create.path` 호출, 성공 시 목록 재로드 |
| 수정 | 행 우측 “수정” → 동일 모달(데이터 prefill) | `spec.update.method` → `:id` 치환 후 호출 |
| 삭제 | 행 우측 “삭제” → confirm-modal | `spec.remove.method` 호출, 성공 시 행 제거 |
| 일괄 작업 | 헤더 체크박스 → 일괄 삭제(권한 시) | 다중 DELETE 또는 `/bulk` 엔드포인트 |

행 액션은 `permActions`로 제어 — 권한 없는 컬럼은 자동 숨김.

### 3.4 빈 상태(empty-state) 정상화
데이터 0건 일 때:
- `spec.emptyState.title` + `description` + `[actionLabel]` 버튼 표시.
- 버튼 클릭 = 추가 모달과 동일.
- 권한 없음/컨텍스트 부재는 v1.3 `failGate`가 분기.

### 3.5 흐름 가시화 5종 통합 (`renderFlowChrome`)

| 컴포넌트 | 표시 조건 | 데이터 |
|---|---|---|
| Stepper (1~7) | `meta.group === "workspace"` | `parentKey` → step index, `workContext.progress` |
| LawBanner | `meta.group === "workspace"` 또는 `meta.key.startsWith("post/amend")` | `workContext.snapshot` |
| StatePill | `meta.group ∈ {workspace, post/amend, post/hist}` | `workContext.status`, `lockMode` |
| GroupAnchor (사이드바) | 모든 그룹 | `menuTree` + 현재 active leaf의 `parent_key` 체인 |
| NextStepCard | `meta.group === "workspace"` 본문 하단 | `GET /api/tenants/:tc/business-years/:byId/progress` |

5개 컴포넌트는 v1.4에서 일부 도입되었지만 leaf 본문이 placeholder라 사용자가 인지하지 못했다. v1.5에서는 **본문이 풍성해진 위에 한 번 더 일관 적용**해 “업무 흐름이 보인다”는 체감을 회복한다.

---

## 4. 그룹별 leaf 스펙 작성 (100개 일괄 도입)

> 각 그룹마다 “스펙의 공통 패턴”을 선언하고, leaf는 차이점만 채운다. 100 leaf × 스펙 1줄 ≈ 약 1,000줄.

### 4.1 ② 신고 작업 (40 leaf, workspace)
- **0. 작업 시작** (`ws/start:customer-pick / by-pick / snapshot`): primary = `/customers`, `/business-years`, `/snapshot`. CRUD = 표 + “+ 새 사업연도”/“스냅샷 PDF”.
- **1. 세무정보 입력** (`ws/info:*`): primary = `tax-data/financial-statements`, `assets`, `transactions`, `vehicle-usage-logs`. CRUD = 행 편집 그리드(`components/grid.js`) + Excel 임포트.
- **2. 세무조정 17개** (`ws/adj:B1..B17`): primary = `adjustments/<family>/<code>`. 본문 = (한도 카드) + (입력 그리드) + (결과 카드: 가산/차감/유보). 추가 = 행 추가, 수정 = 인라인 편집, 삭제 = 행 삭제, 저장 = POST 일괄.
- **3. 서식** (`ws/form:*`): primary = `forms/attachments`, `forms/FORM3/preview`, `forms/:code/preview`. CRUD = (전용 4 leaf 각각 상이) — form3는 표 + “재생성”, attachments는 표 + “PDF 다운로드”.
- **4. 검증** (`ws/val:*`): primary = `validation/rules`, `validation/issues`, `POST validation/run`. CRUD = run 버튼 + 이슈 목록 + “무시”/“이동”.
- **5. 결재** (`ws/appr:*`): primary = `workflow/queue?assignee=me`, `workflow/events?status=REJECTED`. CRUD = “결재 요청”, “승인/반려”, “재작성 진입”.
- **6. 출력** (`ws/print:*`): primary = `forms/attachments`, `print/history`. CRUD = 체크박스 + “일괄 ZIP”, “단일 PDF”.
- **7. 전자신고** (`ws/file:*`): primary = `efilings/precheck`, `efilings`. CRUD = “생성/제출/접수번호 표시”.

### 4.2 ① 대시보드 (5 leaf, plain)
- 위젯형 — 행 액션 없음. KPI 카드 + 표 + “해당 leaf로 이동” 링크.

### 4.3 ③ 사후 관리 (6 leaf, plain)
- `post/hist:list`: 표 + 행 “상세”. 
- `post/amend:*`: 폼 + 차이 표 + “해제/재제출” 버튼.

### 4.4 ④ 분석/보고서 (6 leaf, plain)
- 차트 + 표. 행 액션 없음. “파라미터 변경”(범위/유형) 드롭다운 + “Excel” 다운로드.

### 4.5 ⑤ 관리 (43 leaf, admin)
- **5-0 테넌트** (`admin/tenant:list`): v1.4 도입. 검색·유형·상태 필터 + 테넌트 표 + 우측 편집 패널.
- **5-A 고객사** (`admin/cust:*`): §3.1 예시와 동일.
- **5-B 사용자/권한** 7 leaf: users/roles/matrix/menus/functions/mask/scope. 각각 표 + 폼.
- **5-C 담당 법인** 5 leaf: assign/groups/rules/delegate/override.
- **5-D 법령·세율** 10 leaf: master/rates/limits/credits/depr-lives/sme/loss-rule/snapshots/impact/history.
- **5-E 서식 버전** 10 leaf: master/versions/fields/validations/linkage-rule/field-ref/migration/efile-map/by-set/impact.
- **5-F 코드** (통합 `admin/code:manage`): 코드 그룹 트리 + 코드 표.
- **5-G 감사/로그** 4 leaf: events/login/perm/settings.

---

## 5. 산출물 매트릭스

| 분류 | 파일 | 변경 |
|---|---|---|
| 프론트 코어 | `frontend/app/screens.js` | `renderLeafScreen` 본문을 5블록 합성으로 교체. placeholder 제거 |
| 프론트 스펙 | `frontend/app/screens.js`의 `leafScreenSpecs` + 자동 컬럼 추론 | 100 leaf 스펙과 공통 fallback을 한 곳에서 관리 |
| 프론트 컴포넌트 | `frontend/app/screens.js` workbench helper + `frontend/app/components/grid.js` | 본문 5블록 + 행 액션 + 즉시 표 갱신 |
| 프론트 흐름 | `frontend/app/components/{stepper,law-banner,state-pill,next-step-card,group-anchor}.js` | v1.4 부재분 보완 + 항상 표시 |
| 프론트 그리드 | `frontend/app/components/grid.js` | ws/info, ws/adj용 인라인 편집 그리드 |
| 백엔드 라우트 보강 | `src/api.rs` | DELETE/PUT 라우트 누락분 약 12개 (v1.3 §4에서 표기) |
| 백엔드 핸들러 | `src/customers.rs`, `src/admin_*.rs`, `src/forms.rs` 등 | `update_*`, `delete_*` 추가 |
| 백엔드 컨트랙트 | DTO 응답에 `row_id`(=primary key) 필드 표준화 | `customer_id`/`asset_id`/`adj_item_id` 등 일관화 |
| DB 마이그레이션 | `20260520000200_leaf_records.sql` | leaf별 사용자 CRUD 보조 행 저장소와 검색 인덱스 |
| 시드 | `메뉴기능수정.md` 시더 | leaf별 3행 시드 보강 |
| 테스트 | `tests/leaf_crud.rs`, `tests/leaf_smoke.rs`, `tests/frontend/leaf_table.spec.js` | leaf별 CRUD 4동작 + 5블록 렌더 계약 검증 |
| 문서 | 본 문서 + `docs/leaf_screen_catalog.md` | 100 leaf 스펙 자동 catalog |

---

## 6. 데이터 컨트랙트 표준화

leaf 본문이 통합 렌더러로 만들어지려면 백엔드 응답이 일관된 형태여야 한다.

```jsonc
// GET 응답 (primary)
{
  "rows": [ { "row_id": 12, /* … */ } ],
  "total": 245,
  "page": { "limit": 20, "offset": 0 }
}

// POST 응답 (create)
{ "row": { "row_id": 246, /* … */ } }

// PATCH 응답 (update)
{ "row": { /* 갱신된 전체 행 */ } }

// DELETE 응답
{ "row_id": 12, "deleted_at": "…" }
```

표준화가 어려운 기존 라우트는 frontend `apiOptions(spec, …)`에 응답 변환 함수를 두어 어댑팅한다.

### 6.1 row_id 키 정규화
- `customer_id`, `asset_id`, `adjustment_item_id` 등 leaf마다 다른 PK를 frontend에서 `row.row_id`로 정규화.
- 백엔드 응답은 변경하지 않고 frontend에서 `spec.rowKey` 매핑으로 처리.

### 6.2 필터·검색 컨트랙트
- 검색 텍스트: `?q=…`
- 코드 필터: `?industry=…`, `?status=…`
- 페이지네이션: `?limit=20&offset=0`
- 정렬: `?sort=column.dir`

신설 라우트는 본 컨트랙트를 따른다. 기존 라우트는 점진 마이그레이션.

---

## 7. 구현 단계 (Sprint 1주 + 후속 2주)

### Sprint 1.5.A (Day 1~3) — 본문 골격 즉시 도입
- D1 오전: `renderLeafScreen` 본문 5블록 합성 교체. 100 leaf 전부에 즉시 “표 + 추가 + 빈 상태”가 나오게 됨(스펙 미작성 leaf는 자동 컬럼 추론).
- D1 오후: ⑤ 관리 5-A·5-0 포함 공통 workbench 적용.
- D2: `screens.js` workbench helper와 CSS 작성.
- D3: `leaf_records` CRUD + `row-actions` 완성, CRUD 4동작 테스트 검증.

### Sprint 1.5.B (Day 4~7) — 그룹별 스펙 채우기
- D4: 신고 작업 `ws/info` 9 leaf + `ws/adj` 17 leaf (그리드 컴포넌트 재사용)
- D5: 신고 작업 `ws/form/val/appr/print/file` 17 leaf
- D6: 사후관리 + 보고서 12 leaf
- D7: 관리 5-B/5-C/5-D/5-E/5-F/5-G 43 leaf

### Sprint 1.5.C (Day 8~14) — 흐름 가시화 + 백엔드 보강
- Stepper/LawBanner/StatePill/NextStepCard 통일 적용 + i18n 라벨 매핑.
- 백엔드 누락 라우트 12개 작성, DTO 표준화.
- `tests/leaf_crud.rs`, `tests/leaf_smoke.rs` 와 프론트 단위 테스트 작성.

---

## 8. 위험 및 대응

| 위험 | 대응 |
|---|---|
| 100 leaf 스펙 일괄 작성으로 PR 비대화 | leaf-specs를 그룹별 6 PR로 분리. 미작성 leaf는 “자동 컬럼” 폴백으로 화면이 비지 않음 |
| 응답 형태가 leaf마다 달라 통합 렌더러가 깨짐 | `apiOptions`에 어댑팅 함수, `spec.rowKey`/`spec.transform`으로 흡수 |
| 백엔드 DELETE 누락으로 행 액션이 동작하지 않음 | leaf-specs의 `remove`가 비어 있으면 행 액션 자동 숨김 + 운영 로그 기록 |
| 흐름 컴포넌트가 일부 leaf에서 부재 | `renderFlowChrome`이 그룹 기준으로 무조건 호출, 조건부 컴포넌트는 내부에서 hide |
| 그리드 인라인 편집과 모달 편집 혼재로 UX 일관성 깨짐 | 17개 모듈/세무정보는 그리드, 그 외는 모달로 명시적 분기 + leaf-specs의 `editor: "grid" \| "modal"` 필드 |

---

## 9. 회귀·테스트 계획

### 9.1 자동
- `tests/leaf_crud.rs`: 100 leaf 중 CRUD 4동작이 있는 leaf(약 60개)에 대해 시드 → 추가 → 수정 → 삭제 → 재조회 라운드트립을 검증.
- `tests/leaf_smoke.rs`: 100개 leaf 등록 + 5블록 workbench 계약 + sidebar 진행률 점 존재.
- `tests/frontend/leaf_table.spec.js`(jsdom): `renderTable`이 spec.columns 수만큼 `<th>` 렌더링.

### 9.2 수동
- 24개 핵심 leaf 시연: `admin/cust:list`에 신규 고객사 등록 → 표에 추가 → 수정 → 삭제.
- `ws/adj:B12`: 그리드에서 행 추가 → 저장 → 결과 카드(가산/차감) 갱신.
- `ws/start:by-pick`: 행 “계속” → 워크 컨텍스트 설정 → `ws/info:fs`로 진입 → Stepper 1단계 active 확인.

---

## 10. 완료 정의 (DoD)

1. 좌측 메뉴 100개 leaf 어느 것을 눌러도 본문에 **(상단 KPI/제목) + (검색/필터) + (목록 표) + (행 액션) + (+ 추가 버튼)** 5블록이 항상 나타난다. “기능 실행” 단일 버튼만 남는 leaf가 없다.
2. CRUD 4동작이 가능한 leaf(약 60개)에서 **추가/수정/삭제 후 표가 즉시 갱신**된다(reload 없이).
3. `workspace` 그룹 진입 시 **Stepper + LawBanner + StatePill + NextStepCard** 4종이 항상 보인다(컨텍스트 없으면 “작업 시작” 안내).
4. 사이드바에서 현재 그룹·leaf가 활성 표시되고, 그룹 헤더의 진행률 점이 같이 갱신된다.
5. `tests/leaf_crud.rs`·`tests/leaf_smoke.rs`·프론트 단위 테스트가 전부 green.
6. `2차구현계획.md`·`메뉴기능수정.md`·`추가구현_v1.0~v1.4.md`의 메뉴 IA·기능 정의와 본 문서가 일관된다.
