# 추가 구현 계획 v1.8

설계서/업무흐름 기준 화면 구성 정합성 복구 계획

- 작성일: 2026-05-21
- 기준 문서:
  - `법인세_세무조정계산서_시스템_설계서.md`
  - `업무흐름.md`
- 점검 대상:
  - `frontend/app/screens.js`
  - `frontend/app/i18n.js`
  - `src/modules.rs`
  - `src/api.rs`
  - `src/auth.rs`
  - `src/tax.rs`
  - `src/tax_data.rs`
  - `src/forms.rs`
  - `src/efiling.rs`
  - `src/tenant.rs`

## 1. 종합 판정

현재 구현은 메뉴/라우트/API 커버리지는 넓게 확보되어 있으나, `업무흐름.md`의 3.1~3.11 단계와 설계서의 화면 와이어프레임을 기준으로 보면 화면 구성과 세부 기능은 아직 완전 구현 상태가 아니다.

핵심 원인은 다음과 같다.

1. 활성 leaf route가 대부분 `renderLeafScreen()` 기반의 범용 화면으로 렌더링된다.
2. 설계서에 가까운 일부 전용 렌더러(`renderWorkInfo`, `renderAdjustments`, `renderForms`, `renderValidation`, `renderApproval`, `renderPrint`, `renderEfiling`, `renderPostAmend`)는 존재하지만, 실제 업무 leaf route의 주 렌더링 경로로 충분히 연결되어 있지 않다.
3. 백엔드 API는 입력/조정/서식/검증/결재/전자신고/사후관리 기능을 상당 부분 제공하지만, 프론트 화면은 API 기능을 업무 단계별 UX로 완전히 노출하지 못한다.
4. 조정 모듈 코드 체계가 문서, 백엔드, 메뉴, i18n, 프론트 사이에서 일부 불일치한다. 특히 B-5~B-10 명칭/의미가 서로 다르다.
5. 상태 전이, 잠금, 검증 오류 차단, 결재 반려 후 재작업, 전자신고 후 FILED 잠금 같은 업무 제어가 화면 단위로 일관되게 강제되지 않는다.

따라서 v1.8의 목표는 "기능 추가"보다 먼저 "문서 기준 업무 흐름 화면을 활성 라우트에 정확히 연결하고, 누락된 세부 기능을 단계적으로 채우는 것"이다.

## 2. 현재 구현 기반

이미 구현되어 v1.8에서 활용해야 할 기반은 다음과 같다.

- 업무 leaf route 100개가 `frontend/app/screens.js`의 `leafRoutes`, `leafScreenSpecs`, `screenByLeaf`를 통해 노출된다.
- 공통 업무 컨텍스트, 다음 단계 카드, 법령 배너 계열 UI가 일부 존재한다.
- 고객사/사업연도/세무 데이터/세무조정/서식/검증/결재/전자신고/사후관리 API가 Rust 백엔드에 구현되어 있다.
- 전자신고 enqueue 단계에서 OTP step-up 검증이 백엔드에 존재한다.
- 서식 미리보기, 서식 업데이트, PDF 생성, 번들 생성, 연계 데이터 조회 API가 존재한다.
- 세무 데이터 업로드, 표준 템플릿, 파일 목록, 검증 이슈 API가 존재한다.
- 조정 계산, 이력, 증빙, 차량운행기록, 유보 잔액, 세무 영향 API가 존재한다.

이 기반은 유지하고, 화면 구조와 업무 연결성을 설계서 기준으로 재정렬한다.

## 3. 업무 흐름별 점검 결과

| 업무 단계 | 문서 요구 | 현재 구현 상태 | 판정 | v1.8 조치 |
|---|---|---|---|---|
| 3.1 관리자 초기 설정 | 역할/권한, 메뉴, 표준 서식, 법령/세율, 보안 정책, 테넌트 기본값을 관리자 화면에서 설정 | 관리자 메뉴와 일부 API/화면은 있으나 대부분 범용 leaf 화면 또는 단순 리스트 중심 | 미완성 | 관리자 설정 전용 화면 묶음으로 재구성하고 역할-기능 매트릭스, 메뉴-기능 연결, 필드 마스킹, 법령/서식 버전 관리 UI 추가 |
| 3.2 로그인/인증 | ID/PW, OTP, IP 제한, 계정 잠금, 비밀번호 정책, 감사 로그 | 백엔드에 OTP/IP/잠금 기반이 있으나 로그인 API가 2FA 강제를 완전히 호출하지 않고, 프론트 로그인 화면에 OTP challenge UX가 부족 | 미완성 | 로그인 2단계 인증 플로우, IP 제한 메시지, 계정 잠금/비밀번호 만료 안내, 관리자 보안정책 화면 연결 |
| 3.3 고객사 등록/사업연도 생성 | 고객사 검색, 최근 작업, 신규 고객사/사업연도 생성, 전년도 이월, 업무 스냅샷 | `ws/start:*` 활성 leaf가 범용 화면으로 표시됨. 별도 `renderWorkStart`는 충분히 연결되지 않음 | 미완성 | 업무 시작 전용 화면을 활성 route에 연결하고 고객사 검색/최근 작업/사업연도 생성/전년도 이월 옵션/스냅샷을 구현 |
| 3.4 담당 법인 배정 | 담당자/검토자/승인자 배정, 권한 범위, 고객사 그룹, 위임/대체자 | 담당자/권한 관련 메뉴는 있으나 배정 업무 화면과 상태 흐름이 약함 | 미완성 | 고객사-사용자 배정 화면, 역할별 접근 미리보기, 기간 위임, 배정 이력, 결재선 자동 추천 기능 구현 |
| 3.5 세무정보 입력 | 표준 템플릿, ERP/엑셀 업로드, 수입금액/재무상태표/손익계산서/세무정보/차량운행기록/일관성 검증 | API는 있으나 활성 화면은 범용 목록/카드 위주. 업로드 오류 drilldown, 매핑, 차량 입력, 일관성 점프가 부족 | 부분 구현 | 입력 단계 전용 workbench, 표준 템플릿 다운로드, 업로드 오류 상세, 계정 매핑, 차량운행 편집기, 입력 데이터 일관성 jump action 구현 |
| 3.6 세무조정 | 17개 조정 모듈별 Excel-like grid, 자동 계산, 유보 관리, 서식 연계, 이력/잠금/증빙/법령 근거 | 백엔드 계산/API는 비교적 풍부하나 활성 화면은 모듈별 전문 UI가 부족. B-code 명칭도 일부 불일치 | 미완성 | B-code taxonomy 정합화 후 17개 모듈 공통 shell + 모듈별 세부 패널 구현 |
| 3.7 서식 자동 생성/검토 | 100+ 서식 목록, PDF 동일 미리보기, 자동/수동 필드 구분, 원천 데이터 jump, 서식 이력 | 서식 API는 있으나 화면은 단순 preview/update 수준. 설계서식 화면 요구보다 부족 | 부분 구현 | 서식 목록/검색/상태, WYSIWYG preview, 필드 source jump, 수동수정 사유, 서식별 검증/이력 패널 구현 |
| 3.8 검증/결재 | 오류/경고 검증, 오류 클릭 시 원천 화면 이동, ERROR=0 결재 요청, 결재/반려/재작업 | 검증/결재 API와 단순 UI 존재. 상태 차단과 반려 후 재검증 UX가 약함 | 부분 구현 | 검증 결과 triage, 원천 이동, 오류 0건 gate, 결재 요청 wizard, 반려 사유/첨부/변경 이력 통합 |
| 3.9 PDF 출력 | 제출용/보관용/검토용 PDF, 워터마크, 출력 이력, 메일/다운로드, 버전 표시 | PDF API는 있으나 화면은 단순 생성/다운로드 중심 | 부분 구현 | 출력 묶음 선택, 워터마크/용도 옵션, 미리보기, 출력 이력, 결재본 버전 고정 표시 구현 |
| 3.10 전자신고 | 사전검증, 신고파일 생성, 오류 위치 이동, OTP step-up, 제출/접수증/상태 반영 | 백엔드 precheck/generate/submit 기반은 있으나 활성 화면이 범용 렌더러로 빠질 가능성이 큼 | 부분 구현 | 4단계 전자신고 wizard를 활성 route에 연결하고 제출/접수증/FILED 잠금/오류 위치 이동 구현 |
| 3.11 신고 후 관리 | 신고 이력, 수정신고/경정청구, 버전 비교, 잠금 해제, 재검증/재결재/재제출 | API는 있으나 UI가 일반적인 사후관리 카드 수준 | 부분 구현 | 신고 이력 타임라인, 원본/수정본 diff, 수정신고 wizard, 잠금 해제 승인, 재제출 상태 흐름 구현 |

## 4. 우선 해결해야 할 구조적 불일치

### 4.1 B-code taxonomy 불일치

설계서와 업무흐름의 세무조정 모듈 기준은 다음 의미 체계로 읽힌다.

- B-1: 소득금액조정명세서
- B-2: 기부금
- B-3: 접대비
- B-4: 감가상각비
- B-5: 퇴직급여충당금/퇴직연금
- B-6: 대손충당금 및 대손금
- B-7: 외화자산·부채 평가
- B-8: 재고자산·유가증권 평가
- B-9: 지급이자 손금불산입
- B-10: 업무용승용차 관련비용
- B-11: 이월결손금
- B-12: 세액공제·감면
- B-13: 최저한세
- B-14: 가산세
- B-15: 자본금과 적립금
- B-16: 외국법인 세무조정
- B-17: 연결납세

백엔드 `src/tax.rs`는 대체로 이 흐름에 맞지만, `src/modules.rs`, `frontend/app/i18n.js`, `frontend/app/screens.js`의 일부 명칭은 다르다. 예를 들어 B-5가 인정이자로, B-9가 재고평가로, B-10 영문명이 business transfer difference로 표시되는 등 혼선이 있다.

v1.8에서 가장 먼저 이 taxonomy를 단일 source of truth로 정리해야 한다.

필수 조치:

- `src/modules.rs`의 메뉴/모듈 정의를 설계서 기준으로 수정
- `frontend/app/screens.js`의 `adjustmentModules` 정의 수정
- `frontend/app/i18n.js`의 한국어/영어 라벨 수정
- 백엔드 API route, request/response type, 프론트 화면 라벨의 B-code 일치 테스트 추가
- 기존 데이터 마이그레이션이 필요한 code/name 변경 여부 검토

### 4.2 활성 라우트와 전용 렌더러 불일치

현재 active leaf route는 `screenByLeaf`와 `renderLeafScreen()`을 중심으로 동작한다. 이 구조는 많은 화면을 빠르게 노출하기에는 좋지만, 설계서의 업무 단계별 상세 화면을 구현하기에는 부족하다.

필수 조치:

- leaf route별 renderer contract를 명시한다.
- 업무 단계 화면은 generic renderer가 아니라 stage-specific renderer를 우선 사용한다.
- 기존 전용 렌더러를 단순 보관 코드로 두지 말고 활성 route에 연결한다.
- `screens.js`가 지나치게 비대해졌으므로 업무 영역별 모듈 분리를 검토한다.

권장 파일 구조:

```text
frontend/app/screens.js
frontend/app/screens/registry.js
frontend/app/screens/work-start.js
frontend/app/screens/tax-data.js
frontend/app/screens/adjustments.js
frontend/app/screens/forms.js
frontend/app/screens/validation.js
frontend/app/screens/approval.js
frontend/app/screens/print.js
frontend/app/screens/efiling.js
frontend/app/screens/post-filing.js
frontend/app/screens/admin.js
```

### 4.3 상태/잠금/gate 일관성 부족

설계서의 업무는 단순 메뉴 이동이 아니라 상태 전이가 핵심이다.

필수 gate:

- 입력 데이터 필수 항목 누락 시 조정 단계 경고
- 조정 계산 미완료 또는 검증 ERROR 존재 시 결재 요청 차단
- 결재 완료 전 제출용 PDF/전자신고 차단
- 전자신고 제출 완료 후 FILED 잠금
- 수정신고/경정청구 시 별도 버전 생성 및 원본 보존

v1.8에서는 각 stage renderer가 공통 `work_status`를 읽어 CTA 활성/비활성, 잠금 배너, 다음 단계 안내를 일관되게 표시해야 한다.

## 5. 단계별 구현 계획

### Phase 0. 기준 계약 정리 및 안전망 구축

목표: 설계서 기준과 코드 기준을 먼저 고정한다.

작업:

1. `업무흐름.md` 3.1~3.11을 기준으로 workflow stage enum을 정의한다.
2. B-1~B-17 조정 모듈 taxonomy를 단일 테이블로 확정한다.
3. route key, backend module code, i18n key, 화면 title/subtitle의 매핑표를 만든다.
4. 활성 leaf route가 어떤 renderer를 사용해야 하는지 contract를 작성한다.
5. generic renderer 허용 화면과 전용 renderer 필수 화면을 구분한다.

대상 파일:

- `frontend/app/screens.js`
- `frontend/app/i18n.js`
- `src/modules.rs`
- `src/tax.rs`

검증:

- B-code 매핑 snapshot 테스트
- leaf route별 renderer contract 테스트
- i18n 누락 key 테스트

완료 기준:

- B-1~B-17 명칭이 문서/백엔드/프론트/i18n에서 동일하다.
- 업무 단계 핵심 route가 generic renderer로 빠지지 않는다.

### Phase 1. 업무 화면 라우팅 구조 재정렬

목표: 실제 사용자가 보는 활성 화면을 설계서 화면 구조에 맞춘다.

작업:

1. `screenByLeaf` 생성 로직에 `renderer` 또는 `screenKind`를 명시한다.
2. 업무 단계별 route group을 분리한다.
3. 기존 전용 렌더러를 활성 route에 연결한다.
4. 공통 업무 header를 설계서 기준으로 통일한다.
   - 고객사
   - 사업연도
   - 신고구분
   - 현재 상태
   - 법령/서식 버전
   - 잠금 상태
   - 다음 단계 CTA
5. generic leaf 화면은 관리자 보조 화면, 단순 로그/목록 화면 등으로 제한한다.

대상 파일:

- `frontend/app/screens.js`
- 신규 `frontend/app/screens/*.js`
- `frontend/app/app.js` 또는 route dispatch 계층

검증:

- 주요 leaf route smoke render
- 각 workflow stage route가 stage renderer를 사용하는지 테스트

완료 기준:

- 3.3~3.11 핵심 업무 route는 모두 전용 renderer를 사용한다.
- 화면 상단 context/header 구성이 설계서와 일치한다.

### Phase 2. 로그인/인증 및 업무 시작 화면

목표: 설계서의 로그인, 고객사 선택, 사업연도 생성 화면을 실제 UX로 구현한다.

작업:

1. 로그인 API에서 사용자별 2FA 강제 여부를 확인하도록 보완한다.
2. 프론트 로그인 화면에 OTP challenge/input 상태를 추가한다.
3. IP 제한, 계정 잠금, 비밀번호 만료, 비밀번호 변경 필요 메시지를 분기 처리한다.
4. 업무 시작 화면을 활성 route에 연결한다.
5. 고객사 검색, 최근 작업, 신규 고객사 등록, 신규 사업연도 생성, 전년도 이월 선택을 구현한다.
6. 생성 전 스냅샷 preview와 생성 후 업무 context 진입을 구현한다.

대상 파일:

- `src/auth.rs`
- `src/api.rs`
- `frontend/app/screens.js`
- `frontend/app/i18n.js`

검증:

- OTP required 사용자의 로그인 플로우 테스트
- OTP optional 사용자의 기존 로그인 플로우 회귀 테스트
- 고객사/사업연도 생성 화면 smoke 테스트

완료 기준:

- 로그인 화면이 설계서의 인증 흐름을 만족한다.
- 업무 시작 route가 고객사/사업연도 생성까지 연결된다.

### Phase 3. 세무정보 입력 workbench

목표: 설계서의 세무정보 입력 화면을 범용 카드가 아닌 입력 전용 workbench로 구현한다.

작업:

1. 표준 템플릿 다운로드 영역 구현
2. ERP/엑셀 업로드 영역 구현
3. 업로드 결과 요약 및 오류 상세 drilldown 구현
4. 재무상태표/손익계산서/수입금액/세무정보/차량운행기록 탭 구현
5. 계정 매핑 및 mapping rule 저장 UI 구현
6. 차량운행기록 편집 grid 구현
7. 입력 일관성 검증 결과에서 원천 row로 이동하는 jump action 구현
8. 입력 완료 상태 전환 CTA 구현

대상 API:

- 세무 데이터 업로드
- 템플릿 다운로드
- 파일 목록
- 파일 검증 이슈
- 차량운행기록 관련 tax API

검증:

- 템플릿 다운로드 버튼 표시/동작
- 업로드 성공/실패 상태 렌더링
- validation issue 클릭 시 관련 입력 탭 이동

완료 기준:

- 3.5 단계의 주요 입력 화면과 세부 기능을 한 화면 흐름에서 수행할 수 있다.

### Phase 4. 세무조정 17개 모듈 workbench

목표: B-1~B-17 조정 모듈을 설계서 기준으로 실제 업무 가능한 화면으로 만든다.

공통 shell:

- 모듈 설명/상태/담당자
- 법령 버전 배너
- 계산 기준/산식/근거
- Excel-like 입력 grid
- 원천 데이터 import
- 자동 계산 실행
- 유보/소득처분/세무 영향 요약
- 서식 연계 상태
- 검증 오류 jump
- 이력/메모/첨부
- 잠금/해제 상태

모듈별 필수 보강:

| 모듈 | v1.8 화면 보강 |
|---|---|
| B-1 접대비 | 계정별 집계, 한도 계산, 초과액 소득처분, FORM3 연계 |
| B-2 감가상각 | 자산별 상각 grid, 회사상각/세법상각 비교, 즉시상각/한도초과 |
| B-3 업무무관/가지급금 | 업무무관 자산, 인정이자, 지급이자 손금불산입 연결 |
| B-4 대손충당금 | 채권별 설정률, 한도, 초과/부족, 유보 추적 |
| B-5 퇴직급여충당금 | 퇴직급여/충당금 한도, 세무상 부인/환입 |
| B-6 대손금 | 대손 사유, 증빙, 손금 인정 여부, 세무조정 |
| B-7 외화평가 | 외화자산/부채, 환율, 평가손익, 세무인정 여부 |
| B-8 재고/유가증권 평가 | 평가방법, 평가손익, 세무조정, 명세서 연계 |
| B-9 지급이자 | 차입금, 업무무관 관련 지급이자, 한도/배제 계산 |
| B-10 업무용승용차 | 차량별 비용, 운행기록, 한도, 손금불산입 |
| B-11 세액공제/감면 | 공제/감면 항목, 한도, 중복/최저한세 영향 |
| B-12 이월결손금 | 발생연도별 잔액, 공제한도, 소멸/사용 추적 |
| B-13 기부금 | 법정/지정 구분, 한도, 이월공제 |
| B-14 자본금과 적립금 | 자본금/잉여금/세무상 자본 변동, 별지 연계 |
| B-15 소득금액조정합계표 | 전체 조정 항목 집계, 소득처분, FORM3 중심 연계 |
| B-16 과세표준 및 세액조정 | 과세표준, 산출세액, 공제감면, 최종세액 |
| B-17 가산세 | 가산세 유형, 산식, 감면/면제, 납부세액 반영 |

대상 파일:

- `frontend/app/screens/adjustments.js`
- `frontend/app/screens.js`
- `frontend/app/i18n.js`
- `src/modules.rs`
- 필요 시 `src/tax.rs`

검증:

- B-1~B-17 route별 title/module code/API endpoint 매칭
- 계산 실행 버튼 smoke 테스트
- 유보/서식 연계/검증 jump 영역 표시 테스트

완료 기준:

- 모든 조정 모듈이 설계서의 공통 화면 요소를 가진다.
- 모듈별 핵심 입력/계산/연계 요소가 누락되지 않는다.

### Phase 5. 서식 자동 생성 및 검토

목표: 서식 화면을 단순 미리보기에서 "자동 생성/검토/수정/연계 추적" 화면으로 확장한다.

작업:

1. 서식 목록을 신고서/명세서/첨부서류/지방세 등으로 분류한다.
2. 서식 상태를 표시한다.
   - 미생성
   - 자동생성
   - 수동수정
   - 검증오류
   - 승인완료
3. PDF 동일 수준의 미리보기 패널을 구현한다.
4. 자동 산출 필드와 수동 수정 필드를 시각적으로 구분한다.
5. 필드 클릭 시 원천 조정/입력 데이터로 이동한다.
6. 수동 수정 시 사유 입력과 변경 이력을 남긴다.
7. 서식별 validation issue와 바로가기 버튼을 제공한다.

대상 API:

- form preview
- form update
- form history
- form linkage
- pdf generation

검증:

- 자동/수동 필드 표시 테스트
- 필드 source jump 테스트
- 수동 수정 사유 저장 테스트

완료 기준:

- 3.7 단계에서 서식 자동 생성, 검토, 수동 보정, 원천 추적이 가능하다.

### Phase 6. 검증 및 결재

목표: 검증-결재를 업무 상태 gate로 구현한다.

작업:

1. 검증 실행 화면을 오류/경고/정보 severity별로 정리한다.
2. 오류 클릭 시 입력/조정/서식 원천 화면으로 이동한다.
3. ERROR가 1건 이상이면 결재 요청 CTA를 비활성화한다.
4. 검증 통과 시 결재 요청 wizard를 표시한다.
5. 결재선 선택/자동 추천/검토자 코멘트/첨부를 구현한다.
6. 승인/반려 액션을 분리한다.
7. 반려 시 반려 사유, 관련 화면 jump, 재검증 요구를 표시한다.
8. 결재 완료 후 서식/PDF/전자신고 단계 잠금을 갱신한다.

대상 API:

- validation run
- validation issue list
- workflow status
- approval lines
- approval events

검증:

- ERROR 존재 시 결재 요청 차단
- 반려 후 상태가 재작업으로 돌아가는지 확인
- 승인 완료 후 다음 단계 CTA 활성화

완료 기준:

- 검증/결재가 단순 버튼이 아니라 업무 상태 전이로 동작한다.

### Phase 7. PDF 출력 및 전자신고

목표: 출력/전자신고 단계를 제출 가능한 업무 화면으로 완성한다.

PDF 출력 작업:

1. 제출용/보관용/검토용 출력 유형 선택
2. 출력할 서식 묶음 선택
3. 워터마크/버전/승인자/출력일 표시
4. PDF preview와 다운로드
5. 출력 이력 저장 및 재다운로드

전자신고 작업:

1. 사전검증 단계
2. 신고파일 생성 단계
3. 제출 전 확인 및 OTP step-up
4. 제출/접수증 수신
5. 오류 발생 시 필드/서식 위치 이동
6. 제출 완료 시 FILED 상태와 잠금 적용

대상 API:

- pdf generation
- form bundle
- efiling precheck
- efiling generate
- efiling submit
- efiling download
- efiling receipt/status

검증:

- 결재 미완료 상태에서 제출용 PDF/전자신고 차단
- OTP required 사용자의 전자신고 제출 테스트
- 제출 완료 후 FILED 잠금 표시

완료 기준:

- 3.9와 3.10 단계가 설계서의 출력/전자신고 흐름을 따른다.

### Phase 8. 신고 후 관리

목표: 신고 이력과 수정신고/경정청구를 별도 버전 관리 업무로 구현한다.

작업:

1. 신고 이력 timeline 구현
2. 원신고/수정신고/경정청구 버전 구분
3. 원본 잠금 상태와 수정본 작업본 생성
4. 변경 diff report 표시
5. 잠금 해제 요청/승인
6. 수정본 재검증/재결재/재출력/재전자신고 흐름 연결
7. 접수증과 신고파일 보관함 구현

대상 API:

- filed return history
- amendment preview
- amendment version
- unlock request
- resubmission

검증:

- 원본 버전이 직접 수정되지 않는지 확인
- 수정본 생성 후 별도 workflow 상태를 가지는지 확인
- diff report가 변경 필드를 표시하는지 확인

완료 기준:

- 3.11 단계가 단순 파일 목록이 아니라 신고 후 업무 흐름으로 동작한다.

### Phase 9. 관리자 설정 화면

목표: 3.1 관리자 초기 설정을 실제 운영 가능한 관리 화면으로 만든다.

작업:

1. 역할-기능 매트릭스 화면 구현
2. 메뉴별 권한/노출 설정 화면 구현
3. 고객사 그룹/담당자 배정 규칙 구현
4. 결재선 템플릿 및 자동 추천 규칙 구현
5. 보안 정책 화면 구현
   - 2FA 필수 여부
   - IP allowlist
   - 비밀번호 정책
   - 세션 정책
   - 필드 masking
6. 법령/세율/서식 버전 관리 화면 구현
7. 법령/서식 변경 영향도 및 승인 workflow 구현
8. 데이터 migration 관리 화면 구현

대상 파일:

- `frontend/app/screens/admin.js`
- `frontend/app/i18n.js`
- `src/tenant.rs`
- `src/modules.rs`
- 필요 시 admin API 확장

검증:

- 권한 없는 사용자의 메뉴 숨김/접근 차단
- 보안 정책 변경 후 로그인/전자신고 반영
- 법령 버전 변경 후 업무 화면 배너 반영

완료 기준:

- 관리자 초기 설정만으로 업무 흐름이 시작 가능한 수준의 기본 설정을 구성할 수 있다.

### Phase 10. 테스트 및 회귀 검증

목표: 문서 요구와 실제 화면이 다시 벌어지지 않도록 자동화한다.

추가할 테스트:

```text
tests/frontend/workflow_contract.spec.js
tests/frontend/workflow_stage_renderers.spec.js
tests/frontend/adjustment_taxonomy.spec.js
tests/frontend/auth_2fa.spec.js
tests/frontend/tax_data_workbench.spec.js
tests/frontend/forms_review.spec.js
tests/frontend/validation_approval_gate.spec.js
tests/frontend/efiling_wizard.spec.js
tests/frontend/post_filing.spec.js
```

검증 범위:

- 업무 단계 route가 전용 renderer를 사용하는지
- B-code 명칭/순서/API 매핑이 일치하는지
- 주요 CTA가 상태에 따라 활성/비활성되는지
- 법령/서식 버전 배너가 모든 핵심 업무 화면에 표시되는지
- 검증 오류 클릭 시 원천 화면 이동 정보가 존재하는지
- 결재 완료 전 전자신고 제출이 불가능한지
- 전자신고 완료 후 FILED 잠금이 표시되는지
- i18n 누락 key가 없는지

수동 QA:

1. 관리자 초기 설정
2. 로그인/OTP
3. 고객사/사업연도 생성
4. 세무정보 업로드
5. B-1/B-2/B-10/B-15/B-16 대표 조정 계산
6. 서식 자동 생성/수동 수정
7. 검증 오류 수정
8. 결재 요청/승인/반려
9. PDF 출력
10. 전자신고 생성/제출
11. 수정신고 버전 생성

완료 기준:

- 신규 frontend 테스트 통과
- 기존 Rust 테스트 통과
- 주요 업무 흐름 browser smoke 통과
- 문서 기준 체크리스트 전 항목 PASS

## 6. v1.8 구현 우선순위

### P0. 즉시 수정

1. B-code taxonomy 불일치 수정
2. 활성 route와 전용 renderer 연결
3. 로그인 2FA 강제 흐름 보완
4. 업무 상태/gate 공통 모델 정의

### P1. 핵심 업무 화면

1. 업무 시작
2. 세무정보 입력
3. 세무조정 17개 모듈 shell
4. 서식 검토
5. 검증/결재

### P2. 제출/사후관리

1. PDF 출력
2. 전자신고 wizard
3. 신고 후 관리
4. 수정신고/경정청구

### P3. 운영 완성도

1. 관리자 설정
2. 권한/보안 정책
3. 법령/서식 버전 관리
4. 테스트 자동화 확대

## 7. 화면별 완료 정의

각 화면은 다음 기준을 모두 만족해야 완료로 본다.

1. 설계서/업무흐름의 해당 단계 목적이 화면에서 바로 드러난다.
2. 고객사, 사업연도, 신고구분, 상태, 법령/서식 버전이 표시된다.
3. 단계별 핵심 입력/조회/계산/검증/승인 액션이 노출된다.
4. 다음 단계로 이동하기 위한 조건과 차단 사유가 명확히 표시된다.
5. 백엔드 API의 실제 결과를 사용한다.
6. 오류/경고/잠금/권한 없음 상태가 별도 UI로 처리된다.
7. i18n key 기반으로 한국어/영어 전환 시 누락 문구가 없다.
8. 최소 1개 이상의 frontend smoke 또는 contract 테스트가 있다.

## 8. 예상 작업 순서

1. 문서 기준 route/stage/module 매핑표 확정
2. B-code taxonomy 수정 및 테스트 추가
3. `screens.js` renderer dispatch 구조 정리
4. 업무 시작/입력/조정/서식/검증/결재 전용 화면 활성화
5. 출력/전자신고/사후관리 wizard 구현
6. 관리자 설정 화면 보강
7. 상태 gate/잠금/권한 제어 전 화면 적용
8. frontend 테스트와 browser smoke 보강
9. 문서 기준 체크리스트 재점검

## 9. 리스크와 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| B-code 변경으로 기존 저장 데이터와 충돌 | 기존 샘플/테스트 데이터가 깨질 수 있음 | code는 유지하되 label/meaning 정정 우선, 필요 시 migration 별도 작성 |
| `screens.js` 대형 파일 분리 중 회귀 | route dispatch 누락 가능 | 먼저 contract 테스트 작성 후 단계별 분리 |
| API는 있으나 프론트에서 기대하는 응답 shape가 다름 | 화면 구현 중 adapter 증가 | stage별 data adapter 계층을 얇게 둠 |
| 전자신고/결재 gate가 기존 demo 흐름을 막음 | 개발 smoke가 불편해질 수 있음 | demo fixture에 상태별 work item 제공 |
| 관리자/업무 화면 동시 수정 범위 과대 | 일정 증가 | P0/P1 업무 흐름 우선, 관리자 고도화는 P3로 분리 |

## 10. v1.8 최종 산출물

v1.8 완료 시 다음 산출물이 있어야 한다.

1. 설계서 기준 업무 단계별 전용 화면
2. B-1~B-17 조정 모듈 taxonomy 정합화
3. 세무정보 입력 workbench
4. 세무조정 17개 모듈 workbench shell 및 핵심 기능
5. 서식 자동 생성/검토 화면
6. 검증/결재 gate 화면
7. PDF 출력 화면
8. 전자신고 wizard
9. 신고 후 관리/수정신고 화면
10. 관리자 초기 설정 화면
11. workflow contract/frontend smoke 테스트

## 11. 결론

현재 시스템은 업무 전체를 담을 수 있는 API와 메뉴 골격은 갖췄지만, 설계서와 업무흐름에서 요구한 실제 화면 구성은 아직 범용 렌더러 수준에 머무는 구간이 많다. v1.8은 신규 기능을 무작정 추가하기보다, 활성 route를 문서 기준 전용 화면으로 재연결하고 B-code/상태/gate/권한/i18n을 정합화하는 작업을 우선해야 한다.

이 계획대로 진행하면 사용자는 관리자 설정부터 로그인, 고객사/사업연도 생성, 세무정보 입력, 세무조정, 서식 검토, 검증/결재, PDF 출력, 전자신고, 신고 후 관리까지 설계서의 업무 흐름 그대로 화면에서 수행할 수 있다.
