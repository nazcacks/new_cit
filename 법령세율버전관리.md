# 법령·세율 버전 관리 모듈 상세 설계

## 1. 개요

법령·세율 버전 관리 모듈(Tax Law Versioning)은 법인세 세무조정계산서 시스템에서 사업연도별로 적용되는 세법, 세율, 한도, 공제율, 신고 서식, 전자신고 마스터를 시점별로 관리하는 핵심 공통 모듈이다.

세법은 매년 개정되고, 같은 거래라도 사업연도와 시행일에 따라 계산 결과가 달라진다. 따라서 모든 계산 엔진과 서식 생성 기능은 현재 날짜가 아니라 해당 사업연도의 적용 법령 스냅샷을 기준으로 동작해야 한다.

## 2. 목표

| 구분 | 목표 |
| --- | --- |
| 시점별 적용 | 사업연도 시작일·종료일과 법령 시행일을 기준으로 적용 버전을 자동 선택한다. |
| 데이터 기반 계산 | 세율, 한도, 공제율, 내용연수, 결손금 규칙은 코드에 하드코딩하지 않고 버전 데이터로 관리한다. |
| 신고 재현성 | 신고 완료된 사업연도는 당시 적용 법령 스냅샷을 잠금 처리하여 사후 개정 영향을 차단한다. |
| 감사 가능성 | 계산 결과, 서식 데이터, 전자신고 파일이 사용한 법령 버전과 세부 파라미터를 추적한다. |
| 승인 통제 | 법령 개정 데이터는 등록, 검토, 승인, 활성화 절차를 거쳐 운영 반영한다. |
| 영향 분석 | 새 법령 반영 전 기존 미신고 사업연도와 작업 중 데이터에 대한 차이와 영향 범위를 산출한다. |

## 3. 범위

### 3.1 포함 기능

- 법령 버전 마스터 등록·수정·검토·승인·활성화·사용중단
- 법인세율표 시점별 관리
- 한도·율표 관리
  - 접대비
  - 기부금
  - 대손충당금
  - 업무용승용차
  - 퇴직급여충당금
  - 가산세
- 세액공제·감면 율표 관리
- 감가상각 기준내용연수표 관리
- 중소기업 판정기준 관리
- 이월결손금 공제기간·공제한도 관리
- 사업연도별 적용 법령 스냅샷 생성·조회·재계산·잠금
- 법령 개정 영향 시뮬레이션
- 법령 변경 공지와 감사 이력
- 계산 엔진, 서식 생성, 전자신고 모듈에 적용 버전 제공

### 3.2 제외 기능

- 실제 법령 원문 전문 관리 시스템
- 외부 법령 API 자동 크롤링
- 전자신고 서식 버전의 전체 필드 매핑 상세 설계

단, 전자신고와 서식 버전은 사업연도 스냅샷에 함께 저장되어야 하므로 이 모듈의 스냅샷 데이터 모델에는 포함한다.

## 4. 메뉴 구조

```text
법령·세율 버전 관리
├── 법령 버전 마스터
├── 법인세율표
├── 한도·율표
├── 세액공제·감면 율표
├── 기준내용연수표
├── 중소기업 판정기준
├── 결손금 공제규정
├── 사업연도별 적용 스냅샷
├── 영향 시뮬레이션
└── 개정 공지/이력
```

## 5. 핵심 설계 원칙

### 5.1 Temporal Validity

모든 법령 파라미터는 다음 기간 컬럼을 가진다.

- `effective_from`: 적용 시작일
- `effective_to`: 적용 종료일. 현행 버전은 `NULL`
- `basis_date_rule`: 사업연도 종료일, 개시일, 시행일 안분 등 적용 기준

동일한 `item_code`와 동일한 법령 분류 안에서는 같은 기간에 활성 버전이 중복될 수 없다.

### 5.2 Immutable Snapshot

사업연도에 한 번 적용된 법령 세트는 `by_law_snapshot`으로 저장한다. 신고 완료 또는 사용자가 잠금 처리한 스냅샷은 수정하지 않는다.

수정신고와 경정청구는 원 신고 당시 스냅샷을 기본으로 사용한다. 법령 적용 오류가 확인되어 재산정이 필요한 경우에는 새 스냅샷을 만들고 이전 스냅샷과 연결 이력을 남긴다.

### 5.3 Code/Data Separation

계산 엔진은 다음 역할만 담당한다.

- 사업연도와 항목 코드로 적용 파라미터 조회
- 금액 계산 공식 실행
- 사용된 파라미터와 법령 근거를 계산 결과에 기록

세율 구간, 한도 금액, 공제율, 공제 기간 등은 DB 데이터로 관리한다.

### 5.4 Traceability

모든 계산 결과는 다음 식별자를 저장한다.

- `snapshot_id`
- `law_version_id`
- 사용한 `tax_rate_id` 또는 `tax_limit_id`
- 항목별 `item_code`
- 계산 근거 JSON

## 6. 상태 모델

### 6.1 법령 버전 상태

| 상태 | 설명 | 수정 가능 | 계산 적용 |
| --- | --- | --- | --- |
| `DRAFT` | 등록 중 | 가능 | 불가 |
| `REVIEWED` | 검토 완료 | 제한적 가능 | 불가 |
| `APPROVED` | 승인 완료 | 불가 | 가능 |
| `ACTIVE` | 운영 반영 | 불가 | 가능 |
| `RETIRED` | 사용 중단 | 불가 | 기존 스냅샷만 가능 |
| `REJECTED` | 반려 | 불가 | 불가 |

현재 코드에는 `APPROVED` 중심으로 동작하는 기본 구현이 있다. 상세 설계 기준으로는 운영 반영 상태를 `ACTIVE`까지 확장하되, 기존 호환을 위해 `APPROVED`도 계산 적용 가능 상태로 본다.

### 6.2 전이 규칙

```text
DRAFT -> REVIEWED -> APPROVED -> ACTIVE -> RETIRED
      └────────────> REJECTED
```

- `DRAFT`만 상세 항목 수정이 가능하다.
- `REVIEWED` 이후에는 검토자 또는 승인자가 반려할 수 있다.
- `ACTIVE` 전환 시 기간 중복 검증, 영향 시뮬레이션, 감사 로그 기록을 필수 수행한다.
- `RETIRED`는 신규 스냅샷 선택 대상에서 제외하지만, 기존 잠금 스냅샷의 재현에는 계속 사용된다.

## 7. 데이터 모델

### 7.1 주요 엔티티

| 엔티티 | 설명 |
| --- | --- |
| `tax_law_versions` | 법령 버전 마스터 |
| `tax_rates` | 법인세율, 최저한세율, 농어촌특별세율, 가산세율 등 구간 세율 |
| `tax_limits` | 접대비, 기부금, 대손충당금, 업무용승용차 등 한도·율 파라미터 |
| `tax_credit_rules` | 세액공제·감면 항목별 요건과 공제율 |
| `depreciation_lives` | 자산 분류별 기준내용연수 |
| `sme_criteria_versions` | 중소기업 판정기준 |
| `loss_carryforward_rules` | 이월결손금 공제기간과 공제한도 |
| `by_law_snapshot` | 사업연도별 적용 법령 스냅샷 |
| `law_amendment_history` | 개정 이력과 승인 이력 |
| `law_impact_runs` | 영향 시뮬레이션 실행 이력 |
| `law_notifications` | 사용자 공지 발송 이력 |

### 7.2 `tax_law_versions`

| 컬럼 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| `law_version_id` | BIGSERIAL | Y | PK |
| `version_code` | VARCHAR(50) | Y | 예: `CIT-2026-01` |
| `law_name` | VARCHAR(200) | Y | 예: 법인세법 2026 개정 |
| `law_category` | VARCHAR(50) | Y | `CORPORATE_TAX`, `SPECIAL_TAX`, `LOCAL_TAX` |
| `amendment_no` | VARCHAR(80) | N | 법률 번호, 시행령 번호 |
| `promulgated_at` | DATE | N | 공포일 |
| `effective_from` | DATE | Y | 시행일 |
| `effective_to` | DATE | N | 종료일 |
| `basis_date_rule` | VARCHAR(50) | Y | `BY_END_DATE`, `BY_START_DATE`, `PRORATE_BY_EFFECTIVE_DATE` |
| `status` | VARCHAR(20) | Y | 상태 |
| `metadata` | JSONB | Y | 근거 법령, 설명, 첨부 링크 |
| `created_by` | BIGINT | Y | 등록자 |
| `reviewed_by` | BIGINT | N | 검토자 |
| `approved_by` | BIGINT | N | 승인자 |
| `created_at` | TIMESTAMPTZ | Y | 등록일시 |
| `approved_at` | TIMESTAMPTZ | N | 승인일시 |

### 7.3 `tax_rates`

법인세율처럼 과세표준 구간별로 산출세액을 계산하는 항목에 사용한다.

| 컬럼 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| `tax_rate_id` | BIGSERIAL | Y | PK |
| `law_version_id` | BIGINT | Y | 법령 버전 |
| `item_code` | VARCHAR(50) | Y | `CORP_TAX`, `MIN_TAX`, `PENALTY_LATE_PAY` |
| `taxpayer_type` | VARCHAR(50) | N | `GENERAL`, `SME`, `NON_PROFIT` |
| `taxable_from` | BIGINT | Y | 과세표준 하한 |
| `taxable_to` | BIGINT | N | 과세표준 상한 |
| `base_tax` | BIGINT | Y | 기본 산출세액 |
| `rate_bps` | INT | Y | 세율 basis point. 10% = 1000 |
| `progressive_deduction` | BIGINT | Y | 누진공제 |
| `effective_from` | DATE | Y | 적용 시작일 |
| `effective_to` | DATE | N | 적용 종료일 |
| `legal_basis` | VARCHAR(300) | N | 법 조항 |
| `metadata` | JSONB | Y | 비고 |

### 7.4 `tax_limits`

한도 금액, 한도율, 월할 계산, 업종별 예외 등 범용 파라미터를 관리한다.

| 컬럼 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| `tax_limit_id` | BIGSERIAL | Y | PK |
| `law_version_id` | BIGINT | Y | 법령 버전 |
| `item_code` | VARCHAR(50) | Y | `ENT_BASE_LIMIT`, `DONATION_STATUTORY_RATE` |
| `limit_group` | VARCHAR(50) | Y | `ENTERTAINMENT`, `DONATION`, `BAD_DEBT`, `BUSINESS_CAR` |
| `amount` | BIGINT | N | 금액 한도 |
| `rate_bps` | INT | N | 한도율 |
| `condition_json` | JSONB | Y | 적용 조건. 중소기업 여부, 수입금액 구간 등 |
| `formula_json` | JSONB | Y | 계산식 메타데이터 |
| `effective_from` | DATE | Y | 적용 시작일 |
| `effective_to` | DATE | N | 적용 종료일 |
| `legal_basis` | VARCHAR(300) | N | 법 조항 |
| `metadata` | JSONB | Y | 비고 |

예시:

```json
{
  "item_code": "ENT_BASE_LIMIT_SME",
  "limit_group": "ENTERTAINMENT",
  "amount": 36000000,
  "condition_json": {
    "is_sme": true
  },
  "formula_json": {
    "method": "monthly_proration",
    "months_source": "business_year.months"
  }
}
```

### 7.5 `tax_credit_rules`

| 컬럼 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| `credit_rule_id` | BIGSERIAL | Y | PK |
| `law_version_id` | BIGINT | Y | 법령 버전 |
| `credit_code` | VARCHAR(50) | Y | `RND`, `INVESTMENT`, `FOREIGN_TAX` |
| `credit_name` | VARCHAR(200) | Y | 공제·감면명 |
| `rate_bps` | INT | N | 공제율 |
| `max_amount` | BIGINT | N | 최대 공제금액 |
| `carryforward_years` | INT | N | 이월 가능 연수 |
| `condition_json` | JSONB | Y | 업종, 기업규모, 투자유형 등 |
| `formula_json` | JSONB | Y | 산식 |
| `effective_from` | DATE | Y | 적용 시작일 |
| `effective_to` | DATE | N | 적용 종료일 |
| `legal_basis` | VARCHAR(300) | N | 법 조항 |

### 7.6 `by_law_snapshot`

테넌트 스키마별 사업연도에 생성되는 적용 법령 묶음이다.

| 컬럼 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| `snapshot_id` | BIGSERIAL | Y | PK |
| `by_id` | BIGINT | Y | 사업연도 |
| `law_version_id` | BIGINT | Y | 대표 법령 버전 |
| `rate_version_ids` | JSONB | Y | 적용 세율 ID 목록 |
| `limit_version_ids` | JSONB | Y | 적용 한도 ID 목록 |
| `credit_rule_ids` | JSONB | Y | 적용 공제규칙 ID 목록 |
| `form_version_ids` | JSONB | Y | 적용 서식 버전 목록 |
| `efile_master_ids` | JSONB | Y | 적용 전자신고 마스터 목록 |
| `snapshot_data` | JSONB | Y | 전체 적용 근거와 파라미터 |
| `locked` | BOOLEAN | Y | 신고 완료 후 잠금 |
| `locked_at` | TIMESTAMPTZ | N | 잠금 시각 |
| `locked_by` | BIGINT | N | 잠금 사용자 |
| `created_at` | TIMESTAMPTZ | Y | 생성 시각 |

## 8. 적용 버전 선택 규칙

### 8.1 기본 알고리즘

```text
입력: by_id, item_code, module_code

1. 사업연도 조회
   - start_date
   - end_date
   - filed/locked 상태

2. 기존 by_law_snapshot 조회
   - 존재하고 locked = true 이면 그대로 반환
   - 존재하고 미잠금이면 최신 법령 기준 재계산 가능 여부 판단

3. 후보 법령 버전 조회
   - status in ('APPROVED', 'ACTIVE')
   - effective_from <= basis_date
   - effective_to is null or effective_to >= basis_date

4. basis_date_rule 적용
   - BY_END_DATE: 사업연도 종료일 기준
   - BY_START_DATE: 사업연도 개시일 기준
   - PRORATE_BY_EFFECTIVE_DATE: 시행일 전후 기간 안분

5. 항목별 파라미터 선택
   - item_code
   - taxpayer_type
   - industry_code
   - is_sme
   - revenue bracket
   - asset category

6. snapshot_data 구성
   - 선택된 모든 버전 ID
   - 법 조항
   - 계산 파라미터
   - 선택 사유

7. by_law_snapshot 저장
```

### 8.2 기간 중복 방지

동일 분류와 동일 조건의 활성 파라미터는 기간이 겹치면 안 된다.

예:

```text
item_code = CORP_TAX
taxpayer_type = GENERAL
effective_from = 2026-01-01
effective_to = 2026-12-31
```

위 조건과 겹치는 활성 세율 구간이 있으면 저장 또는 활성화를 차단한다.

### 8.3 사업연도가 개정일을 걸치는 경우

| 규칙 | 설명 | 예시 |
| --- | --- | --- |
| `BY_END_DATE` | 종료일 현재 시행 법령 적용 | 대부분의 법인세 계산 |
| `BY_START_DATE` | 개시일 현재 법령 적용 | 특정 경과규정 |
| `PRORATE_BY_EFFECTIVE_DATE` | 시행일 전후 기간 또는 금액 안분 | 월할 한도, 일할 이자율 |
| `TRANSACTION_DATE` | 거래일별 시행 법령 적용 | 개별 거래 기반 공제·감면 |

## 9. 업무 흐름

### 9.1 법령 개정 등록

```text
관리자: 신규 개정 등록
  -> 법령 버전 DRAFT 생성
  -> 세율표/한도표/공제율표 입력
  -> 첨부 근거와 변경 요약 입력
  -> 저장
```

필수 입력:

- 버전 코드
- 법령명
- 공포일
- 시행일
- 적용 기준
- 변경 요약
- 영향 모듈

### 9.2 검토와 승인

```text
DRAFT
  -> 데이터 정합성 검증
  -> 전기 버전과 차이 보고서 생성
  -> REVIEWED
  -> 승인자 승인
  -> APPROVED
```

검토 시 자동 검증:

- 필수 파라미터 누락
- 구간 세율의 공백·중복
- 기간 중복
- 음수 금액·음수 세율
- 기준내용연수 범위
- 경과규정 누락
- 회귀 테스트 케이스 실패

### 9.3 운영 반영

```text
APPROVED
  -> 영향 시뮬레이션 실행
  -> 활성화
  -> ACTIVE
  -> 미신고 사업연도 재계산 알림
```

운영 반영 시 자동 처리:

- 해당 기간 기존 활성 버전의 `effective_to` 정리
- 계산 캐시 무효화
- 미신고 사업연도 알림 생성
- 감사 로그 기록

### 9.4 스냅샷 생성

스냅샷은 다음 시점에 생성한다.

- 사업연도 최초 세무조정 계산 실행
- 서식 생성 실행
- 전자신고 파일 생성 실행
- 사용자가 명시적으로 스냅샷 생성 버튼 클릭

이미 스냅샷이 있고 잠금 상태가 아니면 사용자가 재계산할 수 있다. 잠금 상태이면 기존 스냅샷을 그대로 사용한다.

### 9.5 신고 완료 잠금

전자신고 제출 완료 또는 관리자가 신고 완료 처리하면 스냅샷을 잠근다.

잠금 후 제한:

- `snapshot_data` 수정 불가
- 연결된 계산 결과 삭제 불가
- 연결된 서식 버전 변경 불가
- 전자신고 파일 재생성 시 동일 스냅샷 사용

## 10. 화면 상세 설계

### 10.1 법령 버전 마스터

목적: 법령 개정 단위의 버전 목록과 상태를 관리한다.

주요 UI:

- 검색 조건
  - 법령 분류
  - 상태
  - 시행일 범위
  - 버전 코드
- 목록 컬럼
  - 버전 코드
  - 법령명
  - 공포일
  - 시행일
  - 종료일
  - 상태
  - 영향 모듈
  - 승인자
  - 승인일
- 상세 탭
  - 기본정보
  - 변경 요약
  - 첨부 근거
  - 세율표
  - 한도·율표
  - 영향 분석
  - 감사 이력

주요 액션:

- 신규 개정 등록
- 복사해서 새 버전 생성
- 검토 요청
- 승인
- 활성화
- 사용중단
- 차이 비교
- 영향 시뮬레이션

### 10.2 법인세율표

목적: 과세표준 구간별 산출세액을 관리한다.

입력 그리드:

| 컬럼 | 설명 |
| --- | --- |
| 납세자 유형 | 일반, 중소, 비영리 등 |
| 과세표준 From | 구간 하한 |
| 과세표준 To | 구간 상한 |
| 세율 | % 입력, 내부 저장은 bps |
| 기본세액 | 구간별 기본 산출세액 |
| 누진공제 | 누진공제 방식 사용 시 입력 |
| 법 조항 | 근거 조항 |
| 비고 | 관리자 메모 |

검증:

- 구간 하한은 0 이상
- 같은 납세자 유형 내 구간 공백 금지
- 같은 납세자 유형 내 구간 중복 금지
- 마지막 구간은 `taxable_to = NULL` 허용
- 세율은 0 이상 100% 이하

### 10.3 한도·율표

목적: 접대비, 기부금, 대손충당금, 업무용승용차 등 한도 계산 파라미터를 관리한다.

항목별 입력 예:

| 항목 | 주요 파라미터 |
| --- | --- |
| 접대비 기본한도 | 일반 1,200만원, 중소 3,600만원, 월할 여부 |
| 접대비 수입금액 한도율 | 100억 이하, 500억 이하, 초과 구간별 한도율 |
| 기부금 | 특례기부금 한도율, 일반기부금 한도율, 이월기간 |
| 대손충당금 | 기본 설정률, 실적률 우선 여부 |
| 업무용승용차 | 감가상각 한도, 임차료 한도, 처분손실 이월기간 |
| 퇴직급여충당금 | 충당금 한도율, 퇴직연금 손금산입 한도 |
| 가산세 | 무신고, 과소신고, 납부지연, 원천징수 가산세율 |

### 10.4 세액공제·감면 율표

목적: 연구·인력개발비, 통합투자세액공제, 외국납부세액공제, 재해손실세액공제, 중소기업특별세액감면 등 공제·감면 규칙을 관리한다.

상세 입력:

- 공제 코드
- 공제명
- 적용 기업 규모
- 적용 업종
- 공제 대상 금액 산정 방식
- 공제율
- 최소한세 적용 여부
- 농어촌특별세 적용 여부
- 이월 가능 연수
- 법 조항

### 10.5 기준내용연수표

목적: 자산 분류별 감가상각 기준내용연수와 신고내용연수 허용 범위를 관리한다.

입력:

- 자산 대분류
- 자산 중분류
- 업종 코드
- 기준내용연수
- 허용 하한
- 허용 상한
- 상각 방법 제한
- 법 조항

### 10.6 중소기업 판정기준

목적: 사업연도별 중소기업 여부 판정 기준을 관리한다.

입력:

- 업종 코드
- 매출액 기준
- 자산총액 기준
- 독립성 기준
- 제외 업종
- 유예기간 규칙
- 관계기업 포함 여부

### 10.7 결손금 공제규정

목적: 이월결손금 공제기간과 공제한도를 관리한다.

입력:

- 발생 사업연도
- 공제 가능 연수
- 일반법인 공제한도율
- 중소기업 공제한도율
- 회생·구조조정 특례
- 적용 시작일

### 10.8 사업연도별 적용 스냅샷

목적: 특정 고객사·사업연도에 적용된 법령 세트를 조회하고 재계산한다.

표시 정보:

- 고객사
- 사업연도
- 스냅샷 ID
- 잠금 여부
- 법령 버전
- 세율표 버전
- 한도·율표 버전
- 세액공제 버전
- 서식 버전
- 전자신고 마스터
- 생성일시
- 잠금일시

액션:

- 스냅샷 생성
- 미잠금 스냅샷 재계산
- 전기/최신 법령 비교
- 근거 법령 PDF 다운로드
- 계산 결과 재산정

### 10.9 영향 시뮬레이션

목적: 신규 법령 버전 활성화 전 영향 범위를 확인한다.

입력:

- 대상 법령 버전
- 비교 기준 버전
- 대상 사업연도 범위
- 대상 고객사 또는 전체
- 미신고만 포함 여부

출력:

- 영향 사업연도 수
- 영향 고객사 수
- 모듈별 영향 건수
- 세액 차이 합계
- 세액 차이 상위 고객사
- 재계산 필요 목록
- 사용자 공지 대상

### 10.10 개정 공지/이력

목적: 법령 변경 사항을 사용자에게 알리고, 감사 가능하도록 보관한다.

공지 항목:

- 제목
- 변경 요약
- 영향 사업연도
- 영향 모듈
- 재계산 필요 여부
- 공지 대상 역할
- 발송 채널
- 발송일시

## 11. API 상세 설계

### 11.1 법령 버전

| Method | Path | 설명 |
| --- | --- | --- |
| GET | `/admin/laws` | 법령 버전 목록 |
| GET | `/admin/laws/{versionId}` | 법령 버전 상세 |
| GET | `/admin/laws/{lawCode}/timeline` | 법령 코드별 타임라인 |
| POST | `/admin/laws` | 신규 법령 버전 등록 |
| PUT | `/admin/laws/{versionId}` | DRAFT 수정 |
| POST | `/admin/laws/{versionId}/review` | 검토 완료 |
| POST | `/admin/laws/{versionId}/approve` | 승인 |
| POST | `/admin/laws/{versionId}/activate` | 운영 반영 |
| POST | `/admin/laws/{versionId}/retire` | 사용 중단 |

`POST /admin/laws` 요청:

```json
{
  "versionCode": "CIT-2026-01",
  "lawName": "법인세법 2026년 개정",
  "lawCategory": "CORPORATE_TAX",
  "amendmentNo": "법률 제00000호",
  "promulgatedAt": "2026-01-01",
  "effectiveFrom": "2026-01-01",
  "effectiveTo": null,
  "basisDateRule": "BY_END_DATE",
  "changeSummary": "법인세율 및 일부 한도 규정 개정",
  "affectedModules": ["adjustment", "forms", "efiling"],
  "metadata": {
    "sourceUrl": "https://example.local/law",
    "notes": "관리자 입력"
  }
}
```

응답:

```json
{
  "lawVersionId": 1201,
  "versionCode": "CIT-2026-01",
  "status": "DRAFT"
}
```

### 11.2 세율표

| Method | Path | 설명 |
| --- | --- | --- |
| GET | `/admin/tax-rates?versionId=1201` | 세율표 조회 |
| PUT | `/admin/tax-rates/{versionId}` | 세율 구간 일괄 저장 |
| POST | `/admin/tax-rates/{versionId}/validate` | 구간 검증 |
| GET | `/admin/tax-rates/{versionId}/diff?baseVersionId=...` | 버전 차이 |

`PUT /admin/tax-rates/{versionId}` 요청:

```json
{
  "rates": [
    {
      "itemCode": "CORP_TAX",
      "taxpayerType": "GENERAL",
      "taxableFrom": 0,
      "taxableTo": 200000000,
      "baseTax": 0,
      "rateBps": 900,
      "progressiveDeduction": 0,
      "effectiveFrom": "2026-01-01",
      "effectiveTo": null,
      "legalBasis": "법인세법 제55조"
    }
  ]
}
```

### 11.3 한도·율표

| Method | Path | 설명 |
| --- | --- | --- |
| GET | `/admin/tax-limits?versionId=1201&limitGroup=ENTERTAINMENT` | 한도·율표 조회 |
| PUT | `/admin/tax-limits/{versionId}` | 한도·율표 일괄 저장 |
| POST | `/admin/tax-limits/{versionId}/validate` | 산식·조건 검증 |
| GET | `/admin/tax-limits/{versionId}/diff?baseVersionId=...` | 버전 차이 |

### 11.4 적용 버전 조회

| Method | Path | 설명 |
| --- | --- | --- |
| GET | `/laws/applicable?byId=42&code=CORP_TAX` | 사업연도·항목별 적용 버전 |
| GET | `/business-years/{byId}/law-snapshot` | 적용 스냅샷 조회 |
| POST | `/business-years/{byId}/law-snapshot` | 스냅샷 생성 |
| POST | `/business-years/{byId}/law-snapshot/rebuild` | 미잠금 스냅샷 재계산 |
| POST | `/business-years/{byId}/law-snapshot/lock` | 스냅샷 잠금 |

`GET /business-years/{byId}/law-snapshot` 응답:

```json
{
  "snapshotId": 2517,
  "byId": 42,
  "locked": true,
  "law": {
    "lawVersionId": 1201,
    "versionCode": "CIT-2026-01",
    "lawName": "법인세법 2026년 개정"
  },
  "rates": [
    {
      "itemCode": "CORP_TAX",
      "taxRateId": 9001,
      "displayName": "법인세율 2026-01"
    }
  ],
  "limits": [
    {
      "itemCode": "ENT_BASE_LIMIT_SME",
      "taxLimitId": 8001,
      "amount": 36000000
    }
  ],
  "forms": [
    {
      "formCode": "BJ_03",
      "versionNo": "2026-01"
    }
  ],
  "efileMasters": [
    {
      "masterCode": "CIT_EFILE_2026",
      "versionNo": "2026-01"
    }
  ]
}
```

### 11.5 영향 시뮬레이션

| Method | Path | 설명 |
| --- | --- | --- |
| POST | `/admin/laws/{versionId}/simulate` | 영향 시뮬레이션 실행 |
| GET | `/admin/laws/{versionId}/impact` | 영향 사업연도 목록 |
| GET | `/admin/law-impact-runs/{runId}` | 실행 결과 상세 |
| POST | `/admin/laws/{versionId}/notify` | 사용자 공지 발송 |

`POST /admin/laws/{versionId}/simulate` 요청:

```json
{
  "baseVersionId": 1100,
  "targetBusinessYearFrom": 2026,
  "targetBusinessYearTo": 2026,
  "includeFiled": false,
  "tenantCodes": ["demo"]
}
```

응답:

```json
{
  "runId": "impact-20260515-001",
  "summary": {
    "businessYears": 18,
    "customers": 16,
    "affectedAdjustments": 42,
    "totalTaxDelta": 12500000
  },
  "moduleImpacts": [
    {
      "module": "adjustment",
      "itemCode": "ENTERTAINMENT",
      "count": 12,
      "taxDelta": 3200000
    }
  ]
}
```

## 12. 계산 엔진 연동

### 12.1 공통 인터페이스

계산 모듈은 직접 법령 테이블을 조회하지 않고 다음 서비스를 사용한다.

```text
LawVersionService
├── getSnapshot(byId)
├── ensureSnapshot(byId)
├── getApplicableRate(snapshotId, itemCode, taxableIncome, context)
├── getApplicableLimit(snapshotId, itemCode, context)
├── getCreditRule(snapshotId, creditCode, context)
└── getLegalBasis(snapshotId, itemCode)
```

### 12.2 계산 결과 저장

`tax_adjustments`와 `form_data`는 `snapshot_id`를 저장한다.

계산 결과의 `metadata` 또는 `calc_detail_json`에는 다음을 포함한다.

```json
{
  "snapshotId": 2517,
  "lawVersionId": 1201,
  "usedParameters": [
    {
      "type": "tax_limit",
      "id": 8001,
      "itemCode": "ENT_BASE_LIMIT_SME",
      "value": 36000000,
      "legalBasis": "법인세법 시행령 제42조"
    }
  ],
  "formula": "min(expense, base_limit + revenue_limit)",
  "calculatedAt": "2026-05-15T10:00:00+09:00"
}
```

## 13. 권한 설계

| 권한 코드 | 설명 |
| --- | --- |
| `LAW_VERSION_READ` | 법령 버전 조회 |
| `LAW_VERSION_WRITE` | DRAFT 등록·수정 |
| `LAW_VERSION_REVIEW` | 검토 완료·반려 |
| `LAW_VERSION_APPROVE` | 승인 |
| `LAW_VERSION_ACTIVATE` | 운영 반영·사용중단 |
| `LAW_RATE_WRITE` | 세율표 저장 |
| `LAW_LIMIT_WRITE` | 한도·율표 저장 |
| `LAW_SNAPSHOT_REBUILD` | 미잠금 스냅샷 재계산 |
| `LAW_SNAPSHOT_LOCK` | 스냅샷 잠금 |
| `LAW_IMPACT_RUN` | 영향 시뮬레이션 실행 |
| `LAW_NOTIFY` | 법령 변경 공지 발송 |

승인과 활성화는 동일 사용자가 연속 수행할 수 없도록 분리한다.

## 14. 감사 로그

감사 대상:

- 법령 버전 등록·수정·삭제 시도
- 세율표 변경
- 한도·율표 변경
- 검토, 승인, 활성화, 사용중단
- 스냅샷 생성, 재계산, 잠금
- 영향 시뮬레이션 실행
- 공지 발송

감사 로그 필드:

- 이벤트 ID
- 이벤트 유형
- 대상 테이블
- 대상 ID
- 변경 전 JSON
- 변경 후 JSON
- 사용자 ID
- 사용자 IP
- 발생 일시
- 승인 워크플로 ID

## 15. 검증 규칙

### 15.1 공통 검증

- `effective_from`은 필수다.
- `effective_to`가 있으면 `effective_from <= effective_to`이어야 한다.
- 동일 조건의 활성 기간은 중복될 수 없다.
- DRAFT 외 상태에서는 파라미터를 직접 수정할 수 없다.
- 승인 전 영향 시뮬레이션 결과가 있어야 한다.
- 승인 전 회귀 테스트 케이스가 통과해야 한다.

### 15.2 세율표 검증

- 과세표준 구간은 0부터 시작한다.
- 구간은 오름차순이며 공백이 없다.
- 구간은 서로 겹치지 않는다.
- 마지막 구간은 상한이 없을 수 있다.
- `rate_bps`는 0 이상이다.
- `base_tax`와 `progressive_deduction`은 0 이상이다.

### 15.3 한도·율표 검증

- 금액 한도와 한도율 중 적어도 하나는 있어야 한다.
- `condition_json`은 허용된 스키마를 따라야 한다.
- `formula_json`에서 참조하는 입력값은 계산 엔진 컨텍스트에 존재해야 한다.
- 월할·일할 계산 여부가 명시되어야 한다.

### 15.4 스냅샷 검증

- 신고 완료 사업연도의 잠금 스냅샷은 재계산할 수 없다.
- 스냅샷 내 모든 참조 ID는 존재해야 한다.
- 스냅샷 생성 시점의 파라미터 전체가 `snapshot_data`에 복제되어야 한다.
- 스냅샷과 계산 결과의 `snapshot_id`가 일치해야 한다.

## 16. 배치와 알림

### 16.1 법령 변경 알림 배치

실행 조건:

- 법령 버전이 `ACTIVE`가 됨
- 미신고 사업연도 중 새 버전 영향 대상이 존재함

처리:

1. 영향 대상 사업연도 조회
2. 담당 사용자 조회
3. 알림 생성
4. 화면 배너 표시
5. 메일 또는 외부 알림 발송

### 16.2 회귀 테스트 배치

각 법령 버전별 표준 케이스를 저장하고, 빌드 또는 활성화 전 자동 실행한다.

테스트 케이스 예:

- 법인세율 구간 경계값
- 접대비 한도 월할 계산
- 기부금 한도와 이월
- 이월결손금 공제한도
- 가산세 납부지연 일수 계산

## 17. 오류 코드

| 코드 | 메시지 | 설명 |
| --- | --- | --- |
| `LAW_001` | 적용 가능한 법령 버전이 없습니다. | 사업연도에 맞는 승인 버전 없음 |
| `LAW_002` | 법령 버전 기간이 중복됩니다. | 같은 조건의 활성 기간 중복 |
| `LAW_003` | DRAFT 상태에서만 수정할 수 있습니다. | 상태 위반 |
| `LAW_004` | 스냅샷이 잠겨 있어 재계산할 수 없습니다. | 신고 완료 또는 잠금 |
| `LAW_005` | 세율 구간이 연속되지 않습니다. | 구간 공백 |
| `LAW_006` | 영향 시뮬레이션이 필요합니다. | 활성화 전 필수 절차 누락 |
| `LAW_007` | 승인 권한이 없습니다. | 권한 부족 |
| `LAW_008` | 계산 파라미터가 누락되었습니다. | 필수 한도·율 없음 |

## 18. 구현 단계

### 18.1 1단계

- 법령 버전 마스터 확장
- 세율표 관리 API
- 한도·율표 관리 API
- 사업연도 스냅샷 생성·조회
- 계산 엔진의 `snapshot_id` 저장

### 18.2 2단계

- 승인 워크플로
- 기간 중복 검증
- 영향 시뮬레이션
- 미신고 사업연도 재계산
- 법령 변경 배너

### 18.3 3단계

- 세액공제·감면 율표
- 기준내용연수표
- 중소기업 판정기준
- 결손금 공제규정
- 표준 회귀 테스트 케이스 관리

### 18.4 4단계

- 서식 버전 관리와 통합
- 전자신고 마스터 버전 통합
- 근거 법령 PDF 출력
- 상세 감사 리포트

## 19. 수용 기준

- 법령 버전 9개 이상 항목군이 메뉴에서 조회된다.
- 법령 버전은 `DRAFT -> REVIEWED -> APPROVED -> ACTIVE` 흐름으로 전환된다.
- 같은 항목·조건·기간의 활성 세율 또는 한도가 중복 저장되지 않는다.
- 사업연도별 스냅샷이 생성되고 계산 결과가 해당 `snapshot_id`를 참조한다.
- 잠금 스냅샷은 법령 개정 후에도 계산 결과가 바뀌지 않는다.
- 새 법령 활성화 전 영향 시뮬레이션 결과를 확인할 수 있다.
- 미신고 사업연도에는 법령 변경 알림과 재계산 액션이 제공된다.
- 세율 구간 경계값, 접대비 한도, 기부금 한도, 결손금 공제한도에 대한 회귀 테스트가 통과한다.

## 20. 현재 구현과의 연결

현재 프로젝트에는 다음 기본 구현이 존재한다.

- `tax_law_versions`: 법령 버전 마스터
- `tax_rates`: 법인세율표
- `tax_limits`: 한도표
- `by_law_snapshot`: 사업연도별 적용 스냅샷
- `law_amendment_history`: 개정 이력
- `GET /api/tax-laws`
- `POST /api/tax-laws`
- `POST /api/tax-rates`
- `GET/POST /api/tenants/{tenant_code}/business-years/{by_id}/snapshot`

상세 설계 기준으로 확장해야 할 핵심은 다음과 같다.

- 상태 워크플로를 `DRAFT`, `REVIEWED`, `APPROVED`, `ACTIVE`, `RETIRED`로 세분화
- 세율표와 한도표의 일괄 저장 API 추가
- 기간 중복 검증 추가
- 스냅샷 잠금·재계산 API 추가
- 영향 시뮬레이션과 공지 기능 추가
- 세액공제, 기준내용연수, 중소기업 판정, 결손금 규칙 테이블 추가
- 계산 결과별 사용 파라미터 추적 JSON 추가

## Multi-Tenant 테넌트-고객사 관계 보완 (2026-05-15)

- 대상 사용자 조직은 `회계법인`, `세무법인`, `세무사사무소`이며, 시스템에서는 각각 독립 테넌트로 관리한다.
- 관계 모델은 `테넌트 1 : N 고객사`이다. 하나의 테넌트는 여러 고객사를 관리하고, 각 고객사는 반드시 하나의 테넌트에만 소속된다.
- UI 기준으로 `prototype/index.html`의 `5-0 테넌트 관리` 화면에서 테넌트 유형, 계약, 상태, 사용자 수, 고객사 수, 소속 고객사 샘플을 확인한다.
- `5-A 고객사 관리` 화면은 고객사 목록에 소속 테넌트 컬럼과 테넌트 필터를 제공하고, 신규 등록/편집 시 소속 테넌트를 선택한다.
- 사용자 권한은 테넌트 범위 안에서 적용되며, 특정 고객사 접근은 `5-E 담당 법인 권한`에서 사용자-고객사 단위로 추가 제한한다.
- DB/도메인 설계 시 고객사 테이블은 `tenant_id` 또는 `tenant_code` 외래키를 가져야 하며, 고객사 코드는 `tenant_id + customer_code` 범위에서 유일해야 한다.
## 사용자-테넌트-고객사 관리 보완 (2026-05-15)

- 사용자는 반드시 하나의 소속 테넌트(`tenant_id`)를 가진다. 테넌트는 회계법인, 세무법인, 세무사사무소 단위 조직이다.
- 사용자의 고객사 접근 범위는 `user_customer_access` 또는 동등한 매핑으로 관리하며, 한 사용자는 소속 테넌트 안의 여러 고객사에 접근할 수 있다.
- `prototype/index.html`의 `5-B 사용자 관리` 화면은 테넌트 필터, 고객사 필터, 소속 테넌트 컬럼, 고객사 권한 요약을 제공한다.
- 사용자 등록/편집 시 소속 테넌트를 먼저 선택하고, 선택한 테넌트에 속한 고객사만 접근 범위로 선택할 수 있다.
- 역할별 기능 권한은 `5-C 역할 / 권한 매트릭스`에서 관리하고, 특정 고객사 담당 등급 또는 차단 같은 예외는 `5-E 담당 법인 권한`에서 관리한다.
- 권한 판정 순서는 `테넌트 소속 확인 -> 고객사 접근 범위 확인 -> 역할/기능 권한 확인 -> 고객사별 예외 권한 확인`을 기본 원칙으로 한다.
## 고객사별 대상 업무 범위 보완 (2026-05-15)

- 사용자 권한은 `tenant_id + customer_id + work_scope` 조합으로 판정한다. 같은 테넌트와 고객사라도 사용자마다 허용 업무가 다를 수 있다.
- 대상 업무 코드는 `INFO`, `ADJUST`, `FORM`, `VALIDATE`, `APPROVE`, `PRINT`, `EFILE`, `POST`를 기본값으로 사용한다.
- 권한 저장은 `user_customer_work_scope` 테이블을 별도로 두거나, `user_customer_access`에 `work_scopes` JSON/배열 컬럼을 추가하는 방식으로 구현한다.
- `prototype/index.html`의 `5-B 사용자 관리` 화면은 대상 업무 필터와 고객사별 대상 업무 체크리스트를 제공한다.
- 사용자 등록/편집 시 소속 테넌트를 선택한 뒤, 해당 테넌트의 고객사를 선택하고, 선택된 고객사마다 허용 업무 범위를 별도로 지정한다.
- 업무 실행 전 권한 판정 순서는 `테넌트 소속 확인 -> 고객사 접근 확인 -> 고객사별 대상 업무 확인 -> 역할/기능 권한 확인 -> 예외 권한 확인`을 따른다.
- 예: 같은 `EY 회계법인 / ㈜OOO 제조` 고객사라도 A 사용자는 `ADJUST`, `FORM`, `VALIDATE`만 가능하고, B 사용자는 `APPROVE`, `PRINT`만 가능하게 분리할 수 있다.
## 테넌트별 고객사 대상 업무 구분 보완 (2026-05-15)

- 법령세율 버전 관리 화면/업무는 고객사 대상 업무와 사용자별 업무 권한을 분리해 판단한다.
- 고객사 대상 업무는 테넌트별 고객사 마스터의 `work_scopes`에 저장하고, 사용자 부여 업무는 `user_customer_work_scope`에 저장한다.
- 사용자 부여 업무는 고객사 대상 업무의 부분집합이어야 하므로, 고객사에 없는 업무 코드로 법령세율 버전 작업 권한을 줄 수 없다.

## Phase 7 세무정보 입력 연계 (2026-05-15)

- 사업연도 생성 시 확정된 법령·서식 스냅샷 위에 재무제표, 자산대장, 거래 명세를 적재한다.
- 세무정보 입력 결과는 후속 세무조정 엔진이 해당 사업연도의 법령·세율 버전을 적용할 때 사용하는 기준 데이터다.
- 업로드 배치와 검증 오류는 사업연도별로 추적되어 스냅샷 기준 데이터의 품질 상태를 확인할 수 있다.

## Phase 8 세무조정 엔진 연계 (2026-05-15)

- B-1 소득금액조정은 `by_law_snapshot.snapshot_id`를 계산 결과와 조정 항목에 부착한다.
- 스냅샷의 법령 버전 정보는 세무조정 화면의 적용 법령 배너로 표시된다.
- 이후 법령 영향 시뮬레이션은 잠금 전 사업연도의 B-1 결과를 재계산 대상으로 식별할 수 있다.

## Phase 9 자산 기반 세무조정 연계 (2026-05-15)

- 자산 기반 세무조정은 사업연도 스냅샷의 법령 버전과 기준내용연수 한도 데이터를 사용한다.
- B-4/B-10 계산 결과는 법령 개정 영향 분석 시 재계산 대상이 된다.
- 잠금된 사업연도는 기존 스냅샷 기준 계산 결과를 보존한다.
