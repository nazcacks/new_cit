# 추가 구현 계획 v1.2
## 메뉴 클릭 시 “해당 화면이 나오지 않는” 문제 수정 계획

> 작성일: 2026-05-18 · 작성자: 강수 (Kang-Soo.Cho@kr.ey.com)
> 선행 문서: `2차구현계획.md`(IA 채택) · `메뉴기능수정.md`(시드/점검) · `추가구현_v1.0.md`(기능 갭) · `추가구현_v1.1.md`(leaf 단위 분리)
> 본 문서의 범위: leaf 메뉴를 클릭해도 본문이 “직전 화면 그대로 보이거나 비어 보이는” 현상을 해소하는 데 필요한 **즉시 수정 항목 + 단계적 화면 분리** 계획.

---

## v1.5 동기화 메모

v1.2의 “본문이 동일하거나 비어 보이는” 문제는 현재 100개 active leaf에 대해 v1.5 공통 workbench로 처리한다. 각 leaf의 본문 최상위에는 `data-leaf-key`가 있고, 상단 KPI/제목, 검색/필터, 목록 표, 행 액션, `+ 추가` 5블록이 항상 렌더링된다. workContext가 없으면 workspace leaf는 `empty-state` 작업 시작 안내로 분기한다.

## 1. 증상 (사용자 보고)

> “각 메뉴를 선택했을 때 메뉴에 해당하는 화면이 나오지 않고 있어”

세부 케이스(추정):
- A. 좌측 메뉴 클릭 시 URL은 `#/workspace/ws/adj/B12` 등으로 바뀌지만, 본문은 직전과 같은 ‘세무조정 통합 화면’이 계속 보인다.
- B. B-1, B-3, B-12 등 형제 leaf를 순차 클릭해도 본문이 동일하다(상단 “Leaf” 배지의 라벨만 바뀜).
- C. 일부 메뉴는 클릭해도 좌측 사이드바 active 표시조차 안 되거나, 헤더 타이틀만 바뀌고 본문은 “데이터 없음”.
- D. (백엔드 빌드 실패 시) 좌측 메뉴 자체가 비어 있거나, “준비 중” 안내만 표시.

---

## 2. 코드 추적으로 확인된 원인

### 2.1 백엔드: `src/modules.rs`가 **컴파일 불능 상태**로 잘려 있다 (★ P0)

- 파일 크기 15,576바이트, 595행에서 미완성 문자열로 끝남: `… "Amen` (string literal 미닫힘).
- `prototype_menu_defs()` 내부에서 호출되는 다음 헬퍼·정의가 **현 파일에 존재하지 않음**:
  - 헬퍼: `children_for`, `leaf`, `group`, `legacy_node`, `korean_label`, `work_context`
  - 정의 함수: `report_defs`, `admin_customer_defs`, `admin_security_defs`, `admin_customer_access_defs`, `admin_law_defs`, `admin_form_defs`, `admin_code_defs`, `admin_audit_defs`
- 결과: `cargo build` 실패 → 운영 중인 바이너리는 직전 빌드(prototype IA 이전) 또는 fallback. 프론트로 내려오는 메뉴 트리가 신규 leaf 구조와 일치하지 않을 가능성이 높다.

**확인 SQL/CLI 한 줄**:
```bash
cargo build --release   # 현재 상태에서 fail이어야 가설 일치
curl -s $BASE/api/auth/me | jq '.modules.children[].code'  # 트리가 새 키 체계인지 확인
```

### 2.2 프론트엔드: leaf 99개가 모두 **24개 legacy 화면 중 하나로 위임** (★ P0)

`frontend/app/screens.js:264 renderScreen()`의 구조:
```js
const meta = routes[env.key] || routes.dashboard;     // leaf의 delegate 조회
const screen = screenByDelegate[meta.delegate] || renderDashboard;
await screen({ ...env, key: meta.delegate, routeKey: env.key, ... });
if (meta.s1) prependLeafFocus(env.outlet, env.key, displayMeta, env.locale);
```
- `routes`(=`{...legacyRoutes, ...leafRoutes}`)에서 leaf `ws/adj:B12`의 `delegate`는 `"ws-adj"`.
- `screenByDelegate["ws-adj"]` = `renderAdjustments`(한 화면에 17개 카드 + “실행” 버튼).
- 즉 **B1~B17 leaf 17개가 같은 `renderAdjustments`를 호출**한다. 화면이 동일해 보이는 이유.
- 다른 그룹도 동일: `ws/info:*` 9개 → `renderWorkInfo` 1개, `admin/law:*` 10개 → `renderAdminLaw` 1개, `admin/form:*` 10개 → `renderAdminForms` 1개 등.

이는 `추가구현_v1.1.md`에서 의도한 **S1 단계(메뉴 트리만 펼치고 본문은 기존 통합 화면 재사용)** 그 자체이지만, 사용자 입장에서는 “메뉴를 눌러도 같은 화면”이라 **불량**으로 인지된다.

### 2.3 부수 문제

- C. 헤더 타이틀만 갱신: `renderRoute`가 `routeGroup/routeTitle`을 갱신한 뒤 `renderScreen`이 통합 화면을 그리므로, 사용자 입장에서는 “타이틀만 바뀌고 본문 그대로”.
- D. `requireWorkContext` 가드: workContext가 없으면 `ws/*` 모든 leaf가 “작업 시작 안내” 화면으로 빠진다 — 정상 동작이지만 사용자에게는 “화면이 안 나오는 것”으로 보일 수 있다.
- E. `keyToHash` ↔ `parseHash`는 일관되어 라우팅 자체에는 결함이 없음(검토 완료).
- F. `routes` 단일 dispatch에 의해 새 leaf에 대해 **단독 화면 추가 시점이 모듈 단위로 묶여 있어 점진 도입이 어렵다**.

---

## 3. 수정 전략 (Two-Track)

### Track A. 즉시 “보이는 차이” 만들기 (1.5일)

> 사용자 신뢰 회복 우선. 큰 화면을 leaf 단위로 ‘부분 강조 + 행/탭 자동 선택’으로 갈라 보여준다. 백엔드 컴파일도 같이 푼다.

| # | 작업 | 효과 |
|---|---|---|
| A1 | **`src/modules.rs` 잘림 복구** — 미정의 헬퍼/정의 함수(섹션 2.1 참조) 전부 구현, 빌드 통과 | 백엔드 메뉴 트리가 정상 전송됨 |
| A2 | **delegate 위임에 “leaf 컨텍스트” 전달** — `renderScreen` 호출 시 `{ leafKey, leafSuffix, leafTitle }`를 env에 포함 | 통합 화면이 leaf에 맞게 자기 자신을 좁혀 그릴 수 있음 |
| A3 | **통합 화면이 leaf에 맞게 자동 선택**: `renderAdjustments`가 `env.leafKey`(예: `ws/adj:B12`)를 읽어 그 모듈만 보이도록 필터, “17개 카드”는 좌측 서브내비로 이동. `renderWorkInfo`도 1-1~1-9 탭을 leaf 키로 자동 선택. `renderAdminLaw`, `renderAdminForms`도 10탭 자동 선택 | 클릭마다 본문이 실제로 바뀐다 |
| A4 | **헤더에 “leaf focus” 표시 강화** — 현재 prepend로 1행만 표시되는 leaf 배지를 카드 상단 워터마크 + 좌측 서브내비의 active hi-light로 강화 | 사용자가 “어디 와 있는지” 즉시 인지 |
| A5 | **빈 화면 가드 정상 안내** — workContext 미설정으로 표시되는 “작업 시작” 안내 카드의 문구를 명확화. 클릭 가능한 “고객사·연도 선택하기” 버튼이 항상 보이도록 보장 | 빈 화면이 ‘오류 화면’으로 오인되지 않음 |

#### A1 상세 — `src/modules.rs` 잘림 복구

복구 시 추가/완성할 항목:
- `fn group(key, parent, label, path, icon, layout, sort_order) -> MenuDef`
- `fn leaf(key, parent, label, icon, layout, requires_context, perm_module, perm_function, sort_order) -> MenuDef`
- `fn children_for(defs: &[MenuDef], parent: Option<&str>) -> Vec<Value>` (계층 JSON 직렬화)
- `fn node_value(def: &MenuDef, defs: &[MenuDef]) -> Value` (단일 노드 JSON, `code`/`key` 두 키를 모두 채워 frontend 호환)
- `fn legacy_node(code, number, name, path) -> Value`
- `fn korean_label(key, fallback) -> String` (i18n 영문 fallback의 한글 매핑)
- `fn work_context() -> Vec<String>` (`vec!["customer_id".into(), "business_year_id".into()]`)
- `report_defs`, `admin_customer_defs`, `admin_security_defs`, `admin_customer_access_defs`, `admin_law_defs`, `admin_form_defs`, `admin_code_defs`, `admin_audit_defs` 미작성분 8개
- 잘린 `post_defs`의 나머지 항목(`post/amend:diff`, `post/amend:resubmit`, `post/correction`) + 함수 닫기

복구 기준:
- `cargo build --release` 0 warning.
- `cargo test --test menu_routing` 통과(`tests/menu_routing.rs` 기대 키 명세).
- `GET /api/modules/tree`가 99개 leaf를 반환.
- 응답 노드는 `code`, `key`, `label`, `path`, `parent_key`, `layout`, `requires_context`, `required_perm_module`, `required_perm_function`, `feature_flag`, `sort_order`, `children` 12개 필드를 채운다.

#### A3 상세 — 통합 화면 “leaf 자동 선택” 패치

`frontend/app/screens.js`의 각 통합 렌더러를 다음 패턴으로 수정한다.

```js
async function renderAdjustments(env) {
  if (!requireWorkContext(env)) return;
  const focus = leafFocus(env.routeKey); // "ws/adj:B12" → "B12"
  // 1. 좌측 서브내비: 형제 17 leaf 목록 (active=focus)
  // 2. 본문: focus 모듈만 표시 (한도 카드 + 그리드 + 액션)
  // 3. 헤더: 워터마크 "2-12 세액공제·감면"
  ...
}
```

다음 6개 통합 렌더러를 동일 패턴으로 패치한다:
- `renderWorkInfo` (9 leaf)
- `renderAdjustments` (17 leaf)
- `renderForms` (4 leaf)
- `renderValidation` (3 leaf)
- `renderApproval` (3 leaf)
- `renderPrint` (3 leaf)
- `renderEfiling` (4 leaf)
- `renderPostHistory`, `renderPostAmend` (7 leaf)
- `renderAlerts`, `renderYearCompare`, `renderTaxBurden`, `renderReserveTrend` (6 leaf)
- `renderAdminTenants`, `renderAdminCustomers`, `renderAdminUsers`, `renderAdminRoles`, `renderAdminMenus`, `renderAdminCustomerAccess`, `renderAdminLaw`, `renderAdminForms`, `renderAdminAudit` (40+ leaf)

각 렌더러는 `env.routeKey`(또는 `env.leafKey`)를 읽어 다음 3가지를 수행:
1. **좌측 서브내비**가 형제 leaf를 보여주고 활성 leaf를 강조.
2. **본문**이 활성 leaf에 해당하는 영역만 그린다(나머지는 collapse).
3. **상단 워터마크**가 활성 leaf 라벨을 큰 글씨로 표시.

작업량: 통합 렌더러 약 25개 × 평균 10분 ≈ 0.5일.

---

### Track B. leaf별 단독 화면으로 점진 분리 (4주, `추가구현_v1.1.md` Pack B/C/D와 일치)

> 즉시 효과(Track A) 이후, leaf별 단독 모듈로 화면을 분리해 단일 책임을 달성한다.

#### B1. 스크린 디스패치를 “delegate” → “leaf 키” 우선으로 변경

`frontend/app/screens.js`를 다음 구조로 재편:
```js
const screenByLeaf = {
  "ws/adj:B1": renderAdjB1,    // (점진 추가)
  "ws/adj:B12": renderAdjB12,
  ...
};

export async function renderScreen(env) {
  const meta = routes[env.key] || routes.dashboard;
  layoutChrome(env, meta);                       // Track A의 layout 그리기
  const leafRender = screenByLeaf[env.key];
  if (leafRender) {
    await leafRender(env);                       // 단독 leaf 화면
  } else {
    const fallback = screenByDelegate[meta.delegate] || renderDashboard;
    await fallback({ ...env, leafKey: env.key }); // 통합 화면 (Track A 패치본)
  }
}
```

#### B2. 화면 모듈 분리 (디렉토리)

`frontend/app/screens.js` 를 `frontend/app/screens/<group>/<leafKey>.js` 99개로 점진 분할(`추가구현_v1.1.md` Pack B/C/D).

우선 분리 대상(P1):
- 모든 `ws/adj:B*` 17 leaf — 모듈별 한도·계산·법조항 툴팁이 모두 다르므로 단독 화면이 가장 큰 가치.
- `admin/law:impact` (영향 시뮬레이션), `admin/law:history` — 표시 데이터 형태가 다른 leaf와 완전히 다름.
- `admin/form:fields`, `admin/form:linkage-rule`, `admin/form:field-ref`, `admin/form:migration` — 와이어프레임 G-2/G-5/G-6/G-3 직접 매핑.
- `ws/file:precheck/generate/submit/done` 4 leaf — 4-step Wizard를 각 단계 단독 화면으로 분리.

#### B3. 백엔드 신설 라우트와 짝짓기

`추가구현_v1.0.md` Pack 2~6에서 신설하기로 한 API를 leaf별 단독 화면 도입 시점에 맞춰 함께 활성화:
- `admin/form:fields` ↔ `GET/PUT /api/form-versioning/versions/:id/fields`
- `admin/law:impact` ↔ `POST /api/law-versioning/impact`(현 라우트 결과 스키마 보강)
- `report:loss-expiry`, `report:industry-stats`, `report:custom` ↔ 신설 보고서 API
- `post/amend:resubmit`, `post/correction` ↔ 신설 재제출·경정청구 API

---

## 4. 변경 파일 매트릭스

| 파일 | 변경 |
|---|---|
| `src/modules.rs` | 잘림 복구 + 12 필드 노드 직렬화 + 누락 helper/defs 모두 작성 |
| `tests/menu_routing.rs` | 99 leaf 키·필드 명세 검사 |
| `frontend/app/screens.js` | (Track A) `screenByDelegate` 호출부에 `leafKey` 전달, 25개 통합 렌더러에 leaf 자동 선택 도입. (Track B) `screenByLeaf` 우선 디스패치 |
| `frontend/app/components/subnav.js` (신규) | 형제 leaf 목록 + 활성 강조 |
| `frontend/app/components/leaf-watermark.js` (신규) | 상단 워터마크 |
| `frontend/app/menu.js` | `requires_context` 가드를 진입 시 `requireWorkContext`로 위임하도록 통일 |
| `frontend/app.css` | leaf 워터마크, 형제 서브내비 스타일 |
| `frontend/app/screens/<group>/<leaf>.js` × 점진 99 | leaf별 단독 화면 |
| `docs/menu_smoke_checklist.md` | 99 leaf “본문 차이” 체크리스트 갱신 |
| `tests/menu_smoke.rs` | leaf별 응답 + 본문 시그너처(예: `data-leaf-key`) 확인 |

---

## 5. 검증 절차

### 5.1 백엔드 검증
1. `cargo build --release` — 0 warning 통과.
2. `cargo test --test menu_routing` — 99 leaf 키·필드 검증 green.
3. `curl -s "$BASE/api/modules/tree" | jq '[.children[]|.. | objects | select(.code) | .code]'`이 99 키를 반환.

### 5.2 프론트엔드 수동 검증 (Track A 직후)
- 99 leaf를 순회하면서 다음 3가지가 leaf마다 다른지 확인:
  - 좌측 서브내비의 active leaf
  - 본문 상단 워터마크 라벨
  - 본문에 표시되는 카드/표(필터 기준)
- 동일한 통합 화면 위에서도 leaf 전환마다 본문이 “보이는 차이”를 만들어야 합격.

### 5.3 e2e (Track A 종료 시)
- Playwright 또는 수동 스크립트: 24개 핵심 leaf를 순차 클릭하고 `screenshot diff` 또는 `outerText` 다이프로 ‘본문 변경 ≠ 0’ 검증.

### 5.4 e2e (Track B 점진 종료 시)
- `screenByLeaf`에 등록된 leaf는 `data-leaf-key="ws/adj:B12"` 같은 마커가 본문 최상위 요소에 있는지 검사.
- 등록되지 않은 leaf는 Track A의 통합 화면이 자동 선택되어 있는지 검사.

---

## 6. 단계별 일정

| 일자 | 작업 |
|---|---|
| D1 오전 | `src/modules.rs` 복구(A1), `cargo build` green |
| D1 오후 | `tests/menu_routing.rs` + 응답 트리 검증, frontend `data-leaf-key` 전달(A2) |
| D2 오전 | 통합 렌더러 25개에 leaf 자동 선택 도입(A3) — `ws/adj`, `ws/info`, `admin/law`, `admin/form` 4그룹 |
| D2 오후 | 나머지 통합 렌더러 + 워터마크 + 서브내비(A4), `requireWorkContext` 메시지(A5) |
| D3 | 수동 검증 + 스모크 자동화 |
| W2~W5 | Track B (leaf별 단독 화면 분리, 추가구현_v1.1.md Pack B/C/D와 통합) |

---

## 7. 위험과 대응

| 위험 | 대응 |
|---|---|
| `src/modules.rs` 복구 시 helper 시그니처가 다른 호출처와 어긋남 | 잘림 직전 `prototype_menu_defs()`에서 사용된 시그니처를 진리원으로 두고 helper를 그에 맞춰 작성. `cargo check`로 즉시 확인 |
| leaf 자동 선택 도입 후 일부 화면에서 ‘활성 leaf 본문이 비어 보임’ | 빈 본문이면 placeholder + “전체 보기” 토글 제공해 종전 통합 보기로 폴백 |
| Track A로 “보이는 차이”만 만들고 실제 기능은 그대로라는 오인 | 본문 워터마크에 “v1.2 임시(통합 화면 기반)” 라벨, 단독 화면 분리 진행률 표 첨부 |
| 백엔드 응답이 `code`만 보내고 `key`를 안 보내 frontend의 `menuTreeContains`가 실패 | A1에서 `code/key` 둘 다 채우도록 강제. `menu.js`도 `node.code || node.key` 둘 다 인정 |
| 테스트 부재로 회귀 발견 지연 | A1 종료 직후 `tests/menu_routing.rs`·`tests/menu_smoke.rs` 회귀를 CI 게이트로 등록 |

---

## 8. 완료 정의 (DoD)

1. `cargo build --release` 통과 + `GET /api/modules/tree`가 99 leaf를 반환.
2. 프론트 좌측 메뉴에서 임의 leaf 두 개를 순차 클릭했을 때 본문의 보이는 차이(워터마크 라벨, 카드, 표 데이터)가 **항상 존재**한다.
3. `tests/menu_routing.rs`·`tests/menu_smoke.rs`가 99 leaf 모두 green.
4. workContext 미설정 상태에서 `ws/*` leaf 진입 시 “작업 시작” 안내가 명확하게 보이고, 1클릭으로 작업 시작에 도달한다.
5. Track B 점진 분리 시 단독 화면 도입된 leaf는 `data-leaf-key`가 본문 최상위에 출력되어 회귀 검증이 자동으로 통과한다.
